# Local certificate placeholders

This folder is kept in the repository as a stable location for local or manual certificate fixtures.

Tracked PEM files are intentionally no longer stored in Git.
CodeXR now generates its default HTTPS certificate pair locally inside VS Code global storage on first startup.

Any *.pem files placed here remain local-only and are ignored by Git.
