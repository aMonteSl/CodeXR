/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ([
/* 0 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.activate = activate;
exports.deactivate = deactivate;
const vscode = __importStar(__webpack_require__(1));
const index_1 = __webpack_require__(2);
const views_1 = __webpack_require__(175);
const commonCommands_1 = __webpack_require__(173);
const serverSettingsManager_1 = __webpack_require__(11);
const activeServerRegistry_1 = __webpack_require__(28);
const serverControl_1 = __webpack_require__(30);
const visualizeDataModel_1 = __webpack_require__(226);
const SSEManager_1 = __webpack_require__(18);
const fileToServerMap_1 = __webpack_require__(20);
const serverWatcherIntegration_1 = __webpack_require__(92);
const cleanAnalysisCommands_1 = __webpack_require__(145);
const configuration_1 = __webpack_require__(151);
const errorHandler_1 = __webpack_require__(37);
const extensionContext_1 = __webpack_require__(227);
// Global context reference for cleanup
let extensionContext;
let globalModularTreeDataProvider = null;
// This method is called when your extension is activated
// Your extension is activated the very first time the command is executed
async function activate(context) {
    // Store context globally for cleanup
    extensionContext = context;
    // Initialize the extension context holder (available to all modules)
    (0, extensionContext_1.initializeExtensionContext)(context);
    // Use the console to output diagnostic information (console.log) and errors (console.error)
    // This line of code will only be executed once when your extension is activated
    console.log('Congratulations, your extension "CodeXR" is now active!');
    try {
        // Step 1: Initialize server settings manager and restore settings FIRST
        console.log('SERVER: Initializing server settings manager');
        const settingsManager = serverSettingsManager_1.ServerSettingsManager.getInstance(context);
        await settingsManager.restoreServerSettings();
        console.log('SERVER: Settings restoration completed');
        // Step 1.5: Initialize analysis configuration storage (singleton)
        console.log('NEW_CODE_ANALYSIS: Initializing configuration storage');
        configuration_1.AnalysisConfigurationStorage.initialize(context);
        console.log('NEW_CODE_ANALYSIS: Configuration storage initialized');
        // Step 2: Initialize active servers registry
        console.log('ACTIVE_SERVERS: Initializing active servers registry');
        const activeServerRegistry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        serverControl_1.ServerControl.initialize(context);
        console.log('ACTIVE_SERVERS: Registry initialized');
        // Step 2.5: Initialize server-watcher integration service
        console.log('SERVER_WATCHER_INTEGRATION: Initializing integration service');
        serverWatcherIntegration_1.ServerWatcherIntegration.initialize(context);
        console.log('SERVER_WATCHER_INTEGRATION: Service initialized');
        // Step 2.7: Execute automatic workspace cleanup on plugin startup
        console.log('WORKSPACE_CLEANUP: Executing automatic workspace cleanup on plugin restart');
        await cleanAnalysisCommands_1.CleanAnalysisCommands.executeStartupCleanup(context);
        console.log('WORKSPACE_CLEANUP: Automatic cleanup completed');
        // Step 3: Register the modular tree view AFTER settings are restored
        console.log('MODULAR_TREE: Registering modular tree view with all sections');
        const modularTreeDataProvider = new views_1.ModularTreeDataProvider(context);
        globalModularTreeDataProvider = modularTreeDataProvider; // Store globally for cleanup
        const modularTreeView = vscode.window.createTreeView('codexrTree', {
            treeDataProvider: modularTreeDataProvider,
            showCollapseAll: true,
            canSelectMany: false
        });
        context.subscriptions.push(modularTreeView);
        console.log('MODULAR_TREE: Tree view registered successfully with all sections');
        // Step 3.5: Set up common commands with the modular tree provider
        console.log('COMMON_COMMANDS: Setting up common commands with modular tree provider');
        commonCommands_1.CommonCommands.setModularTreeProvider(modularTreeDataProvider);
        console.log('COMMON_COMMANDS: Common commands configured');
        // Step 3.6: Code Analysis will start background scanning automatically when provider is created
        console.log('CODE_ANALYSIS: Background file scanning will start automatically');
        // Step 4: Register all commands after tree views are created
        (0, index_1.registerAllCommands)(context, modularTreeDataProvider, undefined);
        // Step 5: Reset Visualize Data state to ensure clean UI/model synchronization
        console.log('VISUALIZE-DATA: Resetting state to ensure clean UI/model synchronization');
        visualizeDataModel_1.VisualizeDataModel.resetVisualizeDataState(context);
        console.log('VISUALIZE-DATA: State reset completed');
        // Step 7: Trigger initial refresh to ensure UI reflects loaded settings
        console.log('MODULAR_TREE: Triggering initial tree view refresh with loaded settings');
        vscode.commands.executeCommand('codexr.tree.refresh');
        console.log('MODULAR_TREE: Extension activation completed successfully');
    }
    catch (error) {
        console.error('MODULAR_TREE: Error during extension activation:', error);
        vscode.window.showErrorMessage(`CodeXR activation failed: ${error}`);
    }
}
// This method is called when your extension is deactivated
async function deactivate() {
    console.log('MODULAR_TREE: CodeXR extension deactivated');
    // Cleanup active servers registry
    try {
        console.log('ACTIVE_SERVERS: Cleaning up active servers registry');
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const cleanedCount = registry.cleanupInactiveServers();
        console.log(`ACTIVE_SERVERS: Registry cleanup completed - removed ${cleanedCount} inactive servers`);
    }
    catch (error) {
        (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.ActiveServers, 'Registry cleanup', error, errorHandler_1.ErrorSeverity.Warn);
    }
    // Cleanup SSE manager and file-to-server mapping
    try {
        console.log('SSE: Cleaning up SSE manager and file mappings');
        SSEManager_1.sseManager.dispose();
        fileToServerMap_1.fileToServerMap.clearAll();
        console.log('SSE: SSE and file mapping cleanup completed');
    }
    catch (error) {
        (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.SSE, 'SSE and file mapping cleanup', error, errorHandler_1.ErrorSeverity.Warn);
    }
    // Cleanup modular tree data provider and file watchers
    try {
        console.log('MODULAR_TREE: Cleaning up modular tree data provider');
        if (globalModularTreeDataProvider) {
            globalModularTreeDataProvider.dispose();
            globalModularTreeDataProvider = null;
        }
        console.log('MODULAR_TREE: Modular tree cleanup completed');
    }
    catch (error) {
        (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.UI, 'Modular tree cleanup', error, errorHandler_1.ErrorSeverity.Warn);
    }
}


/***/ }),
/* 1 */
/***/ ((module) => {

module.exports = require("vscode");

/***/ }),
/* 2 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.registerAllCommands = registerAllCommands;
const vscode = __importStar(__webpack_require__(1));
const serverCommands_1 = __webpack_require__(3);
const activeServersCommands_1 = __webpack_require__(40);
const babiaExamplesCommands_1 = __webpack_require__(42);
const visualizeDataCommands_1 = __webpack_require__(46);
const newCodeAnalysisCommands_1 = __webpack_require__(68);
const pythonEnvCommands_1 = __webpack_require__(166);
const visualizationSettingsCommands_1 = __webpack_require__(168);
const learnMoreCommands_1 = __webpack_require__(170);
const generalCommands_1 = __webpack_require__(172);
const debugThemeProblem_1 = __webpack_require__(174);
/**
 * Entry point that registers all extension commands
 */
function registerAllCommands(context, treeDataProvider, babiaExamplesTreeDataProvider) {
    // Register general/common commands first
    (0, generalCommands_1.registerGeneralCommands)(context);
    // Register server commands
    (0, serverCommands_1.registerServerCommands)(context);
    // Register active servers commands with any refreshable tree data provider
    (0, activeServersCommands_1.registerActiveServersCommands)(context, treeDataProvider);
    // Always register Babia examples commands (they work independently now)
    (0, babiaExamplesCommands_1.registerBabiaExamplesCommands)(context, babiaExamplesTreeDataProvider);
    // Register visualize data commands
    (0, visualizeDataCommands_1.registerVisualizeDataCommands)(context);
    // Register new code analysis commands with specific section refresh callback
    const newCodeAnalysisRefreshCallback = () => {
        if (treeDataProvider) {
            // Try to get the specific section provider if it's a ModularTreeDataProvider
            if ('getSectionProvider' in treeDataProvider) {
                const sectionProvider = treeDataProvider.getSectionProvider('newCodeAnalysis');
                if (sectionProvider && typeof sectionProvider.refresh === 'function') {
                    console.log('COMMAND_REGISTRATION: Using specific section provider refresh');
                    sectionProvider.refresh();
                    return;
                }
            }
            // Fallback to general refresh
            console.log('COMMAND_REGISTRATION: Using general tree provider refresh');
            treeDataProvider.refresh();
        }
    };
    (0, newCodeAnalysisCommands_1.registerNewCodeAnalysisCommands)(context, newCodeAnalysisRefreshCallback);
    // Register Python environment commands
    (0, pythonEnvCommands_1.registerPythonEnvCommands)(context);
    // Register visualization settings commands
    (0, visualizationSettingsCommands_1.registerVisualizationSettingsCommands)(context);
    // Register learn more commands
    (0, learnMoreCommands_1.registerLearnMoreCommands)(context);
    // Register debug commands for theme configuration problem
    const debugCommands = debugThemeProblem_1.DebugThemeProblem.registerDebugCommands(context);
    context.subscriptions.push(...debugCommands);
    // Register the existing hello world command
    const helloWorldCommand = vscode.commands.registerCommand('CodeXR.helloWorld', () => {
        vscode.window.showInformationMessage('Hello World from Code-XR!');
    });
    context.subscriptions.push(helloWorldCommand);
}


/***/ }),
/* 3 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.registerServerCommands = registerServerCommands;
const vscode = __importStar(__webpack_require__(1));
const serverCommands_1 = __webpack_require__(4);
/**
 * Registers all server-related commands
 */
function registerServerCommands(context) {
    console.log('SERVER: Registering server commands');
    // Set the extension context for server commands
    (0, serverCommands_1.setExtensionContext)(context);
    // Main server commands
    const configureServerCommand = vscode.commands.registerCommand('codexr.server.configure', serverCommands_1.configureServer);
    const startLocalServerCommand = vscode.commands.registerCommand('codexr.server.launch', serverCommands_1.startLocalServer);
    // Configuration option commands (UI stubs)
    const configureHttpModeCommand = vscode.commands.registerCommand('codexr.server.config.httpMode', serverCommands_1.configureHttpMode);
    const configurePortCommand = vscode.commands.registerCommand('codexr.server.config.port', serverCommands_1.configurePort);
    const toggleAutoOpenCommand = vscode.commands.registerCommand('codexr.server.config.autoOpen', serverCommands_1.toggleAutoOpen);
    const configureOpenModeCommand = vscode.commands.registerCommand('codexr.server.config.openMode', serverCommands_1.configureOpenMode);
    const resetToDefaultCommand = vscode.commands.registerCommand('codexr.server.config.resetToDefault', serverCommands_1.resetToDefault);
    context.subscriptions.push(configureServerCommand, startLocalServerCommand, configureHttpModeCommand, configurePortCommand, toggleAutoOpenCommand, configureOpenModeCommand, resetToDefaultCommand);
    console.log('SERVER: Server commands registered successfully');
}


/***/ }),
/* 4 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.configureServer = configureServer;
exports.startLocalServer = startLocalServer;
exports.getCurrentServerConfig = getCurrentServerConfig;
exports.configureHttpMode = configureHttpMode;
exports.configurePort = configurePort;
exports.toggleAutoOpen = toggleAutoOpen;
exports.configureOpenMode = configureOpenMode;
exports.resetToDefault = resetToDefault;
exports.setExtensionContext = setExtensionContext;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(6));
const handleConfigurationClicks_1 = __webpack_require__(7);
const multiServerLauncher_1 = __webpack_require__(14);
const serverSettingsManager_1 = __webpack_require__(11);
const previewRenderer_1 = __webpack_require__(38);
let currentServerConfig = {
    mode: 'HTTPS (default certificates)',
    description: 'HTTPS with default certificates'
};
// Global multi-server launcher instance
let multiServerLauncher = null;
// Extension context (set during command registration)
let extensionContext = null;
/**
 * Opens server configuration panel
 */
async function configureServer() {
    console.log('SERVER: Server configuration triggered');
    const options = [
        'HTTPS (default certificates)',
        'HTTPS (custom certificates)',
        'HTTP (development only)'
    ];
    const selected = await vscode.window.showQuickPick(options, {
        placeHolder: 'Select server mode',
        canPickMany: false
    });
    if (selected) {
        currentServerConfig.mode = selected;
        switch (selected) {
            case 'HTTPS (default certificates)':
                currentServerConfig.description = 'HTTPS with default certificates';
                break;
            case 'HTTPS (custom certificates)':
                currentServerConfig.description = 'HTTPS with custom certificates';
                break;
            case 'HTTP (development only)':
                currentServerConfig.description = 'HTTP for development only';
                break;
        }
        console.log(`SERVER: Configuration changed to ${currentServerConfig.mode}`);
        vscode.window.showInformationMessage(`SERVER: Configuration updated to ${currentServerConfig.mode}`);
        // Refresh the tree view
        vscode.commands.executeCommand('codexr.servers.refresh');
    }
}
/**
 * Starts the local server with current configuration
 * Main entry point for the "Start Local Server" command
 */
async function startLocalServer() {
    console.log('SERVER: Start Local Server command triggered');
    try {
        // Step 1: Prompt user to select an HTML file
        const selectedFile = await promptForHtmlFile();
        if (!selectedFile) {
            console.log('SERVER: No file selected, operation cancelled');
            return;
        }
        console.log(`SERVER: Selected HTML file: ${selectedFile}`);
        // Step 2: Get extension context and initialize multi-server launcher
        if (!extensionContext) {
            vscode.window.showErrorMessage('SERVER: Extension context not available');
            return;
        }
        if (!multiServerLauncher) {
            multiServerLauncher = new multiServerLauncher_1.MultiServerLauncher(extensionContext);
        }
        // Step 3: Get current server configuration
        const settingsManager = serverSettingsManager_1.ServerSettingsManager.getInstance(extensionContext);
        const settings = settingsManager.getServerSettings();
        console.log('SERVER: Current configuration:', {
            mode: settings.mode,
            defaultPort: settings.defaultPort,
            autoOpen: settings.launch.autoOpen,
            openMode: settings.launch.openMode
        });
        // Step 4: Validate HTTPS custom certificates if needed
        if (settings.mode === 'HTTPS' && settings.https.certSource === 'custom') {
            const isValid = await validateCustomCertificates(settings);
            if (!isValid) {
                return; // Error message already shown in validation function
            }
        }
        // Step 5: Support multiple servers - show current status
        const runningCount = multiServerLauncher.getRunningServerCount();
        if (runningCount > 0) {
            console.log(`SERVER: ${runningCount} server(s) already running, launching additional server with dynamic port allocation`);
            vscode.window.showInformationMessage(`Launching additional server (${runningCount} servers already running)`);
        }
        // Step 6: Launch the server with the selected HTML file using multi-server launcher
        console.log('SERVER: Launching server...');
        const result = await multiServerLauncher.launchServer(selectedFile);
        if (!result.success) {
            console.error('SERVER: Failed to launch server:', result.error);
            vscode.window.showErrorMessage(`Failed to start server: ${result.error}`);
            return;
        }
        console.log(`SERVER: Server launched successfully at ${result.serverUrl} (Server ID: ${result.serverId})`);
        // Provide detailed messages based on port allocation and HTTPS override
        if (result.portChanged && result.httpsOverridden) {
            vscode.window.showInformationMessage(`Server started at ${result.serverUrl} (Port changed from ${result.originalPort} to ${result.port}, HTTP mode - HTTPS overridden for lateral panel compatibility)`);
        }
        else if (result.portChanged) {
            vscode.window.showInformationMessage(`Server started at ${result.serverUrl} (Port changed from ${result.originalPort} to ${result.port})`);
        }
        else if (result.httpsOverridden) {
            vscode.window.showInformationMessage(`Server started at ${result.serverUrl} (HTTP mode - HTTPS overridden for lateral panel compatibility)`);
        }
        else {
            vscode.window.showInformationMessage(`Server started at ${result.serverUrl}`);
        }
        // Step 7: Auto-open if configured
        if (settings.launch.autoOpen && result.serverUrl) {
            await handleAutoOpen(result.serverUrl, selectedFile, settings.launch.openMode);
        }
        // Refresh the tree view to show server status
        vscode.commands.executeCommand('codexr.servers.refresh');
    }
    catch (error) {
        console.error('SERVER: Error in startLocalServer:', error);
        vscode.window.showErrorMessage(`Failed to start server: ${error instanceof Error ? error.message : String(error)}`);
    }
}
/**
 * Gets the current server configuration
 */
function getCurrentServerConfig() {
    return currentServerConfig;
}
/**
 * Configuration interaction handlers
 */
async function configureHttpMode() {
    await (0, handleConfigurationClicks_1.handleHttpModeClick)();
}
async function configurePort() {
    await (0, handleConfigurationClicks_1.handlePortClick)();
}
async function toggleAutoOpen() {
    await (0, handleConfigurationClicks_1.handleAutoOpenClick)();
}
async function configureOpenMode() {
    await (0, handleConfigurationClicks_1.handleOpenModeClick)();
}
/**
 * Reset server configuration to default values
 */
async function resetToDefault() {
    console.log('SERVER: Resetting server configuration to defaults');
    const confirm = await vscode.window.showWarningMessage('This will reset all server configuration to default values. Are you sure?', { modal: true }, 'Reset to Default', 'Cancel');
    if (confirm !== 'Reset to Default') {
        return;
    }
    try {
        if (!extensionContext) {
            vscode.window.showErrorMessage('SERVER: Extension context not available');
            return;
        }
        const settingsManager = serverSettingsManager_1.ServerSettingsManager.getInstance(extensionContext);
        await settingsManager.resetSettings();
        console.log('SERVER: Configuration reset to defaults successfully');
        vscode.window.showInformationMessage('Server configuration reset to default values');
        // Refresh the tree view to show updated configuration
        vscode.commands.executeCommand('codexr.servers.refresh');
    }
    catch (error) {
        console.error('SERVER: Error resetting configuration to defaults:', error);
        vscode.window.showErrorMessage(`Failed to reset configuration: ${error instanceof Error ? error.message : String(error)}`);
    }
}
/**
 * Set the extension context (called during command registration)
 */
function setExtensionContext(context) {
    extensionContext = context;
}
/**
 * Helper Functions for Server Launching
 */
/**
 * Prompts user to select an HTML file from the file system
 * @returns Promise<string | undefined> - Path to selected file or undefined if cancelled
 */
async function promptForHtmlFile() {
    console.log('SERVER: Prompting user to select HTML file');
    const options = {
        canSelectMany: false,
        openLabel: 'Select HTML File',
        filters: {
            'HTML Files': ['html', 'htm'],
            'All Files': ['*']
        },
        title: 'Select HTML file to serve'
    };
    const fileUri = await vscode.window.showOpenDialog(options);
    if (fileUri && fileUri[0]) {
        const filePath = fileUri[0].fsPath;
        console.log(`SERVER: User selected file: ${filePath}`);
        // Validate that the file exists and is readable
        try {
            await fs.promises.access(filePath, fs.constants.R_OK);
            return filePath;
        }
        catch (error) {
            console.error(`SERVER: Cannot read selected file: ${error}`);
            vscode.window.showErrorMessage(`Cannot read the selected file: ${path.basename(filePath)}`);
            return undefined;
        }
    }
    return undefined;
}
/**
 * Validates custom HTTPS certificates exist and are readable
 * @param settings - Server settings containing certificate paths
 * @returns Promise<boolean> - True if certificates are valid
 */
async function validateCustomCertificates(settings) {
    console.log('SERVER: Validating custom certificates');
    const certPath = settings.https.certPath;
    const keyPath = settings.https.keyPath;
    if (!certPath || !keyPath) {
        console.log('SERVER: Custom certificate paths not configured');
        vscode.window.showErrorMessage('Custom certificates are not configured. Please configure certificate paths in server settings.', 'Configure').then(action => {
            if (action === 'Configure') {
                vscode.commands.executeCommand('codexr.server.configure');
            }
        });
        return false;
    }
    // Check if certificate file exists
    if (!fs.existsSync(certPath)) {
        console.log(`SERVER: Certificate file not found: ${certPath}`);
        vscode.window.showErrorMessage(`Certificate file not found: ${path.basename(certPath)}`, 'Configure').then(action => {
            if (action === 'Configure') {
                vscode.commands.executeCommand('codexr.server.configure');
            }
        });
        return false;
    }
    // Check if key file exists
    if (!fs.existsSync(keyPath)) {
        console.log(`SERVER: Private key file not found: ${keyPath}`);
        vscode.window.showErrorMessage(`Private key file not found: ${path.basename(keyPath)}`, 'Configure').then(action => {
            if (action === 'Configure') {
                vscode.commands.executeCommand('codexr.server.configure');
            }
        });
        return false;
    }
    // Check if files are readable
    try {
        await fs.promises.access(certPath, fs.constants.R_OK);
        await fs.promises.access(keyPath, fs.constants.R_OK);
        console.log('SERVER: Custom certificates validated successfully');
        return true;
    }
    catch (error) {
        console.error(`SERVER: Cannot read certificate files: ${error}`);
        vscode.window.showErrorMessage('Cannot read certificate files. Check file permissions.');
        return false;
    }
}
/**
 * Handles auto-opening the server URL based on configuration
 * @param serverUrl - The server URL to open
 * @param selectedFile - The HTML file being served
 * @param openMode - How to open ('browser' or 'lateralPanel')
 */
async function handleAutoOpen(serverUrl, selectedFile, openMode, serverId) {
    console.log(`SERVER: Auto-opening ${serverUrl} in ${openMode} mode for file: ${selectedFile}`);
    try {
        if (openMode === 'browser') {
            console.log(`SERVER: Opening ${serverUrl} in external browser`);
            await previewRenderer_1.PreviewRenderer.openPreview(serverUrl, selectedFile, openMode);
            vscode.window.showInformationMessage(`Opened ${path.basename(selectedFile)} in browser`);
        }
        else if (openMode === 'lateralPanel') {
            console.log(`SERVER: Opening ${selectedFile} in VS Code webview panel`);
            if (serverId) {
                await previewRenderer_1.PreviewRenderer.openPreview(serverUrl, selectedFile, openMode, serverId);
            }
            else {
                // For legacy calls without serverId, generate a temporary one
                const tempServerId = `temp-${Date.now()}`;
                console.log(`SERVER: No serverId provided, using temporary ID: ${tempServerId}`);
                await previewRenderer_1.PreviewRenderer.openPreview(serverUrl, selectedFile, openMode, tempServerId);
            }
            vscode.window.showInformationMessage(`Opened ${path.basename(selectedFile)} in VS Code panel`);
        }
    }
    catch (error) {
        console.error(`SERVER: Error opening preview: ${error}`);
        vscode.window.showWarningMessage(`Failed to auto-open: ${error instanceof Error ? error.message : String(error)}`);
    }
}


/***/ }),
/* 5 */
/***/ ((module) => {

module.exports = require("path");

/***/ }),
/* 6 */
/***/ ((module) => {

module.exports = require("fs");

/***/ }),
/* 7 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.handleHttpModeClick = handleHttpModeClick;
exports.handlePortClick = handlePortClick;
exports.handleAutoOpenClick = handleAutoOpenClick;
exports.handleOpenModeClick = handleOpenModeClick;
const vscode = __importStar(__webpack_require__(1));
const configurationItems_1 = __webpack_require__(8);
/**
 * Handle HTTP Mode configuration click
 */
async function handleHttpModeClick() {
    console.log('SERVER: HTTP Mode configuration clicked');
    const options = [
        'HTTP',
        'HTTPS (default certificates)',
        'HTTPS (custom certificates)'
    ];
    const selected = await vscode.window.showQuickPick(options, {
        placeHolder: 'Select HTTP mode',
        canPickMany: false
    });
    if (selected) {
        // Handle HTTP mode selection with confirmation dialog
        if (selected === 'HTTP') {
            const confirmed = await vscode.window.showWarningMessage('HTTP is not secure and XR features like VR will not work. Are you sure you want to proceed?', { modal: true }, 'Yes', 'No');
            if (confirmed !== 'Yes') {
                console.log('SERVER: HTTP mode selection cancelled by user');
                return;
            }
            console.log('SERVER: HTTP mode confirmed by user');
        }
        // Handle HTTPS with custom certificates
        if (selected === 'HTTPS (custom certificates)') {
            console.log('SERVER: HTTPS custom certificates selected, opening file pickers');
            // First: Select certificate file (cert.pem)
            const certFileUri = await vscode.window.showOpenDialog({
                canSelectFiles: true,
                canSelectFolders: false,
                canSelectMany: false,
                filters: {
                    'Certificate files': ['pem', 'crt', 'cert']
                },
                openLabel: 'Select Certificate File (cert.pem)'
            });
            if (!certFileUri || certFileUri.length === 0) {
                console.log('SERVER: Certificate file selection cancelled');
                return;
            }
            const certPath = certFileUri[0].fsPath;
            console.log(`SERVER: Certificate file selected: ${certPath}`);
            // Second: Select private key file (key.pem)
            const keyFileUri = await vscode.window.showOpenDialog({
                canSelectFiles: true,
                canSelectFolders: false,
                canSelectMany: false,
                filters: {
                    'Key files': ['pem', 'key']
                },
                openLabel: 'Select Private Key File (key.pem)'
            });
            if (!keyFileUri || keyFileUri.length === 0) {
                console.log('SERVER: Private key file selection cancelled');
                return;
            }
            const keyPath = keyFileUri[0].fsPath;
            console.log(`SERVER: Private key file selected: ${keyPath}`);
            console.log(`SERVER: Custom certificate configuration - Cert: ${certPath}, Key: ${keyPath}`);
            // Store the custom certificate configuration in a single update
            await (0, configurationItems_1.updateServerConfig)({
                httpMode: selected,
                customCertPath: certPath,
                customKeyPath: keyPath
            });
            console.log(`SERVER: HTTP mode changed to ${selected} with custom certificates`);
            vscode.window.showInformationMessage(`SERVER: HTTPS mode updated with custom certificates`);
        }
        else {
            // Handle HTTP and HTTPS with default certificates
            await (0, configurationItems_1.updateServerConfig)({ httpMode: selected });
            console.log(`SERVER: HTTP mode changed to ${selected}`);
            vscode.window.showInformationMessage(`SERVER: HTTP mode updated to ${selected}`);
        }
    }
}
/**
 * Handle Default Port configuration click
 */
async function handlePortClick() {
    console.log('SERVER: Port configuration clicked');
    const input = await vscode.window.showInputBox({
        prompt: 'Enter port number (3000-8080)',
        placeHolder: '3000',
        validateInput: (value) => {
            const num = parseInt(value);
            if (isNaN(num)) {
                return 'Please enter a valid number';
            }
            if (num < 3000 || num > 8080) {
                return 'Port must be between 3000 and 8080';
            }
            return null;
        }
    });
    if (input) {
        const port = parseInt(input);
        await (0, configurationItems_1.updateServerConfig)({ port });
        console.log(`SERVER: Port changed to ${port}`);
        vscode.window.showInformationMessage(`SERVER: Port updated to ${port}`);
    }
}
/**
 * Handle Auto-Open toggle click
 */
async function handleAutoOpenClick() {
    console.log('SERVER: Auto-Open toggle clicked');
    const currentConfig = (0, configurationItems_1.getServerConfig)();
    const newValue = !currentConfig.autoOpen;
    await (0, configurationItems_1.updateServerConfig)({ autoOpen: newValue });
    console.log(`SERVER: Auto-Open toggled to ${newValue ? 'Yes' : 'No'}`);
    const message = newValue ? 'Auto-Open enabled' : 'Auto-Open disabled';
    vscode.window.showInformationMessage(`SERVER: ${message}`);
}
/**
 * Handle Open Mode configuration click
 */
async function handleOpenModeClick() {
    console.log('SERVER: Open Mode configuration clicked');
    const currentConfig = (0, configurationItems_1.getServerConfig)();
    const newMode = currentConfig.openMode === 'Browser' ? 'Lateral Panel' : 'Browser';
    await (0, configurationItems_1.updateServerConfig)({ openMode: newMode });
    console.log(`SERVER: Open Mode changed to ${newMode}`);
    vscode.window.showInformationMessage(`SERVER: Open Mode set to ${newMode}`);
}


/***/ }),
/* 8 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.getServerConfig = getServerConfig;
exports.updateServerConfig = updateServerConfig;
exports.createConfigurationItems = createConfigurationItems;
const vscode = __importStar(__webpack_require__(1));
const unifiedServersTreeView_1 = __webpack_require__(9);
const serverNodeIcons_1 = __webpack_require__(10);
const serverSettingsManager_1 = __webpack_require__(11);
/**
 * Get current server configuration
 */
function getServerConfig() {
    return serverSettingsManager_1.ServerSettingsManager.getInstance().getLegacyConfig();
}
/**
 * Update server configuration
 */
async function updateServerConfig(updates) {
    const manager = serverSettingsManager_1.ServerSettingsManager.getInstance();
    await manager.updateFromLegacyConfig(updates);
}
/**
 * Create configuration items for the server tree view
 */
function createConfigurationItems() {
    const config = getServerConfig();
    // Determine icon based on HTTP mode security
    const httpModeIcon = config.httpMode === 'HTTP'
        ? serverNodeIcons_1.ServerNodeIcons.httpModeUnsecure
        : serverNodeIcons_1.ServerNodeIcons.httpModeSecure;
    return [
        new unifiedServersTreeView_1.ServerTreeItem(`HTTP Mode: ${config.httpMode}`, vscode.TreeItemCollapsibleState.None, 'config-option', {
            command: 'codexr.server.config.httpMode',
            title: 'Configure HTTP Mode'
        }, serverNodeIcons_1.ServerNodeIcons.httpMode, `Click to change server mode (currently: ${config.httpMode})`),
        new unifiedServersTreeView_1.ServerTreeItem(`Default Port: ${config.port}`, vscode.TreeItemCollapsibleState.None, 'config-option', {
            command: 'codexr.server.config.port',
            title: 'Configure Port'
        }, serverNodeIcons_1.ServerNodeIcons.defaultPort, `Click to change default port (currently: ${config.port})`),
        new unifiedServersTreeView_1.ServerTreeItem(`Auto-Open: ${config.autoOpen ? 'Enabled' : 'Disabled'}`, vscode.TreeItemCollapsibleState.None, 'config-option', {
            command: 'codexr.server.config.autoOpen',
            title: 'Toggle Auto-Open'
        }, serverNodeIcons_1.ServerNodeIcons.autoOpen, `Click to toggle auto-open (currently: ${config.autoOpen ? 'enabled' : 'disabled'})`),
        new unifiedServersTreeView_1.ServerTreeItem(`Open Mode: ${config.openMode}`, vscode.TreeItemCollapsibleState.None, 'config-option', {
            command: 'codexr.server.config.openMode',
            title: 'Configure Open Mode'
        }, serverNodeIcons_1.ServerNodeIcons.openMode, `Click to change open mode (currently: ${config.openMode})`),
        new unifiedServersTreeView_1.ServerTreeItem('Reset to Default', vscode.TreeItemCollapsibleState.None, 'config-option', {
            command: 'codexr.server.config.resetToDefault',
            title: 'Reset to Default Settings'
        }, serverNodeIcons_1.ServerNodeIcons.reset, 'Reset all server configuration to default values')
    ];
}


/***/ }),
/* 9 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServersTreeDataProvider = exports.ServerTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
const configurationItems_1 = __webpack_require__(8);
const configurationItems_2 = __webpack_require__(8);
const serverNodeIcons_1 = __webpack_require__(10);
/**
 * Server tree item that represents server configuration and launch options
 * This class is specific to the servers section only
 */
class ServerTreeItem extends vscode.TreeItem {
    label;
    collapsibleState;
    type;
    command;
    iconPath;
    tooltip;
    description;
    contextValue;
    constructor(label, collapsibleState, type, command, iconPath, tooltip, description, contextValue) {
        super(label, collapsibleState);
        this.label = label;
        this.collapsibleState = collapsibleState;
        this.type = type;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip;
        this.description = description;
        this.contextValue = contextValue;
        this.tooltip = tooltip || this.label;
        this.iconPath = iconPath;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.ServerTreeItem = ServerTreeItem;
/**
 * Servers tree data provider that handles only the SERVERS section
 *
 * Architecture Notes:
 * - This view is specific to server configuration and launch
 * - Follows the modular architecture pattern with items/ and interactions/ directories
 * - Delegates to appropriate handlers for user interactions
 */
class ServersTreeDataProvider {
    context;
    _onDidChangeTreeData = new vscode.EventEmitter();
    onDidChangeTreeData = this._onDidChangeTreeData.event;
    constructor(context) {
        this.context = context;
        console.log('SERVERS_TREE: Servers tree data provider initialized');
        // Register refresh command (only if not already registered)
        try {
            vscode.commands.registerCommand('codexr.servers.refreshServers', () => {
                this.refresh();
            });
        }
        catch (error) {
            // Command might already be registered, ignore this error
            console.log('SERVERS_TREE: Refresh servers command already registered');
        }
    }
    /**
     * Refresh the tree view
     */
    refresh() {
        console.log('SERVERS_TREE: Refreshing servers tree view');
        this._onDidChangeTreeData.fire();
    }
    /**
     * Get tree item representation
     */
    getTreeItem(element) {
        return element;
    }
    /**
     * Get children for the tree view
     */
    getChildren(element) {
        if (!element) {
            // Root level - return the SERVERS section
            console.log('SERVERS_TREE: Loading root SERVERS section');
            return Promise.resolve([
                new ServerTreeItem('SERVERS', vscode.TreeItemCollapsibleState.Expanded, 'section', undefined, new vscode.ThemeIcon('server-environment'), 'Server configuration and launch options')
            ]);
        }
        switch (element.type) {
            case 'section':
                if (element.label === 'SERVERS') {
                    return this.getServersChildren();
                }
                break;
            case 'config-group':
                if (element.label === 'Server Configuration') {
                    return this.getServerConfigChildren();
                }
                break;
            default:
                return Promise.resolve([]);
        }
        return Promise.resolve([]);
    }
    /**
     * Get children for the SERVERS section
     */
    getServersChildren() {
        console.log('SERVERS_TREE: Loading servers section children');
        const config = (0, configurationItems_1.getServerConfig)();
        return Promise.resolve([
            new ServerTreeItem('Server Configuration', vscode.TreeItemCollapsibleState.Collapsed, 'config-group', undefined, serverNodeIcons_1.ServerNodeIcons.configuration, 'Configure server settings'),
            new ServerTreeItem('Start Local Server', vscode.TreeItemCollapsibleState.None, 'launch-option', {
                command: 'codexr.server.launch',
                title: 'Start Local Server'
            }, serverNodeIcons_1.ServerNodeIcons.startServer, `Start server on port ${config.port} (${config.httpMode})`)
        ]);
    }
    /**
     * Get children for the server configuration group
     */
    getServerConfigChildren() {
        console.log('SERVERS_TREE: Loading server configuration children');
        const configItems = (0, configurationItems_2.createConfigurationItems)();
        const children = configItems.map(item => new ServerTreeItem(item.label, vscode.TreeItemCollapsibleState.None, 'config-option', item.command, item.iconPath, item.tooltip, item.description));
        return Promise.resolve(children);
    }
}
exports.ServersTreeDataProvider = ServersTreeDataProvider;


/***/ }),
/* 10 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServerNodeIcons = void 0;
const vscode = __importStar(__webpack_require__(1));
/**
 * VS Code icon references for server tree items
 */
exports.ServerNodeIcons = {
    // Main groups
    servers: new vscode.ThemeIcon('server-environment'),
    configuration: new vscode.ThemeIcon('gear'),
    startServer: new vscode.ThemeIcon('play'),
    // Configuration options
    httpMode: new vscode.ThemeIcon('globe'),
    httpModeSecure: new vscode.ThemeIcon('lock'),
    httpModeUnsecure: new vscode.ThemeIcon('unlock'),
    defaultPort: new vscode.ThemeIcon('plug'),
    autoOpen: new vscode.ThemeIcon('eye'),
    openMode: new vscode.ThemeIcon('layout'),
    reset: new vscode.ThemeIcon('discard'),
    // Active servers actions
    stopAll: new vscode.ThemeIcon('stop-circle')
};


/***/ }),
/* 11 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServerSettingsManager = exports.DEFAULT_SERVER_SETTINGS = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const nonceGenerator_1 = __webpack_require__(12);
/**
 * Default server settings
 */
exports.DEFAULT_SERVER_SETTINGS = {
    mode: 'HTTPS',
    https: {
        certSource: 'default',
        certPath: '',
        keyPath: ''
    },
    defaultPort: 3000,
    launch: {
        autoOpen: true,
        openMode: 'browser'
    },
    configNonce: (0, nonceGenerator_1.generateNonce)(),
    version: '1.0.0'
};
/**
 * Server Settings Manager
 * Handles structured storage and retrieval of server configuration using file system
 */
class ServerSettingsManager {
    static instance;
    settings;
    context;
    SETTINGS_FILENAME = 'server-settings.json';
    settingsFilePath;
    constructor(context) {
        this.context = context;
        this.settings = { ...exports.DEFAULT_SERVER_SETTINGS };
        this.settingsFilePath = path.join(context.globalStorageUri.fsPath, this.SETTINGS_FILENAME);
        this.ensureStorageDirectory();
    }
    /**
     * Ensure the global storage directory exists
     */
    ensureStorageDirectory() {
        const storageDir = path.dirname(this.settingsFilePath);
        if (!fs.existsSync(storageDir)) {
            fs.mkdirSync(storageDir, { recursive: true });
            console.log(`SERVER: Created storage directory: ${storageDir}`);
        }
    }
    /**
     * Get singleton instance
     */
    static getInstance(context) {
        if (!ServerSettingsManager.instance) {
            if (!context) {
                throw new Error('SERVER: Context required for first initialization');
            }
            ServerSettingsManager.instance = new ServerSettingsManager(context);
        }
        return ServerSettingsManager.instance;
    }
    /**
     * Get current server settings
     */
    getServerSettings() {
        return { ...this.settings };
    }
    /**
     * Get extension context
     */
    getExtensionContext() {
        return this.context;
    }
    /**
     * Update server settings
     */
    async updateServerSettings(updates) {
        console.log('SERVER: Updating server settings', updates);
        // Deep merge the updates
        this.settings = this.deepMerge(this.settings, updates);
        this.settings.configNonce = (0, nonceGenerator_1.generateNonce)();
        // Persist to file system asynchronously
        await this.persistSettings();
        // Refresh the tree view
        vscode.commands.executeCommand('codexr.servers.refresh');
    }
    /**
     * Restore server settings from file system
     */
    async restoreServerSettings() {
        console.log('SERVER: Restoring server settings from file system');
        console.log('SERVER: Settings file path:', this.settingsFilePath);
        try {
            if (fs.existsSync(this.settingsFilePath)) {
                console.log('SERVER: Settings file exists, reading content...');
                const fileContent = await fs.promises.readFile(this.settingsFilePath, 'utf8');
                const savedSettings = JSON.parse(fileContent);
                console.log('SERVER: Loaded settings from file:', savedSettings);
                // Validate and merge with defaults to ensure all required fields exist
                this.settings = this.deepMerge(exports.DEFAULT_SERVER_SETTINGS, savedSettings);
                // Regenerate nonce on restore for security
                this.settings.configNonce = (0, nonceGenerator_1.generateNonce)();
                console.log('SERVER: Settings merged with defaults and nonce regenerated');
                console.log('SERVER: Final restored settings:', this.settings);
                // Persist the updated settings with new nonce
                await this.persistSettings();
                console.log('SERVER: Settings successfully restored from file system');
            }
            else {
                console.log('SERVER: No saved settings file found at:', this.settingsFilePath);
                console.log('SERVER: Using default settings and creating initial file');
                this.settings = { ...exports.DEFAULT_SERVER_SETTINGS };
                await this.persistSettings();
                console.log('SERVER: Default settings applied and file created');
            }
        }
        catch (error) {
            console.error('SERVER: Error restoring settings from file system:', error);
            console.log('SERVER: Falling back to default settings');
            this.settings = { ...exports.DEFAULT_SERVER_SETTINGS };
            await this.persistSettings();
        }
    }
    /**
     * Persist settings to file system
     */
    async persistSettings() {
        try {
            this.ensureStorageDirectory();
            const settingsJson = JSON.stringify(this.settings, null, 2);
            await fs.promises.writeFile(this.settingsFilePath, settingsJson, 'utf8');
            console.log(`SERVER: Settings persisted to file system: ${this.settingsFilePath}`);
        }
        catch (error) {
            console.error('SERVER: Error persisting settings to file system', error);
            throw error;
        }
    }
    /**
     * Reset settings to defaults
     */
    async resetSettings() {
        console.log('SERVER: Resetting settings to defaults');
        this.settings = { ...exports.DEFAULT_SERVER_SETTINGS };
        this.settings.configNonce = (0, nonceGenerator_1.generateNonce)();
        await this.persistSettings();
        vscode.commands.executeCommand('codexr.servers.refresh');
    }
    /**
     * Get the path to the settings file
     */
    getSettingsFilePath() {
        return this.settingsFilePath;
    }
    /**
     * Deep merge utility for partial updates
     */
    deepMerge(target, source) {
        const result = { ...target };
        for (const key in source) {
            if (source[key] !== null && typeof source[key] === 'object' && !Array.isArray(source[key])) {
                result[key] = this.deepMerge(target[key] || {}, source[key]);
            }
            else {
                result[key] = source[key];
            }
        }
        return result;
    }
    /**
     * Get settings in legacy format for UI compatibility
     */
    getLegacyConfig() {
        let httpModeDisplay;
        if (this.settings.mode === 'HTTP') {
            httpModeDisplay = 'HTTP';
        }
        else {
            // HTTPS mode
            if (this.settings.https.certSource === 'default') {
                httpModeDisplay = 'HTTPS (default certificates)';
            }
            else {
                // Custom certificates - show paths if available for debugging
                const certInfo = this.settings.https.certPath && this.settings.https.keyPath
                    ? ` [Cert: ${this.settings.https.certPath}, Key: ${this.settings.https.keyPath}]`
                    : ' [Paths not configured]';
                httpModeDisplay = `HTTPS (custom certificates)${certInfo}`;
                console.log('SERVER: Custom HTTPS certificate status:', {
                    certPath: this.settings.https.certPath,
                    keyPath: this.settings.https.keyPath,
                    certSource: this.settings.https.certSource
                });
            }
        }
        const openModeDisplay = this.settings.launch.openMode === 'browser' ? 'Browser' : 'Lateral Panel';
        const config = {
            httpMode: httpModeDisplay,
            port: this.settings.defaultPort,
            autoOpen: this.settings.launch.autoOpen,
            openMode: openModeDisplay
        };
        console.log('SERVER: Legacy config generated:', config);
        return config;
    }
    /**
     * Update settings from legacy format
     */
    async updateFromLegacyConfig(updates) {
        const newUpdates = {};
        // Handle HTTP mode changes
        if (updates.httpMode) {
            if (updates.httpMode === 'HTTP') {
                newUpdates.mode = 'HTTP';
            }
            else if (updates.httpMode === 'HTTPS (default certificates)') {
                newUpdates.mode = 'HTTPS';
                newUpdates.https = {
                    ...this.settings.https,
                    certSource: 'default'
                };
            }
            else if (updates.httpMode === 'HTTPS (custom certificates)') {
                newUpdates.mode = 'HTTPS';
                newUpdates.https = {
                    ...this.settings.https,
                    certSource: 'custom'
                };
            }
        }
        // Handle certificate and key path updates
        // Merge these into the existing https object to avoid overwriting
        if (updates.customCertPath !== undefined || updates.customKeyPath !== undefined) {
            const currentHttps = newUpdates.https || this.settings.https;
            newUpdates.https = {
                ...currentHttps,
                ...(updates.customCertPath !== undefined && { certPath: updates.customCertPath }),
                ...(updates.customKeyPath !== undefined && { keyPath: updates.customKeyPath })
            };
            console.log('SERVER: Updating HTTPS certificate paths', {
                certPath: newUpdates.https.certPath,
                keyPath: newUpdates.https.keyPath,
                certSource: newUpdates.https.certSource
            });
        }
        // Handle port updates
        if (updates.port !== undefined) {
            newUpdates.defaultPort = updates.port;
        }
        // Handle launch configuration updates
        if (updates.autoOpen !== undefined || updates.openMode !== undefined) {
            newUpdates.launch = {
                ...this.settings.launch,
                ...(updates.autoOpen !== undefined && { autoOpen: updates.autoOpen }),
                ...(updates.openMode !== undefined && {
                    openMode: updates.openMode === 'Browser' ? 'browser' : 'lateralPanel'
                })
            };
        }
        console.log('SERVER: Legacy config update merged:', newUpdates);
        await this.updateServerSettings(newUpdates);
    }
}
exports.ServerSettingsManager = ServerSettingsManager;


/***/ }),
/* 12 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.generateNonce = generateNonce;
exports.generateTimestampedNonce = generateTimestampedNonce;
exports.validateNonce = validateNonce;
exports.validateTimestampedNonce = validateTimestampedNonce;
const crypto_1 = __webpack_require__(13);
/**
 * Generates a cryptographically secure nonce (number used once) for configuration tracking.
 * Uses Node.js crypto module to create unpredictable random values.
 *
 * @param length - The length of the nonce in bytes (default: 16 bytes = 128 bits)
 * @returns A hex-encoded string representing the nonce
 */
function generateNonce(length = 16) {
    return (0, crypto_1.randomBytes)(length).toString('hex');
}
/**
 * Generates a timestamped nonce that includes both time and random components.
 * Useful for scenarios where temporal ordering is important alongside uniqueness.
 *
 * @param length - The length of the random component in bytes (default: 8 bytes)
 * @returns A hex-encoded string with timestamp prefix and random suffix
 */
function generateTimestampedNonce(length = 8) {
    const timestamp = Date.now().toString(16);
    const random = (0, crypto_1.randomBytes)(length).toString('hex');
    return `${timestamp}-${random}`;
}
/**
 * Validates that a nonce has the expected format and length.
 *
 * @param nonce - The nonce to validate
 * @param expectedLength - Expected length in bytes (default: 16)
 * @returns True if the nonce is valid, false otherwise
 */
function validateNonce(nonce, expectedLength = 16) {
    if (typeof nonce !== 'string') {
        return false;
    }
    // Check if it's a hex string of expected length
    const expectedHexLength = expectedLength * 2;
    // Special case: zero length is valid if we expect zero length
    if (expectedHexLength === 0) {
        return nonce.length === 0;
    }
    // Check non-empty nonce
    if (!nonce) {
        return false;
    }
    const hexPattern = /^[0-9a-f]+$/i;
    return nonce.length === expectedHexLength && hexPattern.test(nonce);
}
/**
 * Validates timestamped nonce format.
 *
 * @param nonce - The timestamped nonce to validate
 * @returns True if the nonce has valid timestamped format, false otherwise
 */
function validateTimestampedNonce(nonce) {
    if (!nonce || typeof nonce !== 'string') {
        return false;
    }
    // Check format: timestamp-random
    const parts = nonce.split('-');
    if (parts.length !== 2) {
        return false;
    }
    const [timestampHex, randomHex] = parts;
    const hexPattern = /^[0-9a-f]+$/i;
    return hexPattern.test(timestampHex) && hexPattern.test(randomHex);
}


/***/ }),
/* 13 */
/***/ ((module) => {

module.exports = require("crypto");

/***/ }),
/* 14 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.MultiServerLauncher = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
const httpServer_1 = __webpack_require__(15);
const httpsDefaultServer_1 = __webpack_require__(23);
const httpsCustomServer_1 = __webpack_require__(27);
const portManager_1 = __webpack_require__(25);
const serverSettingsManager_1 = __webpack_require__(11);
const activeServerRegistry_1 = __webpack_require__(28);
const handleServerActions_1 = __webpack_require__(29);
const serverRegistrar_1 = __webpack_require__(39);
const networkUtils_1 = __webpack_require__(21);
/**
 * Multi-Server Launcher
 * Manages multiple HTTP/HTTPS servers running concurrently with dynamic port allocation
 */
class MultiServerLauncher {
    context;
    servers = new Map();
    settingsManager;
    constructor(context) {
        this.context = context;
        this.settingsManager = serverSettingsManager_1.ServerSettingsManager.getInstance(context);
        console.log('SERVER: Multi-server launcher initialized');
    }
    /**
     * Launch a new server instance with dynamic port allocation
     * @param htmlFile Optional HTML file to serve
     * @param customName Optional custom display name for the server
     * @returns Promise<MultiServerLaunchResult>
     */
    async launchServer(htmlFile, customName, additionalMetadata) {
        try {
            console.log('SERVER: Starting multi-server launch process...');
            console.log('SERVER: launchServer called with parameters:', { htmlFile, customName });
            // Load current settings
            const settings = this.settingsManager.getServerSettings();
            console.log('SERVER: Loaded settings:', {
                ...settings,
                https: settings.https ? {
                    ...settings.https,
                    certPath: settings.https.certPath ? '***REDACTED***' : undefined,
                    keyPath: settings.https.keyPath ? '***REDACTED***' : undefined
                } : undefined
            });
            // Check for HTTPS + lateral panel conflict and handle appropriately
            const serverTypeResult = this.checkAndHandleLateralPanelHttpsConflict(settings);
            const serverType = serverTypeResult.serverType;
            const isHttpsOverridden = serverTypeResult.isOverridden;
            console.log('SERVER: Determined server type:', serverType);
            if (isHttpsOverridden) {
                console.log('SERVER: *** IMPORTANT *** Temporarily overriding HTTPS to HTTP for lateral panel compatibility');
            }
            // Find available port using dynamic allocation
            const requestedPort = settings.defaultPort;
            const finalPort = await this.findAvailablePortWithLogging(requestedPort);
            const portChanged = finalPort !== requestedPort;
            if (portChanged) {
                console.log(`SERVER: Port ${requestedPort} already in use. Using port ${finalPort} instead.`);
                vscode.window.showInformationMessage(`Port ${requestedPort} was busy, launching server on port ${finalPort} instead.`);
            }
            else {
                console.log(`SERVER: Using requested port ${finalPort}`);
            }
            // Launch the server - Use 0.0.0.0 to listen on all network interfaces for external access
            const host = '0.0.0.0';
            console.log(`SERVER: DEBUG - Configuring server to listen on ${host}:${finalPort} for network access`);
            const result = await this.launchServerByType(serverType, settings, finalPort, host, htmlFile);
            if (result.success && result.serverUrl) {
                console.log(`SERVER: Successfully launched ${serverType} server on port ${finalPort}`);
                // Generate unique server ID
                const serverId = this.generateServerId();
                // Store server info
                const serverInfo = {
                    id: serverId,
                    server: result.serverInstance,
                    serverType: serverType,
                    port: finalPort,
                    htmlFile: htmlFile,
                    activeServerId: ''
                };
                // Register server in active servers registry
                try {
                    const launchMode = this.determineLaunchMode(settings);
                    const certMode = this.determineCertMode(serverType, result.httpsOverridden);
                    console.log('SERVER: 🔍 DEBUG - About to register server with the following details:');
                    console.log('SERVER: 🔍 DEBUG - customName:', customName);
                    console.log('SERVER: 🔍 DEBUG - port:', finalPort);
                    console.log('SERVER: 🔍 DEBUG - url:', result.serverUrl);
                    const registrar = (0, serverRegistrar_1.getServerRegistrar)();
                    const activeServer = registrar.registerServer({
                        port: finalPort,
                        url: result.serverUrl,
                        launchMode: launchMode,
                        certMode: certMode,
                        timestamp: Date.now(),
                        htmlFile: htmlFile,
                        customName: customName,
                        serverInstance: result.serverInstance,
                        metadata: {
                            serverType: serverType,
                            originalPort: requestedPort,
                            portChanged: portChanged,
                            httpsOverridden: isHttpsOverridden,
                            launcherId: 'multi-server',
                            serverInstanceId: serverId,
                            ...additionalMetadata // Add any additional metadata passed in
                        }
                    });
                    console.log('SERVER: ✅ Successfully registered server with registry:');
                    console.log('SERVER: ✅ Registered server ID:', activeServer.id);
                    console.log('SERVER: ✅ Registered server customName:', activeServer.customName);
                    serverInfo.activeServerId = activeServer.id;
                    this.servers.set(serverId, serverInfo);
                    console.log(`SERVER: Successfully registered server ${serverId} (active ID: ${activeServer.id}) via registrar service`);
                    console.log(`SERVER: Currently managing ${this.servers.size} server(s)`);
                    // Handle auto-opening based on configuration
                    await this.handleAutoOpening(settings, result.serverUrl, htmlFile, launchMode, serverInfo.activeServerId);
                }
                catch (error) {
                    console.error('SERVER: Failed to register server in registry:', error);
                    // Don't fail the launch if registry registration fails
                }
                return {
                    success: true,
                    serverUrl: result.serverUrl,
                    serverType: serverType,
                    serverId: serverId,
                    port: finalPort,
                    httpsOverridden: isHttpsOverridden,
                    portChanged: portChanged,
                    originalPort: requestedPort
                };
            }
            else {
                return {
                    success: false,
                    error: result.error || 'Unknown server launch error',
                    httpsOverridden: isHttpsOverridden
                };
            }
        }
        catch (error) {
            console.error('SERVER: Error during multi-server launch:', error);
            return {
                success: false,
                error: `Failed to launch server: ${error instanceof Error ? error.message : String(error)}`
            };
        }
    }
    /**
     * Stop a specific server by ID
     * @param serverId Server ID to stop
     * @returns Promise<boolean>
     */
    async stopServer(serverId) {
        const serverInfo = this.servers.get(serverId);
        if (!serverInfo) {
            console.log(`SERVER: Server ${serverId} not found or already stopped`);
            return true;
        }
        try {
            console.log(`SERVER: Stopping server ${serverId} (${serverInfo.serverType}) on port ${serverInfo.port}...`);
            await serverInfo.server.stop();
            // Unregister from active servers registry
            if (serverInfo.activeServerId) {
                try {
                    const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
                    registry.unregisterServer(serverInfo.activeServerId);
                    console.log(`SERVER: Unregistered server ${serverInfo.activeServerId} from registry`);
                }
                catch (error) {
                    console.error('SERVER: Failed to unregister server from registry:', error);
                }
            }
            this.servers.delete(serverId);
            console.log(`SERVER: Server ${serverId} stopped successfully. ${this.servers.size} server(s) remaining`);
            return true;
        }
        catch (error) {
            console.error(`SERVER: Error stopping server ${serverId}:`, error);
            return false;
        }
    }
    /**
     * Stop all running servers
     * @returns Promise<boolean>
     */
    async stopAllServers() {
        console.log(`SERVER: Stopping all ${this.servers.size} servers...`);
        const stopPromises = Array.from(this.servers.keys()).map(serverId => this.stopServer(serverId));
        const results = await Promise.all(stopPromises);
        const allStopped = results.every(result => result === true);
        if (allStopped) {
            console.log('SERVER: All servers stopped successfully');
        }
        else {
            console.error('SERVER: Some servers failed to stop properly');
        }
        return allStopped;
    }
    /**
     * Get information about all running servers
     * @returns Array of server info
     */
    getRunningServers() {
        return Array.from(this.servers.values()).map(serverInfo => ({
            id: serverInfo.id,
            serverType: serverInfo.serverType,
            port: serverInfo.port,
            htmlFile: serverInfo.htmlFile,
            isRunning: serverInfo.server.getIsRunning()
        }));
    }
    /**
     * Check if any servers are running
     * @returns boolean
     */
    hasRunningServers() {
        return this.servers.size > 0;
    }
    /**
     * Get count of running servers
     * @returns number
     */
    getRunningServerCount() {
        return this.servers.size;
    }
    /**
     * Find available port with enhanced logging
     * @private
     */
    async findAvailablePortWithLogging(requestedPort) {
        console.log(`SERVER: Checking availability of port ${requestedPort}...`);
        const isAvailable = await portManager_1.PortManager.isPortAvailable(requestedPort);
        if (isAvailable) {
            console.log(`SERVER: Port ${requestedPort} is available`);
            return requestedPort;
        }
        console.log(`SERVER: Port ${requestedPort} already in use. Searching for next available port (${requestedPort + 1})...`);
        try {
            const availablePort = await portManager_1.PortManager.findAvailablePort(requestedPort + 1);
            console.log(`SERVER: Free port found at ${availablePort}. Launching server...`);
            return availablePort;
        }
        catch (error) {
            console.error(`SERVER: Failed to find available port starting from ${requestedPort + 1}:`, error);
            throw new Error(`No available ports found starting from ${requestedPort}: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Generate unique server ID
     * @private
     */
    generateServerId() {
        const timestamp = Date.now();
        const random = Math.random().toString(36).substr(2, 5);
        return `server_${timestamp}_${random}`;
    }
    /**
     * Determine server type based on settings
     * @private
     */
    determineServerType(settings) {
        if (settings.mode === 'HTTP') {
            return 'http';
        }
        else if (settings.mode === 'HTTPS') {
            return settings.https.certSource === 'default' ? 'https-default' : 'https-custom';
        }
        throw new Error(`Invalid server mode: ${settings.mode}`);
    }
    /**
     * Determine launch mode based on settings
     * @private
     */
    determineLaunchMode(settings) {
        return settings.launch.openMode === 'lateralPanel' ? 'lateralPanel' : 'browser';
    }
    /**
     * Determine certificate mode based on server type and overrides
     * @private
     */
    determineCertMode(serverType, httpsOverridden) {
        if (httpsOverridden) {
            return 'http';
        }
        switch (serverType) {
            case 'http':
                return 'http';
            case 'https-default':
                return 'https-default';
            case 'https-custom':
                return 'https-custom';
            default:
                return 'http';
        }
    }
    /**
     * Launch server by type - delegates to existing server implementations
     * @private
     */
    async launchServerByType(serverType, settings, port, host, htmlFile) {
        try {
            let server;
            let serverUrl;
            // Default server configuration
            const staticRoot = htmlFile ? path.dirname(htmlFile) : path.join(__dirname, '../../../templates');
            const mainFile = htmlFile ? path.basename(htmlFile) : undefined;
            const enableCors = true;
            const allowedOrigins = ['*'];
            console.log(`SERVER: Using static root: ${staticRoot}`);
            if (mainFile) {
                console.log(`SERVER: Main file configured: ${mainFile}`);
            }
            switch (serverType) {
                case 'http':
                    console.log('SERVER: Creating HTTP server...');
                    server = new httpServer_1.HttpServer({
                        port,
                        host,
                        staticRoot,
                        enableCors,
                        allowedOrigins,
                        mainFile
                    });
                    break;
                case 'https-default':
                    console.log('SERVER: Creating HTTPS server with default certificates...');
                    server = new httpsDefaultServer_1.HttpsDefaultServer({
                        port,
                        host,
                        staticRoot,
                        enableCors,
                        allowedOrigins,
                        mainFile,
                        extensionContext: this.context
                    });
                    break;
                case 'https-custom':
                    console.log('SERVER: Creating HTTPS server with custom certificates...');
                    if (!settings.https?.certPath || !settings.https.keyPath) {
                        throw new Error('Custom certificate paths are required for custom HTTPS server');
                    }
                    server = new httpsCustomServer_1.HttpsCustomServer({
                        port,
                        host,
                        staticRoot,
                        enableCors,
                        allowedOrigins,
                        mainFile,
                        certPath: settings.https.certPath,
                        keyPath: settings.https.keyPath,
                        extensionContext: this.context
                    });
                    break;
                default:
                    throw new Error(`Unsupported server type: ${serverType}`);
            }
            // Start the server
            console.log(`SERVER: Starting ${serverType} server...`);
            serverUrl = await server.start();
            return {
                success: true,
                serverUrl,
                serverType,
                port,
                serverInstance: server
            };
        }
        catch (error) {
            console.error(`SERVER: Error launching ${serverType} server:`, error);
            return {
                success: false,
                error: error instanceof Error ? error.message : String(error),
                serverType
            };
        }
    }
    /**
     * Check for lateral panel + HTTPS conflict and provide fallback
     * @private
     * @param settings - Server settings
     * @returns Modified server type if fallback needed, original type otherwise
     */
    checkAndHandleLateralPanelHttpsConflict(settings) {
        const originalServerType = this.determineServerType(settings);
        // Check if we have lateral panel + HTTPS combination
        const isLateralPanel = settings.launch.openMode === 'lateralPanel';
        const isHttps = originalServerType === 'https-default' || originalServerType === 'https-custom';
        if (isLateralPanel && isHttps) {
            console.log('SERVER: HTTPS + Lateral Panel conflict detected - overriding to HTTP for compatibility');
            console.log(`SERVER: Original server type would have been: ${originalServerType}`);
            console.log('SERVER: VS Code webview panels do not support HTTPS content, using HTTP fallback');
            // Show non-blocking warning to user with actionable information
            vscode.window.showWarningMessage('Launching in HTTP mode due to VS Code limitations: lateral panel views do not support HTTPS content. Use browser mode to preserve HTTPS.', 'Change to Browser Mode').then(action => {
                if (action === 'Change to Browser Mode') {
                    // Open the configuration to change open mode
                    vscode.commands.executeCommand('codexr.server.config.openMode');
                }
            });
            return {
                serverType: 'http',
                isOverridden: true
            };
        }
        return {
            serverType: originalServerType,
            isOverridden: false
        };
    }
    /**
     * Handle auto-opening based on configuration
     * @private
     */
    async handleAutoOpening(settings, serverUrl, htmlFile, launchMode, activeServerId) {
        const isAutoOpenEnabled = settings.launch.autoOpen;
        console.log(`SERVER: Auto-open configuration: enabled=${isAutoOpenEnabled}, mode=${launchMode}`);
        if (!isAutoOpenEnabled) {
            console.log('SERVER: Auto-open is disabled, showing success notification only');
            // Extract port from serverUrl to generate network URLs
            const portMatch = serverUrl.match(/:(\d+)/);
            const port = portMatch ? parseInt(portMatch[1]) : null;
            const protocol = serverUrl.startsWith('https') ? 'https' : 'http';
            let notificationMessage = `Server launched successfully on ${serverUrl}`;
            // Add network access information if port is available
            if (port) {
                const localIP = networkUtils_1.NetworkUtils.getLocalIPAddress();
                if (localIP !== 'localhost') {
                    const networkUrl = `${protocol}://${localIP}:${port}`;
                    notificationMessage = `Server launched successfully! Network: ${networkUrl} Local: ${serverUrl}`;
                    console.log(`SERVER: Network access URL: ${networkUrl}`);
                }
            }
            vscode.window.showInformationMessage(notificationMessage, 'Open Now', 'Copy Network URL').then(action => {
                if (action === 'Open Now') {
                    // Use the active server action handler to respect the configured mode
                    if (launchMode === 'browser') {
                        this.openServerInBrowser(activeServerId);
                    }
                    else {
                        this.openServerInPanel(activeServerId);
                    }
                }
                else if (action === 'Copy Network URL' && port) {
                    const localIP = networkUtils_1.NetworkUtils.getLocalIPAddress();
                    if (localIP !== 'localhost') {
                        const networkUrl = `${protocol}://${localIP}:${port}`;
                        vscode.env.clipboard.writeText(networkUrl);
                        vscode.window.showInformationMessage('Network URL copied to clipboard');
                    }
                }
            });
            return;
        }
        console.log(`SERVER: Auto-opening server in ${launchMode} mode`);
        try {
            // Auto-open using the configured launch mode
            if (launchMode === 'browser') {
                await this.openServerInBrowser(activeServerId);
            }
            else {
                await this.openServerInPanel(activeServerId);
            }
        }
        catch (error) {
            console.error('SERVER: Error during auto-opening:', error);
            vscode.window.showWarningMessage(`Server launched but failed to auto-open: ${error instanceof Error ? error.message : String(error)}`, 'Open Manually').then(action => {
                if (action === 'Open Manually') {
                    if (launchMode === 'browser') {
                        this.openServerInBrowser(activeServerId);
                    }
                    else {
                        this.openServerInPanel(activeServerId);
                    }
                }
            });
        }
    }
    /**
     * Open server in browser using shared handler
     * @private
     */
    async openServerInBrowser(activeServerId) {
        console.log(`SERVER: Opening server ${activeServerId} in browser via shared handler`);
        try {
            await handleServerActions_1.ServerActionHandlers.openInBrowser(activeServerId);
        }
        catch (error) {
            console.error(`SERVER: Failed to open server in browser: ${error}`);
            throw error;
        }
    }
    /**
     * Open server in lateral panel using shared handler
     * @private
     */
    async openServerInPanel(activeServerId) {
        console.log(`SERVER: Opening server ${activeServerId} in lateral panel via shared handler`);
        try {
            await handleServerActions_1.ServerActionHandlers.openInPanel(activeServerId);
        }
        catch (error) {
            console.error(`SERVER: Failed to open server in lateral panel: ${error}`);
            throw error;
        }
    }
}
exports.MultiServerLauncher = MultiServerLauncher;


/***/ }),
/* 15 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.HttpServer = void 0;
const http = __importStar(__webpack_require__(16));
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(6));
const url_1 = __webpack_require__(17);
const SSEManager_1 = __webpack_require__(18);
const fileToServerMap_1 = __webpack_require__(20);
const networkUtils_1 = __webpack_require__(21);
/**
 * HTTP Server instance
 * Provides basic HTTP server functionality for CodeXR
 */
class HttpServer {
    server = null;
    config;
    isRunning = false;
    constructor(config) {
        // Ensure port is a number and create clean config
        const cleanConfig = {
            ...config,
            port: Number(config.port)
        };
        this.config = {
            host: '0.0.0.0', // ✅ Listen on all network interfaces for VR/mobile access
            staticRoot: path.join(__dirname, '../../../templates'),
            enableCors: true,
            allowedOrigins: ['*'],
            ...cleanConfig
        };
        console.log('SERVER: HTTP server initialized with config:', this.config);
    }
    /**
     * Start the HTTP server
     * @returns Promise<string> - Server URL
     */
    async start() {
        if (this.isRunning) {
            throw new Error('SERVER: HTTP server is already running');
        }
        return new Promise((resolve, reject) => {
            try {
                this.server = http.createServer(this.handleRequest.bind(this));
                this.server.on('error', (error) => {
                    console.error('SERVER: HTTP server error:', error);
                    this.isRunning = false;
                    if (error.code === 'EADDRINUSE') {
                        reject(new Error(`SERVER: Port ${this.config.port} is already in use`));
                    }
                    else {
                        reject(new Error(`SERVER: Failed to start HTTP server: ${error.message}`));
                    }
                });
                this.server.on('listening', () => {
                    const address = this.server?.address();
                    const portNumber = Number(this.config.port);
                    console.log(`SERVER: HTTP server listening on http://${this.config.host}:${this.config.port}`);
                    console.log('SERVER: Server address info:', address);
                    // Display network information for external access
                    networkUtils_1.NetworkUtils.displayNetworkInfo(portNumber, 'http');
                    this.isRunning = true;
                    // Return the localhost URL for browser/panel access
                    const localhostUrl = networkUtils_1.NetworkUtils.getLocalhostUrl(portNumber, 'http');
                    resolve(localhostUrl);
                });
                // Add graceful shutdown handling
                this.server.on('close', () => {
                    console.log('SERVER: HTTP server closed');
                    this.isRunning = false;
                });
                this.server.listen(this.config.port, this.config.host);
            }
            catch (error) {
                console.error('SERVER: Error starting HTTP server:', error);
                reject(error);
            }
        });
    }
    /**
     * Stop the HTTP server
     * @returns Promise<void>
     */
    async stop() {
        if (!this.server || !this.isRunning) {
            console.log('SERVER: HTTP server is not running');
            return;
        }
        return new Promise((resolve, reject) => {
            this.server.close((error) => {
                if (error) {
                    console.error('SERVER: Error stopping HTTP server:', error);
                    reject(error);
                }
                else {
                    console.log('SERVER: HTTP server stopped successfully');
                    this.isRunning = false;
                    this.server = null;
                    resolve();
                }
            });
        });
    }
    /**
     * Check if server is running
     * @returns boolean
     */
    getIsRunning() {
        return this.isRunning;
    }
    /**
     * Get server configuration
     * @returns HttpServerConfig
     */
    getConfig() {
        return { ...this.config };
    }
    /**
     * Get server URL
     * @returns string | null
     */
    getServerUrl() {
        if (!this.isRunning) {
            return null;
        }
        return `http://${this.config.host}:${this.config.port}`;
    }
    /**
     * Handle incoming HTTP requests
     * @param req - HTTP request
     * @param res - HTTP response
     */
    handleRequest(req, res) {
        const startTime = Date.now();
        const requestUrl = req.url || '/';
        const method = req.method || 'GET';
        console.log(`SERVER: ${method} ${requestUrl} - Processing request`);
        // Add CORS headers if enabled
        if (this.config.enableCors) {
            this.addCorsHeaders(res);
        }
        // Handle preflight requests
        if (method === 'OPTIONS') {
            res.writeHead(200);
            res.end();
            return;
        }
        // Route the request
        this.routeRequest(req, res, requestUrl)
            .then(() => {
            const duration = Date.now() - startTime;
            console.log(`SERVER: ${method} ${requestUrl} - Completed in ${duration}ms`);
        })
            .catch((error) => {
            console.error(`SERVER: ${method} ${requestUrl} - Error:`, error);
            this.sendErrorResponse(res, 500, 'Internal Server Error');
        });
    }
    /**
     * Route incoming requests
     * @param req - HTTP request
     * @param res - HTTP response
     * @param url - Request URL
     */
    async routeRequest(req, res, url) {
        // Root path - serve main page
        if (url === '/' || url === '/index.html') {
            await this.serveMainPage(res);
            return;
        }
        // Health check endpoint
        if (url === '/health') {
            this.sendJsonResponse(res, 200, {
                status: 'healthy',
                timestamp: new Date().toISOString(),
                server: 'CodeXR HTTP Server'
            });
            return;
        }
        // Server-Sent Events endpoint for analysis updates
        if (url === '/events') {
            await this.handleSSERequest(req, res);
            return;
        }
        // API endpoints
        if (url.startsWith('/api/')) {
            await this.handleApiRequest(req, res, url);
            return;
        }
        // Static files
        await this.serveStaticFile(req, res, url);
    }
    /**
     * Serve the main CodeXR page
     * @param res - HTTP response
     */
    async serveMainPage(res) {
        let mainPagePath;
        if (this.config.mainFile) {
            // If a specific main file is configured, use it
            if (path.isAbsolute(this.config.mainFile)) {
                mainPagePath = this.config.mainFile;
            }
            else {
                mainPagePath = path.join(this.config.staticRoot, this.config.mainFile);
            }
            console.log(`SERVER: Attempting to serve configured main file: ${mainPagePath}`);
            console.log(`SERVER: Static root: ${this.config.staticRoot}`);
            console.log(`SERVER: Main file: ${this.config.mainFile}`);
        }
        else {
            // Default to xr-visualization.html
            mainPagePath = path.join(this.config.staticRoot, 'xr', 'xr-visualization.html');
            console.log(`SERVER: Serving default main file: ${mainPagePath}`);
        }
        // Check if file exists and serve it
        if (fs.existsSync(mainPagePath)) {
            console.log(`SERVER: Successfully found main file, serving: ${mainPagePath}`);
            await this.serveFile(res, mainPagePath, 'text/html');
        }
        else {
            console.error(`SERVER: Main file not found: ${mainPagePath}`);
            console.error(`SERVER: Current working directory: ${process.cwd()}`);
            console.error(`SERVER: Static root exists: ${fs.existsSync(this.config.staticRoot)}`);
            if (this.config.staticRoot) {
                try {
                    const files = fs.readdirSync(this.config.staticRoot);
                    console.error(`SERVER: Files in static root: ${files.join(', ')}`);
                }
                catch (e) {
                    console.error(`SERVER: Cannot read static root directory: ${e}`);
                }
            }
            // Fallback to a basic HTML page
            console.warn(`SERVER: Serving fallback HTML instead of selected file`);
            const fallbackHtml = this.generateFallbackHtml();
            res.writeHead(200, { 'Content-Type': 'text/html' });
            res.end(fallbackHtml);
        }
    }
    /**
     * Handle API requests
     * @param req - HTTP request
     * @param res - HTTP response
     * @param url - Request URL
     */
    async handleApiRequest(req, res, url) {
        const apiPath = url.replace('/api', '');
        switch (apiPath) {
            case '/status':
                this.sendJsonResponse(res, 200, {
                    server: 'CodeXR HTTP Server',
                    mode: 'HTTP',
                    port: this.config.port,
                    uptime: process.uptime(),
                    timestamp: new Date().toISOString()
                });
                break;
            case '/config':
                this.sendJsonResponse(res, 200, {
                    mode: 'HTTP',
                    host: this.config.host,
                    port: this.config.port,
                    cors: this.config.enableCors
                });
                break;
            default:
                this.sendErrorResponse(res, 404, 'API endpoint not found');
        }
    }
    /**
     * Serve static files
     * @param req - HTTP request
     * @param res - HTTP response
     * @param url - Request URL
     */
    async serveStaticFile(req, res, url) {
        // Parse URL to extract pathname without query string
        const parsedUrl = (0, url_1.parse)(url, true);
        const pathname = parsedUrl.pathname || '/';
        const filePath = path.join(this.config.staticRoot, pathname);
        const normalizedPath = path.normalize(filePath);
        console.log(`SERVER_DEBUG: Request for static file: ${url}`);
        console.log(`SERVER_DEBUG: Parsed pathname: ${pathname}`);
        console.log(`SERVER_DEBUG: Query string removed: ${url} -> ${pathname}`);
        console.log(`SERVER_DEBUG: Static root: ${this.config.staticRoot}`);
        console.log(`SERVER_DEBUG: Full file path: ${normalizedPath}`);
        console.log(`SERVER_DEBUG: File exists: ${fs.existsSync(normalizedPath)}`);
        // Security check: ensure the file is within the static root
        if (!normalizedPath.startsWith(path.normalize(this.config.staticRoot))) {
            console.log(`SERVER_DEBUG: Access denied - path outside static root`);
            this.sendErrorResponse(res, 403, 'Access denied');
            return;
        }
        if (fs.existsSync(normalizedPath) && fs.statSync(normalizedPath).isFile()) {
            console.log(`SERVER_DEBUG: Serving file: ${normalizedPath}`);
            await this.serveFile(res, normalizedPath);
        }
        else {
            console.log(`SERVER_DEBUG: File not found: ${normalizedPath}`);
            // List directory contents for debugging
            try {
                const dirPath = path.dirname(normalizedPath);
                const dirContents = fs.readdirSync(dirPath);
                console.log(`SERVER_DEBUG: Directory contents of ${dirPath}:`, dirContents);
            }
            catch (dirError) {
                console.log(`SERVER_DEBUG: Could not read directory: ${dirError}`);
            }
            this.sendErrorResponse(res, 404, 'File not found');
        }
    }
    /**
     * Serve a file
     * @param res - HTTP response
     * @param filePath - Path to the file
     * @param contentType - Content type (optional, will be detected)
     */
    async serveFile(res, filePath, contentType) {
        try {
            const content = await fs.promises.readFile(filePath);
            const detectedContentType = contentType || this.getContentType(filePath);
            res.writeHead(200, { 'Content-Type': detectedContentType });
            res.end(content);
        }
        catch (error) {
            console.error('SERVER: Error serving file:', error);
            this.sendErrorResponse(res, 500, 'Error reading file');
        }
    }
    /**
     * Add CORS headers to response
     * @param res - HTTP response
     */
    addCorsHeaders(res) {
        const allowedOrigins = this.config.allowedOrigins?.join(', ') || '*';
        res.setHeader('Access-Control-Allow-Origin', allowedOrigins);
        res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, OPTIONS');
        res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
        res.setHeader('Access-Control-Max-Age', '86400'); // 24 hours
    }
    /**
     * Send JSON response
     * @param res - HTTP response
     * @param statusCode - HTTP status code
     * @param data - Data to send
     */
    sendJsonResponse(res, statusCode, data) {
        res.writeHead(statusCode, { 'Content-Type': 'application/json' });
        res.end(JSON.stringify(data, null, 2));
    }
    /**
     * Send error response
     * @param res - HTTP response
     * @param statusCode - HTTP status code
     * @param message - Error message
     */
    sendErrorResponse(res, statusCode, message) {
        const errorData = {
            error: true,
            status: statusCode,
            message: message,
            timestamp: new Date().toISOString()
        };
        this.sendJsonResponse(res, statusCode, errorData);
    }
    /**
     * Handle Server-Sent Events request for analysis updates
     * @param req - HTTP request
     * @param res - HTTP response
     */
    async handleSSERequest(req, res) {
        console.log('REQUEST_UPDATE: SSE connection request received on server');
        console.log(`REQUEST_UPDATE: Server port: ${this.config.port}`);
        // Find which analysis file this server is serving
        const serverPort = this.config.port;
        const fileUri = fileToServerMap_1.fileToServerMap.findFileByPort(serverPort);
        console.log(`REQUEST_UPDATE: Looking up file for port ${serverPort}`);
        console.log(`REQUEST_UPDATE: Found file mapping: ${fileUri || 'NOT FOUND'}`);
        if (!fileUri) {
            console.warn(`REQUEST_UPDATE: No file mapping found for server on port ${serverPort}`);
            console.warn(`REQUEST_UPDATE: Available mappings: ${fileToServerMap_1.fileToServerMap.getAllFileUris().join(', ')}`);
            res.writeHead(404, { 'Content-Type': 'text/plain' });
            res.end('No analysis file associated with this server');
            return;
        }
        console.log(`REQUEST_UPDATE: Setting up SSE for analysis file: ${fileUri}`);
        // Register the SSE client with the manager
        SSEManager_1.sseManager.registerClient(fileUri, res);
        console.log(`REQUEST_UPDATE: SSE client registration completed for ${fileUri}`);
    }
    /**
     * Get content type based on file extension
     * @param filePath - Path to the file
     * @returns string - Content type
     */
    getContentType(filePath) {
        const ext = path.extname(filePath).toLowerCase();
        const contentTypes = {
            '.html': 'text/html',
            '.css': 'text/css',
            '.js': 'application/javascript',
            '.json': 'application/json',
            '.png': 'image/png',
            '.jpg': 'image/jpeg',
            '.jpeg': 'image/jpeg',
            '.gif': 'image/gif',
            '.svg': 'image/svg+xml',
            '.ico': 'image/x-icon',
            '.txt': 'text/plain'
        };
        return contentTypes[ext] || 'application/octet-stream';
    }
    /**
     * Generate fallback HTML page
     * @returns string - HTML content
     */
    /**
     * Generate fallback HTML when main file is not found
     * @returns string - HTML content
     */
    generateFallbackHtml() {
        return `<!DOCTYPE html>
<html>
<head>
    <title>CodeXR Server - File Not Found</title>
    <style>
        body { font-family: Arial; max-width: 800px; margin: 0 auto; padding: 20px; background: #1e1e1e; color: #d4d4d4; }
        .error { background: #dc2626; color: white; padding: 15px; margin: 20px 0; }
        .info { background: #374151; padding: 15px; margin: 10px 0; }
    </style>
</head>
<body>
    <h1>CodeXR Server - File Not Found</h1>
    <div class="error">
        <h3>Selected HTML File Not Found</h3>
        <p>The HTML file you selected could not be located.</p>
        <p><strong>Attempted File:</strong> ${this.config.mainFile || 'Not specified'}</p>
        <p><strong>Static Root:</strong> ${this.config.staticRoot}</p>
    </div>
    <div class="info">
        <h3>Server Information</h3>
        <p><strong>Port:</strong> ${this.config.port}</p>
        <p><strong>Host:</strong> ${this.config.host}</p>
    </div>
    <div class="info">
        <h3>Troubleshooting</h3>
        <p>1. Check that the file still exists</p>
        <p>2. Verify file permissions</p>
        <p>3. Try restarting the server</p>
    </div>
</body>
</html>`;
    }
}
exports.HttpServer = HttpServer;


/***/ }),
/* 16 */
/***/ ((module) => {

module.exports = require("http");

/***/ }),
/* 17 */
/***/ ((module) => {

module.exports = require("url");

/***/ }),
/* 18 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.sseManager = exports.SSEManager = void 0;
const sseClientRegistry_1 = __webpack_require__(19);
const fileToServerMap_1 = __webpack_require__(20);
/**
 * SSE Manager
 * Central module for managing Server-Sent Events for analysis updates
 */
class SSEManager {
    static instance = null;
    clientRegistry;
    constructor() {
        console.log('[SSE_MANAGER] Initializing SSE manager');
        this.clientRegistry = sseClientRegistry_1.SSEClientRegistry.getInstance();
    }
    /**
     * Get the singleton instance
     */
    static getInstance() {
        if (!SSEManager.instance) {
            SSEManager.instance = new SSEManager();
        }
        return SSEManager.instance;
    }
    /**
     * Register a new SSE client for a specific analysis file
     * Sets up the SSE connection with proper headers and keep-alive
     *
     * @param fileUri - URI of the analyzed file
     * @param res - HTTP response object
     */
    registerClient(fileUri, res) {
        console.log(`[SSE_MANAGER] Setting up SSE connection for: ${fileUri}`);
        // Set SSE headers
        res.writeHead(200, {
            'Content-Type': 'text/event-stream',
            'Cache-Control': 'no-cache',
            'Connection': 'keep-alive',
            'Access-Control-Allow-Origin': '*',
            'Access-Control-Allow-Headers': 'Cache-Control',
            'Access-Control-Allow-Methods': 'GET, POST, OPTIONS'
        });
        // Send initial connection message
        res.write(`data: ${JSON.stringify({
            type: 'connected',
            fileUri: fileUri,
            timestamp: new Date().toISOString(),
            message: 'SSE connection established for analysis updates'
        })}\n\n`);
        // Register the client
        this.clientRegistry.registerClient(fileUri, res);
        console.log(`[SSE_MANAGER] SSE client registered for: ${fileUri}`);
    }
    /**
     * Unregister an SSE client
     *
     * @param fileUri - URI of the analyzed file
     * @param res - HTTP response object
     */
    unregisterClient(fileUri, res) {
        console.log(`[SSE_MANAGER] Unregistering SSE client for: ${fileUri}`);
        this.clientRegistry.unregisterClient(fileUri, res);
    }
    /**
     * Send an update notification to all clients of a specific analysis file
     * This is called when the data.json file is regenerated
     *
     * @param fileUri - URI of the analyzed file that was updated
     */
    sendUpdate(fileUri) {
        console.log(`REQUEST_UPDATE: Server received update request for file: ${fileUri}`);
        console.log(`[SSE_MANAGER] Sending analysis update for: ${fileUri}`);
        // Debug: Check file-to-server mapping
        const mapping = fileToServerMap_1.fileToServerMap.getServerInfo(fileUri);
        if (mapping) {
            console.log(`REQUEST_UPDATE: Found mapping - Port: ${mapping.port}, TempDir: ${mapping.tempDir}`);
            // Verify data.json exists in the temp directory
            const dataJsonPath = (__webpack_require__(5).join)(mapping.tempDir, 'data.json');
            const fs = __webpack_require__(6);
            try {
                const exists = fs.existsSync(dataJsonPath);
                console.log(`REQUEST_UPDATE: data.json exists at ${dataJsonPath}: ${exists}`);
                if (exists) {
                    const stats = fs.statSync(dataJsonPath);
                    console.log(`REQUEST_UPDATE: data.json size: ${stats.size} bytes, modified: ${stats.mtime}`);
                }
            }
            catch (fileError) {
                console.error(`REQUEST_UPDATE: Error checking data.json:`, fileError);
            }
        }
        else {
            console.log(`REQUEST_UPDATE: No mapping found for file: ${fileUri}`);
        }
        const updateMessage = JSON.stringify({
            type: 'analysis-updated',
            fileUri: fileUri,
            timestamp: new Date().toISOString(),
            message: 'Analysis data has been updated, please refresh',
            action: 'reload-data'
        });
        this.clientRegistry.sendToClients(fileUri, updateMessage);
        console.log(`[SSE_MANAGER] Update notification sent for: ${fileUri}`);
        console.log(`REQUEST_UPDATE: Update message dispatched to clients for: ${fileUri}`);
    }
    /**
     * Send a dataRefresh event specifically for XR visualizations
     * This triggers the dataRefresh event listener in live_sse_fileXR.js
     *
     * @param fileUri - URI of the analyzed file that was updated
     */
    sendDataRefresh(fileUri) {
        console.log(`[SSE_MANAGER] Sending dataRefresh event for XR visualization: ${fileUri}`);
        // Create the dataRefresh event data
        const refreshData = JSON.stringify({
            type: 'dataRefresh',
            fileUri: fileUri,
            timestamp: new Date().toISOString(),
            message: 'XR data has been refreshed, updating visualization',
            action: 'xr-data-refresh'
        });
        // Send as a specific SSE event
        this.clientRegistry.sendEventToClients(fileUri, 'dataRefresh', refreshData);
        console.log(`[SSE_MANAGER] dataRefresh event sent for XR visualization: ${fileUri}`);
    }
    /**
     * Send a custom message to all clients of a specific analysis file
     *
     * @param fileUri - URI of the analyzed file
     * @param message - Custom message object
     */
    sendCustomMessage(fileUri, message) {
        console.log(`[SSE_MANAGER] Sending custom message for: ${fileUri}`);
        console.log(`[SSE_MANAGER] Message type: ${message.type || 'unknown'}`);
        const customMessage = JSON.stringify({
            ...message,
            timestamp: new Date().toISOString()
        });
        console.log(`[SSE_MANAGER] Full message content: ${customMessage}`);
        console.log(`[SSE_MANAGER] Message size: ${customMessage.length} characters`);
        // Check if we have clients for this fileUri
        const clientCount = this.clientRegistry.getClients(fileUri).length;
        console.log(`[SSE_MANAGER] Number of clients for ${fileUri}: ${clientCount}`);
        this.clientRegistry.sendToClients(fileUri, customMessage);
        console.log(`[SSE_MANAGER] Message sent to ${clientCount} clients of: ${fileUri}`);
    }
    /**
     * Send a specific SSE event to all clients of a specific analysis file
     *
     * @param fileUri - URI of the analyzed file
     * @param eventType - Type of SSE event (e.g., 'analysisUpdated')
     * @param message - Message object or string
     */
    sendEventMessage(fileUri, eventType, message) {
        console.log(`[SSE_MANAGER] Sending SSE event '${eventType}' for: ${fileUri}`);
        let eventMessage;
        if (typeof message === 'string') {
            eventMessage = message;
        }
        else {
            eventMessage = JSON.stringify({
                ...message,
                timestamp: new Date().toISOString()
            });
        }
        this.clientRegistry.sendEventToClients(fileUri, eventType, eventMessage);
    }
    /**
     * Send a heartbeat/keep-alive message to all clients of a specific file
     *
     * @param fileUri - URI of the analyzed file
     */
    sendHeartbeat(fileUri) {
        const heartbeatMessage = JSON.stringify({
            type: 'heartbeat',
            timestamp: new Date().toISOString()
        });
        this.clientRegistry.sendToClients(fileUri, heartbeatMessage);
    }
    /**
     * Send heartbeat to all active files
     * Useful for maintaining connections
     */
    sendHeartbeatToAll() {
        const activeFiles = this.clientRegistry.getActiveFiles();
        if (activeFiles.length > 0) {
            console.log(`[SSE_MANAGER] Sending heartbeat to ${activeFiles.length} active file(s)`);
            for (const fileUri of activeFiles) {
                this.sendHeartbeat(fileUri);
            }
        }
    }
    /**
     * Get the number of active clients for a specific file
     *
     * @param fileUri - URI of the analyzed file
     * @returns Number of active clients
     */
    getClientCount(fileUri) {
        return this.clientRegistry.getClients(fileUri).length;
    }
    /**
     * Get all file URIs that have active SSE clients
     *
     * @returns Array of file URIs with active clients
     */
    getActiveFiles() {
        return this.clientRegistry.getActiveFiles();
    }
    /**
     * Remove all clients for a specific file
     * Useful when an analysis is stopped or file is deleted
     *
     * @param fileUri - URI of the analyzed file
     */
    removeAllClients(fileUri) {
        console.log(`[SSE_MANAGER] Removing all SSE clients for: ${fileUri}`);
        this.clientRegistry.removeAllClients(fileUri);
    }
    /**
     * Get statistics about active SSE connections
     *
     * @returns Statistics object
     */
    getStats() {
        return this.clientRegistry.getStats();
    }
    /**
     * Clean up all SSE connections
     * Should be called during extension deactivation
     */
    dispose() {
        console.log('[SSE_MANAGER] Disposing SSE manager');
        this.clientRegistry.clearAll();
    }
}
exports.SSEManager = SSEManager;
// Export singleton instance for easy access
exports.sseManager = SSEManager.getInstance();


/***/ }),
/* 19 */
/***/ ((__unused_webpack_module, exports) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SSEClientRegistry = void 0;
/**
 * SSE Client Registry
 * Maintains active Server-Sent Events connections for analysis updates
 */
class SSEClientRegistry {
    static instance = null;
    clients = new Map();
    constructor() {
        console.log('[SSE_CLIENT_REGISTRY] Initializing SSE client registry');
    }
    /**
     * Get the singleton instance
     */
    static getInstance() {
        if (!SSEClientRegistry.instance) {
            SSEClientRegistry.instance = new SSEClientRegistry();
        }
        return SSEClientRegistry.instance;
    }
    /**
     * Register a new SSE client for a specific file
     * @param fileUri - URI of the analyzed file
     * @param res - HTTP response object for SSE
     */
    registerClient(fileUri, res) {
        console.log(`[SSE_CLIENT_REGISTRY] Registering SSE client for: ${fileUri}`);
        if (!this.clients.has(fileUri)) {
            this.clients.set(fileUri, []);
        }
        const clientList = this.clients.get(fileUri);
        clientList.push(res);
        console.log(`[SSE_CLIENT_REGISTRY] Client registered. Total clients for ${fileUri}: ${clientList.length}`);
        // Set up cleanup when client disconnects
        res.on('close', () => {
            this.unregisterClient(fileUri, res);
        });
        res.on('error', (error) => {
            console.error(`[SSE_CLIENT_REGISTRY] Client error for ${fileUri}:`, error);
            this.unregisterClient(fileUri, res);
        });
    }
    /**
     * Unregister an SSE client
     * @param fileUri - URI of the analyzed file
     * @param res - HTTP response object for SSE
     */
    unregisterClient(fileUri, res) {
        console.log(`[SSE_CLIENT_REGISTRY] Unregistering SSE client for: ${fileUri}`);
        const clientList = this.clients.get(fileUri);
        if (!clientList) {
            return;
        }
        const index = clientList.indexOf(res);
        if (index > -1) {
            clientList.splice(index, 1);
            console.log(`[SSE_CLIENT_REGISTRY] Client unregistered. Remaining clients for ${fileUri}: ${clientList.length}`);
            // Clean up empty arrays
            if (clientList.length === 0) {
                this.clients.delete(fileUri);
                console.log(`[SSE_CLIENT_REGISTRY] Removed empty client list for: ${fileUri}`);
            }
        }
    }
    /**
     * Get all active clients for a file
     * @param fileUri - URI of the analyzed file
     * @returns Array of response objects
     */
    getClients(fileUri) {
        return this.clients.get(fileUri) || [];
    }
    /**
     * Send a message to all clients for a specific file
     * @param fileUri - URI of the analyzed file
     * @param message - Message to send
     */
    sendToClients(fileUri, message) {
        const clientList = this.clients.get(fileUri);
        console.log(`REQUEST_UPDATE: SSE Registry lookup for file: ${fileUri}`);
        console.log(`REQUEST_UPDATE: Found ${clientList ? clientList.length : 0} client(s) for file: ${fileUri}`);
        if (!clientList || clientList.length === 0) {
            console.log(`REQUEST_UPDATE: No SSE clients registered for: ${fileUri}`);
            console.log(`REQUEST_UPDATE: Available files in registry: ${Array.from(this.clients.keys()).join(', ')}`);
            return;
        }
        console.log(`[SSE_CLIENT_REGISTRY] Sending message to ${clientList.length} client(s) for: ${fileUri}`);
        console.log(`REQUEST_UPDATE: About to send message to ${clientList.length} client(s)`);
        // Send to all active clients, removing any that error out
        const activeClients = [];
        for (const client of clientList) {
            try {
                if (!client.headersSent) {
                    // Headers not sent yet, this shouldn't happen in SSE
                    console.warn(`[SSE_CLIENT_REGISTRY] Skipping client with unsent headers for: ${fileUri}`);
                    continue;
                }
                client.write(`data: ${message}\n\n`);
                activeClients.push(client);
                console.log(`REQUEST_UPDATE: Message sent to client successfully`);
            }
            catch (error) {
                console.error(`[SSE_CLIENT_REGISTRY] Error sending to client for ${fileUri}:`, error);
                console.error(`REQUEST_UPDATE: Failed to send to client: ${error}`);
                // Client will be removed via error event handler
            }
        }
        // Update the client list to only include active clients
        if (activeClients.length !== clientList.length) {
            if (activeClients.length === 0) {
                this.clients.delete(fileUri);
                console.log(`[SSE_CLIENT_REGISTRY] All clients disconnected for: ${fileUri}`);
            }
            else {
                this.clients.set(fileUri, activeClients);
                console.log(`[SSE_CLIENT_REGISTRY] Updated active clients for ${fileUri}: ${activeClients.length}`);
            }
        }
    }
    /**
     * Send a specific SSE event to all clients for a specific file
     * @param fileUri - URI of the analyzed file
     * @param eventType - Type of SSE event (e.g., 'analysisUpdated')
     * @param data - Data to send with the event
     */
    sendEventToClients(fileUri, eventType, data) {
        const clientList = this.clients.get(fileUri);
        console.log(`[SSE_CLIENT_REGISTRY] Sending SSE event '${eventType}' to clients for: ${fileUri}`);
        console.log(`[SSE_CLIENT_REGISTRY] Found ${clientList ? clientList.length : 0} client(s) for file: ${fileUri}`);
        if (!clientList || clientList.length === 0) {
            console.log(`[SSE_CLIENT_REGISTRY] No SSE clients registered for: ${fileUri}`);
            console.log(`[SSE_CLIENT_REGISTRY] Available files in registry: ${Array.from(this.clients.keys()).join(', ')}`);
            return;
        }
        console.log(`[SSE_CLIENT_REGISTRY] Sending SSE event '${eventType}' to ${clientList.length} client(s)`);
        // Send to all active clients, removing any that error out
        const activeClients = [];
        for (const client of clientList) {
            try {
                if (!client.headersSent) {
                    // Headers not sent yet, this shouldn't happen in SSE
                    console.warn(`[SSE_CLIENT_REGISTRY] Skipping client with unsent headers for: ${fileUri}`);
                    continue;
                }
                // Send as specific SSE event: event: eventType\ndata: data\n\n
                client.write(`event: ${eventType}\ndata: ${data}\n\n`);
                activeClients.push(client);
                console.log(`[SSE_CLIENT_REGISTRY] SSE event '${eventType}' sent to client successfully`);
            }
            catch (error) {
                console.error(`[SSE_CLIENT_REGISTRY] Error sending SSE event '${eventType}' to client for ${fileUri}:`, error);
                // Client will be removed via error event handler
            }
        }
        // Update the client list to only include active clients
        if (activeClients.length !== clientList.length) {
            if (activeClients.length === 0) {
                this.clients.delete(fileUri);
                console.log(`[SSE_CLIENT_REGISTRY] All clients disconnected for: ${fileUri}`);
            }
            else {
                this.clients.set(fileUri, activeClients);
                console.log(`[SSE_CLIENT_REGISTRY] Updated active clients for ${fileUri}: ${activeClients.length}`);
            }
        }
    }
    /**
     * Get all file URIs with active clients
     * @returns Array of file URIs
     */
    getActiveFiles() {
        return Array.from(this.clients.keys());
    }
    /**
     * Get total number of active clients across all files
     * @returns Total client count
     */
    getTotalClientCount() {
        let total = 0;
        for (const clientList of this.clients.values()) {
            total += clientList.length;
        }
        return total;
    }
    /**
     * Remove all clients for a specific file
     * @param fileUri - URI of the analyzed file
     */
    removeAllClients(fileUri) {
        const clientList = this.clients.get(fileUri);
        if (!clientList) {
            return;
        }
        console.log(`[SSE_CLIENT_REGISTRY] Removing all ${clientList.length} client(s) for: ${fileUri}`);
        // Close all connections
        for (const client of clientList) {
            try {
                client.end();
            }
            catch (error) {
                console.error(`[SSE_CLIENT_REGISTRY] Error closing client for ${fileUri}:`, error);
            }
        }
        this.clients.delete(fileUri);
    }
    /**
     * Clear all clients (useful for cleanup)
     */
    clearAll() {
        console.log(`[SSE_CLIENT_REGISTRY] Clearing all clients for ${this.clients.size} file(s)`);
        for (const [fileUri, clientList] of this.clients.entries()) {
            console.log(`[SSE_CLIENT_REGISTRY] Closing ${clientList.length} client(s) for: ${fileUri}`);
            for (const client of clientList) {
                try {
                    client.end();
                }
                catch (error) {
                    console.error(`[SSE_CLIENT_REGISTRY] Error closing client:`, error);
                }
            }
        }
        this.clients.clear();
    }
    /**
     * Get statistics about the registry
     * @returns Registry statistics
     */
    getStats() {
        const clientsPerFile = {};
        for (const [fileUri, clientList] of this.clients.entries()) {
            clientsPerFile[fileUri] = clientList.length;
        }
        return {
            fileCount: this.clients.size,
            totalClients: this.getTotalClientCount(),
            clientsPerFile
        };
    }
}
exports.SSEClientRegistry = SSEClientRegistry;


/***/ }),
/* 20 */
/***/ ((__unused_webpack_module, exports) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.fileToServerMap = void 0;
/**
 * Central mapping between analysis files and their corresponding servers
 * This enables efficient lookup for SSE notifications and cleanup operations
 */
class FileToServerMapping {
    static instance = null;
    fileToServer = new Map();
    constructor() {
        console.log('[FILE_TO_SERVER_MAP] Initializing file-to-server mapping registry');
    }
    /**
     * Get the singleton instance
     */
    static getInstance() {
        if (!FileToServerMapping.instance) {
            FileToServerMapping.instance = new FileToServerMapping();
        }
        return FileToServerMapping.instance;
    }
    /**
     * Register a mapping between a file and its analysis server
     * @param fileUri - URI of the analyzed file
     * @param serverInfo - Information about the server serving this analysis
     */
    registerMapping(fileUri, serverInfo) {
        console.log(`REQUEST_UPDATE: Registering file-to-server mapping`);
        console.log(`REQUEST_UPDATE: File URI: ${fileUri}`);
        console.log(`REQUEST_UPDATE: Server port: ${serverInfo.port}`);
        console.log(`REQUEST_UPDATE: Temp dir: ${serverInfo.tempDir}`);
        console.log(`[FILE_TO_SERVER_MAP] Registering mapping: ${fileUri} -> port ${serverInfo.port}`);
        this.fileToServer.set(fileUri, serverInfo);
        console.log(`REQUEST_UPDATE: Total mappings after registration: ${this.fileToServer.size}`);
    }
    /**
     * Unregister a mapping for a file
     * @param fileUri - URI of the analyzed file
     */
    unregisterMapping(fileUri) {
        console.log(`[FILE_TO_SERVER_MAP] Unregistering mapping for: ${fileUri}`);
        this.fileToServer.delete(fileUri);
    }
    /**
     * Get server information for a file
     * @param fileUri - URI of the analyzed file
     * @returns ServerInfo or null if not found
     */
    getServerInfo(fileUri) {
        return this.fileToServer.get(fileUri) || null;
    }
    /**
     * Find file URI by server port
     * @param port - Server port number
     * @returns File URI or null if not found
     */
    findFileByPort(port) {
        console.log(`REQUEST_UPDATE: Looking up file for port ${port}`);
        console.log(`REQUEST_UPDATE: Available ports: ${Array.from(this.fileToServer.values()).map(info => info.port).join(', ')}`);
        for (const [fileUri, serverInfo] of this.fileToServer.entries()) {
            if (serverInfo.port === port) {
                console.log(`REQUEST_UPDATE: Found file ${fileUri} for port ${port}`);
                return fileUri;
            }
        }
        console.log(`REQUEST_UPDATE: No file found for port ${port}`);
        return null;
    }
    /**
     * Find file URI by server reference
     * @param serverRef - HTTP server reference
     * @returns File URI or null if not found
     */
    findFileByServerRef(serverRef) {
        for (const [fileUri, serverInfo] of this.fileToServer.entries()) {
            if (serverInfo.serverRef === serverRef) {
                return fileUri;
            }
        }
        return null;
    }
    /**
     * Find file URI by temp directory path
     * @param tempDir - Temporary directory path
     * @returns File URI or null if not found
     */
    findFileByTempDir(tempDir) {
        for (const [fileUri, serverInfo] of this.fileToServer.entries()) {
            if (serverInfo.tempDir === tempDir) {
                return fileUri;
            }
        }
        return null;
    }
    /**
     * Get all registered mappings
     * @returns Array of [fileUri, ServerInfo] entries
     */
    getAllMappings() {
        return Array.from(this.fileToServer.entries());
    }
    /**
     * Get all file URIs that have servers
     * @returns Array of file URIs
     */
    getAllFileUris() {
        return Array.from(this.fileToServer.keys());
    }
    /**
     * Check if a file has a registered server
     * @param fileUri - URI of the analyzed file
     * @returns True if mapping exists
     */
    hasMapping(fileUri) {
        return this.fileToServer.has(fileUri);
    }
    /**
     * Clear all mappings (useful for cleanup)
     */
    clearAll() {
        console.log(`[FILE_TO_SERVER_MAP] Clearing all ${this.fileToServer.size} mappings`);
        this.fileToServer.clear();
    }
    /**
     * Get the number of registered mappings
     */
    size() {
        return this.fileToServer.size;
    }
}
// Export singleton instance
exports.fileToServerMap = FileToServerMapping.getInstance();


/***/ }),
/* 21 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Network Utilities for Server Configuration
 * Provides network-related helper functions for server setup
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.NetworkUtils = void 0;
const os = __importStar(__webpack_require__(22));
class NetworkUtils {
    /**
     * Get the local IP address for external device access
     * @returns string - Local IP address or fallback to localhost
     */
    static getLocalIPAddress() {
        const interfaces = os.networkInterfaces();
        // Look for non-internal IPv4 addresses
        for (const name of Object.keys(interfaces)) {
            if (interfaces[name]) {
                for (const iface of interfaces[name]) {
                    // Skip internal (localhost) and IPv6 addresses
                    if (iface.family === 'IPv4' && !iface.internal) {
                        console.log(`SERVER: Found local IP address: ${iface.address} (${name})`);
                        return iface.address;
                    }
                }
            }
        }
        console.warn('SERVER: Could not find local IP address, falling back to localhost');
        return 'localhost';
    }
    /**
     * Generate server URLs for different access methods
     * @param port - Server port
     * @param protocol - 'http' or 'https'
     * @returns Object with different URL options
     */
    static generateServerUrls(port, protocol = 'https') {
        const localIP = this.getLocalIPAddress();
        return {
            localhost: `${protocol}://localhost:${port}`,
            localIP: `${protocol}://${localIP}:${port}`,
            allInterfaces: [
                `${protocol}://localhost:${port}`,
                `${protocol}://${localIP}:${port}`,
                `${protocol}://0.0.0.0:${port}`
            ]
        };
    }
    /**
     * Get the primary external URL for client connections
     * @param port - Server port
     * @param protocol - 'http' or 'https'
     * @returns string - Primary external URL
     */
    static getPrimaryExternalUrl(port, protocol = 'https') {
        const localIP = this.getLocalIPAddress();
        return `${protocol}://${localIP}:${port}`;
    }
    /**
     * Get the localhost URL for browser/panel access
     * @param port - Server port
     * @param protocol - 'http' or 'https'
     * @returns string - Localhost URL for local access
     */
    static getLocalhostUrl(port, protocol = 'https') {
        return `${protocol}://localhost:${port}`;
    }
    /**
     * Display network configuration information
     * @param port - Server port
     * @param protocol - 'http' or 'https'
     */
    static displayNetworkInfo(port, protocol = 'https') {
        const urls = this.generateServerUrls(port, protocol);
        console.log('\n=== SERVER NETWORK CONFIGURATION ===');
        console.log('====================================');
        console.log(`Primary External URL: ${urls.localIP}`);
        console.log(`Localhost URL: ${urls.localhost}`);
        console.log('');
        console.log('For VR/Mobile devices use: ' + urls.localIP);
        console.log('For local testing use: ' + urls.localhost);
        console.log('');
        console.log('Network Diagnostics:');
        console.log(`  - Server listening on: 0.0.0.0:${port}`);
        console.log(`  - Local IP detected: ${this.getLocalIPAddress()}`);
        console.log(`  - Protocol: ${protocol.toUpperCase()}`);
        console.log('====================================\n');
        // Additional diagnostic info
        this.performNetworkDiagnostics(port);
    }
    /**
     * Perform network diagnostics
     * @param port - Server port to diagnose
     */
    static performNetworkDiagnostics(port) {
        console.log('=== NETWORK DIAGNOSTICS ===');
        // Check all network interfaces
        const interfaces = os.networkInterfaces();
        console.log('Available network interfaces:');
        for (const [name, details] of Object.entries(interfaces)) {
            if (details) {
                for (const detail of details) {
                    if (detail.family === 'IPv4') {
                        const status = detail.internal ? '(internal)' : '(external)';
                        console.log(`  ${name}: ${detail.address} ${status}`);
                    }
                }
            }
        }
        console.log(`\nServer should be accessible at:`);
        console.log(`  - Local: http://127.0.0.1:${port}`);
        console.log(`  - Network: http://${this.getLocalIPAddress()}:${port}`);
        console.log('===========================\n');
    }
}
exports.NetworkUtils = NetworkUtils;


/***/ }),
/* 22 */
/***/ ((module) => {

module.exports = require("os");

/***/ }),
/* 23 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.HttpsDefaultServer = void 0;
const https = __importStar(__webpack_require__(24));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const vscode = __importStar(__webpack_require__(1));
const httpServer_1 = __webpack_require__(15);
const networkUtils_1 = __webpack_require__(21);
const portManager_1 = __webpack_require__(25);
/**
 * HTTPS Server with default certificates
 * Extends HTTP server functionality with SSL/TLS support using default certificates
 */
class HttpsDefaultServer {
    server = null;
    config;
    isRunning = false;
    httpHandler;
    /**
     * Get default certificate paths based on extension context
     * @private
     */
    getDefaultCertificatePaths(extensionContext) {
        if (extensionContext) {
            // Use extension context to resolve absolute paths
            const certPath = path.join(extensionContext.extensionPath, 'certs', 'babia_cert.pem');
            const keyPath = path.join(extensionContext.extensionPath, 'certs', 'babia_key.pem');
            console.log('SERVER: Using extension context for default certificates');
            console.log('SERVER: Extension path:', extensionContext.extensionPath);
            return { certPath, keyPath };
        }
        else {
            // Fallback to relative paths
            console.log('SERVER: Using fallback relative paths for default certificates');
            return {
                certPath: path.join(__dirname, '../../../certs/babia_cert.pem'),
                keyPath: path.join(__dirname, '../../../certs/babia_key.pem')
            };
        }
    }
    constructor(config) {
        // Get default certificate paths based on extension context
        const defaultCertPaths = this.getDefaultCertificatePaths(config.extensionContext);
        this.config = {
            host: '0.0.0.0', // ✅ Listen on all network interfaces for VR/mobile access
            staticRoot: path.join(__dirname, '../../../templates'),
            enableCors: true,
            allowedOrigins: ['*'],
            certPath: defaultCertPaths.certPath,
            keyPath: defaultCertPaths.keyPath,
            ...config
        };
        // Create HTTP handler instance for request processing with ALL configuration
        this.httpHandler = new httpServer_1.HttpServer({
            port: this.config.port,
            host: this.config.host,
            staticRoot: this.config.staticRoot,
            enableCors: this.config.enableCors,
            allowedOrigins: this.config.allowedOrigins,
            mainFile: this.config.mainFile // ← FIX: Pass mainFile to HTTP handler
        });
        console.log('SERVER: HTTPS server (default certs) initialized with config:', {
            ...this.config,
            certPath: '***REDACTED***',
            keyPath: '***REDACTED***'
        });
        console.log('SERVER: Using default certificates from:', this.config.certPath);
    }
    /**
     * Start the HTTPS server with default certificates
     * @returns Promise<string> - Server URL
     */
    async start() {
        if (this.isRunning) {
            throw new Error('SERVER: HTTPS server is already running');
        }
        // Validate certificate files
        await this.validateCertificates();
        // Find an available port starting from the configured port
        console.log(`SERVER: 🔍 Looking for available port starting from ${this.config.port} on host ${this.config.host}...`);
        const availablePort = await portManager_1.PortManager.findAvailablePort(this.config.port, this.config.port + 100, this.config.host);
        if (availablePort !== this.config.port) {
            console.log(`SERVER: ⚠️  Port ${this.config.port} was busy, using ${availablePort} instead on host ${this.config.host}`);
            this.config.port = availablePort;
        }
        else {
            console.log(`SERVER: ✅ Port ${this.config.port} is available on host ${this.config.host}`);
        }
        return new Promise((resolve, reject) => {
            try {
                // Load SSL certificates
                const sslOptions = this.loadSslOptions();
                // Create HTTPS server using the HTTP handler's request processing
                this.server = https.createServer(sslOptions, (req, res) => {
                    // Use the HTTP handler's request processing logic
                    this.httpHandler.handleRequest(req, res);
                });
                this.server.on('error', (error) => {
                    console.error('SERVER: HTTPS server error:', error);
                    this.isRunning = false;
                    if (error.code === 'EADDRINUSE') {
                        reject(new Error(`SERVER: Port ${this.config.port} is already in use`));
                    }
                    else if (error.code === 'EACCES') {
                        reject(new Error(`SERVER: Permission denied. May need to run as administrator for port ${this.config.port}`));
                    }
                    else {
                        reject(new Error(`SERVER: Failed to start HTTPS server: ${error.message}`));
                    }
                });
                this.server.on('listening', () => {
                    const address = this.server?.address();
                    const port = this.config.port;
                    console.log(`SERVER: ✅ HTTPS server successfully listening!`);
                    console.log(`SERVER: 🔍 Address object:`, address);
                    console.log(`SERVER: 🔍 Config host: "${this.config.host}"`);
                    console.log(`SERVER: 🔍 Config port: ${this.config.port}`);
                    console.log(`SERVER: 🔍 Actual binding: ${typeof address === 'object' && address ? `${address.family || 'unknown'} ${address.address || 'unknown'}:${address.port || 'unknown'}` : 'unknown'}`);
                    // Generate proper URLs for external access
                    const urls = networkUtils_1.NetworkUtils.generateServerUrls(port, 'https');
                    const primaryExternalUrl = networkUtils_1.NetworkUtils.getPrimaryExternalUrl(port, 'https');
                    console.log(`SERVER: HTTPS server listening on port ${port}`);
                    console.log('SERVER: Using default certificates from:', this.config.certPath);
                    console.log('SERVER: ⚠️  NOTE: Using self-signed certificate - browsers may show security warnings');
                    // Display comprehensive network information
                    networkUtils_1.NetworkUtils.displayNetworkInfo(port, 'https');
                    console.log('SERVER: Server address info:', address);
                    // Show VR-friendly instructions with the correct external URL
                    this.showVRCertificateInstructions(primaryExternalUrl, urls.localhost);
                    this.isRunning = true;
                    // Return the localhost URL for browser/panel access
                    const localhostUrl = networkUtils_1.NetworkUtils.getLocalhostUrl(port, 'https');
                    resolve(localhostUrl);
                });
                this.server.on('close', () => {
                    console.log('SERVER: HTTPS server closed');
                    this.isRunning = false;
                });
                // Handle TLS errors gracefully (common with self-signed certificates)
                this.server.on('tlsClientError', (err, tlsSocket) => {
                    // Only log specific errors to avoid spam, and provide helpful guidance
                    if (err.message.includes('SSLV3_ALERT_CERTIFICATE_UNKNOWN') ||
                        err.message.includes('certificate unknown') ||
                        err.message.includes('SSL alert number 46')) {
                        console.warn('SERVER: ⚠️  Client rejected self-signed certificate - this is expected behavior');
                        console.warn('SERVER: 🥽 VR Solution: Access the server URL in browser first and accept certificate');
                        console.warn(`SERVER: 🌐 Navigate to: https://${this.config.host}:${this.config.port} and click "Advanced" -> "Proceed to localhost"`);
                    }
                    else {
                        console.warn('SERVER: TLS client error (non-critical):', err.message);
                    }
                    // Don't terminate the connection, just log the warning
                    // This allows the client to potentially retry or handle the error
                });
                console.log(`SERVER: 🔧 About to call server.listen with:`);
                console.log(`SERVER: 🔧   port: ${this.config.port} (type: ${typeof this.config.port})`);
                console.log(`SERVER: 🔧   host: "${this.config.host}" (type: ${typeof this.config.host})`);
                // Use callback version of listen for better error handling and debugging
                this.server.listen(this.config.port, this.config.host, () => {
                    console.log(`SERVER: 🔧 Listen callback executed successfully`);
                });
            }
            catch (error) {
                console.error('SERVER: Error starting HTTPS server:', error);
                reject(error);
            }
        });
    }
    /**
     * Stop the HTTPS server
     * @returns Promise<void>
     */
    async stop() {
        if (!this.server || !this.isRunning) {
            console.log('SERVER: HTTPS server is not running');
            return;
        }
        return new Promise((resolve, reject) => {
            this.server.close((error) => {
                if (error) {
                    console.error('SERVER: Error stopping HTTPS server:', error);
                    reject(error);
                }
                else {
                    console.log('SERVER: HTTPS server stopped successfully');
                    this.isRunning = false;
                    this.server = null;
                    resolve();
                }
            });
        });
    }
    /**
     * Check if server is running
     * @returns boolean
     */
    getIsRunning() {
        return this.isRunning;
    }
    /**
     * Get server configuration
     * @returns HttpsDefaultServerConfig
     */
    getConfig() {
        return { ...this.config };
    }
    /**
     * Get server URL
     * @returns string | null
     */
    getServerUrl() {
        if (!this.isRunning) {
            return null;
        }
        return `https://${this.config.host}:${this.config.port}`;
    }
    /**
     * Get certificate information
     * @returns object with certificate details
     */
    getCertificateInfo() {
        const certExists = fs.existsSync(this.config.certPath);
        const keyExists = fs.existsSync(this.config.keyPath);
        return {
            certPath: this.config.certPath,
            keyPath: this.config.keyPath,
            certExists,
            keyExists,
            isValid: certExists && keyExists
        };
    }
    /**
     * Validate that certificate files exist and are readable
     * @private
     */
    async validateCertificates() {
        const certPath = this.config.certPath;
        const keyPath = this.config.keyPath;
        console.log('SERVER: Validating default certificates...');
        console.log('SERVER: Certificate path:', certPath);
        console.log('SERVER: Key path:', keyPath);
        // Check if certificate file exists
        if (!fs.existsSync(certPath)) {
            throw new Error(`SERVER: Certificate file not found: ${certPath}`);
        }
        // Check if key file exists
        if (!fs.existsSync(keyPath)) {
            throw new Error(`SERVER: Private key file not found: ${keyPath}`);
        }
        try {
            // Test reading the files
            await fs.promises.access(certPath, fs.constants.R_OK);
            await fs.promises.access(keyPath, fs.constants.R_OK);
            console.log('SERVER: Default certificates validated successfully');
        }
        catch (error) {
            throw new Error(`SERVER: Cannot read certificate files: ${error}`);
        }
    }
    /**
     * Load SSL options for HTTPS server
     * @private
     * @returns https.ServerOptions
     */
    loadSslOptions() {
        try {
            const cert = fs.readFileSync(this.config.certPath, 'utf8');
            const key = fs.readFileSync(this.config.keyPath, 'utf8');
            console.log('SERVER: SSL certificates loaded successfully');
            console.log('SERVER: 🔐 Configuring SSL options for self-signed certificate compatibility');
            return {
                cert: cert,
                key: key,
                // Self-signed certificate friendly options
                rejectUnauthorized: false, // Allow self-signed certificates
                requestCert: false, // Don't require client certificates
                // Security protocols - more permissive for VR compatibility
                secureProtocol: 'TLS_method', // Allow wider range of TLS versions
                // Cipher suites compatible with VR browsers - let client choose
                honorCipherOrder: false, // Let client choose preferred cipher
                ciphers: [
                    'ECDHE-RSA-AES128-GCM-SHA256',
                    'ECDHE-RSA-AES256-GCM-SHA384',
                    'ECDHE-RSA-AES128-SHA256',
                    'ECDHE-RSA-AES256-SHA384',
                    'DHE-RSA-AES128-GCM-SHA256', // Added for wider compatibility
                    'DHE-RSA-AES256-GCM-SHA384',
                    'AES128-GCM-SHA256', // Fallback for older clients
                    'AES256-GCM-SHA384'
                ].join(':'),
                // Additional options for VR headset compatibility
                secureOptions: 0, // Allow all SSL/TLS versions
                // SNI (Server Name Indication) callback for flexibility
                SNICallback: (servername, callback) => {
                    console.log(`SERVER: 🥽 SNI request for: ${servername}`);
                    callback(null); // Accept any servername
                }
            };
        }
        catch (error) {
            throw new Error(`SERVER: Failed to load SSL certificates: ${error}`);
        }
    }
    /**
     * Get server status including certificate information
     * @returns object with detailed server status
     */
    getDetailedStatus() {
        return {
            isRunning: this.isRunning,
            url: this.getServerUrl(),
            config: this.getConfig(),
            certificates: this.getCertificateInfo(),
            uptime: this.isRunning ? process.uptime() : undefined
        };
    }
    /**
     * Test certificate validity without starting the server
     * @returns Promise<boolean>
     */
    async testCertificates() {
        try {
            await this.validateCertificates();
            // Try to create SSL options to validate certificate format
            this.loadSslOptions();
            console.log('SERVER: Certificate test passed');
            return true;
        }
        catch (error) {
            console.error('SERVER: Certificate test failed:', error);
            return false;
        }
    }
    /**
     * Get certificate expiration information (if available)
     * @returns Promise<object | null>
     */
    async getCertificateExpiration() {
        try {
            // This is a basic implementation
            // For more detailed certificate parsing, we could use a library like 'node-forge'
            const cert = fs.readFileSync(this.config.certPath, 'utf8');
            // Extract dates from certificate (basic regex parsing)
            const notBeforeMatch = cert.match(/Not Before: (.+)/);
            const notAfterMatch = cert.match(/Not After : (.+)/);
            if (notBeforeMatch && notAfterMatch) {
                const notBefore = new Date(notBeforeMatch[1]);
                const notAfter = new Date(notAfterMatch[1]);
                const now = new Date();
                const isExpired = now > notAfter;
                const daysUntilExpiry = Math.ceil((notAfter.getTime() - now.getTime()) / (1000 * 60 * 60 * 24));
                return {
                    notBefore,
                    notAfter,
                    isExpired,
                    daysUntilExpiry
                };
            }
            return null;
        }
        catch (error) {
            console.error('SERVER: Error parsing certificate expiration:', error);
            return null;
        }
    }
    /**
     * Show VR-friendly certificate acceptance instructions
     * @private
     */
    showVRCertificateInstructions(externalUrl, localhostUrl) {
        const primaryUrl = externalUrl;
        const fallbackUrl = localhostUrl || externalUrl;
        const message = `🥽 VR/HTTPS Server ready! For VR headsets to work:\n\n` +
            `1️⃣ Primary URL (VR/Mobile): ${primaryUrl}\n` +
            `2️⃣ Local URL (Testing): ${fallbackUrl}\n\n` +
            `📱 For VR/Mobile devices:\n` +
            `   • Open browser and go to: ${primaryUrl}\n` +
            `   • Click "Advanced" → "Proceed to [IP] (unsafe)"\n` +
            `   • Now your VR headset can access securely!\n\n` +
            `🔐 This accepts the self-signed certificate for your session.`;
        console.log('SERVER: 🥽 VR Certificate Instructions:');
        console.log('SERVER: =====================================');
        console.log(`SERVER: 📱 VR/Mobile URL: ${primaryUrl}`);
        console.log(`SERVER: 💻 Local URL: ${fallbackUrl}`);
        console.log('SERVER: 1. Navigate to the VR/Mobile URL from your device');
        console.log('SERVER: 2. Accept certificate warning in browser');
        console.log('SERVER: 3. Then use VR headset to access the same URL');
        console.log('SERVER: =====================================');
        // Show information message to user (non-blocking)
        if (vscode && vscode.window) {
            vscode.window.showInformationMessage(`🥽 HTTPS Server ready! VR/Mobile: ${primaryUrl}`, 'Copy VR URL', 'Copy Local URL', 'Show Instructions').then(selection => {
                if (selection === 'Copy VR URL') {
                    vscode.env.clipboard.writeText(primaryUrl);
                    vscode.window.showInformationMessage('✅ VR/Mobile URL copied to clipboard!');
                }
                else if (selection === 'Copy Local URL') {
                    vscode.env.clipboard.writeText(fallbackUrl);
                    vscode.window.showInformationMessage('✅ Local URL copied to clipboard!');
                }
                else if (selection === 'Show Instructions') {
                    vscode.window.showInformationMessage(message, { modal: true });
                }
            });
        }
    }
}
exports.HttpsDefaultServer = HttpsDefaultServer;


/***/ }),
/* 24 */
/***/ ((module) => {

module.exports = require("https");

/***/ }),
/* 25 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PortManager = void 0;
const net = __importStar(__webpack_require__(26));
/**
 * Port availability checker and manager
 * Provides utilities to find available ports starting from a given port number
 */
class PortManager {
    static DEFAULT_START_PORT = 3000;
    static DEFAULT_END_PORT = 8080;
    static MAX_RETRIES = 50;
    /**
     * Check if a specific port is available
     * @param port - The port number to check
     * @param host - The host to bind to (default: '0.0.0.0')
     * @returns Promise<boolean> - True if port is available, false otherwise
     */
    static async isPortAvailable(port, host = '0.0.0.0') {
        return new Promise((resolve) => {
            const server = net.createServer();
            server.listen(port, host, () => {
                server.close(() => {
                    console.log(`SERVER: Port ${port} is available on ${host}`);
                    resolve(true);
                });
            });
            server.on('error', (err) => {
                if (err.code === 'EADDRINUSE') {
                    console.log(`SERVER: Port ${port} is already in use on ${host}`);
                    resolve(false);
                }
                else {
                    console.error(`SERVER: Error checking port ${port}:`, err);
                    resolve(false);
                }
            });
        });
    }
    /**
     * Find the first available port starting from a given port number
     * @param startPort - The starting port number (default: 3000)
     * @param endPort - The ending port number (default: 8080)
     * @param host - The host to bind to (default: '0.0.0.0')
     * @returns Promise<number> - The first available port found
     * @throws Error if no available port is found within the range
     */
    static async findAvailablePort(startPort = PortManager.DEFAULT_START_PORT, endPort = PortManager.DEFAULT_END_PORT, host = '0.0.0.0') {
        console.log(`SERVER: Searching for available port starting from ${startPort} on host ${host}`);
        // Validate inputs
        if (startPort < 1 || startPort > 65535) {
            throw new Error(`SERVER: Invalid start port ${startPort}. Must be between 1 and 65535`);
        }
        if (endPort < startPort || endPort > 65535) {
            throw new Error(`SERVER: Invalid end port ${endPort}. Must be between ${startPort} and 65535`);
        }
        try {
            // Use get-port for more reliable port detection with sequential checking
            const getPort = (await __webpack_require__.e(/* import() */ 1).then(__webpack_require__.bind(__webpack_require__, 228))).default;
            // Create a sequential range array for get-port
            const maxRange = Math.min(endPort - startPort + 1, 100); // Check up to 100 ports sequentially
            const ports = [];
            for (let i = 0; i < maxRange; i++) {
                const port = startPort + i;
                if (port <= endPort) {
                    ports.push(port);
                }
            }
            console.log(`SERVER: Checking ${ports.length} ports sequentially from ${startPort} to ${startPort + maxRange - 1}`);
            const availablePort = await getPort({
                port: ports,
                host: '0.0.0.0' // Use 0.0.0.0 to check availability on all interfaces
            });
            console.log(`SERVER: Found available port: ${availablePort} (sequential search successful)`);
            return availablePort;
        }
        catch (error) {
            console.error(`SERVER: get-port failed, using manual sequential detection:`, error);
            // Fallback to manual sequential implementation  
            console.log(`SERVER: Manual port scanning from ${startPort} to ${endPort}`);
            let currentPort = startPort;
            let attempts = 0;
            const maxAttempts = Math.min(endPort - startPort + 1, PortManager.MAX_RETRIES);
            while (attempts < maxAttempts) {
                if (currentPort > endPort) {
                    break;
                }
                console.log(`SERVER: Testing port ${currentPort} availability on ${host}`);
                if (await PortManager.isPortAvailable(currentPort, host)) {
                    console.log(`SERVER: Port ${currentPort} is available and ready for use`);
                    return currentPort;
                }
                else {
                    console.log(`SERVER: Port ${currentPort} is occupied, trying next port ${currentPort + 1}`);
                }
                currentPort++;
                attempts++;
            }
            throw new Error(`SERVER: No available port found in range ${startPort}-${endPort} after checking ${attempts} ports sequentially`);
        }
    }
    /**
     * Find multiple available ports
     * @param count - Number of ports needed
     * @param startPort - The port to start searching from
     * @param endPort - The maximum port to check
     * @returns Promise<number[]> - Array of available ports
     */
    static async findMultipleAvailablePorts(count, startPort = PortManager.DEFAULT_START_PORT, endPort = PortManager.DEFAULT_END_PORT) {
        if (count <= 0) {
            throw new Error('SERVER: Port count must be greater than 0');
        }
        console.log(`SERVER: Searching for ${count} available ports starting sequentially from ${startPort}`);
        const availablePorts = [];
        let currentPort = startPort;
        while (availablePorts.length < count && currentPort <= endPort) {
            if (await PortManager.isPortAvailable(currentPort)) {
                availablePorts.push(currentPort);
                console.log(`SERVER: Found available port ${currentPort} (${availablePorts.length}/${count})`);
            }
            else {
                console.log(`SERVER: Port ${currentPort} is occupied, checking next port ${currentPort + 1}`);
            }
            currentPort++;
        }
        if (availablePorts.length < count) {
            throw new Error(`SERVER: Only found ${availablePorts.length} available ports out of ${count} needed in range ${startPort}-${endPort}`);
        }
        console.log(`SERVER: Successfully found ${count} available ports: [${availablePorts.join(', ')}]`);
        return availablePorts;
    }
    /**
     * Validate port number
     * @param port - Port number to validate
     * @returns boolean - True if port is valid
     */
    static isValidPort(port) {
        return Number.isInteger(port) && port >= 1 && port <= 65535;
    }
    /**
     * Get a random port within a range
     * @param min - Minimum port number
     * @param max - Maximum port number
     * @returns number - Random port number
     */
    static getRandomPort(min = 3000, max = 8080) {
        if (!PortManager.isValidPort(min) || !PortManager.isValidPort(max) || min > max) {
            throw new Error(`SERVER: Invalid port range ${min}-${max}`);
        }
        return Math.floor(Math.random() * (max - min + 1)) + min;
    }
    /**
     * Check if port is in privileged range (1-1023)
     * @param port - Port number to check
     * @returns boolean - True if port is privileged
     */
    static isPrivilegedPort(port) {
        return port >= 1 && port <= 1023;
    }
    /**
     * Get suggested ports based on service type
     * @param serviceType - Type of service ('http', 'https', 'dev')
     * @returns number[] - Array of suggested ports
     */
    static getSuggestedPorts(serviceType) {
        switch (serviceType) {
            case 'http':
                return [3000, 8000, 8080, 8008, 3001];
            case 'https':
                return [3443, 8443, 3001, 4443, 5443];
            case 'dev':
                return [3000, 3001, 3002, 8000, 8080, 8888];
            default:
                return [3000, 3001, 8000, 8080];
        }
    }
}
exports.PortManager = PortManager;


/***/ }),
/* 26 */
/***/ ((module) => {

module.exports = require("net");

/***/ }),
/* 27 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.HttpsCustomServer = void 0;
const https = __importStar(__webpack_require__(24));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const vscode = __importStar(__webpack_require__(1));
const httpServer_1 = __webpack_require__(15);
const networkUtils_1 = __webpack_require__(21);
const portManager_1 = __webpack_require__(25);
/**
 * HTTPS Server with custom user-selected certificates
 * Extends HTTP server functionality with SSL/TLS support using user-provided certificates
 */
class HttpsCustomServer {
    server = null;
    config;
    isRunning = false;
    httpHandler;
    usingFallbackCerts = false;
    /**
     * Validate custom certificates and provide fallback to default certificates
     * @private
     */
    validateAndFallbackCertificates(config) {
        // Check if custom certificates exist
        const customCertExists = fs.existsSync(config.certPath);
        const customKeyExists = fs.existsSync(config.keyPath);
        if (customCertExists && customKeyExists) {
            console.log('SERVER: Custom certificates found and will be used');
            return {
                certPath: config.certPath,
                keyPath: config.keyPath,
                usingFallback: false
            };
        }
        // Log the issues with custom certificates
        if (!customCertExists) {
            console.warn(`SERVER: Custom certificate file not found: ${config.certPath}`);
        }
        if (!customKeyExists) {
            console.warn(`SERVER: Custom key file not found: ${config.keyPath}`);
        }
        // Fallback to default certificates
        console.warn('SERVER: Falling back to default certificates due to missing custom certificates');
        if (config.extensionContext) {
            const certPath = path.join(config.extensionContext.extensionPath, 'certs', 'babia_cert.pem');
            const keyPath = path.join(config.extensionContext.extensionPath, 'certs', 'babia_key.pem');
            // Verify default certificates exist
            if (fs.existsSync(certPath) && fs.existsSync(keyPath)) {
                console.log('SERVER: Using default certificates as fallback');
                return { certPath, keyPath, usingFallback: true };
            }
        }
        // Last resort: try relative path to default certificates
        const fallbackCertPath = path.join(__dirname, '../../../certs/babia_cert.pem');
        const fallbackKeyPath = path.join(__dirname, '../../../certs/babia_key.pem');
        if (fs.existsSync(fallbackCertPath) && fs.existsSync(fallbackKeyPath)) {
            console.log('SERVER: Using relative path default certificates as last resort');
            return {
                certPath: fallbackCertPath,
                keyPath: fallbackKeyPath,
                usingFallback: true
            };
        }
        // If we get here, no certificates are available
        throw new Error('SERVER: No valid certificates found - neither custom nor default certificates are available');
    }
    constructor(config) {
        // Validate required certificate paths
        if (!config.certPath || !config.keyPath) {
            throw new Error('SERVER: Certificate path and key path are required for custom HTTPS server');
        }
        // Validate certificates and setup fallback if needed
        const certInfo = this.validateAndFallbackCertificates(config);
        this.usingFallbackCerts = certInfo.usingFallback;
        this.config = {
            host: '0.0.0.0', // ✅ Listen on all network interfaces for VR/mobile access
            staticRoot: path.join(__dirname, '../../../templates'),
            enableCors: true,
            allowedOrigins: ['*'],
            ...config,
            certPath: certInfo.certPath, // Use validated/fallback certificate path
            keyPath: certInfo.keyPath // Use validated/fallback key path
        };
        // Create HTTP handler instance for request processing with ALL configuration
        this.httpHandler = new httpServer_1.HttpServer({
            port: this.config.port,
            host: this.config.host,
            staticRoot: this.config.staticRoot,
            enableCors: this.config.enableCors,
            allowedOrigins: this.config.allowedOrigins,
            mainFile: this.config.mainFile // ← FIX: Pass mainFile to HTTP handler
        });
        console.log('SERVER: HTTPS server (custom certs) initialized with config:', {
            ...this.config,
            // Don't log actual certificate paths for security
            certPath: '***REDACTED***',
            keyPath: '***REDACTED***'
        });
    }
    /**
     * Start the HTTPS server with custom certificates
     * @returns Promise<string> - Server URL
     */
    async start() {
        if (this.isRunning) {
            throw new Error('SERVER: HTTPS server is already running');
        }
        // Validate certificate files
        await this.validateCertificates();
        // Find an available port starting from the configured port
        console.log(`SERVER: 🔍 Looking for available port starting from ${this.config.port} on host ${this.config.host}...`);
        const availablePort = await portManager_1.PortManager.findAvailablePort(this.config.port, this.config.port + 100, this.config.host);
        if (availablePort !== this.config.port) {
            console.log(`SERVER: ⚠️  Port ${this.config.port} was busy, using ${availablePort} instead on host ${this.config.host}`);
            this.config.port = availablePort;
        }
        else {
            console.log(`SERVER: ✅ Port ${this.config.port} is available on host ${this.config.host}`);
        }
        return new Promise((resolve, reject) => {
            try {
                // Load SSL certificates
                const sslOptions = this.loadSslOptions();
                // Create HTTPS server using the HTTP handler's request processing
                this.server = https.createServer(sslOptions, (req, res) => {
                    // Use the HTTP handler's request processing logic
                    this.httpHandler.handleRequest(req, res);
                });
                this.server.on('error', (error) => {
                    console.error('SERVER: HTTPS server error:', error);
                    this.isRunning = false;
                    if (error.code === 'EADDRINUSE') {
                        reject(new Error(`SERVER: Port ${this.config.port} is already in use`));
                    }
                    else if (error.code === 'EACCES') {
                        reject(new Error(`SERVER: Permission denied. May need to run as administrator for port ${this.config.port}`));
                    }
                    else if (error.code === 'ENOENT') {
                        reject(new Error(`SERVER: Certificate file not found. Please check your certificate paths.`));
                    }
                    else {
                        reject(new Error(`SERVER: Failed to start HTTPS server: ${error.message}`));
                    }
                });
                this.server.on('listening', () => {
                    const address = this.server?.address();
                    const port = this.config.port;
                    // Generate proper URLs for external access
                    const urls = networkUtils_1.NetworkUtils.generateServerUrls(port, 'https');
                    const primaryExternalUrl = networkUtils_1.NetworkUtils.getPrimaryExternalUrl(port, 'https');
                    console.log(`SERVER: HTTPS server listening on port ${port}`);
                    if (this.usingFallbackCerts) {
                        console.warn('SERVER: WARNING - Using default certificates instead of custom certificates');
                        // Show user warning
                        vscode.window.showWarningMessage('HTTPS server started with default certificates. Custom certificates were not found or invalid.', 'Configure Certificates').then(action => {
                            if (action === 'Configure Certificates') {
                                vscode.commands.executeCommand('codexr.server.configure');
                            }
                        });
                    }
                    else {
                        console.log('SERVER: Using custom certificates');
                    }
                    // Display comprehensive network information
                    networkUtils_1.NetworkUtils.displayNetworkInfo(port, 'https');
                    console.log('SERVER: Server address info:', address);
                    this.isRunning = true;
                    // Return the localhost URL for browser/panel access  
                    const localhostUrl = networkUtils_1.NetworkUtils.getLocalhostUrl(port, 'https');
                    resolve(localhostUrl);
                });
                this.server.on('close', () => {
                    console.log('SERVER: HTTPS server closed');
                    this.isRunning = false;
                });
                // Handle TLS errors gracefully (common with custom certificates)
                this.server.on('tlsClientError', (err, tlsSocket) => {
                    // Only log specific errors to avoid spam, and provide helpful guidance
                    if (err.message.includes('SSLV3_ALERT_CERTIFICATE_UNKNOWN') ||
                        err.message.includes('certificate unknown') ||
                        err.message.includes('SSL alert number 46')) {
                        console.warn('SERVER: ⚠️  Client rejected custom certificate - this may be expected');
                        console.warn('SERVER: 🥽 VR Solution: Access the server URL in browser first and accept certificate');
                        console.warn(`SERVER: 🌐 Navigate to: https://${this.config.host}:${this.config.port} and click "Advanced" -> "Proceed"`);
                    }
                    else if (err.message.includes('certificate')) {
                        console.warn('SERVER: ⚠️  Certificate-related TLS error:', err.message);
                        console.warn('SERVER: 🔧 This may indicate an issue with the custom certificates');
                    }
                    else {
                        console.warn('SERVER: TLS client error (non-critical):', err.message);
                    }
                    // Don't terminate the connection, just log the warning
                });
                this.server.listen(this.config.port, this.config.host);
            }
            catch (error) {
                console.error('SERVER: Error starting HTTPS server:', error);
                reject(error);
            }
        });
    }
    /**
     * Stop the HTTPS server
     * @returns Promise<void>
     */
    async stop() {
        if (!this.server || !this.isRunning) {
            console.log('SERVER: HTTPS server is not running');
            return;
        }
        return new Promise((resolve, reject) => {
            this.server.close((error) => {
                if (error) {
                    console.error('SERVER: Error stopping HTTPS server:', error);
                    reject(error);
                }
                else {
                    console.log('SERVER: HTTPS server stopped successfully');
                    this.isRunning = false;
                    this.server = null;
                    resolve();
                }
            });
        });
    }
    /**
     * Check if server is running
     * @returns boolean
     */
    getIsRunning() {
        return this.isRunning;
    }
    /**
     * Get server configuration (with redacted certificate paths)
     * @returns HttpsCustomServerConfig
     */
    getConfig() {
        return {
            ...this.config,
            certPath: '***REDACTED***',
            keyPath: '***REDACTED***'
        };
    }
    /**
     * Get server configuration with actual paths (for internal use)
     * @returns HttpsCustomServerConfig
     */
    getFullConfig() {
        return { ...this.config };
    }
    /**
     * Get server URL
     * @returns string | null
     */
    getServerUrl() {
        if (!this.isRunning) {
            return null;
        }
        return `https://${this.config.host}:${this.config.port}`;
    }
    /**
     * Get certificate information
     * @returns object with certificate details
     */
    getCertificateInfo() {
        const certExists = fs.existsSync(this.config.certPath);
        const keyExists = fs.existsSync(this.config.keyPath);
        let certSize;
        let keySize;
        try {
            if (certExists) {
                certSize = fs.statSync(this.config.certPath).size;
            }
            if (keyExists) {
                keySize = fs.statSync(this.config.keyPath).size;
            }
        }
        catch (error) {
            console.warn('SERVER: Could not get certificate file sizes:', error);
        }
        return {
            certPath: this.config.certPath,
            keyPath: this.config.keyPath,
            certExists,
            keyExists,
            isValid: certExists && keyExists,
            certSize,
            keySize
        };
    }
    /**
     * Validate that certificate files exist and are readable
     * @private
     */
    async validateCertificates() {
        const certPath = this.config.certPath;
        const keyPath = this.config.keyPath;
        console.log('SERVER: Validating custom certificates...');
        console.log('SERVER: Certificate path provided:', !!certPath);
        console.log('SERVER: Key path provided:', !!keyPath);
        // Check if certificate file exists
        if (!fs.existsSync(certPath)) {
            throw new Error(`SERVER: Certificate file not found: ${certPath}`);
        }
        // Check if key file exists
        if (!fs.existsSync(keyPath)) {
            throw new Error(`SERVER: Private key file not found: ${keyPath}`);
        }
        try {
            // Test reading the files
            await fs.promises.access(certPath, fs.constants.R_OK);
            await fs.promises.access(keyPath, fs.constants.R_OK);
            // Basic validation of file content
            const certContent = await fs.promises.readFile(certPath, 'utf8');
            const keyContent = await fs.promises.readFile(keyPath, 'utf8');
            if (!certContent.includes('BEGIN CERTIFICATE')) {
                throw new Error('SERVER: Certificate file does not appear to be a valid certificate');
            }
            if (!keyContent.includes('BEGIN PRIVATE KEY') && !keyContent.includes('BEGIN RSA PRIVATE KEY')) {
                throw new Error('SERVER: Key file does not appear to be a valid private key');
            }
            console.log('SERVER: Custom certificates validated successfully');
        }
        catch (error) {
            if (error instanceof Error && error.message.includes('SERVER:')) {
                throw error;
            }
            throw new Error(`SERVER: Cannot read certificate files: ${error}`);
        }
    }
    /**
     * Load SSL options for HTTPS server
     * @private
     * @returns https.ServerOptions
     */
    loadSslOptions() {
        try {
            const cert = fs.readFileSync(this.config.certPath, 'utf8');
            const key = fs.readFileSync(this.config.keyPath, 'utf8');
            console.log('SERVER: Custom SSL certificates loaded successfully');
            console.log('SERVER: 🔐 Configuring SSL options for custom certificate VR compatibility');
            return {
                cert: cert,
                key: key,
                // Custom certificate friendly options for VR
                rejectUnauthorized: false, // Allow custom certificates
                requestCert: false, // Don't require client certificates
                // Security protocols - more permissive for VR compatibility
                secureProtocol: 'TLS_method', // Allow wider range of TLS versions
                // Cipher suites compatible with VR browsers - let client choose
                honorCipherOrder: false, // Let client choose preferred cipher
                ciphers: [
                    'ECDHE-RSA-AES128-GCM-SHA256',
                    'ECDHE-RSA-AES256-GCM-SHA384',
                    'ECDHE-RSA-AES128-SHA256',
                    'ECDHE-RSA-AES256-SHA384',
                    'DHE-RSA-AES128-GCM-SHA256', // Added for wider compatibility
                    'DHE-RSA-AES256-GCM-SHA384',
                    'AES128-GCM-SHA256', // Fallback for older clients
                    'AES256-GCM-SHA384'
                ].join(':'),
                // Additional options for VR headset compatibility
                secureOptions: 0, // Allow all SSL/TLS versions
                // SNI (Server Name Indication) callback for flexibility
                SNICallback: (servername, callback) => {
                    console.log(`SERVER: 🥽 SNI request for custom cert: ${servername}`);
                    callback(null); // Accept any servername
                }
            };
        }
        catch (error) {
            throw new Error(`SERVER: Failed to load custom SSL certificates: ${error}`);
        }
    }
    /**
     * Get server status including certificate information
     * @returns object with detailed server status
     */
    getDetailedStatus() {
        return {
            isRunning: this.isRunning,
            url: this.getServerUrl(),
            config: this.getConfig(),
            certificates: this.getCertificateInfo(),
            uptime: this.isRunning ? process.uptime() : undefined
        };
    }
    /**
     * Test certificate validity without starting the server
     * @returns Promise<boolean>
     */
    async testCertificates() {
        try {
            await this.validateCertificates();
            // Try to create SSL options to validate certificate format
            this.loadSslOptions();
            console.log('SERVER: Custom certificate test passed');
            return true;
        }
        catch (error) {
            console.error('SERVER: Custom certificate test failed:', error);
            return false;
        }
    }
    /**
     * Update certificate paths
     * @param certPath - Path to certificate file
     * @param keyPath - Path to private key file
     * @returns Promise<boolean> - True if certificates are valid
     */
    async updateCertificates(certPath, keyPath) {
        if (this.isRunning) {
            throw new Error('SERVER: Cannot update certificates while server is running. Stop the server first.');
        }
        const oldCertPath = this.config.certPath;
        const oldKeyPath = this.config.keyPath;
        try {
            // Temporarily update paths for validation
            this.config.certPath = certPath;
            this.config.keyPath = keyPath;
            // Test the new certificates
            const isValid = await this.testCertificates();
            if (!isValid) {
                // Restore old paths if validation failed
                this.config.certPath = oldCertPath;
                this.config.keyPath = oldKeyPath;
                return false;
            }
            console.log('SERVER: Certificate paths updated successfully');
            return true;
        }
        catch (error) {
            // Restore old paths if error occurred
            this.config.certPath = oldCertPath;
            this.config.keyPath = oldKeyPath;
            console.error('SERVER: Failed to update certificates:', error);
            return false;
        }
    }
    /**
     * Get certificate expiration information (if available)
     * @returns Promise<object | null>
     */
    async getCertificateExpiration() {
        try {
            // This is a basic implementation
            // For more detailed certificate parsing, we could use a library like 'node-forge'
            const cert = fs.readFileSync(this.config.certPath, 'utf8');
            // Extract dates from certificate (basic regex parsing)
            const notBeforeMatch = cert.match(/Not Before: (.+)/);
            const notAfterMatch = cert.match(/Not After : (.+)/);
            if (notBeforeMatch && notAfterMatch) {
                const notBefore = new Date(notBeforeMatch[1]);
                const notAfter = new Date(notAfterMatch[1]);
                const now = new Date();
                const isExpired = now > notAfter;
                const daysUntilExpiry = Math.ceil((notAfter.getTime() - now.getTime()) / (1000 * 60 * 60 * 24));
                return {
                    notBefore,
                    notAfter,
                    isExpired,
                    daysUntilExpiry
                };
            }
            return null;
        }
        catch (error) {
            console.error('SERVER: Error parsing certificate expiration:', error);
            return null;
        }
    }
    /**
     * Validate certificate file format and content
     * @param certPath - Path to certificate file
     * @param keyPath - Path to private key file
     * @returns Promise<{isValid: boolean, errors: string[]}>
     */
    static async validateCertificateFiles(certPath, keyPath) {
        const errors = [];
        try {
            // Check if files exist
            if (!fs.existsSync(certPath)) {
                errors.push(`Certificate file not found: ${certPath}`);
            }
            if (!fs.existsSync(keyPath)) {
                errors.push(`Private key file not found: ${keyPath}`);
            }
            if (errors.length > 0) {
                return { isValid: false, errors };
            }
            // Check file permissions
            try {
                await fs.promises.access(certPath, fs.constants.R_OK);
            }
            catch {
                errors.push(`Cannot read certificate file: ${certPath}`);
            }
            try {
                await fs.promises.access(keyPath, fs.constants.R_OK);
            }
            catch {
                errors.push(`Cannot read private key file: ${keyPath}`);
            }
            if (errors.length > 0) {
                return { isValid: false, errors };
            }
            // Validate file content
            const certContent = await fs.promises.readFile(certPath, 'utf8');
            const keyContent = await fs.promises.readFile(keyPath, 'utf8');
            if (!certContent.includes('BEGIN CERTIFICATE')) {
                errors.push('Certificate file does not appear to be a valid certificate');
            }
            if (!keyContent.includes('BEGIN PRIVATE KEY') && !keyContent.includes('BEGIN RSA PRIVATE KEY')) {
                errors.push('Key file does not appear to be a valid private key');
            }
            return { isValid: errors.length === 0, errors };
        }
        catch (error) {
            errors.push(`Error validating certificates: ${error}`);
            return { isValid: false, errors };
        }
    }
}
exports.HttpsCustomServer = HttpsCustomServer;


/***/ }),
/* 28 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ActiveServerRegistry = void 0;
exports.getActiveServerRegistry = getActiveServerRegistry;
exports.registerActiveServer = registerActiveServer;
const vscode = __importStar(__webpack_require__(1));
/**
 * Active Server Registry
 * Centralized tracking and management of all active servers
 */
class ActiveServerRegistry {
    static instance = null;
    servers = new Map();
    eventEmitter = new vscode.EventEmitter();
    /** Event fired when registry changes */
    onRegistryChange = this.eventEmitter.event;
    constructor() {
        console.log('ACTIVE_SERVERS: Registry initialized');
    }
    /**
     * Get singleton instance
     */
    static getInstance() {
        if (!ActiveServerRegistry.instance) {
            ActiveServerRegistry.instance = new ActiveServerRegistry();
        }
        return ActiveServerRegistry.instance;
    }
    /**
     * Register a new active server
     */
    registerServer(config) {
        console.log('SERVER: registerServer called with config:', {
            port: config.port,
            htmlFile: config.htmlFile,
            customName: config.customName,
            url: config.url
        });
        const serverId = this.generateServerId(config.port, config.timestamp);
        const server = {
            id: serverId,
            port: config.port,
            url: config.url,
            launchMode: config.launchMode,
            certMode: config.certMode,
            timestamp: config.timestamp,
            status: 'running',
            htmlFile: config.htmlFile,
            customName: config.customName,
            serverInstance: config.serverInstance,
            metadata: config.metadata
        };
        this.servers.set(serverId, server);
        // Enhanced logging for custom names
        console.log(`ACTIVE_SERVERS: Registered server ${serverId} at ${config.url} (${config.certMode}/${config.launchMode})`);
        if (config.customName && config.customName.trim().length > 0) {
            console.log(`ACTIVE_SERVERS: Received custom name from launcher: ${config.customName}`);
            console.log(`ACTIVE_SERVERS: Registering server with name: ${config.customName}`);
        }
        else {
            const fallbackName = `localhost:${config.port}`;
            console.log(`ACTIVE_SERVERS: No custom name provided. Using default name: ${fallbackName}`);
        }
        this.emitEvent('serverAdded', serverId, server);
        return server;
    }
    /**
     * Remove a server from the registry
     */
    unregisterServer(serverId) {
        const server = this.servers.get(serverId);
        if (!server) {
            console.warn(`ACTIVE_SERVERS: Attempted to unregister non-existent server: ${serverId}`);
            return false;
        }
        this.servers.delete(serverId);
        console.log(`ACTIVE_SERVERS: Unregistered server ${serverId} (${server.url})`);
        this.emitEvent('serverRemoved', serverId, server);
        return true;
    }
    /**
     * Update server status
     */
    updateServerStatus(serverId, status) {
        const server = this.servers.get(serverId);
        if (!server) {
            console.warn(`ACTIVE_SERVERS: Attempted to update status of non-existent server: ${serverId}`);
            return false;
        }
        server.status = status;
        console.log(`ACTIVE_SERVERS: Updated server ${serverId} status to ${status}`);
        this.emitEvent('serverUpdated', serverId, server);
        return true;
    }
    /**
     * Get server by ID
     */
    getServer(serverId) {
        return this.servers.get(serverId);
    }
    /**
     * Get all active servers
     */
    getAllServers() {
        return Array.from(this.servers.values());
    }
    /**
     * Get servers by status
     */
    getServersByStatus(status) {
        return this.getAllServers().filter(server => server.status === status);
    }
    /**
     * Get servers by port
     */
    getServerByPort(port) {
        return this.getAllServers().find(server => server.port === port);
    }
    /**
     * Check if a server is registered
     */
    hasServer(serverId) {
        return this.servers.has(serverId);
    }
    /**
     * Get count of active servers
     */
    getServerCount() {
        return this.servers.size;
    }
    /**
     * Get count of running servers
     */
    getRunningServerCount() {
        return this.getServersByStatus('running').length;
    }
    /**
     * Clear all servers from registry
     */
    clearAll() {
        const count = this.servers.size;
        this.servers.clear();
        console.log(`ACTIVE_SERVERS: Cleared all servers (${count} removed)`);
        this.emitEvent('registryCleared');
    }
    /**
     * Cleanup stopped/error servers
     */
    cleanupInactiveServers() {
        const inactiveServers = this.getAllServers().filter(server => server.status === 'stopped' || server.status === 'error');
        let removedCount = 0;
        for (const server of inactiveServers) {
            if (this.unregisterServer(server.id)) {
                removedCount++;
            }
        }
        if (removedCount > 0) {
            console.log(`ACTIVE_SERVERS: Cleaned up ${removedCount} inactive servers`);
        }
        return removedCount;
    }
    /**
     * Get registry statistics
     */
    getStats() {
        const servers = this.getAllServers();
        const stats = {
            total: servers.length,
            running: 0,
            stopped: 0,
            error: 0,
            byMode: {},
            byCertMode: {}
        };
        for (const server of servers) {
            // Count by status
            stats[server.status]++;
            // Count by launch mode
            stats.byMode[server.launchMode] = (stats.byMode[server.launchMode] || 0) + 1;
            // Count by cert mode
            stats.byCertMode[server.certMode] = (stats.byCertMode[server.certMode] || 0) + 1;
        }
        return stats;
    }
    /**
     * Dispose of the registry
     */
    dispose() {
        this.eventEmitter.dispose();
        this.clearAll();
        console.log('ACTIVE_SERVERS: Registry disposed');
    }
    /**
     * Generate unique server ID
     * @private
     */
    generateServerId(port, timestamp) {
        return `server-${port}-${timestamp}`;
    }
    /**
     * Emit registry event
     * @private
     */
    emitEvent(type, serverId, server) {
        const event = {
            type,
            serverId,
            server,
            timestamp: Date.now()
        };
        this.eventEmitter.fire(event);
    }
}
exports.ActiveServerRegistry = ActiveServerRegistry;
/**
 * Convenience function to get the registry instance
 */
function getActiveServerRegistry() {
    return ActiveServerRegistry.getInstance();
}
/**
 * Convenience function to register a server
 */
function registerActiveServer(config) {
    return getActiveServerRegistry().registerServer(config);
}


/***/ }),
/* 29 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServerActionHandlers = void 0;
const vscode = __importStar(__webpack_require__(1));
const activeServerRegistry_1 = __webpack_require__(28);
const serverControl_1 = __webpack_require__(30);
const previewRenderer_1 = __webpack_require__(38);
const networkUtils_1 = __webpack_require__(21);
/**
 * Server Action Handlers
 * Handle user interactions with active servers
 */
class ServerActionHandlers {
    /**
     * Show server actions quick pick
     */
    static async showServerActions(serverId) {
        console.log(`ACTIVE_SERVER: Showing actions for server ${serverId}`);
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const server = registry.getServer(serverId);
        if (!server) {
            console.error(`ACTIVE_SERVER: Server ${serverId} not found`);
            vscode.window.showErrorMessage(`Server not found: ${serverId}`);
            return;
        }
        const actions = this.getAvailableActions(server);
        const selectedAction = await vscode.window.showQuickPick(actions, {
            placeHolder: `Actions for ${server.url}`,
            title: `Server Actions - localhost:${server.port}`
        });
        if (selectedAction) {
            await this.executeAction(selectedAction.action, server);
        }
    }
    /**
     * Open server in browser
     */
    static async openInBrowser(serverId) {
        console.log(`ACTIVE_SERVER: Opening server ${serverId} in browser`);
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const server = registry.getServer(serverId);
        if (!server) {
            console.error(`ACTIVE_SERVER: Server ${serverId} not found`);
            vscode.window.showErrorMessage(`Server not found: ${serverId}`);
            return;
        }
        try {
            await previewRenderer_1.PreviewRenderer.openPreview(server.url, server.htmlFile || '', 'browser');
            console.log(`ACTIVE_SERVER: Opened ${server.url} in browser`);
            vscode.window.showInformationMessage(`Opened ${server.url} in browser`);
        }
        catch (error) {
            console.error(`ACTIVE_SERVER: Error opening ${server.url} in browser:`, error);
            vscode.window.showErrorMessage(`Failed to open in browser: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Open server in lateral panel
     */
    static async openInPanel(serverId) {
        console.log(`ACTIVE_SERVER: Opening server ${serverId} in lateral panel`);
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const server = registry.getServer(serverId);
        if (!server) {
            console.error(`ACTIVE_SERVER: Server ${serverId} not found`);
            vscode.window.showErrorMessage(`Server not found: ${serverId}`);
            return;
        }
        // Check for HTTPS + lateral panel conflict
        if (server.certMode !== 'http') {
            console.log(`ACTIVE_SERVER: HTTPS server ${serverId} cannot be opened in lateral panel - cert mode: ${server.certMode}`);
            const response = await vscode.window.showWarningMessage('HTTPS content cannot be displayed in VS Code panels due to security restrictions. The content will not load properly.', 'Open in Browser Instead', 'Cancel');
            if (response === 'Open in Browser Instead') {
                console.log(`ACTIVE_SERVER: Redirecting server ${serverId} to browser due to HTTPS incompatibility`);
                return this.openInBrowser(serverId);
            }
            else {
                console.log(`ACTIVE_SERVER: User cancelled opening HTTPS server ${serverId} in panel`);
                return; // User cancelled
            }
        }
        try {
            console.log(`ACTIVE_SERVER: Opening HTTP server ${serverId} (${server.url}) in lateral panel`);
            await previewRenderer_1.PreviewRenderer.openPreview(server.url, server.htmlFile || '', 'lateralPanel', serverId);
            console.log(`ACTIVE_SERVER: Successfully opened ${server.url} in lateral panel`);
            vscode.window.showInformationMessage(`Opened ${server.url} in VS Code panel`);
        }
        catch (error) {
            console.error(`ACTIVE_SERVER: Error opening ${server.url} in panel:`, error);
            vscode.window.showErrorMessage(`Failed to open in panel: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Copy server URL to clipboard
     */
    static async copyUrl(serverId) {
        console.log(`ACTIVE_SERVER: Copying URL for server ${serverId}`);
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const server = registry.getServer(serverId);
        if (!server) {
            console.error(`ACTIVE_SERVER: Server ${serverId} not found`);
            vscode.window.showErrorMessage(`Server not found: ${serverId}`);
            return;
        }
        try {
            // Extract port and protocol from server URL
            const portMatch = server.url.match(/:(\d+)/);
            const port = portMatch ? parseInt(portMatch[1]) : null;
            const protocol = server.url.startsWith('https') ? 'https' : 'http';
            const localIP = networkUtils_1.NetworkUtils.getLocalIPAddress();
            const hasNetworkAccess = localIP !== 'localhost' && port;
            if (hasNetworkAccess) {
                // Show options for localhost vs network URL
                const networkUrl = `${protocol}://${localIP}:${port}`;
                const choice = await vscode.window.showQuickPick([
                    {
                        label: 'Local URL',
                        description: server.url,
                        detail: 'For use on this computer only'
                    },
                    {
                        label: 'Network URL',
                        description: networkUrl,
                        detail: 'For access from other devices on the network'
                    }
                ], {
                    placeHolder: 'Choose URL type to copy'
                });
                if (!choice) {
                    return; // User cancelled
                }
                const urlToCopy = choice.label.includes('Network') ? networkUrl : server.url;
                await vscode.env.clipboard.writeText(urlToCopy);
                console.log(`ACTIVE_SERVER: Copied ${urlToCopy} to clipboard`);
                vscode.window.showInformationMessage(`Copied ${choice.label.includes('Network') ? 'Network' : 'Local'} URL to clipboard`);
            }
            else {
                // Fallback to original behavior if no network access available
                await vscode.env.clipboard.writeText(server.url);
                console.log(`ACTIVE_SERVER: Copied ${server.url} to clipboard`);
                vscode.window.showInformationMessage(`Copied ${server.url} to clipboard`);
            }
        }
        catch (error) {
            console.error(`ACTIVE_SERVER: Error copying URL to clipboard:`, error);
            vscode.window.showErrorMessage('Failed to copy URL to clipboard');
        }
    }
    /**
     * Stop server
     */
    static async stopServer(serverId) {
        console.log(`ACTIVE_SERVER: Stopping server ${serverId}`);
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const server = registry.getServer(serverId);
        if (!server) {
            console.error(`ACTIVE_SERVER: Server ${serverId} not found`);
            vscode.window.showErrorMessage(`Server not found: ${serverId}`);
            return;
        }
        const response = await vscode.window.showWarningMessage(`Stop server ${server.url}?`, 'Stop', 'Cancel');
        if (response === 'Stop') {
            try {
                console.log(`ACTIVE_SERVER: Stopping server ${serverId} (${server.url})`);
                const success = await serverControl_1.ServerControl.stopServer(serverId);
                if (success) {
                    console.log(`ACTIVE_SERVER: Successfully stopped server ${serverId}`);
                    vscode.window.showInformationMessage(`Stopped server ${server.url}`);
                    // Refresh the tree view
                    vscode.commands.executeCommand('codeXR.activeServers.refreshServers');
                }
                else {
                    console.error(`ACTIVE_SERVER: Failed to stop server ${serverId}`);
                    vscode.window.showErrorMessage(`Failed to stop server ${server.url}`);
                }
            }
            catch (error) {
                console.error(`ACTIVE_SERVER: Error stopping server ${serverId}:`, error);
                vscode.window.showErrorMessage(`Error stopping server: ${error instanceof Error ? error.message : String(error)}`);
            }
        }
        else {
            console.log(`ACTIVE_SERVER: User cancelled stopping server ${serverId}`);
        }
    }
    /**
     * Show detailed server information
     */
    static async showServerDetails(serverId) {
        console.log(`ACTIVE_SERVER: Showing details for server ${serverId}`);
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const server = registry.getServer(serverId);
        if (!server) {
            console.error(`ACTIVE_SERVER: Server ${serverId} not found`);
            vscode.window.showErrorMessage(`Server not found: ${serverId}`);
            return;
        }
        const details = this.formatServerDetails(server);
        console.log(`ACTIVE_SERVER: Displaying detailed information for server ${serverId}`);
        await vscode.window.showInformationMessage(`Server Information - ${server.url}`, { modal: true, detail: details });
    }
    /**
     * Stop all servers
     */
    static async stopAllServers() {
        console.log('ACTIVE_SERVER: Stopping all servers');
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const allServers = registry.getAllServers();
        if (allServers.length === 0) {
            vscode.window.showInformationMessage('No servers are currently running');
            return;
        }
        const response = await vscode.window.showWarningMessage(`Stop all ${allServers.length} server(s)?`, 'Stop All', 'Cancel');
        if (response === 'Stop All') {
            try {
                console.log(`ACTIVE_SERVER: Stopping ${allServers.length} servers`);
                const success = await serverControl_1.ServerControl.stopAllServers();
                if (success) {
                    console.log('ACTIVE_SERVER: Successfully stopped all servers');
                    vscode.window.showInformationMessage(`Stopped all ${allServers.length} servers`);
                }
                else {
                    console.error('ACTIVE_SERVER: Failed to stop some servers');
                    vscode.window.showWarningMessage('Some servers may not have stopped properly');
                }
                // Refresh the tree view
                vscode.commands.executeCommand('codeXR.activeServers.refreshServers');
            }
            catch (error) {
                console.error('ACTIVE_SERVER: Error stopping all servers:', error);
                vscode.window.showErrorMessage(`Error stopping servers: ${error instanceof Error ? error.message : String(error)}`);
            }
        }
        else {
            console.log('ACTIVE_SERVER: User cancelled stopping all servers');
        }
    }
    /**
     * Refresh servers display
     */
    static async refreshServers() {
        console.log('ACTIVE_SERVER: Refreshing servers display');
        vscode.commands.executeCommand('codeXR.activeServers.refreshServers');
    }
    /**
     * Get available actions for a server based on its type
     * @private
     */
    static getAvailableActions(server) {
        const isHttp = server.certMode === 'http';
        const actions = [
            {
                label: 'Open in Browser',
                description: 'Open server in external browser',
                action: 'openInBrowser'
            }
        ];
        // Add lateral panel option only for HTTP servers
        if (isHttp) {
            actions.push({
                label: 'Open in Panel',
                description: 'Open server in VS Code lateral panel',
                action: 'openInPanel'
            });
        }
        actions.push({
            label: 'Copy URL',
            description: 'Copy server URL to clipboard',
            action: 'copyUrl'
        }, {
            label: 'Server Info',
            description: 'Show detailed server information',
            action: 'showDetails'
        }, {
            label: 'Stop Server',
            description: 'Stop this server',
            action: 'stopServer'
        });
        return actions;
    }
    /**
     * Execute an action on a server
     * @private
     */
    static async executeAction(action, server) {
        console.log(`ACTIVE_SERVER: Executing action '${action}' on server ${server.id}`);
        switch (action) {
            case 'openInBrowser':
                await this.openInBrowser(server.id);
                break;
            case 'openInPanel':
                await this.openInPanel(server.id);
                break;
            case 'copyUrl':
                await this.copyUrl(server.id);
                break;
            case 'showDetails':
                await this.showServerDetails(server.id);
                break;
            case 'stopServer':
                await this.stopServer(server.id);
                break;
            default:
                console.error(`ACTIVE_SERVER: Unknown action: ${action}`);
                vscode.window.showErrorMessage(`Unknown action: ${action}`);
        }
    }
    /**
     * Format server details for display
     * @private
     */
    static formatServerDetails(server) {
        const uptimeMs = Date.now() - server.timestamp;
        const uptime = this.formatUptime(uptimeMs);
        let details = '';
        details += `URL: ${server.url}\\n`;
        details += `Port: ${server.port}\\n`;
        details += `Status: ${server.status}\\n`;
        details += `Security: ${server.certMode.toUpperCase()}\\n`;
        details += `Launch Mode: ${server.launchMode}\\n`;
        details += `Uptime: ${uptime}\\n`;
        if (server.htmlFile) {
            const fileName = server.htmlFile.split('/').pop() || server.htmlFile;
            details += `Serving File: ${fileName}\\n`;
        }
        if (server.metadata) {
            if (server.metadata.host) {
                details += `Host: ${server.metadata.host}\\n`;
            }
            if (server.metadata.staticRoot) {
                details += `Static Root: ${server.metadata.staticRoot}\\n`;
            }
            if (server.metadata.description) {
                details += `Description: ${server.metadata.description}\\n`;
            }
        }
        return details;
    }
    /**
     * Format uptime duration
     * @private
     */
    static formatUptime(milliseconds) {
        const seconds = Math.floor(milliseconds / 1000);
        const minutes = Math.floor(seconds / 60);
        const hours = Math.floor(minutes / 60);
        if (hours > 0) {
            return `${hours}h ${minutes % 60}m`;
        }
        else if (minutes > 0) {
            return `${minutes}m ${seconds % 60}s`;
        }
        else {
            return `${seconds}s`;
        }
    }
}
exports.ServerActionHandlers = ServerActionHandlers;


/***/ }),
/* 30 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServerControl = void 0;
const vscode = __importStar(__webpack_require__(1));
const activeServerRegistry_1 = __webpack_require__(28);
const panelManager_1 = __webpack_require__(31);
const fileToServerMap_1 = __webpack_require__(20);
const SSEManager_1 = __webpack_require__(18);
const sessionRegistry_1 = __webpack_require__(32);
const errorHandler_1 = __webpack_require__(37);
/**
 * Server Control
 * Runtime operations for managing active servers
 */
class ServerControl {
    static extensionContext;
    /**
     * Initialize with extension context
     */
    static initialize(context) {
        ServerControl.extensionContext = context;
    }
    /**
     * Stop an active server
     */
    static async stopServer(serverId) {
        console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Attempting to stop server ${serverId}`);
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const server = registry.getServer(serverId);
        if (!server) {
            console.error(`ACTIVE_SERVERS: ❌ Server ${serverId} not found in registry`);
            vscode.window.showErrorMessage(`Server not found: ${serverId}`);
            return false;
        }
        console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Server found:`, {
            id: server.id,
            port: server.port,
            url: server.url,
            status: server.status,
            launchMode: server.launchMode,
            customName: server.customName,
            htmlFile: server.htmlFile,
            metadata: server.metadata
        });
        try {
            // Close associated lateral panel if it exists
            const panelManager = (0, panelManager_1.getPanelManager)();
            if (server.launchMode === 'lateralPanel' && panelManager.hasPanel(serverId)) {
                console.log(`ACTIVE_SERVER_PANEL: 🔍 DEBUG - Closing lateral panel for server ${serverId}`);
                panelManager.removePanel(serverId);
            }
            // Check if this server is associated with an analysis file and clean up
            console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Checking for file-to-server mapping for server on port ${server.port}`);
            const fileUri = fileToServerMap_1.fileToServerMap.findFileByPort(server.port);
            if (fileUri) {
                console.log(`ACTIVE_SERVERS: ✅ Found associated analysis file: ${fileUri}`);
                // Clean up SSE clients for this file
                console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Cleaning up SSE clients for ${fileUri}`);
                SSEManager_1.sseManager.removeAllClients(fileUri);
                // Remove the file-to-server mapping
                console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Removing file-to-server mapping for ${fileUri}`);
                fileToServerMap_1.fileToServerMap.unregisterMapping(fileUri);
            }
            else {
                console.log(`ACTIVE_SERVERS: ⚠️ No associated analysis file found for server on port ${server.port}`);
            }
            // Close associated analysis session if it exists
            console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Attempting to close associated analysis session`);
            await this.closeAssociatedAnalysisSession(serverId, server);
            // Update status to indicate stopping
            console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Updating server status to 'stopped'`);
            registry.updateServerStatus(serverId, 'stopped');
            // Stop the actual server instance if available
            if (server.serverInstance && typeof server.serverInstance.stop === 'function') {
                console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Stopping server instance for ${serverId}`);
                await server.serverInstance.stop();
                console.log(`ACTIVE_SERVERS: ✅ Server instance stopped for ${serverId}`);
            }
            else {
                console.log(`ACTIVE_SERVERS: ⚠️ No server instance or stop method for ${serverId}`);
            }
            // Remove from registry
            console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Removing server from registry`);
            registry.unregisterServer(serverId);
            console.log(`ACTIVE_SERVERS: ✅ Successfully stopped server ${serverId} (${server.url})`);
            vscode.window.showInformationMessage(`Server stopped: ${server.url}`);
            return true;
        }
        catch (error) {
            // Update status to error
            registry.updateServerStatus(serverId, 'error');
            (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.ActiveServers, `Failed to stop server ${server.url}`, error, errorHandler_1.ErrorSeverity.User);
            return false;
        }
    }
    /**
     * Stop all active servers
     */
    static async stopAllServers() {
        console.log('ACTIVE_SERVERS: Stopping all active servers');
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const runningServers = registry.getServersByStatus('running');
        if (runningServers.length === 0) {
            console.log('ACTIVE_SERVERS: No running servers to stop');
            vscode.window.showInformationMessage('No active servers to stop');
            return 0;
        }
        // First, close all lateral panels for servers that have them
        const panelManager = (0, panelManager_1.getPanelManager)();
        const lateralPanelServers = runningServers.filter(server => server.launchMode === 'lateralPanel');
        if (lateralPanelServers.length > 0) {
            console.log(`ACTIVE_SERVER_PANEL: Closing ${lateralPanelServers.length} lateral panels before stopping servers`);
            const serverIdsWithPanels = lateralPanelServers.map(server => server.id);
            const closedPanelCount = panelManager.removePanelsForServers(serverIdsWithPanels);
            console.log(`ACTIVE_SERVER_PANEL: Closed ${closedPanelCount}/${lateralPanelServers.length} lateral panels`);
        }
        let stoppedCount = 0;
        const stopPromises = runningServers.map(async (server) => {
            const success = await this.stopServer(server.id);
            if (success) {
                stoppedCount++;
            }
            return success;
        });
        await Promise.all(stopPromises);
        console.log(`ACTIVE_SERVERS: Stopped ${stoppedCount}/${runningServers.length} servers`);
        if (stoppedCount === runningServers.length) {
            vscode.window.showInformationMessage(`All ${stoppedCount} servers stopped successfully`);
        }
        else {
            vscode.window.showWarningMessage(`Stopped ${stoppedCount}/${runningServers.length} servers. Some servers may require manual intervention.`);
        }
        return stoppedCount;
    }
    /**
     * Get server status information
     */
    static getServerStatus(serverId) {
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const server = registry.getServer(serverId);
        if (!server) {
            return null;
        }
        const uptime = Date.now() - server.timestamp;
        const isRunning = server.status === 'running';
        let details = `Status: ${server.status}\\n`;
        details += `URL: ${server.url}\\n`;
        details += `Mode: ${server.certMode}/${server.launchMode}\\n`;
        details += `Uptime: ${this.formatUptime(uptime)}\\n`;
        if (server.htmlFile) {
            details += `File: ${server.htmlFile}\\n`;
        }
        return {
            server,
            isRunning,
            uptime,
            details
        };
    }
    /**
     * Refresh server status by checking actual server instance
     */
    static async refreshServerStatus(serverId) {
        console.log(`ACTIVE_SERVERS: Refreshing status for server ${serverId}`);
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const server = registry.getServer(serverId);
        if (!server) {
            console.warn(`ACTIVE_SERVERS: Cannot refresh non-existent server ${serverId}`);
            return false;
        }
        try {
            let actualStatus = 'running';
            // Check if server instance has a status check method
            if (server.serverInstance) {
                if (typeof server.serverInstance.getIsRunning === 'function') {
                    const isRunning = server.serverInstance.getIsRunning();
                    actualStatus = isRunning ? 'running' : 'stopped';
                }
                else if (typeof server.serverInstance.status !== 'undefined') {
                    actualStatus = server.serverInstance.status;
                }
            }
            // Update status if it has changed
            if (server.status !== actualStatus) {
                console.log(`ACTIVE_SERVERS: Status changed for ${serverId}: ${server.status} -> ${actualStatus}`);
                registry.updateServerStatus(serverId, actualStatus);
                // If server is stopped/error, consider removing from registry
                if (actualStatus !== 'running') {
                    console.log(`ACTIVE_SERVERS: Server ${serverId} is no longer running, removing from registry`);
                    registry.unregisterServer(serverId);
                }
            }
            return true;
        }
        catch (error) {
            (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.ActiveServers, `Refreshing server ${serverId} status`, error, errorHandler_1.ErrorSeverity.Warn);
            registry.updateServerStatus(serverId, 'error');
            return false;
        }
    }
    /**
     * Refresh all server statuses
     */
    static async refreshAllServerStatuses() {
        console.log('ACTIVE_SERVERS: Refreshing all server statuses');
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const servers = registry.getAllServers();
        const refreshPromises = servers.map(server => this.refreshServerStatus(server.id));
        await Promise.all(refreshPromises);
        console.log('ACTIVE_SERVERS: Completed status refresh for all servers');
    }
    /**
     * Get comprehensive registry information
     */
    static getRegistryInfo() {
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const servers = registry.getAllServers();
        const stats = registry.getStats();
        let summary = `Total: ${stats.total} servers\\n`;
        summary += `Running: ${stats.running}\\n`;
        summary += `Stopped: ${stats.stopped}\\n`;
        summary += `Errors: ${stats.error}\\n`;
        if (stats.total > 0) {
            summary += `\\nLaunch modes:\\n`;
            Object.entries(stats.byMode).forEach(([mode, count]) => {
                summary += `  ${mode}: ${count}\\n`;
            });
            summary += `\\nCertificate modes:\\n`;
            Object.entries(stats.byCertMode).forEach(([mode, count]) => {
                summary += `  ${mode}: ${count}\\n`;
            });
        }
        return {
            servers,
            stats,
            summary
        };
    }
    /**
     * Cleanup inactive servers from registry
     */
    static cleanupInactiveServers() {
        console.log('ACTIVE_SERVERS: Cleaning up inactive servers');
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const removedCount = registry.cleanupInactiveServers();
        if (removedCount > 0) {
            vscode.window.showInformationMessage(`Cleaned up ${removedCount} inactive servers`);
        }
        return removedCount;
    }
    /**
     * Format uptime duration
     * @private
     */
    static formatUptime(milliseconds) {
        const seconds = Math.floor(milliseconds / 1000);
        const minutes = Math.floor(seconds / 60);
        const hours = Math.floor(minutes / 60);
        if (hours > 0) {
            return `${hours}h ${minutes % 60}m ${seconds % 60}s`;
        }
        else if (minutes > 0) {
            return `${minutes}m ${seconds % 60}s`;
        }
        else {
            return `${seconds}s`;
        }
    }
    /**
     * Close analysis session associated with a server
     */
    static async closeAssociatedAnalysisSession(serverId, server) {
        try {
            console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Looking for analysis session associated with server ${serverId}`);
            console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Server details:`, {
                id: serverId,
                port: server.port,
                htmlFile: server.htmlFile,
                metadata: server.metadata
            });
            const sessionRegistry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(ServerControl.extensionContext);
            const allSessions = sessionRegistry.getAllSessions();
            console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Found ${allSessions.length} active analysis sessions`);
            allSessions.forEach((session, index) => {
                console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Session ${index + 1}:`, {
                    id: session.id,
                    targetPath: session.targetPath,
                    outputPath: session.outputPath,
                    status: session.status
                });
            });
            // Find session that matches this server
            let associatedSession = null;
            // Try to find by sessionId in server metadata (when implemented)
            if (server.metadata && server.metadata.sessionId) {
                const sessionId = server.metadata.sessionId;
                console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Trying to find session by metadata sessionId: ${sessionId}`);
                associatedSession = sessionRegistry.getSession(sessionId);
                if (associatedSession) {
                    console.log(`ACTIVE_SERVERS: ✅ Found session by metadata sessionId: ${sessionId}`);
                }
                else {
                    console.log(`ACTIVE_SERVERS: ❌ No session found with sessionId: ${sessionId}`);
                }
            }
            // If not found, try to find by matching output directory or static root
            if (!associatedSession) {
                const serverRoot = server.metadata?.staticRoot || server.htmlFile;
                console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Trying to find session by server root: ${serverRoot}`);
                for (const session of allSessions) {
                    console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Checking session ${session.id} with outputPath: ${session.outputPath}`);
                    if (session.outputPath && serverRoot && (session.outputPath === serverRoot ||
                        serverRoot.includes(session.outputPath) ||
                        session.outputPath.includes(serverRoot))) {
                        associatedSession = session;
                        console.log(`ACTIVE_SERVERS: ✅ Found session by output directory match: ${session.id}`);
                        break;
                    }
                }
                if (!associatedSession) {
                    console.log(`ACTIVE_SERVERS: ❌ No session found by directory matching`);
                }
            }
            // If found, close the session
            if (associatedSession) {
                console.log(`ACTIVE_SERVERS: 🔍 DEBUG - Closing associated analysis session ${associatedSession.id}`);
                const success = sessionRegistry.closeSession(associatedSession.id);
                if (success) {
                    console.log(`ACTIVE_SERVERS: ✅ Successfully closed associated analysis session ${associatedSession.id}`);
                }
                else {
                    console.warn(`ACTIVE_SERVERS: ⚠️ Failed to close associated analysis session ${associatedSession.id}`);
                }
            }
            else {
                console.log(`ACTIVE_SERVERS: ❌ No associated analysis session found for server ${serverId}`);
            }
        }
        catch (error) {
            (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.ActiveServers, `Closing associated analysis session for server ${serverId}`, error, errorHandler_1.ErrorSeverity.Warn);
        }
    }
}
exports.ServerControl = ServerControl;


/***/ }),
/* 31 */
/***/ ((__unused_webpack_module, exports) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PanelManager = void 0;
exports.getPanelManager = getPanelManager;
/**
 * Panel Manager Service
 * Manages WebviewPanel instances for servers launched in lateral panel mode
 */
class PanelManager {
    static instance;
    panels = new Map();
    constructor() {
        console.log('ACTIVE_SERVER_PANEL: Panel manager initialized');
    }
    /**
     * Get singleton instance
     */
    static getInstance() {
        if (!PanelManager.instance) {
            PanelManager.instance = new PanelManager();
        }
        return PanelManager.instance;
    }
    /**
     * Register a panel for a server
     * @param serverId - Server ID
     * @param panel - WebviewPanel instance
     */
    registerPanel(serverId, panel) {
        console.log(`ACTIVE_SERVER_PANEL: Registering panel for server ${serverId}`);
        // Remove any existing panel for this server first
        this.removePanel(serverId);
        // Register the new panel
        this.panels.set(serverId, panel);
        // Set up disposal listener to auto-cleanup
        panel.onDidDispose(() => {
            console.log(`ACTIVE_SERVER_PANEL: Panel for server ${serverId} was disposed by user`);
            this.panels.delete(serverId);
        });
        console.log(`ACTIVE_SERVER_PANEL: Panel registered for server ${serverId}, total panels: ${this.panels.size}`);
    }
    /**
     * Remove and dispose a panel for a server
     * @param serverId - Server ID
     * @returns true if panel was found and disposed, false otherwise
     */
    removePanel(serverId) {
        const panel = this.panels.get(serverId);
        if (panel) {
            console.log(`ACTIVE_SERVER_PANEL: Disposing panel for server ${serverId}`);
            try {
                panel.dispose();
                this.panels.delete(serverId);
                console.log(`ACTIVE_SERVER_PANEL: Panel for server ${serverId} disposed successfully`);
                return true;
            }
            catch (error) {
                console.warn(`ACTIVE_SERVER_PANEL: Error disposing panel for server ${serverId}:`, error);
                // Still remove from map even if disposal failed
                this.panels.delete(serverId);
                return false;
            }
        }
        return false;
    }
    /**
     * Remove and dispose all panels
     * @returns number of panels that were disposed
     */
    removeAllPanels() {
        console.log(`ACTIVE_SERVER_PANEL: Disposing all ${this.panels.size} panels`);
        let disposedCount = 0;
        const panelEntries = Array.from(this.panels.entries());
        for (const [serverId, panel] of panelEntries) {
            try {
                console.log(`ACTIVE_SERVER_PANEL: Disposing panel for server ${serverId}`);
                panel.dispose();
                this.panels.delete(serverId);
                disposedCount++;
            }
            catch (error) {
                console.warn(`ACTIVE_SERVER_PANEL: Error disposing panel for server ${serverId}:`, error);
                // Still remove from map even if disposal failed
                this.panels.delete(serverId);
            }
        }
        console.log(`ACTIVE_SERVER_PANEL: Disposed ${disposedCount}/${panelEntries.length} panels`);
        return disposedCount;
    }
    /**
     * Get panel for a server (if exists)
     * @param serverId - Server ID
     * @returns WebviewPanel or undefined
     */
    getPanel(serverId) {
        return this.panels.get(serverId);
    }
    /**
     * Check if a server has an associated panel
     * @param serverId - Server ID
     * @returns true if panel exists
     */
    hasPanel(serverId) {
        return this.panels.has(serverId);
    }
    /**
     * Get number of currently managed panels
     * @returns number of active panels
     */
    getPanelCount() {
        return this.panels.size;
    }
    /**
     * Get all server IDs that have panels
     * @returns array of server IDs
     */
    getServerIdsWithPanels() {
        return Array.from(this.panels.keys());
    }
    /**
     * Remove panels for multiple servers
     * @param serverIds - Array of server IDs
     * @returns number of panels that were disposed
     */
    removePanelsForServers(serverIds) {
        console.log(`ACTIVE_SERVER_PANEL: Disposing panels for ${serverIds.length} servers`);
        let disposedCount = 0;
        for (const serverId of serverIds) {
            if (this.removePanel(serverId)) {
                disposedCount++;
            }
        }
        console.log(`ACTIVE_SERVER_PANEL: Disposed ${disposedCount}/${serverIds.length} requested panels`);
        return disposedCount;
    }
    /**
     * Get debug information about current panels
     * @returns debug information
     */
    getDebugInfo() {
        return {
            serverCount: this.panels.size,
            serverIds: Array.from(this.panels.keys())
        };
    }
}
exports.PanelManager = PanelManager;
/**
 * Get global panel manager instance
 */
function getPanelManager() {
    return PanelManager.getInstance();
}


/***/ }),
/* 32 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Unified Session Registry
 * Only the essentials for creating and managing sessions
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.UnifiedSessionRegistry = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(33));
const nonceGenerator_1 = __webpack_require__(12);
const directoryFilter_1 = __webpack_require__(34);
const supportedLanguages_1 = __webpack_require__(35);
const sha256Generator_1 = __webpack_require__(36);
class UnifiedSessionRegistry {
    static instance;
    sessions = new Map();
    _onSessionChanged = new vscode.EventEmitter();
    onSessionChanged = this._onSessionChanged.event;
    context;
    constructor(context) {
        console.log('UNIFIED_REGISTRY: Initializing Simplified Session Registry');
        this.context = context;
    }
    static getInstance(context) {
        if (!UnifiedSessionRegistry.instance) {
            UnifiedSessionRegistry.instance = new UnifiedSessionRegistry(context);
        }
        return UnifiedSessionRegistry.instance;
    }
    /**
     * Check if a session with the same parameters already exists
     * A session is considered duplicate if it has the same:
     * - targetPath
     * - analysisMode
     * - targetType
     * - isDeep
     */
    hasDuplicateSession(params) {
        const existingSessions = this.getAllSessions();
        console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - hasDuplicateSession called - Checking ${existingSessions.length} existing sessions`);
        console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - Looking for duplicates of:`, {
            targetPath: params.targetPath,
            analysisMode: params.analysisMode,
            targetType: params.targetType,
            isDeep: params.isDeep || false
        });
        for (const session of existingSessions) {
            console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - Comparing with session ${session.id}:`, {
                targetPath: session.targetPath,
                analysisMode: session.analysisMode,
                targetType: session.targetType,
                isDeep: session.isDeep,
                status: session.status
            });
            const isDuplicate = session.targetPath === params.targetPath &&
                session.analysisMode === params.analysisMode &&
                session.targetType === params.targetType &&
                session.isDeep === (params.isDeep || false) &&
                session.status !== 'closed' && // Ignore closed sessions
                session.status !== 'error'; // Ignore error sessions
            if (isDuplicate) {
                console.log(`UNIFIED_REGISTRY: 🎯 DUPLICATE DETECTED - Found duplicate session ${session.id} for ${params.targetPath} (${params.analysisMode}, ${params.targetType}, deep: ${params.isDeep})`);
                return session;
            }
            else {
                console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - Session ${session.id} is NOT a duplicate`);
            }
        }
        console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - No duplicate sessions found`);
        return null;
    }
    /**
     * Create a new unified analysis session - SIMPLIFIED
     */
    async createSession(params) {
        try {
            console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - createSession called with params:`, {
                targetPath: params.targetPath,
                analysisMode: params.analysisMode,
                targetType: params.targetType,
                isDeep: params.isDeep || false
            });
            // Check for duplicate sessions first
            const duplicateSession = this.hasDuplicateSession(params);
            if (duplicateSession) {
                console.log(`UNIFIED_REGISTRY: 🔄 DUPLICATE FOUND - Returning null instead of creating duplicate`);
                console.log(`UNIFIED_REGISTRY: Existing session status: ${duplicateSession.status}, progress: ${duplicateSession.progress || 0}%`);
                // Show user-friendly message based on session status
                if (duplicateSession.status === 'monitoring') {
                    vscode.window.showWarningMessage(`Analysis already complete for "${duplicateSession.targetName}". Use Active Analyses panel to view results.`, 'Open Active Analyses').then(choice => {
                        if (choice === 'Open Active Analyses') {
                            vscode.commands.executeCommand('workbench.view.extension.codeXRView');
                        }
                    });
                }
                else if (duplicateSession.status === 'creating' || duplicateSession.status === 'analyzing') {
                    vscode.window.showWarningMessage(`Analysis for "${duplicateSession.targetName}" is already in progress (${duplicateSession.status})...`);
                }
                else {
                    vscode.window.showWarningMessage(`Analysis session for "${duplicateSession.targetName}" already exists with status: ${duplicateSession.status}`);
                }
                // Fire the change event to update UI
                this._onSessionChanged.fire(duplicateSession);
                return null; // Return null to indicate duplicate
            }
            console.log(`UNIFIED_REGISTRY: ✅ No duplicate found - Creating new session`);
            const id = (0, nonceGenerator_1.generateNonce)();
            const targetName = path.basename(params.targetPath);
            // Generate output directory
            const storageUri = params.context.storageUri;
            if (!storageUri) {
                throw new Error('Workspace storage URI is not available');
            }
            const outputDirectory = `${path.parse(targetName).name}_${params.analysisMode}_${params.targetType}_${id}`;
            // Use appropriate base directory based on target type
            const baseDirectory = params.targetType === 'file' ? 'fileAnalysis' : 'directoryAnalysis';
            const outputPath = path.join(storageUri.fsPath, baseDirectory, outputDirectory);
            // Calculate hash based on target type
            let calculatedHash = '';
            if (params.targetType === 'file') {
                console.log(`UNIFIED_REGISTRY: Calculating SHA256 hash for file: ${params.targetPath}`);
                try {
                    calculatedHash = await sha256Generator_1.SHA256Generator.generateFileHash(params.targetPath);
                    console.log(`UNIFIED_REGISTRY: File hash calculated: ${calculatedHash.substring(0, 12)}...`);
                }
                catch (hashError) {
                    console.error(`UNIFIED_REGISTRY: Error calculating file hash:`, hashError);
                    throw new Error(`Failed to calculate file hash: ${hashError instanceof Error ? hashError.message : String(hashError)}`);
                }
            }
            const session = {
                id,
                targetPath: params.targetPath,
                targetType: params.targetType,
                targetName,
                analysisMode: params.analysisMode,
                isDeep: params.isDeep || false,
                status: 'creating',
                startTime: new Date(),
                hash256: calculatedHash, // Now calculated during session creation for files
                outputDirectory,
                outputPath,
                requiredFiles: new Map(),
                templatePaths: new Map(),
                metadata: {}
            };
            // If it's directory analysis, we need to discover and filter directories to analyze
            if (params.targetType === 'directory') {
                console.log(`UNIFIED_REGISTRY: Discovering directories and files for analysis in: ${params.targetPath}`);
                // 🔍 DEBUG: Verificar si el directorio existe y es accesible
                try {
                    const stats = await fs.stat(params.targetPath);
                    console.log(`UNIFIED_REGISTRY: Target path stats - isDirectory: ${stats.isDirectory()}, size: ${stats.size}`);
                }
                catch (statError) {
                    console.error(`UNIFIED_REGISTRY: Error accessing target path ${params.targetPath}:`, statError);
                }
                // Discover directories
                session.directoriesToAnalyze = await this.discoverDirectoriesToAnalyze(params.targetPath, params.isDeep || false);
                console.log(`UNIFIED_REGISTRY: Found ${session.directoriesToAnalyze?.length || 0} directories to analyze (without duplicates)`);
                console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - directoriesToAnalyze:`, session.directoriesToAnalyze?.slice(0, 5));
                // Discover files in those directories
                session.filesToHash = await this.discoverFilesToAnalyze(session.directoriesToAnalyze || [], params.analysisMode);
                console.log(`UNIFIED_REGISTRY: Discovered ${session.filesToHash?.length || 0} supported files`);
                console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - filesToHash sample:`, session.filesToHash?.slice(0, 3));
                if ((session.directoriesToAnalyze?.length || 0) === 0) {
                    console.warn(`UNIFIED_REGISTRY: No directories found to analyze in: ${params.targetPath}`);
                }
                if ((session.filesToHash?.length || 0) === 0) {
                    console.warn(`UNIFIED_REGISTRY: No supported files found to analyze in directories`);
                }
            }
            this.sessions.set(id, session);
            this._onSessionChanged.fire(session);
            console.log(`UNIFIED_REGISTRY: Created basic session ${id} for ${targetName} (ready for launcher processing)`);
            return session;
        }
        catch (error) {
            console.error('UNIFIED_REGISTRY: Error creating session:', error);
            throw error;
        }
    }
    /**
     * Update session status - COMPATIBLE
     */
    updateSessionStatus(sessionId, status, progress, error) {
        const session = this.sessions.get(sessionId);
        if (!session) {
            return false;
        }
        session.status = status;
        if (progress !== undefined) {
            session.progress = progress;
        }
        if (error) {
            session.error = error;
        }
        if (status === 'monitoring' || status === 'error' || status === 'closed') {
            session.endTime = new Date();
        }
        this._onSessionChanged.fire(session);
        return true;
    }
    /**
     * Add required file to session
     */
    addRequiredFile(sessionId, fileName, content) {
        const session = this.sessions.get(sessionId);
        if (!session) {
            return false;
        }
        session.requiredFiles.set(fileName, content);
        this._onSessionChanged.fire(session);
        return true;
    }
    /**
     * Get session by ID
     */
    getSession(sessionId) {
        return this.sessions.get(sessionId);
    }
    /**
     * Get all sessions
     */
    getAllSessions() {
        return Array.from(this.sessions.values());
    }
    /**
     * Remove session by ID
     */
    removeSession(sessionId) {
        const session = this.sessions.get(sessionId);
        if (!session) {
            return false;
        }
        // Mark as closed before removal
        session.status = 'closed';
        session.endTime = new Date();
        // Remove from registry
        const removed = this.sessions.delete(sessionId);
        if (removed) {
            console.log(`UNIFIED_REGISTRY: Removed session ${sessionId} (${session.targetName})`);
            this._onSessionChanged.fire(session);
        }
        return removed;
    }
    /**
     * Get active sessions - SIMPLE
     */
    getActiveSessions() {
        return this.getAllSessions().filter(s => s.status === 'creating' || s.status === 'analyzing' || s.status === 'monitoring');
    }
    /**
     * Set session monitoring - SIMPLE
     */
    setSessionMonitoring(sessionId, port) {
        const session = this.sessions.get(sessionId);
        if (!session) {
            return false;
        }
        session.status = 'monitoring';
        session.endTime = new Date();
        if (port) {
            session.port = port;
        }
        this._onSessionChanged.fire(session);
        return true;
    }
    /**
     * Register server port for session - Centralized function for port assignment
     */
    registerSessionPort(sessionId, port) {
        console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - Registering port ${port} for session ${sessionId}`);
        const session = this.sessions.get(sessionId);
        if (!session) {
            console.warn(`UNIFIED_REGISTRY: ❌ Session ${sessionId} not found for port registration`);
            return false;
        }
        console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - Session before port assignment:`, {
            id: session.id,
            targetPath: session.targetPath,
            targetName: session.targetName,
            analysisMode: session.analysisMode,
            currentPort: session.port,
            currentAssignedPort: session.assignedPort,
            status: session.status
        });
        // Assign the port to both fields for compatibility
        session.port = port;
        session.assignedPort = port;
        console.log(`UNIFIED_REGISTRY: ✅ Successfully registered port ${port} for session ${sessionId}`);
        console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - Session after port assignment:`, {
            id: session.id,
            targetPath: session.targetPath,
            targetName: session.targetName,
            analysisMode: session.analysisMode,
            assignedPort: session.assignedPort,
            port: session.port,
            status: session.status
        });
        // Fire change event
        this._onSessionChanged.fire(session);
        return true;
    }
    /**
     * Get sessions by target path - SIMPLE
     */
    getSessionsByTargetPath(targetPath) {
        return this.getAllSessions().filter(s => s.targetPath === targetPath);
    }
    /**
     * Get sessions by analysis mode - SIMPLE
     */
    getSessionsByAnalysisMode(analysisMode) {
        return this.getAllSessions().filter(s => s.analysisMode === analysisMode);
    }
    /**
     * Get sessions by target type - SIMPLE
     */
    getSessionsByTargetType(targetType) {
        return this.getAllSessions().filter(s => s.targetType === targetType);
    }
    /**
     * Clean up old sessions - COMPATIBLE
     */
    cleanupOldSessions(maxAge) {
        const now = Date.now();
        const ageLimit = maxAge || 3600000; // Default 1 hour
        let cleaned = 0;
        for (const [id, session] of this.sessions) {
            if (session.status === 'closed' && session.endTime) {
                const age = now - session.endTime.getTime();
                if (age > ageLimit) {
                    this.sessions.delete(id);
                    cleaned++;
                }
            }
        }
        return cleaned;
    }
    /**
     * Get statistics - SIMPLE
     */
    getStatistics() {
        const sessions = this.getAllSessions();
        return {
            total: sessions.length,
            active: this.getActiveSessions().length,
            monitoring: sessions.filter(s => s.status === 'monitoring').length,
            error: sessions.filter(s => s.status === 'error').length,
            closed: sessions.filter(s => s.status === 'closed').length
        };
    }
    /**
     * Close session - SIMPLIFIED
     */
    closeSession(sessionId) {
        const session = this.sessions.get(sessionId);
        if (!session) {
            return false;
        }
        session.status = 'closed';
        session.endTime = new Date();
        this._onSessionChanged.fire(session);
        // Remove after delay
        setTimeout(() => this.sessions.delete(sessionId), 2000);
        return true;
    }
    /**
     * Descubre los directorios que deben ser analizados dentro de un directorio base
     */
    async discoverDirectoriesToAnalyze(basePath, isDeep) {
        try {
            console.log(`UNIFIED_REGISTRY: Discovering directories in: ${basePath} (deep: ${isDeep})`);
            const allDirectories = new Set();
            allDirectories.add(basePath); // Incluir el directorio base
            if (isDeep) {
                // Análisis profundo: busca recursivamente en subdirectorios
                await this.traverseDirectoryRecursive(basePath, allDirectories);
            }
            const directoriesArray = Array.from(allDirectories);
            console.log(`UNIFIED_REGISTRY: Found ${directoriesArray.length} directories before filtering`);
            // Filtrar directorios usando el directoryFilter
            const filteredDirectories = (0, directoryFilter_1.filterDirectoriesForAnalysis)(directoriesArray, basePath);
            console.log(`UNIFIED_REGISTRY: Filtered to ${filteredDirectories.length} directories for analysis`);
            return filteredDirectories;
        }
        catch (error) {
            console.error(`UNIFIED_REGISTRY: Error discovering directories in ${basePath}:`, error);
            return [];
        }
    }
    /**
     * Traversa directorios recursivamente agregando a un Set
     */
    async traverseDirectoryRecursive(basePath, directories) {
        try {
            const entries = await fs.readdir(basePath, { withFileTypes: true });
            for (const entry of entries) {
                if (entry.isDirectory()) {
                    const fullPath = path.join(basePath, entry.name);
                    // Solo agregar si no lo tenemos
                    if (!directories.has(fullPath)) {
                        directories.add(fullPath);
                        // Recursivamente traversar subdirectorios
                        await this.traverseDirectoryRecursive(fullPath, directories);
                    }
                }
            }
        }
        catch (error) {
            console.error(`UNIFIED_REGISTRY: Error reading directory ${basePath}:`, error);
        }
    }
    /**
     * Descubre archivos soportados en los directorios especificados
     * Filtra archivos HTML para análisis XR según el modo especificado
     */
    async discoverFilesToAnalyze(directories, analysisMode) {
        const filesToHash = [];
        console.log(`UNIFIED_REGISTRY: Discovering files in ${directories.length} directories for ${analysisMode} analysis`);
        console.log(`UNIFIED_REGISTRY: 🔍 DEBUG - Directories to process:`, directories);
        // Mostrar información sobre filtros aplicados
        if (analysisMode === 'XR') {
            console.log(`UNIFIED_REGISTRY: XR analysis mode - HTML files will be filtered out`);
        }
        // Obtener todas las extensiones soportadas
        const supportedExtensions = this.getSupportedExtensions();
        console.log(`UNIFIED_REGISTRY: Supported extensions: ${supportedExtensions.join(', ')}`);
        for (const directory of directories) {
            console.log(`UNIFIED_REGISTRY: 📂 Processing directory: ${directory}`);
            try {
                // Verificar si el directorio existe
                const dirStats = await fs.stat(directory);
                if (!dirStats.isDirectory()) {
                    console.warn(`UNIFIED_REGISTRY: ⚠️ Path is not a directory: ${directory}`);
                    continue;
                }
                const entries = await fs.readdir(directory, { withFileTypes: true });
                console.log(`UNIFIED_REGISTRY: Found ${entries.length} entries in ${directory}`);
                let fileCount = 0;
                for (const entry of entries) {
                    if (entry.isFile()) {
                        fileCount++;
                        const filePath = path.join(directory, entry.name);
                        const extension = path.extname(entry.name).toLowerCase();
                        console.log(`UNIFIED_REGISTRY: 📄 Checking file: ${entry.name} (${extension})`);
                        // Verificar si la extensión es soportada
                        if (supportedExtensions.includes(extension)) {
                            // Filtrar archivos HTML para análisis XR
                            const isHtmlFile = ['.html', '.htm', '.xhtml'].includes(extension);
                            if (analysisMode === 'XR' && isHtmlFile) {
                                console.log(`UNIFIED_REGISTRY: Skipping HTML file in XR analysis: ${filePath} (${extension})`);
                                continue; // Skip HTML files in XR analysis
                            }
                            try {
                                // Usar SHA256Generator para hash real del archivo
                                const fileHash = await sha256Generator_1.SHA256Generator.generateFileHash(filePath);
                                filesToHash.push({
                                    filePath,
                                    hash: fileHash
                                });
                                console.log(`UNIFIED_REGISTRY: ✅ Found supported file: ${filePath} (${extension})`);
                            }
                            catch (hashError) {
                                console.error(`UNIFIED_REGISTRY: Error generating hash for ${filePath}:`, hashError);
                                // Continuar con el siguiente archivo si falla el hash
                            }
                        }
                        else {
                            console.log(`UNIFIED_REGISTRY: ⏭️ Skipping unsupported file: ${entry.name} (${extension})`);
                        }
                    }
                }
                console.log(`UNIFIED_REGISTRY: 📊 Directory ${directory} contained ${fileCount} files`);
            }
            catch (error) {
                console.error(`UNIFIED_REGISTRY: Error reading files in directory ${directory}:`, error);
            }
        }
        console.log(`UNIFIED_REGISTRY: Discovered ${filesToHash.length} supported files`);
        return filesToHash;
    }
    /**
     * Obtiene todas las extensiones soportadas por el plugin
     */
    getSupportedExtensions() {
        const extensions = [];
        for (const languageConfig of Object.values(supportedLanguages_1.SUPPORTED_LANGUAGES)) {
            extensions.push(...languageConfig.extensions);
        }
        // Eliminar duplicados y retornar
        return [...new Set(extensions)];
    }
    /**
     * Dispose of registry - SIMPLIFIED
     */
    dispose() {
        console.log('UNIFIED_REGISTRY: 🧹 Disposing Simplified Session Registry');
        this._onSessionChanged.dispose();
        this.sessions.clear();
    }
}
exports.UnifiedSessionRegistry = UnifiedSessionRegistry;


/***/ }),
/* 33 */
/***/ ((module) => {

module.exports = require("fs/promises");

/***/ }),
/* 34 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Directory Filter Utility
 * Handles filtering of directories that should not be watched or analyzed
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.isDirectoryExcluded = isDirectoryExcluded;
exports.filterDirectoriesForAnalysis = filterDirectoriesForAnalysis;
exports.getExcludedDirectoryPatterns = getExcludedDirectoryPatterns;
const path = __importStar(__webpack_require__(5));
/**
 * Directories that should be excluded from analysis and watching
 * These directories typically contain:
 * - Build artifacts
 * - Dependencies
 * - Temporary files
 * - System files
 * - Version control metadata
 *
 * IMPORTANT: This list should NOT include standard source code directories like:
 * - src, source, lib, app (source code)
 * - test, tests, spec (test code)
 * - docs, doc (documentation source)
 * - examples, samples (example code)
 */
const EXCLUDED_DIRECTORIES = [
    // Node.js
    'node_modules',
    // Build outputs (but NOT source directories!)
    'dist',
    'build',
    'out',
    '.next',
    '.nuxt',
    // Dependencies and package managers
    'bower_components',
    'jspm_packages',
    // Version control
    '.git',
    '.svn',
    '.hg',
    '.bzr',
    // IDE and editor files
    '.vscode',
    '.idea',
    '.vs',
    // Temporary and cache
    '.tmp',
    'tmp',
    'temp',
    '.cache',
    'cache',
    // OS specific
    '.DS_Store',
    'Thumbs.db',
    '__pycache__',
    '.pytest_cache',
    // Python virtual environments
    'venv',
    'env',
    '.venv',
    '.env',
    'site-packages',
    // Java build outputs (but NOT src!)
    'target',
    // C/C++ build outputs
    'obj',
    // Logs
    'logs',
    'log',
    // Coverage
    'coverage',
    '.nyc_output',
    // Documentation builds (but NOT docs source!)
    'docs/_build',
    '_site',
];
/**
 * Check if a directory should be excluded from analysis
 * Now checks the entire path hierarchy, but NEVER excludes the root directory chosen by user
 */
function isDirectoryExcluded(dirPath, rootDirectory) {
    // NEVER exclude the root directory that the user explicitly selected
    if (rootDirectory && path.resolve(dirPath) === path.resolve(rootDirectory)) {
        console.log(`DIRECTORY_FILTER: 🚫 NEVER excluding root directory: ${dirPath}`);
        return false;
    }
    // 🔥 TEMPORARY FIX: Only exclude very specific problematic directories
    // to diagnose the issue with directory filtering
    const pathComponents = dirPath.split(path.sep);
    for (const component of pathComponents) {
        if (!component) {
            continue; // Skip empty components
        }
        // Only exclude these CRITICAL directories that should never be analyzed
        const criticalExclusions = [
            'node_modules', // Dependencies
            '.git', // Version control
            'build', // Build output
            'dist', // Build output
            '__pycache__' // Python cache
        ];
        if (criticalExclusions.includes(component)) {
            console.log(`DIRECTORY_FILTER: 🚫 Excluding critical directory: ${dirPath} (component: ${component})`);
            return true;
        }
        // Allow hidden directories for now to debug
        if (component.startsWith('.') && component !== '.' && component !== '..' && component !== '.git') {
            // Allow all hidden directories temporarily except .git
            console.log(`DIRECTORY_FILTER: ✅ TEMPORARILY allowing hidden directory: ${dirPath} (component: ${component})`);
        }
    }
    console.log(`DIRECTORY_FILTER: ✅ ALLOWING directory: ${dirPath}`);
    return false;
}
/**
 * Filter a list of directories, removing those that should not be analyzed
 * @param directories List of directory paths to filter
 * @param rootDirectory The root directory chosen by user (never excluded)
 * @returns Filtered list of directories that are safe to analyze
 */
function filterDirectoriesForAnalysis(directories, rootDirectory) {
    console.log(`DIRECTORY_FILTER: Filtering ${directories.length} directories`);
    if (rootDirectory) {
        console.log(`DIRECTORY_FILTER: Root directory (never excluded): ${rootDirectory}`);
    }
    const filtered = directories.filter(dir => {
        const shouldExclude = isDirectoryExcluded(dir, rootDirectory);
        if (shouldExclude) {
            console.log(`DIRECTORY_FILTER: Excluding directory (contains excluded component): ${dir}`);
        }
        else {
            console.log(`DIRECTORY_FILTER: Including directory: ${dir}`);
        }
        return !shouldExclude;
    });
    console.log(`DIRECTORY_FILTER: Filtered to ${filtered.length} directories (excluded ${directories.length - filtered.length})`);
    console.log(`DIRECTORY_FILTER: Remaining directories:`, filtered);
    return filtered;
}
/**
 * Get a list of all excluded directory patterns
 * Useful for debugging or informational purposes
 */
function getExcludedDirectoryPatterns() {
    return EXCLUDED_DIRECTORIES;
}


/***/ }),
/* 35 */
/***/ ((__unused_webpack_module, exports) => {


/**
 * Supported languages configuration for file detection and analysis
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SUPPORTED_LANGUAGES = void 0;
exports.getLanguageByExtension = getLanguageByExtension;
exports.getAllSupportedExtensions = getAllSupportedExtensions;
exports.isSupportedExtension = isSupportedExtension;
exports.getLanguageStats = getLanguageStats;
exports.SUPPORTED_LANGUAGES = {
    // Programming Languages - CodeXR Supported
    python: {
        extensions: ['.py', '.pyw', '.pyi'],
        name: 'Python'
    },
    ruby: {
        extensions: ['.rb', '.rbw'],
        name: 'Ruby'
    },
    java: {
        extensions: ['.java'],
        name: 'Java'
    },
    c: {
        extensions: ['.c', '.h'],
        name: 'C'
    },
    cplusplus: {
        extensions: ['.cpp', '.cxx', '.cc', '.hpp', '.hxx'],
        name: 'C++'
    },
    csharp: {
        extensions: ['.cs'],
        name: 'C#'
    },
    erlang: {
        extensions: ['.erl', '.hrl'],
        name: 'Erlang'
    },
    fortran: {
        extensions: ['.f90', '.f95', '.f03', '.f08', '.f'],
        name: 'Fortran'
    },
    gdscript: {
        extensions: ['.gd'],
        name: 'GDScript'
    },
    go: {
        extensions: ['.go'],
        name: 'Go'
    },
    javascript: {
        extensions: ['.js', '.mjs', '.cjs'],
        name: 'JavaScript'
    },
    kotlin: {
        extensions: ['.kt', '.kts'],
        name: 'Kotlin'
    },
    lua: {
        extensions: ['.lua'],
        name: 'Lua'
    },
    objectivec: {
        extensions: ['.m', '.mm'],
        name: 'Objective-C'
    },
    php: {
        extensions: ['.php', '.phtml', '.php3', '.php4', '.php5'],
        name: 'PHP'
    },
    perl: {
        extensions: ['.pl', '.pm'],
        name: 'Perl'
    },
    scala: {
        extensions: ['.scala', '.sc'],
        name: 'Scala'
    },
    solidity: {
        extensions: ['.sol'],
        name: 'Solidity'
    },
    swift: {
        extensions: ['.swift'],
        name: 'Swift'
    },
    typescript: {
        extensions: ['.ts', '.tsx'],
        name: 'TypeScript'
    },
    ttcn3: {
        extensions: ['.ttcn', '.ttcn3'],
        name: 'TTCN-3'
    },
    vue: {
        extensions: ['.vue'],
        name: 'Vue'
    },
    zig: {
        extensions: ['.zig'],
        name: 'Zig'
    },
    // Web Technologies
    html: {
        extensions: ['.html', '.htm', '.xhtml'],
        name: 'HTML'
    }
};
/**
 * Get language configuration by file extension
 */
function getLanguageByExtension(extension) {
    const normalizedExt = extension.toLowerCase();
    for (const [key, config] of Object.entries(exports.SUPPORTED_LANGUAGES)) {
        if (config.extensions.includes(normalizedExt)) {
            return { ...config, icon: key }; // Add the key as icon identifier
        }
    }
    return null;
}
/**
 * Get all supported extensions as a flat array
 */
function getAllSupportedExtensions() {
    return Object.values(exports.SUPPORTED_LANGUAGES)
        .flatMap(config => config.extensions);
}
/**
 * Check if a file extension is supported
 */
function isSupportedExtension(extension) {
    return getLanguageByExtension(extension) !== null;
}
/**
 * Get language statistics for logging
 */
function getLanguageStats() {
    const languages = Object.keys(exports.SUPPORTED_LANGUAGES);
    const extensions = getAllSupportedExtensions();
    return {
        totalLanguages: languages.length,
        totalExtensions: extensions.length
    };
}


/***/ }),
/* 36 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * SHA256 Generator Utility
 * Generates SHA256 hash for files to track changes and ensure integrity
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SHA256Generator = void 0;
const crypto = __importStar(__webpack_require__(13));
const fs = __importStar(__webpack_require__(6));
class SHA256Generator {
    /**
     * Generate SHA256 hash from file path
     */
    static async generateFileHash(filePath) {
        try {
            const fileBuffer = await fs.promises.readFile(filePath);
            const hash = crypto.createHash('sha256');
            hash.update(fileBuffer);
            return hash.digest('hex');
        }
        catch (error) {
            console.error(`SHA256_GENERATOR: Error generating hash for file ${filePath}:`, error);
            throw new Error(`Failed to generate SHA256 hash: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Generate SHA256 hash from string content
     */
    static generateStringHash(content) {
        try {
            const hash = crypto.createHash('sha256');
            hash.update(content, 'utf8');
            return hash.digest('hex');
        }
        catch (error) {
            console.error(`SHA256_GENERATOR: Error generating hash for string content:`, error);
            throw new Error(`Failed to generate SHA256 hash: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Verify if file content matches expected hash
     */
    static async verifyFileHash(filePath, expectedHash) {
        try {
            const currentHash = await SHA256Generator.generateFileHash(filePath);
            return currentHash === expectedHash;
        }
        catch (error) {
            console.error(`SHA256_GENERATOR: Error verifying hash for file ${filePath}:`, error);
            return false;
        }
    }
    /**
     * Generate short hash (first 8 characters) for display purposes
     */
    static async generateShortFileHash(filePath) {
        const fullHash = await SHA256Generator.generateFileHash(filePath);
        return fullHash.substring(0, 8);
    }
}
exports.SHA256Generator = SHA256Generator;


/***/ }),
/* 37 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Centralized Error Handler
 *
 * Provides consistent error classification, formatting and reporting
 * across the entire extension. Replaces inconsistent patterns of
 * console.error / showErrorMessage / silent swallowing.
 *
 * Usage:
 *   import { handleError, ErrorSeverity, ErrorDomain } from '../utils/errorHandler';
 *
 *   try { ... } catch (error) {
 *       handleError(ErrorDomain.Server, 'Failed to start HTTPS server', error, ErrorSeverity.User);
 *   }
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ErrorDomain = exports.ErrorSeverity = void 0;
exports.formatErrorMessage = formatErrorMessage;
exports.handleError = handleError;
exports.withErrorHandler = withErrorHandler;
const vscode = __importStar(__webpack_require__(1));
/** Error severity determines how the error is surfaced */
var ErrorSeverity;
(function (ErrorSeverity) {
    /** Show to user via showErrorMessage — user can act on it */
    ErrorSeverity["User"] = "user";
    /** Log via console.error — internal/recoverable issue */
    ErrorSeverity["Log"] = "log";
    /** Log as warning — expected/non-critical failure */
    ErrorSeverity["Warn"] = "warn";
    /** Silently ignore — intentional no-op on failure */
    ErrorSeverity["Silent"] = "silent";
})(ErrorSeverity || (exports.ErrorSeverity = ErrorSeverity = {}));
/** Error domain for categorization and structured logging */
var ErrorDomain;
(function (ErrorDomain) {
    ErrorDomain["PythonEnv"] = "PYTHON_ENV";
    ErrorDomain["Server"] = "SERVER";
    ErrorDomain["ActiveServers"] = "ACTIVE_SERVERS";
    ErrorDomain["Analysis"] = "ANALYSIS";
    ErrorDomain["FileIO"] = "FILE_IO";
    ErrorDomain["Config"] = "CONFIG";
    ErrorDomain["Template"] = "TEMPLATE";
    ErrorDomain["UI"] = "UI";
    ErrorDomain["SSE"] = "SSE";
    ErrorDomain["Watcher"] = "WATCHER";
    ErrorDomain["Command"] = "COMMAND";
    ErrorDomain["Visualization"] = "VISUALIZATION";
})(ErrorDomain || (exports.ErrorDomain = ErrorDomain = {}));
/**
 * Extract a human-readable message from any thrown value.
 */
function formatErrorMessage(error) {
    if (error instanceof Error) {
        return error.message;
    }
    return String(error);
}
/**
 * Centralized error handler with consistent formatting and severity routing.
 *
 * @param domain   The area of the extension where the error occurred
 * @param operation  A short description of what was being done
 * @param error    The caught error (any type)
 * @param severity How to surface the error (default: Log)
 */
function handleError(domain, operation, error, severity = ErrorSeverity.Log) {
    const message = formatErrorMessage(error);
    const prefix = `${domain}: ${operation}`;
    switch (severity) {
        case ErrorSeverity.User:
            console.error(`${prefix}:`, error);
            vscode.window.showErrorMessage(`${prefix}: ${message}`);
            break;
        case ErrorSeverity.Log:
            console.error(`${prefix}:`, error);
            break;
        case ErrorSeverity.Warn:
            console.warn(`${prefix}:`, error);
            break;
        case ErrorSeverity.Silent:
            // Intentional no-op
            break;
    }
}
/**
 * Convenience wrapper for async operations with consistent error handling.
 * Returns the result or undefined on failure.
 *
 * @param domain    Error domain
 * @param operation Operation description
 * @param fn        Async function to execute
 * @param severity  Error severity (default: Log)
 */
async function withErrorHandler(domain, operation, fn, severity = ErrorSeverity.Log) {
    try {
        return await fn();
    }
    catch (error) {
        handleError(domain, operation, error, severity);
        return undefined;
    }
}


/***/ }),
/* 38 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PreviewRenderer = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
const panelManager_1 = __webpack_require__(31);
/**
 * Preview Renderer
 * Handles opening HTML content in browser or VS Code webview panel
 */
class PreviewRenderer {
    /**
     * Open HTML content based on the specified mode
     * @param serverUrl - Server URL where the file is served
     * @param htmlFilePath - Path to the HTML file (for reference/logging)
     * @param openMode - How to open ('browser' or 'lateralPanel')
     * @param serverId - Optional server ID for panel tracking (required for lateralPanel mode)
     */
    static async openPreview(serverUrl, htmlFilePath, openMode, serverId) {
        console.log(`SERVER: Opening preview for ${htmlFilePath} at ${serverUrl} in ${openMode} mode`);
        try {
            if (openMode === 'browser') {
                await this.openInBrowser(serverUrl);
            }
            else if (openMode === 'lateralPanel') {
                if (!serverId) {
                    throw new Error('Server ID is required for lateral panel mode');
                }
                await this.openInWebviewPanel(serverUrl, htmlFilePath, serverId);
            }
            else {
                throw new Error(`Unsupported open mode: ${openMode}`);
            }
        }
        catch (error) {
            console.error(`SERVER: Error opening preview: ${error}`);
            vscode.window.showWarningMessage(`Failed to open preview: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Open URL in external browser
     * @private
     */
    static async openInBrowser(serverUrl) {
        console.log(`SERVER: Opening ${serverUrl} in external browser`);
        await vscode.env.openExternal(vscode.Uri.parse(serverUrl));
        vscode.window.showInformationMessage(`Opened ${serverUrl} in browser`);
    }
    /**
     * Open server URL in VS Code webview panel using iframe
     * @private
     */
    static async openInWebviewPanel(serverUrl, htmlFilePath, serverId) {
        console.log(`ACTIVE_SERVER_PANEL: Opening ${serverUrl} in VS Code webview panel for server ${serverId}`);
        try {
            const fileName = path.basename(htmlFilePath);
            // Create webview panel
            const panel = vscode.window.createWebviewPanel('serverPreview', `Local Server Preview - ${fileName}`, vscode.ViewColumn.Two, {
                enableScripts: true,
                enableForms: true,
                retainContextWhenHidden: true
            });
            // Register panel with panel manager
            const panelManager = (0, panelManager_1.getPanelManager)();
            panelManager.registerPanel(serverId, panel);
            console.log(`ACTIVE_SERVER_PANEL: Panel registered for server ${serverId}`);
            // Create iframe HTML pointing to the server URL
            const iframeHtml = this.createIframeHtml(serverUrl, fileName);
            // Set the HTML content
            panel.webview.html = iframeHtml;
            // Handle messages from webview
            panel.webview.onDidReceiveMessage(message => {
                console.log(`ACTIVE_SERVER_PANEL: Webview message for server ${serverId}:`, message);
                switch (message.command) {
                    case 'openExternal':
                        vscode.env.openExternal(vscode.Uri.parse(message.url));
                        break;
                    case 'showError':
                        vscode.window.showErrorMessage(message.text);
                        break;
                    case 'serverError':
                        vscode.window.showWarningMessage(`Server Error: ${message.text}`);
                        break;
                }
            }, undefined);
            // Handle panel disposal
            panel.onDidDispose(() => {
                console.log(`ACTIVE_SERVER_PANEL: Webview panel disposed for server ${serverId}`);
                // Panel manager will automatically clean up via its disposal listener
            });
            console.log(`SERVER: Created webview panel for ${serverUrl}`);
            vscode.window.showInformationMessage(`Opened ${fileName} in VS Code panel`);
        }
        catch (error) {
            console.error(`SERVER: Error creating webview panel: ${error}`);
            throw new Error(`Failed to create webview panel: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Create iframe HTML for embedding the server URL
     * @private
     */
    static createIframeHtml(serverUrl, fileName) {
        // Escape template literals in serverUrl for safe injection
        const escapedServerUrl = serverUrl.replace(/'/g, "\\'").replace(/"/g, '\\"');
        return `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Local Server Preview - ${fileName}</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            background: #1e1e1e;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .loading-container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            color: #cccccc;
            z-index: 1000;
        }
        
        .loading-spinner {
            border: 3px solid #333;
            border-top: 3px solid #007acc;
            border-radius: 50%;
            width: 30px;
            height: 30px;
            animation: spin 1s linear infinite;
            margin: 0 auto 15px;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .error-container {
            display: none;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            color: #f48771;
            background: #2d2d30;
            padding: 20px;
            border-radius: 8px;
            border: 1px solid #f48771;
            max-width: 80%;
        }
        
        .retry-button {
            background: #007acc;
            color: white;
            border: none;
            padding: 8px 16px;
            border-radius: 4px;
            cursor: pointer;
            margin-top: 10px;
        }
        
        .retry-button:hover {
            background: #005a9e;
        }
        
        iframe {
            width: 100%;
            height: 100vh;
            border: none;
            display: none;
        }
        
        iframe.loaded {
            display: block;
        }
    </style>
</head>
<body>
    <div class="loading-container" id="loadingContainer">
        <div class="loading-spinner"></div>
        <div>Loading server content...</div>
        <div style="font-size: 12px; margin-top: 8px; opacity: 0.7;">${escapedServerUrl}</div>
    </div>
    
    <div class="error-container" id="errorContainer">
        <h3>⚠️ Connection Error</h3>
        <p>Could not load content from the local server.</p>
        <p style="font-size: 12px; opacity: 0.8;">URL: ${escapedServerUrl}</p>
        <button class="retry-button" onclick="reloadFrame()">Retry</button>
        <button class="retry-button" onclick="openInBrowser()" style="margin-left: 8px;">Open in Browser</button>
    </div>
    
    <iframe id="contentFrame" src="${escapedServerUrl}" title="Server Content"></iframe>
    
    <script>
        const iframe = document.getElementById('contentFrame');
        const loadingContainer = document.getElementById('loadingContainer');
        const errorContainer = document.getElementById('errorContainer');
        
        let loadTimeout;
        
        function showLoading() {
            loadingContainer.style.display = 'block';
            errorContainer.style.display = 'none';
            iframe.classList.remove('loaded');
        }
        
        function showContent() {
            loadingContainer.style.display = 'none';
            errorContainer.style.display = 'none';
            iframe.classList.add('loaded');
        }
        
        function showError() {
            loadingContainer.style.display = 'none';
            errorContainer.style.display = 'block';
            iframe.classList.remove('loaded');
        }
        
        function reloadFrame() {
            showLoading();
            iframe.src = iframe.src; // Reload
            setupLoadTimeout();
        }
        
        function openInBrowser() {
            if (typeof acquireVsCodeApi !== 'undefined') {
                const vscode = acquireVsCodeApi();
                vscode.postMessage({
                    command: 'openExternal',
                    url: '${escapedServerUrl}'
                });
            }
        }
        
        function setupLoadTimeout() {
            clearTimeout(loadTimeout);
            loadTimeout = setTimeout(() => {
                console.log('SERVER: Load timeout, showing error');
                showError();
            }, 10000); // 10 second timeout
        }
        
        iframe.addEventListener('load', () => {
            console.log('SERVER: Iframe loaded successfully');
            clearTimeout(loadTimeout);
            showContent();
        });
        
        iframe.addEventListener('error', () => {
            console.log('SERVER: Iframe load error');
            clearTimeout(loadTimeout);
            showError();
        });
        
        // Initial setup
        showLoading();
        setupLoadTimeout();
        
        // Send ready message to extension
        if (typeof acquireVsCodeApi !== 'undefined') {
            const vscode = acquireVsCodeApi();
            vscode.postMessage({
                command: 'webviewReady',
                url: '${escapedServerUrl}'
            });
        }
    </script>
</body>
</html>`;
    }
}
exports.PreviewRenderer = PreviewRenderer;


/***/ }),
/* 39 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServerRegistrar = void 0;
exports.getServerRegistrar = getServerRegistrar;
const activeServerRegistry_1 = __webpack_require__(28);
/**
 * Server Registrar Service
 * Centralized service for registering servers with the active servers registry
 * This layer provides validation, logging, and abstraction between server launchers and the registry
 */
class ServerRegistrar {
    static instance = null;
    constructor() {
        console.log('SERVER_REGISTRAR: Service initialized');
    }
    /**
     * Get singleton instance
     */
    static getInstance() {
        if (!ServerRegistrar.instance) {
            ServerRegistrar.instance = new ServerRegistrar();
        }
        return ServerRegistrar.instance;
    }
    /**
     * Register a server with the active servers registry
     * @param config - Server registration configuration
     * @returns Registered ActiveServer
     */
    registerServer(config) {
        console.log('SERVER_REGISTRAR: Registering server with config:', {
            port: config.port,
            customName: config.customName,
            url: config.url,
            launchMode: config.launchMode,
            certMode: config.certMode
        });
        // Input validation
        this.validateConfig(config);
        try {
            // Register with the registry
            const activeServer = (0, activeServerRegistry_1.registerActiveServer)(config);
            console.log(`SERVER_REGISTRAR: Successfully registered server ${activeServer.id}`);
            return activeServer;
        }
        catch (error) {
            console.error('SERVER_REGISTRAR: Failed to register server:', error);
            throw new Error(`Server registration failed: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Validate server registration configuration
     * @private
     */
    validateConfig(config) {
        // Validate required fields
        if (!config.port || config.port <= 0 || config.port > 65535) {
            throw new Error(`Invalid port number: ${config.port}`);
        }
        if (!config.url || typeof config.url !== 'string') {
            throw new Error(`Invalid URL: ${config.url}`);
        }
        if (!config.launchMode || !['browser', 'lateralPanel'].includes(config.launchMode)) {
            throw new Error(`Invalid launch mode: ${config.launchMode}`);
        }
        if (!config.certMode || !['http', 'https-default', 'https-custom'].includes(config.certMode)) {
            throw new Error(`Invalid cert mode: ${config.certMode}`);
        }
        if (!config.timestamp || config.timestamp <= 0) {
            throw new Error(`Invalid timestamp: ${config.timestamp}`);
        }
        // Validate optional fields
        if (config.customName !== undefined && typeof config.customName !== 'string') {
            throw new Error(`Custom name must be a string: ${typeof config.customName}`);
        }
        if (config.htmlFile !== undefined && typeof config.htmlFile !== 'string') {
            throw new Error(`HTML file path must be a string: ${typeof config.htmlFile}`);
        }
    }
}
exports.ServerRegistrar = ServerRegistrar;
/**
 * Convenience function to get the registrar instance
 */
function getServerRegistrar() {
    return ServerRegistrar.getInstance();
}


/***/ }),
/* 40 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.registerActiveServersCommands = registerActiveServersCommands;
exports.getActiveServersCommandIds = getActiveServersCommandIds;
const activeServersCommands_1 = __webpack_require__(41);
/**
 * Active Servers Commands Wrapper
 * Re-exports active servers commands for centralized command registration
 */
/**
 * Register all active servers commands
 * @param context VS Code extension context
 * @param treeDataProvider Any tree data provider that supports refresh operations
 */
function registerActiveServersCommands(context, treeDataProvider) {
    console.log('COMMANDS: Registering active servers commands');
    activeServersCommands_1.ActiveServersCommands.registerCommands(context, treeDataProvider);
}
/**
 * Get active servers command IDs for external reference
 */
function getActiveServersCommandIds() {
    return activeServersCommands_1.ActiveServersCommands.getCommandIds();
}


/***/ }),
/* 41 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ActiveServersCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const handleServerActions_1 = __webpack_require__(29);
/**
 * Active Servers Commands
 * VS Code command definitions for active servers functionality
 */
class ActiveServersCommands {
    // Store tree data provider reference for refresh operations
    static treeDataProvider;
    /**
     * Register all active servers commands
     */
    static registerCommands(context, treeDataProvider) {
        console.log('ACTIVE_SERVER: Registering active servers commands');
        // Store the tree data provider reference
        this.treeDataProvider = treeDataProvider;
        // Command: Show server actions (from tree item click)
        const showServerActionsCmd = vscode.commands.registerCommand('codeXR.activeServers.showActions', async (serverId) => {
            await handleServerActions_1.ServerActionHandlers.showServerActions(serverId);
        });
        // Command: Open server in browser
        const openInBrowserCmd = vscode.commands.registerCommand('codeXR.activeServers.openInBrowser', async (treeItem) => {
            const serverId = this.extractServerIdFromTreeItem(treeItem);
            if (serverId) {
                await handleServerActions_1.ServerActionHandlers.openInBrowser(serverId);
            }
        });
        // Command: Open server in lateral panel
        const openInPanelCmd = vscode.commands.registerCommand('codeXR.activeServers.openInPanel', async (treeItem) => {
            const serverId = this.extractServerIdFromTreeItem(treeItem);
            if (serverId) {
                await handleServerActions_1.ServerActionHandlers.openInPanel(serverId);
            }
        });
        // Command: Copy server URL to clipboard
        const copyUrlCmd = vscode.commands.registerCommand('codeXR.activeServers.copyUrl', async (treeItem) => {
            const serverId = this.extractServerIdFromTreeItem(treeItem);
            if (serverId) {
                await handleServerActions_1.ServerActionHandlers.copyUrl(serverId);
            }
        });
        // Command: Stop specific server
        const stopServerCmd = vscode.commands.registerCommand('codeXR.activeServers.stopServer', async (treeItem) => {
            const serverId = this.extractServerIdFromTreeItem(treeItem);
            if (serverId) {
                await handleServerActions_1.ServerActionHandlers.stopServer(serverId);
            }
        });
        // Command: Show server details
        const showDetailsCmd = vscode.commands.registerCommand('codeXR.activeServers.showDetails', async (treeItem) => {
            const serverId = this.extractServerIdFromTreeItem(treeItem);
            if (serverId) {
                await handleServerActions_1.ServerActionHandlers.showServerDetails(serverId);
            }
        });
        // Command: Stop all servers
        const stopAllServersCmd = vscode.commands.registerCommand('codeXR.activeServers.stopAllServers', async () => {
            await handleServerActions_1.ServerActionHandlers.stopAllServers();
        });
        // Command: Refresh server statuses
        const refreshServersCmd = vscode.commands.registerCommand('codeXR.activeServers.refreshServers', async () => {
            // Use the unified tree data provider if available
            if (this.treeDataProvider && this.treeDataProvider.refresh) {
                console.log('ACTIVE_SERVER: Refreshing unified tree view');
                this.treeDataProvider.refresh();
            }
            else {
                console.log('ACTIVE_SERVER: Using fallback refresh handler');
                vscode.commands.executeCommand('codexr.tree.refresh');
            }
        });
        // Command: Open active servers view 
        const openViewCmd = vscode.commands.registerCommand('codeXR.activeServers.openView', async () => {
            await vscode.commands.executeCommand('codexrTree.focus');
        });
        // Register all commands with the extension context
        context.subscriptions.push(showServerActionsCmd, openInBrowserCmd, openInPanelCmd, copyUrlCmd, stopServerCmd, showDetailsCmd, stopAllServersCmd, refreshServersCmd, openViewCmd);
        console.log('ACTIVE_SERVER: Registered 9 active servers commands');
    }
    /**
     * Extract server ID from tree item context
     * @private
     */
    static extractServerIdFromTreeItem(treeItem) {
        // Handle different tree item formats
        if (treeItem && treeItem.server && treeItem.server.id) {
            console.log(`ACTIVE_SERVER: Extracted server ID from tree item: ${treeItem.server.id}`);
            return treeItem.server.id;
        }
        // Fallback: if treeItem is a string, use it directly (for backward compatibility)
        if (typeof treeItem === 'string') {
            console.log(`ACTIVE_SERVER: Using direct server ID: ${treeItem}`);
            return treeItem;
        }
        console.error('ACTIVE_SERVER: Could not extract server ID from tree item:', treeItem);
        return null;
    }
    /**
     * Get all command IDs for external reference
     */
    static getCommandIds() {
        return {
            showActions: 'codeXR.activeServers.showActions',
            openInBrowser: 'codeXR.activeServers.openInBrowser',
            openInPanel: 'codeXR.activeServers.openInPanel',
            copyUrl: 'codeXR.activeServers.copyUrl',
            stopServer: 'codeXR.activeServers.stopServer',
            showDetails: 'codeXR.activeServers.showDetails',
            stopAllServers: 'codeXR.activeServers.stopAllServers',
            refreshServers: 'codeXR.activeServers.refreshServers',
            openView: 'codeXR.activeServers.openView'
        };
    }
}
exports.ActiveServersCommands = ActiveServersCommands;


/***/ }),
/* 42 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.registerBabiaExamplesCommands = registerBabiaExamplesCommands;
const babiaExamplesCommands_1 = __webpack_require__(43);
/**
 * Register Babia Examples Commands
 * Entry point for registering all Babia examples related commands
 */
function registerBabiaExamplesCommands(context, treeDataProvider) {
    console.log('EXAMPLES: Registering Babia examples commands...');
    babiaExamplesCommands_1.BabiaExamplesCommands.registerCommands(context, treeDataProvider);
    console.log('EXAMPLES: Babia examples commands registration complete');
}


/***/ }),
/* 43 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BabiaExamplesCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const handleExampleClicks_1 = __webpack_require__(44);
/**
 * Babia Examples Commands
 * VS Code command definitions for Babia examples functionality
 */
class BabiaExamplesCommands {
    /**
     * Register all Babia examples commands
     */
    static registerCommands(context, treeDataProvider) {
        console.log('EXAMPLES: Registering Babia examples commands...');
        // Initialize the click handler
        const clickHandler = new handleExampleClicks_1.ExampleClickHandler(context);
        // Command: Launch example
        const launchExampleCmd = vscode.commands.registerCommand('codeXR.babiaExamples.launchExample', async (example) => {
            try {
                console.log(`EXAMPLES: Launch command triggered for "${example.name}"`);
                await clickHandler.handleExampleClick(example);
            }
            catch (error) {
                console.error('EXAMPLES: Error in launch command:', error);
                vscode.window.showErrorMessage(`Failed to launch example: ${error instanceof Error ? error.message : String(error)}`);
            }
        });
        // Command: Refresh examples (only register if tree data provider is available)
        if (treeDataProvider) {
            const refreshExamplesCmd = vscode.commands.registerCommand('codeXR.babiaExamples.refresh', async () => {
                try {
                    console.log('EXAMPLES: Refresh command triggered');
                    await treeDataProvider.rescan();
                    vscode.window.showInformationMessage('Babia examples refreshed');
                }
                catch (error) {
                    console.error('EXAMPLES: Error in refresh command:', error);
                    vscode.window.showErrorMessage(`Failed to refresh examples: ${error instanceof Error ? error.message : String(error)}`);
                }
            });
            context.subscriptions.push(refreshExamplesCmd);
        }
        // Command: Open examples folder
        const openExamplesFolderCmd = vscode.commands.registerCommand('codeXR.babiaExamples.openFolder', async () => {
            try {
                console.log('EXAMPLES: Open folder command triggered');
                const workspaceRoots = vscode.workspace.workspaceFolders;
                if (!workspaceRoots || workspaceRoots.length === 0) {
                    vscode.window.showWarningMessage('No workspace folder is open');
                    return;
                }
                const examplesPath = vscode.Uri.joinPath(workspaceRoots[0].uri, 'examples', 'charts');
                await vscode.commands.executeCommand('vscode.openFolder', examplesPath, { forceNewWindow: false });
            }
            catch (error) {
                console.error('EXAMPLES: Error in open folder command:', error);
                vscode.window.showErrorMessage(`Failed to open examples folder: ${error instanceof Error ? error.message : String(error)}`);
            }
        });
        // Command: Show example details
        const showExampleDetailsCmd = vscode.commands.registerCommand('codeXR.babiaExamples.showDetails', async (example) => {
            try {
                console.log(`EXAMPLES: Show details command triggered for "${example.name}"`);
                const details = [
                    `# Babia Example: ${example.name}`,
                    '',
                    `**Category:** ${example.category}`,
                    `**Valid:** ${example.isValid ? 'Yes' : 'No'}`,
                    `**Directory:** ${example.directory}`,
                    `**HTML File:** ${example.htmlFilePath || 'Not found'}`,
                    ''
                ];
                if (example.description) {
                    details.push(`**Description:** ${example.description}`);
                    details.push('');
                }
                if (example.lastModified) {
                    const lastModified = new Date(example.lastModified).toLocaleString();
                    details.push(`**Last Modified:** ${lastModified}`);
                    details.push('');
                }
                if (!example.isValid) {
                    details.push('## Issues');
                    details.push('- No valid HTML file found in the example directory');
                    details.push('');
                }
                details.push('## Actions');
                if (example.isValid) {
                    details.push('- Click the example in the tree to launch it');
                }
                else {
                    details.push('- Fix the HTML file issue to make this example launchable');
                }
                const content = details.join('\\n');
                // Create and show a new untitled document with the details
                const doc = await vscode.workspace.openTextDocument({
                    content: content,
                    language: 'markdown'
                });
                await vscode.window.showTextDocument(doc);
            }
            catch (error) {
                console.error('EXAMPLES: Error in show details command:', error);
                vscode.window.showErrorMessage(`Failed to show example details: ${error instanceof Error ? error.message : String(error)}`);
            }
        });
        // Command: Open examples view
        const openExamplesViewCmd = vscode.commands.registerCommand('codeXR.babiaExamples.openView', async () => {
            try {
                console.log('EXAMPLES: Open view command triggered');
                await vscode.commands.executeCommand('codeXR.babiaExamplesView.focus');
            }
            catch (error) {
                console.error('EXAMPLES: Error in open view command:', error);
                // Don't show error message for this, it's likely the view isn't registered yet
            }
        });
        // Register commands that don't require tree data provider
        const commandsToRegister = [
            launchExampleCmd,
            openExamplesFolderCmd,
            showExampleDetailsCmd,
            openExamplesViewCmd
        ];
        // Register all commands with the extension context
        context.subscriptions.push(...commandsToRegister);
        // Store click handler for cleanup
        context.subscriptions.push({
            dispose: () => clickHandler.cleanup()
        });
        console.log(`EXAMPLES: Registered ${commandsToRegister.length} Babia examples commands`);
    }
}
exports.BabiaExamplesCommands = BabiaExamplesCommands;


/***/ }),
/* 44 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ExampleClickHandler = void 0;
const vscode = __importStar(__webpack_require__(1));
const exampleLauncher_1 = __webpack_require__(45);
/**
 * Handle Example Clicks
 * Manages user interactions with Babia examples in the tree view
 */
class ExampleClickHandler {
    exampleLauncher;
    constructor(context) {
        this.exampleLauncher = new exampleLauncher_1.ExampleLauncher(context);
        console.log('EXAMPLES: Example click handler initialized');
    }
    /**
     * Handle click on an example to launch it
     * @param example The example to launch
     */
    async handleExampleClick(example) {
        console.log(`EXAMPLES: User clicked on example "${example.name}"`);
        try {
            if (!example.isValid) {
                await this.handleInvalidExample(example);
                return;
            }
            // Show launching message
            const launchingMessage = vscode.window.setStatusBarMessage(`$(loading~spin) Launching Babia example "${example.name}"...`);
            try {
                const result = await this.exampleLauncher.launchExample(example);
                if (result.success) {
                    console.log(`EXAMPLES: Successfully launched "${example.name}" on port ${result.port}`);
                }
                else {
                    console.error(`EXAMPLES: Failed to launch "${example.name}":`, result.error);
                }
            }
            finally {
                launchingMessage.dispose();
            }
        }
        catch (error) {
            const errorMsg = `Failed to handle example click: ${error instanceof Error ? error.message : String(error)}`;
            console.error('EXAMPLES:', errorMsg);
            vscode.window.showErrorMessage(errorMsg);
        }
    }
    /**
     * Handle click on invalid example
     * @private
     */
    async handleInvalidExample(example) {
        console.log(`EXAMPLES: User clicked on invalid example "${example.name}"`);
        const action = await vscode.window.showWarningMessage(`Example "${example.name}" is not valid and cannot be launched.`, 'Show Details', 'Rescan Examples');
        switch (action) {
            case 'Show Details':
                await this.showExampleDetails(example);
                break;
            case 'Rescan Examples':
                await this.rescanExamples();
                break;
        }
    }
    /**
     * Show example details
     * @private
     */
    async showExampleDetails(example) {
        const details = [
            `Example: ${example.name}`,
            `Category: ${example.category}`,
            `Directory: ${example.directory}`,
            `HTML File: ${example.htmlFilePath || 'Not found'}`,
            `Valid: ${example.isValid ? 'Yes' : 'No'}`,
            ''
        ];
        if (example.description) {
            details.push(`Description: ${example.description}`);
        }
        if (!example.isValid) {
            details.push('Issues:');
            details.push('- No valid HTML file found in the example directory');
        }
        const content = details.join('\\n');
        // Create and show a new untitled document with the details
        const doc = await vscode.workspace.openTextDocument({
            content: content,
            language: 'plaintext'
        });
        await vscode.window.showTextDocument(doc);
    }
    /**
     * Rescan examples
     * @private
     */
    async rescanExamples() {
        console.log('EXAMPLES: User requested example rescan');
        const scanning = vscode.window.setStatusBarMessage('$(loading~spin) Scanning Babia examples...');
        try {
            const result = await this.exampleLauncher.scanExamples();
            console.log(`EXAMPLES: Rescan complete. Found ${result.validCount} valid, ${result.invalidCount} invalid examples`);
            // Refresh the tree view
            vscode.commands.executeCommand('codeXR.babiaExamples.refresh');
            // Show result message
            if (result.errors.length > 0) {
                vscode.window.showWarningMessage(`Rescan complete: ${result.validCount} valid, ${result.invalidCount} invalid examples. ${result.errors.length} errors occurred.`);
            }
            else {
                vscode.window.showInformationMessage(`Rescan complete: Found ${result.validCount} valid and ${result.invalidCount} invalid examples.`);
            }
        }
        catch (error) {
            console.error('EXAMPLES: Error during rescan:', error);
            vscode.window.showErrorMessage(`Failed to rescan examples: ${error instanceof Error ? error.message : String(error)}`);
        }
        finally {
            scanning.dispose();
        }
    }
    /**
     * Get the example launcher instance
     */
    getExampleLauncher() {
        return this.exampleLauncher;
    }
    /**
     * Cleanup method
     */
    async cleanup() {
        console.log('EXAMPLES: Cleaning up example click handler...');
        await this.exampleLauncher.cleanup();
    }
}
exports.ExampleClickHandler = ExampleClickHandler;


/***/ }),
/* 45 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ExampleLauncher = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(6));
const multiServerLauncher_1 = __webpack_require__(14);
/**
 * Example Launcher
 * Responsible for launching Babia examples using the existing server infrastructure
 */
class ExampleLauncher {
    context;
    multiServerLauncher;
    examplesCache = [];
    lastScanTime = 0;
    CACHE_DURATION = 30000; // 30 seconds
    constructor(context) {
        this.context = context;
        this.multiServerLauncher = new multiServerLauncher_1.MultiServerLauncher(context);
        console.log('EXAMPLES: Example launcher initialized');
    }
    /**
     * Scan for Babia examples in the charts directory
     * @returns Promise<ExampleScanResult>
     */
    async scanExamples() {
        console.log('EXAMPLES: Scanning for Babia examples...');
        const result = {
            examples: [],
            validCount: 0,
            invalidCount: 0,
            errors: []
        };
        try {
            // First, try to use the extension's own path to find CodeXR directory
            let workspaceRoot;
            // Method 1: Use extension context to find CodeXR directory
            const extensionPath = this.context.extensionPath;
            console.log(`EXAMPLES: Extension path: ${extensionPath}`);
            if (extensionPath.includes('CodeXR')) {
                // Extract CodeXR root from extension path
                const codeXRPath = extensionPath.substring(0, extensionPath.lastIndexOf('CodeXR') + 6);
                workspaceRoot = codeXRPath;
                console.log(`EXAMPLES: Found CodeXR from extension path: ${workspaceRoot}`);
            }
            else {
                // Method 2: Force use the known CodeXR path
                workspaceRoot = '/home/adrian/CodeXR';
                console.log(`EXAMPLES: Using hardcoded CodeXR path: ${workspaceRoot}`);
            }
            // Verify the path exists and has examples/charts
            let chartsPath = path.join(workspaceRoot, 'examples', 'charts');
            if (!fs.existsSync(chartsPath)) {
                // Method 3: Try to find from VS Code workspace folders as fallback
                const workspaceRoots = vscode.workspace.workspaceFolders;
                if (workspaceRoots && workspaceRoots.length > 0) {
                    const potentialCodeXRRoot = workspaceRoots.find(folder => folder.name === 'CodeXR' ||
                        folder.uri.fsPath.includes('CodeXR') ||
                        fs.existsSync(path.join(folder.uri.fsPath, 'examples', 'charts')));
                    if (potentialCodeXRRoot) {
                        workspaceRoot = potentialCodeXRRoot.uri.fsPath;
                        chartsPath = path.join(workspaceRoot, 'examples', 'charts');
                        console.log(`EXAMPLES: Found CodeXR from workspace folders: ${workspaceRoot}`);
                    }
                    else {
                        result.errors.push('Could not find CodeXR directory with examples/charts');
                        return result;
                    }
                }
                else {
                    result.errors.push('No workspace folder open and CodeXR path not found');
                    return result;
                }
            }
            console.log(`EXAMPLES: Using workspace root: ${workspaceRoot}`);
            console.log(`EXAMPLES: Scanning charts directory: ${chartsPath}`);
            if (!fs.existsSync(chartsPath)) {
                result.errors.push(`Charts directory not found: ${chartsPath}`);
                return result;
            }
            const chartDirectories = fs.readdirSync(chartsPath)
                .filter(item => {
                const itemPath = path.join(chartsPath, item);
                return fs.statSync(itemPath).isDirectory();
            });
            console.log(`EXAMPLES: Found ${chartDirectories.length} chart directories`);
            for (const chartDir of chartDirectories) {
                const chartPath = path.join(chartsPath, chartDir);
                const example = await this.processExampleDirectory(chartPath, chartDir);
                if (example) {
                    console.log(`EXAMPLES: FOUND chart ${chartDir} example`);
                    result.examples.push(example);
                    if (example.isValid) {
                        result.validCount++;
                    }
                    else {
                        result.invalidCount++;
                    }
                }
            }
            // Update cache
            this.examplesCache = result.examples;
            this.lastScanTime = Date.now();
            console.log(`EXAMPLES: total found ${result.examples.length}`);
            console.log(`EXAMPLES: Scan complete. Found ${result.validCount} valid and ${result.invalidCount} invalid examples`);
        }
        catch (error) {
            const errorMsg = `Failed to scan examples: ${error instanceof Error ? error.message : String(error)}`;
            console.error('EXAMPLES:', errorMsg);
            result.errors.push(errorMsg);
        }
        return result;
    }
    /**
     * Get cached examples or scan if cache is stale
     * @returns Promise<BabiaExample[]>
     */
    async getExamples() {
        const now = Date.now();
        if (this.examplesCache.length === 0 || (now - this.lastScanTime) > this.CACHE_DURATION) {
            console.log('EXAMPLES: Cache is stale, rescanning...');
            const result = await this.scanExamples();
            return result.examples;
        }
        console.log(`EXAMPLES: Using cached examples (${this.examplesCache.length} items)`);
        return this.examplesCache;
    }
    /**
     * Launch a specific Babia example
     * @param example The example to launch
     * @returns Promise<MultiServerLaunchResult>
     */
    async launchExample(example) {
        console.log(`EXAMPLES: Launching example "${example.name}" from ${example.htmlFilePath}`);
        try {
            if (!example.isValid) {
                throw new Error(`Example "${example.name}" is not valid - missing HTML file`);
            }
            if (!fs.existsSync(example.htmlFilePath)) {
                throw new Error(`HTML file not found: ${example.htmlFilePath}`);
            }
            console.log(`EXAMPLES: Delegating launch to multi-server launcher with user configuration`);
            // Create custom name in format "ExampleName" (already ends with proper format from scanning)
            const customName = example.name; // e.g., "DonutExample", "BarsExample"
            console.log(`SERVER: Using custom name '${customName}' for example server`);
            // Delegate everything to the multi-server launcher
            // This will handle:
            // - Reading current user configuration (HTTP mode, port, auto-open, lateral panel vs browser)
            // - Launching server with correct settings
            // - Auto-opening in the configured mode (if enabled)
            // - Registering in Active Servers
            const result = await this.multiServerLauncher.launchServer(example.htmlFilePath, customName);
            if (result.success) {
                console.log(`EXAMPLES: Successfully launched example "${example.name}" on port ${result.port}`);
                console.log(`EXAMPLES: Server configuration and auto-opening handled by shared infrastructure`);
            }
            else {
                console.error(`EXAMPLES: Failed to launch example "${example.name}":`, result.error);
                vscode.window.showErrorMessage(`Failed to launch example "${example.name}": ${result.error}`);
            }
            return result;
        }
        catch (error) {
            const errorMsg = `Failed to launch example "${example.name}": ${error instanceof Error ? error.message : String(error)}`;
            console.error('EXAMPLES:', errorMsg);
            vscode.window.showErrorMessage(errorMsg);
            return {
                success: false,
                error: errorMsg
            };
        }
    }
    /**
     * Process a single example directory
     * @private
     */
    async processExampleDirectory(directoryPath, categoryName) {
        try {
            console.log(`EXAMPLES: Processing directory: ${directoryPath}`);
            // Look for HTML files in the directory
            const files = fs.readdirSync(directoryPath);
            const htmlFiles = files.filter(file => file.toLowerCase().endsWith('.html'));
            console.log(`EXAMPLES: Found ${htmlFiles.length} HTML files in ${categoryName}: ${htmlFiles.join(', ')}`);
            if (htmlFiles.length === 0) {
                console.log(`EXAMPLES: No HTML files found in ${categoryName}`);
                return {
                    id: this.generateExampleId(categoryName, 'no-html'),
                    name: categoryName,
                    htmlFilePath: '',
                    directory: directoryPath,
                    category: categoryName,
                    description: 'No HTML files found',
                    isValid: false
                };
            }
            // Prefer index.html, then first HTML file
            let selectedFile = htmlFiles.find(file => file.toLowerCase() === 'index.html') || htmlFiles[0];
            const htmlFilePath = path.join(directoryPath, selectedFile);
            const stats = fs.statSync(htmlFilePath);
            const example = {
                id: this.generateExampleId(categoryName, selectedFile),
                name: this.formatExampleName(categoryName),
                htmlFilePath: htmlFilePath,
                directory: directoryPath,
                category: categoryName,
                description: this.generateDescription(categoryName, selectedFile),
                isValid: true,
                lastModified: stats.mtime.getTime()
            };
            console.log(`EXAMPLES: Created example: ${example.name} (${example.id})`);
            return example;
        }
        catch (error) {
            console.error(`EXAMPLES: Error processing directory ${directoryPath}:`, error);
            return null;
        }
    }
    /**
     * Generate a unique ID for an example
     * @private
     */
    generateExampleId(category, filename) {
        return `example_${category}_${filename}`.replace(/[^a-zA-Z0-9_]/g, '_');
    }
    /**
     * Format example name for display
     * @private
     */
    formatExampleName(category) {
        // Convert kebab-case or snake_case to Title Case
        return category
            .split(/[-_]/)
            .map(word => word.charAt(0).toUpperCase() + word.slice(1).toLowerCase())
            .join(' ');
    }
    /**
     * Generate description for example
     * @private
     */
    generateDescription(category, filename) {
        const formattedCategory = this.formatExampleName(category);
        if (filename.toLowerCase() === 'index.html') {
            return `${formattedCategory} visualization example`;
        }
        else {
            const formattedFilename = filename.replace('.html', '').replace(/[-_]/g, ' ');
            return `${formattedCategory} - ${formattedFilename}`;
        }
    }
    /**
     * Cleanup method
     */
    async cleanup() {
        console.log('EXAMPLES: Cleaning up example launcher...');
        // The MultiServerLauncher will handle its own cleanup
        this.examplesCache = [];
        this.lastScanTime = 0;
    }
}
exports.ExampleLauncher = ExampleLauncher;


/***/ }),
/* 46 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.registerVisualizeDataCommands = registerVisualizeDataCommands;
const visualizeDataCommands_1 = __webpack_require__(47);
/**
 * Register Visualize Data Commands
 * Entry point for registering all visualize data related commands
 */
function registerVisualizeDataCommands(context) {
    console.log('VISUALIZE_DATA: Registering visualize data commands...');
    visualizeDataCommands_1.VisualizeDataCommands.registerCommands(context);
    console.log('VISUALIZE_DATA: Visualize data commands registration complete');
}


/***/ }),
/* 47 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizeDataCommands = void 0;
const visualizationLauncher_1 = __webpack_require__(48);
const visualizationRestorer_1 = __webpack_require__(66);
const commandBuilder_1 = __webpack_require__(67);
const MODULE = 'VISUALIZE_DATA';
const BROWSE = 'BROWSE-VISUALIZATIONS';
/**
 * Visualize Data Commands
 * Uses CommandBuilder for consistent error handling and logging.
 */
class VisualizeDataCommands {
    static registerCommands(context) {
        console.log(`${MODULE}: Registering visualize data commands...`);
        /** Helper: create a launcher, run the action, cleanup. */
        const withLauncher = (action) => async () => {
            const launcher = new visualizationLauncher_1.VisualizationLauncher(context);
            await action(launcher);
            launcher.cleanup();
        };
        commandBuilder_1.CommandBuilder.registerAll(context, [
            {
                id: 'codeXR.visualizeData.chartType',
                module: MODULE,
                description: 'Chart Type selection',
                errorMessage: 'Failed to handle chart type',
                handler: withLauncher(l => l.handleChartType()),
            },
            {
                id: 'codeXR.visualizeData.selectJson',
                module: MODULE,
                description: 'Select JSON',
                errorMessage: 'Failed to select JSON',
                handler: withLauncher(l => l.handleSelectJson()),
            },
            {
                id: 'codeXR.visualizeData.dimensionMapping',
                module: MODULE,
                description: 'Dimension Mapping',
                errorMessage: 'Failed to handle dimension mapping',
                handler: withLauncher(l => l.handleDimensionMapping()),
            },
            {
                id: 'codeXR.visualizeData.mapDimensionField',
                module: MODULE,
                description: 'Map Dimension Field',
                errorMessage: 'Failed to map dimension field',
                handler: async (dimensionName) => {
                    console.log(`${MODULE}: Mapping dimension field for: ${dimensionName}`);
                    const launcher = new visualizationLauncher_1.VisualizationLauncher(context);
                    await launcher.handleDimensionFieldMapping(dimensionName);
                    launcher.cleanup();
                },
            },
            {
                id: 'codeXR.visualizeData.launchVisualization',
                module: MODULE,
                description: 'Launch Visualization',
                errorMessage: 'Failed to launch visualization',
                handler: withLauncher(l => l.handleLaunchVisualization()),
            },
            {
                id: 'codeXR.visualizeData.debugState',
                module: MODULE,
                description: 'Debug State',
                errorMessage: 'Failed to show debug state',
                handler: withLauncher(l => l.handleDebugState()),
            },
            {
                id: 'codeXR.browseVisualizations.launch',
                module: BROWSE,
                description: 'Launch stored visualization',
                errorMessage: 'Failed to launch visualization',
                handler: async (visualization) => {
                    console.log(`${BROWSE}: Launching: ${visualization.name}`);
                    const restorer = new visualizationRestorer_1.VisualizationRestorer(context);
                    await restorer.launchVisualization(visualization);
                },
            },
            {
                id: 'codeXR.browseVisualizations.resetAll',
                module: BROWSE,
                description: 'Reset all visualizations',
                errorMessage: 'Failed to reset visualizations',
                handler: async () => {
                    const restorer = new visualizationRestorer_1.VisualizationRestorer(context);
                    await restorer.resetAllVisualizations();
                },
            },
        ]);
        context.subscriptions.push({
            dispose: () => console.log(`${MODULE}: Commands cleanup complete`),
        });
    }
}
exports.VisualizeDataCommands = VisualizeDataCommands;


/***/ }),
/* 48 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizationLauncher = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(6));
const chartRegistry_1 = __webpack_require__(49);
const visualizeDataState_1 = __webpack_require__(51);
const jsonFieldAnalyzer_1 = __webpack_require__(52);
const nonceGenerator_1 = __webpack_require__(12);
const templateProcessor_1 = __webpack_require__(53);
const index_1 = __webpack_require__(65);
/**
 * Visualization Launcher
 * Manages visualization creation and launching using centralized template processing
 */
class VisualizationLauncher {
    context;
    stateManager;
    constructor(context) {
        this.context = context;
        console.log('VISUALIZE_DATA: Action handler initialized');
        this.stateManager = visualizeDataState_1.VisualizeDataStateManager.getInstance(context);
    }
    /**
     * Handle chart type selection
     */
    async handleChartType() {
        console.log('VISUALIZE_DATA: Chart type action triggered');
        try {
            // Get available charts from BabiaXR registry
            const chartRegistry = chartRegistry_1.BabiaChartRegistry.getInstance();
            const availableCharts = chartRegistry.getAllCharts();
            if (availableCharts.length === 0) {
                console.error('BABIA-TEMPLATES: No chart types found in registry');
                vscode.window.showErrorMessage('No chart templates available');
                return;
            }
            // Create quick pick items for available charts
            const quickPickItems = availableCharts.map(chart => ({
                label: chart.name,
                description: chart.description,
                detail: `Category: ${chart.category} | Dimensions: ${chart.dimensions.map(d => d.name).join(', ')}`,
                chart: chart
            }));
            // Show quick pick
            const selectedItem = await vscode.window.showQuickPick(quickPickItems, {
                placeHolder: 'Select a chart type for visualization',
                title: 'BabiaXR Chart Type Selection'
            });
            if (selectedItem && selectedItem.chart) {
                const selectedChart = selectedItem.chart;
                // Update state with selected chart
                this.stateManager.updateSelectedChart(selectedChart);
                // Trigger tree refresh to update display
                vscode.commands.executeCommand('codexr.servers.refresh');
                console.log(`BABIA-TEMPLATES: Chart type selected: ${selectedChart.name}`);
                vscode.window.showInformationMessage(`Chart type selected: ${selectedChart.name}`);
            }
            else {
                console.log('BABIA-TEMPLATES: Chart type selection cancelled');
            }
        }
        catch (error) {
            console.error('VISUALIZE_DATA: Error in chart type action:', error);
            vscode.window.showErrorMessage(`Failed to handle chart type: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Handle JSON file selection
     */
    async handleSelectJson() {
        console.log('VISUALIZE_DATA: Select JSON action triggered');
        try {
            const options = {
                canSelectMany: false,
                openLabel: 'Select JSON File',
                filters: {
                    'JSON files': ['json']
                },
                title: 'Select JSON Data File for Visualization'
            };
            const fileUri = await vscode.window.showOpenDialog(options);
            if (fileUri && fileUri[0]) {
                const filePath = fileUri[0].fsPath;
                const fileName = path.basename(filePath);
                // Analyze JSON file to extract field information
                console.log(`BABIA-TEMPLATES: Starting JSON analysis for ${fileName}`);
                const jsonAnalysis = await jsonFieldAnalyzer_1.JsonFieldAnalyzer.analyzeJsonFile(filePath);
                if (jsonAnalysis.success) {
                    console.log(`BABIA-TEMPLATES: JSON analysis successful - found ${jsonAnalysis.fields.length} fields`);
                    // Update state with selected JSON and analysis
                    this.stateManager.updateSelectedJson(filePath, fileName);
                    this.stateManager.updateJsonAnalysis(jsonAnalysis);
                    // Trigger tree refresh to update display
                    vscode.commands.executeCommand('codexr.servers.refresh');
                    console.log(`BABIA-TEMPLATES: JSON file selected: ${fileName} (${filePath})`);
                    vscode.window.showInformationMessage(`JSON file selected: ${fileName} (${jsonAnalysis.fields.length} fields found)`);
                }
                else {
                    console.error(`BABIA-TEMPLATES: JSON analysis failed: ${jsonAnalysis.error}`);
                    vscode.window.showErrorMessage(`Failed to analyze JSON file: ${jsonAnalysis.error}`);
                }
            }
            else {
                console.log('BABIA-TEMPLATES: No JSON file selected');
            }
        }
        catch (error) {
            console.error('VISUALIZE_DATA: Error in select JSON action:', error);
            vscode.window.showErrorMessage(`Failed to select JSON file: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Handle dimension mapping configuration (informational only)
     */
    async handleDimensionMapping() {
        console.log('DIMENSION-MAPPING: Dimension mapping overview requested');
        try {
            const state = this.stateManager.getState();
            if (!state.selectedChart) {
                vscode.window.showWarningMessage('Please select a chart type first');
                return;
            }
            if (!state.jsonAnalysis) {
                vscode.window.showWarningMessage('Please select a JSON file first');
                return;
            }
            // Show dimension mapping status overview
            const requiredDimensions = state.selectedChart.dimensions.filter(d => d.required);
            const mappedDimensions = state.dimensionMappings.length;
            const totalDimensions = state.selectedChart.dimensions.length;
            let message = `Chart: ${state.selectedChart.name}\n`;
            message += `Dimensions: ${mappedDimensions}/${totalDimensions} configured\n`;
            message += `Required: ${requiredDimensions.map(d => d.name).join(', ')}\n`;
            message += `Available fields: ${state.jsonAnalysis.fields.length}`;
            // Check for duplicate fields
            const duplicateFields = this.findDuplicateFields(state);
            if (duplicateFields.length > 0) {
                message += `\n⚠️ Duplicate field usage: ${duplicateFields.join(', ')}`;
            }
            vscode.window.showInformationMessage(`Dimension Mapping Status:\n${message}`);
        }
        catch (error) {
            console.error('DIMENSION-MAPPING: Error in dimension mapping overview:', error);
            vscode.window.showErrorMessage(`Failed to show dimension mapping overview: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Find fields that are used in multiple dimension mappings
     */
    findDuplicateFields(state) {
        const fieldCounts = new Map();
        state.dimensionMappings.forEach((mapping) => {
            const count = fieldCounts.get(mapping.dataField) || 0;
            fieldCounts.set(mapping.dataField, count + 1);
        });
        return Array.from(fieldCounts.entries())
            .filter(([_, count]) => count > 1)
            .map(([field, _]) => field);
    }
    /**
     * Handle dimension field mapping for a specific dimension
     */
    async handleDimensionFieldMapping(dimensionName) {
        console.log(`DIMENSION-MAPPING: Field mapping for dimension '${dimensionName}' triggered`);
        try {
            const state = this.stateManager.getState();
            if (!state.selectedChart || !state.jsonAnalysis) {
                vscode.window.showWarningMessage('Please select a chart type and JSON file first');
                return;
            }
            // Find the dimension definition
            const dimension = state.selectedChart.dimensions.find(d => d.name === dimensionName);
            if (!dimension) {
                vscode.window.showErrorMessage(`Dimension '${dimensionName}' not found in chart`);
                return;
            }
            // Get available fields for this dimension type
            const availableFields = jsonFieldAnalyzer_1.JsonFieldAnalyzer.getFieldsForDimensionType(state.jsonAnalysis, dimension.dataType);
            if (availableFields.length === 0) {
                const typeInfo = dimension.dataType === 'numeric' ? 'numeric fields' : 'fields';
                vscode.window.showWarningMessage(`No ${typeInfo} available for dimension '${dimension.name}'`);
                return;
            }
            // Create QuickPick items with duplicate field indicators
            const quickPickItems = availableFields.map(field => {
                const displayInfo = jsonFieldAnalyzer_1.JsonFieldAnalyzer.formatFieldForDisplay(field);
                const isAlreadyUsed = state.dimensionMappings.some(mapping => mapping.dataField === field.name && mapping.dimension !== dimensionName);
                let label = displayInfo.label;
                let description = displayInfo.description;
                if (isAlreadyUsed) {
                    label += ' ⚠️';
                    description += ' (already used in another dimension)';
                }
                return {
                    label: label,
                    description: description,
                    detail: displayInfo.detail,
                    field: field
                };
            });
            // Show QuickPick
            const selectedItem = await vscode.window.showQuickPick(quickPickItems, {
                placeHolder: `Select field for ${dimension.name} (${dimension.dataType === 'numeric' ? 'numeric only' : 'any value'})`,
                title: `Map Dimension: ${dimension.name}`,
                matchOnDescription: true,
                matchOnDetail: true
            });
            if (selectedItem) {
                // Check if field is already used and warn user
                const isAlreadyUsed = state.dimensionMappings.some(mapping => mapping.dataField === selectedItem.field.name && mapping.dimension !== dimensionName);
                if (isAlreadyUsed) {
                    const existingMapping = state.dimensionMappings.find(mapping => mapping.dataField === selectedItem.field.name && mapping.dimension !== dimensionName);
                    console.log(`DIMENSION-MAPPING: Warning - Field '${selectedItem.field.name}' is already mapped to dimension '${existingMapping?.dimension}'`);
                    const proceed = await vscode.window.showWarningMessage(`Field '${selectedItem.field.name}' is already used for dimension '${existingMapping?.dimension}'. Continue?`, 'Yes, Continue', 'Cancel');
                    if (proceed !== 'Yes, Continue') {
                        console.log(`DIMENSION-MAPPING: Duplicate field mapping cancelled by user`);
                        return;
                    }
                }
                // Update dimension mapping
                this.stateManager.updateSingleDimensionMapping(dimensionName, selectedItem.field.name);
                // Trigger tree refresh
                vscode.commands.executeCommand('codexr.servers.refresh');
                console.log(`DIMENSION-MAPPING: Mapped dimension '${dimensionName}' to field '${selectedItem.field.name}'`);
                vscode.window.showInformationMessage(`Mapped ${dimension.name} to field: ${selectedItem.field.name}`);
            }
            else {
                console.log(`DIMENSION-MAPPING: Field mapping for dimension '${dimensionName}' cancelled`);
            }
        }
        catch (error) {
            console.error(`DIMENSION-MAPPING: Error in field mapping for dimension '${dimensionName}':`, error);
            vscode.window.showErrorMessage(`Failed to map dimension field: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Handle visualization launch
     */
    async handleLaunchVisualization() {
        console.log('VISUALIZE_DATA: Launch visualization action triggered');
        try {
            const state = this.stateManager.getState();
            // Check if ready to launch (icon should already be showing correct state)
            if (!state.isReadyToLaunch) {
                const missingItems = [];
                if (!state.selectedChart) {
                    missingItems.push('Chart Type');
                }
                if (!state.selectedJsonPath) {
                    missingItems.push('JSON File');
                }
                if (!state.isDimensionMappingConfigured) {
                    missingItems.push('Dimension Mapping');
                }
                vscode.window.showWarningMessage(`Cannot launch visualization. Please configure: ${missingItems.join(', ')}`);
                return;
            }
            // Get visualization name from user
            const visualizationName = await vscode.window.showInputBox({
                prompt: 'Enter a name for your visualization',
                placeHolder: 'e.g., ventas, sales_analysis',
                value: 'my_visualization',
                validateInput: (value) => {
                    if (!value || value.trim().length === 0) {
                        return 'Visualization name cannot be empty';
                    }
                    if (!/^[a-zA-Z0-9_-]+$/.test(value.trim())) {
                        return 'Name can only contain letters, numbers, underscores, and dashes';
                    }
                    return null;
                }
            });
            if (!visualizationName) {
                console.log('VISUALIZE_DATA: User cancelled visualization name input');
                return;
            }
            // Generate secure unique name
            const nonce = (0, nonceGenerator_1.generateNonce)(8); // 8 bytes = 16 hex characters
            const uniqueName = `${visualizationName.trim()}_${nonce}`;
            console.log('VISUALIZE_DATA: Creating visualization:', uniqueName);
            // Prepare visualization directory
            const visualizationDir = await this.prepareVisualizationDirectory(uniqueName);
            // Generate visualization files
            const result = await this.generateVisualizationFiles(state, visualizationDir, visualizationName.trim());
            if (!result.success) {
                vscode.window.showErrorMessage(`Failed to generate visualization: ${result.error}`);
                return;
            }
            // Launch the server with custom name
            const indexHtmlPath = path.join(visualizationDir, 'index.html');
            console.log('VISUALIZE_DATA: Launching server with file:', indexHtmlPath);
            console.log(`SERVER: Using custom name '${visualizationName.trim()}' for visualization server`);
            const launchResult = await (0, index_1.launchServerWithFile)(this.context, indexHtmlPath, visualizationName.trim());
            if (launchResult.success && launchResult.serverUrl) {
                vscode.window.showInformationMessage(`🚀 Visualization '${visualizationName}' launched successfully!`, 'View in Browser').then(selection => {
                    if (selection === 'View in Browser' && launchResult.serverUrl) {
                        vscode.env.openExternal(vscode.Uri.parse(launchResult.serverUrl));
                    }
                });
            }
            else {
                vscode.window.showErrorMessage(`Failed to launch visualization server: ${launchResult.error || 'Unknown error'}`);
            }
        }
        catch (error) {
            console.error('VISUALIZE_DATA: Error in launch visualization action:', error);
            vscode.window.showErrorMessage(`Failed to launch visualization: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Prepare the visualization directory structure
     */
    async prepareVisualizationDirectory(uniqueName) {
        const globalStorageUri = this.context.globalStorageUri;
        const visualizeDataDir = path.join(globalStorageUri.fsPath, 'visualize-data');
        const visualizationDir = path.join(visualizeDataDir, uniqueName);
        // Ensure directories exist
        if (!fs.existsSync(visualizeDataDir)) {
            fs.mkdirSync(visualizeDataDir, { recursive: true });
            console.log('VISUALIZE_DATA: Created visualize-data directory:', visualizeDataDir);
        }
        if (!fs.existsSync(visualizationDir)) {
            fs.mkdirSync(visualizationDir, { recursive: true });
            console.log('VISUALIZE_DATA: Created visualization directory:', visualizationDir);
        }
        return visualizationDir;
    }
    /**
     * Generate visualization files using centralized TemplateProcessor
     */
    async generateVisualizationFiles(state, visualizationDir, userVisualizationName) {
        try {
            if (!state.selectedChart || !state.selectedJsonPath) {
                return { success: false, error: 'Missing chart or JSON file configuration' };
            }
            console.log('VISUALIZATION_LAUNCHER: Using centralized TemplateProcessor for HTML generation');
            // Copy JSON file as data.json
            const dataJsonPath = path.join(visualizationDir, 'data.json');
            fs.copyFileSync(state.selectedJsonPath, dataJsonPath);
            console.log('VISUALIZATION_LAUNCHER: Copied data file to:', dataJsonPath);
            // Prepare output path for index.html
            const indexHtmlPath = path.join(visualizationDir, 'index.html');
            // Use centralized TemplateProcessor to generate the complete XR visualization
            const result = await templateProcessor_1.TemplateProcessor.generateXRVisualization(state.selectedChart.id, state.dimensionMappings, userVisualizationName, './data.json', this.context, indexHtmlPath, undefined // No analysis data available in data visualization context
            );
            if (!result.success) {
                console.error('VISUALIZATION_LAUNCHER: TemplateProcessor failed:', result.error);
                return {
                    success: false,
                    error: `Template processing failed: ${result.error}`
                };
            }
            console.log('VISUALIZATION_LAUNCHER: Successfully generated index.html using TemplateProcessor');
            return { success: true };
        }
        catch (error) {
            console.error('VISUALIZATION_LAUNCHER: Error generating visualization files:', error);
            return {
                success: false,
                error: error instanceof Error ? error.message : String(error)
            };
        }
    }
    /**
     * Handle debug state command (for troubleshooting)
     */
    async handleDebugState() {
        console.log('VISUALIZE_DATA: Debug state action triggered');
        try {
            const state = this.stateManager.getState();
            // Validate file path existence
            const fileExists = state.selectedJsonPath ? fs.existsSync(state.selectedJsonPath) : false;
            // Prepare state information
            const stateInfo = {
                selectedChart: state.selectedChart?.name || 'None',
                selectedJsonPath: state.selectedJsonPath || 'None',
                selectedJsonName: state.selectedJsonName || 'None',
                fileExists: fileExists,
                jsonAnalysisPresent: !!state.jsonAnalysis,
                jsonAnalysisFields: state.jsonAnalysis ? state.jsonAnalysis.fields.map(f => f.name) : [],
                dimensionMappingsCount: state.dimensionMappings.length,
                isDimensionMappingConfigured: state.isDimensionMappingConfigured,
                isReadyToLaunch: state.isReadyToLaunch,
                requiredDimensions: state.selectedChart?.dimensions.map(d => d.name) || [],
                mappedDimensions: state.dimensionMappings.map(m => `${m.dimension}: ${m.dataField}`)
            };
            // Create diagnostic message
            const message = [
                'Visualize Data State Diagnostic:',
                '',
                `Chart: ${stateInfo.selectedChart}`,
                `Required Dimensions: [${stateInfo.requiredDimensions.join(', ')}]`,
                '',
                `JSON File: ${stateInfo.selectedJsonName}`,
                `Path: ${stateInfo.selectedJsonPath}`,
                `File Exists: ${stateInfo.fileExists}`,
                `Analysis Present: ${stateInfo.jsonAnalysisPresent}`,
                `Available Fields: [${stateInfo.jsonAnalysisFields.join(', ')}]`,
                '',
                `Mapped Dimensions: ${stateInfo.mappedDimensions.length}`,
                ...stateInfo.mappedDimensions.map(mapping => `  - ${mapping}`),
                '',
                `Configuration Complete: ${stateInfo.isDimensionMappingConfigured}`,
                `Ready to Launch: ${stateInfo.isReadyToLaunch}`
            ].join('\n');
            console.log('VISUALIZE_DATA: State diagnostic:', stateInfo);
            // Show diagnostic information
            await vscode.window.showInformationMessage('Visualize Data state diagnostic sent to console. Check Output > Log (Extension Host) for details.', { modal: false });
            console.log('VISUALIZE_DATA: Full state diagnostic:\n' + message);
        }
        catch (error) {
            console.error('VISUALIZE_DATA: Error generating debug state:', error);
            vscode.window.showErrorMessage(`Debug state failed: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Cleanup resources
     */
    cleanup() {
        console.log('VISUALIZE_DATA: Action handler cleanup');
        // Note: We don't dispose the state manager here as it may be used by other components
    }
    /**
     * Get state manager instance
     */
    getStateManager() {
        return this.stateManager;
    }
}
exports.VisualizationLauncher = VisualizationLauncher;


/***/ }),
/* 49 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BabiaChartRegistry = void 0;
const templateCharts_1 = __webpack_require__(50);
/**
 * BabiaXR Chart Registry
 * Central registry for available chart types and their metadata
 */
class BabiaChartRegistry {
    static instance;
    charts = new Map();
    constructor() {
        this.initializeCharts();
    }
    /**
     * Get the singleton instance
     */
    static getInstance() {
        if (!BabiaChartRegistry.instance) {
            BabiaChartRegistry.instance = new BabiaChartRegistry();
        }
        return BabiaChartRegistry.instance;
    }
    /**
     * Initialize all chart definitions from templates
     */
    initializeCharts() {
        // Register all chart templates
        for (const chartTemplate of templateCharts_1.chartTemplates) {
            this.charts.set(chartTemplate.id, chartTemplate);
        }
        console.log('BABIA_TEMPLATES: Initialized chart registry with chart templates');
    }
    /**
     * Register a new chart type
     */
    registerChart(chart) {
        this.charts.set(chart.id, chart);
        console.log(`BABIA_TEMPLATES: Registered chart type '${chart.id}'`);
    }
    /**
     * Get a chart by ID
     */
    getChart(chartId) {
        return this.charts.get(chartId);
    }
    /**
     * Get all available charts
     */
    getAllCharts() {
        return Array.from(this.charts.values());
    }
    /**
     * Get charts by category
     */
    getChartsByCategory(category) {
        return Array.from(this.charts.values()).filter(chart => chart.category === category);
    }
    /**
     * Check if a chart type exists
     */
    hasChart(chartId) {
        return this.charts.has(chartId);
    }
    /**
     * Get all available chart IDs
     */
    getChartIds() {
        return Array.from(this.charts.keys());
    }
    /**
     * Get chart names for display
     */
    getChartNames() {
        return Array.from(this.charts.values()).map(chart => ({
            id: chart.id,
            name: chart.name,
            description: chart.description
        }));
    }
}
exports.BabiaChartRegistry = BabiaChartRegistry;


/***/ }),
/* 50 */
/***/ ((__unused_webpack_module, exports) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.chartTemplates = void 0;
/**
 * BabiaXR Chart Templates
 * Defines all available chart types with their metadata and simplified HTML templates
 */
exports.chartTemplates = [
    // Bar Chart Template
    {
        id: 'bars',
        name: 'Bar Chart',
        description: '3D vertical bars representing data values',
        category: 'linear',
        dimensions: [
            {
                name: 'x_axis',
                label: 'X-Axis',
                dataType: 'any',
                required: true,
                description: 'Field containing category names for x-axis'
            },
            {
                name: 'height',
                label: 'Height',
                dataType: 'numeric',
                required: true,
                description: 'Field containing numeric values for bar heights'
            }
        ],
        htmlTemplate: `<!-- Bar Chart -->
                <a-entity id="chart"
                    babia-bars="from: data;
                                title: {{TITLE}};
                                legend: true;
                                palette: {{PALETTE}};
                                x_axis: {{X_AXIS_FIELD}};
                                height: {{HEIGHT_FIELD}};
                                axis_name: true"
                    position="0 2 -10"
                    rotation="0 0 0"
                    scale="1.5 1.5 1.5"
                    class="babiaxraycasterclass">
                </a-entity>`
    },
    // Barsmap Chart Template
    {
        id: 'barsmap',
        name: 'Barsmap Chart',
        description: '3D bar map with multiple axes representing data relationships',
        category: 'linear',
        dimensions: [
            {
                name: 'x_axis',
                label: 'X-Axis',
                dataType: 'any',
                required: true,
                description: 'Field containing category names for x-axis'
            },
            {
                name: 'z_axis',
                label: 'Z-Axis',
                dataType: 'any',
                required: true,
                description: 'Field containing category names for z-axis'
            },
            {
                name: 'height',
                label: 'Height',
                dataType: 'numeric',
                required: true,
                description: 'Field containing numeric values for bar heights'
            }
        ],
        htmlTemplate: `<!-- Barsmap Chart -->
                <a-entity id="chart"
                    babia-barsmap="from: data;
                                   title: {{TITLE}};
                                   legend: true;
                                   palette: {{PALETTE}};
                                   x_axis: {{X_AXIS_FIELD}};
                                   z_axis: {{Z_AXIS_FIELD}};
                                   height: {{HEIGHT_FIELD}};
                                   axis_name: true"
                    position="0 2 -10"
                    rotation="0 0 0"
                    scale="1.5 1.5 1.5"
                    class="babiaxraycasterclass">
                </a-entity>`
    },
    // Cyls Chart Template
    {
        id: 'cyls',
        name: 'Cyls Chart',
        description: '3D cylinders representing data values with configurable radius',
        category: 'cylindrical',
        dimensions: [
            {
                name: 'x_axis',
                label: 'X-Axis',
                dataType: 'any',
                required: true,
                description: 'Field containing category names for x-axis'
            },
            {
                name: 'height',
                label: 'Height',
                dataType: 'numeric',
                required: true,
                description: 'Field containing numeric values for cylinder heights'
            },
            {
                name: 'radius',
                label: 'Radius',
                dataType: 'numeric',
                required: true,
                description: 'Field containing numeric values for cylinder radius'
            }
        ],
        htmlTemplate: `<!-- Cyls Chart -->
                <a-entity id="chart"
                    babia-cyls="from: data;
                                title: {{TITLE}};
                                legend: true;
                                palette: {{PALETTE}};
                                x_axis: {{X_AXIS_FIELD}};
                                height: {{HEIGHT_FIELD}};
                                radius: {{RADIUS_FIELD}};
                                axis_name: true;
                                radiusMax: 1;"
                    position="0 2 -10"
                    rotation="0 0 0"
                    scale="1.5 1.5 1.5"
                    class="babiaxraycasterclass">
                </a-entity>`
    },
    // Cylsmap Chart Template
    {
        id: 'cylsmap',
        name: 'Cylsmap Chart',
        description: '3D cylinder map with multiple axes representing data relationships',
        category: 'cylindrical',
        dimensions: [
            {
                name: 'x_axis',
                label: 'X-Axis',
                dataType: 'any',
                required: true,
                description: 'Field containing category names for x-axis'
            },
            {
                name: 'z_axis',
                label: 'Z-Axis',
                dataType: 'any',
                required: true,
                description: 'Field containing category names for z-axis'
            },
            {
                name: 'height',
                label: 'Height',
                dataType: 'numeric',
                required: true,
                description: 'Field containing numeric values for cylinder heights'
            },
            {
                name: 'radius',
                label: 'Radius Values',
                dataType: 'numeric',
                required: true,
                description: 'Field containing numeric values for cylinder radius'
            }
        ],
        htmlTemplate: `<!-- Cylsmap Chart -->
                <a-entity id="chart"
                    babia-cylsmap="from: data;
                                   title: {{TITLE}};
                                   legend: true;
                                   palette: {{PALETTE}};
                                   x_axis: {{X_AXIS_FIELD}};
                                   z_axis: {{Z_AXIS_FIELD}};
                                   height: {{HEIGHT_FIELD}};
                                   radius: {{RADIUS_FIELD}};
                                   axis_name: true;
                                   radiusMax: 1;"
                    position="0 2 -10"
                    rotation="0 0 0"
                    scale="1.5 1.5 1.5"
                    class="babiaxraycasterclass">
                </a-entity>`
    },
    // Donut Chart Template
    {
        id: 'donut',
        name: 'Donut Chart',
        description: 'A circular chart with a hole in the center, ideal for showing proportional data',
        category: 'circular',
        dimensions: [
            {
                name: 'key',
                label: 'Key',
                dataType: 'any',
                required: true,
                description: 'Field containing category names'
            },
            {
                name: 'size',
                label: 'Size',
                dataType: 'numeric',
                required: true,
                description: 'Field containing numeric values for each category'
            }
        ],
        htmlTemplate: `<!-- Donut Chart -->
                <a-entity id="chart"
                    babia-doughnut="from: data;
                                 title: {{TITLE}};
                                titlePosition: 2.5 0 -3;
                                 legend: true;
                                 palette: {{PALETTE}};
                                 key: {{KEY_FIELD}};
                                 size: {{SIZE_FIELD}};
                                 axis_name: true"
                    position="0 4 0"
                    rotation="90 0 0"
                    scale="1.5 1.5 1.5"
                    class="babiaxraycasterclass">
                </a-entity>`
    },
    // Pie Chart Template
    {
        id: 'pie',
        name: 'Pie Chart',
        description: 'Circular chart divided into sectors representing proportional data',
        category: 'circular',
        dimensions: [
            {
                name: 'key',
                label: 'Key',
                dataType: 'any',
                required: true,
                description: 'Field containing category names'
            },
            {
                name: 'size',
                label: 'Size',
                dataType: 'numeric',
                required: true,
                description: 'Field containing numeric values for each sector'
            }
        ],
        htmlTemplate: `<!-- Pie Chart -->
                <a-entity id="chart"
                    babia-pie="from: data;
                               title: {{TITLE}};
                                titlePosition: 2.5 0 -3;
                               legend: true;
                               palette: {{PALETTE}};
                               key: {{KEY_FIELD}};
                               size: {{SIZE_FIELD}};
                               axis_name: true"
                    position="0 4 0"
                    rotation="90 0 0"
                    scale="1.5 1.5 1.5"
                    class="babiaxraycasterclass">
                </a-entity>`
    },
    // Bubbles Chart Template
    {
        id: 'bubbles',
        name: 'Bubbles Chart',
        description: '3D bubbles representing data values with variable size and position',
        category: 'scatter',
        dimensions: [
            {
                name: 'x_axis',
                label: 'X-Axis',
                dataType: 'any',
                required: true,
                description: 'Field containing values for x-axis positioning'
            },
            {
                name: 'z_axis',
                label: 'Z-Axis',
                dataType: 'any',
                required: true,
                description: 'Field containing values for z-axis positioning'
            },
            {
                name: 'height',
                label: 'Height',
                dataType: 'numeric',
                required: true,
                description: 'Field containing numeric values for bubble height positioning'
            },
            {
                name: 'radius',
                label: 'Radius',
                dataType: 'numeric',
                required: true,
                description: 'Field containing numeric values for bubble radius/size'
            }
        ],
        htmlTemplate: `<!-- Bubbles Chart -->
                <a-entity id="chart"
                    babia-bubbles="from: data;
                                   title: {{TITLE}};
                                   legend: true;
                                   palette: {{PALETTE}};
                                   x_axis: {{X_AXIS_FIELD}};
                                   z_axis: {{Z_AXIS_FIELD}};
                                   height: {{HEIGHT_FIELD}};
                                   radius: {{RADIUS_FIELD}};
                                   axis_name: true;
                                   heightMax: 5;
                                   radiusMax: 1;"

                    position="0 2 -10"
                    rotation="0 0 0"
                    scale="1.5 1.5 1.5"
                    class="babiaxraycasterclass">
                </a-entity>`
    },
    // Boats Chart Template
    {
        id: 'boats',
        name: 'Boats Chart',
        description: '3D boat-shaped visualizations representing data with area, height, and color mapping',
        category: 'geometric',
        dimensions: [
            {
                name: 'area',
                label: 'Area',
                dataType: 'any',
                required: true,
                description: 'Field containing any values for boat area size (e.g., parameters, function count)'
            },
            {
                name: 'height',
                label: 'Height',
                dataType: 'any',
                required: true,
                description: 'Field containing any values for boat height (e.g., lines count, complexity)'
            },
            {
                name: 'color',
                label: 'Color',
                dataType: 'any',
                required: true,
                description: 'Field containing any values for color mapping (e.g., complexity, density)'
            }
        ],
        htmlTemplate: `<!-- Boats Chart -->
                <a-entity id="chart"
                    babia-boats="from: tree;
                                 title: {{TITLE}};
                                 legend: true;
                                 palette: {{PALETTE}};
                                 area: {{AREA_FIELD}};
                                 height: {{HEIGHT_FIELD}};
                                 color: {{COLOR_FIELD}};
                                 axis_name: true;
                                 extra: 0.5;
                                 zone_elevation: 0.1"
                    position="0 2 -10"
                    rotation="0 0 0"
                    scale="0.5 0.5 0.5"
                    class="babiaxraycasterclass">
                </a-entity>`
    }
];


/***/ }),
/* 51 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizeDataStateManager = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
/**
 * Visualize Data State Manager
 * Manages state persistence and updates for visualization configuration
 */
class VisualizeDataStateManager {
    static instance;
    state;
    context;
    // Event emitter for state changes
    _onStateChanged = new vscode.EventEmitter();
    onStateChanged = this._onStateChanged.event;
    constructor(context) {
        this.context = context;
        this.state = this.loadState();
        console.log('VISUALIZE-STATE: State manager initialized with state:', {
            hasChart: !!this.state.selectedChart,
            hasJsonPath: !!this.state.selectedJsonPath,
            mappingsCount: this.state.dimensionMappings.length,
            isReadyToLaunch: this.state.isReadyToLaunch
        });
    }
    /**
     * Get singleton instance
     */
    static getInstance(context) {
        if (!VisualizeDataStateManager.instance) {
            if (!context) {
                throw new Error('Context required for first initialization');
            }
            VisualizeDataStateManager.instance = new VisualizeDataStateManager(context);
        }
        return VisualizeDataStateManager.instance;
    }
    /**
     * Check if instance exists (for safe access)
     */
    static hasInstance() {
        return !!VisualizeDataStateManager.instance;
    }
    /**
     * Get initial state
     */
    getInitialState() {
        return {
            selectedChart: undefined,
            selectedJsonPath: undefined,
            selectedJsonName: undefined,
            jsonAnalysis: undefined,
            dimensionMappings: [],
            isDimensionMappingConfigured: false,
            isReadyToLaunch: false
        };
    }
    /**
     * Get current state
     */
    getState() {
        return { ...this.state };
    }
    /**
     * Update selected chart
     */
    updateSelectedChart(chart) {
        console.log('VISUALIZE-STATE: Chart selected:', {
            chartId: chart.id,
            chartName: chart.name,
            requiredDimensions: chart.dimensions.filter(d => d.required).map(d => d.name),
            previousMappings: this.state.dimensionMappings.length
        });
        this.state = {
            ...this.state,
            selectedChart: chart,
            dimensionMappings: [], // Clear dimension mappings when chart changes
            isDimensionMappingConfigured: false, // Reset dimension mapping when chart changes
            isReadyToLaunch: this.calculateReadyToLaunch(chart, this.state.selectedJsonPath, false)
        };
        this.saveState();
        this.notifyStateChange();
        console.log('VISUALIZE-STATE: Chart selection updated, cleared previous mappings');
    }
    /**
     * Update selected JSON file
     */
    updateSelectedJson(filePath, fileName) {
        const isValidPath = filePath && fs.existsSync(filePath);
        console.log('VISUALIZE-STATE: JSON file selection:', {
            fileName: fileName || 'none',
            filePath: filePath || 'none',
            fileExists: isValidPath,
            previousFile: this.state.selectedJsonName || 'none'
        });
        this.state = {
            ...this.state,
            selectedJsonPath: isValidPath ? filePath : undefined,
            selectedJsonName: isValidPath ? fileName : undefined,
            jsonAnalysis: isValidPath ? this.state.jsonAnalysis : undefined,
            dimensionMappings: isValidPath ? this.state.dimensionMappings : [], // Clear mappings if invalid file
            isDimensionMappingConfigured: isValidPath ? this.state.isDimensionMappingConfigured : false,
            isReadyToLaunch: this.calculateReadyToLaunch(this.state.selectedChart, isValidPath ? filePath : undefined, isValidPath ? this.state.isDimensionMappingConfigured : false)
        };
        this.saveState();
        this.notifyStateChange();
        if (!isValidPath && filePath) {
            console.warn('VISUALIZE-STATE: Invalid or non-existent file path provided:', filePath);
        }
    }
    /**
     * Update dimension mapping configuration
     */
    updateDimensionMapping(isConfigured) {
        console.log(`BABIA-TEMPLATES: Dimension mapping configured: ${isConfigured}`);
        this.state = {
            ...this.state,
            isDimensionMappingConfigured: isConfigured,
            isReadyToLaunch: this.calculateReadyToLaunch(this.state.selectedChart, this.state.selectedJsonPath, isConfigured)
        };
        this.notifyStateChange();
    }
    /**
     * Calculate if ready to launch visualization
     */
    calculateReadyToLaunch(chart, jsonPath, dimensionMappingConfigured) {
        const hasChart = !!chart;
        const hasJson = !!jsonPath;
        const hasDimensionMapping = dimensionMappingConfigured ?? this.state.isDimensionMappingConfigured;
        return hasChart && hasJson && hasDimensionMapping;
    }
    /**
     * Reset state
     */
    reset() {
        console.log('BABIA-TEMPLATES: State reset');
        this.state = this.getInitialState();
        this.notifyStateChange();
    }
    /**
     * Notify state change
     */
    notifyStateChange() {
        this._onStateChanged.fire(this.getState());
    }
    /**
     * Check if chart is selected
     */
    hasSelectedChart() {
        return !!this.state.selectedChart;
    }
    /**
     * Check if JSON is selected
     */
    hasSelectedJson() {
        return !!this.state.selectedJsonPath;
    }
    /**
     * Get selected chart name for display
     */
    getSelectedChartName() {
        return this.state.selectedChart?.name;
    }
    /**
     * Get selected JSON name for display
     */
    getSelectedJsonName() {
        return this.state.selectedJsonName;
    }
    /**
     * Update JSON analysis result
     */
    updateJsonAnalysis(jsonAnalysis) {
        console.log('DIMENSION-MAPPING: Updating JSON analysis result');
        this.state.jsonAnalysis = jsonAnalysis;
        // Clear existing dimension mappings when JSON changes
        this.state.dimensionMappings = [];
        this.updateComputedProperties();
        this.saveState();
        this._onStateChanged.fire(this.state);
        console.log('DIMENSION-MAPPING: JSON analysis updated', {
            fieldsCount: jsonAnalysis.fields.length,
            numericFieldsCount: jsonAnalysis.fields.filter(f => f.isNumeric).length
        });
    }
    /**
     * Update dimension mappings
     */
    updateDimensionMappings(mappings) {
        console.log('DIMENSION-MAPPING: Updating dimension mappings');
        this.state.dimensionMappings = mappings;
        this.updateComputedProperties();
        this.saveState();
        this._onStateChanged.fire(this.state);
        console.log('DIMENSION-MAPPING: Dimension mappings updated', {
            count: mappings.length,
            mappings: mappings.map(m => `${m.dimension} -> ${m.dataField}`)
        });
    }
    /**
     * Update single dimension mapping
     */
    updateSingleDimensionMapping(dimensionName, fieldName) {
        console.log(`DIMENSION-MAPPING: Updating mapping for dimension '${dimensionName}' to field '${fieldName}'`);
        // Remove existing mapping for this dimension
        this.state.dimensionMappings = this.state.dimensionMappings.filter(mapping => mapping.dimension !== dimensionName);
        // Add new mapping
        const newMapping = {
            dimension: dimensionName,
            dataField: fieldName
        };
        this.state.dimensionMappings.push(newMapping);
        // Check for duplicate field usage and provide detailed warning
        const duplicateMappings = this.state.dimensionMappings.filter(mapping => mapping.dataField === fieldName);
        if (duplicateMappings.length > 1) {
            const affectedDimensions = duplicateMappings.map(m => m.dimension).join(', ');
            console.log(`DIMENSION-MAPPING: Warning - Field '${fieldName}' is used in multiple dimensions: ${affectedDimensions}`);
            // Log each duplicate mapping for clarity
            duplicateMappings.forEach(mapping => {
                console.log(`DIMENSION-MAPPING: Field '${fieldName}' mapped to dimension '${mapping.dimension}'`);
            });
        }
        this.updateComputedProperties();
        this.saveState();
        this._onStateChanged.fire(this.state);
        console.log('DIMENSION-MAPPING: Single dimension mapping updated', {
            dimension: dimensionName,
            field: fieldName,
            totalMappings: this.state.dimensionMappings.length
        });
    }
    /**
     * Update computed properties based on current state
     */
    updateComputedProperties() {
        // Check if dimension mapping is configured
        this.state.isDimensionMappingConfigured = this.areDimensionsMapped();
        // Check if ready to launch visualization
        this.state.isReadyToLaunch = this.canLaunchVisualization();
    }
    /**
     * Check if dimensions are properly mapped
     */
    areDimensionsMapped() {
        if (!this.state.selectedChart) {
            return false;
        }
        const requiredDimensions = this.state.selectedChart.dimensions.filter(d => d.required);
        return requiredDimensions.every(dimension => this.state.dimensionMappings.some(mapping => mapping.dimension === dimension.name));
    }
    /**
     * Check if visualization can be launched
     */
    canLaunchVisualization() {
        return !!(this.state.selectedChart &&
            this.state.selectedJsonPath &&
            this.state.isDimensionMappingConfigured);
    }
    /**
     * Save state to persistent storage
     */
    saveState() {
        try {
            this.context.workspaceState.update('visualizeDataState', this.state);
            console.log('BABIA-TEMPLATES: State saved to workspace storage');
        }
        catch (error) {
            console.error('BABIA-TEMPLATES: Failed to save state:', error);
        }
    }
    /**
     * Load state from persistent storage
     */
    loadState() {
        try {
            const savedState = this.context.workspaceState.get('visualizeDataState');
            if (savedState) {
                console.log('VISUALIZE-STATE: Loading saved state from workspace storage:', {
                    selectedChart: savedState.selectedChart || 'none',
                    selectedJsonPath: savedState.selectedJsonPath || 'none',
                    hasJsonAnalysis: !!savedState.jsonAnalysis,
                    hasDimensionMappings: !!savedState.dimensionMappings?.length,
                    isDimensionMappingConfigured: savedState.isDimensionMappingConfigured || false
                });
                // Validate file path if it exists
                const isValidJsonPath = savedState.selectedJsonPath && fs.existsSync(savedState.selectedJsonPath);
                if (savedState.selectedJsonPath && !isValidJsonPath) {
                    console.warn('VISUALIZE-STATE: Stored JSON file no longer exists:', savedState.selectedJsonPath);
                }
                // Ensure all required properties exist with validation
                const validatedState = {
                    ...this.getInitialState(),
                    ...savedState,
                    // Reset file-related state if path is invalid
                    selectedJsonPath: isValidJsonPath ? savedState.selectedJsonPath : undefined,
                    selectedJsonName: isValidJsonPath ? savedState.selectedJsonName : undefined,
                    jsonAnalysis: isValidJsonPath ? savedState.jsonAnalysis : undefined,
                    dimensionMappings: isValidJsonPath ? (savedState.dimensionMappings || []) : [],
                    isDimensionMappingConfigured: isValidJsonPath ? (savedState.isDimensionMappingConfigured || false) : false
                };
                // Recalculate ready state based on validated data
                validatedState.isReadyToLaunch = this.calculateReadyToLaunch(validatedState.selectedChart, validatedState.selectedJsonPath, validatedState.isDimensionMappingConfigured);
                console.log('VISUALIZE-STATE: State loaded and validated:', {
                    finalChart: validatedState.selectedChart,
                    finalJsonPath: validatedState.selectedJsonPath,
                    finalJsonExists: validatedState.selectedJsonPath ? fs.existsSync(validatedState.selectedJsonPath) : false,
                    finalMappingsCount: validatedState.dimensionMappings.length,
                    finalIsConfigured: validatedState.isDimensionMappingConfigured,
                    finalIsReady: validatedState.isReadyToLaunch
                });
                return validatedState;
            }
        }
        catch (error) {
            console.error('VISUALIZE-STATE: Failed to load state:', error);
        }
        console.log('VISUALIZE-STATE: No stored state found, using initial state');
        return this.getInitialState();
    }
    /**
     * Cleanup resources
     */
    dispose() {
        this._onStateChanged.dispose();
    }
}
exports.VisualizeDataStateManager = VisualizeDataStateManager;


/***/ }),
/* 52 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.JsonFieldAnalyzer = void 0;
const fs = __importStar(__webpack_require__(6));
/**
 * JSON Field Analyzer
 * Analyzes JSON files to extract available fields and their types
 */
class JsonFieldAnalyzer {
    /**
     * Analyze a JSON file and extract field information
     */
    static async analyzeJsonFile(filePath) {
        console.log(`DIMENSION-MAPPING: Analyzing JSON file: ${filePath}`);
        try {
            // Read and parse JSON file
            const fileContent = fs.readFileSync(filePath, 'utf8');
            const jsonData = JSON.parse(fileContent);
            console.log(`DIMENSION-MAPPING: JSON parsed successfully`);
            // Analyze the data structure
            const analysisResult = this.analyzeDataStructure(jsonData, filePath);
            console.log(`DIMENSION-MAPPING: Found ${analysisResult.fields.length} fields in ${analysisResult.recordCount} records`);
            analysisResult.fields.forEach(field => {
                console.log(`DIMENSION-MAPPING: Field '${field.name}' - Type: ${field.type}, Numeric: ${field.isNumeric}, Values: ${field.valueCount}`);
            });
            return analysisResult;
        }
        catch (error) {
            console.error(`DIMENSION-MAPPING: Error analyzing JSON file:`, error);
            return {
                success: false,
                fields: [],
                error: `Failed to analyze JSON file: ${error instanceof Error ? error.message : 'Unknown error'}`,
                recordCount: 0,
                filePath
            };
        }
    }
    /**
     * Analyze data structure and extract field information
     */
    static analyzeDataStructure(data, filePath) {
        const fields = new Map();
        let recordCount = 0;
        // Handle different data structures
        if (Array.isArray(data)) {
            // Array of objects
            recordCount = data.length;
            data.forEach((record, index) => {
                if (typeof record === 'object' && record !== null) {
                    this.analyzeRecord(record, fields, index < 10); // Only collect samples from first 10 records
                }
            });
        }
        else if (typeof data === 'object' && data !== null) {
            // Single object
            recordCount = 1;
            this.analyzeRecord(data, fields, true);
        }
        else {
            throw new Error('JSON data must be an object or array of objects');
        }
        return {
            success: true,
            fields: Array.from(fields.values()),
            recordCount,
            filePath
        };
    }
    /**
     * Analyze a single record and update field information
     */
    static analyzeRecord(record, fields, collectSamples) {
        for (const [fieldName, value] of Object.entries(record)) {
            let fieldInfo = fields.get(fieldName);
            if (!fieldInfo) {
                fieldInfo = {
                    name: fieldName,
                    type: 'unknown',
                    isNumeric: false,
                    sampleValues: [],
                    valueCount: 0
                };
                fields.set(fieldName, fieldInfo);
            }
            // Skip null/undefined values
            if (value === null || value === undefined) {
                return;
            }
            fieldInfo.valueCount++;
            // Determine field type
            const valueType = this.getValueType(value);
            if (fieldInfo.type === 'unknown') {
                fieldInfo.type = valueType;
            }
            else if (fieldInfo.type !== valueType) {
                // Mixed types - mark as string by default
                fieldInfo.type = 'string';
            }
            // Check if numeric
            if (this.isNumericValue(value)) {
                fieldInfo.isNumeric = true;
            }
            // Collect sample values
            if (collectSamples && fieldInfo.sampleValues.length < 5) {
                fieldInfo.sampleValues.push(value);
            }
        }
    }
    /**
     * Get the type of a value
     */
    static getValueType(value) {
        if (typeof value === 'string') {
            return 'string';
        }
        if (typeof value === 'number') {
            return 'number';
        }
        if (typeof value === 'boolean') {
            return 'boolean';
        }
        if (Array.isArray(value)) {
            return 'array';
        }
        if (typeof value === 'object') {
            return 'object';
        }
        if (value === null) {
            return 'null';
        }
        return 'unknown';
    }
    /**
     * Check if a value is numeric
     */
    static isNumericValue(value) {
        if (typeof value === 'number') {
            return !isNaN(value) && isFinite(value);
        }
        if (typeof value === 'string') {
            const num = parseFloat(value);
            return !isNaN(num) && isFinite(num) && value.trim() !== '';
        }
        return false;
    }
    /**
     * Get fields suitable for a specific dimension type
     */
    static getFieldsForDimensionType(analysisResult, dimensionDataType) {
        if (!analysisResult.success) {
            return [];
        }
        if (dimensionDataType === 'numeric') {
            return analysisResult.fields.filter(field => field.isNumeric);
        }
        // For 'any' type, return all fields
        return analysisResult.fields;
    }
    /**
     * Format field for display in QuickPick
     */
    static formatFieldForDisplay(field) {
        const typeInfo = field.isNumeric ? `${field.type} (numeric)` : field.type;
        const sampleText = field.sampleValues.length > 0
            ? `Samples: ${field.sampleValues.slice(0, 3).map(v => JSON.stringify(v)).join(', ')}`
            : '';
        return {
            label: field.name,
            description: typeInfo,
            detail: `${field.valueCount} values${sampleText ? ' • ' + sampleText : ''}`
        };
    }
}
exports.JsonFieldAnalyzer = JsonFieldAnalyzer;


/***/ }),
/* 53 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.TemplateProcessor = void 0;
const getVisualizationConfiguration_1 = __webpack_require__(54);
const createChart_1 = __webpack_require__(63);
const createStructure_1 = __webpack_require__(64);
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
/**
 * BabiaXR Template Processor - Modular Architecture
 * Streamlined processor using CreateChart and CreateStructure modules
 * Combines chart entities with structural elements for complete XR visualization
 */
class TemplateProcessor {
    /**
     * Main method to generate complete XR visualization index.html
     * Uses both CreateChart and CreateStructure modules for modular processing
     */
    static async generateXRVisualization(chartId, mappings, title, dataSource, context, outputPath, analysisData // Add optional analysis data to detect type
    ) {
        try {
            console.log('TEMPLATE_PROCESSOR: Starting modular XR visualization generation');
            console.log('TEMPLATE_PROCESSOR: Chart ID:', chartId);
            console.log('TEMPLATE_PROCESSOR: Mappings:', mappings);
            console.log('TEMPLATE_PROCESSOR: Title:', title);
            console.log('TEMPLATE_PROCESSOR: Data source:', dataSource);
            // Get visualization settings
            const visualizationSettings = await this.getVisualizationSettings();
            console.log('TEMPLATE_PROCESSOR: Using visualization settings:', visualizationSettings);
            // Detect if this is directory analysis by checking the data structure
            const isDirectoryAnalysis = this.detectDirectoryAnalysis(analysisData);
            console.log('TEMPLATE_PROCESSOR: Detected directory analysis:', isDirectoryAnalysis);
            // Create chart entity using CreateChart module
            const chartResult = createChart_1.CreateChart.createChartEntity(chartId, mappings, title, visualizationSettings.palette);
            if (!chartResult.success) {
                return {
                    success: false,
                    error: `Chart creation failed: ${chartResult.error}`
                };
            }
            // Create structural placeholders using CreateStructure module
            const structureResult = createStructure_1.CreateStructure.createStructuralPlaceholders(title, dataSource, visualizationSettings, 'xr', // Analysis type for XR visualization
            context, chartId, // Pass chart type for tree builder decision
            isDirectoryAnalysis // Pass directory analysis flag
            );
            if (!structureResult.success) {
                return {
                    success: false,
                    error: `Structure creation failed: ${structureResult.error}`
                };
            }
            // Load XR base template
            const xrTemplate = await this.loadXRTemplate(context);
            if (!xrTemplate) {
                return { success: false, error: 'Failed to load XR template' };
            }
            // First, replace chart component placeholder
            let templateWithChart = xrTemplate.replace(/\$\{CHART_COMPONENT\}|\{\{\s*CHART_COMPONENT\s*\}\}/g, chartResult.chartHtml || '');
            // Then replace all structural placeholders
            let finalHtml = createStructure_1.CreateStructure.replaceStructuralPlaceholders(templateWithChart, structureResult.placeholders);
            // Special replacement for the SSE script tag in case it wasn't replaced
            const nonce = structureResult.placeholders.get('nonce') || structureResult.placeholders.get('NONCE');
            if (nonce) {
                finalHtml = finalHtml.replace(/<script nonce="\$\{nonce\}" src="\$\{scriptUri\}"><\/script>/g, `<script nonce="${nonce}" src="./main.js"></script>`);
            }
            // Write the final HTML file
            fs.writeFileSync(outputPath, finalHtml, 'utf8');
            console.log('TEMPLATE_PROCESSOR: Generated modular XR visualization HTML at:', outputPath);
            return { success: true };
        }
        catch (error) {
            console.error('TEMPLATE_PROCESSOR: Error generating XR visualization:', error);
            return {
                success: false,
                error: error instanceof Error ? error.message : String(error)
            };
        }
    }
    /**
     * Get visualization settings (palette, environment, colors)
     */
    static async getVisualizationSettings() {
        return await (0, getVisualizationConfiguration_1.getVisualizationConfiguration)();
    }
    /**
     * Load XR base template from templates/xr/file/xr-visualization.html
     */
    static async loadXRTemplate(context) {
        try {
            const templatePath = path.join(context.extensionPath, 'templates', 'xr', 'file', 'xr-visualization.html');
            if (!fs.existsSync(templatePath)) {
                console.error('TEMPLATE_PROCESSOR: XR template not found at:', templatePath);
                return null;
            }
            const template = fs.readFileSync(templatePath, 'utf8');
            console.log('TEMPLATE_PROCESSOR: Loaded XR template from:', templatePath);
            return template;
        }
        catch (error) {
            console.error('TEMPLATE_PROCESSOR: Error loading XR template:', error);
            return null;
        }
    }
    /**
     * Get available charts (delegated to CreateChart)
     */
    static getAvailableCharts() {
        return createChart_1.CreateChart.getAvailableCharts();
    }
    /**
     * Create default XR analysis mappings (delegated to CreateChart)
     */
    static createDefaultXRMappings() {
        return createChart_1.CreateChart.createDefaultXRMappings();
    }
    /**
     * Create structural placeholders for any analysis type (delegated to CreateStructure)
     */
    static async createStructuralPlaceholders(title, dataSource, analysisType, context, chartType, isDirectoryAnalysis) {
        const visualizationSettings = await this.getVisualizationSettings();
        return createStructure_1.CreateStructure.createStructuralPlaceholders(title, dataSource, visualizationSettings, analysisType, context, chartType, isDirectoryAnalysis);
    }
    /**
     * Detect if this is a directory analysis by examining the data structure
     * Directory analysis has 'fileName' field, file analysis has 'functionName' field
     */
    static detectDirectoryAnalysis(analysisData) {
        if (!analysisData || !Array.isArray(analysisData) || analysisData.length === 0) {
            return false;
        }
        // Check the first item in the data to determine the type
        const firstItem = analysisData[0];
        if (firstItem && typeof firstItem === 'object') {
            // Directory analysis has 'fileName' field, file analysis has 'functionName' field
            const hasFileName = 'fileName' in firstItem;
            const hasFunctionName = 'functionName' in firstItem;
            console.log('TEMPLATE_PROCESSOR: Data structure detection:', {
                hasFileName,
                hasFunctionName,
                keys: Object.keys(firstItem)
            });
            return hasFileName && !hasFunctionName;
        }
        return false;
    }
}
exports.TemplateProcessor = TemplateProcessor;


/***/ }),
/* 54 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Visualization Configuration Utility
 * Common functions for retrieving visualization settings across templates
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.getVisualizationConfiguration = getVisualizationConfiguration;
exports.getIndividualSetting = getIndividualSetting;
/**
 * Get visualization settings (palette, environment, colors)
 * Centralized function used by all template processors
 */
async function getVisualizationConfiguration() {
    try {
        console.log('VISUALIZATION_CONFIG: Retrieving visualization settings...');
        // Import visualization settings functions directly (no dynamic import needed)
        const { getSelectedPalette, getSelectedEnvironment, getSelectedBackgroundColor, getSelectedGroundColor } = __webpack_require__(55);
        const settings = {
            palette: await getSelectedPalette(),
            environment: await getSelectedEnvironment(),
            backgroundColor: await getSelectedBackgroundColor(),
            groundColor: await getSelectedGroundColor()
        };
        console.log('VISUALIZATION_CONFIG: Settings retrieved:', settings);
        return settings;
    }
    catch (error) {
        console.error('VISUALIZATION_CONFIG: Error retrieving settings:', error);
        // Return default settings if there's an error
        const defaultSettings = {
            palette: 'blues',
            environment: 'forest',
            backgroundColor: '#001122',
            groundColor: '#553311'
        };
        console.log('VISUALIZATION_CONFIG: Using default settings:', defaultSettings);
        return defaultSettings;
    }
}
/**
 * Get individual setting with fallback
 */
async function getIndividualSetting(settingType) {
    try {
        const settings = await getVisualizationConfiguration();
        return settings[settingType];
    }
    catch (error) {
        console.error(`VISUALIZATION_CONFIG: Error getting ${settingType}:`, error);
        // Default fallbacks
        const defaults = {
            palette: 'blues',
            environment: 'forest',
            backgroundColor: '#001122',
            groundColor: '#553311'
        };
        return defaults[settingType];
    }
}


/***/ }),
/* 55 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Visualization Settings Module
 * Main entry point for visualization configuration management
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.getAllSelectedSettings = exports.getSelectedPalette = exports.getSelectedEnvironment = exports.getSelectedGroundColor = exports.getSelectedBackgroundColor = exports.initializeSettingsAccessors = exports.VisualizationSettingsInteractionHandler = exports.VisualizationSettingsTreeItem = exports.VisualizationSettingsItemFactory = exports.VisualizationSettingsStorage = exports.DEFAULT_VISUALIZATION_SETTINGS = void 0;
var settingsModel_1 = __webpack_require__(56);
Object.defineProperty(exports, "DEFAULT_VISUALIZATION_SETTINGS", ({ enumerable: true, get: function () { return settingsModel_1.DEFAULT_VISUALIZATION_SETTINGS; } }));
var settingsStorage_1 = __webpack_require__(57);
Object.defineProperty(exports, "VisualizationSettingsStorage", ({ enumerable: true, get: function () { return settingsStorage_1.VisualizationSettingsStorage; } }));
var visualizationSettingsItems_1 = __webpack_require__(58);
Object.defineProperty(exports, "VisualizationSettingsItemFactory", ({ enumerable: true, get: function () { return visualizationSettingsItems_1.VisualizationSettingsItemFactory; } }));
Object.defineProperty(exports, "VisualizationSettingsTreeItem", ({ enumerable: true, get: function () { return visualizationSettingsItems_1.VisualizationSettingsTreeItem; } }));
var handleSettingsInteraction_1 = __webpack_require__(60);
Object.defineProperty(exports, "VisualizationSettingsInteractionHandler", ({ enumerable: true, get: function () { return handleSettingsInteraction_1.VisualizationSettingsInteractionHandler; } }));
// Export settings accessors for babia-templates integration
var settingsAccessors_1 = __webpack_require__(62);
Object.defineProperty(exports, "initializeSettingsAccessors", ({ enumerable: true, get: function () { return settingsAccessors_1.initializeSettingsAccessors; } }));
Object.defineProperty(exports, "getSelectedBackgroundColor", ({ enumerable: true, get: function () { return settingsAccessors_1.getSelectedBackgroundColor; } }));
Object.defineProperty(exports, "getSelectedGroundColor", ({ enumerable: true, get: function () { return settingsAccessors_1.getSelectedGroundColor; } }));
Object.defineProperty(exports, "getSelectedEnvironment", ({ enumerable: true, get: function () { return settingsAccessors_1.getSelectedEnvironment; } }));
Object.defineProperty(exports, "getSelectedPalette", ({ enumerable: true, get: function () { return settingsAccessors_1.getSelectedPalette; } }));
Object.defineProperty(exports, "getAllSelectedSettings", ({ enumerable: true, get: function () { return settingsAccessors_1.getAllSelectedSettings; } }));


/***/ }),
/* 56 */
/***/ ((__unused_webpack_module, exports) => {


/**
 * Visualization Settings Model
 * Defines the structure and interfaces for visualization configuration
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SETTING_FIELDS = exports.HEX_COLOR_REGEX = exports.CHART_PALETTES = exports.ENVIRONMENT_PRESETS = exports.PREDEFINED_COLORS = exports.DEFAULT_VISUALIZATION_SETTINGS = void 0;
exports.isValidHexColor = isValidHexColor;
/**
 * Default visualization settings
 */
exports.DEFAULT_VISUALIZATION_SETTINGS = {
    backgroundColor: '#FFFFFF',
    groundColor: '#000000',
    environmentPreset: 'default',
    chartPalette: 'ubuntu'
};
/**
 * Predefined color options for quick selection
 */
exports.PREDEFINED_COLORS = [
    { label: '#FFFFFF (white)', value: '#FFFFFF' },
    { label: '#000000 (black)', value: '#000000' },
    { label: '#B10DC9 (pink)', value: '#B10DC9' }
];
/**
 * Available environment preset options with descriptions
 */
exports.ENVIRONMENT_PRESETS = [
    { label: 'none', value: 'none', description: 'No environment, just a sky' },
    { label: 'default', value: 'default', description: 'Default environment with hills and sky' },
    { label: 'forest', value: 'forest', description: 'A forest with trees and directional light' },
    { label: 'egypt', value: 'egypt', description: 'Egyptian landscape with sand and pyramids' },
    { label: 'dream', value: 'dream', description: 'Surreal dreamlike environment' },
    { label: 'volcano', value: 'volcano', description: 'Volcanic terrain with lava and smoke' },
    { label: 'arches', value: 'arches', description: 'Desert with rock arches' },
    { label: 'tron', value: 'tron', description: 'Futuristic Tron-like environment' },
    { label: 'japan', value: 'japan', description: 'Stylized Japanese landscape' },
    { label: 'threetowers', value: 'threetowers', description: 'Fantasy environment with three towers' },
    { label: 'poison', value: 'poison', description: 'Toxic environment with green fog' },
    { label: 'contact', value: 'contact', description: 'Sci-fi environment with landing pad' }
];
/**
 * Available chart palette options with descriptions
 */
exports.CHART_PALETTES = [
    { label: 'ubuntu', value: 'ubuntu', description: 'Ubuntu style colors (default)' },
    { label: 'blues', value: 'blues', description: 'Variations of blue colors' },
    { label: 'bussiness', value: 'bussiness', description: 'Professional business colors' },
    { label: 'commerce', value: 'commerce', description: 'E-commerce friendly palette' },
    { label: 'flat', value: 'flat', description: 'Flat design color scheme' },
    { label: 'foxy', value: 'foxy', description: 'FireFox palette with oranges and blues' },
    { label: 'icecream', value: 'icecream', description: 'Sweet pastel colors' },
    { label: 'pearl', value: 'pearl', description: 'Pearlescent subtle colors' },
    { label: 'sunset', value: 'sunset', description: 'Warm sunset color gradients' }
];
/**
 * Validation for hex color format
 */
exports.HEX_COLOR_REGEX = /^#[0-9a-fA-F]{6}$/;
/**
 * Validate hex color format
 */
function isValidHexColor(color) {
    return exports.HEX_COLOR_REGEX.test(color);
}
/**
 * Configuration for all setting fields
 */
exports.SETTING_FIELDS = [
    {
        key: 'backgroundColor',
        label: 'Background Color',
        type: 'color',
        description: 'Set the background color for the visualization scene',
        icon: 'color-mode'
    },
    {
        key: 'groundColor',
        label: 'Ground Color',
        type: 'color',
        description: 'Set the ground color for the visualization scene',
        icon: 'symbol-color'
    },
    {
        key: 'environmentPreset',
        label: 'Environment Preset',
        type: 'preset',
        description: 'Choose an environment preset for the scene',
        icon: 'globe'
    },
    {
        key: 'chartPalette',
        label: 'Chart Palette',
        type: 'palette',
        description: 'Select color palette for chart visualization',
        icon: 'symbol-misc'
    }
];


/***/ }),
/* 57 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizationSettingsStorage = void 0;
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const settingsModel_1 = __webpack_require__(56);
/**
 * Visualization Settings Storage
 * Manages persistent storage and retrieval of visualization configuration using file system
 */
class VisualizationSettingsStorage {
    static VISUALIZATION_CONFIG_DIR = 'visualization-configuration';
    static SETTINGS_FILE = 'visualization-settings.json';
    static LEGACY_STORAGE_KEY = 'visualizationSettings'; // For migration
    context;
    constructor(context) {
        this.context = context;
        console.log('VISUALIZATION-SETTINGS: Storage manager initialized with file-based storage');
    }
    /**
     * Get the visualization configuration directory path
     */
    getConfigDirectory() {
        const globalStorageUri = this.context.globalStorageUri;
        return path.join(globalStorageUri.fsPath, VisualizationSettingsStorage.VISUALIZATION_CONFIG_DIR);
    }
    /**
     * Get the settings file path
     */
    getSettingsFilePath() {
        return path.join(this.getConfigDirectory(), VisualizationSettingsStorage.SETTINGS_FILE);
    }
    /**
     * Ensure the configuration directory exists
     */
    ensureConfigDirectory() {
        const configDir = this.getConfigDirectory();
        try {
            if (!fs.existsSync(configDir)) {
                console.log(`VISUALIZATION-SETTINGS: Creating configuration directory: ${configDir}`);
                fs.mkdirSync(configDir, { recursive: true });
            }
        }
        catch (error) {
            console.error('VISUALIZATION-SETTINGS: Error creating configuration directory:', error);
            throw new Error(`Failed to create configuration directory: ${error}`);
        }
    }
    /**
     * Migrate legacy settings from globalState to file system
     */
    migrateLegacySettings() {
        try {
            const legacySettings = this.context.globalState.get(VisualizationSettingsStorage.LEGACY_STORAGE_KEY);
            if (legacySettings && !fs.existsSync(this.getSettingsFilePath())) {
                console.log('VISUALIZATION-SETTINGS: Migrating legacy settings to file system');
                const settingsFilePath = this.getSettingsFilePath();
                const jsonSettings = {
                    backgroundColor: legacySettings.backgroundColor,
                    groundColor: legacySettings.groundColor,
                    environment: legacySettings.environmentPreset,
                    palette: legacySettings.chartPalette
                };
                fs.writeFileSync(settingsFilePath, JSON.stringify(jsonSettings, null, 2), 'utf8');
                console.log('VISUALIZATION-SETTINGS: Legacy settings migration completed with all settings');
            }
        }
        catch (error) {
            console.error('VISUALIZATION-SETTINGS: Error during legacy migration:', error);
            // Don't throw - migration failure shouldn't prevent normal operation
        }
    }
    /**
     * Get current visualization settings from file system
     */
    getSettings() {
        try {
            // Ensure directory exists and migrate legacy settings if needed
            this.ensureConfigDirectory();
            this.migrateLegacySettings();
            const settingsFilePath = this.getSettingsFilePath();
            if (fs.existsSync(settingsFilePath)) {
                const fileContent = fs.readFileSync(settingsFilePath, 'utf8');
                const jsonSettings = JSON.parse(fileContent);
                // Build complete settings from JSON with fallbacks to defaults
                const settings = {
                    backgroundColor: jsonSettings.backgroundColor || settingsModel_1.DEFAULT_VISUALIZATION_SETTINGS.backgroundColor,
                    groundColor: jsonSettings.groundColor || settingsModel_1.DEFAULT_VISUALIZATION_SETTINGS.groundColor,
                    environmentPreset: jsonSettings.environment,
                    chartPalette: jsonSettings.palette
                };
                console.log('VISUALIZATION-SETTINGS: Loaded settings from file', settings);
                return settings;
            }
        }
        catch (error) {
            console.error('VISUALIZATION-SETTINGS: Failed to load settings from file:', error);
        }
        console.log('VISUALIZATION-SETTINGS: Using default settings');
        return { ...settingsModel_1.DEFAULT_VISUALIZATION_SETTINGS };
    }
    /**
     * Save visualization settings to file system
     */
    async saveSettings(settings) {
        try {
            this.ensureConfigDirectory();
            const settingsFilePath = this.getSettingsFilePath();
            // Create settings object for JSON file (all four settings)
            const jsonSettings = {
                backgroundColor: settings.backgroundColor,
                groundColor: settings.groundColor,
                environment: settings.environmentPreset,
                palette: settings.chartPalette
            };
            // Save to JSON file
            fs.writeFileSync(settingsFilePath, JSON.stringify(jsonSettings, null, 2), 'utf8');
            // Also save complete settings to globalState for backward compatibility
            await this.context.globalState.update(VisualizationSettingsStorage.LEGACY_STORAGE_KEY, settings);
            console.log('VISUALIZATION-SETTINGS: Settings saved to file and globalState', settings);
        }
        catch (error) {
            console.error('VISUALIZATION-SETTINGS: Failed to save settings:', error);
            throw new Error(`Failed to save visualization settings: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Update a single setting field
     */
    async updateSetting(key, value) {
        const currentSettings = this.getSettings();
        const updatedSettings = {
            ...currentSettings,
            [key]: value
        };
        await this.saveSettings(updatedSettings);
        console.log(`VISUALIZATION-SETTINGS: Updated ${key} to '${value}'`);
    }
    /**
     * Reset settings to defaults
     */
    async resetSettings() {
        await this.saveSettings({ ...settingsModel_1.DEFAULT_VISUALIZATION_SETTINGS });
        console.log('VISUALIZATION-SETTINGS: Settings reset to defaults');
    }
    /**
     * Check if settings exist in storage
     */
    hasStoredSettings() {
        const settingsFilePath = this.getSettingsFilePath();
        return fs.existsSync(settingsFilePath);
    }
    /**
     * Get formatted settings for display
     */
    getFormattedSettings() {
        const settings = this.getSettings();
        return {
            backgroundColor: settings.backgroundColor,
            groundColor: settings.groundColor,
            environmentPreset: settings.environmentPreset,
            chartPalette: settings.chartPalette
        };
    }
    /**
     * Validate settings structure
     */
    validateSettings(settings) {
        return (typeof settings === 'object' &&
            typeof settings.backgroundColor === 'string' &&
            typeof settings.groundColor === 'string' &&
            typeof settings.environmentPreset === 'string' &&
            typeof settings.chartPalette === 'string');
    }
}
exports.VisualizationSettingsStorage = VisualizationSettingsStorage;


/***/ }),
/* 58 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizationSettingsIcons = exports.VisualizationSettingsItemFactory = exports.VisualizationSettingsTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
const settingsModel_1 = __webpack_require__(56);
const dynamicColorIconGenerator_1 = __webpack_require__(59);
/**
 * Tree item for visualization settings
 */
class VisualizationSettingsTreeItem extends vscode.TreeItem {
    label;
    collapsibleState;
    type;
    settingField;
    currentValue;
    command;
    iconPath;
    tooltip;
    description;
    contextValue;
    constructor(label, collapsibleState, type, settingField, currentValue, command, iconPath, tooltip, description, contextValue) {
        super(label, collapsibleState);
        this.label = label;
        this.collapsibleState = collapsibleState;
        this.type = type;
        this.settingField = settingField;
        this.currentValue = currentValue;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip;
        this.description = description;
        this.contextValue = contextValue;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.VisualizationSettingsTreeItem = VisualizationSettingsTreeItem;
/**
 * Factory for creating visualization settings items
 */
class VisualizationSettingsItemFactory {
    /**
     * Create all visualization settings items with dynamic color icons
     */
    static async createVisualizationSettingsItems(settings, context) {
        console.log('VISUALIZATION-SETTINGS: Creating settings items with dynamic color icons...');
        const items = await Promise.all(settingsModel_1.SETTING_FIELDS.map(field => this.createSettingItem(field, settings, context)));
        console.log(`VISUALIZATION-SETTINGS: Created ${items.length} setting items`);
        return items;
    }
    /**
     * Create individual setting tree item with dynamic color icon support
     */
    static async createSettingItem(field, settings, context) {
        const currentValue = settings[field.key];
        // Format the display value (remove Unicode block for color fields since we have icons now)
        let displayValue = String(currentValue);
        let iconPath;
        if (field.type === 'color') {
            console.log(`COLOR-PICKER: Processing color field ${field.key} with value ${currentValue}`);
            try {
                // Generate dynamic color icon
                const normalizedColor = dynamicColorIconGenerator_1.DynamicColorIconGenerator.normalizeHexColor(currentValue);
                const colorIconUri = await dynamicColorIconGenerator_1.DynamicColorIconGenerator.getOrCreateColorIcon(context, field.key, normalizedColor);
                // Clean up old icons for this setting
                dynamicColorIconGenerator_1.DynamicColorIconGenerator.cleanupOldColorIcons(context, field.key, normalizedColor);
                iconPath = colorIconUri;
                console.log(`COLOR-PICKER: Successfully created color icon for ${field.key}`);
            }
            catch (error) {
                console.error(`COLOR-PICKER: Error creating color icon for ${field.key}:`, error);
                // Fallback to theme icon
                iconPath = new vscode.ThemeIcon(field.icon);
            }
        }
        else {
            // Use regular theme icon for non-color fields
            iconPath = new vscode.ThemeIcon(field.icon);
        }
        const item = new VisualizationSettingsTreeItem(field.label, vscode.TreeItemCollapsibleState.None, 'settings-field', field, displayValue, {
            command: 'codeXR.visualizationSettings.configure',
            title: `Configure ${field.label}`,
            arguments: [field.key]
        }, iconPath, `${field.description}\nCurrent value: ${currentValue}`, displayValue, 'visualization-settings-field');
        return item;
    }
    /**
     * Create setting item with updated value
     */
    static async createUpdatedSettingItem(field, newValue, context) {
        const dummySettings = { [field.key]: newValue };
        return await this.createSettingItem(field, dummySettings, context);
    }
}
exports.VisualizationSettingsItemFactory = VisualizationSettingsItemFactory;
/**
 * Icons for visualization settings items
 */
class VisualizationSettingsIcons {
    static backgroundColor = new vscode.ThemeIcon('color-mode');
    static groundColor = new vscode.ThemeIcon('symbol-color');
    static environmentPreset = new vscode.ThemeIcon('globe');
    static chartPalette = new vscode.ThemeIcon('symbol-misc');
    static section = new vscode.ThemeIcon('settings-gear');
}
exports.VisualizationSettingsIcons = VisualizationSettingsIcons;


/***/ }),
/* 59 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DynamicColorIconGenerator = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
/**
 * Dynamic SVG Color Icon Generator
 * Generates and manages SVG icons for color visualization in tree items
 */
class DynamicColorIconGenerator {
    static VISUALIZATION_CONFIG_DIR = 'visualization-configuration';
    static ICON_SIZE = 16; // 16x16 pixels for VS Code tree items
    /**
     * Generate a square SVG icon for a given hex color
     */
    static generateColorSVG(hexColor) {
        // Ensure the color is properly formatted
        const cleanColor = hexColor.startsWith('#') ? hexColor : `#${hexColor}`;
        console.log(`COLOR-PICKER: Generating SVG for color ${cleanColor}`);
        return `<?xml version="1.0" encoding="UTF-8"?>
<svg width="${this.ICON_SIZE}" height="${this.ICON_SIZE}" viewBox="0 0 ${this.ICON_SIZE} ${this.ICON_SIZE}" xmlns="http://www.w3.org/2000/svg">
    <!-- Background square with subtle border -->
    <rect x="1" y="1" width="14" height="14" 
          fill="${cleanColor}" 
          stroke="rgba(255,255,255,0.3)" 
          stroke-width="0.5" 
          rx="2" ry="2"/>
    <!-- Subtle inner shadow effect -->
    <rect x="1.5" y="1.5" width="13" height="13" 
          fill="none" 
          stroke="rgba(0,0,0,0.15)" 
          stroke-width="0.5" 
          rx="1.5" ry="1.5"/>
</svg>`;
    }
    /**
     * Get the global storage directory for color icons
     */
    static getColorIconsDirectory(context) {
        const globalStorageUri = context.globalStorageUri;
        const colorIconsPath = path.join(globalStorageUri.fsPath, this.VISUALIZATION_CONFIG_DIR);
        console.log(`COLOR-PICKER: Visualization configuration directory: ${colorIconsPath}`);
        return colorIconsPath;
    }
    /**
     * Ensure the color icons directory exists
     */
    static ensureColorIconsDirectory(context) {
        const configPath = this.getColorIconsDirectory(context);
        try {
            if (!fs.existsSync(configPath)) {
                console.log(`COLOR-PICKER: Creating visualization configuration directory: ${configPath}`);
                fs.mkdirSync(configPath, { recursive: true });
            }
        }
        catch (error) {
            console.error(`COLOR-PICKER: Error creating visualization configuration directory:`, error);
            throw new Error(`Failed to create visualization configuration directory: ${error}`);
        }
    }
    /**
     * Generate filename for a color icon
     */
    static getColorIconFilename(settingKey, hexColor) {
        // Remove # from hex color and make it safe for filename
        const cleanColor = hexColor.replace('#', '').toLowerCase();
        return `${settingKey}_${cleanColor}.svg`;
    }
    /**
     * Generate and save a color icon, return the file URI
     */
    static async generateColorIcon(context, settingKey, hexColor) {
        console.log(`COLOR-PICKER: Generating color icon for ${settingKey} with color ${hexColor}`);
        try {
            // Ensure directory exists
            this.ensureColorIconsDirectory(context);
            // Generate SVG content
            const svgContent = this.generateColorSVG(hexColor);
            // Create filename and full path
            const filename = this.getColorIconFilename(settingKey, hexColor);
            const configPath = this.getColorIconsDirectory(context);
            const iconPath = path.join(configPath, filename);
            // Write SVG file
            fs.writeFileSync(iconPath, svgContent, 'utf8');
            console.log(`COLOR-PICKER: Color icon saved to: ${iconPath}`);
            // Return VS Code URI
            const iconUri = vscode.Uri.file(iconPath);
            console.log(`COLOR-PICKER: Color icon URI: ${iconUri.toString()}`);
            return iconUri;
        }
        catch (error) {
            console.error(`COLOR-PICKER: Error generating color icon for ${settingKey}:`, error);
            throw new Error(`Failed to generate color icon: ${error}`);
        }
    }
    /**
     * Get existing color icon URI if it exists, otherwise generate new one
     */
    static async getOrCreateColorIcon(context, settingKey, hexColor) {
        console.log(`COLOR-PICKER: Getting or creating color icon for ${settingKey} with color ${hexColor}`);
        try {
            const configPath = this.getColorIconsDirectory(context);
            const filename = this.getColorIconFilename(settingKey, hexColor);
            const iconPath = path.join(configPath, filename);
            if (fs.existsSync(iconPath)) {
                console.log(`COLOR-PICKER: Using existing color icon: ${iconPath}`);
                return vscode.Uri.file(iconPath);
            }
            else {
                console.log(`COLOR-PICKER: Creating new color icon for ${settingKey}`);
                return await this.generateColorIcon(context, settingKey, hexColor);
            }
        }
        catch (error) {
            console.error(`COLOR-PICKER: Error getting/creating color icon:`, error);
            throw error;
        }
    }
    /**
     * Clean up old color icons for a specific setting
     */
    static cleanupOldColorIcons(context, settingKey, currentHexColor) {
        console.log(`COLOR-PICKER: Cleaning up old color icons for ${settingKey}`);
        try {
            const configPath = this.getColorIconsDirectory(context);
            if (!fs.existsSync(configPath)) {
                return;
            }
            // Read directory and find old icons for this setting
            const files = fs.readdirSync(configPath);
            const currentFilename = this.getColorIconFilename(settingKey, currentHexColor);
            let cleanedCount = 0;
            files.forEach(file => {
                if (file.startsWith(`${settingKey}_`) && file.endsWith('.svg') && file !== currentFilename) {
                    const oldIconPath = path.join(configPath, file);
                    try {
                        fs.unlinkSync(oldIconPath);
                        cleanedCount++;
                        console.log(`COLOR-PICKER: Removed old color icon: ${file}`);
                    }
                    catch (error) {
                        console.warn(`COLOR-PICKER: Failed to remove old icon ${file}:`, error);
                    }
                }
            });
            if (cleanedCount > 0) {
                console.log(`COLOR-PICKER: Cleaned up ${cleanedCount} old color icons for ${settingKey}`);
            }
        }
        catch (error) {
            console.error(`COLOR-PICKER: Error during cleanup for ${settingKey}:`, error);
            // Don't throw - cleanup is non-critical
        }
    }
    /**
     * Validate hex color format
     */
    static isValidHexColor(color) {
        const hexPattern = /^#[0-9A-Fa-f]{6}$/;
        return hexPattern.test(color);
    }
    /**
     * Normalize hex color to uppercase with # prefix
     */
    static normalizeHexColor(color) {
        if (!color) {
            return '#FFFFFF';
        }
        let normalized = color.trim().toUpperCase();
        if (!normalized.startsWith('#')) {
            normalized = `#${normalized}`;
        }
        return this.isValidHexColor(normalized) ? normalized : '#FFFFFF';
    }
}
exports.DynamicColorIconGenerator = DynamicColorIconGenerator;


/***/ }),
/* 60 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizationSettingsInteractionHandler = void 0;
const vscode = __importStar(__webpack_require__(1));
const settingsModel_1 = __webpack_require__(56);
const settingsStorage_1 = __webpack_require__(57);
const colorPickerUtils_1 = __webpack_require__(61);
const dynamicColorIconGenerator_1 = __webpack_require__(59);
/**
 * Handle Visualization Settings Interactions
 * Manages user interactions with visualization settings items
 */
class VisualizationSettingsInteractionHandler {
    context;
    storage;
    constructor(context) {
        this.context = context;
        console.log('VISUALIZATION-SETTINGS: Interaction handler initialized');
        this.storage = new settingsStorage_1.VisualizationSettingsStorage(context);
    }
    /**
     * Handle configuration of a specific setting field
     */
    async handleSettingConfiguration(settingKey) {
        console.log(`VISUALIZATION-SETTINGS: Configuring setting '${settingKey}'`);
        try {
            switch (settingKey) {
                case 'backgroundColor':
                case 'groundColor':
                    await this.handleColorConfiguration(settingKey);
                    break;
                case 'environmentPreset':
                    await this.handleEnvironmentPresetConfiguration();
                    break;
                case 'chartPalette':
                    await this.handleChartPaletteConfiguration();
                    break;
                default:
                    throw new Error(`Unknown setting key: ${settingKey}`);
            }
        }
        catch (error) {
            console.error(`VISUALIZATION-SETTINGS: Error configuring ${settingKey}:`, error);
            vscode.window.showErrorMessage(`Failed to configure ${settingKey}: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Handle color configuration (background or ground color) using HTML-based color picker
     */
    async handleColorConfiguration(colorType) {
        console.log(`VISUALIZATION-SETTINGS: Configuring ${colorType} with HTML color picker`);
        const currentSettings = this.storage.getSettings();
        const currentValue = currentSettings[colorType];
        try {
            // Prepare color picker options
            const fieldName = colorType === 'backgroundColor' ? 'Background Color' : 'Ground Color';
            const options = {
                fieldName,
                currentColor: colorPickerUtils_1.ColorPickerUtils.normalizeColor(currentValue)
            };
            // Create webview panel
            const panel = colorPickerUtils_1.ColorPickerUtils.createColorPickerWebview(this.context, options);
            // Load and set HTML content
            const htmlContent = await colorPickerUtils_1.ColorPickerUtils.loadColorPickerTemplate(this.context, options);
            panel.webview.html = htmlContent;
            // Handle messages from the webview
            const messageDisposable = panel.webview.onDidReceiveMessage(async (message) => {
                switch (message.type) {
                    case 'colorPicker.confirm':
                        const newColor = colorPickerUtils_1.ColorPickerUtils.normalizeColor(message.color);
                        console.log(`VISUALIZATION-SETTINGS: Color confirmed for ${colorType}: ${newColor}`);
                        console.log(`COLOR-PICKER: Generating new icon for ${colorType} with color ${newColor}`);
                        try {
                            // Generate new color icon
                            const iconUri = await dynamicColorIconGenerator_1.DynamicColorIconGenerator.getOrCreateColorIcon(this.context, colorType, newColor);
                            console.log(`COLOR-PICKER: Successfully generated icon for ${colorType}: ${iconUri.toString()}`);
                            // Clean up old icons
                            dynamicColorIconGenerator_1.DynamicColorIconGenerator.cleanupOldColorIcons(this.context, colorType, newColor);
                        }
                        catch (iconError) {
                            console.error(`COLOR-PICKER: Error generating icon for ${colorType}:`, iconError);
                            // Continue with setting update even if icon generation fails
                        }
                        // Update the setting
                        await this.storage.updateSetting(colorType, newColor);
                        // Refresh the tree view to show new icon
                        vscode.commands.executeCommand('codexr.servers.refresh');
                        vscode.window.showInformationMessage(`${fieldName} set to: ${newColor}`);
                        // Close the panel
                        panel.dispose();
                        break;
                    case 'colorPicker.cancel':
                        console.log(`VISUALIZATION-SETTINGS: Color picker cancelled for ${colorType}`);
                        panel.dispose();
                        break;
                }
            });
            // Clean up when panel is disposed
            panel.onDidDispose(() => {
                messageDisposable.dispose();
                console.log(`VISUALIZATION-SETTINGS: Color picker panel disposed for ${colorType}`);
            });
        }
        catch (error) {
            console.error(`VISUALIZATION-SETTINGS: Error opening color picker for ${colorType}:`, error);
            vscode.window.showErrorMessage(`Failed to open color picker: ${error}`);
            // Fallback to the original QuickPick method
            await this.handleColorConfigurationFallback(colorType);
        }
    }
    /**
     * Fallback color configuration using QuickPick (in case HTML color picker fails)
     */
    async handleColorConfigurationFallback(colorType) {
        console.log(`VISUALIZATION-SETTINGS: Using fallback QuickPick for ${colorType}`);
        const currentSettings = this.storage.getSettings();
        const currentValue = currentSettings[colorType];
        // Create QuickPick options
        const colorOptions = [
            ...settingsModel_1.PREDEFINED_COLORS.map(color => ({
                label: color.label,
                value: color.value,
                picked: color.value === currentValue
            })),
            {
                label: 'Pick a custom color...',
                value: 'custom',
                picked: false
            }
        ];
        const selectedOption = await vscode.window.showQuickPick(colorOptions, {
            placeHolder: `Select ${colorType.replace(/([A-Z])/g, ' $1').toLowerCase()}`,
            title: `Configure ${colorType.replace(/([A-Z])/g, ' $1')}`,
            matchOnDescription: true
        });
        if (!selectedOption) {
            console.log(`VISUALIZATION-SETTINGS: ${colorType} configuration cancelled`);
            return;
        }
        let newColor;
        if (selectedOption.value === 'custom') {
            const customColor = await this.getCustomColorInput(colorType, currentValue);
            if (!customColor) {
                return; // User cancelled custom color input
            }
            newColor = customColor;
        }
        else {
            newColor = selectedOption.value;
        }
        // Generate color icon before updating setting
        try {
            console.log(`COLOR-PICKER: Generating fallback icon for ${colorType} with color ${newColor}`);
            const iconUri = await dynamicColorIconGenerator_1.DynamicColorIconGenerator.getOrCreateColorIcon(this.context, colorType, newColor);
            console.log(`COLOR-PICKER: Successfully generated fallback icon for ${colorType}: ${iconUri.toString()}`);
            // Clean up old icons
            dynamicColorIconGenerator_1.DynamicColorIconGenerator.cleanupOldColorIcons(this.context, colorType, newColor);
        }
        catch (iconError) {
            console.error(`COLOR-PICKER: Error generating fallback icon for ${colorType}:`, iconError);
            // Continue with setting update even if icon generation fails
        }
        // Update the setting
        await this.storage.updateSetting(colorType, newColor);
        // Refresh the tree view
        vscode.commands.executeCommand('codexr.servers.refresh');
        console.log(`VISUALIZATION-SETTINGS: ${colorType} updated to '${newColor}'`);
        vscode.window.showInformationMessage(`${colorType.replace(/([A-Z])/g, ' $1')} set to: ${newColor}`);
    }
    /**
     * Get custom color input from user
     */
    async getCustomColorInput(colorType, currentValue) {
        let attempts = 0;
        const maxAttempts = 3;
        while (attempts < maxAttempts) {
            const customColor = await vscode.window.showInputBox({
                prompt: `Enter hex color for ${colorType.replace(/([A-Z])/g, ' $1').toLowerCase()} (e.g., #FF5733)`,
                value: currentValue,
                validateInput: (value) => {
                    if (!value) {
                        return 'Color value is required';
                    }
                    if (!(0, settingsModel_1.isValidHexColor)(value)) {
                        return 'Invalid hex color format. Use format: #RRGGBB (e.g., #FF5733)';
                    }
                    return null;
                }
            });
            if (customColor === undefined) {
                console.log(`VISUALIZATION-SETTINGS: Custom ${colorType} input cancelled`);
                return undefined;
            }
            if ((0, settingsModel_1.isValidHexColor)(customColor)) {
                console.log(`VISUALIZATION-SETTINGS: Valid custom ${colorType} entered: ${customColor}`);
                return customColor;
            }
            attempts++;
            console.log(`VISUALIZATION-SETTINGS: Invalid ${colorType} format attempt ${attempts}/${maxAttempts}: ${customColor}`);
            if (attempts < maxAttempts) {
                const retry = await vscode.window.showErrorMessage(`Invalid hex color format: ${customColor}. Please use format #RRGGBB (e.g., #FF5733)`, 'Try Again', 'Cancel');
                if (retry !== 'Try Again') {
                    console.log(`VISUALIZATION-SETTINGS: Custom ${colorType} input cancelled after ${attempts} attempts`);
                    return undefined;
                }
            }
            else {
                vscode.window.showErrorMessage(`Failed to set ${colorType} after ${maxAttempts} attempts. Please try again later.`);
                console.log(`VISUALIZATION-SETTINGS: Custom ${colorType} input failed after ${maxAttempts} attempts`);
                return undefined;
            }
        }
        return undefined;
    }
    /**
     * Handle environment preset configuration
     */
    async handleEnvironmentPresetConfiguration() {
        console.log('VISUALIZATION-SETTINGS: Configuring environment preset');
        const currentSettings = this.storage.getSettings();
        const currentValue = currentSettings.environmentPreset;
        const presetOptions = settingsModel_1.ENVIRONMENT_PRESETS.map(preset => ({
            label: preset.label,
            description: preset.description,
            value: preset.value,
            picked: preset.value === currentValue
        }));
        const selectedPreset = await vscode.window.showQuickPick(presetOptions, {
            placeHolder: 'Select environment preset',
            title: 'Configure Environment Preset',
            matchOnDescription: true
        });
        if (!selectedPreset) {
            console.log('VISUALIZATION-SETTINGS: Environment preset configuration cancelled');
            return;
        }
        // Update the setting
        await this.storage.updateSetting('environmentPreset', selectedPreset.value);
        // Refresh the tree view
        vscode.commands.executeCommand('codexr.servers.refresh');
        console.log(`VISUALIZATION-SETTINGS: Environment preset updated to '${selectedPreset.value}'`);
        vscode.window.showInformationMessage(`Environment preset set to: ${selectedPreset.label} - ${selectedPreset.description}`);
    }
    /**
     * Handle chart palette configuration
     */
    async handleChartPaletteConfiguration() {
        console.log('VISUALIZATION-SETTINGS: Configuring chart palette');
        const currentSettings = this.storage.getSettings();
        const currentValue = currentSettings.chartPalette;
        const paletteOptions = settingsModel_1.CHART_PALETTES.map(palette => ({
            label: palette.label,
            description: palette.description,
            value: palette.value,
            picked: palette.value === currentValue
        }));
        const selectedPalette = await vscode.window.showQuickPick(paletteOptions, {
            placeHolder: 'Select chart palette',
            title: 'Configure Chart Palette',
            matchOnDescription: true
        });
        if (!selectedPalette) {
            console.log('VISUALIZATION-SETTINGS: Chart palette configuration cancelled');
            return;
        }
        // Update the setting
        await this.storage.updateSetting('chartPalette', selectedPalette.value);
        // Refresh the tree view
        vscode.commands.executeCommand('codexr.servers.refresh');
        console.log(`VISUALIZATION-SETTINGS: Chart palette updated to '${selectedPalette.value}'`);
        vscode.window.showInformationMessage(`Chart palette set to: ${selectedPalette.label} - ${selectedPalette.description}`);
    }
    /**
     * Get current storage instance for external access
     */
    getStorage() {
        return this.storage;
    }
    /**
     * Cleanup resources
     */
    dispose() {
        console.log('VISUALIZATION-SETTINGS: Interaction handler disposed');
    }
}
exports.VisualizationSettingsInteractionHandler = VisualizationSettingsInteractionHandler;


/***/ }),
/* 61 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ColorPickerUtils = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
class ColorPickerUtils {
    static TEMPLATE_PATH = 'templates/utils/color-picker.html';
    /**
     * Load and process the color picker HTML template
     */
    static async loadColorPickerTemplate(context, options) {
        try {
            const templatePath = path.join(context.extensionPath, this.TEMPLATE_PATH);
            let templateContent = fs.readFileSync(templatePath, 'utf8');
            // Replace placeholders
            templateContent = templateContent
                .replace(/\$\{FIELD_NAME\}/g, options.fieldName)
                .replace(/\$\{CURRENT_COLOR\}/g, options.currentColor);
            return templateContent;
        }
        catch (error) {
            console.error('[VISUALIZATION-SETTINGS] Error loading color picker template:', error);
            throw new Error(`Failed to load color picker template: ${error}`);
        }
    }
    /**
     * Create and configure a webview for the color picker
     */
    static createColorPickerWebview(context, options) {
        const panel = vscode.window.createWebviewPanel('colorPicker', `Color Picker - ${options.fieldName}`, vscode.ViewColumn.One, {
            enableScripts: true,
            retainContextWhenHidden: true,
            localResourceRoots: [
                vscode.Uri.file(path.join(context.extensionPath, 'templates'))
            ]
        });
        // Set the icon for the panel
        panel.iconPath = {
            light: vscode.Uri.file(path.join(context.extensionPath, 'resources', 'icon.svg')),
            dark: vscode.Uri.file(path.join(context.extensionPath, 'resources', 'icon.svg'))
        };
        return panel;
    }
    /**
     * Validate hex color format
     */
    static validateHexColor(color) {
        return /^#[0-9a-fA-F]{6}$/.test(color);
    }
    /**
     * Normalize color to uppercase hex format
     */
    static normalizeColor(color) {
        if (this.validateHexColor(color)) {
            return color.toUpperCase();
        }
        return '#FFFFFF'; // Default fallback
    }
    /**
     * Get predefined colors for fallback
     */
    static getPredefinedColors() {
        return [
            '#FFFFFF', // White
            '#000000', // Black
            '#B10DC9', // Purple
            '#FF4081', // Pink
            '#F44336', // Red
            '#FF9800', // Orange
            '#FFEB3B', // Yellow
            '#4CAF50', // Green
            '#2196F3', // Blue
            '#9C27B0', // Violet
            '#607D8B', // Blue Grey
            '#795548' // Brown
        ];
    }
}
exports.ColorPickerUtils = ColorPickerUtils;


/***/ }),
/* 62 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.initializeSettingsAccessors = initializeSettingsAccessors;
exports.getSelectedBackgroundColor = getSelectedBackgroundColor;
exports.getSelectedGroundColor = getSelectedGroundColor;
exports.getSelectedEnvironment = getSelectedEnvironment;
exports.getSelectedPalette = getSelectedPalette;
exports.getAllSelectedSettings = getAllSelectedSettings;
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const settingsModel_1 = __webpack_require__(56);
/**
 * Settings Accessors
 * Clean utility functions to access visualization settings for babia-templates integration
 */
// Module-level cache for context
let extensionContext = null;
/**
 * Initialize the settings accessors with extension context
 * Must be called during extension activation
 */
function initializeSettingsAccessors(context) {
    extensionContext = context;
    console.log('VISUALIZATION-SETTINGS: Settings accessors initialized');
}
/**
 * Get the visualization configuration directory path
 */
function getConfigDirectory() {
    if (!extensionContext) {
        throw new Error('Settings accessors not initialized. Call initializeSettingsAccessors() first.');
    }
    const globalStorageUri = extensionContext.globalStorageUri;
    return path.join(globalStorageUri.fsPath, 'visualization-configuration');
}
/**
 * Get the settings file path
 */
function getSettingsFilePath() {
    return path.join(getConfigDirectory(), 'visualization-settings.json');
}
/**
 * Read settings from the JSON file
 */
function readSettingsFromFile() {
    try {
        const settingsFilePath = getSettingsFilePath();
        if (fs.existsSync(settingsFilePath)) {
            const fileContent = fs.readFileSync(settingsFilePath, 'utf8');
            const jsonSettings = JSON.parse(fileContent);
            console.log('VISUALIZATION-SETTINGS: Read settings from file:', jsonSettings);
            return jsonSettings;
        }
    }
    catch (error) {
        console.error('VISUALIZATION-SETTINGS: Error reading settings file:', error);
    }
    return null;
}
/**
 * Get current background color from file storage or globalState fallback
 */
async function getBackgroundColorFromStorage() {
    try {
        // First try to read from JSON file
        const fileSettings = readSettingsFromFile();
        if (fileSettings && fileSettings.backgroundColor) {
            return fileSettings.backgroundColor;
        }
        // Fallback to globalState for backward compatibility
        if (extensionContext) {
            const legacySettings = extensionContext.globalState.get('visualizationSettings');
            if (legacySettings && legacySettings.backgroundColor) {
                return legacySettings.backgroundColor;
            }
        }
    }
    catch (error) {
        console.error('VISUALIZATION-SETTINGS: Error reading background color from storage:', error);
    }
    return settingsModel_1.DEFAULT_VISUALIZATION_SETTINGS.backgroundColor;
}
/**
 * Get current ground color from file storage or globalState fallback
 */
async function getGroundColorFromStorage() {
    try {
        // First try to read from JSON file
        const fileSettings = readSettingsFromFile();
        if (fileSettings && fileSettings.groundColor) {
            return fileSettings.groundColor;
        }
        // Fallback to globalState for backward compatibility
        if (extensionContext) {
            const legacySettings = extensionContext.globalState.get('visualizationSettings');
            if (legacySettings && legacySettings.groundColor) {
                return legacySettings.groundColor;
            }
        }
    }
    catch (error) {
        console.error('VISUALIZATION-SETTINGS: Error reading ground color from storage:', error);
    }
    return settingsModel_1.DEFAULT_VISUALIZATION_SETTINGS.groundColor;
}
/**
 * Get the currently selected background color
 * @returns Promise<string> Hex color value (e.g., "#B10DC9")
 */
async function getSelectedBackgroundColor() {
    console.log('VISUALIZATION-SETTINGS: Getting selected background color');
    const color = await getBackgroundColorFromStorage();
    console.log(`VISUALIZATION-SETTINGS: Background color: ${color}`);
    return color;
}
/**
 * Get the currently selected ground color
 * @returns Promise<string> Hex color value (e.g., "#FFFFFF")
 */
async function getSelectedGroundColor() {
    console.log('VISUALIZATION-SETTINGS: Getting selected ground color');
    const color = await getGroundColorFromStorage();
    console.log(`VISUALIZATION-SETTINGS: Ground color: ${color}`);
    return color;
}
/**
 * Get the currently selected environment preset
 * @returns Promise<string> Environment preset name (e.g., "forest")
 */
async function getSelectedEnvironment() {
    console.log('VISUALIZATION-SETTINGS: Getting selected environment');
    const settings = readSettingsFromFile();
    const environment = settings?.environment || settingsModel_1.DEFAULT_VISUALIZATION_SETTINGS.environmentPreset;
    console.log(`VISUALIZATION-SETTINGS: Environment: ${environment}`);
    return environment;
}
/**
 * Get the currently selected chart palette
 * @returns Promise<string> Chart palette name (e.g., "ubuntu")
 */
async function getSelectedPalette() {
    console.log('VISUALIZATION-SETTINGS: Getting selected chart palette');
    const settings = readSettingsFromFile();
    const palette = settings?.palette || settingsModel_1.DEFAULT_VISUALIZATION_SETTINGS.chartPalette;
    console.log(`VISUALIZATION-SETTINGS: Palette: ${palette}`);
    return palette;
}
/**
 * Get all current settings in a single call (for efficiency)
 * @returns Promise<object> Object containing all current settings
 */
async function getAllSelectedSettings() {
    console.log('VISUALIZATION-SETTINGS: Getting all selected settings');
    const [backgroundColor, groundColor] = await Promise.all([
        getSelectedBackgroundColor(),
        getSelectedGroundColor()
    ]);
    const settings = readSettingsFromFile();
    const environment = settings?.environment || settingsModel_1.DEFAULT_VISUALIZATION_SETTINGS.environmentPreset;
    const palette = settings?.palette || settingsModel_1.DEFAULT_VISUALIZATION_SETTINGS.chartPalette;
    const allSettings = {
        backgroundColor,
        groundColor,
        environment,
        palette
    };
    console.log('VISUALIZATION-SETTINGS: All settings:', allSettings);
    return allSettings;
}


/***/ }),
/* 63 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateChart = void 0;
const templateCharts_1 = __webpack_require__(50);
/**
 * Chart Creator Module - Specialized for Chart Entity Creation
 * Focused solely on creating chart entities with dimension mappings
 * Part of the modular restructuring for cleaner architecture
 */
class CreateChart {
    /**
     * Main method to create a chart entity with dimension mappings
     * Returns only the chart HTML component, no structural elements
     */
    static createChartEntity(chartId, mappings, title = 'Chart Visualization', palette = 'ubuntu') {
        console.log(`CREATE_CHART: Creating chart entity with ID: ${chartId}`);
        console.log(`CREATE_CHART: Mappings:`, mappings);
        try {
            // Get or create chart template
            const chart = this.getOrCreateChart(chartId);
            if (!chart) {
                return {
                    success: false,
                    error: `Failed to get or create chart with ID: ${chartId}`
                };
            }
            // Validate mappings against chart dimensions
            const validation = this.validateMappings(chart, mappings);
            if (!validation.isValid) {
                return {
                    success: false,
                    error: `Invalid mappings: ${validation.errors.join(', ')}`
                };
            }
            // Create placeholders map for chart-specific placeholders
            const placeholders = this.createChartPlaceholders(chart, mappings, title, palette);
            // Replace placeholders in chart template
            const processedChartHtml = this.replacePlaceholders(chart.htmlTemplate, placeholders);
            console.log(`CREATE_CHART: Chart entity created successfully for: ${chartId}`);
            return {
                success: true,
                chartHtml: processedChartHtml
            };
        }
        catch (error) {
            console.error(`CREATE_CHART: Error creating chart entity:`, error);
            return {
                success: false,
                error: error instanceof Error ? error.message : String(error)
            };
        }
    }
    /**
     * Get existing chart from chartTemplates or create default boats chart
     */
    static getOrCreateChart(chartId) {
        // First try to find existing chart
        const existingChart = templateCharts_1.chartTemplates.find(c => c.id === chartId);
        if (existingChart) {
            console.log(`CREATE_CHART: Found existing chart: ${chartId}`);
            return existingChart;
        }
        // If not found and it's 'boats', create default boats chart
        if (chartId === 'boats') {
            console.log(`CREATE_CHART: Creating default boats chart`);
            return this.createDefaultBoatsChart();
        }
        // For other unknown charts, return null
        console.error(`CREATE_CHART: Unknown chart ID and cannot create default: ${chartId}`);
        return null;
    }
    /**
     * Create default boats chart with standard XR analysis dimensions
     */
    static createDefaultBoatsChart() {
        return {
            id: 'boats',
            name: 'Boats Chart',
            description: 'XR Analysis Boats Chart with Parameters, Lines Count, and Complexity',
            category: 'geometric',
            dimensions: [
                {
                    name: 'area',
                    label: 'Area (Parameters)',
                    dataType: 'numeric',
                    required: true,
                    description: 'Function parameters count'
                },
                {
                    name: 'height',
                    label: 'Height (Lines Count)',
                    dataType: 'numeric',
                    required: true,
                    description: 'Lines of code count'
                },
                {
                    name: 'color',
                    label: 'Color (Complexity)',
                    dataType: 'numeric',
                    required: true,
                    description: 'Cyclomatic complexity'
                }
            ],
            htmlTemplate: `<!-- XR Analysis Boats Chart -->
                <a-entity id="{{CHART_ID}}"
                    babia-boats="from: data;
                                 title: {{TITLE}};
                                 legend: true;
                                 palette: {{PALETTE}};
                                 area: {{AREA_FIELD}};
                                 height: {{HEIGHT_FIELD}};
                                 color: {{COLOR_FIELD}};
                                 axis_name: true"
                    position="0 1 -10"
                    rotation="0 0 0"
                    scale="1.5 1.5 1.5"
                    class="babiaxraycasterclass">
                </a-entity>`
        };
    }
    /**
     * Validate that mappings match chart dimensions
     */
    static validateMappings(chart, mappings) {
        const errors = [];
        // Check that all required dimensions are mapped
        for (const dimension of chart.dimensions) {
            if (dimension.required) {
                const mapping = mappings.find(m => m.dimension === dimension.name);
                if (!mapping) {
                    errors.push(`Required dimension '${dimension.name}' is not mapped`);
                }
                else if (!mapping.dataField || mapping.dataField.trim() === '') {
                    errors.push(`Required dimension '${dimension.name}' has empty dataField`);
                }
            }
        }
        // Check that all mappings reference valid dimensions
        for (const mapping of mappings) {
            const dimension = chart.dimensions.find(d => d.name === mapping.dimension);
            if (!dimension) {
                errors.push(`Mapping references unknown dimension: '${mapping.dimension}'`);
            }
        }
        return {
            isValid: errors.length === 0,
            errors
        };
    }
    /**
     * Create chart-specific placeholders map (no structural placeholders)
     */
    static createChartPlaceholders(chart, mappings, title, palette) {
        const placeholders = new Map();
        // Basic chart placeholders
        placeholders.set('TITLE', title);
        placeholders.set('PALETTE', palette);
        placeholders.set('CHART_ID', `chart-${chart.id}-${Date.now()}`);
        // Dimension-specific placeholders
        for (const mapping of mappings) {
            const dimension = chart.dimensions.find(d => d.name === mapping.dimension);
            if (dimension) {
                const fieldName = mapping.dataField;
                const upperDimension = mapping.dimension.toUpperCase();
                // Multiple placeholder formats for flexibility
                placeholders.set(`${upperDimension}_FIELD`, fieldName);
                placeholders.set(`${mapping.dimension}_field`, fieldName);
                placeholders.set(mapping.dimension, fieldName);
                // Special common dimension mappings
                switch (mapping.dimension.toLowerCase()) {
                    case 'area':
                        placeholders.set('AREA_FIELD', fieldName);
                        break;
                    case 'height':
                        placeholders.set('HEIGHT_FIELD', fieldName);
                        break;
                    case 'color':
                        placeholders.set('COLOR_FIELD', fieldName);
                        break;
                    case 'key':
                    case 'category':
                        placeholders.set('KEY_FIELD', fieldName);
                        placeholders.set('CATEGORY_FIELD', fieldName);
                        break;
                    case 'size':
                    case 'value':
                        placeholders.set('SIZE_FIELD', fieldName);
                        placeholders.set('VALUE_FIELD', fieldName);
                        break;
                    case 'x_axis':
                        placeholders.set('X_AXIS_FIELD', fieldName);
                        break;
                    case 'y_axis':
                        placeholders.set('Y_AXIS_FIELD', fieldName);
                        break;
                }
            }
        }
        console.log(`CREATE_CHART: Created ${placeholders.size} chart placeholders`);
        return placeholders;
    }
    /**
     * Replace placeholders in chart template with actual values
     */
    static replacePlaceholders(template, placeholders) {
        let result = template;
        // Replace placeholders with multiple formats: {{PLACEHOLDER}} and ${PLACEHOLDER}
        for (const [placeholder, value] of placeholders) {
            const patterns = [
                new RegExp(`\\{\\{\\s*${this.escapeRegex(placeholder)}\\s*\\}\\}`, 'g'),
                new RegExp(`\\$\\{\\s*${this.escapeRegex(placeholder)}\\s*\\}`, 'g')
            ];
            for (const pattern of patterns) {
                result = result.replace(pattern, value);
            }
        }
        // Check for unresolved chart placeholders
        const unresolvedPlaceholders = result.match(/\{\{[^}]+\}\}|\$\{[^}]+\}/g);
        if (unresolvedPlaceholders) {
            console.warn(`CREATE_CHART: Unresolved chart placeholders found:`, unresolvedPlaceholders);
        }
        return result;
    }
    /**
     * Escape special regex characters
     */
    static escapeRegex(str) {
        return str.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    }
    /**
     * Get available charts with their dimensions
     */
    static getAvailableCharts() {
        return templateCharts_1.chartTemplates.map(chart => ({
            id: chart.id,
            name: chart.name,
            dimensions: chart.dimensions.map(d => d.name)
        }));
    }
    /**
     * Get chart metadata by ID
     */
    static getChartMetadata(chartId) {
        return templateCharts_1.chartTemplates.find(c => c.id === chartId) || null;
    }
    /**
     * Create default mappings for XR analysis (boats chart)
     */
    static createDefaultXRMappings() {
        return [
            { dimension: 'area', dataField: 'parameters' },
            { dimension: 'height', dataField: 'lineCount' },
            { dimension: 'color', dataField: 'complexity' }
        ];
    }
}
exports.CreateChart = CreateChart;


/***/ }),
/* 64 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CreateStructure = void 0;
const nonceGenerator_1 = __webpack_require__(12);
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
/**
 * Structure Creator Module - Specialized for General Structure Placeholders
 * Handles background, environment, SSE scripts, tree_builder, and other structural elements
 * Part of the modular restructuring for cleaner architecture
 */
class CreateStructure {
    /**
     * Main method to create structural placeholders for complete XR template
     * Returns placeholders for background, environment, SSE scripts, etc.
     */
    static createStructuralPlaceholders(title, dataSource, visualizationSettings, analysisType = 'none', context, chartType, isDirectoryAnalysis) {
        console.log(`CREATE_STRUCTURE: Creating structural placeholders for analysis type: ${analysisType}, chart type: ${chartType}`);
        try {
            const placeholders = new Map();
            // Basic structural placeholders
            placeholders.set('TITLE', title);
            placeholders.set('DATA_SOURCE', dataSource);
            // Environment and visualization settings
            placeholders.set('ENVIRONMENT_PRESET', visualizationSettings.environment);
            placeholders.set('BACKGROUND_COLOR', visualizationSettings.backgroundColor);
            placeholders.set('GROUND_COLOR', visualizationSettings.groundColor);
            placeholders.set('CHART_PALETTE', visualizationSettings.palette);
            // Generate unique nonce for security
            const nonce = (0, nonceGenerator_1.generateNonce)();
            placeholders.set('NONCE', nonce);
            placeholders.set('nonce', nonce); // lowercase for template compatibility
            // SSE script with nonce and standard main.js path
            const sseScriptWithNonce = this.getSSEScriptWithNonce(analysisType, nonce, context);
            placeholders.set('SSE_SCRIPT', sseScriptWithNonce);
            // Template-specific placeholders for backwards compatibility
            placeholders.set('scriptUri', './main.js');
            placeholders.set('SCRIPT_URI', './main.js');
            // Tree builder - only for boats chart in XR analysis
            const treeBuilder = this.getTreeBuilder(analysisType, chartType, isDirectoryAnalysis);
            placeholders.set('TREE_BUILDER', treeBuilder);
            // Icon path (optional)
            placeholders.set('ICON_PATH', '');
            // Additional metadata
            placeholders.set('TIMESTAMP', new Date().toISOString());
            placeholders.set('ANALYSIS_TYPE', analysisType);
            console.log(`CREATE_STRUCTURE: Created ${placeholders.size} structural placeholders`);
            return {
                success: true,
                placeholders
            };
        }
        catch (error) {
            console.error(`CREATE_STRUCTURE: Error creating structural placeholders:`, error);
            return {
                success: false,
                error: error instanceof Error ? error.message : String(error)
            };
        }
    }
    /**
     * Get appropriate SSE script with nonce and main.js path based on analysis type
     */
    static getSSEScriptWithNonce(analysisType, nonce, context) {
        console.log(`CREATE_STRUCTURE: Creating SSE script with nonce for analysis type: ${analysisType}`);
        try {
            let scriptFileName;
            switch (analysisType) {
                case 'xr':
                    scriptFileName = 'live_sse_fileXR.js';
                    break;
                case 'dom':
                    scriptFileName = 'live_sse_fileDOM.js';
                    break;
                case 'none':
                default:
                    // No SSE script needed
                    return '';
            }
            const scriptPath = path.join(context.extensionPath, 'templates', 'xr', 'sse', scriptFileName);
            if (!fs.existsSync(scriptPath)) {
                console.warn(`CREATE_STRUCTURE: SSE script not found at: ${scriptPath}`);
                return '';
            }
            const scriptContent = fs.readFileSync(scriptPath, 'utf8');
            console.log(`CREATE_STRUCTURE: Loaded SSE script: ${scriptFileName} (${scriptContent.length} chars)`);
            // Add nonce script tag for main.js (standard path for all analyses)
            const mainJsScript = `<script nonce="${nonce}" src="./main.js"></script>`;
            return scriptContent + '\n' + mainJsScript;
        }
        catch (error) {
            console.error(`CREATE_STRUCTURE: Error loading SSE script:`, error);
            return '';
        }
    }
    /**
     * Get tree builder entity for boats chart in XR analysis
     */
    static getTreeBuilder(analysisType, chartType, isDirectoryAnalysis) {
        console.log(`CREATE_STRUCTURE: Getting tree builder for analysis type: ${analysisType}, chart type: ${chartType}, directory analysis: ${isDirectoryAnalysis}`);
        // Tree builder is only needed for boats chart in XR analysis
        if (analysisType === 'xr' && chartType === 'boats') {
            if (isDirectoryAnalysis) {
                console.log(`CREATE_STRUCTURE: Adding tree builder for XR boats chart (directory analysis)`);
                return '<a-entity id="tree" babia-treebuilder="field: filePath; split_by: /; from: data"></a-entity>';
            }
            else {
                console.log(`CREATE_STRUCTURE: Adding tree builder for XR boats chart (file analysis)`);
                return '<a-entity id="tree" babia-treebuilder="field: functionName; split_by: /; from: data"></a-entity>';
            }
        }
        console.log(`CREATE_STRUCTURE: No tree builder needed for this configuration`);
        return '';
    }
    /**
     * Get appropriate SSE script based on analysis type (legacy method)
     */
    static getSSEScript(analysisType, context) {
        console.log(`CREATE_STRUCTURE: Selecting SSE script for analysis type: ${analysisType}`);
        try {
            let scriptFileName;
            switch (analysisType) {
                case 'xr':
                    scriptFileName = 'live_sse_fileXR.js';
                    break;
                case 'dom':
                    scriptFileName = 'live_sse_fileDOM.js';
                    break;
                case 'none':
                default:
                    // No SSE script needed
                    return '';
            }
            const scriptPath = path.join(context.extensionPath, 'templates', 'xr', 'sse', scriptFileName);
            if (!fs.existsSync(scriptPath)) {
                console.warn(`CREATE_STRUCTURE: SSE script not found at: ${scriptPath}`);
                return '';
            }
            const scriptContent = fs.readFileSync(scriptPath, 'utf8');
            console.log(`CREATE_STRUCTURE: Loaded SSE script: ${scriptFileName}`);
            // Return script wrapped in script tags for injection
            return `<script type="text/javascript">
${scriptContent}
</script>`;
        }
        catch (error) {
            console.error(`CREATE_STRUCTURE: Error loading SSE script:`, error);
            return '';
        }
    }
    /**
     * Replace all structural placeholders in template
     */
    static replaceStructuralPlaceholders(template, placeholders) {
        console.log('CREATE_STRUCTURE: Replacing structural placeholders');
        let result = template;
        // Replace placeholders with multiple formats: {{PLACEHOLDER}} and ${PLACEHOLDER}
        for (const [placeholder, value] of placeholders) {
            const patterns = [
                new RegExp(`\\$\\{${this.escapeRegex(placeholder)}\\}`, 'g'),
                new RegExp(`\\{\\{\\s*${this.escapeRegex(placeholder)}\\s*\\}\\}`, 'g')
            ];
            for (const pattern of patterns) {
                result = result.replace(pattern, value);
            }
        }
        // Check for unresolved structural placeholders
        const unresolvedPlaceholders = result.match(/\{\{[^}]+\}\}|\$\{[^}]+\}/g);
        if (unresolvedPlaceholders) {
            console.warn(`CREATE_STRUCTURE: Unresolved structural placeholders found:`, unresolvedPlaceholders);
        }
        console.log('CREATE_STRUCTURE: Structural placeholders replaced');
        return result;
    }
    /**
     * Create environment-specific placeholders
     */
    static createEnvironmentPlaceholders(visualizationSettings) {
        const placeholders = new Map();
        placeholders.set('ENVIRONMENT_PRESET', visualizationSettings.environment);
        placeholders.set('BACKGROUND_COLOR', visualizationSettings.backgroundColor);
        placeholders.set('GROUND_COLOR', visualizationSettings.groundColor);
        placeholders.set('PALETTE', visualizationSettings.palette);
        console.log(`CREATE_STRUCTURE: Created environment placeholders`);
        return placeholders;
    }
    /**
     * Create data source placeholders
     */
    static createDataSourcePlaceholders(dataSource) {
        const placeholders = new Map();
        placeholders.set('DATA_SOURCE', dataSource);
        placeholders.set('DATA_PATH', dataSource);
        placeholders.set('JSON_SOURCE', dataSource);
        console.log(`CREATE_STRUCTURE: Created data source placeholders`);
        return placeholders;
    }
    /**
     * Create metadata placeholders
     */
    static createMetadataPlaceholders(title, analysisType = 'unknown') {
        const placeholders = new Map();
        placeholders.set('TITLE', title);
        placeholders.set('PAGE_TITLE', title);
        placeholders.set('ANALYSIS_TYPE', analysisType);
        placeholders.set('TIMESTAMP', new Date().toISOString());
        placeholders.set('NONCE', (0, nonceGenerator_1.generateNonce)());
        console.log(`CREATE_STRUCTURE: Created metadata placeholders`);
        return placeholders;
    }
    /**
     * Combine multiple placeholder maps
     */
    static combinePlaceholders(...placeholderMaps) {
        const combined = new Map();
        for (const map of placeholderMaps) {
            for (const [key, value] of map) {
                combined.set(key, value);
            }
        }
        console.log(`CREATE_STRUCTURE: Combined ${combined.size} total placeholders`);
        return combined;
    }
    /**
     * Escape special regex characters
     */
    static escapeRegex(str) {
        return str.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    }
    /**
     * Get available analysis types for SSE script selection
     */
    static getAvailableAnalysisTypes() {
        return ['xr', 'dom', 'none'];
    }
    /**
     * Validate analysis type
     */
    static isValidAnalysisType(type) {
        return ['xr', 'dom', 'none'].includes(type);
    }
}
exports.CreateStructure = CreateStructure;


/***/ }),
/* 65 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Server Runtime Module
 *
 * This module provides the complete server runtime infrastructure for the CodeXR extension.
 * It includes HTTP/HTTPS servers, port management, and a unified launcher system.
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.MultiServerLauncher = exports.PortManager = exports.HttpsCustomServer = exports.HttpsDefaultServer = exports.HttpServer = void 0;
exports.createServerLauncher = createServerLauncher;
exports.launchServerWithFile = launchServerWithFile;
exports.isPortAvailable = isPortAvailable;
exports.findAvailablePort = findAvailablePort;
exports.getSuggestedPorts = getSuggestedPorts;
// Core server implementations
var httpServer_1 = __webpack_require__(15);
Object.defineProperty(exports, "HttpServer", ({ enumerable: true, get: function () { return httpServer_1.HttpServer; } }));
var httpsDefaultServer_1 = __webpack_require__(23);
Object.defineProperty(exports, "HttpsDefaultServer", ({ enumerable: true, get: function () { return httpsDefaultServer_1.HttpsDefaultServer; } }));
var httpsCustomServer_1 = __webpack_require__(27);
Object.defineProperty(exports, "HttpsCustomServer", ({ enumerable: true, get: function () { return httpsCustomServer_1.HttpsCustomServer; } }));
// Utility classes
var portManager_1 = __webpack_require__(25);
Object.defineProperty(exports, "PortManager", ({ enumerable: true, get: function () { return portManager_1.PortManager; } }));
// Main launcher and types
var multiServerLauncher_1 = __webpack_require__(14);
Object.defineProperty(exports, "MultiServerLauncher", ({ enumerable: true, get: function () { return multiServerLauncher_1.MultiServerLauncher; } }));
// Import for use in utility functions
const multiServerLauncher_2 = __webpack_require__(14);
const portManager_2 = __webpack_require__(25);
/**
 * Create a new multi-server launcher instance
 * @param context - VS Code extension context
 * @returns MultiServerLauncher instance
 */
function createServerLauncher(context) {
    return new multiServerLauncher_2.MultiServerLauncher(context);
}
/**
 * Launch server with a specific HTML file
 * @param context - VS Code extension context
 * @param htmlFilePath - Path to HTML file to serve
 * @param customName - Optional custom display name for the server
 * @returns Promise<MultiServerLaunchResult>
 */
async function launchServerWithFile(context, htmlFilePath, customName) {
    const launcher = new multiServerLauncher_2.MultiServerLauncher(context);
    return launcher.launchServer(htmlFilePath, customName);
}
/**
 * Utility function to check if a port is available
 * @param port - Port number to check
 * @returns Promise<boolean> - True if port is available
 */
async function isPortAvailable(port) {
    return portManager_2.PortManager.isPortAvailable(port);
}
/**
 * Utility function to find an available port
 * @param startPort - Port to start searching from
 * @param endPort - Maximum port to check (optional)
 * @returns Promise<number> - First available port found
 */
async function findAvailablePort(startPort, endPort) {
    return portManager_2.PortManager.findAvailablePort(startPort, endPort);
}
/**
 * Get suggested ports for a service type
 * @param serviceType - Type of service ('http', 'https', 'dev')
 * @returns number[] - Array of suggested ports
 */
function getSuggestedPorts(serviceType) {
    return portManager_2.PortManager.getSuggestedPorts(serviceType);
}


/***/ }),
/* 66 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizationRestorer = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const index_1 = __webpack_require__(65);
const activeServerRegistry_1 = __webpack_require__(28);
/**
 * Visualization Restorer
 * Responsible for scanning and relaunching stored visualizations
 */
class VisualizationRestorer {
    context;
    visualizeDataPath;
    constructor(context) {
        this.context = context;
        this.visualizeDataPath = path.join(context.globalStorageUri.fsPath, 'visualize-data');
        console.log('BROWSE-VISUALIZATIONS: Restorer initialized with path:', this.visualizeDataPath);
    }
    /**
     * Scan the visualize-data directory for stored visualizations
     */
    async scanStoredVisualizations() {
        console.log('BROWSE-VISUALIZATIONS: Scanning for stored visualizations...');
        const visualizations = [];
        try {
            // Ensure the visualize-data directory exists
            if (!fs.existsSync(this.visualizeDataPath)) {
                console.log('BROWSE-VISUALIZATIONS: visualize-data directory does not exist yet');
                return visualizations;
            }
            // Read directory contents
            const entries = await fs.promises.readdir(this.visualizeDataPath, { withFileTypes: true });
            const folders = entries.filter(entry => entry.isDirectory());
            console.log(`BROWSE-VISUALIZATIONS: Found ${folders.length} folders in visualize-data directory`);
            for (const folder of folders) {
                const folderName = folder.name;
                const folderPath = path.join(this.visualizeDataPath, folderName);
                // Extract name from folder name (everything before the last underscore)
                const lastUnderscoreIndex = folderName.lastIndexOf('_');
                const name = lastUnderscoreIndex > 0 ? folderName.substring(0, lastUnderscoreIndex) : folderName;
                // Check for required files
                const indexPath = path.join(folderPath, 'index.html');
                const dataPath = path.join(folderPath, 'data.json');
                const indexExists = fs.existsSync(indexPath);
                const dataExists = fs.existsSync(dataPath);
                const isValid = indexExists && dataExists;
                const visualization = {
                    name,
                    folderName,
                    folderPath,
                    indexPath,
                    dataPath,
                    isValid
                };
                visualizations.push(visualization);
                console.log(`BROWSE-VISUALIZATIONS: Found visualization "${name}" (${folderName}), valid: ${isValid}`);
                if (!isValid) {
                    console.warn(`BROWSE-VISUALIZATIONS: Invalid visualization - index.html exists: ${indexExists}, data.json exists: ${dataExists}`);
                }
            }
        }
        catch (error) {
            console.error('BROWSE-VISUALIZATIONS: Error scanning visualizations:', error);
            vscode.window.showErrorMessage(`Failed to scan visualizations: ${error instanceof Error ? error.message : String(error)}`);
        }
        console.log(`BROWSE-VISUALIZATIONS: Scan completed, found ${visualizations.length} visualizations`);
        return visualizations;
    }
    /**
     * Launch a stored visualization
     */
    async launchVisualization(visualization) {
        console.log(`BROWSE-VISUALIZATIONS: Launching visualization "${visualization.name}"`);
        try {
            if (!visualization.isValid) {
                throw new Error(`Visualization "${visualization.name}" is missing required files`);
            }
            // Check if visualization is already running
            const activeRegistry = (0, activeServerRegistry_1.getActiveServerRegistry)();
            const activeServers = activeRegistry.getAllServers();
            // Check by custom name or by path
            const alreadyActive = activeServers.some((server) => server.customName === visualization.name ||
                server.filePath === visualization.indexPath);
            if (alreadyActive) {
                console.log(`BROWSE-VISUALIZATIONS: Visualization "${visualization.name}" is already active`);
                vscode.window.showInformationMessage(`Visualization "${visualization.name}" is already running`);
                return;
            }
            // Launch the visualization using the existing server launcher
            console.log(`BROWSE-VISUALIZATIONS: Launching server for visualization "${visualization.name}" with file: ${visualization.indexPath}`);
            await (0, index_1.launchServerWithFile)(this.context, visualization.indexPath, visualization.name // Use the extracted name as customName
            );
            console.log(`BROWSE-VISUALIZATIONS: Successfully launched visualization "${visualization.name}"`);
            vscode.window.showInformationMessage(`Launched visualization: ${visualization.name}`);
        }
        catch (error) {
            console.error(`BROWSE-VISUALIZATIONS: Error launching visualization "${visualization.name}":`, error);
            vscode.window.showErrorMessage(`Failed to launch visualization "${visualization.name}": ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Delete all stored visualizations (Reset All)
     */
    async resetAllVisualizations() {
        console.log('BROWSE-VISUALIZATIONS: Resetting all visualizations...');
        try {
            if (!fs.existsSync(this.visualizeDataPath)) {
                console.log('BROWSE-VISUALIZATIONS: No visualize-data directory to reset');
                return;
            }
            // Get list of folders to delete
            const entries = await fs.promises.readdir(this.visualizeDataPath, { withFileTypes: true });
            const folders = entries.filter(entry => entry.isDirectory());
            if (folders.length === 0) {
                console.log('BROWSE-VISUALIZATIONS: No visualizations to reset');
                vscode.window.showInformationMessage('No stored visualizations to reset');
                return;
            }
            // Confirm deletion
            const confirmResult = await vscode.window.showWarningMessage(`Delete all ${folders.length} stored visualizations? This action cannot be undone.`, { modal: true }, 'Delete All', 'Cancel');
            if (confirmResult !== 'Delete All') {
                console.log('BROWSE-VISUALIZATIONS: Reset cancelled by user');
                return;
            }
            // Delete each folder
            for (const folder of folders) {
                const folderPath = path.join(this.visualizeDataPath, folder.name);
                console.log(`BROWSE-VISUALIZATIONS: Deleting folder: ${folderPath}`);
                try {
                    await fs.promises.rm(folderPath, { recursive: true, force: true });
                }
                catch (deleteError) {
                    console.error(`BROWSE-VISUALIZATIONS: Error deleting folder ${folderPath}:`, deleteError);
                }
            }
            console.log(`BROWSE-VISUALIZATIONS: Reset completed, deleted ${folders.length} visualizations`);
            vscode.window.showInformationMessage(`Deleted ${folders.length} stored visualizations`);
            // Trigger refresh of the tree view
            vscode.commands.executeCommand('codexr.servers.refresh');
        }
        catch (error) {
            console.error('BROWSE-VISUALIZATIONS: Error during reset:', error);
            vscode.window.showErrorMessage(`Failed to reset visualizations: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Get the visualize-data directory path
     */
    getVisualizeDataPath() {
        return this.visualizeDataPath;
    }
}
exports.VisualizationRestorer = VisualizationRestorer;


/***/ }),
/* 67 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Command Builder
 * Centralized utility for registering VS Code commands with consistent
 * error handling, logging, and lifecycle management.
 *
 * Eliminates repeated try-catch / console.error / showErrorMessage boilerplate
 * across 56+ commands.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CommandBuilder = void 0;
const vscode = __importStar(__webpack_require__(1));
const errorHandler_1 = __webpack_require__(37);
class CommandBuilder {
    /**
     * Register a single command with standardized logging + error handling.
     */
    static register(context, config) {
        const disposable = vscode.commands.registerCommand(config.id, async (...args) => {
            console.log(`${config.module}: ${config.description} command triggered`);
            try {
                await config.handler(...args);
            }
            catch (error) {
                const severity = config.silentErrors ? errorHandler_1.ErrorSeverity.Log : errorHandler_1.ErrorSeverity.User;
                const operation = config.errorMessage ?? `Error in ${config.description}`;
                (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.Command, operation, error, severity);
            }
        });
        context.subscriptions.push(disposable);
        return disposable;
    }
    /**
     * Register multiple commands at once.
     */
    static registerAll(context, configs) {
        const disposables = configs.map(c => this.register(context, c));
        console.log(`COMMAND_BUILDER: Registered ${configs.length} commands`);
        return disposables;
    }
}
exports.CommandBuilder = CommandBuilder;


/***/ }),
/* 68 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * New Code Analysis Commands Registration
 * Command registration for new code analysis module
 * This is the final level of the "nested dolls" architecture
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.registerNewCodeAnalysisCommands = registerNewCodeAnalysisCommands;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisCommands_1 = __webpack_require__(69);
/**
 * Register all new code analysis commands
 * This is the ONLY place where VS Code commands are actually registered
 */
function registerNewCodeAnalysisCommands(context, refreshCallback) {
    console.log('COMMAND_REGISTRATION: Starting New Code Analysis commands registration');
    console.log('COMMAND_REGISTRATION: refreshCallback provided:', refreshCallback !== undefined);
    try {
        // Get all command registrations using the "nested dolls" pattern
        const commandRegistrations = newCodeAnalysisCommands_1.NewCodeAnalysisCommands.getCommandRegistrations(context, refreshCallback);
        console.log(`COMMAND_REGISTRATION: Processing ${commandRegistrations.length} command registrations`);
        // Helper function to safely register commands (avoid duplicates)
        const safeRegisterCommand = (registration) => {
            try {
                const command = vscode.commands.registerCommand(registration.commandId, registration.callback);
                context.subscriptions.push(command);
                console.log(`COMMAND_REGISTRATION: ✓ Registered: ${registration.commandId} - ${registration.description}`);
                return true;
            }
            catch (error) {
                console.warn(`COMMAND_REGISTRATION: ⚠️ Command ${registration.commandId} may already exist:`, error);
                return false;
            }
        };
        // Register all collected commands
        let successCount = 0;
        let failureCount = 0;
        commandRegistrations.forEach(registration => {
            console.log(`COMMAND_REGISTRATION: Attempting to register: ${registration.commandId}`);
            if (safeRegisterCommand(registration)) {
                successCount++;
            }
            else {
                failureCount++;
            }
        });
        console.log(`COMMAND_REGISTRATION: New Code Analysis commands registration complete`);
        console.log(`COMMAND_REGISTRATION: ✓ Successfully registered: ${successCount} commands`);
        if (failureCount > 0) {
            console.log(`COMMAND_REGISTRATION: ⚠️ Failed to register: ${failureCount} commands (may already exist)`);
        }
    }
    catch (error) {
        console.error('COMMAND_REGISTRATION: Error during New Code Analysis commands registration:', error);
        throw error;
    }
}


/***/ }),
/* 69 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * New Code Analysis Commands
 * Command handlers for the new code analysis functionality
 * Implements the "nested dolls" architecture pattern
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.NewCodeAnalysisCommands = void 0;
const subsections_1 = __webpack_require__(70);
const file_analysis_1 = __webpack_require__(134);
const directory_analysis_1 = __webpack_require__(139);
const clean_analysis_1 = __webpack_require__(144);
const dom_visualization_1 = __webpack_require__(146);
const analysisFileMode_1 = __webpack_require__(149);
const analysisDirectoryMode_1 = __webpack_require__(153);
const viewTheme_1 = __webpack_require__(154);
const autoAnalysisDelay_1 = __webpack_require__(155);
const autoAnalysisEnabled_1 = __webpack_require__(156);
const chartTypeFile_1 = __webpack_require__(157);
const chartTypeDirectory_1 = __webpack_require__(158);
const dimensionMappingFile_1 = __webpack_require__(159);
const dimensionMappingDirectory_1 = __webpack_require__(161);
const files_by_language_sorting_1 = __webpack_require__(162);
const profile_configuration_1 = __webpack_require__(164);
const analysisConfigurationStorage_1 = __webpack_require__(87);
/**
 * Main command coordinator for New Code Analysis
 * Follows the "nested dolls" pattern: collects all command registrations
 * without actually registering them - that's done at the top level
 */
class NewCodeAnalysisCommands {
    /**
     * Get all new code analysis command registrations (nested dolls pattern)
     * This collects all commands from subsections without registering them
     */
    static getCommandRegistrations(context, refreshCallback) {
        console.log('NEW_CODE_ANALYSIS: Collecting all command registrations');
        // Create a default refresh callback if none provided
        const defaultRefreshCallback = refreshCallback || (() => {
            console.log('NEW_CODE_ANALYSIS: Default refresh callback executed');
        });
        // Create setting instances
        const tempAnalysisFileSetting = new analysisFileMode_1.AnalysisFileSetting(context);
        const tempAnalysisDirectorySetting = new analysisDirectoryMode_1.AnalysisDirectorySetting(context);
        const tempViewThemeSetting = new viewTheme_1.ViewThemeSetting(context);
        const tempAutoAnalysisDelaySetting = new autoAnalysisDelay_1.AutoAnalysisDelaySetting(context);
        const tempAutoAnalysisEnabledSetting = new autoAnalysisEnabled_1.AutoAnalysisEnabledSetting(context);
        const tempChartTypeFileSetting = new chartTypeFile_1.ChartTypeFileSetting(context);
        const tempChartTypeDirectorySetting = new chartTypeDirectory_1.ChartTypeDirectorySetting(context);
        const tempDimensionMappingFileSetting = new dimensionMappingFile_1.DimensionMappingFileSetting(context);
        const tempDimensionMappingDirectorySetting = new dimensionMappingDirectory_1.DimensionMappingDirectorySetting(context);
        const tempFilesByLanguageSortingSetting = new files_by_language_sorting_1.FilesByLanguageSortingSetting(context);
        // Create profile configuration setting with storage
        const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
        const tempProfileConfigurationSetting = new profile_configuration_1.ProfileConfigurationSetting(context, storage);
        const allCommandRegistrations = [];
        // Collect subsection command registrations
        const analysisSettingsCommands = subsections_1.AnalysisSettingsCommands.getCommandRegistrations(context, tempAnalysisFileSetting, tempAnalysisDirectorySetting, tempViewThemeSetting, tempAutoAnalysisDelaySetting, tempAutoAnalysisEnabledSetting, tempChartTypeFileSetting, tempChartTypeDirectorySetting, tempDimensionMappingFileSetting, tempDimensionMappingDirectorySetting, tempFilesByLanguageSortingSetting, tempProfileConfigurationSetting, defaultRefreshCallback);
        allCommandRegistrations.push(...analysisSettingsCommands);
        const activeAnalysesCommands = subsections_1.ActiveAnalysesCommands.getCommandRegistrations(context, defaultRefreshCallback);
        allCommandRegistrations.push(...activeAnalysesCommands);
        const projectByLanguageCommands = subsections_1.ProjectByLanguageCommands.getCommandRegistrations(context, defaultRefreshCallback);
        allCommandRegistrations.push(...projectByLanguageCommands);
        const filesByLanguageCommands = subsections_1.FilesByLanguageCommands.getCommandRegistrations(context, defaultRefreshCallback);
        allCommandRegistrations.push(...filesByLanguageCommands);
        // Add main section commands
        const mainCommands = NewCodeAnalysisCommands.getMainCommandRegistrations(context, defaultRefreshCallback);
        allCommandRegistrations.push(...mainCommands);
        // Add file analysis commands (these are direct registrations, not command registrations)
        // This now includes both LivePanel and XR commands
        const fileAnalysisCommands = file_analysis_1.FileAnalysisCommands.registerCommands(context);
        // Note: FileAnalysisCommands returns disposables directly, not CommandRegistrations
        // These will be automatically added to the context subscriptions
        // Add directory analysis commands
        const directoryAnalysisCommands = directory_analysis_1.DirectoryAnalysisCommands.registerCommands(context);
        // Note: DirectoryAnalysisCommands returns disposables directly, not CommandRegistrations
        // This includes DirectoryAnalysisLivePanelCommands internally
        // Add project analysis commands
        const projectAnalysisCommands = directory_analysis_1.ProjectAnalysisCommands.registerCommands(context);
        // Note: ProjectAnalysisCommands returns disposables directly, not CommandRegistrations
        // Add clean analysis commands
        const cleanAnalysisCommands = clean_analysis_1.CleanAnalysisCommands.getCommandRegistrations(context, defaultRefreshCallback);
        allCommandRegistrations.push(...cleanAnalysisCommands);
        // Add DOM visualization commands
        const domVisualizationCommands = dom_visualization_1.DOMVisualizationCommands.getCommandRegistrations(context, defaultRefreshCallback);
        allCommandRegistrations.push(...domVisualizationCommands);
        // Execute startup cleanup
        clean_analysis_1.CleanAnalysisCommands.executeStartupCleanup(context);
        console.log(`NEW_CODE_ANALYSIS: Collected total of ${allCommandRegistrations.length} command registrations + file analysis commands`);
        // Log specific commands for debugging
        const dimensionMappingDirectoryCommands = allCommandRegistrations.filter(cmd => cmd.commandId.includes('DimensionMappingDirectory'));
        console.log(`NEW_CODE_ANALYSIS: Found ${dimensionMappingDirectoryCommands.length} dimension mapping directory commands:`, dimensionMappingDirectoryCommands.map(cmd => cmd.commandId));
        return allCommandRegistrations;
    }
    /**
     * Get main section command registrations (not subsection-specific)
     */
    static getMainCommandRegistrations(context, refreshCallback) {
        console.log('NEW_CODE_ANALYSIS: Collecting main command registrations');
        // TODO: Add main new code analysis commands
        const mainCommands = [
        // Example main commands:
        // {
        //     commandId: 'newCodeAnalysis.refresh',
        //     callback: refreshCallback,
        //     description: 'Refresh New Code Analysis tree view'
        // }
        ];
        console.log(`NEW_CODE_ANALYSIS: Collected ${mainCommands.length} main command registrations`);
        return mainCommands;
    }
}
exports.NewCodeAnalysisCommands = NewCodeAnalysisCommands;


/***/ }),
/* 70 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * New Code Analysis Subsection Commands
 * Exports for all subsection command modules
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FilesByLanguageCommands = exports.ProjectByLanguageCommands = exports.ActiveAnalysesCommands = exports.AnalysisSettingsCommands = void 0;
var analysisSettingsCommands_1 = __webpack_require__(71);
Object.defineProperty(exports, "AnalysisSettingsCommands", ({ enumerable: true, get: function () { return analysisSettingsCommands_1.AnalysisSettingsCommands; } }));
var activeAnalysesCommands_1 = __webpack_require__(91);
Object.defineProperty(exports, "ActiveAnalysesCommands", ({ enumerable: true, get: function () { return activeAnalysesCommands_1.ActiveAnalysesCommands; } }));
var projectByLanguageCommands_1 = __webpack_require__(116);
Object.defineProperty(exports, "ProjectByLanguageCommands", ({ enumerable: true, get: function () { return projectByLanguageCommands_1.ProjectByLanguageCommands; } }));
var filesByLanguageCommands_1 = __webpack_require__(133);
Object.defineProperty(exports, "FilesByLanguageCommands", ({ enumerable: true, get: function () { return filesByLanguageCommands_1.FilesByLanguageCommands; } }));


/***/ }),
/* 71 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Analysis Settings Commands Coordinator
 * Coordinates all commands for the Analysis Settings subsection
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisSettingsCommands = void 0;
const analysis_file_mode_1 = __webpack_require__(72);
const analysis_directory_mode_1 = __webpack_require__(74);
const view_theme_1 = __webpack_require__(76);
const auto_analysis_delay_1 = __webpack_require__(78);
const auto_analysis_enabled_1 = __webpack_require__(80);
const chartTypeFileCommands_1 = __webpack_require__(82);
const chartTypeDirectoryCommands_1 = __webpack_require__(83);
const dimensionMappingFileCommands_1 = __webpack_require__(84);
const dimensionMappingDirectoryCommands_1 = __webpack_require__(85);
const filesByLanguageSortingCommands_1 = __webpack_require__(86);
const profile_configuration_1 = __webpack_require__(89);
class AnalysisSettingsCommands {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Get all analysis settings command registrations (nested dolls pattern)
     * Collects commands from all sub-components without registering them
     */
    static getCommandRegistrations(context, analysisFileSetting, analysisDirectorySetting, viewThemeSetting, autoAnalysisDelaySetting, autoAnalysisEnabledSetting, chartTypeFileSetting, chartTypeDirectorySetting, dimensionMappingFileSetting, dimensionMappingDirectorySetting, filesByLanguageSortingSetting, profileConfigurationSetting, refreshCallback) {
        console.log('NEW_CODE_ANALYSIS: Collecting Analysis Settings command registrations');
        const commandRegistrations = [];
        // Collect analysis file mode command registrations
        const fileModeCommands = analysis_file_mode_1.AnalysisFileModeCommands.getCommandRegistrations(context, analysisFileSetting, refreshCallback);
        commandRegistrations.push(...fileModeCommands);
        // Collect analysis directory mode command registrations
        const directoryModeCommands = analysis_directory_mode_1.AnalysisDirectoryModeCommands.getCommandRegistrations(context, analysisDirectorySetting, refreshCallback);
        commandRegistrations.push(...directoryModeCommands);
        // Collect view theme command registrations
        const viewThemeCommands = view_theme_1.ViewThemeCommands.getCommandRegistrations(context, viewThemeSetting, refreshCallback);
        commandRegistrations.push(...viewThemeCommands);
        // Collect chart type file command registrations
        const chartTypeFileCommands = chartTypeFileCommands_1.ChartTypeFileCommands.getCommandRegistrations(context, chartTypeFileSetting, refreshCallback);
        commandRegistrations.push(...chartTypeFileCommands);
        // Collect chart type directory command registrations
        const chartTypeDirectoryCommands = chartTypeDirectoryCommands_1.ChartTypeDirectoryCommands.getCommandRegistrations(context, chartTypeDirectorySetting, refreshCallback);
        commandRegistrations.push(...chartTypeDirectoryCommands);
        // Collect dimension mapping file command registrations
        const dimensionMappingFileCommands = dimensionMappingFileCommands_1.DimensionMappingFileCommands.getCommandRegistrations(context, dimensionMappingFileSetting, refreshCallback);
        commandRegistrations.push(...dimensionMappingFileCommands);
        // Collect dimension mapping directory command registrations
        const dimensionMappingDirectoryCommands = dimensionMappingDirectoryCommands_1.DimensionMappingDirectoryCommands.getCommandRegistrations(context, dimensionMappingDirectorySetting, refreshCallback);
        console.log(`ANALYSIS_SETTINGS_COMMANDS: Got ${dimensionMappingDirectoryCommands.length} dimension mapping directory commands`);
        commandRegistrations.push(...dimensionMappingDirectoryCommands);
        // Collect auto-analysis delay command registrations
        const autoAnalysisDelayCommands = auto_analysis_delay_1.AutoAnalysisDelayCommands.getCommandRegistrations(context, autoAnalysisDelaySetting, refreshCallback);
        commandRegistrations.push(...autoAnalysisDelayCommands);
        // Collect auto-analysis enabled command registrations
        const autoAnalysisEnabledCommands = auto_analysis_enabled_1.AutoAnalysisEnabledCommands.getCommandRegistrations(context, autoAnalysisEnabledSetting, refreshCallback);
        commandRegistrations.push(...autoAnalysisEnabledCommands);
        // Collect files by language sorting command registrations
        const filesByLanguageSortingCommands = filesByLanguageSortingCommands_1.FilesByLanguageSortingCommands.getCommandRegistrations(context, filesByLanguageSortingSetting, refreshCallback);
        commandRegistrations.push(...filesByLanguageSortingCommands);
        // Collect profile configuration command registrations
        const profileConfigurationCommands = profile_configuration_1.ProfileConfigurationCommands.getCommandRegistrations(context, profileConfigurationSetting, refreshCallback);
        commandRegistrations.push(...profileConfigurationCommands);
        console.log(`NEW_CODE_ANALYSIS: Collected ${commandRegistrations.length} Analysis Settings command registrations`);
        return commandRegistrations;
    }
}
exports.AnalysisSettingsCommands = AnalysisSettingsCommands;


/***/ }),
/* 72 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Analysis File Mode Commands Module
 * Exports for analysis file mode commands
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisFileModeCommands = void 0;
var analysisFileModeCommands_1 = __webpack_require__(73);
Object.defineProperty(exports, "AnalysisFileModeCommands", ({ enumerable: true, get: function () { return analysisFileModeCommands_1.AnalysisFileModeCommands; } }));


/***/ }),
/* 73 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Analysis File Mode Commands
 * Commands specific to the Analysis File Mode setting
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisFileModeCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
class AnalysisFileModeCommands {
    context;
    analysisFileSetting;
    constructor(context, analysisFileSetting) {
        this.context = context;
        this.analysisFileSetting = analysisFileSetting;
    }
    /**
     * Get command registrations (don't register them yet)
     * This follows the "nested dolls" architecture pattern
     */
    static getCommandRegistrations(context, analysisFileSetting, refreshCallback) {
        const commands = new AnalysisFileModeCommands(context, analysisFileSetting);
        return [
            {
                commandId: 'newCodeAnalysis.toggleAnalysisFileMode',
                callback: () => commands.toggleAnalysisFileMode(refreshCallback),
                description: 'Toggle Analysis File Mode between XR and LivePanel'
            },
            {
                commandId: 'newCodeAnalysis.setAnalysisFileModeXR',
                callback: () => commands.setAnalysisFileMode('XR', refreshCallback),
                description: 'Set Analysis File Mode to XR'
            },
            {
                commandId: 'newCodeAnalysis.setAnalysisFileModeHivePanel',
                callback: () => commands.setAnalysisFileMode('LivePanel', refreshCallback),
                description: 'Set Analysis File Mode to LivePanel'
            }
        ];
    }
    /**
     * Toggle analysis file mode command handler
     */
    async toggleAnalysisFileMode(refreshCallback) {
        try {
            const newMode = await this.analysisFileSetting.toggleMode();
            // Show notification to user
            vscode.window.showInformationMessage(`Analysis File mode switched to: ${newMode}`, { modal: false });
            // Refresh the tree view to show updated state
            refreshCallback();
            console.log(`NEW_CODE_ANALYSIS: Command executed - Analysis file mode: ${newMode}`);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error toggling analysis file mode:', error);
            vscode.window.showErrorMessage(`Failed to toggle analysis file mode: ${error}`);
        }
    }
    /**
     * Set specific analysis file mode command handler
     */
    async setAnalysisFileMode(mode, refreshCallback) {
        try {
            await this.analysisFileSetting.setMode(mode);
            // Show notification to user
            vscode.window.showInformationMessage(`Analysis File mode set to: ${mode}`, { modal: false });
            // Refresh the tree view to show updated state
            refreshCallback();
            console.log(`NEW_CODE_ANALYSIS: Command executed - Analysis file mode set to: ${mode}`);
        }
        catch (error) {
            console.error(`NEW_CODE_ANALYSIS: Error setting analysis file mode to ${mode}:`, error);
            vscode.window.showErrorMessage(`Failed to set analysis file mode to ${mode}: ${error}`);
        }
    }
}
exports.AnalysisFileModeCommands = AnalysisFileModeCommands;


/***/ }),
/* 74 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Analysis Directory Mode Commands Module
 * Exports for analysis directory mode commands
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisDirectoryModeCommands = void 0;
var analysisDirectoryModeCommands_1 = __webpack_require__(75);
Object.defineProperty(exports, "AnalysisDirectoryModeCommands", ({ enumerable: true, get: function () { return analysisDirectoryModeCommands_1.AnalysisDirectoryModeCommands; } }));


/***/ }),
/* 75 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Analysis Directory Mode Commands
 * Command handlers for the analysis directory mode setting
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisDirectoryModeCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
class AnalysisDirectoryModeCommands {
    /**
     * Get command registrations for analysis directory mode (nested dolls pattern)
     */
    static getCommandRegistrations(context, analysisDirectorySetting, refreshCallback) {
        console.log('NEW_CODE_ANALYSIS: Collecting Analysis Directory Mode command registrations');
        const commandRegistrations = [
            {
                commandId: 'newCodeAnalysis.selectAnalysisDirectoryMode',
                callback: async () => {
                    try {
                        console.log('NEW_CODE_ANALYSIS: Select Analysis Directory Mode command executed');
                        const selectedMode = await analysisDirectorySetting.selectMode();
                        if (selectedMode) {
                            const modeText = selectedMode.includes('Deep')
                                ? `${selectedMode.replace('Deep', '')} Deep`
                                : selectedMode;
                            vscode.window.showInformationMessage(`Directory analysis mode set to: ${modeText}`, 'OK');
                            // Refresh tree to show updated state
                            refreshCallback();
                        }
                    }
                    catch (error) {
                        console.error('NEW_CODE_ANALYSIS: Error selecting analysis directory mode:', error);
                        vscode.window.showErrorMessage(`Failed to select directory analysis mode: ${error instanceof Error ? error.message : String(error)}`);
                    }
                },
                description: 'Select between LivePanel, LivePanelDeep, XR, and XRDeep modes for directory analysis'
            }
        ];
        console.log(`NEW_CODE_ANALYSIS: Collected ${commandRegistrations.length} Analysis Directory Mode command registrations`);
        return commandRegistrations;
    }
}
exports.AnalysisDirectoryModeCommands = AnalysisDirectoryModeCommands;


/***/ }),
/* 76 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * View Theme Commands Module
 * Exports for view theme commands
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ViewThemeCommands = void 0;
var viewThemeCommands_1 = __webpack_require__(77);
Object.defineProperty(exports, "ViewThemeCommands", ({ enumerable: true, get: function () { return viewThemeCommands_1.ViewThemeCommands; } }));


/***/ }),
/* 77 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * View Theme Commands
 * Commands specific to the View Theme setting
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ViewThemeCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
class ViewThemeCommands {
    context;
    viewThemeSetting;
    constructor(context, viewThemeSetting) {
        this.context = context;
        this.viewThemeSetting = viewThemeSetting;
    }
    /**
     * Get command registrations (don't register them yet)
     * This follows the "nested dolls" architecture pattern
     */
    static getCommandRegistrations(context, viewThemeSetting, refreshCallback) {
        const commands = new ViewThemeCommands(context, viewThemeSetting);
        return [
            {
                commandId: 'newCodeAnalysis.toggleViewTheme',
                callback: () => commands.toggleViewTheme(refreshCallback),
                description: 'Toggle View Theme between Dark and Light'
            },
            {
                commandId: 'newCodeAnalysis.setViewThemeDark',
                callback: () => commands.setViewTheme('Dark', refreshCallback),
                description: 'Set View Theme to Dark'
            },
            {
                commandId: 'newCodeAnalysis.setViewThemeLight',
                callback: () => commands.setViewTheme('Light', refreshCallback),
                description: 'Set View Theme to Light'
            }
        ];
    }
    /**
     * Toggle view theme command handler
     */
    async toggleViewTheme(refreshCallback) {
        try {
            console.log('VIEW_THEME_COMMAND: Starting toggle view theme command');
            const newTheme = await this.viewThemeSetting.toggleTheme();
            // Show notification to user
            vscode.window.showInformationMessage(`View theme switched to: ${newTheme}`, { modal: false });
            console.log('VIEW_THEME_COMMAND: About to call refreshCallback');
            // Refresh the tree view to show updated state
            refreshCallback();
            console.log('VIEW_THEME_COMMAND: refreshCallback executed');
            console.log(`NEW_CODE_ANALYSIS: Command executed - View theme: ${newTheme}`);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error toggling view theme:', error);
            vscode.window.showErrorMessage(`Failed to toggle view theme: ${error}`);
        }
    }
    /**
     * Set specific view theme command handler
     */
    async setViewTheme(theme, refreshCallback) {
        try {
            await this.viewThemeSetting.setTheme(theme);
            // Show notification to user
            vscode.window.showInformationMessage(`View theme set to: ${theme}`, { modal: false });
            // Refresh the tree view to show updated state
            refreshCallback();
            console.log(`NEW_CODE_ANALYSIS: Command executed - View theme set to: ${theme}`);
        }
        catch (error) {
            console.error(`NEW_CODE_ANALYSIS: Error setting view theme to ${theme}:`, error);
            vscode.window.showErrorMessage(`Failed to set view theme to ${theme}: ${error}`);
        }
    }
}
exports.ViewThemeCommands = ViewThemeCommands;


/***/ }),
/* 78 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Auto-Analysis Delay Commands Module
 * Exports for auto-analysis delay commands
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AutoAnalysisDelayCommands = void 0;
var autoAnalysisDelayCommands_1 = __webpack_require__(79);
Object.defineProperty(exports, "AutoAnalysisDelayCommands", ({ enumerable: true, get: function () { return autoAnalysisDelayCommands_1.AutoAnalysisDelayCommands; } }));


/***/ }),
/* 79 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Auto-Analysis Delay Commands
 * Commands specific to the Auto-Analysis Delay setting
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AutoAnalysisDelayCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
class AutoAnalysisDelayCommands {
    context;
    autoAnalysisDelaySetting;
    constructor(context, autoAnalysisDelaySetting) {
        this.context = context;
        this.autoAnalysisDelaySetting = autoAnalysisDelaySetting;
    }
    /**
     * Get command registrations (don't register them yet)
     * This follows the "nested dolls" architecture pattern
     */
    static getCommandRegistrations(context, autoAnalysisDelaySetting, refreshCallback) {
        const commands = new AutoAnalysisDelayCommands(context, autoAnalysisDelaySetting);
        return [
            {
                commandId: 'newCodeAnalysis.selectAutoAnalysisDelay',
                callback: () => commands.selectAutoAnalysisDelay(refreshCallback),
                description: 'Select Auto-Analysis Delay from options'
            },
            {
                commandId: 'newCodeAnalysis.setAutoAnalysisDelayRealTime',
                callback: () => commands.setAutoAnalysisDelay('RealTime', refreshCallback),
                description: 'Set Auto-Analysis Delay to Real Time (0s)'
            },
            {
                commandId: 'newCodeAnalysis.setAutoAnalysisDelay1s',
                callback: () => commands.setAutoAnalysisDelay('1s', refreshCallback),
                description: 'Set Auto-Analysis Delay to 1 second'
            },
            {
                commandId: 'newCodeAnalysis.setAutoAnalysisDelay3s',
                callback: () => commands.setAutoAnalysisDelay('3s', refreshCallback),
                description: 'Set Auto-Analysis Delay to 3 seconds'
            },
            {
                commandId: 'newCodeAnalysis.setAutoAnalysisDelay5s',
                callback: () => commands.setAutoAnalysisDelay('5s', refreshCallback),
                description: 'Set Auto-Analysis Delay to 5 seconds'
            },
            {
                commandId: 'newCodeAnalysis.setAutoAnalysisDelay10s',
                callback: () => commands.setAutoAnalysisDelay('10s', refreshCallback),
                description: 'Set Auto-Analysis Delay to 10 seconds'
            }
        ];
    }
    /**
     * Select auto-analysis delay from QuickPick command handler
     */
    async selectAutoAnalysisDelay(refreshCallback) {
        try {
            // Create QuickPick items for predefined options
            const predefinedItems = [
                {
                    label: '⚡ Real Time (0s)',
                    description: 'Instant analysis as you type',
                    detail: 'No delay - immediate response',
                    value: 'RealTime'
                },
                {
                    label: '🕐 1 Second',
                    description: 'Short delay for quick feedback',
                    detail: '1000ms delay',
                    value: '1s'
                },
                {
                    label: '🕒 3 Seconds',
                    description: 'Balanced delay for most users',
                    detail: '3000ms delay',
                    value: '3s'
                },
                {
                    label: '🕔 5 Seconds',
                    description: 'Longer delay for complex analysis',
                    detail: '5000ms delay',
                    value: '5s'
                },
                {
                    label: '🕙 10 Seconds',
                    description: 'Maximum predefined delay',
                    detail: '10000ms delay',
                    value: '10s'
                },
                {
                    label: '⚙️ Custom Value...',
                    description: 'Set your own delay (0-20000ms)',
                    detail: 'Enter custom milliseconds',
                    value: 'Custom'
                }
            ];
            const selectedItem = await vscode.window.showQuickPick(predefinedItems, {
                placeHolder: 'Select auto-analysis delay timing',
                title: 'Auto-Analysis Delay Configuration',
                matchOnDescription: true,
                matchOnDetail: true
            });
            if (!selectedItem) {
                return; // User cancelled
            }
            if (selectedItem.value === 'Custom') {
                // Handle custom input
                await this.setCustomAutoAnalysisDelay(refreshCallback);
            }
            else {
                // Handle predefined value
                await this.setAutoAnalysisDelay(selectedItem.value, refreshCallback);
            }
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error selecting auto-analysis delay:', error);
            vscode.window.showErrorMessage(`Failed to select auto-analysis delay: ${error}`);
        }
    }
    /**
     * Set custom auto-analysis delay command handler
     */
    async setCustomAutoAnalysisDelay(refreshCallback) {
        try {
            // Show input box for custom delay
            const input = await vscode.window.showInputBox({
                prompt: 'Enter custom auto-analysis delay in milliseconds (0-20000)',
                placeHolder: '1000',
                validateInput: (value) => {
                    const num = parseInt(value);
                    if (isNaN(num)) {
                        return 'Please enter a valid number';
                    }
                    if (num < 0 || num > 20000) {
                        return 'Delay must be between 0 and 20000 milliseconds (20 seconds)';
                    }
                    return null;
                }
            });
            if (input === undefined) {
                return; // User cancelled
            }
            const customMs = parseInt(input);
            const newDelayConfig = await this.autoAnalysisDelaySetting.setCustomDelay(customMs);
            // Show notification to user
            vscode.window.showInformationMessage(`Auto-analysis delay set to custom: ${customMs}ms`, { modal: false });
            // Refresh the tree view to show updated state
            refreshCallback();
            console.log(`NEW_CODE_ANALYSIS: Command executed - Custom auto-analysis delay: ${customMs}ms`);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error setting custom auto-analysis delay:', error);
            vscode.window.showErrorMessage(`Failed to set custom auto-analysis delay: ${error}`);
        }
    }
    /**
     * Set specific auto-analysis delay command handler
     */
    async setAutoAnalysisDelay(delayType, refreshCallback) {
        try {
            await this.autoAnalysisDelaySetting.setDelay(delayType);
            // Show notification to user
            vscode.window.showInformationMessage(`Auto-analysis delay set to: ${delayType}`, { modal: false });
            // Refresh the tree view to show updated state
            refreshCallback();
            console.log(`NEW_CODE_ANALYSIS: Command executed - Auto-analysis delay set to: ${delayType}`);
        }
        catch (error) {
            console.error(`NEW_CODE_ANALYSIS: Error setting auto-analysis delay to ${delayType}:`, error);
            vscode.window.showErrorMessage(`Failed to set auto-analysis delay to ${delayType}: ${error}`);
        }
    }
}
exports.AutoAnalysisDelayCommands = AutoAnalysisDelayCommands;


/***/ }),
/* 80 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Auto-Analysis Enabled Commands Module
 * Exports for auto-analysis enabled commands
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AutoAnalysisEnabledCommands = void 0;
var autoAnalysisEnabledCommands_1 = __webpack_require__(81);
Object.defineProperty(exports, "AutoAnalysisEnabledCommands", ({ enumerable: true, get: function () { return autoAnalysisEnabledCommands_1.AutoAnalysisEnabledCommands; } }));


/***/ }),
/* 81 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Auto-Analysis Enabled Commands
 * Commands specific to the Auto-Analysis Enabled setting
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AutoAnalysisEnabledCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
class AutoAnalysisEnabledCommands {
    context;
    autoAnalysisEnabledSetting;
    constructor(context, autoAnalysisEnabledSetting) {
        this.context = context;
        this.autoAnalysisEnabledSetting = autoAnalysisEnabledSetting;
    }
    /**
     * Get command registrations (don't register them yet)
     * This follows the "nested dolls" architecture pattern
     */
    static getCommandRegistrations(context, autoAnalysisEnabledSetting, refreshCallback) {
        const commands = new AutoAnalysisEnabledCommands(context, autoAnalysisEnabledSetting);
        return [
            {
                commandId: 'newCodeAnalysis.toggleAutoAnalysisEnabled',
                callback: () => commands.toggleAutoAnalysisEnabled(refreshCallback),
                description: 'Toggle Auto-Analysis Enabled/Disabled state'
            },
            {
                commandId: 'newCodeAnalysis.enableAutoAnalysis',
                callback: () => commands.setAutoAnalysisEnabled(true, refreshCallback),
                description: 'Enable Auto-Analysis (watchers and debounce time)'
            },
            {
                commandId: 'newCodeAnalysis.disableAutoAnalysis',
                callback: () => commands.setAutoAnalysisEnabled(false, refreshCallback),
                description: 'Disable Auto-Analysis (pause watchers and debounce time)'
            }
        ];
    }
    /**
     * Toggle auto-analysis enabled state command handler
     */
    async toggleAutoAnalysisEnabled(refreshCallback) {
        try {
            console.log('NEW_CODE_ANALYSIS: Toggling auto-analysis enabled state');
            const newState = await this.autoAnalysisEnabledSetting.toggleEnabled();
            console.log(`NEW_CODE_ANALYSIS: Auto-analysis enabled toggled to: ${newState}`);
            // Refresh the tree view to show the new state
            refreshCallback();
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error toggling auto-analysis enabled state:', error);
            vscode.window.showErrorMessage(`Failed to toggle auto-analysis state: ${error}`);
        }
    }
    /**
     * Set specific auto-analysis enabled state command handler
     */
    async setAutoAnalysisEnabled(enabled, refreshCallback) {
        try {
            console.log(`NEW_CODE_ANALYSIS: Setting auto-analysis enabled to: ${enabled}`);
            await this.autoAnalysisEnabledSetting.setEnabled(enabled);
            console.log(`NEW_CODE_ANALYSIS: Auto-analysis enabled set to: ${enabled}`);
            // Refresh the tree view to show the new state
            refreshCallback();
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error setting auto-analysis enabled state:', error);
            vscode.window.showErrorMessage(`Failed to set auto-analysis state: ${error}`);
        }
    }
}
exports.AutoAnalysisEnabledCommands = AutoAnalysisEnabledCommands;


/***/ }),
/* 82 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Chart Type File Commands
 * Commands specific to the Chart Type (File) setting
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ChartTypeFileCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
class ChartTypeFileCommands {
    context;
    chartTypeFileSetting;
    constructor(context, chartTypeFileSetting) {
        this.context = context;
        this.chartTypeFileSetting = chartTypeFileSetting;
    }
    /**
     * Get command registrations (don't register them yet)
     * This follows the "nested dolls" architecture pattern
     */
    static getCommandRegistrations(context, chartTypeFileSetting, refreshCallback) {
        const commands = new ChartTypeFileCommands(context, chartTypeFileSetting);
        return [
            {
                commandId: 'newCodeAnalysis.selectChartTypeFile',
                callback: async () => {
                    await commands.selectChartType();
                    refreshCallback();
                },
                description: 'Select chart type for file analysis visualization'
            }
        ];
    }
    /**
     * Show chart type selection from quick pick menu
     * Shows different chart types available for XR and LivePanel modes
     */
    async selectChartType() {
        console.log('CHART_TYPE_FILE_COMMANDS: Chart type selection command executed');
        try {
            // Use the chart type setting's built-in selection dialog
            await this.chartTypeFileSetting.showChartTypeSelection();
        }
        catch (error) {
            console.error('CHART_TYPE_FILE_COMMANDS: Error selecting chart type:', error);
            vscode.window.showErrorMessage(`Failed to select chart type: ${error}`);
        }
    }
}
exports.ChartTypeFileCommands = ChartTypeFileCommands;


/***/ }),
/* 83 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Chart Type Directory Commands
 * Commands specific to the Chart Type (Directory) setting
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ChartTypeDirectoryCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
class ChartTypeDirectoryCommands {
    context;
    chartTypeDirectorySetting;
    constructor(context, chartTypeDirectorySetting) {
        this.context = context;
        this.chartTypeDirectorySetting = chartTypeDirectorySetting;
    }
    /**
     * Get command registrations (don't register them yet)
     * This follows the "nested dolls" architecture pattern
     */
    static getCommandRegistrations(context, chartTypeDirectorySetting, refreshCallback) {
        const commands = new ChartTypeDirectoryCommands(context, chartTypeDirectorySetting);
        return [
            {
                commandId: 'newCodeAnalysis.selectChartTypeDirectory',
                callback: async () => {
                    await commands.selectChartType();
                    refreshCallback();
                },
                description: 'Select chart type for directory analysis visualization'
            }
        ];
    }
    /**
     * Show chart type selection from quick pick menu
     * Shows different chart types available for XR and LivePanel modes
     */
    async selectChartType() {
        console.log('CHART_TYPE_DIRECTORY_COMMANDS: Chart type selection command executed');
        try {
            // Use the chart type setting's built-in selection dialog
            await this.chartTypeDirectorySetting.showChartTypeSelection();
        }
        catch (error) {
            console.error('CHART_TYPE_DIRECTORY_COMMANDS: Error in chart type selection:', error);
            vscode.window.showErrorMessage(`Failed to select chart type: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
}
exports.ChartTypeDirectoryCommands = ChartTypeDirectoryCommands;


/***/ }),
/* 84 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Dimension Mapping File Commands
 * Commands specific to the Dimension Mapping (File) setting with real BabiaXR dimension mapping
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DimensionMappingFileCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
class DimensionMappingFileCommands {
    context;
    dimensionMappingSetting;
    constructor(context, dimensionMappingSetting) {
        this.context = context;
        this.dimensionMappingSetting = dimensionMappingSetting;
    }
    /**
     * Get command registrations (don't register them yet)
     * This follows the "nested dolls" architecture pattern
     */
    static getCommandRegistrations(context, dimensionMappingSetting, refreshCallback) {
        const commands = new DimensionMappingFileCommands(context, dimensionMappingSetting);
        return [
            {
                commandId: 'newCodeAnalysis.selectDimensionMappingFile',
                callback: async (dimension) => {
                    await commands.selectDimensionMapping(dimension);
                    refreshCallback();
                },
                description: 'Select dimension mapping for a specific chart dimension'
            }
        ];
    }
    /**
     * Select dimension mapping for a specific dimension
     */
    async selectDimensionMapping(dimension) {
        try {
            console.log(`DIMENSION_MAPPING_FILE_COMMANDS: Selecting dimension mapping for dimension: "${dimension.name}"`);
            console.log(`DIMENSION_MAPPING_FILE_COMMANDS: Dimension details - Label: "${dimension.label}", DataType: "${dimension.dataType}", Required: ${dimension.required}`);
            // Get available fields for this dimension type using the filtering logic
            const availableFields = this.dimensionMappingSetting.getAvailableFieldsForDimension(dimension);
            if (availableFields.length === 0) {
                vscode.window.showWarningMessage(`No available fields for dimension: ${dimension.label} (requires ${dimension.dataType} fields)`);
                return;
            }
            console.log(`DIMENSION_MAPPING_FILE_COMMANDS: Available fields for "${dimension.name}":`, availableFields);
            // Create quick pick items with detailed descriptions
            const quickPickItems = availableFields.map(field => ({
                label: field,
                description: this.getFieldDescription(field),
                detail: this.getFieldTypeDescription(field, dimension)
            }));
            // Add "Clear mapping" option
            quickPickItems.unshift({
                label: '$(clear-all) Clear mapping',
                description: 'Remove the mapping for this dimension',
                detail: dimension.required ? 'Note: This is a required dimension' : 'This optional dimension will not be mapped to any data field'
            });
            // Show validation info in the placeholder
            const typeInfo = dimension.dataType === 'numeric' ? 'numeric fields only' : 'any field type';
            const requiredInfo = dimension.required ? 'REQUIRED' : 'optional';
            const selection = await vscode.window.showQuickPick(quickPickItems, {
                title: `Map ${dimension.label}`,
                placeHolder: `Select a data field for ${dimension.label} (${typeInfo}, ${requiredInfo})`,
                ignoreFocusOut: true,
                matchOnDescription: true,
                matchOnDetail: true
            });
            if (!selection) {
                console.log(`DIMENSION_MAPPING_FILE_COMMANDS: User cancelled dimension mapping for "${dimension.name}"`);
                return; // User cancelled
            }
            if (selection.label.includes('Clear mapping')) {
                console.log(`DIMENSION_MAPPING_FILE_COMMANDS: Clearing mapping for dimension "${dimension.name}"`);
                await this.dimensionMappingSetting.clearDimensionMapping(dimension.name);
                vscode.window.showInformationMessage(`Cleared mapping for ${dimension.label}`);
            }
            else {
                console.log(`DIMENSION_MAPPING_FILE_COMMANDS: Mapping dimension "${dimension.name}" to field "${selection.label}"`);
                // Check for conflicts with other dimensions before setting
                const currentMappings = await this.dimensionMappingSetting.getCurrentMappings();
                const conflictDimension = Object.keys(currentMappings).find(key => key !== dimension.name && currentMappings[key] === selection.label);
                if (conflictDimension) {
                    const shouldProceed = await vscode.window.showWarningMessage(`The field "${selection.label}" is already mapped to another dimension. Do you want to proceed? This will clear the previous mapping.`, 'Proceed', 'Cancel');
                    if (shouldProceed !== 'Proceed') {
                        console.log('DIMENSION_MAPPING_FILE_COMMANDS: Mapping cancelled due to conflict');
                        return;
                    }
                    // Clear the conflicting mapping
                    await this.dimensionMappingSetting.clearDimensionMapping(conflictDimension);
                    console.log(`DIMENSION_MAPPING_FILE_COMMANDS: Cleared conflicting mapping for dimension "${conflictDimension}"`);
                }
                // Set the mapping using persistent storage
                await this.dimensionMappingSetting.setDimensionMapping(dimension.name, selection.label);
                vscode.window.showInformationMessage(`Mapped ${dimension.label} → ${selection.label}`);
            }
            console.log(`DIMENSION_MAPPING_FILE_COMMANDS: Dimension mapping operation completed for "${dimension.name}"`);
        }
        catch (error) {
            console.error('DIMENSION_MAPPING_FILE_COMMANDS: Error selecting dimension mapping:', error);
            vscode.window.showErrorMessage(`Failed to select dimension mapping: ${error}`);
        }
    }
    /**
     * Get detailed description for a data field (real analysis data fields)
     */
    getFieldDescription(field) {
        const descriptions = {
            'functionName': 'Unique identifier name of the function or method',
            'parameters': 'Total count of input parameters in the function signature',
            'lineCount': 'Total number of source code lines within the function body',
            'complexity': 'Cyclomatic Complexity Number - measure of code complexity based on decision points',
            'cyclomaticDensity': 'Cyclomatic Complexity Density ratio (complexity divided by line count)'
        };
        return descriptions[field] || 'Analysis data field';
    }
    /**
     * Get type compatibility description (removed compatibility info, now just empty)
     */
    getFieldTypeDescription(field, dimension) {
        // No type compatibility description in detail - that's now shown in dimension description
        return '';
    }
}
exports.DimensionMappingFileCommands = DimensionMappingFileCommands;


/***/ }),
/* 85 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Dimension Mapping Directory Commands
 * Commands specific to the Dimension Mapping (Directory) setting
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DimensionMappingDirectoryCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
class DimensionMappingDirectoryCommands {
    context;
    dimensionMappingDirectorySetting;
    constructor(context, dimensionMappingDirectorySetting) {
        this.context = context;
        this.dimensionMappingDirectorySetting = dimensionMappingDirectorySetting;
    }
    /**
     * Get command registrations (don't register them yet)
     * This follows the "nested dolls" architecture pattern
     */
    static getCommandRegistrations(context, dimensionMappingDirectorySetting, refreshCallback) {
        console.log('DIMENSION_MAPPING_DIRECTORY_COMMANDS: Creating command registrations');
        const commands = new DimensionMappingDirectoryCommands(context, dimensionMappingDirectorySetting);
        const registrations = [
            {
                commandId: 'newCodeAnalysis.selectDimensionMappingDirectory',
                callback: async (dimensionName, dimensionLabel) => {
                    console.log(`DIMENSION_MAPPING_DIRECTORY_COMMANDS: Command TRIGGERED for ${dimensionName} (${dimensionLabel})`);
                    await commands.selectDimensionMapping(dimensionName, dimensionLabel);
                    refreshCallback();
                },
                description: 'Select dimension mapping for directory analysis visualization'
            }
        ];
        console.log(`DIMENSION_MAPPING_DIRECTORY_COMMANDS: Created ${registrations.length} command registrations`);
        return registrations;
    }
    /**
     * Show dimension mapping selection from quick pick menu
     */
    async selectDimensionMapping(dimensionName, dimensionLabel) {
        console.log(`DIMENSION_MAPPING_DIRECTORY_COMMANDS: Dimension mapping selection command executed for ${dimensionName} (${dimensionLabel})`);
        try {
            // Add safety check
            if (!this.dimensionMappingDirectorySetting) {
                console.error('DIMENSION_MAPPING_DIRECTORY_COMMANDS: dimensionMappingDirectorySetting is not initialized');
                vscode.window.showErrorMessage('Dimension mapping setting not initialized. Please reload the window.');
                return;
            }
            // Use the dimension mapping setting's built-in selection dialog
            await this.dimensionMappingDirectorySetting.showDimensionMappingSelection(dimensionName, dimensionLabel);
        }
        catch (error) {
            console.error(`DIMENSION_MAPPING_DIRECTORY_COMMANDS: Error in selectDimensionMapping:`, error);
            vscode.window.showErrorMessage(`Failed to show dimension mapping selection: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
}
exports.DimensionMappingDirectoryCommands = DimensionMappingDirectoryCommands;


/***/ }),
/* 86 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Files by Language Sorting Commands
 * Commands for configuring Files by Language sorting
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FilesByLanguageSortingCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const analysisConfigurationStorage_1 = __webpack_require__(87);
class FilesByLanguageSortingCommands {
    /**
     * Get Files by Language Sorting command registrations
     */
    static getCommandRegistrations(context, filesByLanguageSortingSetting, refreshCallback) {
        console.log('NEW_CODE_ANALYSIS: Collecting Files by Language Sorting command registrations');
        const commandRegistrations = [
            // Language Group Primary Sorting
            {
                commandId: 'newCodeAnalysis.selectLanguageGroupPrimarySorting',
                callback: async () => {
                    await FilesByLanguageSortingCommands.selectLanguageGroupPrimarySorting(context, filesByLanguageSortingSetting, refreshCallback);
                },
                description: 'Select primary sorting for language groups'
            },
            // Language Group Secondary Sorting
            {
                commandId: 'newCodeAnalysis.selectLanguageGroupSecondarySorting',
                callback: async () => {
                    await FilesByLanguageSortingCommands.selectLanguageGroupSecondarySorting(context, filesByLanguageSortingSetting, refreshCallback);
                },
                description: 'Select secondary sorting for language groups'
            },
            // Files Sorting
            {
                commandId: 'newCodeAnalysis.selectFilesSorting',
                callback: async () => {
                    await FilesByLanguageSortingCommands.selectFilesSorting(filesByLanguageSortingSetting, refreshCallback);
                },
                description: 'Select sorting for files within language groups'
            }
        ];
        console.log(`NEW_CODE_ANALYSIS: Collected ${commandRegistrations.length} Files by Language Sorting command registrations`);
        return commandRegistrations;
    }
    /**
     * Show picker for language group primary sorting
     */
    static async selectLanguageGroupPrimarySorting(context, setting, refreshCallback) {
        console.log('NEW_CODE_ANALYSIS: Showing language group primary sorting picker');
        // Get current configuration to check for conflicts
        const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
        const currentConfig = await storage.getFilesByLanguageSorting();
        const options = [
            { label: 'Alphabetical Ascending', sortBy: 'alphabetical', direction: 'ascending' },
            { label: 'Alphabetical Descending', sortBy: 'alphabetical', direction: 'descending' },
            { label: 'File Count Ascending', sortBy: 'fileCount', direction: 'ascending' },
            { label: 'File Count Descending', sortBy: 'fileCount', direction: 'descending' }
        ];
        const picked = await vscode.window.showQuickPick(options, {
            placeHolder: 'Select primary sorting for language groups',
            title: 'Language Groups Primary Sorting'
        });
        if (picked) {
            // Check if the new primary conflicts with current secondary
            const newPrimarySortBy = picked.sortBy;
            const currentSecondarySortBy = currentConfig.languageGroupSecondarySortBy;
            // If secondary uses the same type as the new primary, reset secondary to None
            if (currentSecondarySortBy && currentSecondarySortBy === newPrimarySortBy) {
                console.log('NEW_CODE_ANALYSIS: Primary conflicts with secondary, resetting secondary to None');
                await setting.updateLanguageGroupSecondarySorting(undefined, undefined);
                vscode.window.showInformationMessage(`Secondary sorting reset to None to avoid conflict with primary sorting`);
            }
            await setting.updateLanguageGroupPrimarySorting(picked.sortBy, picked.direction);
            refreshCallback();
            vscode.window.showInformationMessage(`Language groups primary sorting changed to: ${picked.label}`);
        }
    }
    /**
     * Show picker for language group secondary sorting
     */
    static async selectLanguageGroupSecondarySorting(context, setting, refreshCallback) {
        console.log('NEW_CODE_ANALYSIS: Showing language group secondary sorting picker');
        // Get current configuration to determine what's available for secondary
        const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
        const currentConfig = await storage.getFilesByLanguageSorting();
        const primarySortBy = currentConfig.languageGroupSortBy;
        const options = [{ label: 'None', sortBy: undefined, direction: undefined }];
        if (primarySortBy === 'alphabetical') {
            // Primary is alphabetical, so secondary can be fileCount
            options.push({ label: 'File Count Ascending', sortBy: 'fileCount', direction: 'ascending' }, { label: 'File Count Descending', sortBy: 'fileCount', direction: 'descending' });
        }
        else if (primarySortBy === 'fileCount') {
            // Primary is fileCount, so secondary can be alphabetical
            options.push({ label: 'Alphabetical Ascending', sortBy: 'alphabetical', direction: 'ascending' }, { label: 'Alphabetical Descending', sortBy: 'alphabetical', direction: 'descending' });
        }
        const picked = await vscode.window.showQuickPick(options, {
            placeHolder: 'Select secondary sorting for language groups (optional)',
            title: 'Language Groups Secondary Sorting'
        });
        if (picked) {
            await setting.updateLanguageGroupSecondarySorting(picked.sortBy, picked.direction);
            refreshCallback();
            const message = picked.sortBy ?
                `Language groups secondary sorting changed to: ${picked.label}` :
                'Language groups secondary sorting disabled';
            vscode.window.showInformationMessage(message);
        }
    }
    /**
     * Show picker for files sorting
     */
    static async selectFilesSorting(setting, refreshCallback) {
        console.log('NEW_CODE_ANALYSIS: Showing files sorting picker');
        const options = [
            { label: 'Alphabetical Ascending', sortBy: 'alphabetical', direction: 'ascending' },
            { label: 'Alphabetical Descending', sortBy: 'alphabetical', direction: 'descending' },
            { label: 'Last Modified Ascending (Oldest First)', sortBy: 'lastModified', direction: 'ascending' },
            { label: 'Last Modified Descending (Newest First)', sortBy: 'lastModified', direction: 'descending' }
        ];
        const picked = await vscode.window.showQuickPick(options, {
            placeHolder: 'Select sorting for files within language groups',
            title: 'Files Sorting'
        });
        if (picked) {
            await setting.updateFilesSorting(picked.sortBy, picked.direction);
            refreshCallback();
            vscode.window.showInformationMessage(`Files sorting changed to: ${picked.label}`);
        }
    }
}
exports.FilesByLanguageSortingCommands = FilesByLanguageSortingCommands;


/***/ }),
/* 87 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Analysis Configuration Storage Manager
 * Handles reading/writing configuration to VS Code globalStorage
 * Path: codexr_analysis/configuration_analysis.json
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisConfigurationStorage = void 0;
const vscode = __importStar(__webpack_require__(1));
const analysisConfiguration_1 = __webpack_require__(88);
class AnalysisConfigurationStorage {
    static instance;
    STORAGE_FOLDER = 'codexr_analysis';
    CONFIG_FILE = 'configuration_analysis.json';
    context;
    cachedConfiguration = null;
    constructor(context) {
        this.context = context;
        console.log('NEW_CODE_ANALYSIS: Initializing configuration storage');
    }
    /**
     * Initialize the singleton with the extension context.
     * Must be called once during extension activation before any getInstance() calls.
     */
    static initialize(context) {
        if (!AnalysisConfigurationStorage.instance) {
            AnalysisConfigurationStorage.instance = new AnalysisConfigurationStorage(context);
        }
        return AnalysisConfigurationStorage.instance;
    }
    /**
     * Get singleton instance.
     * @param context Optional — only needed if initialize() was not called yet (backward compatibility).
     */
    static getInstance(context) {
        if (!AnalysisConfigurationStorage.instance) {
            if (!context) {
                throw new Error('AnalysisConfigurationStorage not initialized. Call initialize(context) during activation first.');
            }
            AnalysisConfigurationStorage.instance = new AnalysisConfigurationStorage(context);
        }
        return AnalysisConfigurationStorage.instance;
    }
    /**
     * Get the full file path for configuration
     */
    getConfigurationPath() {
        return vscode.Uri.joinPath(this.context.globalStorageUri, this.STORAGE_FOLDER, this.CONFIG_FILE);
    }
    /**
     * Load configuration from storage
     */
    async loadConfiguration() {
        try {
            // Return cached if available
            if (this.cachedConfiguration) {
                return this.cachedConfiguration;
            }
            const configPath = this.getConfigurationPath();
            // Check if file exists
            try {
                const configData = await vscode.workspace.fs.readFile(configPath);
                const configString = Buffer.from(configData).toString('utf8');
                const configFile = JSON.parse(configString);
                // Validate and merge with defaults (in case new settings were added)
                const configuration = {
                    ...analysisConfiguration_1.DEFAULT_ANALYSIS_CONFIGURATION,
                    ...configFile.configuration
                };
                this.cachedConfiguration = configuration;
                console.log('NEW_CODE_ANALYSIS: Configuration loaded from storage:', configuration);
                return configuration;
            }
            catch (readError) {
                // File doesn't exist or is corrupted, return defaults
                console.log('NEW_CODE_ANALYSIS: Configuration file not found, using defaults');
                this.cachedConfiguration = analysisConfiguration_1.DEFAULT_ANALYSIS_CONFIGURATION;
                // Create default file
                await this.saveConfiguration(analysisConfiguration_1.DEFAULT_ANALYSIS_CONFIGURATION);
                return analysisConfiguration_1.DEFAULT_ANALYSIS_CONFIGURATION;
            }
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error loading configuration:', error);
            return analysisConfiguration_1.DEFAULT_ANALYSIS_CONFIGURATION;
        }
    }
    /**
     * Save configuration to storage
     */
    async saveConfiguration(configuration) {
        try {
            const configPath = this.getConfigurationPath();
            // Ensure directory exists
            const folderPath = vscode.Uri.joinPath(this.context.globalStorageUri, this.STORAGE_FOLDER);
            await vscode.workspace.fs.createDirectory(folderPath);
            // Create configuration file structure
            const configFile = {
                metadata: {
                    version: '1.0.0',
                    lastModified: Date.now(),
                    createdBy: 'CodeXR New Code Analysis'
                },
                configuration: configuration
            };
            // Write to file
            const configString = JSON.stringify(configFile, null, 2);
            const configData = Buffer.from(configString, 'utf8');
            await vscode.workspace.fs.writeFile(configPath, configData);
            // Update cache
            this.cachedConfiguration = configuration;
            console.log('NEW_CODE_ANALYSIS: Configuration saved to storage:', configuration);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error saving configuration:', error);
            throw error;
        }
    }
    /**
     * Get specific setting value
     */
    async getAnalysisFileMode() {
        const config = await this.loadConfiguration();
        return config.analysisFileMode;
    }
    /**
     * Set specific setting value
     */
    async setAnalysisFileMode(mode) {
        const config = await this.loadConfiguration();
        const updatedConfig = {
            ...config,
            analysisFileMode: mode
        };
        await this.saveConfiguration(updatedConfig);
        console.log(`NEW_CODE_ANALYSIS: Analysis file mode changed to: ${mode}`);
    }
    /**
     * Get analysis directory mode setting
     */
    async getAnalysisDirectoryMode() {
        const config = await this.loadConfiguration();
        return config.analysisDirectoryMode;
    }
    /**
     * Set analysis directory mode setting
     */
    async setAnalysisDirectoryMode(mode) {
        const config = await this.loadConfiguration();
        const updatedConfig = {
            ...config,
            analysisDirectoryMode: mode
        };
        await this.saveConfiguration(updatedConfig);
        console.log(`NEW_CODE_ANALYSIS: Analysis directory mode changed to: ${mode}`);
    }
    /**
     * Get view theme setting
     */
    async getViewTheme() {
        const config = await this.loadConfiguration();
        return config.viewTheme;
    }
    /**
     * Set view theme setting
     */
    async setViewTheme(theme) {
        const config = await this.loadConfiguration();
        const updatedConfig = {
            ...config,
            viewTheme: theme
        };
        await this.saveConfiguration(updatedConfig);
        console.log(`NEW_CODE_ANALYSIS: View theme changed to: ${theme}`);
    }
    /**
     * Get auto-analysis delay setting
     */
    async getAutoAnalysisDelay() {
        const config = await this.loadConfiguration();
        return config.autoAnalysisDelay;
    }
    /**
     * Set auto-analysis delay setting
     */
    async setAutoAnalysisDelay(delayConfig) {
        const config = await this.loadConfiguration();
        const updatedConfig = {
            ...config,
            autoAnalysisDelay: delayConfig
        };
        await this.saveConfiguration(updatedConfig);
        console.log(`NEW_CODE_ANALYSIS: Auto-analysis delay changed to: ${JSON.stringify(delayConfig)}`);
    }
    /**
     * Get auto-analysis enabled setting
     */
    async getAutoAnalysisEnabled() {
        const config = await this.loadConfiguration();
        return config.autoAnalysisDelay.autoAnalysisEnabled;
    }
    /**
     * Set auto-analysis enabled setting
     */
    async setAutoAnalysisEnabled(enabled) {
        const config = await this.loadConfiguration();
        const updatedConfig = {
            ...config,
            autoAnalysisDelay: {
                ...config.autoAnalysisDelay,
                autoAnalysisEnabled: enabled
            }
        };
        await this.saveConfiguration(updatedConfig);
        console.log(`NEW_CODE_ANALYSIS: Auto-analysis enabled changed to: ${enabled}`);
    }
    /**
     * Update multiple settings at once
     */
    async updateConfiguration(updates) {
        const currentConfig = await this.loadConfiguration();
        const updatedConfig = {
            ...currentConfig,
            ...updates
        };
        await this.saveConfiguration(updatedConfig);
        console.log(`NEW_CODE_ANALYSIS: Configuration updated with: ${JSON.stringify(updates)}`);
    }
    /**
     * Reset configuration to defaults
     */
    async resetConfiguration() {
        await this.saveConfiguration(analysisConfiguration_1.DEFAULT_ANALYSIS_CONFIGURATION);
        this.cachedConfiguration = null; // Clear cache
    }
    /**
     * Get configuration file path for debugging
     */
    getConfigurationFilePath() {
        return this.getConfigurationPath().fsPath;
    }
    /**
     * Check if configuration file exists
     */
    async configurationExists() {
        try {
            const configPath = this.getConfigurationPath();
            await vscode.workspace.fs.stat(configPath);
            return true;
        }
        catch {
            return false;
        }
    }
    /**
     * Debug method to get current configuration file content
     */
    async debugGetConfigurationFileContent() {
        try {
            const configPath = this.getConfigurationPath();
            const configData = await vscode.workspace.fs.readFile(configPath);
            return Buffer.from(configData).toString('utf8');
        }
        catch (error) {
            console.log('NEW_CODE_ANALYSIS: Configuration file not found or error reading:', error);
            return null;
        }
    }
    /**
     * Force reload configuration from disk (bypassing cache)
     */
    async forceReloadConfiguration() {
        this.cachedConfiguration = null;
        return await this.loadConfiguration();
    }
    /**
     * Get chart type for file analysis
     */
    async getChartTypeFile() {
        const config = await this.loadConfiguration();
        return config.chartTypeFile;
    }
    /**
     * Set chart type for file analysis
     */
    async setChartTypeFile(chartType) {
        await this.updateConfiguration({ chartTypeFile: chartType });
    }
    /**
     * Get dimension mapping for file analysis
     */
    async getDimensionMappingFile() {
        const config = await this.loadConfiguration();
        return config.dimensionMappingFile;
    }
    /**
     * Set dimension mapping for file analysis
     */
    async setDimensionMappingFile(mapping) {
        await this.updateConfiguration({ dimensionMappingFile: mapping });
    }
    /**
     * Get current analysis mode (alias for getAnalysisFileMode for compatibility)
     */
    async getAnalysisMode() {
        return await this.getAnalysisFileMode();
    }
    /**
     * Set analysis mode (alias for setAnalysisFileMode for compatibility)
     */
    async setAnalysisMode(mode) {
        await this.setAnalysisFileMode(mode);
    }
    /**
     * Get Files by Language sorting configuration
     */
    async getFilesByLanguageSorting() {
        const config = await this.loadConfiguration();
        return config.filesByLanguageSorting;
    }
    /**
     * Set Files by Language sorting configuration
     */
    async setFilesByLanguageSorting(sortingConfig) {
        await this.updateConfiguration({ filesByLanguageSorting: sortingConfig });
        console.log('NEW_CODE_ANALYSIS: Files by Language sorting configuration updated:', sortingConfig);
    }
    // ============================================================================
    // Directory Chart Type Methods
    // ============================================================================
    /**
     * Get chart type for directory analysis
     */
    async getDirectoryChartType() {
        const config = await this.loadConfiguration();
        return config.chartTypeDirectory;
    }
    /**
     * Set chart type for directory analysis
     */
    async setDirectoryChartType(chartType) {
        await this.updateConfiguration({ chartTypeDirectory: chartType });
    }
    // ============================================================================
    // Directory Dimension Mapping Methods
    // ============================================================================
    /**
     * Get dimension mapping for directory analysis
     */
    async getDimensionMappingDirectory() {
        const config = await this.loadConfiguration();
        return config.dimensionMappingDirectory;
    }
    /**
     * Set dimension mapping for directory analysis
     */
    async setDimensionMappingDirectory(mapping) {
        await this.updateConfiguration({ dimensionMappingDirectory: mapping });
    }
}
exports.AnalysisConfigurationStorage = AnalysisConfigurationStorage;


/***/ }),
/* 88 */
/***/ ((__unused_webpack_module, exports) => {


/**
 * Analysis Configuration Models
 * Types and interfaces for all analysis configuration data
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DEFAULT_CONFIGURATION_FILE = exports.DEFAULT_ANALYSIS_CONFIGURATION = void 0;
/**
 * Default configuration values
 */
exports.DEFAULT_ANALYSIS_CONFIGURATION = {
    analysisFileMode: 'XR', // Default to XR as requested
    analysisDirectoryMode: 'XR', // Default to XR as requested
    viewTheme: 'Dark', // Default to Dark theme
    autoAnalysisDelay: {
        type: 'RealTime', // Default to Real Time (0s)
        customMs: undefined,
        autoAnalysisEnabled: true // Default to enabled
    },
    chartTypeFile: 'boats', // Default chart type changed to boats
    chartTypeDirectory: 'boats', // Default chart type for directories changed to boats
    dimensionMappingFile: {
        area: 'parameters',
        height: 'lineCount',
        color: 'complexity'
    }, // Default dimension mapping for files
    dimensionMappingDirectory: {
        area: 'functionCount',
        height: 'totalLines',
        color: 'cyclomaticComplexityNumber'
    }, // Default dimension mapping for directories
    filesByLanguageSorting: {
        languageGroupSortBy: 'alphabetical',
        languageGroupSortDirection: 'ascending',
        languageGroupSecondarySortBy: 'fileCount',
        languageGroupSecondarySortDirection: 'descending',
        filesSortBy: 'alphabetical',
        filesSortDirection: 'ascending'
    }
};
/**
 * Default configuration file structure
 */
exports.DEFAULT_CONFIGURATION_FILE = {
    metadata: {
        version: '1.0.0',
        lastModified: Date.now(),
        createdBy: 'CodeXR New Code Analysis'
    },
    configuration: exports.DEFAULT_ANALYSIS_CONFIGURATION
};


/***/ }),
/* 89 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Profile Configuration Commands Module
 * Exports for profile configuration commands
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ProfileConfigurationCommands = void 0;
var profileConfigurationCommands_1 = __webpack_require__(90);
Object.defineProperty(exports, "ProfileConfigurationCommands", ({ enumerable: true, get: function () { return profileConfigurationCommands_1.ProfileConfigurationCommands; } }));


/***/ }),
/* 90 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Profile Configuration Commands
 * Commands for managing analysis configuration profiles
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ProfileConfigurationCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const analysisConfigurationStorage_1 = __webpack_require__(87);
class ProfileConfigurationCommands {
    context;
    profileConfigurationSetting;
    constructor(context, profileConfigurationSetting) {
        this.context = context;
        this.profileConfigurationSetting = profileConfigurationSetting;
    }
    /**
     * Get command registrations (don't register them yet)
     * This follows the "nested dolls" architecture pattern
     */
    static getCommandRegistrations(context, profileConfigurationSetting, refreshCallback) {
        console.log('PROFILE_CONFIGURATION_COMMANDS: Creating command registrations');
        const commands = new ProfileConfigurationCommands(context, profileConfigurationSetting);
        const registrations = [
            {
                commandId: 'newCodeAnalysis.resetToDefaults',
                callback: async () => {
                    console.log(`PROFILE_CONFIGURATION_COMMANDS: Reset to defaults command TRIGGERED`);
                    await commands.resetToDefaults();
                    refreshCallback();
                },
                description: 'Reset analysis settings to default values'
            },
            {
                commandId: 'newCodeAnalysis.saveProfile',
                callback: async () => {
                    console.log(`PROFILE_CONFIGURATION_COMMANDS: Save profile command TRIGGERED`);
                    await commands.saveProfile();
                    refreshCallback();
                },
                description: 'Save current configuration as a named profile'
            },
            {
                commandId: 'newCodeAnalysis.loadProfile',
                callback: async () => {
                    console.log(`PROFILE_CONFIGURATION_COMMANDS: Load profile command TRIGGERED`);
                    await commands.loadProfile();
                    refreshCallback();
                },
                description: 'Load a previously saved configuration profile'
            },
            {
                commandId: 'newCodeAnalysis.deleteProfile',
                callback: async () => {
                    console.log(`PROFILE_CONFIGURATION_COMMANDS: Delete profile command TRIGGERED`);
                    await commands.deleteProfile();
                    refreshCallback();
                },
                description: 'Delete a previously saved configuration profile'
            }
        ];
        console.log(`PROFILE_CONFIGURATION_COMMANDS: Created ${registrations.length} command registrations`);
        return registrations;
    }
    /**
     * Reset to default values
     */
    async resetToDefaults() {
        console.log('PROFILE_CONFIGURATION_COMMANDS: Resetting to default values');
        try {
            const shouldReset = await vscode.window.showWarningMessage('This will reset all analysis settings to their default values. Are you sure?', 'Reset to Defaults', 'Cancel');
            if (shouldReset !== 'Reset to Defaults') {
                console.log('PROFILE_CONFIGURATION_COMMANDS: Reset cancelled by user');
                return;
            }
            // Use the profile configuration setting to reset
            await this.profileConfigurationSetting.resetToDefaults();
            console.log('PROFILE_CONFIGURATION_COMMANDS: Reset to defaults completed successfully');
        }
        catch (error) {
            console.error('PROFILE_CONFIGURATION_COMMANDS: Error resetting to defaults:', error);
            vscode.window.showErrorMessage(`Failed to reset to defaults: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Save current configuration as a profile
     */
    async saveProfile() {
        console.log('PROFILE_CONFIGURATION_COMMANDS: Saving profile');
        try {
            // Ask user for profile name
            const profileName = await vscode.window.showInputBox({
                prompt: 'Enter a name for this configuration profile',
                placeHolder: 'e.g., "My Custom Setup", "Project A Settings"',
                validateInput: (value) => {
                    if (!value || value.trim().length === 0) {
                        return 'Profile name cannot be empty';
                    }
                    if (value.includes('/') || value.includes('\\') || value.includes(':')) {
                        return 'Profile name cannot contain special characters (/, \\, :)';
                    }
                    return null;
                }
            });
            if (!profileName) {
                console.log('PROFILE_CONFIGURATION_COMMANDS: Save profile cancelled - no name provided');
                return;
            }
            // Get current configuration from AnalysisConfigurationStorage
            const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(this.context);
            const currentConfig = await storage.loadConfiguration();
            console.log('PROFILE_CONFIGURATION_COMMANDS: Current config to save:', JSON.stringify(currentConfig, null, 2));
            // Save to globalStorage
            const profilesDir = path.join(this.context.globalStorageUri.fsPath, 'analysis_settings');
            // Create directory if it doesn't exist
            if (!fs.existsSync(profilesDir)) {
                fs.mkdirSync(profilesDir, { recursive: true });
            }
            const profilePath = path.join(profilesDir, `${profileName.trim()}.json`);
            // Check if profile already exists
            if (fs.existsSync(profilePath)) {
                const shouldOverwrite = await vscode.window.showWarningMessage(`A profile named "${profileName}" already exists. Do you want to overwrite it?`, 'Overwrite', 'Cancel');
                if (shouldOverwrite !== 'Overwrite') {
                    console.log('PROFILE_CONFIGURATION_COMMANDS: Save profile cancelled - user chose not to overwrite');
                    return;
                }
            }
            // Add metadata to the profile
            const profileData = {
                name: profileName.trim(),
                savedAt: new Date().toISOString(),
                version: '1.0',
                configuration: currentConfig
            };
            // Save the profile
            fs.writeFileSync(profilePath, JSON.stringify(profileData, null, 2));
            console.log(`PROFILE_CONFIGURATION_COMMANDS: Profile "${profileName}" saved successfully`);
            vscode.window.showInformationMessage(`Configuration profile "${profileName}" saved successfully`);
        }
        catch (error) {
            console.error('PROFILE_CONFIGURATION_COMMANDS: Error saving profile:', error);
            vscode.window.showErrorMessage(`Failed to save profile: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Load a previously saved profile
     */
    async loadProfile() {
        console.log('PROFILE_CONFIGURATION_COMMANDS: Loading profile');
        try {
            const profilesDir = path.join(this.context.globalStorageUri.fsPath, 'analysis_settings');
            // Check if profiles directory exists
            if (!fs.existsSync(profilesDir)) {
                vscode.window.showInformationMessage('No saved profiles found');
                return;
            }
            // Get list of saved profiles
            const profileFiles = fs.readdirSync(profilesDir).filter(file => file.endsWith('.json'));
            if (profileFiles.length === 0) {
                vscode.window.showInformationMessage('No saved profiles found');
                return;
            }
            // Create quick pick items
            const profileItems = profileFiles.map(file => {
                const profilePath = path.join(profilesDir, file);
                try {
                    const profileData = JSON.parse(fs.readFileSync(profilePath, 'utf8'));
                    const savedDate = new Date(profileData.savedAt).toLocaleString();
                    return {
                        label: profileData.name || file.replace('.json', ''),
                        description: `Saved: ${savedDate}`,
                        detail: `Version: ${profileData.version || 'Unknown'}`,
                        profilePath: profilePath
                    };
                }
                catch {
                    return {
                        label: file.replace('.json', ''),
                        description: 'Invalid profile data',
                        detail: 'This profile may be corrupted',
                        profilePath: profilePath
                    };
                }
            });
            // Show profile selection
            const selectedProfile = await vscode.window.showQuickPick(profileItems, {
                placeHolder: 'Select a configuration profile to load',
                title: 'Load Configuration Profile'
            });
            if (!selectedProfile) {
                console.log('PROFILE_CONFIGURATION_COMMANDS: Load profile cancelled');
                return;
            }
            // Confirm loading
            const shouldLoad = await vscode.window.showWarningMessage(`This will replace your current configuration with "${selectedProfile.label}". Are you sure?`, 'Load Profile', 'Cancel');
            if (shouldLoad !== 'Load Profile') {
                console.log('PROFILE_CONFIGURATION_COMMANDS: Load profile cancelled by user');
                return;
            }
            // Load the profile
            const profileData = JSON.parse(fs.readFileSync(selectedProfile.profilePath, 'utf8'));
            console.log('PROFILE_CONFIGURATION_COMMANDS: Loaded profile data:', JSON.stringify(profileData.configuration, null, 2));
            // Update configuration using AnalysisConfigurationStorage
            const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(this.context);
            await storage.updateConfiguration(profileData.configuration);
            console.log(`PROFILE_CONFIGURATION_COMMANDS: Profile "${selectedProfile.label}" loaded successfully`);
            vscode.window.showInformationMessage(`Configuration profile "${selectedProfile.label}" loaded successfully`);
        }
        catch (error) {
            console.error('PROFILE_CONFIGURATION_COMMANDS: Error loading profile:', error);
            vscode.window.showErrorMessage(`Failed to load profile: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Delete a saved profile
     */
    async deleteProfile() {
        console.log('PROFILE_CONFIGURATION_COMMANDS: Deleting profile');
        try {
            const profilesDir = path.join(this.context.globalStorageUri.fsPath, 'analysis_settings');
            // Check if profiles directory exists
            if (!fs.existsSync(profilesDir)) {
                vscode.window.showInformationMessage('No saved profiles found');
                return;
            }
            // Get list of saved profiles
            const profileFiles = fs.readdirSync(profilesDir).filter(file => file.endsWith('.json'));
            if (profileFiles.length === 0) {
                vscode.window.showInformationMessage('No saved profiles found');
                return;
            }
            // Create quick pick items
            const profileItems = profileFiles.map(file => {
                const profilePath = path.join(profilesDir, file);
                try {
                    const profileData = JSON.parse(fs.readFileSync(profilePath, 'utf8'));
                    const savedDate = new Date(profileData.savedAt).toLocaleString();
                    return {
                        label: profileData.name || file.replace('.json', ''),
                        description: `Saved: ${savedDate}`,
                        detail: `Version: ${profileData.version || 'Unknown'}`,
                        profilePath: profilePath
                    };
                }
                catch {
                    return {
                        label: file.replace('.json', ''),
                        description: 'Invalid profile data',
                        detail: 'This profile may be corrupted',
                        profilePath: profilePath
                    };
                }
            });
            // Show profile selection
            const selectedProfile = await vscode.window.showQuickPick(profileItems, {
                placeHolder: 'Select a configuration profile to delete',
                title: 'Delete Configuration Profile'
            });
            if (!selectedProfile) {
                console.log('PROFILE_CONFIGURATION_COMMANDS: Delete profile cancelled');
                return;
            }
            // Confirm deletion
            const shouldDelete = await vscode.window.showWarningMessage(`Are you sure you want to delete the profile "${selectedProfile.label}"? This action cannot be undone.`, 'Delete Profile', 'Cancel');
            if (shouldDelete !== 'Delete Profile') {
                console.log('PROFILE_CONFIGURATION_COMMANDS: Delete profile cancelled by user');
                return;
            }
            // Delete the profile
            fs.unlinkSync(selectedProfile.profilePath);
            console.log(`PROFILE_CONFIGURATION_COMMANDS: Profile "${selectedProfile.label}" deleted successfully`);
            vscode.window.showInformationMessage(`Configuration profile "${selectedProfile.label}" deleted successfully`);
        }
        catch (error) {
            console.error('PROFILE_CONFIGURATION_COMMANDS: Error deleting profile:', error);
            vscode.window.showErrorMessage(`Failed to delete profile: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
}
exports.ProfileConfigurationCommands = ProfileConfigurationCommands;


/***/ }),
/* 91 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Active Analyses Commands
 * Commands specific to the Active Analyses subsection
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ActiveAnalysesCommands = void 0;
const sessionRegistry_1 = __webpack_require__(32);
const serverWatcherIntegration_1 = __webpack_require__(92);
const activeAnalysesCommands_1 = __webpack_require__(115);
class ActiveAnalysesCommands {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Get active analyses command registrations (nested dolls pattern)
     */
    static getCommandRegistrations(context, refreshCallback) {
        console.log('NEW_CODE_ANALYSIS: Collecting Active Analyses command registrations');
        // Get the session registry and server watcher
        const sessionRegistry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
        const serverWatcher = serverWatcherIntegration_1.ServerWatcherIntegration.getInstance(context);
        const viewCommands = activeAnalysesCommands_1.ActiveAnalysesCommands.getInstance(sessionRegistry, serverWatcher);
        // Register commands with the view commands handler (this does the actual registration)
        const disposables = viewCommands.registerCommands();
        // Convert disposables to command registrations for the nested pattern
        const commandRegistrations = [];
        console.log(`NEW_CODE_ANALYSIS: Active Analyses commands registered directly, returning ${commandRegistrations.length} command registrations for nested pattern`);
        return commandRegistrations;
    }
}
exports.ActiveAnalysesCommands = ActiveAnalysesCommands;


/***/ }),
/* 92 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Server-Watcher Integration Service
 * Manages communication between servers and file watchers for cleanup coordination
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServerWatcherIntegration = void 0;
const vscode = __importStar(__webpack_require__(1));
const sessionRegistry_1 = __webpack_require__(32);
const sessionWatcherManager_1 = __webpack_require__(93);
const visualizeDOMWatcher_1 = __webpack_require__(110);
const activeServerRegistry_1 = __webpack_require__(28);
const serverControl_1 = __webpack_require__(30);
const fs = __importStar(__webpack_require__(6));
class ServerWatcherIntegration {
    static instance;
    context;
    sessionWatcherManager;
    constructor(context) {
        this.context = context;
        this.sessionWatcherManager = new sessionWatcherManager_1.SessionWatcherManager(context);
        console.log('SERVER_WATCHER_INTEGRATION: Initializing service');
        this.setupServerEventListeners();
    }
    /**
     * Get singleton instance
     */
    static getInstance(context) {
        if (!ServerWatcherIntegration.instance) {
            if (!context) {
                throw new Error('ServerWatcherIntegration: ExtensionContext required for first initialization');
            }
            ServerWatcherIntegration.instance = new ServerWatcherIntegration(context);
        }
        return ServerWatcherIntegration.instance;
    }
    /**
     * Setup listeners for server events
     */
    setupServerEventListeners() {
        try {
            console.log('SERVER_WATCHER_INTEGRATION: Setting up server event listeners');
            const serverRegistry = (0, activeServerRegistry_1.getActiveServerRegistry)();
            // Listen for server registry changes
            serverRegistry.onRegistryChange((event) => {
                if (event.type === 'serverRemoved') {
                    console.log(`SERVER_WATCHER_INTEGRATION: Server removed event detected: ${event.serverId}`);
                    this.handleServerRemoved(event.serverId, event.server);
                }
            });
            console.log('SERVER_WATCHER_INTEGRATION: Server event listeners setup complete');
        }
        catch (error) {
            console.error('SERVER_WATCHER_INTEGRATION: Error setting up server event listeners:', error);
        }
    }
    /**
     * Handle server removal - cleanup associated analysis sessions
     */
    async handleServerRemoved(serverId, server) {
        try {
            console.log(`SERVER_WATCHER_INTEGRATION: Handling server removal: ${serverId} on port ${server.port}`);
            // Find analysis sessions that were using this server
            const sessionRegistry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(this.context);
            const allSessions = sessionRegistry.getAllSessions();
            const affectedSessions = allSessions.filter((session) => {
                // Check if session has the same port (indicating it was using this server)
                return session.port === server.port;
            });
            console.log(`SERVER_WATCHER_INTEGRATION: Found ${affectedSessions.length} affected sessions for server ${serverId}`);
            // Cleanup each affected session
            for (const session of affectedSessions) {
                await this.cleanupSessionAfterServerClosure(session.id, server.port);
            }
        }
        catch (error) {
            console.error(`SERVER_WATCHER_INTEGRATION: Error handling server removal:`, error);
        }
    }
    /**
     * Cleanup session after server closure
     */
    async cleanupSessionAfterServerClosure(sessionId, serverPort) {
        try {
            console.log(`SERVER_WATCHER_INTEGRATION: Cleaning up session ${sessionId} after server closure (port ${serverPort})`);
            const sessionRegistry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(this.context);
            const session = sessionRegistry.getSession(sessionId);
            if (!session) {
                console.log(`SERVER_WATCHER_INTEGRATION: Session ${sessionId} not found, skipping cleanup`);
                return;
            }
            // 1. Stop file watcher
            await this.stopFileWatcherForSession(sessionId, session.targetPath);
            // 2. Cleanup analysis files
            await this.cleanupAnalysisFiles(session.outputPath);
            // 3. Close the session
            sessionRegistry.updateSessionStatus(sessionId, 'closed');
            // 4. Show notification to user
            vscode.window.showInformationMessage(`Analysis cleaned up: ${session.targetName} - ${session.analysisMode} (server on port ${serverPort} was closed)`);
            console.log(`SERVER_WATCHER_INTEGRATION: Successfully cleaned up session ${sessionId}`);
        }
        catch (error) {
            console.error(`SERVER_WATCHER_INTEGRATION: Error cleaning up session ${sessionId}:`, error);
        }
    }
    /**
     * Stop file watcher for a session
     */
    async stopFileWatcherForSession(sessionId, filePath) {
        try {
            console.log(`SERVER_WATCHER_INTEGRATION: Stopping all watchers for session ${sessionId}, file: ${filePath}`);
            // 1. Use SessionWatcherManager to stop the specific session watcher (file/directory watchers)
            const sessionWatcherStopped = await this.sessionWatcherManager.stopWatchingSession(sessionId);
            if (sessionWatcherStopped) {
                console.log(`SERVER_WATCHER_INTEGRATION: ✅ Successfully stopped session file watcher for session ${sessionId}`);
            }
            else {
                console.log(`SERVER_WATCHER_INTEGRATION: ⚠️ No active session watcher found for session ${sessionId}`);
            }
            // 2. Stop any VisualizeDOM watchers that might be active for this session
            let domWatchersStopped = 0;
            const activeWatchers = visualizeDOMWatcher_1.VisualizeDOMWatcher.getActiveWatchers();
            for (const [watcherId, watcherInfo] of activeWatchers) {
                if (watcherInfo.sessionId === sessionId) {
                    const stopped = visualizeDOMWatcher_1.VisualizeDOMWatcher.stopWatching(watcherId);
                    if (stopped) {
                        domWatchersStopped++;
                        console.log(`SERVER_WATCHER_INTEGRATION: ✅ Stopped VisualizeDOM watcher ${watcherId} for session ${sessionId}`);
                    }
                }
            }
            if (domWatchersStopped > 0) {
                console.log(`SERVER_WATCHER_INTEGRATION: ✅ Stopped ${domWatchersStopped} VisualizeDOM watcher(s) for session ${sessionId}`);
            }
            else {
                console.log(`SERVER_WATCHER_INTEGRATION: ⚠️ No VisualizeDOM watchers found for session ${sessionId}`);
            }
            console.log(`SERVER_WATCHER_INTEGRATION: ✅ All watchers stopped for session ${sessionId}`);
        }
        catch (error) {
            console.error(`SERVER_WATCHER_INTEGRATION: Error stopping file watcher for session ${sessionId}:`, error);
        }
    }
    /**
     * Cleanup analysis files
     */
    async cleanupAnalysisFiles(outputPath) {
        try {
            if (outputPath && fs.existsSync(outputPath)) {
                console.log(`SERVER_WATCHER_INTEGRATION: Cleaning up analysis files at: ${outputPath}`);
                // Implementation depends on cleanup strategy
            }
        }
        catch (error) {
            console.error(`SERVER_WATCHER_INTEGRATION: Error cleaning up analysis files:`, error);
        }
    }
    /**
     * Manual cleanup trigger (for when user closes analysis from UI)
     */
    async triggerManualCleanup(sessionId) {
        try {
            console.log(`SERVER_WATCHER_INTEGRATION: Manual cleanup triggered for session ${sessionId}`);
            const sessionRegistry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(this.context);
            const session = sessionRegistry.getSession(sessionId);
            if (!session) {
                console.log(`SERVER_WATCHER_INTEGRATION: Session ${sessionId} not found for manual cleanup`);
                return false;
            }
            console.log(`SERVER_WATCHER_INTEGRATION: Session found:`, {
                id: session.id,
                targetPath: session.targetPath,
                outputPath: session.outputPath,
                assignedPort: session.assignedPort,
                status: session.status,
                analysisMode: session.analysisMode,
                targetName: session.targetName
            });
            // Check if session has port information - support both old and new engine
            const sessionPort = session.assignedPort;
            if (sessionPort) {
                console.log(`SERVER_WATCHER_INTEGRATION: Session has port information: ${sessionPort}`);
            }
            else {
                console.log(`SERVER_WATCHER_INTEGRATION: Session does NOT have port information`);
                console.log(`SERVER_WATCHER_INTEGRATION: Full session object:`, JSON.stringify(session, null, 2));
            }
            // 1. Stop related server if it exists
            if (sessionPort) {
                console.log(`SERVER_WATCHER_INTEGRATION: Session has port ${sessionPort}, stopping server`);
                await this.stopServerByPort(sessionPort);
            }
            else {
                console.log(`SERVER_WATCHER_INTEGRATION: Session has no port information, trying to find server by other means`);
                // Try to find server by other means
                await this.findAndStopServerForSession(sessionId, session);
            }
            // 2. Stop file watcher
            console.log(`SERVER_WATCHER_INTEGRATION: Stopping file watcher for session`);
            await this.stopFileWatcherForSession(sessionId, session.targetPath);
            // 3. Cleanup analysis files  
            console.log(`SERVER_WATCHER_INTEGRATION: Cleaning up analysis files`);
            await this.cleanupAnalysisFiles(session.outputPath);
            // 4. Close the session
            console.log(`SERVER_WATCHER_INTEGRATION: Closing session in registry`);
            const closed = sessionRegistry.updateSessionStatus(sessionId, 'closed');
            console.log(`SERVER_WATCHER_INTEGRATION: Manual cleanup completed for session ${sessionId}: ${closed}`);
            return closed;
        }
        catch (error) {
            console.error(`SERVER_WATCHER_INTEGRATION: Error in manual cleanup:`, error);
            return false;
        }
    }
    /**
     * Stop server by port
     */
    async stopServerByPort(port) {
        try {
            const serverRegistry = (0, activeServerRegistry_1.getActiveServerRegistry)();
            const servers = serverRegistry.getAllServers();
            const serverToStop = servers.find((server) => server.port === port);
            if (serverToStop) {
                console.log(`SERVER_WATCHER_INTEGRATION: Stopping server ${serverToStop.id} on port ${port}`);
                await serverControl_1.ServerControl.stopServer(serverToStop.id);
            }
            else {
                console.log(`SERVER_WATCHER_INTEGRATION: No server found on port ${port}`);
            }
        }
        catch (error) {
            console.error(`SERVER_WATCHER_INTEGRATION: Error stopping server by port ${port}:`, error);
        }
    }
    /**
     * Find and stop server for session using multiple matching strategies
     */
    async findAndStopServerForSession(sessionId, session) {
        try {
            console.log(`SERVER_WATCHER_INTEGRATION: Trying to find server for session ${sessionId}`);
            const serverRegistry = (0, activeServerRegistry_1.getActiveServerRegistry)();
            const servers = serverRegistry.getAllServers();
            // Try to find server by various matching strategies
            const matchingServer = servers.find((server) => {
                // Match by output path
                if (session.outputPath && server.htmlFile && server.htmlFile.includes(session.outputPath)) {
                    return true;
                }
                // Match by file path in server metadata
                if (server.metadata && server.metadata.sessionId === sessionId) {
                    return true;
                }
                return false;
            });
            if (matchingServer) {
                console.log(`SERVER_WATCHER_INTEGRATION: Found matching server ${matchingServer.id}, stopping it`);
                await serverControl_1.ServerControl.stopServer(matchingServer.id);
            }
            else {
                console.log(`SERVER_WATCHER_INTEGRATION: No matching server found for session ${sessionId}`);
            }
        }
        catch (error) {
            console.error(`SERVER_WATCHER_INTEGRATION: Error finding and stopping server for session ${sessionId}:`, error);
        }
    }
    /**
     * Initialize the service (to be called from extension activation)
     */
    static initialize(context) {
        ServerWatcherIntegration.getInstance(context);
        console.log('SERVER_WATCHER_INTEGRATION: Service initialized');
    }
}
exports.ServerWatcherIntegration = ServerWatcherIntegration;


/***/ }),
/* 93 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Session Watcher Manager
 * Maneja file watchers para sesiones de análisis - siguiendo arquitectura muñecas rusas
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SessionWatcherManager = void 0;
const fs = __importStar(__webpack_require__(6));
const fileWatcherOrchestrator_1 = __webpack_require__(94);
const directoryWatcherOrchestrator_1 = __webpack_require__(103);
class SessionWatcherManager {
    context;
    activeWatchers = new Map();
    constructor(context) {
        this.context = context;
        console.log('SESSION_WATCHER_MANAGER: Initializing Session Watcher Manager');
    }
    /**
     * Inicia watcher para una sesión específica
     * Maneja tanto file analysis como directory analysis
     */
    async startWatchingSession(session) {
        try {
            console.log(`SESSION_WATCHER_MANAGER: 🔍 Starting watcher for session ${session.id}`);
            console.log(`SESSION_WATCHER_MANAGER: Target: ${session.targetPath}`);
            console.log(`SESSION_WATCHER_MANAGER: Mode: ${session.analysisMode}, Type: ${session.targetType}`);
            // Solo para LivePanel por ahora
            if (session.analysisMode !== 'LivePanel') {
                console.log(`SESSION_WATCHER_MANAGER: ⏭️ Skipping watcher for ${session.analysisMode} (not implemented yet)`);
                return null;
            }
            let orchestrator;
            let watcherId = null;
            if (session.targetType === 'file') {
                // File analysis - usar FileWatcherOrchestrator
                console.log(`SESSION_WATCHER_MANAGER: 📄 Setting up file watcher`);
                // Verificar que el archivo existe
                if (!fs.existsSync(session.targetPath)) {
                    console.error(`SESSION_WATCHER_MANAGER: ❌ File does not exist: ${session.targetPath}`);
                    return null;
                }
                // Verificar que la sesión tiene archivos guardados
                if (!session.savedFilesPath) {
                    console.error(`SESSION_WATCHER_MANAGER: ❌ Session ${session.id} does not have saved files path`);
                    return null;
                }
                // Crear orchestrator para file analysis
                orchestrator = new fileWatcherOrchestrator_1.FileWatcherOrchestrator(session, this.context);
            }
            else if (session.targetType === 'directory') {
                // Directory analysis - usar DirectoryWatcherOrchestrator
                console.log(`SESSION_WATCHER_MANAGER: 📁 Setting up directory watcher`);
                // Verificar que el directorio existe
                if (!fs.existsSync(session.targetPath)) {
                    console.error(`SESSION_WATCHER_MANAGER: ❌ Directory does not exist: ${session.targetPath}`);
                    return null;
                }
                // Verificar que la sesión tiene archivos guardados
                if (!session.savedFilesPath) {
                    console.error(`SESSION_WATCHER_MANAGER: ❌ Session ${session.id} does not have saved files path`);
                    return null;
                }
                // Verificar que hay archivos para observar
                if (!session.filesToHash || session.filesToHash.length === 0) {
                    console.error(`SESSION_WATCHER_MANAGER: ❌ No files to watch in directory session ${session.id}`);
                    return null;
                }
                // Crear orchestrator para directory analysis
                orchestrator = new directoryWatcherOrchestrator_1.DirectoryWatcherOrchestrator(session, this.context);
            }
            else {
                console.error(`SESSION_WATCHER_MANAGER: ❌ Unknown target type: ${session.targetType}`);
                return null;
            }
            // Iniciar el watcher
            watcherId = await orchestrator.startWatching();
            if (watcherId) {
                this.activeWatchers.set(session.id, orchestrator);
                console.log(`SESSION_WATCHER_MANAGER: ✅ Successfully started watcher for session ${session.id}`);
                console.log(`SESSION_WATCHER_MANAGER: Watcher ID: ${watcherId}`);
                console.log(`SESSION_WATCHER_MANAGER: Total active watchers: ${this.activeWatchers.size}`);
                return watcherId;
            }
            else {
                console.error(`SESSION_WATCHER_MANAGER: ❌ Failed to start watcher for session ${session.id}`);
                return null;
            }
        }
        catch (error) {
            console.error(`SESSION_WATCHER_MANAGER: ❌ Error starting watcher for session ${session.id}:`, error);
            return null;
        }
    }
    /**
     * Para watcher de una sesión
     */
    async stopWatchingSession(sessionId) {
        try {
            console.log(`SESSION_WATCHER_MANAGER: 🛑 Stopping watcher for session ${sessionId}`);
            const orchestrator = this.activeWatchers.get(sessionId);
            if (!orchestrator) {
                console.log(`SESSION_WATCHER_MANAGER: ⚠️ No active watcher found for session ${sessionId}`);
                return false;
            }
            orchestrator.stopWatching();
            this.activeWatchers.delete(sessionId);
            console.log(`SESSION_WATCHER_MANAGER: ✅ Successfully stopped watcher for session ${sessionId}`);
            console.log(`SESSION_WATCHER_MANAGER: Remaining active watchers: ${this.activeWatchers.size}`);
            return true;
        }
        catch (error) {
            console.error(`SESSION_WATCHER_MANAGER: ❌ Error stopping watcher for session ${sessionId}:`, error);
            return false;
        }
    }
    /**
     * Para todos los watchers activos
     */
    async stopAllWatchers() {
        console.log(`SESSION_WATCHER_MANAGER: 🛑 Stopping all ${this.activeWatchers.size} active watchers`);
        let stoppedCount = 0;
        const sessionIds = Array.from(this.activeWatchers.keys());
        for (const sessionId of sessionIds) {
            if (await this.stopWatchingSession(sessionId)) {
                stoppedCount++;
            }
        }
        console.log(`SESSION_WATCHER_MANAGER: 📊 Stopped ${stoppedCount}/${sessionIds.length} watchers`);
        return stoppedCount;
    }
    /**
     * Obtiene información básica de watchers activos
     */
    getActiveWatchersInfo() {
        const info = [];
        for (const [sessionId, orchestrator] of this.activeWatchers) {
            let count = 1; // Default para file watcher
            // Si es DirectoryWatcherOrchestrator, obtener el número de archivos
            if ('getWatchedFilesCount' in orchestrator) {
                count = orchestrator.getWatchedFilesCount();
            }
            info.push({
                sessionId: sessionId,
                count: count
            });
        }
        return info;
    }
    /**
     * Cleanup al cerrar la extensión
     */
    dispose() {
        console.log(`SESSION_WATCHER_MANAGER: 🧹 Disposing Session Watcher Manager`);
        this.stopAllWatchers();
    }
}
exports.SessionWatcherManager = SessionWatcherManager;


/***/ }),
/* 94 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * File Watcher Orchestrator
 * Coordina el file watching, debounce y re-análisis para una sesión específica
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FileWatcherOrchestrator = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const sessionRegistry_1 = __webpack_require__(32);
const reAnalysisManager_1 = __webpack_require__(95);
const debounceManager_1 = __webpack_require__(101);
const analysisConfigurationStorage_1 = __webpack_require__(87);
const configurationConverter_1 = __webpack_require__(102);
class FileWatcherOrchestrator {
    session;
    context;
    watcher = null;
    debounceManager = null;
    reAnalysisManager;
    watcherId = null;
    isWatching = false;
    configurationStorage;
    constructor(session, context) {
        this.session = session;
        this.context = context;
        console.log(`FILE_WATCHER_ORCHESTRATOR: Initializing orchestrator for session ${session.id}`);
        console.log(`FILE_WATCHER_ORCHESTRATOR: Target file: ${session.targetPath}`);
        this.reAnalysisManager = new reAnalysisManager_1.ReAnalysisManager(context);
        this.configurationStorage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
    }
    /**
     * Carga la configuración de debounce del usuario
     */
    async loadDebounceConfiguration() {
        try {
            const config = await this.configurationStorage.loadConfiguration();
            console.log(`FILE_WATCHER_ORCHESTRATOR: 🔍 DEBUG - Loading configuration for auto-analysis`);
            // Verificar si auto-analysis está habilitado
            const autoAnalysisEnabled = await this.configurationStorage.getAutoAnalysisEnabled();
            console.log(`FILE_WATCHER_ORCHESTRATOR: 🔍 DEBUG - Auto-Analysis Enabled: ${autoAnalysisEnabled}`);
            if (!autoAnalysisEnabled) {
                console.log(`FILE_WATCHER_ORCHESTRATOR: ⏸️ Auto-Analysis is DISABLED - Watchers will not be activated`);
                return -1; // Return -1 to indicate disabled state
            }
            const delayMs = configurationConverter_1.ConfigurationConverter.convertToMilliseconds(config.autoAnalysisDelay);
            console.log(`FILE_WATCHER_ORCHESTRATOR: Loaded debounce config - ${configurationConverter_1.ConfigurationConverter.getDisplayName(config.autoAnalysisDelay)} (${delayMs}ms)`);
            return delayMs;
        }
        catch (error) {
            console.error(`FILE_WATCHER_ORCHESTRATOR: Error loading configuration, using default 3s:`, error);
            return 3000; // Default fallback
        }
    }
    /**
     * Inicia el file watcher para la sesión
     */
    async startWatching() {
        if (this.isWatching) {
            console.log(`FILE_WATCHER_ORCHESTRATOR: Already watching session ${this.session.id}`);
            return this.watcherId;
        }
        try {
            const targetPath = this.session.targetPath;
            // Verificar que el archivo existe
            if (!fs.existsSync(targetPath)) {
                console.error(`FILE_WATCHER_ORCHESTRATOR: ❌ Target file does not exist: ${targetPath}`);
                return null;
            }
            console.log(`FILE_WATCHER_ORCHESTRATOR: 👀 Starting file watcher for: ${targetPath}`);
            // Crear watcher usando fs.watch
            this.watcher = fs.watch(targetPath, (eventType, filename) => {
                console.log(`FILE_WATCHER_ORCHESTRATOR: 📝 File change detected: ${eventType} for ${filename || targetPath}`);
                this.onFileChanged();
            });
            this.isWatching = true;
            this.watcherId = `watcher_${this.session.id}_${Date.now()}`;
            console.log(`FILE_WATCHER_ORCHESTRATOR: ✅ Successfully started watching ${targetPath}`);
            console.log(`FILE_WATCHER_ORCHESTRATOR: Watcher ID: ${this.watcherId}`);
            return this.watcherId;
        }
        catch (error) {
            console.error(`FILE_WATCHER_ORCHESTRATOR: ❌ Error starting file watcher:`, error);
            this.isWatching = false;
            return null;
        }
    }
    /**
     * Maneja los cambios de archivo
     */
    async onFileChanged() {
        try {
            console.log(`FILE_WATCHER_ORCHESTRATOR: 🔄 Processing file change for ${this.session.targetPath}`);
            // CRITICAL: Check if session still exists before processing
            const sessionRegistry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(this.context);
            const currentSession = sessionRegistry.getSession(this.session.id);
            if (!currentSession) {
                console.log(`FILE_WATCHER_ORCHESTRATOR: ⚠️ Session ${this.session.id} no longer exists, stopping watcher`);
                this.stopWatching();
                return;
            }
            console.log(`FILE_WATCHER_ORCHESTRATOR: ✅ Session ${this.session.id} still exists, proceeding with file change processing`);
            // Cargar configuración de debounce del usuario
            const delayMs = await this.loadDebounceConfiguration();
            // Verificar si auto-analysis está habilitado
            if (delayMs === -1) {
                console.log(`FILE_WATCHER_ORCHESTRATOR: 🚫 Auto-Analysis is DISABLED - Skipping re-analysis`);
                return; // No procesar cambios si está deshabilitado
            }
            const fileName = path.basename(this.session.targetPath);
            // Cancelar debounce previo si existe
            if (this.debounceManager) {
                this.debounceManager.dispose();
            }
            // Crear nuevo debounce manager
            this.debounceManager = new debounceManager_1.DebounceManager(delayMs, () => this.executeReAnalysis(), fileName);
            // Iniciar el debounce
            this.debounceManager.start();
        }
        catch (error) {
            console.error(`FILE_WATCHER_ORCHESTRATOR: ❌ Error processing file change:`, error);
        }
    }
    /**
     * Ejecuta el re-análisis cuando el debounce se completa
     */
    async executeReAnalysis() {
        try {
            console.log(`FILE_WATCHER_ORCHESTRATOR: 🔬 Executing re-analysis for session ${this.session.id}`);
            console.log(`FILE_WATCHER_ORCHESTRATOR: Target: ${this.session.targetPath}`);
            // CRITICAL: Double-check that session still exists before executing re-analysis
            const sessionRegistry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(this.context);
            const currentSession = sessionRegistry.getSession(this.session.id);
            if (!currentSession) {
                console.log(`FILE_WATCHER_ORCHESTRATOR: ⚠️ Session ${this.session.id} no longer exists, aborting re-analysis`);
                this.stopWatching();
                return;
            }
            console.log(`FILE_WATCHER_ORCHESTRATOR: ✅ Session ${this.session.id} still exists, proceeding with re-analysis`);
            // Usar ReAnalysisManager para regenerar solo el data.json
            const success = await this.reAnalysisManager.executeDataJsonRegeneration(this.session);
            if (success) {
                console.log(`FILE_WATCHER_ORCHESTRATOR: ✅ Re-analysis completed successfully`);
                // Mostrar notificación sutil
                const fileName = path.basename(this.session.targetPath);
                vscode.window.setStatusBarMessage(`$(check) Analysis updated for ${fileName}`, 2000 // 2 segundos
                );
            }
            else {
                console.error(`FILE_WATCHER_ORCHESTRATOR: ❌ Re-analysis failed`);
                vscode.window.showErrorMessage(`Failed to update analysis for ${path.basename(this.session.targetPath)}`);
            }
        }
        catch (error) {
            console.error(`FILE_WATCHER_ORCHESTRATOR: ❌ Error during re-analysis:`, error);
            vscode.window.showErrorMessage(`Error updating analysis: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Para el file watcher
     */
    stopWatching() {
        try {
            console.log(`FILE_WATCHER_ORCHESTRATOR: 🛑 Stopping file watcher for session ${this.session.id}`);
            if (this.watcher) {
                this.watcher.close();
                this.watcher = null;
                console.log(`FILE_WATCHER_ORCHESTRATOR: File watcher closed`);
            }
            if (this.debounceManager) {
                this.debounceManager.dispose();
                this.debounceManager = null;
                console.log(`FILE_WATCHER_ORCHESTRATOR: Debounce manager disposed`);
            }
            this.isWatching = false;
            this.watcherId = null;
            console.log(`FILE_WATCHER_ORCHESTRATOR: ✅ Successfully stopped watching`);
        }
        catch (error) {
            console.error(`FILE_WATCHER_ORCHESTRATOR: ❌ Error stopping file watcher:`, error);
        }
    }
    /**
     * Obtiene el estado actual del watcher
     */
    getStatus() {
        return {
            isWatching: this.isWatching,
            watcherId: this.watcherId,
            targetPath: this.session.targetPath,
            sessionId: this.session.id,
            debounceStatus: this.debounceManager?.getStatus() || null
        };
    }
    /**
     * Limpia recursos
     */
    dispose() {
        console.log(`FILE_WATCHER_ORCHESTRATOR: 🧹 Disposing orchestrator for session ${this.session.id}`);
        this.stopWatching();
    }
}
exports.FileWatcherOrchestrator = FileWatcherOrchestrator;
exports["default"] = FileWatcherOrchestrator;


/***/ }),
/* 95 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Re-Analysis Manager
 * Maneja la regeneración SOLO del data.json cuando un archivo cambia
 * Reutiliza el pipeline existente pero solo actualiza el data.json
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ReAnalysisManager = void 0;
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(33));
const executePython_1 = __webpack_require__(96);
const SSEManager_1 = __webpack_require__(18);
class ReAnalysisManager {
    context;
    executePython;
    sseManager;
    constructor(context) {
        this.context = context;
        console.log('RE_ANALYSIS_MANAGER: Initializing Re-Analysis Manager');
        this.executePython = new executePython_1.ExecutePython(context);
        this.sseManager = SSEManager_1.SSEManager.getInstance();
    }
    /**
     * Ejecuta regeneración SOLO del data.json para una sesión
     * NO regenera archivos .html, .js, .css - solo data.json
     */
    async executeDataJsonRegeneration(session) {
        try {
            console.log(`RE_ANALYSIS_MANAGER: 🔬 Starting data.json regeneration for session ${session.id}`);
            console.log(`RE_ANALYSIS_MANAGER: Target: ${session.targetPath}`);
            console.log(`RE_ANALYSIS_MANAGER: Saved files path: ${session.savedFilesPath}`);
            // Verificar que la sesión tenga path de archivos guardados
            if (!session.savedFilesPath) {
                throw new Error(`Session ${session.id} does not have saved files path`);
            }
            // Verificar que el archivo objetivo existe
            if (!(await this.fileExists(session.targetPath))) {
                throw new Error(`Target file does not exist: ${session.targetPath}`);
            }
            // PASO 1: Ejecutar análisis Python para obtener nuevos datos
            console.log(`RE_ANALYSIS_MANAGER: 🐍 Step 1 - Executing Python analysis...`);
            const analysisData = await this.executePython.executeAnalysis(session);
            console.log(`RE_ANALYSIS_MANAGER: ✅ Python analysis completed successfully`);
            console.log(`RE_ANALYSIS_MANAGER: 📊 Analysis type: ${session.analysisMode}`);
            console.log(`RE_ANALYSIS_MANAGER: 📊 New analysis data received:`, {
                dataType: typeof analysisData,
                isArray: Array.isArray(analysisData),
                length: Array.isArray(analysisData) ? analysisData.length : 'N/A',
                hasSuccessProperty: analysisData && typeof analysisData === 'object' && 'success' in analysisData,
                success: analysisData?.success,
                target: analysisData?.target
            });
            // PASO 2: Determinar qué datos guardar según el tipo de análisis
            let dataToSave;
            if (session.analysisMode === 'XR') {
                // Para XR: Los datos del análisis SON directamente el array de funciones
                console.log(`RE_ANALYSIS_MANAGER: 📊 XR Analysis - Using analysis data directly as function array`);
                dataToSave = analysisData;
                if (Array.isArray(analysisData)) {
                    console.log(`RE_ANALYSIS_MANAGER: 📊 XR Data validation: Array with ${analysisData.length} functions`);
                    if (analysisData.length > 0) {
                        console.log(`RE_ANALYSIS_MANAGER: 📊 Sample function:`, {
                            functionName: analysisData[0].functionName,
                            complexity: analysisData[0].complexity,
                            fileName: analysisData[0].fileName
                        });
                    }
                }
                else {
                    console.warn(`RE_ANALYSIS_MANAGER: ⚠️ XR Analysis did not return array. Type: ${typeof analysisData}`);
                }
            }
            else {
                // Para LivePanel: Los datos del análisis son el objeto completo para data.json
                console.log(`RE_ANALYSIS_MANAGER: 📊 LivePanel Analysis - Using complete analysis object`);
                dataToSave = analysisData;
                if (analysisData && typeof analysisData === 'object') {
                    console.log(`RE_ANALYSIS_MANAGER: 📊 LivePanel Data keys:`, Object.keys(analysisData));
                }
            }
            // PASO 3: Convertir a JSON string
            const newDataJsonContent = JSON.stringify(dataToSave, null, 2);
            console.log(`RE_ANALYSIS_MANAGER: 📝 Generated new data.json content (${newDataJsonContent.length} characters)`);
            console.log(`RE_ANALYSIS_MANAGER: 📝 Content preview: ${newDataJsonContent.substring(0, 200)}${newDataJsonContent.length > 200 ? '...' : ''}`);
            // PASO 4: Guardar SOLO el data.json actualizado
            const dataJsonPath = path.join(session.savedFilesPath, 'data.json');
            console.log(`RE_ANALYSIS_MANAGER: 💾 Step 4 - Updating data.json file: ${dataJsonPath}`);
            await fs.writeFile(dataJsonPath, newDataJsonContent, 'utf-8');
            console.log(`RE_ANALYSIS_MANAGER: ✅ data.json updated successfully`);
            // PASO 5: Actualizar en memoria de la sesión (para compatibilidad)
            session.requiredFiles.set('data.json', newDataJsonContent);
            console.log(`RE_ANALYSIS_MANAGER: 📦 Updated data.json in session memory`);
            // PASO 6: Enviar notificación SSE para actualizar la vista
            console.log(`RE_ANALYSIS_MANAGER: 📡 Step 6 - Sending SSE notification...`);
            // Enviar el evento SSE correcto según el tipo de análisis
            if (session.analysisMode === 'XR') {
                // Para análisis XR, enviamos el evento dataRefresh específico
                console.log(`RE_ANALYSIS_MANAGER: 🥽 Sending dataRefresh event for XR analysis`);
                this.sseManager.sendDataRefresh(session.targetPath);
                console.log(`RE_ANALYSIS_MANAGER: ✅ dataRefresh event sent for XR analysis: ${session.targetPath}`);
            }
            else {
                // Para LivePanel u otros análisis, mantener el comportamiento anterior
                console.log(`RE_ANALYSIS_MANAGER: 🖥️ Sending standard update for LivePanel analysis`);
                this.sseManager.sendUpdate(session.targetPath);
                console.log(`RE_ANALYSIS_MANAGER: ✅ Standard SSE notification sent for LivePanel: ${session.targetPath}`);
            }
            // PASO 7: Log de resumen
            console.log(`RE_ANALYSIS_MANAGER: 🎉 Data.json regeneration completed successfully`);
            console.log(`RE_ANALYSIS_MANAGER: 📊 Summary:`);
            console.log(`RE_ANALYSIS_MANAGER:   - Session: ${session.id}`);
            console.log(`RE_ANALYSIS_MANAGER:   - Analysis Mode: ${session.analysisMode}`);
            console.log(`RE_ANALYSIS_MANAGER:   - Target: ${path.basename(session.targetPath)}`);
            console.log(`RE_ANALYSIS_MANAGER:   - Data size: ${newDataJsonContent.length} chars`);
            console.log(`RE_ANALYSIS_MANAGER:   - Updated file: ${dataJsonPath}`);
            console.log(`RE_ANALYSIS_MANAGER:   - Data type: ${session.analysisMode === 'XR' ? 'Function array' : 'Complete object'}`);
            console.log(`RE_ANALYSIS_MANAGER:   - SSE notification sent: ✅`);
            return true;
        }
        catch (error) {
            console.error(`RE_ANALYSIS_MANAGER: ❌ Error in data.json regeneration:`, error);
            console.error(`RE_ANALYSIS_MANAGER: Session: ${session.id}, Target: ${session.targetPath}`);
            return false;
        }
    }
    /**
     * Verifica si un archivo existe de forma asíncrona
     */
    async fileExists(filePath) {
        try {
            await fs.access(filePath);
            return true;
        }
        catch {
            return false;
        }
    }
    /**
     * Verifica si un directorio existe de forma asíncrona
     */
    async directoryExists(dirPath) {
        try {
            const stat = await fs.stat(dirPath);
            return stat.isDirectory();
        }
        catch {
            return false;
        }
    }
    /**
     * Obtiene información de la sesión para logging
     */
    getSessionInfo(session) {
        return `${session.analysisMode}/${session.targetType} - ${path.basename(session.targetPath)}`;
    }
}
exports.ReAnalysisManager = ReAnalysisManager;


/***/ }),
/* 96 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ExecutePython = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
const cp = __importStar(__webpack_require__(97));
const venvManager_1 = __webpack_require__(98);
/**
 * Utility para ejecutar análisis Python
 *
 * Esta clase:
 * - Recibe el tipo de análisis y sesión
 * - Determina qué script Python ejecutar
 * - Ejecuta el análisis y retorna los datos JSON
 */
class ExecutePython {
    venvManager;
    context;
    constructor(context) {
        console.log('EXECUTE_PYTHON: Initializing ExecutePython utility...');
        this.context = context;
        this.venvManager = new venvManager_1.VenvManager(context);
    }
    /**
     * Ejecuta análisis Python según el tipo de análisis
     *
     * @param session - Sesión de análisis unificada
     * @returns Promise con los datos JSON del análisis
     */
    async executeAnalysis(session) {
        console.log(`EXECUTE_PYTHON: 🐍 Starting Python analysis execution...`);
        console.log(`EXECUTE_PYTHON: Analysis mode: ${session.analysisMode}`);
        console.log(`EXECUTE_PYTHON: Target type: ${session.targetType}`);
        console.log(`EXECUTE_PYTHON: Target path: ${session.targetPath}`);
        // 📊 SHOW PROGRESS IN VS CODE UI
        return await vscode.window.withProgress({
            location: vscode.ProgressLocation.Notification,
            title: "CodeXR Analysis",
            cancellable: false
        }, async (progress, token) => {
            // Mostrar progreso inicial
            progress.report({
                increment: 0,
                message: `Starting ${session.analysisMode} analysis...`
            });
            try {
                // Determinar qué tipo de análisis ejecutar
                if (session.analysisMode === 'LivePanel' && session.targetType === 'file') {
                    console.log(`EXECUTE_PYTHON: 📄 File LivePanel analysis requested`);
                    return await this.executeFileAnalysis(session, progress);
                }
                else if (session.analysisMode === 'LivePanel' && session.targetType === 'directory') {
                    console.log(`EXECUTE_PYTHON: 📁 Directory LivePanel analysis requested`);
                    return await this.executeDirectoryAnalysis(session, progress);
                }
                else if (session.analysisMode === 'VisualizeDOM' && session.targetType === 'file') {
                    console.log(`EXECUTE_PYTHON: 🌐 HTML DOM analysis requested`);
                    return await this.executeHTMLDOMAnalysis(session, progress);
                }
                else if (session.analysisMode === 'XR') {
                    console.log(`EXECUTE_PYTHON: 🥽 XR analysis requested`);
                    return await this.executeXRAnalysis(session, progress);
                }
                else {
                    throw new Error(`Unknown analysis combination: ${session.analysisMode} + ${session.targetType}`);
                }
            }
            catch (error) {
                console.error(`EXECUTE_PYTHON: ❌ Error during Python analysis:`, error);
                progress.report({ message: "Analysis failed" });
                throw error;
            }
        });
    }
    /**
     * Ejecuta análisis de archivo
     */
    async executeFileAnalysis(session, progress) {
        console.log(`EXECUTE_PYTHON: 📄 Executing file analysis for: ${session.targetPath}`);
        try {
            // Preparar argumentos para análisis de archivo
            const args = [
                '--mode', 'livePanel',
                '--type', 'file',
                '--target', session.targetPath
            ];
            console.log(`EXECUTE_PYTHON: Executing file analysis with args: ${args.join(' ')}`);
            // Ejecutar el script Python
            const result = await this.executePythonScript('main.py', args, progress);
            // Para análisis de archivos, el resultado viene directamente sin wrapper
            if (result && (result.success !== false)) {
                console.log(`EXECUTE_PYTHON: ✅ File analysis completed successfully`);
                progress?.report({ message: "File analysis completed!" });
                return result;
            }
            else {
                console.error(`EXECUTE_PYTHON: ❌ File analysis failed:`, result);
                throw new Error(`File analysis failed: ${result?.error || 'Unknown error'}`);
            }
        }
        catch (error) {
            console.error(`EXECUTE_PYTHON: ❌ Error during file analysis:`, error);
            throw error;
        }
    }
    /**
     * Ejecuta análisis de directorio usando script Python unificado
     */
    async executeDirectoryAnalysis(session, progress) {
        console.log(`EXECUTE_PYTHON: 📁 Executing directory analysis...`);
        console.log(`EXECUTE_PYTHON: Will analyze directory: ${session.targetPath}`);
        console.log(`EXECUTE_PYTHON: Analysis mode: ${session.analysisMode}`);
        try {
            // Determinar si es análisis profundo basado en modo
            const isDeepAnalysis = session.analysisMode.includes('Deep');
            // Preparar argumentos para el script Python
            let args = [
                '--mode', 'livePanel',
                '--type', 'directory'
            ];
            // Usar archivos específicos si están disponibles
            if (session.filesToHash && session.filesToHash.length > 0) {
                console.log(`EXECUTE_PYTHON: Using specific file list (${session.filesToHash.length} files)`);
                progress?.report({ message: `Preparing to analyze ${session.filesToHash.length} files...` });
                args.push('--target', session.targetPath);
                // Extraer las rutas de archivo de los FileHash
                const filePaths = session.filesToHash.map(fh => fh.filePath);
                args.push('--files', ...filePaths);
            }
            else {
                console.log(`EXECUTE_PYTHON: Using directory scanning mode`);
                progress?.report({ message: `Scanning directory for files...` });
                args.push('--target', session.targetPath);
            }
            // Agregar flag de análisis profundo si es necesario
            if (isDeepAnalysis) {
                console.log(`EXECUTE_PYTHON: Deep analysis requested`);
                args.push('--deep');
            }
            console.log(`EXECUTE_PYTHON: Executing with args: ${args.join(' ')}`);
            // Ejecutar el script Python
            const result = await this.executePythonScript('main.py', args, progress);
            // Para análisis de directorios, el resultado también viene directamente sin wrapper
            if (result && (result.success !== false)) {
                console.log(`EXECUTE_PYTHON: ✅ Directory analysis completed successfully`);
                progress?.report({ message: "Directory analysis completed!" });
                return result;
            }
            else {
                console.error(`EXECUTE_PYTHON: ❌ Directory analysis failed:`, result);
                throw new Error(`Directory analysis failed: ${result?.error || 'Unknown error'}`);
            }
        }
        catch (error) {
            console.error(`EXECUTE_PYTHON: ❌ Error during directory analysis:`, error);
            throw error;
        }
    }
    /**
     * Ejecuta análisis XR
     */
    async executeXRAnalysis(session, progress) {
        console.log(`EXECUTE_PYTHON: 🥽 Executing XR analysis for: ${session.targetPath}`);
        console.log(`EXECUTE_PYTHON: Target type: ${session.targetType}`);
        try {
            // Preparar argumentos para el análisis XR usando main.py
            const args = [
                '--mode', 'xr',
                '--type', session.targetType,
                '--target', session.targetPath
            ];
            // Para análisis de directorio, usar archivos filtrados si están disponibles
            if (session.targetType === 'directory' && session.filesToHash && session.filesToHash.length > 0) {
                console.log(`EXECUTE_PYTHON: XR_ANALYSIS: Using filtered file list (${session.filesToHash.length} files)`);
                progress?.report({ message: `Preparing to analyze ${session.filesToHash.length} filtered files...` });
                // Extraer las rutas de archivo de los FileHash (archivos ya filtrados)
                const filePaths = session.filesToHash.map(fh => fh.filePath);
                args.push('--files', ...filePaths);
                console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 🎯 Using ${filePaths.length} filtered files instead of scanning entire directory`);
                console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 📁 Sample files:`, filePaths.slice(0, 3).map(fp => path.basename(fp)));
            }
            else if (session.targetType === 'directory') {
                console.log(`EXECUTE_PYTHON: XR_ANALYSIS: ⚠️ No filtered files available, will scan directory (less efficient)`);
                progress?.report({ message: `Scanning directory for files...` });
            }
            // Add --deep flag for deep directory analysis
            if (session.targetType === 'directory' && session.isDeep) {
                console.log(`EXECUTE_PYTHON: XR_ANALYSIS: Adding --deep flag for deep directory analysis`);
                args.push('--deep');
            }
            console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 🚀 Starting XR analysis with main.py`);
            console.log(`EXECUTE_PYTHON: XR_ANALYSIS: Args: ${args.join(' ')}`);
            progress?.report({ message: "Starting XR analysis..." });
            // Ejecutar usando main.py (punto de entrada unificado)
            console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 🐍 Executing main.py with XR parameters`);
            const result = await this.executePythonScript('main.py', args, progress);
            console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 📦 Raw Python result received:`, {
                resultType: typeof result,
                isArray: Array.isArray(result),
                isObject: typeof result === 'object',
                hasSuccessProperty: result && typeof result === 'object' && 'success' in result,
                hasErrorProperty: result && typeof result === 'object' && 'error' in result,
                success: result?.success,
                error: result?.error,
                rawOutputLength: result?.rawOutput?.length || 0
            });
            // Verificar si es un error con rawOutput JSON válido (caso de marcadores faltantes)
            if (result?.success === false && result?.rawOutput && result?.error?.includes('JSON markers not found')) {
                console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 🔄 JSON markers missing, attempting to parse rawOutput directly...`);
                console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 📄 Raw output preview: ${result.rawOutput.substring(0, 200)}${result.rawOutput.length > 200 ? '...' : ''}`);
                try {
                    const parsedData = JSON.parse(result.rawOutput);
                    console.log(`EXECUTE_PYTHON: XR_ANALYSIS: ✅ Successfully parsed rawOutput as direct JSON!`);
                    console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 📊 Parsed data type:`, typeof parsedData);
                    console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 📊 Is array:`, Array.isArray(parsedData));
                    if (Array.isArray(parsedData)) {
                        console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 🎯 Generated ${parsedData.length} XR function records`);
                        if (parsedData.length > 0) {
                            console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 📋 Sample function data:`, {
                                functionName: parsedData[0].functionName,
                                complexity: parsedData[0].complexity,
                                parameters: parsedData[0].parameters,
                                lineCount: parsedData[0].lineCount,
                                fileName: parsedData[0].fileName
                            });
                        }
                        console.log(`EXECUTE_PYTHON: XR_ANALYSIS: ✅ Successfully completed analysis for ${session.targetPath}`);
                    }
                    progress?.report({ message: "XR analysis completed!" });
                    return parsedData;
                }
                catch (parseError) {
                    console.error(`EXECUTE_PYTHON: XR_ANALYSIS: ❌ Failed to parse rawOutput as JSON:`, parseError);
                    console.error(`EXECUTE_PYTHON: XR_ANALYSIS: 📄 Raw output content:`, result.rawOutput);
                    throw new Error(`XR analysis failed: Unable to parse result - ${result.error}`);
                }
            }
            // Verificar el resultado directo (sin error wrapper)
            if (result && result?.success !== false) {
                console.log(`EXECUTE_PYTHON: XR_ANALYSIS: ✅ Direct result received (no error wrapper)`);
                console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 📊 Analysis result details:`, {
                    functionsCount: Array.isArray(result) ? result.length : 0,
                    isArray: Array.isArray(result),
                    resultType: typeof result
                });
                // Para XR file analysis, el resultado debería ser un array de funciones
                if (Array.isArray(result)) {
                    console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 🎯 Generated ${result.length} XR function records`);
                    console.log(`EXECUTE_PYTHON: XR_ANALYSIS: ✅ Successfully completed analysis for ${session.targetPath}`);
                    if (result.length > 0) {
                        console.log(`EXECUTE_PYTHON: XR_ANALYSIS: 📋 Sample function:`, {
                            functionName: result[0].functionName,
                            complexity: result[0].complexity,
                            parameters: result[0].parameters,
                            lineCount: result[0].lineCount,
                            fileName: result[0].fileName
                        });
                    }
                }
                else {
                    console.log(`EXECUTE_PYTHON: XR_ANALYSIS: ⚠️ Result is not an array, type: ${typeof result}`);
                }
                progress?.report({ message: "XR analysis completed!" });
                return result;
            }
            else {
                console.error(`EXECUTE_PYTHON: XR_ANALYSIS: ❌ XR analysis failed with error:`, result);
                throw new Error(`XR analysis failed: ${result?.error || 'Unknown error'}`);
            }
        }
        catch (error) {
            console.error(`EXECUTE_PYTHON: XR_ANALYSIS: ❌ Error during XR analysis:`, error);
            throw error;
        }
    }
    /**
     * Ejecuta análisis HTML DOM usando html_dom_parser.py
     */
    async executeHTMLDOMAnalysis(session, progress) {
        console.log(`EXECUTE_PYTHON: 🌐 Executing HTML DOM analysis for: ${session.targetPath}`);
        try {
            // Preparar argumentos para el análisis HTML DOM
            const args = [session.targetPath];
            console.log(`EXECUTE_PYTHON: Executing HTML DOM analysis with args: ${args.join(' ')}`);
            progress?.report({ message: "Parsing HTML DOM..." });
            // Ejecutar el script Python específico para HTML DOM
            const result = await this.executePythonScript('html/html_dom_parser.py', args, progress);
            // Verificar el resultado
            if (result && !result.error) {
                console.log(`EXECUTE_PYTHON: ✅ HTML DOM analysis completed successfully`);
                console.log(`EXECUTE_PYTHON: 📄 Processed HTML content length: ${result.htmlContent?.length || 0} chars`);
                console.log(`EXECUTE_PYTHON: 📋 Analysis result:`, {
                    originalFile: result.originalFile,
                    preparedForVisualization: result.preparedForVisualization,
                    htmlContentLength: result.htmlContent?.length || 0
                });
                progress?.report({ message: "HTML DOM analysis completed!" });
                return result;
            }
            else {
                console.error(`EXECUTE_PYTHON: ❌ HTML DOM analysis failed:`, result);
                throw new Error(`HTML DOM analysis failed: ${result?.error || 'Unknown error'}`);
            }
        }
        catch (error) {
            console.error(`EXECUTE_PYTHON: ❌ Error during HTML DOM analysis:`, error);
            throw error;
        }
    }
    /**
     * Ejecuta re-análisis de archivos específicos usando file_reanalysis_coordinator.py
     */
    async executeFileReanalysis(filePaths) {
        console.log(`EXECUTE_PYTHON: 🔄 Starting file re-analysis for ${filePaths.length} files`);
        try {
            // Preparar argumentos para el script de re-análisis
            const args = [
                ...filePaths // Pasar directamente las rutas de archivos
            ];
            console.log(`EXECUTE_PYTHON: Executing file re-analysis with args: ${args.join(' ')}`);
            // Ejecutar el script Python
            const result = await this.executePythonScript('sumaryFiles/file_reanalysis_coordinator.py', args);
            // Para re-análisis, el resultado viene directamente como array
            if (result && Array.isArray(result)) {
                console.log(`EXECUTE_PYTHON: ✅ File re-analysis completed successfully for ${result.length} files`);
                return result;
            }
            else if (result && (result.success !== false)) {
                // Si viene un solo archivo, convertir a array
                console.log(`EXECUTE_PYTHON: ✅ File re-analysis completed successfully for single file`);
                return [result];
            }
            else {
                console.error(`EXECUTE_PYTHON: ❌ File re-analysis failed:`, result);
                throw new Error(`File re-analysis failed: ${result?.error || 'Unknown error'}`);
            }
        }
        catch (error) {
            console.error(`EXECUTE_PYTHON: ❌ Error during file re-analysis:`, error);
            throw error;
        }
    }
    /**
     * Ejecuta un script Python específico con argumentos y reporte de progreso
     */
    async executePythonScript(scriptName, args, progress) {
        console.log(`EXECUTE_PYTHON: 🐍 Executing Python script: ${scriptName}`);
        console.log(`EXECUTE_PYTHON: Script arguments: ${args.join(' ')}`);
        // 📊 ENHANCED ANALYSIS START LOGGING
        console.log(`EXECUTE_PYTHON: ═══════════════════════════════════════════════════════════`);
        console.log(`EXECUTE_PYTHON: 🚀 STARTING ANALYSIS`);
        console.log(`EXECUTE_PYTHON: 📄 Script: ${scriptName}`);
        console.log(`EXECUTE_PYTHON: 🎯 Target: ${args.includes('--target') ? args[args.indexOf('--target') + 1] : 'Multiple targets'}`);
        console.log(`EXECUTE_PYTHON: 🔧 Mode: ${args.includes('--mode') ? args[args.indexOf('--mode') + 1] : 'Unknown'}`);
        console.log(`EXECUTE_PYTHON: 📂 Type: ${args.includes('--type') ? args[args.indexOf('--type') + 1] : 'Unknown'}`);
        console.log(`EXECUTE_PYTHON: ═══════════════════════════════════════════════════════════`);
        try {
            // Get Python executable path from virtual environment
            const pythonExecutable = this.venvManager.getPythonExecutablePath();
            if (!pythonExecutable) {
                throw new Error('Python executable not found in virtual environment');
            }
            console.log(`EXECUTE_PYTHON: Using Python executable: ${pythonExecutable}`);
            // Verify the Python executable exists and is accessible
            if (!(__webpack_require__(6).existsSync)(pythonExecutable)) {
                throw new Error(`Python executable does not exist: ${pythonExecutable}`);
            }
            console.log(`EXECUTE_PYTHON: ✅ Python executable verified to exist`);
            // Get script path - check both dist and src directories for compatibility
            const extensionPath = this.context.extensionPath;
            // Try dist directory first (production build)
            let scriptPath = path.join(extensionPath, 'dist', 'new_code_analysis', 'new_python', scriptName);
            // If script doesn't exist in dist, try src directory (development)
            if (!(__webpack_require__(6).existsSync)(scriptPath)) {
                scriptPath = path.join(extensionPath, 'src', 'new_code_analysis', 'new_python', scriptName);
                console.log(`EXECUTE_PYTHON: Script not found in dist, trying src directory`);
            }
            console.log(`EXECUTE_PYTHON: Script path: ${scriptPath}`);
            // Verify the script exists
            if (!(__webpack_require__(6).existsSync)(scriptPath)) {
                // Additional diagnostic information
                const distPath = path.join(extensionPath, 'dist', 'new_code_analysis', 'new_python', scriptName);
                const srcPath = path.join(extensionPath, 'src', 'new_code_analysis', 'new_python', scriptName);
                console.error(`EXECUTE_PYTHON: ❌ Python script not found in either location:`);
                console.error(`EXECUTE_PYTHON: 📁 Dist path: ${distPath} (exists: ${(__webpack_require__(6).existsSync)(distPath)})`);
                console.error(`EXECUTE_PYTHON: 📁 Src path: ${srcPath} (exists: ${(__webpack_require__(6).existsSync)(srcPath)})`);
                console.error(`EXECUTE_PYTHON: 📁 Extension path: ${extensionPath}`);
                // List contents of directories for debugging
                try {
                    const distNewCodeAnalysisPath = path.join(extensionPath, 'dist', 'new_code_analysis');
                    if ((__webpack_require__(6).existsSync)(distNewCodeAnalysisPath)) {
                        const distContents = (__webpack_require__(6).readdirSync)(distNewCodeAnalysisPath);
                        console.error(`EXECUTE_PYTHON: 📂 dist/new_code_analysis contents: ${distContents.join(', ')}`);
                    }
                    const distNewPythonPath = path.join(extensionPath, 'dist', 'new_code_analysis', 'new_python');
                    if ((__webpack_require__(6).existsSync)(distNewPythonPath)) {
                        const distPythonContents = (__webpack_require__(6).readdirSync)(distNewPythonPath);
                        console.error(`EXECUTE_PYTHON: 📂 dist/new_code_analysis/new_python contents: ${distPythonContents.join(', ')}`);
                    }
                }
                catch (listError) {
                    console.error(`EXECUTE_PYTHON: Failed to list directory contents:`, listError);
                }
                throw new Error(`Python script does not exist: ${scriptPath}`);
            }
            console.log(`EXECUTE_PYTHON: ✅ Python script verified to exist`);
            // Build full command
            const fullArgs = [scriptPath, ...args];
            console.log(`EXECUTE_PYTHON: Full command: ${pythonExecutable} ${fullArgs.join(' ')}`);
            console.log(`EXECUTE_PYTHON: 🚀 About to spawn Python process...`);
            return new Promise((resolve, reject) => {
                console.log(`EXECUTE_PYTHON: Creating child process with spawn...`);
                console.log(`EXECUTE_PYTHON: Executable: "${pythonExecutable}"`);
                console.log(`EXECUTE_PYTHON: Arguments: [${fullArgs.map(arg => `"${arg}"`).join(', ')}]`);
                console.log(`EXECUTE_PYTHON: Working directory: ${path.dirname(scriptPath)}`);
                const process = cp.spawn(pythonExecutable, fullArgs, {
                    cwd: path.dirname(scriptPath),
                    stdio: ['pipe', 'pipe', 'pipe']
                });
                console.log(`EXECUTE_PYTHON: ✅ Process spawned with PID: ${process.pid}`);
                // Handle process creation errors
                process.on('error', (error) => {
                    console.error(`EXECUTE_PYTHON: ❌ Failed to start Python process:`, error);
                    console.error(`EXECUTE_PYTHON: Error details:`, {
                        code: error.code,
                        errno: error.errno,
                        syscall: error.syscall,
                        path: error.path
                    });
                    resolve({
                        success: false,
                        error: `Failed to start Python process: ${error}`,
                        rawOutput: ''
                    });
                });
                let stdoutData = '';
                let stderrData = '';
                process.stdout.on('data', (data) => {
                    stdoutData += data.toString();
                    console.log(`EXECUTE_PYTHON: 📤 stdout chunk received (${data.toString().length} chars)`);
                });
                process.stderr.on('data', (data) => {
                    const stderrLine = data.toString();
                    stderrData += stderrLine;
                    // Log Python progress and debug messages
                    const lines = stderrLine.split('\n');
                    for (const line of lines) {
                        if (line.trim()) {
                            try {
                                const parsed = JSON.parse(line.trim());
                                if (parsed.progress) {
                                    // 📊 ENHANCED PROGRESS DISPLAY
                                    const current = parsed.progress.current || 0;
                                    const total = parsed.progress.total || 0;
                                    const percentage = parsed.progress.percentage || 0;
                                    const fileName = parsed.progress.fileName || parsed.progress.file || parsed.progress.current_file || 'Processing file...';
                                    const message = parsed.progress.message || 'Processing';
                                    console.log(`EXECUTE_PYTHON: 🔄 PROGRESS [${current}/${total}] (${percentage}%) - ${message}`);
                                    console.log(`EXECUTE_PYTHON: 📄 Current file: ${fileName}`);
                                    // 🎯 UPDATE VS CODE UI PROGRESS
                                    if (progress && total > 0) {
                                        const increment = percentage > 0 ? 100 / total : 0;
                                        // Mostrar el nombre del archivo completo para más información al usuario
                                        const displayFileName = fileName.includes('/') ? fileName.split('/').pop() : fileName;
                                        progress.report({
                                            message: `${current}/${total} (${percentage}%) - ${displayFileName}`,
                                            increment: increment
                                        });
                                    }
                                    // Additional context if available
                                    if (parsed.progress.operation) {
                                        console.log(`EXECUTE_PYTHON: ⚙️  Operation: ${parsed.progress.operation}`);
                                    }
                                    if (parsed.progress.estimatedTime) {
                                        console.log(`EXECUTE_PYTHON: ⏱️  Estimated time remaining: ${parsed.progress.estimatedTime}`);
                                    }
                                }
                                else if (parsed.debug) {
                                    console.log(`EXECUTE_PYTHON: 🐛 DEBUG: ${parsed.debug}`);
                                }
                                else if (parsed.info) {
                                    console.log(`EXECUTE_PYTHON: ℹ️  INFO: ${parsed.info}`);
                                    // Update UI with info messages
                                    if (progress) {
                                        progress.report({ message: parsed.info });
                                    }
                                }
                                else if (parsed.warning) {
                                    console.log(`EXECUTE_PYTHON: ⚠️  WARNING: ${parsed.warning}`);
                                }
                                else if (parsed.error) {
                                    console.log(`EXECUTE_PYTHON: ❌ ERROR: ${parsed.error}`);
                                }
                            }
                            catch {
                                // Not JSON, check for common progress patterns
                                const cleanLine = line.trim();
                                if (cleanLine) {
                                    // Look for common progress patterns
                                    if (cleanLine.includes('Analyzing') || cleanLine.includes('Processing')) {
                                        console.log(`EXECUTE_PYTHON: 🔍 ${cleanLine}`);
                                        if (progress) {
                                            progress.report({ message: cleanLine });
                                        }
                                    }
                                    else if (cleanLine.includes('Progress:') || cleanLine.includes('%')) {
                                        console.log(`EXECUTE_PYTHON: 📊 ${cleanLine}`);
                                        if (progress) {
                                            progress.report({ message: cleanLine });
                                        }
                                    }
                                    else if (cleanLine.includes('ERROR') || cleanLine.includes('Error')) {
                                        console.log(`EXECUTE_PYTHON: ❌ ${cleanLine}`);
                                    }
                                    else if (cleanLine.includes('WARNING') || cleanLine.includes('Warning')) {
                                        console.log(`EXECUTE_PYTHON: ⚠️  ${cleanLine}`);
                                    }
                                    else {
                                        console.log(`EXECUTE_PYTHON: 📝 ${cleanLine}`);
                                    }
                                }
                            }
                        }
                    }
                });
                process.on('close', (code) => {
                    // 📊 ENHANCED ANALYSIS COMPLETION LOGGING
                    console.log(`EXECUTE_PYTHON: ═══════════════════════════════════════════════════════════`);
                    console.log(`EXECUTE_PYTHON: 🏁 ANALYSIS COMPLETED`);
                    console.log(`EXECUTE_PYTHON: 📄 Script: ${scriptName}`);
                    console.log(`EXECUTE_PYTHON: 🔢 Exit code: ${code}`);
                    console.log(`EXECUTE_PYTHON: 📤 Output size: ${stdoutData.length} chars`);
                    console.log(`EXECUTE_PYTHON: 📥 Error output size: ${stderrData.length} chars`);
                    if (code === 0) {
                        console.log(`EXECUTE_PYTHON: ✅ SUCCESS - Processing results...`);
                        try {
                            // Parse JSON between markers
                            const jsonStartMarker = '=== JSON_START ===';
                            const jsonEndMarker = '=== JSON_END ===';
                            const startIndex = stdoutData.indexOf(jsonStartMarker);
                            const endIndex = stdoutData.indexOf(jsonEndMarker);
                            if (startIndex >= 0 && endIndex >= 0) {
                                const jsonContent = stdoutData.substring(startIndex + jsonStartMarker.length, endIndex).trim();
                                const result = JSON.parse(jsonContent);
                                // 📊 ENHANCED SUCCESS LOGGING
                                console.log(`EXECUTE_PYTHON: ✅ Successfully parsed Python result`);
                                if (Array.isArray(result)) {
                                    console.log(`EXECUTE_PYTHON: 📊 Result type: Array with ${result.length} items`);
                                }
                                else if (typeof result === 'object' && result !== null) {
                                    console.log(`EXECUTE_PYTHON: 📊 Result type: Object`);
                                    if (result.files && Array.isArray(result.files)) {
                                        console.log(`EXECUTE_PYTHON: 📁 Files analyzed: ${result.files.length}`);
                                    }
                                    if (result.summary) {
                                        console.log(`EXECUTE_PYTHON: 📈 Summary available: Yes`);
                                    }
                                }
                                console.log(`EXECUTE_PYTHON: ═══════════════════════════════════════════════════════════`);
                                resolve(result);
                            }
                            else {
                                console.warn(`EXECUTE_PYTHON: ⚠️ JSON markers not found in output`);
                                console.log(`EXECUTE_PYTHON: ═══════════════════════════════════════════════════════════`);
                                resolve({
                                    success: false,
                                    error: 'JSON markers not found in Python output',
                                    rawOutput: stdoutData
                                });
                            }
                        }
                        catch (parseError) {
                            console.error(`EXECUTE_PYTHON: ❌ Failed to parse Python JSON output:`, parseError);
                            console.log(`EXECUTE_PYTHON: ═══════════════════════════════════════════════════════════`);
                            resolve({
                                success: false,
                                error: `Failed to parse Python output: ${parseError}`,
                                rawOutput: stdoutData
                            });
                        }
                    }
                    else {
                        console.error(`EXECUTE_PYTHON: ❌ FAILED - Python script failed with exit code ${code}`);
                        if (stderrData.trim()) {
                            console.error(`EXECUTE_PYTHON: 📥 Error output preview: ${stderrData.substring(0, 200)}${stderrData.length > 200 ? '...' : ''}`);
                        }
                        console.log(`EXECUTE_PYTHON: ═══════════════════════════════════════════════════════════`);
                        resolve({
                            success: false,
                            error: `Python script failed with exit code ${code}`,
                            stderr: stderrData,
                            stdout: stdoutData
                        });
                    }
                });
                process.on('error', (error) => {
                    console.error(`EXECUTE_PYTHON: ❌ Failed to start Python process:`, error);
                    reject(new Error(`Failed to start Python process: ${error.message}`));
                });
            });
        }
        catch (error) {
            console.error(`EXECUTE_PYTHON: ❌ Error executing Python script:`, error);
            throw error;
        }
    }
}
exports.ExecutePython = ExecutePython;


/***/ }),
/* 97 */
/***/ ((module) => {

module.exports = require("child_process");

/***/ }),
/* 98 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VenvManager = void 0;
const vscode = __importStar(__webpack_require__(1));
const cp = __importStar(__webpack_require__(97));
const pythonEnvStorage_1 = __webpack_require__(99);
const pythonEnvUtils_1 = __webpack_require__(100);
/**
 * Core virtual environment management functionality
 */
class VenvManager {
    storage;
    context;
    constructor(context) {
        this.context = context;
        this.storage = new pythonEnvStorage_1.PythonEnvStorage(context);
        console.log('PYTHON_ENV: VenvManager initialized');
    }
    /**
     * Initialize the Python environment on extension startup
     */
    async initializeEnvironment() {
        console.log('PYTHON_ENV: Initializing Python environment...');
        try {
            // Check if environment already exists and is valid
            const metadata = this.storage.loadMetadata();
            const venvExists = this.storage.isVenvValid();
            if (metadata && venvExists) {
                console.log('PYTHON_ENV: Existing valid environment found');
                await this.activateEnvironment();
                // Check if lizard is available and install if missing
                await this.ensureLizardAvailable();
                await this.storage.updateValidation();
                return;
            }
            // Environment doesn't exist or is invalid - create new one
            console.log('PYTHON_ENV: No valid environment found, creating new one...');
            await this.createEnvironment();
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to initialize environment:', error);
            vscode.window.showErrorMessage(`Failed to initialize Python environment: ${error}`);
        }
    }
    /**
     * Create a new virtual environment
     */
    async createEnvironment() {
        console.log('PYTHON_ENV: Creating new virtual environment...');
        try {
            // Show progress to user
            await vscode.window.withProgress({
                location: vscode.ProgressLocation.Notification,
                title: "Creating Python Virtual Environment",
                cancellable: false
            }, async (progress) => {
                progress.report({ increment: 0, message: "Checking Python installation..." });
                // Verify Python is available
                const pythonCommand = pythonEnvUtils_1.PythonEnvUtils.getPythonCommand();
                const pythonVersion = await this.checkPythonVersion(pythonCommand);
                if (!pythonVersion) {
                    throw new Error(`Python not found. Please install Python 3.7+ and ensure '${pythonCommand}' is in your PATH.`);
                }
                progress.report({ increment: 30, message: `Found Python ${pythonVersion}, creating environment...` });
                // Create virtual environment
                const venvPath = this.storage.getVenvPath();
                await this.executeCommand(`${pythonCommand} -m venv "${venvPath}"`);
                progress.report({ increment: 60, message: "Installing base packages..." });
                // Install basic packages
                await this.installBasePackages();
                progress.report({ increment: 90, message: "Saving environment metadata..." });
                // Save metadata
                const metadata = this.storage.createInitialMetadata(pythonVersion);
                await this.storage.saveMetadata(metadata);
                progress.report({ increment: 100, message: "Environment created successfully!" });
            });
            console.log('PYTHON_ENV: Virtual environment created successfully');
            vscode.window.showInformationMessage('Python virtual environment created successfully!');
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to create environment:', error);
            throw error;
        }
    }
    /**
     * Activate the virtual environment
     */
    async activateEnvironment() {
        try {
            const venvPath = this.storage.getVenvPath();
            if (!pythonEnvUtils_1.PythonEnvUtils.isValidVenv(venvPath)) {
                throw new Error('Virtual environment is not valid');
            }
            console.log(`PYTHON_ENV: Activating environment at ${venvPath}`);
            // Update metadata to mark as active
            const metadata = this.storage.loadMetadata();
            if (metadata) {
                metadata.isActive = true;
                await this.storage.saveMetadata(metadata);
            }
            console.log('PYTHON_ENV: Environment activated successfully');
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to activate environment:', error);
            throw error;
        }
    }
    /**
     * Delete the virtual environment
     */
    async deleteEnvironment() {
        console.log('PYTHON_ENV: Deleting virtual environment...');
        try {
            const result = await vscode.window.showWarningMessage('Are you sure you want to delete the Python virtual environment? This action cannot be undone.', { modal: true }, 'Delete Environment');
            if (result !== 'Delete Environment') {
                console.log('PYTHON_ENV: Environment deletion cancelled by user');
                return;
            }
            await vscode.window.withProgress({
                location: vscode.ProgressLocation.Notification,
                title: "Deleting Python Virtual Environment",
                cancellable: false
            }, async (progress) => {
                progress.report({ increment: 50, message: "Removing environment files..." });
                await this.storage.deleteEnvironment();
                progress.report({ increment: 100, message: "Environment deleted successfully!" });
            });
            console.log('PYTHON_ENV: Environment deleted successfully');
            vscode.window.showInformationMessage('Python virtual environment deleted successfully!');
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to delete environment:', error);
            vscode.window.showErrorMessage(`Failed to delete environment: ${error}`);
        }
    }
    /**
     * Install a package in the virtual environment
     */
    async installPackage(packageName) {
        console.log(`PYTHON_ENV: Installing package: ${packageName}`);
        try {
            const venvPath = this.storage.getVenvPath();
            const pipPath = pythonEnvUtils_1.PythonEnvUtils.getVenvPipPath(venvPath);
            await vscode.window.withProgress({
                location: vscode.ProgressLocation.Notification,
                title: `Installing ${packageName}`,
                cancellable: false
            }, async (progress) => {
                progress.report({ increment: 0, message: "Installing package..." });
                await this.executeCommand(`"${pipPath}" install ${packageName}`);
                progress.report({ increment: 80, message: "Updating dependencies list..." });
                // Update dependencies in metadata
                await this.updateDependenciesList();
                progress.report({ increment: 100, message: "Package installed successfully!" });
            });
            console.log(`PYTHON_ENV: Package ${packageName} installed successfully`);
            vscode.window.showInformationMessage(`Package ${packageName} installed successfully!`);
        }
        catch (error) {
            console.error(`PYTHON_ENV: Failed to install package ${packageName}:`, error);
            vscode.window.showErrorMessage(`Failed to install package ${packageName}: ${error}`);
        }
    }
    /**
     * Get environment status information
     */
    getEnvironmentStatus() {
        const metadata = this.storage.loadMetadata();
        const isValid = this.storage.isVenvValid();
        const stats = this.storage.getStorageStats();
        return {
            exists: metadata !== null,
            isValid: isValid,
            metadata: metadata,
            stats: stats
        };
    }
    /**
     * Get the Python executable path for external use
     */
    getPythonExecutablePath() {
        const venvPath = this.storage.getVenvPath();
        if (!pythonEnvUtils_1.PythonEnvUtils.isValidVenv(venvPath)) {
            return null;
        }
        return pythonEnvUtils_1.PythonEnvUtils.getVenvPythonPath(venvPath);
    }
    /**
     * Get the lizard executable command for external use
     */
    getLizardCommand() {
        const pythonPath = this.getPythonExecutablePath();
        if (!pythonPath) {
            return null;
        }
        // Return the command to run lizard using the virtual environment's Python
        return `"${pythonPath}" -m lizard`;
    }
    /**
     * Verify that lizard is working correctly in the environment
     */
    async verifyLizardInstallation() {
        try {
            const pythonPath = this.getPythonExecutablePath();
            if (!pythonPath) {
                return false;
            }
            // Test lizard by running it with --version flag
            await this.executeCommand(`"${pythonPath}" -m lizard --version`);
            return true;
        }
        catch (error) {
            console.log('PYTHON_ENV: Lizard verification failed:', error);
            return false;
        }
    }
    /**
     * Execute a command and return the result
     */
    async executeCommand(command, cwd) {
        return new Promise((resolve, reject) => {
            console.log(`PYTHON_ENV: Executing command: ${command}`);
            const options = {
                cwd: cwd || process.cwd(),
                timeout: 60000, // 60 second timeout
                maxBuffer: 1024 * 1024 // 1MB buffer
            };
            cp.exec(command, options, (error, stdout, stderr) => {
                if (error) {
                    console.error(`PYTHON_ENV: Command failed: ${command}`, error);
                    console.error(`PYTHON_ENV: stderr: ${stderr}`);
                    reject(new Error(`Command failed: ${error.message}`));
                    return;
                }
                console.log(`PYTHON_ENV: Command completed successfully: ${command}`);
                if (stdout) {
                    console.log(`PYTHON_ENV: stdout: ${stdout.trim()}`);
                }
                resolve(stdout.trim());
            });
        });
    }
    /**
     * Check Python version
     */
    async checkPythonVersion(pythonCommand) {
        try {
            const output = await this.executeCommand(`${pythonCommand} --version`);
            const versionMatch = output.match(/Python\s+(\d+\.\d+\.\d+)/);
            return versionMatch ? versionMatch[1] : null;
        }
        catch (error) {
            console.log(`PYTHON_ENV: Could not get Python version with '${pythonCommand}':`, error);
            return null;
        }
    }
    /**
     * Install base packages in the virtual environment
     */
    async installBasePackages() {
        try {
            const venvPath = this.storage.getVenvPath();
            const pipPath = pythonEnvUtils_1.PythonEnvUtils.getVenvPipPath(venvPath);
            // Upgrade pip first
            console.log('PYTHON_ENV: Upgrading pip...');
            await this.executeCommand(`"${pipPath}" install --upgrade pip`);
            // Install essential packages
            const basePackages = [
                'setuptools',
                'wheel',
                'requests' // Common package for HTTP requests
            ];
            for (const pkg of basePackages) {
                console.log(`PYTHON_ENV: Installing ${pkg}...`);
                await this.executeCommand(`"${pipPath}" install ${pkg}`);
            }
            // Install lizard for code complexity analysis
            await this.installLizardPackage();
            // Update dependencies list
            await this.updateDependenciesList();
            console.log('PYTHON_ENV: Base packages installed successfully');
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to install base packages:', error);
            throw error;
        }
    }
    /**
     * Ensure lizard package is available in the environment
     */
    async ensureLizardAvailable() {
        try {
            console.log('PYTHON_ENV: Checking lizard availability in existing environment...');
            const isInstalled = await this.isPackageInstalled('lizard');
            if (!isInstalled) {
                console.log('PYTHON_ENV: Lizard not found in existing environment, installing...');
                await this.installLizardPackage();
                await this.updateDependenciesList();
            }
            else {
                console.log('PYTHON_ENV: Lizard is already installed in the environment');
            }
        }
        catch (error) {
            console.warn('PYTHON_ENV: Failed to ensure lizard availability:', error);
            // Don't throw - this shouldn't break the environment initialization
        }
    }
    /**
     * Install lizard package for code complexity analysis
     */
    async installLizardPackage() {
        try {
            console.log('PYTHON_ENV: Installing lizard package for code complexity analysis...');
            const venvPath = this.storage.getVenvPath();
            const pythonPath = pythonEnvUtils_1.PythonEnvUtils.getVenvPythonPath(venvPath);
            // Check if lizard is already installed
            const isInstalled = await this.isPackageInstalled('lizard');
            if (isInstalled) {
                console.log('PYTHON_ENV: Lizard is already installed - skipping installation');
                return;
            }
            // Install lizard using the virtual environment's Python interpreter
            console.log('PYTHON_ENV: Installing lizard via pip...');
            await this.executeCommand(`"${pythonPath}" -m pip install lizard`);
            console.log('PYTHON_ENV: Lizard has been installed successfully');
            // Verify installation by testing lizard import
            try {
                const output = await this.executeCommand(`"${pythonPath}" -c "import lizard; print('Lizard version:', lizard.__version__)"`);
                console.log('PYTHON_ENV: Lizard installation verified successfully -', output.trim());
            }
            catch (verifyError) {
                console.warn('PYTHON_ENV: Lizard installation succeeded but verification failed:', verifyError);
            }
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to install lizard package:', error);
            // Don't throw the error - lizard installation failure shouldn't break the entire environment setup
            console.warn('PYTHON_ENV: Continuing environment setup despite lizard installation failure');
        }
    }
    /**
     * Check if a package is installed in the virtual environment
     */
    async isPackageInstalled(packageName) {
        try {
            const venvPath = this.storage.getVenvPath();
            const pythonPath = pythonEnvUtils_1.PythonEnvUtils.getVenvPythonPath(venvPath);
            // Try to import the package to check if it's installed
            await this.executeCommand(`"${pythonPath}" -c "import ${packageName}"`);
            return true;
        }
        catch (error) {
            // If import fails, package is not installed
            return false;
        }
    }
    /**
     * Update the dependencies list in metadata
     */
    async updateDependenciesList() {
        try {
            const venvPath = this.storage.getVenvPath();
            const pipPath = pythonEnvUtils_1.PythonEnvUtils.getVenvPipPath(venvPath);
            // Get list of installed packages
            const output = await this.executeCommand(`"${pipPath}" list --format=freeze`);
            const dependencies = output.split('\n')
                .filter(line => line.trim() && !line.startsWith('#'))
                .map(line => line.trim());
            await this.storage.updateDependencies(dependencies);
            console.log(`PYTHON_ENV: Dependencies list updated (${dependencies.length} packages)`);
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to update dependencies list:', error);
        }
    }
}
exports.VenvManager = VenvManager;


/***/ }),
/* 99 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PythonEnvStorage = void 0;
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(6));
const pythonEnvUtils_1 = __webpack_require__(100);
/**
 * Manages persistent storage of Python environment metadata
 */
class PythonEnvStorage {
    static PYTHON_ENV_DIR = 'python-env';
    static STATE_FILE = 'state.json';
    static VENV_DIR = 'venv';
    context;
    pythonEnvPath;
    stateFilePath;
    venvPath;
    constructor(context) {
        this.context = context;
        // Initialize paths using global storage
        const globalStorageUri = this.context.globalStorageUri;
        this.pythonEnvPath = path.join(globalStorageUri.fsPath, PythonEnvStorage.PYTHON_ENV_DIR);
        this.stateFilePath = path.join(this.pythonEnvPath, PythonEnvStorage.STATE_FILE);
        this.venvPath = path.join(this.pythonEnvPath, PythonEnvStorage.VENV_DIR);
        console.log(`PYTHON_ENV: Storage initialized at ${this.pythonEnvPath}`);
        console.log(`PYTHON_ENV: Virtual environment path: ${this.venvPath}`);
        console.log(`PYTHON_ENV: State file path: ${this.stateFilePath}`);
        // Ensure the python-env directory exists
        this.ensurePythonEnvDirectory();
    }
    /**
     * Get the virtual environment path
     */
    getVenvPath() {
        return this.venvPath;
    }
    /**
     * Get the Python environment storage path
     */
    getPythonEnvPath() {
        return this.pythonEnvPath;
    }
    /**
     * Save environment metadata to state.json
     */
    async saveMetadata(metadata) {
        try {
            const jsonData = JSON.stringify(metadata, null, 2);
            fs.writeFileSync(this.stateFilePath, jsonData, 'utf-8');
            console.log('PYTHON_ENV: Metadata saved to state file', metadata);
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to save metadata:', error);
            throw new Error(`Failed to save Python environment metadata: ${error}`);
        }
    }
    /**
     * Load environment metadata from state.json
     */
    loadMetadata() {
        try {
            if (!fs.existsSync(this.stateFilePath)) {
                console.log('PYTHON_ENV: No state file found, returning null');
                return null;
            }
            const jsonData = fs.readFileSync(this.stateFilePath, 'utf-8');
            const metadata = JSON.parse(jsonData);
            console.log('PYTHON_ENV: Metadata loaded from state file', metadata);
            return metadata;
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to load metadata:', error);
            return null;
        }
    }
    /**
     * Check if virtual environment exists and is valid
     */
    isVenvValid() {
        const isValid = pythonEnvUtils_1.PythonEnvUtils.isValidVenv(this.venvPath);
        console.log(`PYTHON_ENV: Virtual environment validation result: ${isValid}`);
        return isValid;
    }
    /**
     * Delete the virtual environment and metadata
     */
    async deleteEnvironment() {
        try {
            // Remove virtual environment directory
            if (fs.existsSync(this.venvPath)) {
                fs.rmSync(this.venvPath, { recursive: true, force: true });
                console.log('PYTHON_ENV: Virtual environment directory deleted');
            }
            // Remove state file
            if (fs.existsSync(this.stateFilePath)) {
                fs.unlinkSync(this.stateFilePath);
                console.log('PYTHON_ENV: State file deleted');
            }
            console.log('PYTHON_ENV: Environment successfully deleted');
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to delete environment:', error);
            throw new Error(`Failed to delete Python environment: ${error}`);
        }
    }
    /**
     * Update metadata with new validation timestamp
     */
    async updateValidation() {
        try {
            const metadata = this.loadMetadata();
            if (metadata) {
                metadata.lastValidated = new Date().toISOString();
                await this.saveMetadata(metadata);
                console.log('PYTHON_ENV: Validation timestamp updated');
            }
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to update validation:', error);
        }
    }
    /**
     * Update dependencies list in metadata
     */
    async updateDependencies(dependencies) {
        try {
            const metadata = this.loadMetadata();
            if (metadata) {
                metadata.dependencies = dependencies;
                await this.saveMetadata(metadata);
                console.log(`PYTHON_ENV: Dependencies updated (${dependencies.length} packages)`);
            }
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to update dependencies:', error);
        }
    }
    /**
     * Create initial metadata for a new environment
     */
    createInitialMetadata(pythonVersion) {
        const now = new Date().toISOString();
        return {
            venvPath: this.venvPath,
            createdAt: now,
            pythonVersion: pythonVersion,
            isActive: true,
            lastValidated: now,
            dependencies: []
        };
    }
    /**
     * Ensure the python-env directory exists
     */
    ensurePythonEnvDirectory() {
        try {
            if (!pythonEnvUtils_1.PythonEnvUtils.ensureDirectoryExists(this.pythonEnvPath)) {
                throw new Error(`Failed to create python-env directory at ${this.pythonEnvPath}`);
            }
        }
        catch (error) {
            console.error('PYTHON_ENV: Failed to ensure directory exists:', error);
            throw error;
        }
    }
    /**
     * Get storage statistics
     */
    getStorageStats() {
        const stats = {
            envExists: fs.existsSync(this.pythonEnvPath),
            stateExists: fs.existsSync(this.stateFilePath),
            venvSize: undefined
        };
        if (fs.existsSync(this.venvPath)) {
            try {
                // Calculate approximate venv size (simplified)
                const getDirectorySize = (dirPath) => {
                    let size = 0;
                    const items = fs.readdirSync(dirPath);
                    for (const item of items) {
                        const itemPath = path.join(dirPath, item);
                        const stat = fs.statSync(itemPath);
                        if (stat.isDirectory()) {
                            size += getDirectorySize(itemPath);
                        }
                        else {
                            size += stat.size;
                        }
                    }
                    return size;
                };
                stats.venvSize = Math.round(getDirectorySize(this.venvPath) / (1024 * 1024)); // MB
            }
            catch (error) {
                console.log('PYTHON_ENV: Could not calculate venv size:', error);
            }
        }
        return stats;
    }
}
exports.PythonEnvStorage = PythonEnvStorage;


/***/ }),
/* 100 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PythonEnvUtils = void 0;
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const os = __importStar(__webpack_require__(22));
/**
 * Platform-specific utilities for Python environment management
 */
class PythonEnvUtils {
    /**
     * Get the Python executable command for the current platform
     */
    static getPythonCommand() {
        const platform = os.platform();
        // On Windows, try python first, then python3
        if (platform === 'win32') {
            return 'python';
        }
        // On Unix-like systems (Linux, macOS), prefer python3
        return 'python3';
    }
    /**
     * Get the virtual environment activation command for the current platform
     */
    static getActivationCommand(venvPath) {
        const platform = os.platform();
        if (platform === 'win32') {
            return path.join(venvPath, 'Scripts', 'activate.bat');
        }
        else {
            return `source ${path.join(venvPath, 'bin', 'activate')}`;
        }
    }
    /**
     * Get the Python executable path within a virtual environment
     */
    static getVenvPythonPath(venvPath) {
        const platform = os.platform();
        if (platform === 'win32') {
            return path.join(venvPath, 'Scripts', 'python.exe');
        }
        else {
            return path.join(venvPath, 'bin', 'python');
        }
    }
    /**
     * Get the pip executable path within a virtual environment
     */
    static getVenvPipPath(venvPath) {
        const platform = os.platform();
        if (platform === 'win32') {
            return path.join(venvPath, 'Scripts', 'pip.exe');
        }
        else {
            return path.join(venvPath, 'bin', 'pip');
        }
    }
    /**
     * Check if a path points to a valid virtual environment
     */
    static isValidVenv(venvPath) {
        try {
            // Check if directory exists
            if (!fs.existsSync(venvPath)) {
                return false;
            }
            // Check for Python executable
            const pythonPath = this.getVenvPythonPath(venvPath);
            if (!fs.existsSync(pythonPath)) {
                return false;
            }
            // Check for pip executable
            const pipPath = this.getVenvPipPath(venvPath);
            if (!fs.existsSync(pipPath)) {
                return false;
            }
            // Check for pyvenv.cfg (standard virtual environment marker)
            const configPath = path.join(venvPath, 'pyvenv.cfg');
            if (!fs.existsSync(configPath)) {
                return false;
            }
            return true;
        }
        catch (error) {
            console.log(`PYTHON_ENV: Error validating venv at ${venvPath}:`, error);
            return false;
        }
    }
    /**
     * Validate a path is safe for virtual environment creation
     */
    static isValidPath(targetPath) {
        try {
            // Check if parent directory exists and is writable
            const parentDir = path.dirname(targetPath);
            if (!fs.existsSync(parentDir)) {
                return false;
            }
            // Check if target path already exists and is not empty
            if (fs.existsSync(targetPath)) {
                const stats = fs.statSync(targetPath);
                if (stats.isDirectory()) {
                    const contents = fs.readdirSync(targetPath);
                    return contents.length === 0; // Only valid if empty
                }
                return false; // File exists at target path
            }
            return true;
        }
        catch (error) {
            console.log(`PYTHON_ENV: Error validating path ${targetPath}:`, error);
            return false;
        }
    }
    /**
     * Extract Python version from a pyvenv.cfg file
     */
    static extractPythonVersion(venvPath) {
        try {
            const configPath = path.join(venvPath, 'pyvenv.cfg');
            if (!fs.existsSync(configPath)) {
                return null;
            }
            const configContent = fs.readFileSync(configPath, 'utf-8');
            const versionMatch = configContent.match(/version\s*=\s*([^\s\n]+)/);
            return versionMatch ? versionMatch[1] : null;
        }
        catch (error) {
            console.log(`PYTHON_ENV: Error extracting version from ${venvPath}:`, error);
            return null;
        }
    }
    /**
     * Get platform-specific environment separator
     */
    static getPathSeparator() {
        return os.platform() === 'win32' ? ';' : ':';
    }
    /**
     * Ensure directory exists, creating it if necessary
     */
    static ensureDirectoryExists(dirPath) {
        try {
            if (!fs.existsSync(dirPath)) {
                fs.mkdirSync(dirPath, { recursive: true });
            }
            return true;
        }
        catch (error) {
            console.error(`PYTHON_ENV: Failed to create directory ${dirPath}:`, error);
            return false;
        }
    }
}
exports.PythonEnvUtils = PythonEnvUtils;


/***/ }),
/* 101 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Debounce Manager (Unified)
 *
 * Single source of truth for all debounce logic in the extension.
 * Handles debounce timing with visual countdown in the VS Code status bar.
 *
 * Consolidates the former watchers/debounceManager.ts and utils/debounceManager.ts
 * into one clean implementation used by all watchers (file, directory, DOM).
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DebounceManager = void 0;
const vscode = __importStar(__webpack_require__(1));
const analysisConfigurationStorage_1 = __webpack_require__(87);
const configurationConverter_1 = __webpack_require__(102);
// ─── DebounceManager class ───────────────────────────────────────────
class DebounceManager {
    delayMs;
    callback;
    targetFileName;
    timeoutId = null;
    progressIntervalId = null;
    isActive = false;
    startTime = 0;
    statusBarItem = null;
    constructor(delayMs, callback, targetFileName) {
        this.delayMs = delayMs;
        this.callback = callback;
        this.targetFileName = targetFileName;
        this.statusBarItem = vscode.window.createStatusBarItem(vscode.StatusBarAlignment.Left, 100);
    }
    // ── Static helpers ───────────────────────────────────────────────
    /**
     * Read the debounce delay from user configuration.
     * Returns -1 when auto-analysis is disabled.
     */
    static async getDebounceDelay(context) {
        try {
            const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
            const autoAnalysisEnabled = await storage.getAutoAnalysisEnabled();
            if (!autoAnalysisEnabled) {
                return -1; // disabled
            }
            const config = await storage.loadConfiguration();
            return configurationConverter_1.ConfigurationConverter.convertToMilliseconds(config.autoAnalysisDelay);
        }
        catch (error) {
            console.error('DEBOUNCE_MANAGER: Error reading debounce delay, falling back to 3 000 ms:', error);
            return 3000;
        }
    }
    // ── Lifecycle ────────────────────────────────────────────────────
    /**
     * Start (or restart) the debounce timer.
     * If delay is 0 the callback fires immediately (real-time mode).
     */
    start() {
        // Cancel any previous timer first
        this.cancel();
        if (this.delayMs > 0) {
            this.isActive = true;
            this.startTime = Date.now();
            this.showProgressIndicator();
            this.timeoutId = setTimeout(async () => {
                this.clearVisuals();
                try {
                    await this.callback();
                }
                catch (err) {
                    console.error('DEBOUNCE_MANAGER: Callback error:', err);
                }
            }, this.delayMs);
        }
        else {
            // Real-time: fire immediately
            Promise.resolve(this.callback()).catch(err => console.error('DEBOUNCE_MANAGER: Immediate callback error:', err));
        }
    }
    /**
     * Cancel any pending debounce timer without executing the callback.
     */
    cancel() {
        if (this.timeoutId) {
            clearTimeout(this.timeoutId);
            this.timeoutId = null;
        }
        this.clearVisuals();
    }
    /**
     * Release all resources. Call this when the owner is being destroyed.
     */
    dispose() {
        this.cancel();
        if (this.statusBarItem) {
            this.statusBarItem.dispose();
            this.statusBarItem = null;
        }
    }
    // ── Queries ──────────────────────────────────────────────────────
    /** Whether the debounce timer is currently counting down. */
    isRunning() {
        return this.isActive;
    }
    /** Milliseconds remaining until the callback fires (0 if idle). */
    getRemainingTime() {
        if (!this.isActive || !this.startTime) {
            return 0;
        }
        return Math.max(0, this.delayMs - (Date.now() - this.startTime));
    }
    /** Current debounce status snapshot. */
    getStatus() {
        return {
            isActive: this.isActive,
            remainingMs: this.getRemainingTime(),
            totalMs: this.delayMs,
        };
    }
    /** Current delay in milliseconds. */
    getDelay() {
        return this.delayMs;
    }
    // ── Mutation ─────────────────────────────────────────────────────
    /**
     * Update the delay. If a timer is already running it is restarted
     * with the new value.
     */
    updateDelay(newDelayMs) {
        this.delayMs = newDelayMs;
        if (this.isActive) {
            this.start(); // restart with new delay
        }
    }
    // ── Private helpers ──────────────────────────────────────────────
    clearVisuals() {
        this.isActive = false;
        if (this.progressIntervalId) {
            clearInterval(this.progressIntervalId);
            this.progressIntervalId = null;
        }
        if (this.statusBarItem) {
            this.statusBarItem.hide();
        }
    }
    showProgressIndicator() {
        if (!this.statusBarItem) {
            return;
        }
        const fileName = this.targetFileName || 'files';
        this.progressIntervalId = setInterval(() => {
            const remaining = Math.max(0, this.delayMs - (Date.now() - this.startTime));
            if (remaining > 0) {
                const seconds = (remaining / 1000).toFixed(1);
                this.statusBarItem.text = `$(clock) Analyzing ${fileName} in ${seconds}s`;
                this.statusBarItem.tooltip = 'Waiting for file changes to settle before re-analyzing';
                this.statusBarItem.show();
            }
            else {
                this.clearVisuals();
            }
        }, 100);
    }
}
exports.DebounceManager = DebounceManager;


/***/ }),
/* 102 */
/***/ ((__unused_webpack_module, exports) => {


/**
 * Configuration Converter
 * Convierte la configuración de AutoAnalysisDelay a milisegundos
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ConfigurationConverter = void 0;
class ConfigurationConverter {
    /**
     * Convierte AutoAnalysisDelayConfig a milisegundos
     */
    static convertToMilliseconds(config) {
        console.log(`CONFIG_CONVERTER: Converting delay config`, config);
        switch (config.type) {
            case 'RealTime':
                return 0;
            case '1s':
                return 1000;
            case '3s':
                return 3000;
            case '5s':
                return 5000;
            case '10s':
                return 10000;
            case 'Custom':
                return config.customMs || 3000; // Default a 3s si no hay valor custom
            default:
                console.warn(`CONFIG_CONVERTER: Unknown delay type ${config.type}, defaulting to 3s`);
                return 3000;
        }
    }
    /**
     * Obtiene una descripción legible del delay
     */
    static getDisplayName(config) {
        switch (config.type) {
            case 'RealTime':
                return 'Real-time';
            case '1s':
                return '1 second';
            case '3s':
                return '3 seconds';
            case '5s':
                return '5 seconds';
            case '10s':
                return '10 seconds';
            case 'Custom':
                return `${config.customMs || 3000}ms custom`;
            default:
                return 'Unknown';
        }
    }
}
exports.ConfigurationConverter = ConfigurationConverter;
exports["default"] = ConfigurationConverter;


/***/ }),
/* 103 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Directory Watcher Orchestrator
 * Thin coordinator that ties together FileHashTracker, ChangeAccumulator,
 * DirectoryReAnalyzer and DebounceManager to monitor directory-based sessions.
 *
 * Decomposed from the original 1102-line god-object into 4 single-responsibility classes
 * (Phase 4 refactoring).
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DirectoryWatcherOrchestrator = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const sessionRegistry_1 = __webpack_require__(32);
const debounceManager_1 = __webpack_require__(101);
const analysisConfigurationStorage_1 = __webpack_require__(87);
const configurationConverter_1 = __webpack_require__(102);
const fileHashTracker_1 = __webpack_require__(104);
const changeAccumulator_1 = __webpack_require__(105);
const directoryReAnalyzer_1 = __webpack_require__(106);
const errorHandler_1 = __webpack_require__(37);
class DirectoryWatcherOrchestrator {
    session;
    context;
    watchers = new Map();
    debounceManager = null;
    watcherId = null;
    isWatching = false;
    configurationStorage;
    hashTracker;
    accumulator;
    reAnalyzer;
    constructor(session, context) {
        this.session = session;
        this.context = context;
        console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Initializing for session ${session.id}`);
        console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Target: ${session.targetPath} (${session.targetType}/${session.analysisMode})`);
        console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Files to watch: ${session.filesToHash?.length || 0}`);
        this.configurationStorage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
        this.hashTracker = new fileHashTracker_1.FileHashTracker(session.filesToHash);
        this.accumulator = new changeAccumulator_1.ChangeAccumulator();
        this.reAnalyzer = new directoryReAnalyzer_1.DirectoryReAnalyzer(context);
    }
    // ── Public API ───────────────────────────────────────────────
    /**
     * Start directory-based watching (creates one fs.watch per tracked directory).
     */
    async startWatching() {
        try {
            const dirs = this.hashTracker.getWatchedDirectories();
            if (dirs.size === 0) {
                console.warn(`DIRECTORY_WATCHER_ORCHESTRATOR: No directories to watch`);
                return null;
            }
            const debounceDelayMs = await this.loadDebounceConfiguration();
            if (debounceDelayMs === -1) {
                console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Auto-Analysis DISABLED — skipping watcher setup`);
                return null;
            }
            console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Watching ${dirs.size} directories with ${debounceDelayMs}ms debounce`);
            // Warn on large projects
            if (dirs.size > 100) {
                console.warn(`DIRECTORY_WATCHER_ORCHESTRATOR: Large project (${dirs.size} dirs)`);
                vscode.window.showWarningMessage(`CodeXR: Large project detected (${dirs.size} directories). Consider disabling auto-analysis in settings.`, 'Open Settings').then(selection => {
                    if (selection === 'Open Settings') {
                        vscode.commands.executeCommand('newCodeAnalysis.showAnalysisSettings');
                    }
                });
            }
            this.debounceManager = new debounceManager_1.DebounceManager(debounceDelayMs, this.handleDebounceCallback.bind(this), `Project (${dirs.size} dirs)`);
            this.watcherId = `directory_watcher_${this.session.id}_${Date.now()}`;
            for (const dirPath of dirs) {
                try {
                    const watcher = fs.watch(dirPath, (eventType, filename) => {
                        if (filename) {
                            this.handleDirectoryChange(dirPath, eventType, filename).catch(err => (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.Watcher, 'handleDirectoryChange', err, errorHandler_1.ErrorSeverity.Log));
                        }
                    });
                    this.watchers.set(dirPath, watcher);
                }
                catch (watchError) {
                    (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.Watcher, `Failed to watch ${dirPath}`, watchError, errorHandler_1.ErrorSeverity.Log);
                }
            }
            this.isWatching = true;
            console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Started with ID ${this.watcherId}, watching ${this.watchers.size} dirs`);
            return this.watcherId;
        }
        catch (error) {
            (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.Watcher, 'Error starting watcher', error, errorHandler_1.ErrorSeverity.Log);
            this.cleanup();
            return null;
        }
    }
    async stopWatching() {
        console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Stopping for session ${this.session.id}`);
        this.cleanup();
    }
    async updateDebounceConfiguration() {
        try {
            const newDelayMs = await this.loadDebounceConfiguration();
            if (newDelayMs === -1) {
                console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Auto-Analysis now DISABLED — stopping`);
                await this.stopWatching();
                return;
            }
            if (this.debounceManager) {
                const oldDelay = this.debounceManager.getDelay();
                console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Updating debounce ${oldDelay}ms → ${newDelayMs}ms`);
                this.debounceManager.updateDelay(newDelayMs);
            }
        }
        catch (error) {
            (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.Watcher, 'Error updating debounce', error, errorHandler_1.ErrorSeverity.Log);
        }
    }
    getWatcherId() { return this.watcherId; }
    isActive() { return this.isWatching; }
    getWatchedFilesCount() { return this.watchers.size; }
    getChangedFilesCount() { return this.accumulator.totalCount(); }
    // ── FS event handling ────────────────────────────────────────
    async handleDirectoryChange(dirPath, eventType, filename) {
        const fullPath = path.join(dirPath, filename);
        // Fast-path filters
        if (this.hashTracker.shouldSkipEvent(filename)) {
            return;
        }
        try {
            const autoEnabled = await this.configurationStorage.getAutoAnalysisEnabled();
            if (!autoEnabled) {
                return;
            }
        }
        catch {
            return;
        }
        if (!this.hashTracker.isAnalyzableExtension(fullPath)) {
            return;
        }
        const dirFiles = this.hashTracker.getDirectoryFiles(dirPath);
        if (!dirFiles) {
            return;
        }
        const wasTracked = dirFiles.has(filename);
        const fileExists = fs.existsSync(fullPath);
        if (eventType === 'rename') {
            if (fileExists && !wasTracked) {
                this.accumulator.addAdded(fullPath);
            }
            else if (!fileExists && wasTracked) {
                this.accumulator.addDeleted(fullPath);
                this.hashTracker.untrackFile(dirPath, filename);
            }
        }
        else if (eventType === 'change' && wasTracked && fileExists) {
            const changed = await this.hashTracker.hasFileChanged(dirPath, filename, fullPath);
            if (changed) {
                this.accumulator.addChanged(fullPath);
            }
        }
        // Start/restart debounce if pending changes exist
        if (this.accumulator.hasChanges()) {
            const delayMs = await this.loadDebounceConfiguration();
            if (delayMs === -1) {
                return;
            }
            // Recreate debounce manager with fresh config
            if (this.debounceManager) {
                this.debounceManager.dispose();
            }
            this.debounceManager = new debounceManager_1.DebounceManager(delayMs, this.handleDebounceCallback.bind(this), `Project (${this.hashTracker.getWatchedDirectories().size} dirs)`);
            this.debounceManager.start();
        }
    }
    // ── Debounce callback ────────────────────────────────────────
    async handleDebounceCallback() {
        try {
            // Check auto-analysis is still enabled
            const autoEnabled = await this.configurationStorage.getAutoAnalysisEnabled();
            if (!autoEnabled) {
                this.accumulator.clear();
                return;
            }
            // Session still alive?
            const registry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(this.context);
            if (!registry.getSession(this.session.id)) {
                console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Session ${this.session.id} gone — stopping`);
                this.stopWatching();
                return;
            }
            const changes = this.accumulator.consumeAll();
            console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Debounce fired — ${changes.changed.length} modified, ${changes.added.length} added, ${changes.deleted.length} deleted`);
            let hasChanges = false;
            // 1. Modified files
            if (changes.changed.length > 0) {
                const actuallyChanged = await this.hashTracker.filterActuallyChanged(changes.changed);
                if (actuallyChanged.length > 0) {
                    const results = await this.reAnalyzer.reAnalyzeFiles(actuallyChanged);
                    if (results) {
                        await this.reAnalyzer.updateDataJson(this.session, results);
                    }
                    hasChanges = true;
                }
            }
            // 2. Added files
            if (changes.added.length > 0) {
                await this.reAnalyzer.handleAddedFiles(this.session, changes.added, this.hashTracker);
                hasChanges = true;
            }
            // 3. Deleted files
            if (changes.deleted.length > 0) {
                await this.reAnalyzer.handleDeletedFiles(this.session, changes.deleted, (filePath) => this.closeSingleWatcher(filePath));
                hasChanges = true;
            }
            if (!hasChanges) {
                console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: No actual changes detected`);
            }
        }
        catch (error) {
            (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.Watcher, 'Error in debounce callback', error, errorHandler_1.ErrorSeverity.Log);
            this.accumulator.clear();
        }
    }
    // ── Internal helpers ─────────────────────────────────────────
    async loadDebounceConfiguration() {
        try {
            const autoEnabled = await this.configurationStorage.getAutoAnalysisEnabled();
            if (!autoEnabled) {
                return -1;
            }
            const config = await this.configurationStorage.loadConfiguration();
            return configurationConverter_1.ConfigurationConverter.convertToMilliseconds(config.autoAnalysisDelay);
        }
        catch {
            return 3000; // Default fallback
        }
    }
    closeSingleWatcher(filePath) {
        if (this.watchers.has(filePath)) {
            try {
                this.watchers.get(filePath)?.close();
                this.watchers.delete(filePath);
            }
            catch (error) {
                (0, errorHandler_1.handleError)(errorHandler_1.ErrorDomain.Watcher, `Error closing watcher for ${filePath}`, error, errorHandler_1.ErrorSeverity.Warn);
            }
        }
    }
    cleanup() {
        for (const [, watcher] of this.watchers) {
            try {
                watcher.close();
            }
            catch { /* ignore */ }
        }
        this.watchers.clear();
        if (this.debounceManager) {
            this.debounceManager.dispose();
            this.debounceManager = null;
        }
        this.accumulator.clear();
        this.isWatching = false;
        this.watcherId = null;
        console.log(`DIRECTORY_WATCHER_ORCHESTRATOR: Cleanup completed`);
    }
}
exports.DirectoryWatcherOrchestrator = DirectoryWatcherOrchestrator;


/***/ }),
/* 104 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * File Hash Tracker
 * Manages a directory → fileName → hash lookup structure.
 * Detects real file changes by comparing SHA-256 hashes, and filters out
 * non-essential filesystem events (temp files, IDE files, unsupported extensions).
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FileHashTracker = void 0;
const path = __importStar(__webpack_require__(5));
const sha256Generator_1 = __webpack_require__(36);
class FileHashTracker {
    /** directoryPath → (fileName → hash) */
    directoryFileHashes = new Map();
    /** Set of directory paths being tracked */
    watchedDirectories = new Set();
    static SUPPORTED_EXTENSIONS = new Set([
        '.py', '.pyw', '.pyi', '.rb', '.rbw', '.java', '.c', '.h', '.cpp', '.cxx', '.cc', '.hpp', '.hxx',
        '.cs', '.erl', '.hrl', '.f90', '.f95', '.f03', '.f08', '.f', '.gd', '.go', '.js', '.mjs', '.cjs',
        '.kt', '.kts', '.lua', '.m', '.mm', '.php', '.phtml', '.php3', '.php4', '.php5', '.pl', '.pm',
        '.scala', '.sc', '.sol', '.swift', '.ts', '.tsx', '.ttcn', '.ttcn3', '.vue', '.zig', '.rs',
        '.dart', '.r', '.sh', '.bash', '.ps1', '.jsx', '.css', '.scss', '.less', '.clj', '.cljs',
        '.hs', '.ml', '.mli', '.pas',
    ]);
    static SKIP_PATTERNS = [
        /^\..*$/, // Hidden files
        /.*~$/, // Backup files
        /.*\.tmp$/, // Temporary files
        /.*\.log$/, // Log files
        /.*\.swp$/, // Vim swap files
        /.*\.swo$/, // Vim swap files
        /.*\.bak$/, // Backup files
        /.*\.orig$/, // Original files
        /^#.*#$/, // Emacs auto-save files
        /.*\.lock$/, // Lock files
        /.*\.pid$/, // Process ID files
        /.*\.cache$/, // Cache files
        /.*\.DS_Store$/, // macOS files
        /^Thumbs\.db$/, // Windows files
        /.*\.pyc$/, // Python bytecode
        /.*\.pyo$/, // Python optimized bytecode
        /.*\.class$/, // Java class files
        /.*\.o$/, // Object files
        /.*\.obj$/, // Object files
        /.*\.exe$/, // Executable files
        /.*\.dll$/, // Dynamic libraries
        /.*\.so$/, // Shared libraries
        /.*\.a$/, // Static libraries
        /.*\.lib$/, // Library files
    ];
    /**
     * Initialize hash tracking from the session's filesToHash array.
     */
    constructor(filesToHash) {
        if (!filesToHash || filesToHash.length === 0) {
            console.log(`FILE_HASH_TRACKER: No filesToHash provided — nothing to track`);
            return;
        }
        console.log(`FILE_HASH_TRACKER: Initializing with ${filesToHash.length} files`);
        for (const entry of filesToHash) {
            const dirPath = path.dirname(entry.filePath);
            const fileName = path.basename(entry.filePath);
            if (!this.directoryFileHashes.has(dirPath)) {
                this.directoryFileHashes.set(dirPath, new Map());
                this.watchedDirectories.add(dirPath);
            }
            this.directoryFileHashes.get(dirPath).set(fileName, entry.hash);
        }
        console.log(`FILE_HASH_TRACKER: Tracking ${this.watchedDirectories.size} directories`);
    }
    /** Returns the set of directories that contain tracked files. */
    getWatchedDirectories() {
        return this.watchedDirectories;
    }
    /** Look up the directory-level file map. */
    getDirectoryFiles(dirPath) {
        return this.directoryFileHashes.get(dirPath);
    }
    /** Whether a filename belongs to a tracked directory. */
    isTrackedFile(dirPath, fileName) {
        return this.directoryFileHashes.get(dirPath)?.has(fileName) ?? false;
    }
    /**
     * Check whether a single file's hash has changed from the stored value.
     * @returns true if the file's hash differs from the stored hash.
     */
    async hasFileChanged(dirPath, fileName, fullPath) {
        try {
            const dirFiles = this.directoryFileHashes.get(dirPath);
            const originalHash = dirFiles?.get(fileName);
            if (!originalHash) {
                return true;
            }
            const currentHash = await sha256Generator_1.SHA256Generator.generateFileHash(fullPath);
            if (currentHash !== originalHash) {
                console.log(`FILE_HASH_TRACKER: Hash changed for ${fileName} — old: ${originalHash.substring(0, 12)}... new: ${currentHash.substring(0, 12)}...`);
                return true;
            }
            return false;
        }
        catch {
            // In case of error, assume changed
            return true;
        }
    }
    /**
     * For a batch of file paths, return only those whose hash actually changed.
     * Also updates the stored hash to the new value.
     */
    async filterActuallyChanged(filePaths) {
        const changed = [];
        for (const filePath of filePaths) {
            try {
                const dirPath = path.dirname(filePath);
                const fileName = path.basename(filePath);
                const currentHash = await sha256Generator_1.SHA256Generator.generateFileHash(filePath);
                const dirFiles = this.directoryFileHashes.get(dirPath);
                const originalHash = dirFiles?.get(fileName);
                if (!originalHash || currentHash !== originalHash) {
                    changed.push(filePath);
                    // Update stored hash
                    dirFiles?.set(fileName, currentHash);
                }
            }
            catch {
                changed.push(filePath);
            }
        }
        console.log(`FILE_HASH_TRACKER: ${changed.length}/${[...filePaths].length} files actually changed`);
        return changed;
    }
    /**
     * Start tracking a new file (e.g. a file that was just added to the directory).
     */
    trackNewFile(filePath, hash) {
        const dirPath = path.dirname(filePath);
        const fileName = path.basename(filePath);
        if (!this.directoryFileHashes.has(dirPath)) {
            this.directoryFileHashes.set(dirPath, new Map());
        }
        this.directoryFileHashes.get(dirPath).set(fileName, hash);
    }
    /**
     * Stop tracking a file (e.g. it was deleted).
     */
    untrackFile(dirPath, fileName) {
        this.directoryFileHashes.get(dirPath)?.delete(fileName);
    }
    /** Whether this file's extension is in the analyzable set. */
    isAnalyzableExtension(filePath) {
        return FileHashTracker.SUPPORTED_EXTENSIONS.has(path.extname(filePath).toLowerCase());
    }
    /** Whether a filesystem event for this filename should be ignored (temp/IDE files). */
    shouldSkipEvent(filename) {
        return FileHashTracker.SKIP_PATTERNS.some(p => p.test(filename));
    }
}
exports.FileHashTracker = FileHashTracker;


/***/ }),
/* 105 */
/***/ ((__unused_webpack_module, exports) => {


/**
 * Change Accumulator
 * Collects changed / added / deleted file paths from filesystem events
 * and provides bulk retrieval + clear.
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ChangeAccumulator = void 0;
class ChangeAccumulator {
    changedFiles = new Set();
    addedFiles = new Set();
    deletedFiles = new Set();
    addChanged(filePath) {
        this.changedFiles.add(filePath);
    }
    addAdded(filePath) {
        this.addedFiles.add(filePath);
    }
    addDeleted(filePath) {
        this.deletedFiles.add(filePath);
        // If we previously recorded it as added or changed, remove it
        this.addedFiles.delete(filePath);
        this.changedFiles.delete(filePath);
    }
    /** Total number of pending changes. */
    totalCount() {
        return this.changedFiles.size + this.addedFiles.size + this.deletedFiles.size;
    }
    hasChanges() {
        return this.totalCount() > 0;
    }
    /** Return all pending changes and reset the accumulators. */
    consumeAll() {
        const result = {
            changed: [...this.changedFiles],
            added: [...this.addedFiles],
            deleted: [...this.deletedFiles],
        };
        this.changedFiles.clear();
        this.addedFiles.clear();
        this.deletedFiles.clear();
        return result;
    }
    /** Clear without returning. */
    clear() {
        this.changedFiles.clear();
        this.addedFiles.clear();
        this.deletedFiles.clear();
    }
    get changedCount() { return this.changedFiles.size; }
    get addedCount() { return this.addedFiles.size; }
    get deletedCount() { return this.deletedFiles.size; }
}
exports.ChangeAccumulator = ChangeAccumulator;


/***/ }),
/* 106 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Directory Re-Analyzer
 * Handles re-analysis of changed files, updating data.json (XR and LivePanel formats),
 * processing added/deleted files, recalculating summaries, and sending SSE notifications.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DirectoryReAnalyzer = void 0;
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const executePython_1 = __webpack_require__(96);
const sessionServerManager_1 = __webpack_require__(107);
const sha256Generator_1 = __webpack_require__(36);
class DirectoryReAnalyzer {
    executePython;
    sessionServerManager;
    constructor(context) {
        this.executePython = new executePython_1.ExecutePython(context);
        this.sessionServerManager = new sessionServerManager_1.SessionServerManager(context);
    }
    // ── Re-analysis ──────────────────────────────────────────────
    /**
     * Re-analyze an array of changed files using the Python coordinator.
     */
    async reAnalyzeFiles(changedFiles) {
        try {
            console.log(`DIRECTORY_REANALYZER: Re-analyzing ${changedFiles.length} changed files`);
            const result = await this.executePython.executeFileReanalysis(changedFiles);
            if (!result || !Array.isArray(result)) {
                console.error(`DIRECTORY_REANALYZER: Invalid re-analysis result`);
                return null;
            }
            console.log(`DIRECTORY_REANALYZER: Re-analysis completed, received ${result.length} file summaries`);
            return result;
        }
        catch (error) {
            console.error(`DIRECTORY_REANALYZER: Error during re-analysis:`, error);
            return null;
        }
    }
    // ── Data.json updates ────────────────────────────────────────
    /**
     * Update the existing data.json with re-analysis results.
     * Automatically detects XR vs LivePanel format.
     */
    async updateDataJson(session, reAnalysisResults) {
        try {
            if (!session.savedFilesPath) {
                console.error(`DIRECTORY_REANALYZER: No saved files path in session`);
                return;
            }
            const dataJsonPath = path.join(session.savedFilesPath, 'data.json');
            if (!fs.existsSync(dataJsonPath)) {
                console.error(`DIRECTORY_REANALYZER: data.json not found at ${dataJsonPath}`);
                return;
            }
            const currentData = JSON.parse(fs.readFileSync(dataJsonPath, 'utf8'));
            if (session.analysisMode === 'XR') {
                this.updateXRDataJson(currentData, reAnalysisResults, dataJsonPath);
            }
            else {
                this.updateLivePanelDataJson(currentData, reAnalysisResults, dataJsonPath);
            }
            // Send SSE notification after update
            await this.sendSSENotification(session);
        }
        catch (error) {
            console.error(`DIRECTORY_REANALYZER: Error updating data.json:`, error);
        }
    }
    updateXRDataJson(currentData, results, dataJsonPath) {
        const fileMap = new Map();
        currentData.forEach((file, index) => {
            fileMap.set(file.filePath, index);
        });
        let updated = 0;
        for (const file of results) {
            const idx = fileMap.get(file.filePath);
            if (idx !== undefined) {
                currentData[idx] = file;
            }
            else {
                currentData.push(file);
            }
            updated++;
        }
        fs.writeFileSync(dataJsonPath, JSON.stringify(currentData, null, 2), 'utf8');
        console.log(`DIRECTORY_REANALYZER: Updated XR data.json — ${updated} files, total ${currentData.length}`);
    }
    updateLivePanelDataJson(currentData, results, dataJsonPath) {
        if (!currentData.files) {
            currentData.files = [];
        }
        const fileMap = new Map();
        currentData.files.forEach((file, index) => {
            fileMap.set(file.filePath, index);
        });
        let updated = 0;
        for (const file of results) {
            const idx = fileMap.get(file.filePath);
            if (idx !== undefined) {
                currentData.files[idx] = file;
            }
            else {
                currentData.files.push(file);
            }
            updated++;
        }
        this.recalculateSummary(currentData);
        if (!currentData.summary) {
            currentData.summary = {};
        }
        currentData.summary.analyzedAt = new Date().toISOString();
        fs.writeFileSync(dataJsonPath, JSON.stringify(currentData, null, 2), 'utf8');
        console.log(`DIRECTORY_REANALYZER: Updated LivePanel data.json — ${updated} files, total ${currentData.files.length}`);
    }
    // ── Added / Deleted file processing ──────────────────────────
    /**
     * Analyze newly added files and append them to data.json.
     */
    async handleAddedFiles(session, addedFiles, hashTracker) {
        try {
            const dataJsonPath = path.join(session.savedFilesPath, 'data.json');
            if (!fs.existsSync(dataJsonPath)) {
                return;
            }
            const data = JSON.parse(fs.readFileSync(dataJsonPath, 'utf8'));
            let hasChanges = false;
            for (const filePath of addedFiles) {
                console.log(`DIRECTORY_REANALYZER: Processing added file: ${filePath}`);
                // Check if already present
                const existing = data.files?.find((f) => f.file_path === filePath || f.filePath === filePath);
                if (existing) {
                    continue;
                }
                try {
                    const tempSession = {
                        ...session,
                        analysisMode: 'LivePanel',
                        targetType: 'file',
                        targetPath: filePath,
                    };
                    const result = await this.executePython.executeAnalysis(tempSession);
                    if (result && result.success !== false) {
                        if (!data.files) {
                            data.files = [];
                        }
                        data.files.push(result);
                        hasChanges = true;
                        // Track the new file's hash
                        const hash = await sha256Generator_1.SHA256Generator.generateFileHash(filePath);
                        hashTracker.trackNewFile(filePath, hash);
                        // Update session's filesToHash
                        if (session.filesToHash) {
                            session.filesToHash.push({ filePath, hash });
                        }
                        console.log(`DIRECTORY_REANALYZER: Added new file to data.json: ${filePath}`);
                    }
                }
                catch (err) {
                    console.error(`DIRECTORY_REANALYZER: Error analyzing new file ${filePath}:`, err);
                }
            }
            if (hasChanges) {
                fs.writeFileSync(dataJsonPath, JSON.stringify(data, null, 2), 'utf8');
                await this.refreshSummaryAndNotify(session);
            }
        }
        catch (error) {
            console.error(`DIRECTORY_REANALYZER: Error processing added files:`, error);
        }
    }
    /**
     * Remove deleted files from data.json.
     * Handles both XR format (plain array) and LivePanel format (object with .files property).
     */
    async handleDeletedFiles(session, deletedFiles, closeFsWatcher) {
        try {
            const dataJsonPath = path.join(session.savedFilesPath, 'data.json');
            if (!fs.existsSync(dataJsonPath)) {
                return;
            }
            const data = JSON.parse(fs.readFileSync(dataJsonPath, 'utf8'));
            let hasChanges = false;
            // Detect format: XR format is a plain array, LivePanel is an object with .files
            const isXRFormat = Array.isArray(data);
            for (const deletedPath of deletedFiles) {
                console.log(`DIRECTORY_REANALYZER: Processing deleted file: ${deletedPath}`);
                if (isXRFormat) {
                    // XR format: data is the array directly
                    hasChanges = this.removeDeletedFileFromXRFormat(data, deletedPath) || hasChanges;
                }
                else {
                    // LivePanel format: data.files is the array
                    hasChanges = this.removeDeletedFileFromLivePanelFormat(data, deletedPath) || hasChanges;
                }
                // Close any individual file watcher
                closeFsWatcher(deletedPath);
            }
            if (hasChanges) {
                fs.writeFileSync(dataJsonPath, JSON.stringify(data, null, 2), 'utf8');
                // For LivePanel, recalculate summary after deletion
                if (!isXRFormat) {
                    this.recalculateSummary(data);
                    fs.writeFileSync(dataJsonPath, JSON.stringify(data, null, 2), 'utf8');
                }
                await this.sendSSENotification(session);
            }
        }
        catch (error) {
            console.error(`DIRECTORY_REANALYZER: Error processing deleted files:`, error);
        }
    }
    /**
     * Remove a deleted file from XR format (plain array).
     * Returns true if an entry was removed, false otherwise.
     */
    removeDeletedFileFromXRFormat(data, deletedPath) {
        const idx = data.findIndex((f) => f.file_path === deletedPath || f.filePath === deletedPath);
        if (idx !== -1) {
            data.splice(idx, 1);
            console.log(`DIRECTORY_REANALYZER: [XR Format] Removed from data.json: ${deletedPath}`);
            return true;
        }
        return false;
    }
    /**
     * Remove a deleted file from LivePanel format (object with .files array).
     * Returns true if an entry was removed, false otherwise.
     */
    removeDeletedFileFromLivePanelFormat(data, deletedPath) {
        if (!data.files || !Array.isArray(data.files)) {
            return false;
        }
        const idx = data.files.findIndex((f) => f.file_path === deletedPath || f.filePath === deletedPath);
        if (idx !== -1) {
            data.files.splice(idx, 1);
            console.log(`DIRECTORY_REANALYZER: [LivePanel Format] Removed from data.json: ${deletedPath}`);
            return true;
        }
        return false;
    }
    // ── Summary recalculation ────────────────────────────────────
    /**
     * Recalculate the summary object in-place for LivePanel data format.
     */
    recalculateSummary(data) {
        if (!data.files || !Array.isArray(data.files)) {
            return;
        }
        const summary = {
            totalFiles: data.files.length,
            totalFilesAnalyzed: 0,
            totalFilesNotAnalyzed: 0,
            totalLines: 0,
            totalLinesOfCode: 0,
            totalComments: 0,
            totalBlankLines: 0,
            totalFunctions: 0,
            totalClasses: 0,
            averageComplexity: 0,
            languages: {},
        };
        let totalComplexity = 0;
        let filesWithComplexity = 0;
        for (const file of data.files) {
            if (file.status === 'success') {
                summary.totalFilesAnalyzed++;
                summary.totalLines += file.totalLines || 0;
                summary.totalLinesOfCode += file.codeLines || 0;
                summary.totalComments += file.commentLines || 0;
                summary.totalBlankLines += file.blankLines || 0;
                summary.totalFunctions += file.functionCount || 0;
                summary.totalClasses += file.classCount || 0;
                const complexity = file.cyclomaticComplexityNumber || file.maxComplexity || 0;
                if (complexity > 0) {
                    totalComplexity += complexity;
                    filesWithComplexity++;
                }
                const language = file.language || 'Unknown';
                summary.languages[language] = (summary.languages[language] || 0) + 1;
            }
            else {
                summary.totalFilesNotAnalyzed++;
            }
        }
        summary.averageComplexity = filesWithComplexity > 0
            ? Math.round((totalComplexity / filesWithComplexity) * 100) / 100
            : 0;
        data.summary = { ...data.summary, ...summary };
        console.log(`DIRECTORY_REANALYZER: Summary recalculated — ${summary.totalFilesAnalyzed}/${summary.totalFiles} files analyzed`);
    }
    // ── SSE notifications ────────────────────────────────────────
    /**
     * Send SSE notification to update connected clients.
     */
    async sendSSENotification(session) {
        try {
            const { SSEManager } = __webpack_require__(18);
            const sseManager = SSEManager.getInstance();
            if (session.analysisMode === 'XR') {
                sseManager.sendDataRefresh(session.targetPath);
            }
            else {
                sseManager.sendUpdate(session.targetPath);
            }
            console.log(`DIRECTORY_REANALYZER: SSE notification sent`);
        }
        catch (sseError) {
            console.error(`DIRECTORY_REANALYZER: SSE direct failed, trying fallback...`);
            try {
                await this.sessionServerManager.notifyAnalysisUpdated(session.id);
                console.log(`DIRECTORY_REANALYZER: SSE fallback succeeded`);
            }
            catch (fallbackError) {
                console.error(`DIRECTORY_REANALYZER: SSE fallback also failed:`, fallbackError);
            }
        }
    }
    // ── Internal helpers ─────────────────────────────────────────
    async refreshSummaryAndNotify(session) {
        try {
            const dataJsonPath = path.join(session.savedFilesPath, 'data.json');
            if (!fs.existsSync(dataJsonPath)) {
                return;
            }
            const data = JSON.parse(fs.readFileSync(dataJsonPath, 'utf8'));
            this.recalculateSummary(data);
            fs.writeFileSync(dataJsonPath, JSON.stringify(data, null, 2), 'utf8');
            await this.sendSSENotification(session);
        }
        catch (error) {
            console.error(`DIRECTORY_REANALYZER: Error refreshing summary:`, error);
        }
    }
}
exports.DirectoryReAnalyzer = DirectoryReAnalyzer;


/***/ }),
/* 107 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Session Server Manager
 * Maneja servidores por sesión siguiendo el patrón muñecas rusas
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SessionServerManager = void 0;
const serverLaunchOrchestrator_1 = __webpack_require__(108);
const sseNotificationManager_1 = __webpack_require__(109);
class SessionServerManager {
    context;
    serverOrchestrator;
    sseManager;
    activeSessions = new Map();
    constructor(context) {
        this.context = context;
        console.log('SESSION_SERVER_MANAGER: Initializing manager');
        this.serverOrchestrator = new serverLaunchOrchestrator_1.ServerLaunchOrchestrator(context);
        this.sseManager = new sseNotificationManager_1.SSENotificationManager();
    }
    /**
     * Inicia un servidor para una sesión de análisis
     */
    async startServerForSession(session) {
        try {
            console.log(`SESSION_SERVER_MANAGER: Starting server for session ${session.id}`);
            // Verificar si ya hay un servidor activo para esta sesión
            if (this.activeSessions.has(session.id)) {
                const existing = this.activeSessions.get(session.id);
                if (existing.isActive) {
                    console.log(`SESSION_SERVER_MANAGER: Server already active for session ${session.id}`);
                    return this.getSessionStatus(session.id);
                }
            }
            // Crear request de lanzamiento
            const launchRequest = {
                sessionId: session.id,
                targetPath: session.targetPath,
                serverType: 'http' // Por defecto HTTP para LivePanels
            };
            // Lanzar servidor pasando la sesión completa para obtener savedFilesPath
            const launchResult = await this.serverOrchestrator.launchServerForSession(launchRequest, session);
            if (launchResult.success && launchResult.sessionServerInfo) {
                // Registrar sesión activa
                this.activeSessions.set(session.id, launchResult.sessionServerInfo);
                // Actualizar la sesión con el puerto asignado
                session.assignedPort = launchResult.sessionServerInfo.port;
                session.serverUrl = launchResult.sessionServerInfo.serverUrl;
                console.log(`SESSION_SERVER_MANAGER: ✅ Server started for session ${session.id} on port ${launchResult.sessionServerInfo.port}`);
                // Enviar notificación SSE
                await this.sseManager.notifyServerStarted(launchResult.sessionServerInfo);
                return {
                    sessionId: session.id,
                    isServerActive: true,
                    isWatcherActive: true, // Asumimos que el watcher ya está activo
                    port: launchResult.sessionServerInfo.port,
                    serverUrl: launchResult.sessionServerInfo.serverUrl,
                    lastUpdate: new Date()
                };
            }
            else {
                console.error(`SESSION_SERVER_MANAGER: ❌ Failed to start server:`, launchResult.error);
                throw new Error(launchResult.error || 'Unknown server launch error');
            }
        }
        catch (error) {
            console.error(`SESSION_SERVER_MANAGER: ❌ Error starting server for session ${session.id}:`, error);
            return {
                sessionId: session.id,
                isServerActive: false,
                isWatcherActive: false
            };
        }
    }
    /**
     * Detiene un servidor para una sesión específica
     */
    async stopServerForSession(sessionId) {
        try {
            console.log(`SESSION_SERVER_MANAGER: Stopping server for session ${sessionId}`);
            const sessionInfo = this.activeSessions.get(sessionId);
            if (!sessionInfo) {
                console.log(`SESSION_SERVER_MANAGER: No active server found for session ${sessionId}`);
                return true; // Ya estaba detenido
            }
            // Detener servidor usando orchestrator
            const success = await this.serverOrchestrator.stopServerForSession(sessionId, sessionInfo.targetPath);
            if (success) {
                // Remover de sesiones activas
                this.activeSessions.delete(sessionId);
                // Enviar notificación SSE
                await this.sseManager.notifyServerStopped(sessionInfo);
                console.log(`SESSION_SERVER_MANAGER: ✅ Server stopped for session ${sessionId}`);
            }
            return success;
        }
        catch (error) {
            console.error(`SESSION_SERVER_MANAGER: ❌ Error stopping server for session ${sessionId}:`, error);
            return false;
        }
    }
    /**
     * Notifica que el análisis se ha actualizado (para SSE)
     */
    async notifyAnalysisUpdated(sessionId) {
        const sessionInfo = this.activeSessions.get(sessionId);
        if (sessionInfo) {
            await this.sseManager.notifyAnalysisUpdated(sessionInfo);
        }
    }
    /**
     * Obtiene el estado de una sesión
     */
    getSessionStatus(sessionId) {
        const sessionInfo = this.activeSessions.get(sessionId);
        if (sessionInfo) {
            return {
                sessionId,
                isServerActive: sessionInfo.isActive,
                isWatcherActive: true, // TODO: conectar con SessionWatcherManager
                port: sessionInfo.port,
                serverUrl: sessionInfo.serverUrl,
                lastUpdate: new Date()
            };
        }
        return {
            sessionId,
            isServerActive: false,
            isWatcherActive: false
        };
    }
    /**
     * Obtiene todas las sesiones activas
     */
    getAllActiveSessions() {
        return Array.from(this.activeSessions.values());
    }
    /**
     * Obtiene información del servidor por puerto
     */
    getSessionByPort(port) {
        for (const sessionInfo of this.activeSessions.values()) {
            if (sessionInfo.port === port) {
                return sessionInfo;
            }
        }
        return null;
    }
    /**
     * Limpia todas las sesiones activas
     */
    async cleanup() {
        console.log(`SESSION_SERVER_MANAGER: Cleaning up ${this.activeSessions.size} active sessions`);
        const cleanupPromises = Array.from(this.activeSessions.keys()).map(sessionId => this.stopServerForSession(sessionId));
        await Promise.all(cleanupPromises);
        this.activeSessions.clear();
        console.log(`SESSION_SERVER_MANAGER: ✅ Cleanup completed`);
    }
}
exports.SessionServerManager = SessionServerManager;
exports["default"] = SessionServerManager;


/***/ }),
/* 108 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Server Launch Orchestrator
 * Coordina el lanzamiento de servidores usando el sistema existente de MultiServerLauncher
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServerLaunchOrchestrator = void 0;
const path = __importStar(__webpack_require__(5));
const multiServerLauncher_1 = __webpack_require__(14);
const serverSettingsManager_1 = __webpack_require__(11);
const fileToServerMap_1 = __webpack_require__(20);
class ServerLaunchOrchestrator {
    context;
    multiServerLauncher = null;
    serverSettingsManager;
    constructor(context) {
        this.context = context;
        console.log('SERVER_LAUNCH_ORCHESTRATOR: Initializing orchestrator');
        this.serverSettingsManager = serverSettingsManager_1.ServerSettingsManager.getInstance(context);
    }
    /**
     * Inicializa el MultiServerLauncher si no está creado
     */
    async ensureMultiServerLauncher() {
        if (!this.multiServerLauncher) {
            console.log('SERVER_LAUNCH_ORCHESTRATOR: Creating MultiServerLauncher instance');
            this.multiServerLauncher = new multiServerLauncher_1.MultiServerLauncher(this.context);
        }
    }
    /**
     * Lanza un servidor para una sesión específica
     */
    async launchServerForSession(request, session) {
        try {
            console.log(`SERVER_LAUNCH_ORCHESTRATOR: Launching server for session ${request.sessionId}`);
            console.log(`SERVER_LAUNCH_ORCHESTRATOR: Target file: ${request.targetPath}`);
            await this.ensureMultiServerLauncher();
            // Obtener configuración del servidor
            const serverSettings = this.serverSettingsManager.getServerSettings();
            const serverType = this.getDefaultServerType(serverSettings);
            console.log(`SERVER_LAUNCH_ORCHESTRATOR: Using server type: ${serverType}`);
            // Obtener el directorio donde están guardados los archivos
            const tempDir = session?.savedFilesPath || this.getTempDirForSession(request.sessionId, request.targetPath);
            console.log(`SERVER_LAUNCH_ORCHESTRATOR: Using saved files directory: ${tempDir}`);
            // Buscar el archivo HTML principal en el directorio guardado
            const htmlFilePath = this.findMainHtmlFile(tempDir);
            console.log(`SERVER_LAUNCH_ORCHESTRATOR: HTML file path: ${htmlFilePath}`);
            // Generate descriptive custom name for the server based on analysis type and target
            const customName = this.generateDescriptiveServerName(request, session);
            console.log(`SERVER_LAUNCH_ORCHESTRATOR: Generated descriptive name: "${customName}"`);
            // Lanzar servidor usando MultiServerLauncher
            const launchResult = await this.multiServerLauncher.launchServer(htmlFilePath, // Pasar la ruta completa del HTML
            customName, // Use descriptive name instead of generic Analysis_{sessionId}
            { sessionId: request.sessionId } // Pass sessionId in metadata for server-session linking
            );
            console.log(`SERVER_LAUNCH_ORCHESTRATOR: Launch result:`, {
                success: launchResult.success,
                port: launchResult.port,
                serverUrl: launchResult.serverUrl,
                serverId: launchResult.serverId,
                error: launchResult.error
            });
            if (launchResult.success && launchResult.port && launchResult.serverUrl && launchResult.serverId) {
                // Crear información de la sesión del servidor
                const sessionServerInfo = {
                    sessionId: request.sessionId,
                    targetPath: request.targetPath,
                    port: launchResult.port,
                    serverUrl: launchResult.serverUrl,
                    serverType: launchResult.serverType || serverType,
                    serverId: launchResult.serverId,
                    tempDir,
                    isActive: true,
                    startedAt: new Date()
                };
                // Registrar en el fileToServerMap para SSE
                fileToServerMap_1.fileToServerMap.registerMapping(request.targetPath, {
                    port: launchResult.port,
                    tempDir,
                    fileUri: request.targetPath,
                    serverRef: this.getServerRefById(launchResult.serverId) // TODO: implementar
                });
                console.log(`SERVER_LAUNCH_ORCHESTRATOR: ✅ Server launched successfully on port ${launchResult.port}`);
                return {
                    success: true,
                    sessionServerInfo,
                    portChanged: launchResult.portChanged,
                    originalPort: launchResult.originalPort
                };
            }
            else {
                console.error(`SERVER_LAUNCH_ORCHESTRATOR: ❌ Server launch failed:`, launchResult.error);
                return {
                    success: false,
                    error: launchResult.error || 'Unknown server launch error'
                };
            }
        }
        catch (error) {
            console.error(`SERVER_LAUNCH_ORCHESTRATOR: ❌ Error launching server:`, error);
            return {
                success: false,
                error: error instanceof Error ? error.message : String(error)
            };
        }
    }
    /**
     * Detiene un servidor para una sesión específica
     */
    async stopServerForSession(sessionId, targetPath) {
        try {
            console.log(`SERVER_LAUNCH_ORCHESTRATOR: Stopping server for session ${sessionId}`);
            // Desregistrar del fileToServerMap
            fileToServerMap_1.fileToServerMap.unregisterMapping(targetPath);
            // TODO: Usar MultiServerLauncher para detener el servidor específico
            // Esto depende de la implementación del MultiServerLauncher
            console.log(`SERVER_LAUNCH_ORCHESTRATOR: ✅ Server stopped for session ${sessionId}`);
            return true;
        }
        catch (error) {
            console.error(`SERVER_LAUNCH_ORCHESTRATOR: ❌ Error stopping server:`, error);
            return false;
        }
    }
    /**
     * Obtiene el tipo de servidor por defecto basado en la configuración
     */
    getDefaultServerType(serverSettings) {
        // TODO: Implementar lógica basada en serverSettings
        return 'http'; // Por defecto HTTP para simplicidad
    }
    /**
     * Obtiene el directorio temporal para una sesión
     */
    getTempDirForSession(sessionId, targetPath) {
        // El directorio se guarda en workspaceStorage/analysis/[outputDirectory]
        // Necesitamos obtener esto de la sesión o reconstruirlo
        const workspaceStorage = this.context.storageUri?.fsPath || '';
        // Buscar en el directorio de análisis por sessionId
        const analysisDir = `${workspaceStorage}/analysis`;
        console.log(`SERVER_LAUNCH_ORCHESTRATOR: Looking for session ${sessionId} in: ${analysisDir}`);
        // Por ahora, return a path pattern. TODO: mejorar para buscar el directorio real
        return analysisDir;
    }
    /**
     * Busca el archivo HTML principal en el directorio de la sesión
     */
    findMainHtmlFile(baseDir) {
        try {
            const fs = __webpack_require__(6);
            const path = __webpack_require__(5);
            console.log(`SERVER_LAUNCH_ORCHESTRATOR: Searching for HTML files in: ${baseDir}`);
            // Buscar recursivamente por archivos HTML
            const findHtmlFiles = (dir) => {
                const files = [];
                try {
                    const entries = fs.readdirSync(dir, { withFileTypes: true });
                    for (const entry of entries) {
                        const fullPath = path.join(dir, entry.name);
                        if (entry.isDirectory()) {
                            files.push(...findHtmlFiles(fullPath));
                        }
                        else if (entry.isFile() && entry.name.endsWith('.html')) {
                            files.push(fullPath);
                            console.log(`SERVER_LAUNCH_ORCHESTRATOR: Found HTML file: ${fullPath}`);
                        }
                    }
                }
                catch (error) {
                    console.error(`SERVER_LAUNCH_ORCHESTRATOR: Error reading directory ${dir}:`, error);
                }
                return files;
            };
            const htmlFiles = findHtmlFiles(baseDir);
            // Buscar archivos con nombres preferidos
            const preferredNames = ['fileAnalysis.html', 'directoryAnalysis.html', 'main.html', 'index.html'];
            for (const preferredName of preferredNames) {
                const found = htmlFiles.find(file => path.basename(file) === preferredName);
                if (found) {
                    console.log(`SERVER_LAUNCH_ORCHESTRATOR: Using preferred HTML file: ${found}`);
                    return found;
                }
            }
            // Si no encuentra uno preferido, usar el primero
            if (htmlFiles.length > 0) {
                console.log(`SERVER_LAUNCH_ORCHESTRATOR: Using first HTML file found: ${htmlFiles[0]}`);
                return htmlFiles[0];
            }
            console.log(`SERVER_LAUNCH_ORCHESTRATOR: No HTML files found in: ${baseDir}`);
            return undefined;
        }
        catch (error) {
            console.error(`SERVER_LAUNCH_ORCHESTRATOR: Error finding HTML file:`, error);
            return undefined;
        }
    }
    /**
     * Obtiene la referencia del servidor por ID
     */
    getServerRefById(serverId) {
        // TODO: Implementar cuando tengamos acceso a las referencias del servidor
        return null;
    }
    /**
     * Generate descriptive custom name for the server based on analysis type and target
     */
    generateDescriptiveServerName(request, session) {
        const baseName = path.basename(request.targetPath);
        const analysisMode = session?.analysisMode || 'unknown';
        const targetType = session?.targetType || 'unknown';
        console.log(`SERVER_LAUNCH_ORCHESTRATOR: 🔍 DEBUG - Generating name for:`, {
            baseName,
            analysisMode,
            targetType,
            targetPath: request.targetPath
        });
        // Create descriptive names based on analysis mode and target type
        if (analysisMode.toLowerCase().includes('xr')) {
            if (targetType === 'directory') {
                const dirName = path.basename(path.dirname(request.targetPath));
                return `XR Directory: ${dirName}`;
            }
            else {
                return `XR File: ${baseName}`;
            }
        }
        else if (analysisMode.toLowerCase().includes('dom')) {
            return `DOM Visualization: ${baseName}`;
        }
        else if (targetType === 'directory') {
            const dirName = path.basename(path.dirname(request.targetPath));
            return `LivePanel Directory: ${dirName}`;
        }
        else {
            return `LivePanel File: ${baseName}`;
        }
    }
}
exports.ServerLaunchOrchestrator = ServerLaunchOrchestrator;
exports["default"] = ServerLaunchOrchestrator;


/***/ }),
/* 109 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * SSE Notification Manager
 * Maneja las notificaciones SSE para sesiones de análisis
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SSENotificationManager = void 0;
const SSEManager_1 = __webpack_require__(18);
class SSENotificationManager {
    constructor() {
        console.log('SSE_NOTIFICATION_MANAGER: Initializing SSE manager');
    } /**
     * Notifica que un servidor ha sido iniciado
     */
    async notifyServerStarted(sessionInfo) {
        try {
            console.log(`SSE_NOTIFICATION_MANAGER: Notifying server started for session ${sessionInfo.sessionId}`);
            const notification = {
                type: 'server-started',
                sessionId: sessionInfo.sessionId,
                timestamp: new Date(),
                data: {
                    port: sessionInfo.port,
                    serverUrl: sessionInfo.serverUrl,
                    targetPath: sessionInfo.targetPath
                }
            };
            await this.sendSSENotification(notification);
        }
        catch (error) {
            console.error(`SSE_NOTIFICATION_MANAGER: Error notifying server started:`, error);
        }
    }
    /**
     * Notifica que un servidor ha sido detenido
     */
    async notifyServerStopped(sessionInfo) {
        try {
            console.log(`SSE_NOTIFICATION_MANAGER: Notifying server stopped for session ${sessionInfo.sessionId}`);
            const notification = {
                type: 'server-stopped',
                sessionId: sessionInfo.sessionId,
                timestamp: new Date(),
                data: {
                    port: sessionInfo.port,
                    targetPath: sessionInfo.targetPath
                }
            };
            await this.sendSSENotification(notification);
        }
        catch (error) {
            console.error(`SSE_NOTIFICATION_MANAGER: Error notifying server stopped:`, error);
        }
    }
    /**
     * Notifica que el análisis ha sido actualizado
     */
    async notifyAnalysisUpdated(sessionInfo) {
        try {
            console.log(`SSE_NOTIFICATION_MANAGER: Notifying analysis updated for session ${sessionInfo.sessionId}`);
            const notification = {
                type: 'analysis-updated',
                sessionId: sessionInfo.sessionId,
                timestamp: new Date(),
                data: {
                    port: sessionInfo.port,
                    serverUrl: sessionInfo.serverUrl,
                    targetPath: sessionInfo.targetPath
                }
            };
            await this.sendSSENotification(notification);
        }
        catch (error) {
            console.error(`SSE_NOTIFICATION_MANAGER: Error notifying analysis updated:`, error);
        }
    }
    /**
     * Notifica que un archivo ha cambiado
     */
    async notifyFileChanged(sessionInfo, changedFile) {
        try {
            console.log(`SSE_NOTIFICATION_MANAGER: Notifying file changed for session ${sessionInfo.sessionId}`);
            const notification = {
                type: 'file-changed',
                sessionId: sessionInfo.sessionId,
                timestamp: new Date(),
                data: {
                    port: sessionInfo.port,
                    targetPath: sessionInfo.targetPath,
                    changedFile
                }
            };
            await this.sendSSENotification(notification);
        }
        catch (error) {
            console.error(`SSE_NOTIFICATION_MANAGER: Error notifying file changed:`, error);
        }
    }
    /**
     * Envía una notificación SSE a todos los clientes conectados
     */
    async sendSSENotification(notification) {
        try {
            console.log(`SSE_NOTIFICATION_MANAGER: Sending SSE notification:`, {
                type: notification.type,
                sessionId: notification.sessionId,
                timestamp: notification.timestamp
            });
            // Obtener el SSEManager real
            const sseManager = SSEManager_1.SSEManager.getInstance();
            // Para sesiones de análisis, usar el targetPath como identificador SSE
            const targetPath = notification.data?.targetPath;
            if (targetPath) {
                console.log(`SSE_NOTIFICATION_MANAGER: Sending to clients of: ${targetPath}`);
                // Crear el mensaje SSE en el formato esperado por el cliente
                const sseMessage = {
                    type: notification.type,
                    fileUri: targetPath,
                    sessionId: notification.sessionId,
                    timestamp: notification.timestamp,
                    data: notification.data
                };
                // Enviar usando el SSEManager real
                sseManager.sendCustomMessage(targetPath, sseMessage);
                console.log(`SSE_NOTIFICATION_MANAGER: ✅ SSE notification sent successfully to ${targetPath}`);
            }
            else {
                console.warn(`SSE_NOTIFICATION_MANAGER: ⚠️ No targetPath found in notification data`);
            }
        }
        catch (error) {
            console.error(`SSE_NOTIFICATION_MANAGER: ❌ Error sending SSE notification:`, error);
            throw error;
        }
    }
}
exports.SSENotificationManager = SSENotificationManager;
exports["default"] = SSENotificationManager;


/***/ }),
/* 110 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * VisualizeDOM Watcher
 * Monitors HTML files for changes and triggers real-time re-analysis using the new engine
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizeDOMWatcher = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const sessionRegistry_1 = __webpack_require__(32);
const VisualizeDOMRequirements_1 = __webpack_require__(111);
const saveFiles_1 = __webpack_require__(114);
const SSEManager_1 = __webpack_require__(18);
const debounceManager_1 = __webpack_require__(101);
class VisualizeDOMWatcher {
    static activeWatchers = new Map();
    /**
     * Get debounce delay from user configuration.
     * Returns -1 when auto-analysis is disabled.
     */
    static async getDebounceDelay(context) {
        return debounceManager_1.DebounceManager.getDebounceDelay(context);
    }
    /**
     * Start watching HTML file for changes
     */
    static async startWatching(sessionId, htmlFilePath, outputDirectory, context) {
        try {
            console.log(`🔍 VISUALIZE_DOM_WATCHER: Starting to watch HTML file: ${htmlFilePath}`);
            console.log(`🔍 VISUALIZE_DOM_WATCHER: Session ID: ${sessionId}`);
            console.log(`🔍 VISUALIZE_DOM_WATCHER: Output directory: ${outputDirectory}`);
            // Check if file exists
            if (!fs.existsSync(htmlFilePath)) {
                console.error(`🔍 VISUALIZE_DOM_WATCHER: HTML file does not exist: ${htmlFilePath}`);
                return '';
            }
            // Check if output directory exists
            if (!fs.existsSync(outputDirectory)) {
                console.error(`🔍 VISUALIZE_DOM_WATCHER: Output directory does not exist: ${outputDirectory}`);
                return '';
            }
            // Create unique watcher ID
            const watcherId = `visualizedom_${path.basename(htmlFilePath)}_${Date.now()}`;
            // Create file watcher
            const watcher = fs.watch(htmlFilePath, { persistent: true }, async (eventType, filename) => {
                if (eventType === 'change') {
                    console.log(`🔍 VISUALIZE_DOM_WATCHER: HTML file change detected: ${htmlFilePath}`);
                    console.log(`🔍 VISUALIZE_DOM_WATCHER: Event type: ${eventType}, filename: ${filename}`);
                    // CRITICAL: Check if session still exists before processing change
                    const registry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
                    const currentSession = registry.getSession(sessionId);
                    if (!currentSession) {
                        console.log(`🔍 VISUALIZE_DOM_WATCHER: ⚠️ Session ${sessionId} no longer exists, stopping watcher ${watcherId}`);
                        VisualizeDOMWatcher.stopWatching(watcherId);
                        return;
                    }
                    console.log(`🔍 VISUALIZE_DOM_WATCHER: ✅ Session ${sessionId} still exists, proceeding with change processing`);
                    // Get current watcher info for debouncing
                    const currentWatcherInfo = VisualizeDOMWatcher.activeWatchers.get(watcherId);
                    if (!currentWatcherInfo) {
                        console.error(`🔍 VISUALIZE_DOM_WATCHER: Watcher info not found for ID: ${watcherId}`);
                        return;
                    }
                    // Dispose previous debounce manager if it exists
                    if (currentWatcherInfo.debounceManager) {
                        currentWatcherInfo.debounceManager.dispose();
                        currentWatcherInfo.debounceManager = null;
                    }
                    // Get fresh debounce delay in case user changed it
                    const currentDebounceMs = await VisualizeDOMWatcher.getDebounceDelay(context);
                    // Skip if auto-analysis is disabled
                    if (currentDebounceMs === -1) {
                        console.log(`🔍 VISUALIZE_DOM_WATCHER: Auto-analysis disabled, skipping`);
                        return;
                    }
                    // Check if we processed this change too recently (additional protection)
                    const now = Date.now();
                    if (currentWatcherInfo.lastProcessedTime &&
                        (now - currentWatcherInfo.lastProcessedTime) < currentDebounceMs) {
                        console.log(`🔍 VISUALIZE_DOM_WATCHER: Skipping duplicate change event (too recent): ${htmlFilePath}`);
                        return;
                    }
                    // Create unified DebounceManager with the re-analysis callback
                    const fileName = path.basename(htmlFilePath);
                    const debounceManager = new debounceManager_1.DebounceManager(currentDebounceMs, async () => {
                        try {
                            console.log(`🔍 VISUALIZE_DOM_WATCHER: Executing debounced re-analysis for: ${htmlFilePath}`);
                            // CRITICAL: Check if session still exists before processing
                            const registry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
                            const currentSession = registry.getSession(sessionId);
                            if (!currentSession) {
                                console.log(`🔍 VISUALIZE_DOM_WATCHER: ⚠️ Session ${sessionId} no longer exists, stopping watcher ${watcherId}`);
                                VisualizeDOMWatcher.stopWatching(watcherId);
                                return;
                            }
                            console.log(`🔍 VISUALIZE_DOM_WATCHER: ✅ Session ${sessionId} still exists, proceeding with re-analysis`);
                            currentWatcherInfo.lastProcessedTime = Date.now();
                            // Update session status to analyzing
                            registry.updateSessionStatus(sessionId, 'analyzing', 50);
                            // Re-execute VisualizeDOM analysis
                            await VisualizeDOMWatcher.reExecuteVisualizeDOMAnalysis(sessionId, htmlFilePath, outputDirectory, context);
                        }
                        catch (error) {
                            console.error(`🔍 VISUALIZE_DOM_WATCHER: Error re-executing VisualizeDOM analysis:`, error);
                            // Update session status to error
                            const registry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
                            registry.updateSessionStatus(sessionId, 'error', 0, `VisualizeDOM re-analysis failed: ${error instanceof Error ? error.message : String(error)}`);
                            vscode.window.showErrorMessage(`HTML visualization update failed: ${error instanceof Error ? error.message : String(error)}`);
                        }
                    }, fileName);
                    currentWatcherInfo.debounceManager = debounceManager;
                    // Start the debounce countdown
                    debounceManager.start();
                    console.log(`🔍 VISUALIZE_DOM_WATCHER: Debounce timer set for: ${htmlFilePath} (${currentDebounceMs}ms)`);
                }
            });
            // Store watcher info
            const watcherInfo = {
                watcher,
                sessionId,
                htmlFilePath,
                outputDirectory,
                context,
                lastProcessedTime: undefined,
                debounceManager: null,
            };
            VisualizeDOMWatcher.activeWatchers.set(watcherId, watcherInfo);
            console.log(`🔍 VISUALIZE_DOM_WATCHER: Successfully started watching with ID: ${watcherId}`);
            console.log(`🔍 VISUALIZE_DOM_WATCHER: Total active VisualizeDOM watchers: ${VisualizeDOMWatcher.activeWatchers.size}`);
            return watcherId;
        }
        catch (error) {
            console.error(`🔍 VISUALIZE_DOM_WATCHER: Error starting HTML file watcher:`, error);
            vscode.window.showErrorMessage(`Error starting HTML file watcher: ${error instanceof Error ? error.message : String(error)}`);
            return '';
        }
    }
    /**
     * Re-execute VisualizeDOM analysis when HTML file changes
     */
    static async reExecuteVisualizeDOMAnalysis(sessionId, htmlFilePath, outputDirectory, context) {
        try {
            console.log(`🔄 VISUALIZE_DOM_WATCHER: Re-executing VisualizeDOM analysis for: ${htmlFilePath}`);
            const registry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
            const session = registry.getSession(sessionId);
            if (!session) {
                throw new Error(`Session ${sessionId} not found`);
            }
            // ========================================
            // STEP 1: GET PROCESSED FILES FROM PROCESSOR
            // ========================================
            console.log(`🔧 VISUALIZE_DOM_WATCHER: STEP 1 - Getting fresh processed files from VisualizeDOMRequirements...`);
            const visualizeDOMRequirements = new VisualizeDOMRequirements_1.VisualizeDOMRequirements(context);
            const processedFiles = await visualizeDOMRequirements.getRequiredFiles(session);
            console.log(`✅ VISUALIZE_DOM_WATCHER: Received ${processedFiles.loadedFiles.size} processed template files`);
            // Verify we have files
            if (processedFiles.loadedFiles.size === 0) {
                throw new Error('No processed files received from VisualizeDOMRequirements');
            }
            // Log received files
            console.log(`📋 VISUALIZE_DOM_WATCHER: Files received from processor:`);
            for (const [fileName, content] of processedFiles.loadedFiles) {
                console.log(`📄 VISUALIZE_DOM_WATCHER: ${fileName} (${content.length} chars)`);
            }
            // ===================================
            // STEP 2: SAVE FILES WITH SAVEFILES
            // ===================================
            console.log(`💾 VISUALIZE_DOM_WATCHER: STEP 2 - Saving updated files to storage...`);
            const saveFiles = new saveFiles_1.SaveFiles();
            const folderName = 'visualizeDOMAnalysis';
            const savedPath = await saveFiles.saveFilesToStorage(processedFiles.loadedFiles, folderName, session.outputDirectory, context);
            console.log(`✅ VISUALIZE_DOM_WATCHER: Files successfully saved to: ${savedPath}`);
            // Update session with new save information
            session.savedFilesPath = savedPath;
            // Update stored files in session for compatibility
            session.requiredFiles.clear();
            for (const [fileName, content] of processedFiles.loadedFiles) {
                session.requiredFiles.set(fileName, content);
            }
            // ===============================================
            // STEP 3: SEND SSE UPDATE TO CLIENTS
            // ===============================================
            console.log(`📡 VISUALIZE_DOM_WATCHER: STEP 3 - Sending SSE update to clients...`);
            try {
                // Extract HTML content for SSE message
                let htmlContent = '';
                const indexHtml = processedFiles.loadedFiles.get('index.html');
                if (indexHtml) {
                    // Try to extract the htmlContent from the index.html or use the original HTML file
                    htmlContent = await fs.promises.readFile(htmlFilePath, 'utf-8');
                }
                console.log(`📡 VISUALIZE_DOM_WATCHER: Sending SSE htmlUpdated message...`);
                console.log(`📡 VISUALIZE_DOM_WATCHER: HTML content length: ${htmlContent.length}`);
                // Send SSE update using the SSEManager
                SSEManager_1.sseManager.sendCustomMessage(htmlFilePath, {
                    type: 'htmlUpdated',
                    htmlContent: htmlContent,
                    action: 'reload-html',
                    message: 'HTML DOM content has been updated'
                });
                console.log(`✅ VISUALIZE_DOM_WATCHER: SSE update sent successfully`);
            }
            catch (sseError) {
                console.warn(`⚠️ VISUALIZE_DOM_WATCHER: Failed to send SSE update (non-critical):`, sseError);
            }
            // =======================================================
            // STEP 4: UPDATE SESSION STATUS TO MONITORING - SUCCESS
            // =======================================================
            console.log(`🎯 VISUALIZE_DOM_WATCHER: STEP 4 - Finalizing session update...`);
            registry.updateSessionStatus(sessionId, 'monitoring', 100);
            session.status = 'monitoring';
            console.log(`🎉 VISUALIZE_DOM_WATCHER: VisualizeDOM re-analysis completed successfully!`);
            console.log(`🎉 VISUALIZE_DOM_WATCHER: Session ${sessionId} updated with ${processedFiles.loadedFiles.size} files`);
            console.log(`🎉 VISUALIZE_DOM_WATCHER: Files saved to: ${savedPath}`);
            // Show success message
            vscode.window.showInformationMessage(`🌐 HTML DOM visualization updated for: ${path.basename(htmlFilePath)}`);
        }
        catch (error) {
            console.error(`❌ VISUALIZE_DOM_WATCHER: Error in reExecuteVisualizeDOMAnalysis:`, error);
            throw error;
        }
    }
    /**
     * Stop watching a specific HTML file
     */
    static stopWatching(watcherId) {
        try {
            const watcherInfo = VisualizeDOMWatcher.activeWatchers.get(watcherId);
            if (!watcherInfo) {
                console.log(`🔍 VISUALIZE_DOM_WATCHER: Watcher not found with ID: ${watcherId}`);
                return false;
            }
            // Dispose debounce manager if it exists
            if (watcherInfo.debounceManager) {
                watcherInfo.debounceManager.dispose();
            }
            // Close the watcher
            watcherInfo.watcher.close();
            // Remove from active watchers
            VisualizeDOMWatcher.activeWatchers.delete(watcherId);
            console.log(`🔍 VISUALIZE_DOM_WATCHER: Stopped watching HTML file: ${watcherInfo.htmlFilePath}`);
            console.log(`🔍 VISUALIZE_DOM_WATCHER: Remaining active VisualizeDOM watchers: ${VisualizeDOMWatcher.activeWatchers.size}`);
            return true;
        }
        catch (error) {
            console.error(`🔍 VISUALIZE_DOM_WATCHER: Error stopping watcher:`, error);
            return false;
        }
    }
    /**
     * Stop all active VisualizeDOM watchers
     */
    static stopAllWatchers() {
        try {
            console.log(`🔍 VISUALIZE_DOM_WATCHER: Stopping all ${VisualizeDOMWatcher.activeWatchers.size} active VisualizeDOM watchers`);
            for (const [watcherId, watcherInfo] of VisualizeDOMWatcher.activeWatchers) {
                // Dispose debounce manager
                if (watcherInfo.debounceManager) {
                    watcherInfo.debounceManager.dispose();
                }
                // Close watcher
                watcherInfo.watcher.close();
                console.log(`🔍 VISUALIZE_DOM_WATCHER: Stopped watcher: ${watcherId} (${watcherInfo.htmlFilePath})`);
            }
            // Clear all watchers
            VisualizeDOMWatcher.activeWatchers.clear();
            console.log(`🔍 VISUALIZE_DOM_WATCHER: All VisualizeDOM watchers stopped successfully`);
        }
        catch (error) {
            console.error(`🔍 VISUALIZE_DOM_WATCHER: Error stopping all watchers:`, error);
        }
    }
    /**
     * Get active watcher count
     */
    static getActiveWatcherCount() {
        return VisualizeDOMWatcher.activeWatchers.size;
    }
    /**
     * Get active watcher info
     */
    static getActiveWatchers() {
        return new Map(VisualizeDOMWatcher.activeWatchers);
    }
}
exports.VisualizeDOMWatcher = VisualizeDOMWatcher;


/***/ }),
/* 111 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizeDOMRequirements = void 0;
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(6));
const visualizeDOMParser_1 = __webpack_require__(112);
const executePython_1 = __webpack_require__(96);
/**
 * Handles template files for VisualizeDOM analysis
 *
 * This class:
 * - Determines which templates are needed for DOM visualization
 * - Calls VisualizeDOMParser to load and process templates
 * - Returns loaded and processed files for HTML DOM visualization
 */
class VisualizeDOMRequirements {
    visualizeDOMParser;
    executePython;
    context;
    constructor(context) {
        console.log('VISUALIZE_DOM_REQUIREMENTS: 🌐 Initializing VisualizeDOMRequirements...');
        this.context = context;
        this.visualizeDOMParser = new visualizeDOMParser_1.VisualizeDOMParser(context);
        this.executePython = new executePython_1.ExecutePython(context);
        console.log('VISUALIZE_DOM_REQUIREMENTS: ✅ VisualizeDOMRequirements initialized successfully');
    }
    /**
     * Gets loaded template files for VisualizeDOM analysis
     *
     * @param session - Unified analysis session
     * @param theme - Current user theme (optional, defaults to 'vscode-light')
     * @returns Promise with loaded and processed template files
     */
    async getRequiredFiles(session, theme) {
        console.log(`VISUALIZE_DOM_REQUIREMENTS: 🎯 Getting template files for VisualizeDOM analysis`);
        console.log(`VISUALIZE_DOM_REQUIREMENTS: Target: ${session.targetName} (${session.targetType})`);
        console.log(`VISUALIZE_DOM_REQUIREMENTS: Session ID: ${session.id}`);
        console.log(`VISUALIZE_DOM_REQUIREMENTS: Theme: ${theme || 'default'}`);
        try {
            // STEP 1: Validate that we have a file (VisualizeDOM only works with files)
            if (session.targetType !== 'file') {
                console.error(`VISUALIZE_DOM_REQUIREMENTS: ❌ VisualizeDOM only supports files, not ${session.targetType}`);
                throw new Error(`VisualizeDOM analysis only supports files, not ${session.targetType}`);
            }
            // STEP 2: Validate HTML file extension
            console.log(`VISUALIZE_DOM_REQUIREMENTS: 📄 Step 1 - Validating HTML file: ${session.targetPath}`);
            const isValidHTMLFile = this.validateHTMLFile(session.targetPath);
            if (!isValidHTMLFile) {
                console.error(`VISUALIZE_DOM_REQUIREMENTS: ❌ File ${session.targetName} is not a valid HTML file`);
                throw new Error(`File ${session.targetName} is not a valid HTML file for DOM visualization`);
            }
            console.log(`VISUALIZE_DOM_REQUIREMENTS: ✅ HTML file validation passed`);
            // STEP 3: Read HTML content directly from file (NO PYTHON FOR NOW)
            console.log(`VISUALIZE_DOM_REQUIREMENTS: � Step 2 - Reading HTML content directly from file...`);
            const htmlContent = await this.readHTMLFile(session.targetPath);
            if (!htmlContent || htmlContent.trim().length === 0) {
                console.error(`VISUALIZE_DOM_REQUIREMENTS: ❌ No HTML content found in file`);
                throw new Error(`No HTML content found in file: ${session.targetName}`);
            }
            console.log(`VISUALIZE_DOM_REQUIREMENTS: ✅ HTML content read successfully`);
            console.log(`VISUALIZE_DOM_REQUIREMENTS: 📄 HTML content length: ${htmlContent.length}`);
            // STEP 4: Process templates using the parser with HTML content
            console.log(`VISUALIZE_DOM_REQUIREMENTS: 🔧 Step 3 - Processing VisualizeDOM templates...`);
            console.log(`VISUALIZE_DOM_REQUIREMENTS: � Passing to visualizeDOMParser:`, {
                fileName: session.targetName,
                filePath: session.targetPath,
                title: `DOM Visualization - ${session.targetName}`,
                htmlContentLength: htmlContent.length
            });
            const processedFiles = await this.visualizeDOMParser.processHTMLTemplates({
                htmlContent: htmlContent, // Use the HTML content directly
                fileName: session.targetName,
                filePath: session.targetPath,
                title: `DOM Visualization - ${session.targetName}`
            }, theme);
            console.log(`VISUALIZE_DOM_REQUIREMENTS: ✅ Template processing completed`);
            console.log(`VISUALIZE_DOM_REQUIREMENTS: 📋 Generated ${processedFiles.size} template files`);
            // STEP 5: Log details of processed files
            console.log(`VISUALIZE_DOM_REQUIREMENTS: 🔍 DEBUG - Processed files details:`);
            console.log(`VISUALIZE_DOM_REQUIREMENTS: 🔍 DEBUG - processedFiles type:`, typeof processedFiles);
            console.log(`VISUALIZE_DOM_REQUIREMENTS: 🔍 DEBUG - processedFiles instanceof Map:`, processedFiles instanceof Map);
            console.log(`VISUALIZE_DOM_REQUIREMENTS: 🔍 DEBUG - processedFiles.size:`, processedFiles.size);
            if (processedFiles.size > 0) {
                console.log(`VISUALIZE_DOM_REQUIREMENTS: 📋 Processed template files:`);
                for (const [fileName, content] of processedFiles) {
                    console.log(`VISUALIZE_DOM_REQUIREMENTS: 📄 ${fileName} (${content.length} chars)`);
                }
            }
            else {
                console.warn(`VISUALIZE_DOM_REQUIREMENTS: ⚠️ No files were generated by the parser!`);
            }
            // STEP 6: Prepare return object
            const returnObject = {
                sessionId: session.id,
                analysisMode: session.analysisMode,
                targetPath: session.targetPath,
                loadedFiles: processedFiles,
                estimatedComplexity: 'low',
                processingTime: new Date()
            };
            console.log(`VISUALIZE_DOM_REQUIREMENTS: 🔍 DEBUG - Return object:`, {
                sessionId: returnObject.sessionId,
                analysisMode: returnObject.analysisMode,
                targetPath: returnObject.targetPath,
                loadedFilesSize: returnObject.loadedFiles.size,
                estimatedComplexity: returnObject.estimatedComplexity
            });
            console.log(`VISUALIZE_DOM_REQUIREMENTS: ✅ Requirements processing completed successfully`);
            return returnObject;
        }
        catch (error) {
            console.error(`VISUALIZE_DOM_REQUIREMENTS: ❌ Error getting required files:`, error);
            return {
                sessionId: session.id,
                analysisMode: session.analysisMode,
                targetPath: session.targetPath,
                loadedFiles: new Map(),
                estimatedComplexity: 'low',
                processingTime: new Date()
            };
        }
    }
    /**
     * Validate if file is a supported HTML file
     * @private
     */
    validateHTMLFile(filePath) {
        const supportedExtensions = ['.html', '.htm', '.xhtml'];
        const fileExtension = path.extname(filePath).toLowerCase();
        console.log(`VISUALIZE_DOM_REQUIREMENTS: 🔍 Checking file extension: ${fileExtension}`);
        const isValid = supportedExtensions.includes(fileExtension);
        if (isValid) {
            console.log(`VISUALIZE_DOM_REQUIREMENTS: ✅ File extension ${fileExtension} is supported`);
        }
        else {
            console.log(`VISUALIZE_DOM_REQUIREMENTS: ❌ File extension ${fileExtension} not supported. Supported: ${supportedExtensions.join(', ')}`);
        }
        return isValid;
    }
    /**
     * Get supported HTML file extensions
     */
    static getSupportedExtensions() {
        return ['.html', '.htm', '.xhtml'];
    }
    /**
     * Read HTML content from file
     * @private
     */
    async readHTMLFile(filePath) {
        try {
            console.log(`VISUALIZE_DOM_REQUIREMENTS: 📖 Reading HTML file: ${filePath}`);
            if (!fs.existsSync(filePath)) {
                throw new Error(`File not found: ${filePath}`);
            }
            const htmlContent = fs.readFileSync(filePath, 'utf-8');
            console.log(`VISUALIZE_DOM_REQUIREMENTS: ✅ HTML file read successfully (${htmlContent.length} chars)`);
            return htmlContent;
        }
        catch (error) {
            console.error(`VISUALIZE_DOM_REQUIREMENTS: ❌ Error reading HTML file:`, error);
            throw error;
        }
    }
}
exports.VisualizeDOMRequirements = VisualizeDOMRequirements;


/***/ }),
/* 112 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizeDOMParser = void 0;
const path = __importStar(__webpack_require__(5));
const templateHTMLProcessor_1 = __webpack_require__(113);
/**
 * VisualizeDOM Parser - Bridge to TemplateHTMLProcessor
 *
 * This class:
 * - Acts as a bridge between the new engine and the existing templateHTMLProcessor
 * - Processes HTML files for DOM visualization
 * - Returns processed template files ready for visualization
 */
class VisualizeDOMParser {
    context;
    constructor(context) {
        console.log('VISUALIZE_DOM_PARSER: 🌐 Initializing VisualizeDOMParser...');
        this.context = context;
        console.log('VISUALIZE_DOM_PARSER: ✅ VisualizeDOMParser initialized successfully');
    }
    /**
     * Process HTML templates for DOM visualization
     * This is essentially a bridge to the templateHTMLProcessor
     *
     * @param templateData - HTML content and metadata
     * @param theme - Current user theme (optional)
     * @returns Map with fileName -> processed content
     */
    async processHTMLTemplates(templateData, theme) {
        console.log(`VISUALIZE_DOM_PARSER: 🔧 Processing HTML templates for DOM visualization`);
        console.log(`VISUALIZE_DOM_PARSER: File: ${templateData.fileName}`);
        console.log(`VISUALIZE_DOM_PARSER: HTML content length: ${templateData.htmlContent.length}`);
        console.log(`VISUALIZE_DOM_PARSER: Theme: ${theme || 'default'}`);
        try {
            // STEP 1: Validate template data
            console.log(`VISUALIZE_DOM_PARSER: 📄 Step 1 - Validating template data...`);
            if (!templateHTMLProcessor_1.TemplateHTMLProcessor.validateTemplateData(templateData)) {
                console.error(`VISUALIZE_DOM_PARSER: ❌ Template data validation failed`);
                throw new Error('Invalid template data provided');
            }
            console.log(`VISUALIZE_DOM_PARSER: ✅ Template data validation passed`);
            // STEP 2: Call templateHTMLProcessor to get processed HTML and JS
            console.log(`VISUALIZE_DOM_PARSER: 🔧 Step 2 - Calling TemplateHTMLProcessor...`);
            console.log(`VISUALIZE_DOM_PARSER: � Calling with templateData:`, {
                fileName: templateData.fileName,
                filePath: templateData.filePath,
                title: templateData.title,
                htmlContentLength: templateData.htmlContent.length
            });
            const processingResult = await templateHTMLProcessor_1.TemplateHTMLProcessor.processHTMLTemplate(templateData, this.context);
            // 🔍 DEBUG: Log detailed information about what templateHTMLProcessor returned
            console.log(`VISUALIZE_DOM_PARSER: � TemplateHTMLProcessor returned:`, {
                success: processingResult.success,
                hasIndexHtml: !!processingResult.indexHtml,
                hasJsContent: !!processingResult.jsContent,
                indexHtmlLength: processingResult.indexHtml?.length || 0,
                jsContentLength: processingResult.jsContent?.length || 0,
                error: processingResult.error || 'No error'
            });
            if (!processingResult.success) {
                console.error(`VISUALIZE_DOM_PARSER: ❌ TemplateHTMLProcessor failed:`, processingResult.error);
                throw new Error(`Template processing failed: ${processingResult.error}`);
            }
            if (!processingResult.indexHtml || !processingResult.jsContent) {
                console.error(`VISUALIZE_DOM_PARSER: ❌ TemplateHTMLProcessor returned incomplete data:`, {
                    hasIndexHtml: !!processingResult.indexHtml,
                    hasJsContent: !!processingResult.jsContent
                });
                throw new Error('TemplateHTMLProcessor returned incomplete data');
            }
            console.log(`VISUALIZE_DOM_PARSER: ✅ TemplateHTMLProcessor returned complete data`);
            console.log(`VISUALIZE_DOM_PARSER: 📄 indexHtml: ${processingResult.indexHtml.length} chars`);
            console.log(`VISUALIZE_DOM_PARSER: � jsContent: ${processingResult.jsContent.length} chars`);
            // STEP 3: Create result Map with the two files from templateHTMLProcessor
            console.log(`VISUALIZE_DOM_PARSER: � Step 3 - Creating result Map...`);
            const resultFiles = new Map();
            // Add index.html from templateHTMLProcessor
            resultFiles.set('index.html', processingResult.indexHtml);
            console.log(`VISUALIZE_DOM_PARSER: ✅ Added index.html (${processingResult.indexHtml.length} chars)`);
            // Add main.js from templateHTMLProcessor 
            resultFiles.set('main.js', processingResult.jsContent);
            console.log(`VISUALIZE_DOM_PARSER: ✅ Added main.js (${processingResult.jsContent.length} chars)`);
            // 🔍 FINAL DEBUG: Verify the result Map
            console.log(`VISUALIZE_DOM_PARSER: 🔍 FINAL DEBUG - Result Map verification:`);
            console.log(`VISUALIZE_DOM_PARSER: 🔍 Map size: ${resultFiles.size}`);
            console.log(`VISUALIZE_DOM_PARSER: 🔍 Map keys: [${Array.from(resultFiles.keys()).join(', ')}]`);
            console.log(`VISUALIZE_DOM_PARSER: 🔍 Map instanceof Map: ${resultFiles instanceof Map}`);
            // Test iteration
            console.log(`VISUALIZE_DOM_PARSER: 🔍 Testing iteration:`);
            let count = 0;
            for (const [fileName, content] of resultFiles) {
                count++;
                console.log(`VISUALIZE_DOM_PARSER: 🔍 File ${count}: ${fileName} (${content.length} chars)`);
            }
            console.log(`VISUALIZE_DOM_PARSER: ✅ Successfully created Map with ${resultFiles.size} files`);
            return resultFiles;
        }
        catch (error) {
            console.error(`VISUALIZE_DOM_PARSER: ❌ Error processing HTML templates:`, error);
            throw error;
        }
    }
    /**
     * Get supported file extensions for DOM visualization
     */
    static getSupportedExtensions() {
        return templateHTMLProcessor_1.TemplateHTMLProcessor.getSupportedExtensions();
    }
    /**
     * Validate if a file can be processed for DOM visualization
     */
    static canProcessFile(filePath) {
        const supportedExtensions = this.getSupportedExtensions();
        const fileExtension = path.extname(filePath).toLowerCase();
        console.log(`VISUALIZE_DOM_PARSER: 🔍 Checking if file can be processed: ${filePath}`);
        console.log(`VISUALIZE_DOM_PARSER: 🔍 File extension: ${fileExtension}`);
        const canProcess = supportedExtensions.includes(fileExtension);
        if (canProcess) {
            console.log(`VISUALIZE_DOM_PARSER: ✅ File ${filePath} can be processed`);
        }
        else {
            console.log(`VISUALIZE_DOM_PARSER: ❌ File ${filePath} cannot be processed. Supported: ${supportedExtensions.join(', ')}`);
        }
        return canProcess;
    }
}
exports.VisualizeDOMParser = VisualizeDOMParser;


/***/ }),
/* 113 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Template HTML Processor
 * Specialized processor for HTML DOM visualization templates
 * Handles template processing, placeholder replacement, and HTML generation for DOM visualization
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.TemplateHTMLProcessor = void 0;
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(6));
const getVisualizationConfiguration_1 = __webpack_require__(54);
const nonceGenerator_1 = __webpack_require__(12);
/**
 * Template HTML Processor for DOM Visualization
 */
class TemplateHTMLProcessor {
    /**
     * Process HTML DOM visualization template
     * @param templateData The HTML content and metadata
     * @param context VS Code extension context
     * @returns Processed HTML and JS content
     */
    static async processHTMLTemplate(templateData, context) {
        try {
            console.log('TEMPLATE_HTML_PROCESSOR: Starting HTML template processing...');
            console.log('TEMPLATE_HTML_PROCESSOR: File:', templateData.fileName);
            console.log('TEMPLATE_HTML_PROCESSOR: HTML content length:', templateData.htmlContent.length);
            // 1. Load DOM visualization template
            const templateHtml = await this.loadDOMTemplate(context);
            if (!templateHtml) {
                return {
                    success: false,
                    error: 'Failed to load DOM visualization template'
                };
            }
            // 2. Load HTML live reload JavaScript
            const jsContent = await this.loadHTMLLiveReloadJS(context);
            if (!jsContent) {
                return {
                    success: false,
                    error: 'Failed to load HTML live reload JavaScript'
                };
            }
            // 3. Get visualization settings
            const visualizationSettings = await (0, getVisualizationConfiguration_1.getVisualizationConfiguration)();
            // 4. Prepare template variables
            const templateVariables = this.prepareTemplateVariables(templateData, visualizationSettings);
            // 5. Replace placeholders in HTML template
            const processedHtml = this.replacePlaceholders(templateHtml, templateVariables);
            // 6. Process JavaScript if needed (for now, return as-is)
            const processedJs = jsContent;
            console.log('TEMPLATE_HTML_PROCESSOR: Template processing completed successfully');
            console.log('TEMPLATE_HTML_PROCESSOR: Generated HTML length:', processedHtml.length);
            console.log('TEMPLATE_HTML_PROCESSOR: Generated JS length:', processedJs.length);
            // 🔍 DEBUG: Verify what we're about to return
            const returnResult = {
                success: true,
                indexHtml: processedHtml,
                jsContent: processedJs
            };
            console.log('TEMPLATE_HTML_PROCESSOR: 🔍 DEBUG - About to return result:', {
                success: returnResult.success,
                hasIndexHtml: !!returnResult.indexHtml,
                hasJsContent: !!returnResult.jsContent,
                indexHtmlLength: returnResult.indexHtml?.length || 0,
                jsContentLength: returnResult.jsContent?.length || 0,
                indexHtmlType: typeof returnResult.indexHtml,
                jsContentType: typeof returnResult.jsContent
            });
            console.log('TEMPLATE_HTML_PROCESSOR: 🔍 DEBUG - indexHtml preview:', returnResult.indexHtml?.substring(0, 100) + '...');
            console.log('TEMPLATE_HTML_PROCESSOR: 🔍 DEBUG - jsContent preview:', returnResult.jsContent?.substring(0, 100) + '...');
            return returnResult;
        }
        catch (error) {
            console.error('TEMPLATE_HTML_PROCESSOR: Error processing HTML template:', error);
            return {
                success: false,
                error: error instanceof Error ? error.message : String(error)
            };
        }
    }
    /**
     * Load DOM visualization template from templates/xr/html/dom-visualization-template.html
     * @private
     */
    static async loadDOMTemplate(context) {
        try {
            const templatePath = path.join(context.extensionPath, 'templates', 'xr', 'html', 'dom-visualization-template.html');
            console.log('TEMPLATE_HTML_PROCESSOR: Loading template from:', templatePath);
            if (!fs.existsSync(templatePath)) {
                console.error('TEMPLATE_HTML_PROCESSOR: Template file not found:', templatePath);
                return null;
            }
            const templateContent = fs.readFileSync(templatePath, 'utf-8');
            console.log('TEMPLATE_HTML_PROCESSOR: Template loaded successfully, length:', templateContent.length);
            return templateContent;
        }
        catch (error) {
            console.error('TEMPLATE_HTML_PROCESSOR: Error loading DOM template:', error);
            return null;
        }
    }
    /**
     * Load HTML live reload JavaScript from templates/xr/sse/live_sse_fileDOM.js
     * @private
     */
    static async loadHTMLLiveReloadJS(context) {
        try {
            const jsPath = path.join(context.extensionPath, 'templates', 'xr', 'sse', 'live_sse_fileDOM.js');
            console.log('TEMPLATE_HTML_PROCESSOR: Loading JS from:', jsPath);
            if (!fs.existsSync(jsPath)) {
                console.error('TEMPLATE_HTML_PROCESSOR: JS file not found:', jsPath);
                return null;
            }
            const jsContent = fs.readFileSync(jsPath, 'utf-8');
            console.log('TEMPLATE_HTML_PROCESSOR: JS loaded successfully, length:', jsContent.length);
            return jsContent;
        }
        catch (error) {
            console.error('TEMPLATE_HTML_PROCESSOR: Error loading HTML live reload JS:', error);
            return null;
        }
    }
    /**
     * Prepare template variables for placeholder replacement
     * @private
     */
    static prepareTemplateVariables(templateData, visualizationSettings) {
        // Escape HTML content for safe injection into babia-html attribute
        const escapedHtmlContent = this.escapeHtmlForAttribute(templateData.htmlContent);
        // Generate nonce for security
        const nonce = (0, nonceGenerator_1.generateNonce)();
        const variables = {
            // File metadata
            TITLE: templateData.title || `DOM Visualization - ${templateData.fileName}`,
            FILE_NAME: templateData.fileName,
            FILE_PATH: templateData.filePath,
            // HTML content for babia-html component
            HTML_CONTENT: escapedHtmlContent,
            // Visualization settings
            BACKGROUND_COLOR: visualizationSettings.backgroundColor,
            ENVIRONMENT_PRESET: visualizationSettings.environment,
            GROUND_COLOR: visualizationSettings.groundColor,
            PALETTE: visualizationSettings.palette,
            // Script loading (matches saveFiles.ts pattern)
            nonce: nonce,
            scriptUri: './main.js'
        };
        console.log('TEMPLATE_HTML_PROCESSOR: Template variables prepared:');
        console.log('TEMPLATE_HTML_PROCESSOR: - Title:', variables.TITLE);
        console.log('TEMPLATE_HTML_PROCESSOR: - File:', variables.FILE_NAME);
        console.log('TEMPLATE_HTML_PROCESSOR: - HTML content length:', templateData.htmlContent.length);
        console.log('TEMPLATE_HTML_PROCESSOR: - Environment:', variables.ENVIRONMENT_PRESET);
        console.log('TEMPLATE_HTML_PROCESSOR: - Background:', variables.BACKGROUND_COLOR);
        console.log('TEMPLATE_HTML_PROCESSOR: - Nonce:', variables.nonce);
        console.log('TEMPLATE_HTML_PROCESSOR: - Script URI:', variables.scriptUri);
        return variables;
    }
    /**
     * Replace placeholders in template with actual values
     * @private
     */
    static replacePlaceholders(template, variables) {
        let processedTemplate = template;
        // Replace each variable placeholder
        for (const [key, value] of Object.entries(variables)) {
            const placeholder = `\${${key}}`;
            const regex = new RegExp(placeholder.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'g');
            processedTemplate = processedTemplate.replace(regex, value);
            console.log(`TEMPLATE_HTML_PROCESSOR: Replaced ${placeholder} with value (${value.length} chars)`);
        }
        return processedTemplate;
    }
    /**
     * Escape HTML content for safe injection into HTML attribute
     * @private
     */
    static escapeHtmlForAttribute(htmlContent) {
        return htmlContent
            // Replace quotes
            .replace(/"/g, '&quot;')
            .replace(/'/g, '&#39;')
            // Keep other HTML entities as-is since they're handled by babia-html
            // Remove extra whitespace
            .replace(/\s+/g, ' ')
            .trim();
    }
    /**
     * Get supported HTML file extensions
     */
    static getSupportedExtensions() {
        return ['.html', '.htm', '.xhtml'];
    }
    /**
     * Validate HTML template data
     */
    static validateTemplateData(templateData) {
        if (!templateData.htmlContent || templateData.htmlContent.trim().length === 0) {
            console.error('TEMPLATE_HTML_PROCESSOR: No HTML content provided');
            return false;
        }
        if (!templateData.fileName || templateData.fileName.trim().length === 0) {
            console.error('TEMPLATE_HTML_PROCESSOR: No file name provided');
            return false;
        }
        if (!templateData.filePath || templateData.filePath.trim().length === 0) {
            console.error('TEMPLATE_HTML_PROCESSOR: No file path provided');
            return false;
        }
        return true;
    }
}
exports.TemplateHTMLProcessor = TemplateHTMLProcessor;


/***/ }),
/* 114 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SaveFiles = void 0;
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(6));
/**
 * Utility para guardar archivos en el workspace storage
 *
 * Esta clase:
 * - Recibe archivos procesados y los guarda en workspace storage
 * - Crea la estructura de carpetas necesaria
 * - Retorna la ruta completa donde se guardaron los archivos
 */
class SaveFiles {
    constructor() {
        console.log('SAVE_FILES: Initializing SaveFiles utility...');
    }
    /**
     * Guarda archivos en el workspace storage
     *
     * @param files - Map con fileName -> fileContent
     * @param folderName - Nombre de la carpeta base (fileAnalysis/directoryAnalysis)
     * @param analysisType - Tipo de análisis para crear subcarpeta única
     * @param context - Contexto de VS Code para acceder al storage
     * @returns Promise con la ruta completa donde se guardaron los archivos
     */
    async saveFilesToStorage(files, folderName, analysisType, context) {
        console.log(`SAVE_FILES: 💾 Saving ${files.size} files to workspace storage...`);
        console.log(`SAVE_FILES: Folder name: ${folderName}`);
        console.log(`SAVE_FILES: Analysis type: ${analysisType}`);
        try {
            // Obtener el workspace storage URI
            const storageUri = context.storageUri;
            if (!storageUri) {
                throw new Error('Workspace storage URI is not available');
            }
            // Crear la ruta completa: storage/folderName/analysisType/
            const storagePath = path.join(storageUri.fsPath, folderName, analysisType);
            console.log(`SAVE_FILES: 📂 Storage path: ${storagePath}`);
            // Crear directorios si no existen
            await this.ensureDirectoryExists(storagePath);
            // Guardar cada archivo
            const savedFiles = [];
            for (const [fileName, content] of files) {
                const filePath = path.join(storagePath, fileName);
                console.log(`SAVE_FILES: 📝 Saving ${fileName}...`);
                await fs.promises.writeFile(filePath, content, 'utf8');
                savedFiles.push(fileName);
                console.log(`SAVE_FILES: ✅ Saved ${fileName} (${content.length} chars)`);
            }
            // Always copy favicon.ico from resources directory
            await this.copyFavicon(storagePath, context);
            savedFiles.push('favicon.ico');
            console.log(`SAVE_FILES: 🎉 Successfully saved ${savedFiles.length} files to: ${storagePath}`);
            console.log(`SAVE_FILES: 📋 Files saved: ${savedFiles.join(', ')}`);
            return storagePath;
        }
        catch (error) {
            console.error(`SAVE_FILES: ❌ Error saving files to storage:`, error);
            throw error;
        }
    }
    /**
     * Asegura que el directorio existe, creándolo si es necesario
     */
    async ensureDirectoryExists(dirPath) {
        try {
            console.log(`SAVE_FILES: 🔍 Checking directory: ${dirPath}`);
            // Verificar si el directorio ya existe
            const exists = fs.existsSync(dirPath);
            if (exists) {
                console.log(`SAVE_FILES: ✅ Directory already exists: ${dirPath}`);
                return;
            }
            // Crear el directorio recursivamente
            console.log(`SAVE_FILES: 🏗️ Creating directory: ${dirPath}`);
            await fs.promises.mkdir(dirPath, { recursive: true });
            console.log(`SAVE_FILES: ✅ Directory created successfully: ${dirPath}`);
        }
        catch (error) {
            console.error(`SAVE_FILES: ❌ Error creating directory ${dirPath}:`, error);
            throw error;
        }
    }
    /**
     * Verifica que los archivos se guardaron correctamente
     */
    async verifyFiles(storagePath, expectedFiles) {
        try {
            console.log(`SAVE_FILES: 🔍 Verifying files in: ${storagePath}`);
            for (const fileName of expectedFiles) {
                const filePath = path.join(storagePath, fileName);
                const exists = fs.existsSync(filePath);
                if (!exists) {
                    console.error(`SAVE_FILES: ❌ File not found: ${fileName}`);
                    return false;
                }
                const stats = fs.statSync(filePath);
                console.log(`SAVE_FILES: ✅ Verified ${fileName} (${stats.size} bytes)`);
            }
            console.log(`SAVE_FILES: 🎉 All files verified successfully`);
            return true;
        }
        catch (error) {
            console.error(`SAVE_FILES: ❌ Error verifying files:`, error);
            return false;
        }
    }
    /**
     * Copia el favicon.ico desde resources al directorio de destino
     */
    async copyFavicon(storagePath, context) {
        try {
            // Usar el extensionPath para acceder a los recursos de la extensión
            const extensionPath = context.extensionPath;
            // Construir ruta al favicon en el directorio de la extensión
            const faviconSourcePath = path.join(extensionPath, 'resources', 'favicon.ico');
            const faviconDestPath = path.join(storagePath, 'favicon.ico');
            console.log(`SAVE_FILES: 🖼️ Copying favicon from extension resources: ${faviconSourcePath}`);
            console.log(`SAVE_FILES: 🖼️ Copying favicon to: ${faviconDestPath}`);
            // Verificar que el archivo fuente existe
            if (!fs.existsSync(faviconSourcePath)) {
                console.warn(`SAVE_FILES: ⚠️ Favicon not found at: ${faviconSourcePath}`);
                return;
            }
            // Copiar el archivo
            await fs.promises.copyFile(faviconSourcePath, faviconDestPath);
            console.log(`SAVE_FILES: ✅ Favicon copied successfully`);
        }
        catch (error) {
            console.error(`SAVE_FILES: ❌ Error copying favicon:`, error);
            // No lanzamos error para que no interrumpa el guardado de otros archivos
        }
    }
}
exports.SaveFiles = SaveFiles;


/***/ }),
/* 115 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Active Analyses Commands
 * Handles command registration and execution for active analysis operations
 * SIMPLIFIED: Direct session access without complex model interfaces
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ActiveAnalysesCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
/**
 * Active Analyses Commands implementation
 */
class ActiveAnalysesCommands {
    sessionRegistry;
    serverWatcher;
    static instance;
    commands;
    registeredCommands = new Set();
    constructor(sessionRegistry, serverWatcher) {
        this.sessionRegistry = sessionRegistry;
        this.serverWatcher = serverWatcher;
        this.commands = [
            {
                commandId: 'codeXR.new_code_analysis.activeAnalyses.showDetails',
                title: 'Show Analysis Details',
                handler: this.showDetails.bind(this)
            },
            {
                commandId: 'codeXR.new_code_analysis.activeAnalyses.close',
                title: 'Close Analysis',
                handler: this.closeAnalysis.bind(this)
            },
            {
                commandId: 'codeXR.new_code_analysis.activeAnalyses.viewInBrowser',
                title: 'View in Browser',
                handler: this.viewInBrowser.bind(this)
            },
            {
                commandId: 'codeXR.new_code_analysis.activeAnalyses.refresh',
                title: 'Refresh Active Analyses',
                handler: this.refreshAnalyses.bind(this)
            }
        ];
    }
    /**
     * Get or create singleton instance
     */
    static getInstance(sessionRegistry, serverWatcher) {
        if (!ActiveAnalysesCommands.instance) {
            if (!sessionRegistry || !serverWatcher) {
                throw new Error('SessionRegistry and ServerWatcher are required for first initialization');
            }
            ActiveAnalysesCommands.instance = new ActiveAnalysesCommands(sessionRegistry, serverWatcher);
        }
        return ActiveAnalysesCommands.instance;
    }
    /**
     * Register all active analysis commands
     */
    registerCommands() {
        const disposables = [];
        for (const command of this.commands) {
            if (!this.registeredCommands.has(command.commandId)) {
                const disposable = vscode.commands.registerCommand(command.commandId, command.handler);
                disposables.push(disposable);
                this.registeredCommands.add(command.commandId);
            }
        }
        return disposables;
    }
    /**
     * Show analysis details command handler
     * SIMPLIFIED: Direct session access
     * FIXED: Handle TreeItem objects passed by VS Code context menus
     */
    async showDetails(arg) {
        console.log('ACTIVE_ANALYSES_COMMANDS: showDetails called with arg:', typeof arg, arg);
        try {
            let sessionId;
            if (typeof arg === 'string') {
                sessionId = arg;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using string arg as sessionId:', sessionId);
            }
            else if (arg && arg.session && arg.session.id) {
                sessionId = arg.session.id;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using arg.session.id as sessionId:', sessionId);
            }
            else if (arg && arg.sessionId) {
                // Handle sessionId property (added in getTreeItem)
                sessionId = arg.sessionId;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using arg.sessionId as sessionId:', sessionId);
            }
            else if (arg && arg.id) {
                // Handle id property (added in getTreeItem)
                sessionId = arg.id;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using arg.id as sessionId:', sessionId);
            }
            else if (arg && arg.originalNewCodeAnalysisItem) {
                // Handle wrapped objects from modular tree
                const originalItem = arg.originalNewCodeAnalysisItem;
                if (originalItem.sessionId) {
                    sessionId = originalItem.sessionId;
                    console.log('ACTIVE_ANALYSES_COMMANDS: Using originalNewCodeAnalysisItem.sessionId:', sessionId);
                }
                else if (originalItem.id) {
                    sessionId = originalItem.id;
                    console.log('ACTIVE_ANALYSES_COMMANDS: Using originalNewCodeAnalysisItem.id:', sessionId);
                }
                else if (originalItem.session && originalItem.session.id) {
                    sessionId = originalItem.session.id;
                    console.log('ACTIVE_ANALYSES_COMMANDS: Using originalNewCodeAnalysisItem.session.id:', sessionId);
                }
                else {
                    console.log('ACTIVE_ANALYSES_COMMANDS: No valid session ID in originalNewCodeAnalysisItem:', originalItem);
                    vscode.window.showErrorMessage('No analysis selected');
                    return;
                }
            }
            else {
                console.log('ACTIVE_ANALYSES_COMMANDS: No valid session ID found in arg');
                console.log('ACTIVE_ANALYSES_COMMANDS: Arg keys:', arg ? Object.keys(arg) : 'arg is null/undefined');
                vscode.window.showErrorMessage('No analysis selected');
                return;
            }
            console.log('ACTIVE_ANALYSES_COMMANDS: Looking for session with ID:', sessionId);
            const session = this.sessionRegistry.getSession(sessionId);
            if (!session) {
                console.log('ACTIVE_ANALYSES_COMMANDS: Session not found for ID:', sessionId);
                vscode.window.showErrorMessage('Analysis session not found');
                return;
            }
            console.log('ACTIVE_ANALYSES_COMMANDS: Session found, generating details content');
            // Show details in a new document
            const detailsContent = this.generateDetailsContent(session);
            const doc = await vscode.workspace.openTextDocument({
                content: detailsContent,
                language: 'markdown'
            });
            await vscode.window.showTextDocument(doc);
        }
        catch (error) {
            console.error('ACTIVE_ANALYSES_COMMANDS: Error showing analysis details:', error);
            vscode.window.showErrorMessage('Failed to show analysis details');
        }
    }
    /**
     * Close analysis command handler
     * FIXED: Now properly stops servers using ServerWatcherIntegration
     * FIXED: Handle TreeItem objects passed by VS Code context menus
     */
    async closeAnalysis(arg) {
        console.log('ACTIVE_ANALYSES_COMMANDS: closeAnalysis called with arg:', typeof arg, arg);
        try {
            let sessionId;
            if (typeof arg === 'string') {
                sessionId = arg;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using string arg as sessionId:', sessionId);
            }
            else if (arg && arg.session && arg.session.id) {
                sessionId = arg.session.id;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using arg.session.id as sessionId:', sessionId);
            }
            else if (arg && arg.sessionId) {
                // Handle sessionId property (added in getTreeItem)
                sessionId = arg.sessionId;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using arg.sessionId as sessionId:', sessionId);
            }
            else if (arg && arg.id) {
                // Handle id property (added in getTreeItem)
                sessionId = arg.id;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using arg.id as sessionId:', sessionId);
            }
            else if (arg && arg.originalNewCodeAnalysisItem) {
                // Handle wrapped objects from modular tree
                const originalItem = arg.originalNewCodeAnalysisItem;
                if (originalItem.sessionId) {
                    sessionId = originalItem.sessionId;
                    console.log('ACTIVE_ANALYSES_COMMANDS: Using originalNewCodeAnalysisItem.sessionId:', sessionId);
                }
                else if (originalItem.id) {
                    sessionId = originalItem.id;
                    console.log('ACTIVE_ANALYSES_COMMANDS: Using originalNewCodeAnalysisItem.id:', sessionId);
                }
                else if (originalItem.session && originalItem.session.id) {
                    sessionId = originalItem.session.id;
                    console.log('ACTIVE_ANALYSES_COMMANDS: Using originalNewCodeAnalysisItem.session.id:', sessionId);
                }
                else {
                    console.log('ACTIVE_ANALYSES_COMMANDS: No valid session ID in originalNewCodeAnalysisItem:', originalItem);
                    vscode.window.showErrorMessage('No analysis selected');
                    return;
                }
            }
            else {
                console.log('ACTIVE_ANALYSES_COMMANDS: No valid session ID found in arg');
                console.log('ACTIVE_ANALYSES_COMMANDS: Arg keys:', arg ? Object.keys(arg) : 'arg is null/undefined');
                vscode.window.showErrorMessage('No analysis selected');
                return;
            }
            console.log('ACTIVE_ANALYSES_COMMANDS: Looking for session with ID:', sessionId);
            const session = this.sessionRegistry.getSession(sessionId);
            if (!session) {
                console.log('ACTIVE_ANALYSES_COMMANDS: Session not found for ID:', sessionId);
                vscode.window.showErrorMessage('Analysis session not found');
                return;
            }
            // Show warning dialog for confirmation
            const choice = await vscode.window.showWarningMessage(`Are you sure you want to close the analysis for "${path.basename(session.targetPath || session.targetName || 'Unknown')}"?`, {
                modal: true,
                detail: `This will stop the analysis server and remove all associated watchers. The analysis results will be lost.\n\nTarget: ${session.targetPath || session.targetName || 'Unknown'}\nType: ${session.targetType || 'Unknown'}\nMode: ${session.analysisMode || 'Unknown'}`
            }, 'Yes, Close Analysis', 'Cancel');
            if (choice === 'Yes, Close Analysis') {
                console.log('ACTIVE_ANALYSES_COMMANDS: User confirmed closure, triggering cleanup');
                // STEP 1: First, remove the session from registry so watchers stop processing
                console.log('ACTIVE_ANALYSES_COMMANDS: Removing session from registry first');
                this.sessionRegistry.removeSession(sessionId);
                // STEP 2: Then trigger cleanup of servers and watchers
                console.log('ACTIVE_ANALYSES_COMMANDS: Triggering server and watcher cleanup');
                await this.serverWatcher.triggerManualCleanup(sessionId);
                // STEP 3: Refresh the tree view
                vscode.commands.executeCommand('codeXR.new_code_analysis.activeAnalyses.refresh');
                vscode.window.showInformationMessage('Analysis closed successfully');
            }
        }
        catch (error) {
            console.error('ACTIVE_ANALYSES_COMMANDS: Error closing analysis:', error);
            vscode.window.showErrorMessage('Failed to close analysis');
        }
    }
    /**
     * View in browser command handler
     * Opens the analysis result in browser if server URL is available
     * FIXED: Handle TreeItem objects passed by VS Code context menus
     */
    async viewInBrowser(arg) {
        console.log('ACTIVE_ANALYSES_COMMANDS: viewInBrowser called with arg:', typeof arg, arg);
        try {
            let sessionId;
            if (typeof arg === 'string') {
                sessionId = arg;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using string arg as sessionId:', sessionId);
            }
            else if (arg && arg.session && arg.session.id) {
                sessionId = arg.session.id;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using arg.session.id as sessionId:', sessionId);
            }
            else if (arg && arg.sessionId) {
                // Handle sessionId property (added in getTreeItem)
                sessionId = arg.sessionId;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using arg.sessionId as sessionId:', sessionId);
            }
            else if (arg && arg.id) {
                // Handle id property (added in getTreeItem)
                sessionId = arg.id;
                console.log('ACTIVE_ANALYSES_COMMANDS: Using arg.id as sessionId:', sessionId);
            }
            else if (arg && arg.originalNewCodeAnalysisItem) {
                // Handle wrapped objects from modular tree
                const originalItem = arg.originalNewCodeAnalysisItem;
                if (originalItem.sessionId) {
                    sessionId = originalItem.sessionId;
                    console.log('ACTIVE_ANALYSES_COMMANDS: Using originalNewCodeAnalysisItem.sessionId:', sessionId);
                }
                else if (originalItem.id) {
                    sessionId = originalItem.id;
                    console.log('ACTIVE_ANALYSES_COMMANDS: Using originalNewCodeAnalysisItem.id:', sessionId);
                }
                else if (originalItem.session && originalItem.session.id) {
                    sessionId = originalItem.session.id;
                    console.log('ACTIVE_ANALYSES_COMMANDS: Using originalNewCodeAnalysisItem.session.id:', sessionId);
                }
                else {
                    console.log('ACTIVE_ANALYSES_COMMANDS: No valid session ID in originalNewCodeAnalysisItem:', originalItem);
                    vscode.window.showErrorMessage('No analysis selected');
                    return;
                }
            }
            else {
                console.log('ACTIVE_ANALYSES_COMMANDS: No valid session ID found in arg');
                console.log('ACTIVE_ANALYSES_COMMANDS: Arg keys:', arg ? Object.keys(arg) : 'arg is null/undefined');
                vscode.window.showErrorMessage('No analysis selected');
                return;
            }
            console.log('ACTIVE_ANALYSES_COMMANDS: Looking for session with ID:', sessionId);
            const session = this.sessionRegistry.getSession(sessionId);
            if (!session) {
                console.log('ACTIVE_ANALYSES_COMMANDS: Session not found for ID:', sessionId);
                vscode.window.showErrorMessage('Analysis session not found');
                return;
            }
            // Check if session has a server URL
            if (!session.serverUrl && !session.assignedPort) {
                vscode.window.showErrorMessage('No server URL available for this analysis');
                return;
            }
            // Construct URL
            let url = session.serverUrl;
            if (!url && session.assignedPort) {
                url = `http://localhost:${session.assignedPort}`;
            }
            if (url) {
                try {
                    console.log('ACTIVE_ANALYSES_COMMANDS: Opening URL in browser:', url);
                    // Validate URL before parsing
                    const parsedUri = vscode.Uri.parse(url);
                    vscode.env.openExternal(parsedUri);
                }
                catch (uriError) {
                    console.error('ACTIVE_ANALYSES_COMMANDS: Invalid URL format:', url, uriError);
                    vscode.window.showErrorMessage(`Invalid URL format: ${url}`);
                }
            }
            else {
                vscode.window.showErrorMessage('Could not determine server URL for this analysis');
            }
        }
        catch (error) {
            console.error('ACTIVE_ANALYSES_COMMANDS: Error opening in browser:', error);
            vscode.window.showErrorMessage('Failed to open analysis in browser');
        }
    }
    /**
     * Refresh analyses command handler
     */
    async refreshAnalyses() {
        try {
            // Trigger tree view refresh
            vscode.commands.executeCommand('codeXR.new_code_analysis.refreshTreeView');
        }
        catch (error) {
            console.error('Error refreshing analyses:', error);
            vscode.window.showErrorMessage('Failed to refresh analyses');
        }
    }
    /**
     * Generate details content for an analysis session
     */
    generateDetailsContent(session) {
        return `# Analysis Details

## Session Information
- **Session ID**: ${session.id}
- **Target Path**: ${session.targetPath}
- **Target Type**: ${session.targetType}
- **Analysis Mode**: ${session.analysisMode}
- **Start Time**: ${session.startTime || 'Unknown'}
- **Status**: ${session.status || 'Active'}

## Server Information
${session.assignedPort ? `- **Port**: ${session.assignedPort}` : ''}
${session.serverUrl ? `- **Server URL**: ${session.serverUrl}` : ''}
${session.serverId ? `- **Server ID**: ${session.serverId}` : ''}

## Configuration
- **Is Deep Analysis**: ${session.isDeep || false}
- **Output Directory**: ${session.outputDirectory || 'Not set'}

## Progress
${session.progress ? `- **Progress**: ${session.progress}%` : '- **Progress**: Not available'}
${session.error ? `- **Error**: ${session.error}` : ''}
`;
    }
    /**
     * Dispose of all registered commands
     */
    dispose() {
        this.registeredCommands.clear();
    }
}
exports.ActiveAnalysesCommands = ActiveAnalysesCommands;


/***/ }),
/* 116 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Project Structure Commands
 * Commands for analyzing files and directories from project structure
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ProjectByLanguageCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
const analysisConfigurationStorage_1 = __webpack_require__(87);
const analysisOrchestrator_1 = __webpack_require__(117);
const supportedLanguages_1 = __webpack_require__(35);
class ProjectByLanguageCommands {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Get project structure command registrations (nested dolls pattern)
     */
    static getCommandRegistrations(context, refreshCallback) {
        console.log('NEW_CODE_ANALYSIS: Collecting Project Structure command registrations');
        const commands = new ProjectByLanguageCommands(context);
        const commandRegistrations = [
            {
                commandId: 'newCodeAnalysis.projectStructure.analyzeFile',
                callback: async (filePath) => {
                    console.log(`PROJECT_STRUCTURE_COMMANDS: Analyze file command TRIGGERED for: ${filePath}`);
                    console.log(`PROJECT_STRUCTURE_COMMANDS: Callback argument type: ${typeof filePath}`);
                    console.log(`PROJECT_STRUCTURE_COMMANDS: Callback argument value: "${filePath}"`);
                    await commands.analyzeFile(filePath);
                    refreshCallback();
                },
                description: 'Analyze a file using the configured analysis mode from Project Structure'
            },
            {
                commandId: 'newCodeAnalysis.projectStructure.analyzeDirectory',
                callback: async (directoryPath) => {
                    console.log(`PROJECT_STRUCTURE_COMMANDS: Analyze directory command TRIGGERED for: ${directoryPath}`);
                    console.log(`PROJECT_STRUCTURE_COMMANDS: Callback argument type: ${typeof directoryPath}`);
                    console.log(`PROJECT_STRUCTURE_COMMANDS: Callback argument value: "${directoryPath}"`);
                    await commands.analyzeDirectory(directoryPath);
                    refreshCallback();
                },
                description: 'Analyze a directory using the configured analysis mode from Project Structure'
            }
        ];
        console.log(`NEW_CODE_ANALYSIS: Collected ${commandRegistrations.length} Project Structure command registrations`);
        return commandRegistrations;
    }
    /**
     * Analyze a file using the configured file analysis mode
     */
    async analyzeFile(filePath) {
        console.log(`PROJECT_STRUCTURE_COMMANDS: Analyzing file: ${filePath}`);
        console.log(`PROJECT_STRUCTURE_COMMANDS: FilePath type: ${typeof filePath}`);
        console.log(`PROJECT_STRUCTURE_COMMANDS: FilePath value: "${filePath}"`);
        try {
            // Validate input
            if (!filePath || typeof filePath !== 'string') {
                console.error('PROJECT_STRUCTURE_COMMANDS: Invalid filePath received:', filePath);
                vscode.window.showErrorMessage('Invalid file path received');
                return;
            }
            const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(this.context);
            const analysisMode = await storage.getAnalysisMode();
            console.log(`PROJECT_STRUCTURE_COMMANDS: Using analysis mode: ${analysisMode}`);
            // Get file extension and language
            const ext = path.extname(filePath).toLowerCase();
            const languageConfig = (0, supportedLanguages_1.getLanguageByExtension)(ext);
            const languageName = languageConfig?.name || 'Unknown';
            console.log(`PROJECT_STRUCTURE_COMMANDS: Running analysis for ${filePath} (${languageName}) in ${analysisMode} mode`);
            // Determine which analysis to run based on file type and configuration
            if (languageName === 'HTML') {
                // HTML files always use DOM Visualization using NEW ENGINE
                console.log('PROJECT_STRUCTURE_COMMANDS: Running DOM Visualization for HTML file using NEW ENGINE');
                await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(filePath, 'VisualizeDOM', 'file', this.context, false);
            }
            else {
                // For other files, use the configured analysis mode with NEW ENGINE
                if (analysisMode === 'XR') {
                    console.log('PROJECT_STRUCTURE_COMMANDS: Running XR Analysis using NEW ENGINE');
                    await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(filePath, 'XR', 'file', this.context, false);
                }
                else if (analysisMode === 'LivePanel') {
                    console.log('PROJECT_STRUCTURE_COMMANDS: Running LivePanel Analysis using NEW ENGINE');
                    await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(filePath, 'LivePanel', 'file', this.context, false);
                }
                else {
                    vscode.window.showWarningMessage(`Unknown analysis mode: ${analysisMode}`);
                }
            }
            vscode.window.showInformationMessage(`Started ${analysisMode} analysis for file: ${path.basename(filePath)}`);
        }
        catch (error) {
            console.error('PROJECT_STRUCTURE_COMMANDS: Error analyzing file:', error);
            vscode.window.showErrorMessage(`Failed to analyze file: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Analyze a directory using the configured directory analysis mode
     */
    async analyzeDirectory(directoryPath) {
        console.log(`PROJECT_STRUCTURE_COMMANDS: Analyzing directory: ${directoryPath}`);
        console.log(`PROJECT_STRUCTURE_COMMANDS: DirectoryPath type: ${typeof directoryPath}`);
        console.log(`PROJECT_STRUCTURE_COMMANDS: DirectoryPath value: "${directoryPath}"`);
        try {
            // Validate input
            if (!directoryPath || typeof directoryPath !== 'string') {
                console.error('PROJECT_STRUCTURE_COMMANDS: Invalid directoryPath received:', directoryPath);
                vscode.window.showErrorMessage('Invalid directory path received');
                return;
            }
            const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(this.context);
            const directoryAnalysisMode = await storage.getAnalysisDirectoryMode();
            console.log(`PROJECT_STRUCTURE_COMMANDS: Using directory analysis mode: ${directoryAnalysisMode}`);
            // Map directory analysis mode to AnalysisOrchestrator parameters
            let analysisMode;
            let isDeep;
            switch (directoryAnalysisMode) {
                case 'XR':
                    analysisMode = 'XR';
                    isDeep = false;
                    break;
                case 'XRDeep':
                    analysisMode = 'XR';
                    isDeep = true;
                    break;
                case 'LivePanel':
                    analysisMode = 'LivePanel';
                    isDeep = false;
                    break;
                case 'LivePanelDeep':
                    analysisMode = 'LivePanel';
                    isDeep = true;
                    break;
                default:
                    vscode.window.showWarningMessage(`Unknown directory analysis mode: ${directoryAnalysisMode}`);
                    return;
            }
            console.log(`PROJECT_STRUCTURE_COMMANDS: Running ${analysisMode} Directory Analysis (Deep: ${isDeep}) using NEW ENGINE`);
            await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(directoryPath, analysisMode, 'directory', this.context, isDeep);
            vscode.window.showInformationMessage(`Started ${directoryAnalysisMode} analysis for directory: ${path.basename(directoryPath)}`);
        }
        catch (error) {
            console.error('PROJECT_STRUCTURE_COMMANDS: Error analyzing directory:', error);
            vscode.window.showErrorMessage(`Failed to analyze directory: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
}
exports.ProjectByLanguageCommands = ProjectByLanguageCommands;


/***/ }),
/* 117 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Analysis Orchestrator
 * Central coordinator for all analysis operations in the new engine
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisOrchestrator = void 0;
const vscode = __importStar(__webpack_require__(1));
const launcherLivePanel_1 = __webpack_require__(118);
const launcherXRAnalysis_1 = __webpack_require__(131);
const launcherVisualizeDOM_1 = __webpack_require__(132);
const sessionRegistry_1 = __webpack_require__(32);
class AnalysisOrchestrator {
    /**
     * Orchestrate analysis based on mode, target type and depth
     */
    static async orchestrateAnalysis(targetPath, analysisMode, targetType, context, isDeep = false) {
        console.log(`ANALYSIS_ORCHESTRATOR: Orchestrating ${analysisMode} analysis for ${targetType}: ${targetPath} (deep: ${isDeep})`);
        try {
            // Create unified session first - CENTRALIZED SESSION CREATION
            const registry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
            let session;
            try {
                console.log(`ANALYSIS_ORCHESTRATOR: 🔍 DEBUG - Creating session with parameters:`, {
                    targetPath,
                    targetType,
                    analysisMode,
                    isDeep
                });
                session = await registry.createSession({
                    targetPath,
                    targetType,
                    analysisMode,
                    isDeep,
                    context
                });
                // Check if session creation was skipped due to duplicate
                if (!session) {
                    console.log(`ANALYSIS_ORCHESTRATOR: Session creation skipped (duplicate detected) - stopping orchestration`);
                    return; // Stop here if duplicate was detected
                }
            }
            catch (sessionError) {
                // Handle session creation errors (e.g., unsupported file types)
                console.error(`ANALYSIS_ORCHESTRATOR: Session creation failed:`, sessionError);
                const errorMessage = sessionError instanceof Error ? sessionError.message : String(sessionError);
                vscode.window.showErrorMessage(`CodeXR Analysis Error: ${errorMessage}`);
                throw sessionError;
            }
            console.log(`ANALYSIS_ORCHESTRATOR: Created session ${session.id} for ${analysisMode} analysis`);
            console.log(`ANALYSIS_ORCHESTRATOR: Session details:`, {
                id: session.id,
                targetName: session.targetName,
                targetPath: session.targetPath,
                analysisMode: session.analysisMode,
                targetType: session.targetType,
                isDeep: session.isDeep,
                status: session.status
            });
            // Send session to appropriate launcher
            switch (analysisMode) {
                case 'LivePanel':
                    await this.orchestrateLivePanelAnalysis(session, context);
                    break;
                case 'XR':
                    await this.orchestrateXRAnalysis(session, context);
                    break;
                case 'VisualizeDOM':
                    await this.orchestrateVisualizeDOMAnalysis(session, context);
                    break;
                default:
                    throw new Error(`Unknown analysis mode: ${analysisMode}`);
            }
        }
        catch (error) {
            console.error('ANALYSIS_ORCHESTRATOR: Error orchestrating analysis:', error);
            throw error;
        }
    }
    /**
     * Orchestrate Live Panel analysis with session
     */
    static async orchestrateLivePanelAnalysis(session, context) {
        switch (session.targetType) {
            case 'file':
                console.log('ANALYSIS_ORCHESTRATOR: Launching Live Panel analysis for file:', session.targetPath);
                await launcherLivePanel_1.LauncherLivePanel.launchFileLivePanelAnalysis(session, context);
                break;
            case 'directory':
                console.log('ANALYSIS_ORCHESTRATOR: Launching Live Panel analysis for directory:', session.targetPath);
                await launcherLivePanel_1.LauncherLivePanel.launchDirectoryLivePanelAnalysis(session, context);
                break;
            default:
                throw new Error(`Unknown target type: ${session.targetType}`);
        }
    }
    /**
     * Orchestrate XR analysis with session
     */
    static async orchestrateXRAnalysis(session, context) {
        switch (session.targetType) {
            case 'file':
                await launcherXRAnalysis_1.LauncherXRAnalysis.launchFileXRAnalysis(session, context);
                break;
            case 'directory':
                await launcherXRAnalysis_1.LauncherXRAnalysis.launchDirectoryXRAnalysis(session, context);
                break;
            default:
                throw new Error(`Unknown target type: ${session.targetType}`);
        }
    }
    /**
     * Orchestrate VisualizeDOM analysis with session
     */
    static async orchestrateVisualizeDOMAnalysis(session, context) {
        // VisualizeDOM only supports files (HTML files specifically)
        if (session.targetType !== 'file') {
            throw new Error(`VisualizeDOM analysis only supports files, not ${session.targetType}`);
        }
        console.log('ANALYSIS_ORCHESTRATOR: Launching VisualizeDOM analysis for file:', session.targetPath);
        await launcherVisualizeDOM_1.LauncherVisualizeDOM.launchVisualizeDOMAnalysis(session, context);
    }
    /**
     * Get analysis type identifier for the given parameters
     */
    static getAnalysisTypeId(analysisMode, targetType, isDeep) {
        const modePrefix = analysisMode === 'LivePanel' ? 'LP' :
            analysisMode === 'XR' ? 'XR' :
                analysisMode === 'VisualizeDOM' ? 'DOM' : 'UNKNOWN';
        const targetPrefix = targetType === 'file' ? 'File' : 'Dir';
        const depthSuffix = isDeep ? 'Deep' : '';
        return `${modePrefix}_${targetPrefix}${depthSuffix}`;
    }
}
exports.AnalysisOrchestrator = AnalysisOrchestrator;


/***/ }),
/* 118 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Live Panel Analysis Launcher
 * Handles the launch and orchestration of Live Panel analysis for both files and directories.
 *
 * Delegates the common 4-step pipeline to {@link executeLaunchPipeline},
 * providing only LivePanel-specific configuration (theme, folder name, watcher type).
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LauncherLivePanel = void 0;
const sessionRegistry_1 = __webpack_require__(32);
const FileRequirementProcessor_1 = __webpack_require__(119);
const sessionWatcherManager_1 = __webpack_require__(93);
const analysisConfigurationStorage_1 = __webpack_require__(87);
const launchPipeline_1 = __webpack_require__(130);
const LOG_PREFIX = 'NEW_LAUNCHER_LIVEPANEL_ANALYSIS';
class LauncherLivePanel {
    /**
     * Launch Live Panel analysis for a file using session
     */
    static async launchFileLivePanelAnalysis(session, context) {
        const registry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
        const configStorage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
        await (0, launchPipeline_1.executeLaunchPipeline)(session, context, {
            logPrefix: LOG_PREFIX,
            folderName: 'fileAnalysis',
            progress: { process: 20, save: 40, watcherOrServer: 60, serverOrWatcher: 80, done: 100 },
            preValidation: async () => {
                if (!session.hash256) {
                    console.error(`${LOG_PREFIX}: File hash not found in session ${session.id}`);
                    registry.updateSessionStatus(session.id, 'error', 100);
                    throw new Error('File hash not found');
                }
                console.log(`${LOG_PREFIX}: File hash from session: ${session.hash256.substring(0, 12)}...`);
            },
            processRequirements: async () => {
                const currentTheme = await configStorage.getViewTheme();
                console.log(`${LOG_PREFIX}: Using theme: ${currentTheme}`);
                const processor = new FileRequirementProcessor_1.FileRequirementProcessor(context);
                return processor.processRequirements(session, currentTheme);
            },
            startWatcher: async () => {
                const watcher = new sessionWatcherManager_1.SessionWatcherManager(context);
                return watcher.startWatchingSession(session);
            },
        });
    }
    /**
     * Launch Live Panel analysis for a directory using session
     */
    static async launchDirectoryLivePanelAnalysis(session, context) {
        const registry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
        const configStorage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
        await (0, launchPipeline_1.executeLaunchPipeline)(session, context, {
            logPrefix: LOG_PREFIX,
            folderName: 'directoryAnalysis',
            progress: { process: 20, save: 40, watcherOrServer: 60, serverOrWatcher: 80, done: 100 },
            preValidation: async () => {
                const dirs = session.directoriesToAnalyze || [];
                const files = session.filesToHash || [];
                console.log(`${LOG_PREFIX}: Session has ${dirs.length} directories, ${files.length} files to hash`);
                if (dirs.length === 0) {
                    console.warn(`${LOG_PREFIX}: No directories to analyze for session ${session.id}`);
                    registry.updateSessionStatus(session.id, 'error', undefined, 'No directories to analyze');
                    throw new Error('No directories to analyze');
                }
                if (files.length === 0) {
                    console.warn(`${LOG_PREFIX}: No files to hash for session ${session.id}`);
                }
            },
            processRequirements: async () => {
                const currentTheme = await configStorage.getViewTheme();
                console.log(`${LOG_PREFIX}: Using theme: ${currentTheme}`);
                const processor = new FileRequirementProcessor_1.FileRequirementProcessor(context);
                return processor.processRequirements(session, currentTheme);
            },
            startWatcher: async () => {
                const watcher = new sessionWatcherManager_1.SessionWatcherManager(context);
                return watcher.startWatchingSession(session);
            },
        });
    }
}
exports.LauncherLivePanel = LauncherLivePanel;


/***/ }),
/* 119 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FileRequirementProcessor = void 0;
const LivePanelFileRequirements_1 = __webpack_require__(120);
const LivePanelDirectoryRequirements_1 = __webpack_require__(123);
const VisualizeDOMRequirements_1 = __webpack_require__(111);
const XRFileRequirements_1 = __webpack_require__(124);
const XRDirectoryRequirements_1 = __webpack_require__(128);
const themeUtils_1 = __webpack_require__(122);
/**
 * Main class for processing and determining required files for analysis
 *
 * This class acts as the main orchestrator that:
 * - Receives session from SessionRegistry
 * - Delegates to specific processors based on analysis mode and target type
 * - Returns necessary files without processing them (only paths and metadata)
 */
class FileRequirementProcessor {
    livePanelFileRequirements;
    livePanelDirectoryRequirements;
    visualizeDOMRequirements;
    xrFileRequirements;
    xrDirectoryRequirements;
    context;
    constructor(context) {
        console.log('FILE_REQUIREMENT_PROCESSOR: Initializing FileRequirementProcessor...');
        this.context = context;
        this.livePanelFileRequirements = new LivePanelFileRequirements_1.LivePanelFileRequirements(context);
        this.livePanelDirectoryRequirements = new LivePanelDirectoryRequirements_1.LivePanelDirectoryRequirements(context);
        this.visualizeDOMRequirements = new VisualizeDOMRequirements_1.VisualizeDOMRequirements(context);
        this.xrFileRequirements = new XRFileRequirements_1.XRFileRequirements(context);
        this.xrDirectoryRequirements = new XRDirectoryRequirements_1.XRDirectoryRequirements(context);
        this.xrFileRequirements = new XRFileRequirements_1.XRFileRequirements(context);
        // Initialize ThemeUtils with extension context
        themeUtils_1.ThemeUtils.initialize(context);
        console.log('FILE_REQUIREMENT_PROCESSOR: ThemeUtils initialized');
        console.log('FILE_REQUIREMENT_PROCESSOR: VisualizeDOMRequirements initialized');
    }
    /**
     * MAIN FUNCTION: Process file requirements for a session
     *
     * @param session - Unified analysis session
     * @param theme - Current user theme (optional, defaults to 'vscode-light')
     * @returns Promise with required files (paths and metadata only)
     */
    async processRequirements(session, theme) {
        console.log(`FILE_REQUIREMENT_PROCESSOR: 🚀 Processing requirements for session ${session.id}`);
        console.log(`FILE_REQUIREMENT_PROCESSOR: Analysis mode: ${session.analysisMode}, Target type: ${session.targetType}`);
        console.log(`FILE_REQUIREMENT_PROCESSOR: Target path: ${session.targetPath}`);
        console.log(`FILE_REQUIREMENT_PROCESSOR: Theme: ${theme || 'default'}`);
        try {
            let requirements;
            // Determine which processor to use based on analysis mode and target type
            switch (session.analysisMode) {
                case 'LivePanel':
                    if (session.targetType === 'file') {
                        console.log(`FILE_REQUIREMENT_PROCESSOR: Delegating to LivePanelFileRequirements...`);
                        requirements = await this.livePanelFileRequirements.getRequiredFiles(session, theme);
                    }
                    else if (session.targetType === 'directory') {
                        console.log(`FILE_REQUIREMENT_PROCESSOR: Delegating to LivePanelDirectoryRequirements...`);
                        requirements = await this.livePanelDirectoryRequirements.getRequiredFiles(session, theme);
                    }
                    else {
                        throw new Error(`Unknown target type for LivePanel: ${session.targetType}`);
                    }
                    break;
                case 'VisualizeDOM':
                    console.log(`FILE_REQUIREMENT_PROCESSOR: Delegating to VisualizeDOMRequirements...`);
                    requirements = await this.visualizeDOMRequirements.getRequiredFiles(session, theme);
                    break;
                case 'XR':
                    if (session.targetType === 'file') {
                        console.log(`FILE_REQUIREMENT_PROCESSOR: Delegating to XRFileRequirements...`);
                        requirements = await this.xrFileRequirements.getRequiredFiles(session, theme);
                    }
                    else if (session.targetType === 'directory') {
                        console.log(`FILE_REQUIREMENT_PROCESSOR: Delegating to XRDirectoryRequirements...`);
                        const xrResult = await this.xrDirectoryRequirements.processDirectoryXRRequirements(session);
                        if (!xrResult.success) {
                            throw new Error(`XR directory processing failed: ${xrResult.error}`);
                        }
                        // Convert XR result to ProcessedRequirements format
                        requirements = {
                            sessionId: session.id,
                            analysisMode: session.analysisMode,
                            targetPath: session.targetPath,
                            loadedFiles: xrResult.loadedFiles,
                            estimatedComplexity: 'low',
                            processingTime: new Date()
                        };
                    }
                    else {
                        throw new Error(`Unknown target type for XR: ${session.targetType}`);
                    }
                    break;
                default:
                    throw new Error(`Unknown analysis mode: ${session.analysisMode}`);
            }
            console.log(`FILE_REQUIREMENT_PROCESSOR: ✅ Requirements processed successfully for session ${session.id}`);
            console.log(`FILE_REQUIREMENT_PROCESSOR: Total files loaded: ${requirements.loadedFiles.size}`);
            console.log(`FILE_REQUIREMENT_PROCESSOR: Estimated complexity: ${requirements.estimatedComplexity}`);
            return requirements;
        }
        catch (error) {
            console.error(`FILE_REQUIREMENT_PROCESSOR: ❌ Error processing requirements for session ${session.id}:`, error);
            throw error;
        }
    }
}
exports.FileRequirementProcessor = FileRequirementProcessor;


/***/ }),
/* 120 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LivePanelFileRequirements = void 0;
const livePanelParser_1 = __webpack_require__(121);
const executePython_1 = __webpack_require__(96);
/**
 * Handles template files for LivePanel analysis
 *
 * This class:
 * - Determines which templates are needed based on analysis type
 * - Calls LivePanelParser to load actual files
 * - Returns loaded files, not just paths
 */
class LivePanelFileRequirements {
    livePanelParser;
    executePython;
    context;
    constructor(context) {
        console.log('LIVEPANEL_FILE_REQUIREMENTS: Initializing LivePanelFileRequirements...');
        this.context = context;
        this.livePanelParser = new livePanelParser_1.LivePanelParser();
        this.executePython = new executePython_1.ExecutePython(context);
    }
    /**
     * Gets loaded template files for LivePanel analysis
     *
     * @param session - Unified analysis session
     * @param theme - Current user theme (optional, defaults to 'vscode-light')
     * @returns Promise with loaded template files
     */
    async getRequiredFiles(session, theme) {
        console.log(`LIVEPANEL_FILE_REQUIREMENTS: 🎯 Getting template files for LivePanel analysis`);
        console.log(`LIVEPANEL_FILE_REQUIREMENTS: Target type: ${session.targetType}`);
        console.log(`LIVEPANEL_FILE_REQUIREMENTS: Theme: ${theme || 'default'}`);
        try {
            // STEP 1: Load template files
            console.log(`LIVEPANEL_FILE_REQUIREMENTS: 📁 Step 1 - Loading template files...`);
            const loadedFiles = await this.livePanelParser.loadTemplateFiles(session.targetType, session.analysisMode, theme);
            // STEP 2: Execute Python analysis to get data.json
            console.log(`LIVEPANEL_FILE_REQUIREMENTS: 🐍 Step 2 - Executing Python analysis...`);
            if (session.analysisMode === 'LivePanel' && session.targetType === 'file') {
                console.log(`LIVEPANEL_FILE_REQUIREMENTS: 📄 Executing file analysis and generating data.json`);
                const analysisData = await this.executePython.executeAnalysis(session);
                console.log(`LIVEPANEL_FILE_REQUIREMENTS: ✅ Python analysis completed successfully!`);
                console.log(`LIVEPANEL_FILE_REQUIREMENTS: 📊 Generated data.json:`, JSON.stringify(analysisData, null, 2));
                // Convert analysisData to JSON string and add to loadedFiles as data.json
                const dataJsonContent = JSON.stringify(analysisData, null, 2);
                loadedFiles.set('data.json', dataJsonContent);
                console.log(`LIVEPANEL_FILE_REQUIREMENTS: ✅ Added data.json to template files (${dataJsonContent.length} characters)`);
            }
            else {
                console.log(`LIVEPANEL_FILE_REQUIREMENTS: ⏭️ Skipping Python analysis for ${session.analysisMode}/${session.targetType}`);
            }
            const requirements = {
                sessionId: session.id,
                analysisMode: session.analysisMode,
                targetPath: session.targetPath,
                loadedFiles: loadedFiles,
                estimatedComplexity: 'low',
                processingTime: new Date()
            };
            console.log(`LIVEPANEL_FILE_REQUIREMENTS: ✅ Loaded ${loadedFiles.size} template files (including data.json)`);
            console.log(`LIVEPANEL_FILE_REQUIREMENTS: 📋 Files prepared:`, Array.from(loadedFiles.keys()));
            return requirements;
        }
        catch (error) {
            console.error(`LIVEPANEL_FILE_REQUIREMENTS: ❌ Error loading template files:`, error);
            throw error;
        }
    }
}
exports.LivePanelFileRequirements = LivePanelFileRequirements;


/***/ }),
/* 121 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * LivePanel Parser (Unified)
 *
 * Single parser for both file-level and directory-level LivePanel analysis.
 * Loads template files from `templates/analysis_livePanel/{targetType}/`,
 * processes placeholders (nonce, theme, style/script URIs) and returns a
 * `Map<string, string>` of processed template contents.
 *
 * Replaces the former `FileLivePanelParser` and `DirectoryLivePanelParser`
 * which were ~95 % identical.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LivePanelParser = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(6));
const nonceGenerator_1 = __webpack_require__(12);
const themeUtils_1 = __webpack_require__(122);
class LivePanelParser {
    // ── Public API ───────────────────────────────────────────────────
    /**
     * Load and process template files for a LivePanel analysis session.
     *
     * @param targetType  - `'file'` or `'directory'` (determines the template subfolder)
     * @param analysisMode - Analysis mode label (e.g. `'LivePanel'`)
     * @param theme        - Optional theme override (`'Dark'` | `'Light'`)
     * @returns A Map of `fileName → processedContent`
     */
    async loadTemplateFiles(targetType, analysisMode, theme) {
        const templatePath = this.getTemplatePath(targetType);
        const loadedFiles = await this.loadAllFilesFromDirectory(templatePath);
        const processedFiles = await this.processTemplateFiles(loadedFiles, theme);
        return processedFiles;
    }
    // ── Template path resolution ─────────────────────────────────────
    getTemplatePath(targetType) {
        const extensionPath = this.getExtensionPath();
        // e.g. …/templates/analysis_livePanel/file/
        return path.join(extensionPath, 'templates', 'analysis_livePanel', targetType);
    }
    getExtensionPath() {
        const extension = vscode.extensions.getExtension('amonteSl.code-xr');
        if (!extension) {
            throw new Error('Extension amonteSl.code-xr not found');
        }
        return extension.extensionPath;
    }
    // ── File loading ─────────────────────────────────────────────────
    async loadAllFilesFromDirectory(dirPath) {
        if (!fs.existsSync(dirPath)) {
            throw new Error(`Template directory does not exist: ${dirPath}`);
        }
        const loadedFiles = new Map();
        for (const fileName of fs.readdirSync(dirPath)) {
            const filePath = path.join(dirPath, fileName);
            if (fs.statSync(filePath).isFile()) {
                loadedFiles.set(fileName, fs.readFileSync(filePath, 'utf8'));
            }
        }
        return loadedFiles;
    }
    // ── Template processing ──────────────────────────────────────────
    /**
     * Walk through loaded files and normalise them:
     *  - HTML  → placeholder replacement  (nonce, theme, style.css, main.js)
     *  - JS    → renamed to `main.js`     (first non-empty JS wins)
     *  - CSS   → renamed to `style.css`
     *  - other → kept as-is
     */
    async processTemplateFiles(loadedFiles, theme) {
        const processedFiles = new Map();
        const nonce = (0, nonceGenerator_1.generateNonce)();
        // Find the first non-empty JS file to use as main.js
        let mainJsContent = null;
        let mainJsFileName = null;
        for (const [fileName, content] of loadedFiles) {
            if (fileName.endsWith('.js') && content.trim().length > 0) {
                mainJsContent = content;
                mainJsFileName = fileName;
                break;
            }
        }
        for (const [fileName, content] of loadedFiles) {
            if (fileName.endsWith('.html')) {
                processedFiles.set(fileName, await this.processHtmlTemplate(content, nonce, theme));
            }
            else if (fileName.endsWith('.js')) {
                // Only emit the first non-empty JS file as main.js
                if (mainJsContent && fileName === mainJsFileName) {
                    processedFiles.set('main.js', mainJsContent);
                }
                // Skip any other JS files (empty or duplicates)
            }
            else if (fileName.endsWith('.css')) {
                processedFiles.set('style.css', content);
            }
            else {
                processedFiles.set(fileName, content);
            }
        }
        return processedFiles;
    }
    // ── HTML placeholder replacement ─────────────────────────────────
    async processHtmlTemplate(content, nonce, theme) {
        const resolvedTheme = await this.resolveTheme(theme);
        return content
            .replace(/\$\{nonce\}/g, nonce)
            .replace(/\$\{styleUri\}/g, 'style.css')
            .replace(/\$\{scriptUri\}/g, 'main.js')
            .replace(/\$\{theme\}/g, resolvedTheme);
    }
    // ── Theme resolution ─────────────────────────────────────────────
    /**
     * Normalise a theme value to `'dark'` or `'light'`.
     * Falls back to the user preference stored in configuration,
     * and ultimately defaults to `'light'`.
     */
    async resolveTheme(theme) {
        if (!theme) {
            try {
                return await themeUtils_1.ThemeUtils.getUserTheme();
            }
            catch {
                return 'light';
            }
        }
        const lower = theme.toLowerCase();
        if (lower === 'dark' || lower === 'light') {
            return lower;
        }
        return 'light';
    }
}
exports.LivePanelParser = LivePanelParser;


/***/ }),
/* 122 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ThemeUtils = void 0;
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
/**
 * Theme utilities for new analysis engine
 *
 * This utility provides functions to retrieve and apply user's theme preferences
 * from the analysis configuration storage.
 */
class ThemeUtils {
    static context = null;
    static STORAGE_FOLDER = 'codexr_analysis';
    static CONFIG_FILE = 'configuration_analysis.json';
    /**
     * Initialize the theme utils with extension context
     */
    static initialize(context) {
        this.context = context;
        console.log('THEME_UTILS: ✅ Initialized with extension context');
        console.log(`THEME_UTILS: 📂 GlobalStorage path: ${context.globalStorageUri.fsPath}`);
        // Test configuration loading immediately
        this.testConfigurationAccess();
    }
    /**
     * Test if we can access the configuration file
     */
    static async testConfigurationAccess() {
        try {
            const theme = await this.getUserTheme();
            console.log(`THEME_UTILS: 🧪 Initialization test - detected theme: ${theme}`);
        }
        catch (error) {
            console.error('THEME_UTILS: ❌ Initialization test failed:', error);
        }
    }
    /**
     * Get the full path to the configuration file
     */
    static getConfigPath() {
        if (!this.context) {
            throw new Error('THEME_UTILS: Context not initialized');
        }
        return path.join(this.context.globalStorageUri.fsPath, this.STORAGE_FOLDER, this.CONFIG_FILE);
    }
    /**
     * Load configuration from the actual file
     */
    static async loadConfiguration() {
        try {
            const configPath = this.getConfigPath();
            console.log(`THEME_UTILS: Loading configuration from ${configPath}`);
            // Check if file exists
            if (!fs.existsSync(configPath)) {
                console.warn(`THEME_UTILS: ❌ Configuration file not found at ${configPath}`);
                return null;
            }
            // Check file stats
            const stats = fs.statSync(configPath);
            console.log(`THEME_UTILS: 📊 File size: ${stats.size} bytes, modified: ${stats.mtime}`);
            const configContent = fs.readFileSync(configPath, 'utf8');
            console.log(`THEME_UTILS: 📖 Raw content length: ${configContent.length} chars`);
            const config = JSON.parse(configContent);
            console.log(`THEME_UTILS: ✅ Configuration loaded successfully`);
            console.log(`THEME_UTILS: 🔍 ViewTheme value: ${config?.configuration?.viewTheme}`);
            return config;
        }
        catch (error) {
            console.error(`THEME_UTILS: ❌ Error loading configuration:`, error);
            if (error instanceof Error) {
                console.error(`THEME_UTILS: 🔍 Error type: ${error.constructor.name}`);
                console.error(`THEME_UTILS: 📝 Error message: ${error.message}`);
            }
            return null;
        }
    }
    /**
     * Get the user's selected theme from analysis settings
     * @returns The theme name ('light' or 'dark')
     */
    static async getUserTheme() {
        console.log(`THEME_UTILS: 🔍 getUserTheme called, context initialized: ${!!this.context}`);
        if (!this.context) {
            console.warn('THEME_UTILS: ⚠️ Extension context not initialized, trying fallback method');
            // Try fallback method without context
            return this.getUserThemeFallback();
        }
        try {
            const config = await this.loadConfiguration();
            if (!config || !config.configuration || !config.configuration.viewTheme) {
                console.warn('THEME_UTILS: ⚠️ No viewTheme found in configuration, trying fallback');
                return this.getUserThemeFallback();
            }
            const viewTheme = config.configuration.viewTheme;
            console.log(`THEME_UTILS: 📖 Retrieved viewTheme from config: ${viewTheme}`);
            // Convert the viewTheme value to template theme class (simply "dark" or "light")
            let templateTheme;
            if (viewTheme === 'Dark' || viewTheme === 'dark') {
                templateTheme = 'dark';
            }
            else if (viewTheme === 'Light' || viewTheme === 'light') {
                templateTheme = 'light';
            }
            else {
                console.warn(`THEME_UTILS: ⚠️ Unknown viewTheme '${viewTheme}', using default light theme`);
                templateTheme = 'light';
            }
            console.log(`THEME_UTILS: ✅ Converted to template theme: ${templateTheme}`);
            return templateTheme;
        }
        catch (error) {
            console.error('THEME_UTILS: ❌ Error retrieving user theme:', error);
            console.warn('THEME_UTILS: 🔄 Trying fallback method');
            return this.getUserThemeFallback();
        }
    }
    /**
     * Fallback method to get theme without context dependency
     */
    static getUserThemeFallback() {
        try {
            // Try to read directly from the known path
            const fallbackPath = '/home/adrian/.config/Code/User/globalStorage/amontesl.code-xr/codexr_analysis/configuration_analysis.json';
            console.log(`THEME_UTILS: 🔄 Trying fallback path: ${fallbackPath}`);
            if (fs.existsSync(fallbackPath)) {
                const configContent = fs.readFileSync(fallbackPath, 'utf8');
                const config = JSON.parse(configContent);
                if (config?.configuration?.viewTheme) {
                    const viewTheme = config.configuration.viewTheme;
                    console.log(`THEME_UTILS: 🎯 Fallback found viewTheme: ${viewTheme}`);
                    if (viewTheme === 'Dark' || viewTheme === 'dark') {
                        return 'dark';
                    }
                    else if (viewTheme === 'Light' || viewTheme === 'light') {
                        return 'light';
                    }
                }
            }
        }
        catch (error) {
            console.error('THEME_UTILS: ❌ Fallback method failed:', error);
        }
        console.log('THEME_UTILS: 🔄 All methods failed, using default light theme');
        return 'light';
    }
    /**
     * Get the user's selected theme synchronously (if context is available)
     * Note: This is a fallback method, prefer async getUserTheme()
     */
    static getUserThemeSync() {
        // For now, return default theme for sync calls
        // This could be enhanced to cache the last known theme
        console.warn('THEME_UTILS: Sync theme access requested, returning default');
        return 'light';
    }
    /**
     * Get CSS class name for the user's theme
     * @returns The CSS class name for the theme
     */
    static async getUserThemeClass() {
        const theme = await this.getUserTheme();
        return theme; // The theme name is already the CSS class name
    }
    /**
     * Check if user has dark theme enabled
     * @returns true if dark theme is enabled
     */
    static async isDarkTheme() {
        const theme = await this.getUserTheme();
        return theme === 'dark';
    }
    /**
     * Check if user has light theme enabled
     * @returns true if light theme is enabled
     */
    static async isLightTheme() {
        const theme = await this.getUserTheme();
        return theme === 'light';
    }
    /**
     * Get theme-specific color values for charts and UI elements
     * @returns Object with theme-specific colors
     */
    static async getThemeColors() {
        const isDark = await this.isDarkTheme();
        return {
            foreground: isDark ? '#ffffff' : '#000000',
            background: isDark ? '#1e1e1e' : '#ffffff',
            border: isDark ? '#404040' : '#e0e0e0'
        };
    }
}
exports.ThemeUtils = ThemeUtils;


/***/ }),
/* 123 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LivePanelDirectoryRequirements = void 0;
const livePanelParser_1 = __webpack_require__(121);
const executePython_1 = __webpack_require__(96);
/**
 * Handles template files for LivePanel directory analysis
 *
 * This class:
 * - Determines which templates are needed based on analysis type
 * - Calls LivePanelParser to load actual files
 * - Returns loaded files, not just paths
 */
class LivePanelDirectoryRequirements {
    livePanelParser;
    context;
    executePython;
    constructor(context) {
        console.log('LIVEPANEL_DIRECTORY_REQUIREMENTS: Initializing LivePanelDirectoryRequirements...');
        this.context = context;
        this.livePanelParser = new livePanelParser_1.LivePanelParser();
        this.executePython = new executePython_1.ExecutePython(context);
    }
    /**
     * Gets loaded template files for LivePanel directory analysis
     *
     * @param session - Unified analysis session
     * @param theme - Current user theme (optional, defaults to 'vscode-light')
     * @returns Promise with loaded template files
     */
    async getRequiredFiles(session, theme) {
        console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: 🎯 Getting template files for LivePanel directory analysis`);
        console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: Target type: ${session.targetType}`);
        console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: Analysis mode: ${session.analysisMode}`);
        console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: Target path: ${session.targetPath}`);
        console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: Theme: ${theme || 'default'}`);
        console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: Directories to analyze: ${session.directoriesToAnalyze?.length || 0}`);
        console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: Files to hash: ${session.filesToHash?.length || 0}`);
        try {
            // STEP 1: Load template files
            console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: 📁 Loading template files...`);
            const loadedFiles = await this.livePanelParser.loadTemplateFiles(session.targetType, session.analysisMode, theme);
            // STEP 2: Execute Python analysis to get data.json
            console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: � Executing Python directory analysis...`);
            try {
                // Execute Python analysis using the unified analysis method
                const analysisResult = await this.executePython.executeAnalysis(session);
                if (analysisResult && analysisResult.summary) {
                    // Add the generated data.json to the loaded files
                    const dataJsonContent = JSON.stringify(analysisResult, null, 2);
                    loadedFiles.set('data.json', dataJsonContent);
                    console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: ✅ Python analysis completed successfully`);
                    console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: 📊 Analysis summary:`, {
                        totalFiles: analysisResult.summary?.totalFiles || 0,
                        analyzedFiles: analysisResult.summary?.totalFilesAnalyzed || 0,
                        notAnalyzedFiles: analysisResult.summary?.totalFilesNotAnalyzed || 0
                    });
                    // 🔥 CRITICAL FIX: Update session.filesToHash with actually analyzed files
                    // This is essential for the DirectoryWatcherOrchestrator to work properly
                    const analyzedFiles = analysisResult.files || [];
                    if (analyzedFiles.length > 0) {
                        console.log(`🔧 LIVEPANEL_DIRECTORY_REQUIREMENTS: Updating session.filesToHash with ${analyzedFiles.length} analyzed files...`);
                        const filesToHash = [];
                        for (const fileData of analyzedFiles) {
                            if (fileData.filePath) {
                                try {
                                    // Generate hash for the analyzed file
                                    const { SHA256Generator } = __webpack_require__(36);
                                    const fileHash = await SHA256Generator.generateFileHash(fileData.filePath);
                                    filesToHash.push({
                                        filePath: fileData.filePath,
                                        hash: fileHash
                                    });
                                }
                                catch (hashError) {
                                    console.error(`LIVEPANEL_DIRECTORY_REQUIREMENTS: Error generating hash for ${fileData.filePath}:`, hashError);
                                    // Add with empty hash as fallback
                                    filesToHash.push({
                                        filePath: fileData.filePath,
                                        hash: ''
                                    });
                                }
                            }
                        }
                        // Update the session with actually analyzed files
                        session.filesToHash = filesToHash;
                        console.log(`✅ LIVEPANEL_DIRECTORY_REQUIREMENTS: Updated session.filesToHash with ${filesToHash.length} files for watchers`);
                    }
                }
                else {
                    console.warn(`LIVEPANEL_DIRECTORY_REQUIREMENTS: ⚠️ Python analysis returned invalid data`);
                    // Add empty data.json as fallback
                    const fallbackData = {
                        summary: {
                            totalFiles: 0,
                            totalFilesAnalyzed: 0,
                            totalFilesNotAnalyzed: 0,
                            totalLines: 0,
                            totalLinesOfCode: 0,
                            totalComments: 0,
                            totalBlankLines: 0,
                            totalFunctions: 0,
                            totalClasses: 0,
                            averageComplexity: 0,
                            analyzedAt: new Date().toISOString(),
                            languages: {}
                        },
                        files: [],
                        metadata: {
                            analysisType: "DirectoryLivePanel",
                            error: "Analysis returned invalid data"
                        }
                    };
                    loadedFiles.set('data.json', JSON.stringify(fallbackData, null, 2));
                }
            }
            catch (pythonError) {
                console.error(`LIVEPANEL_DIRECTORY_REQUIREMENTS: ❌ Error during Python analysis:`, pythonError);
                // Add error data.json as fallback
                const errorData = {
                    summary: {
                        totalFiles: 0,
                        totalFilesAnalyzed: 0,
                        totalFilesNotAnalyzed: 0,
                        totalLines: 0,
                        totalLinesOfCode: 0,
                        totalComments: 0,
                        totalBlankLines: 0,
                        totalFunctions: 0,
                        totalClasses: 0,
                        averageComplexity: 0,
                        analyzedAt: new Date().toISOString(),
                        languages: {}
                    },
                    files: [],
                    metadata: {
                        analysisType: "DirectoryLivePanel",
                        error: `Python execution error: ${pythonError}`
                    }
                };
                loadedFiles.set('data.json', JSON.stringify(errorData, null, 2));
            }
            console.log(`LIVEPANEL_DIRECTORY_REQUIREMENTS: ✅ Successfully loaded ${loadedFiles.size} template files`);
            const requirements = {
                sessionId: session.id,
                analysisMode: session.analysisMode,
                targetPath: session.targetPath,
                loadedFiles: loadedFiles,
                estimatedComplexity: 'low',
                processingTime: new Date()
            };
            return requirements;
        }
        catch (error) {
            console.error(`LIVEPANEL_DIRECTORY_REQUIREMENTS: ❌ Error loading template files:`, error);
            throw error;
        }
    }
}
exports.LivePanelDirectoryRequirements = LivePanelDirectoryRequirements;


/***/ }),
/* 124 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.XRFileRequirements = void 0;
const parsers_1 = __webpack_require__(125);
/**
 * XR File Requirements Processor
 * Handles the specific requirements for XR file analysis
 */
class XRFileRequirements {
    context;
    fileXRParser;
    constructor(context) {
        console.log('XR_FILE_REQUIREMENTS: Initializing XRFileRequirements...');
        this.context = context;
        this.fileXRParser = new parsers_1.FileXRParser(context);
        console.log('XR_FILE_REQUIREMENTS: XRFileRequirements initialized');
    }
    /**
     * Get required files for XR file analysis
     * This method delegates to FileXRParser for processing chart configuration and validation
     */
    async getRequiredFiles(session, theme) {
        console.log(`XR_FILE_REQUIREMENTS: 🎯 Getting XR file requirements for session ${session.id}`);
        console.log(`XR_FILE_REQUIREMENTS: Target file: ${session.targetPath}`);
        console.log(`XR_FILE_REQUIREMENTS: Theme: ${theme || 'default'}`);
        try {
            // Delegate to FileXRParser to handle configuration validation and template processing
            const processedFiles = await this.fileXRParser.parseFileAnalysis(session, theme);
            // Create ProcessedRequirements structure
            const requirements = {
                sessionId: session.id,
                analysisMode: 'XR',
                targetPath: session.targetPath,
                loadedFiles: processedFiles.loadedFiles,
                estimatedComplexity: 'low',
                processingTime: new Date()
            };
            console.log(`XR_FILE_REQUIREMENTS: ✅ XR file requirements processed successfully`);
            console.log(`XR_FILE_REQUIREMENTS: Total files loaded: ${requirements.loadedFiles.size}`);
            return requirements;
        }
        catch (error) {
            console.error(`XR_FILE_REQUIREMENTS: ❌ Error processing XR file requirements:`, error);
            throw error;
        }
    }
}
exports.XRFileRequirements = XRFileRequirements;


/***/ }),
/* 125 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * New Code Analysis Engine - Parsers Module
 *
 * Exports all parsers used to process templates before saving to sessions.
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FileXRParser = exports.VisualizeDOMParser = exports.LivePanelParser = void 0;
var livePanelParser_1 = __webpack_require__(121);
Object.defineProperty(exports, "LivePanelParser", ({ enumerable: true, get: function () { return livePanelParser_1.LivePanelParser; } }));
var visualizeDOMParser_1 = __webpack_require__(112);
Object.defineProperty(exports, "VisualizeDOMParser", ({ enumerable: true, get: function () { return visualizeDOMParser_1.VisualizeDOMParser; } }));
var fileXRParser_1 = __webpack_require__(126);
Object.defineProperty(exports, "FileXRParser", ({ enumerable: true, get: function () { return fileXRParser_1.FileXRParser; } }));


/***/ }),
/* 126 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FileXRParser = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const analysisConfigurationStorage_1 = __webpack_require__(87);
const dimensionValidator_1 = __webpack_require__(127);
const templateCharts_1 = __webpack_require__(50);
const templateProcessor_1 = __webpack_require__(53);
const executePython_1 = __webpack_require__(96);
/**
 * XR File Parser
 * Handles the parsing and validation of XR file analysis configuration
 * Acts as a bridge to the TemplateProcessor from babia_templates
 */
class FileXRParser {
    context;
    configStorage;
    constructor(context) {
        console.log('FILE_XR_PARSER: Initializing FileXRParser...');
        this.context = context;
        this.configStorage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
        console.log('FILE_XR_PARSER: FileXRParser initialized');
    }
    /**
     * Main parsing method for XR file analysis
     * Retrieves chart configuration from globalStorage and validates it
     */
    async parseFileAnalysis(session, theme) {
        console.log(`FILE_XR_PARSER: 🔍 Parsing XR file analysis for session ${session.id}`);
        console.log(`FILE_XR_PARSER: Target file: ${session.targetPath}`);
        try {
            // 1. Get chart type from configuration storage
            const chartType = await this.configStorage.getChartTypeFile();
            console.log(`FILE_XR_PARSER: Chart type from configuration: "${chartType}"`);
            if (!chartType) {
                throw new Error('No chart type configured. Please select a chart type in the Analysis Settings.');
            }
            // 2. Find chart metadata
            const chartMetadata = templateCharts_1.chartTemplates.find(chart => chart.id === chartType);
            if (!chartMetadata) {
                throw new Error(`Chart type "${chartType}" not found in available charts.`);
            }
            console.log(`FILE_XR_PARSER: Found chart metadata for "${chartMetadata.name}"`);
            console.log(`FILE_XR_PARSER: Chart has ${chartMetadata.dimensions.length} dimensions:`, chartMetadata.dimensions.map(d => `${d.name} (${d.dataType}, ${d.required ? 'required' : 'optional'})`));
            // 3. Get dimension mappings from configuration storage
            const dimensionMappings = await this.configStorage.getDimensionMappingFile();
            console.log(`FILE_XR_PARSER: Dimension mappings from configuration:`, dimensionMappings);
            // Convert from configuration format to babia template format
            const babiaFormatMappings = Object.entries(dimensionMappings).map(([dimension, dataField]) => ({
                dimension,
                dataField
            }));
            console.log(`FILE_XR_PARSER: Converted to babia format:`, babiaFormatMappings);
            // 4. Validate dimension mappings using DimensionValidator
            console.log(`FILE_XR_PARSER: 🔍 Validating dimension mappings...`);
            const validationResult = dimensionValidator_1.DimensionValidator.validateMappings(chartMetadata, babiaFormatMappings);
            console.log(`FILE_XR_PARSER: Validation result:`, {
                isValid: validationResult.isValid,
                errorsCount: validationResult.errors.length,
                warningsCount: validationResult.warnings.length
            });
            if (validationResult.errors.length > 0) {
                console.log(`FILE_XR_PARSER: ❌ Validation errors:`, validationResult.errors);
            }
            if (validationResult.warnings.length > 0) {
                console.log(`FILE_XR_PARSER: ⚠️ Validation warnings:`, validationResult.warnings);
            }
            // 5. Display configuration information (TODO message as requested)
            this.displayConfigurationInfo(chartType, chartMetadata, babiaFormatMappings, validationResult);
            // 6. Execute Python analysis to get data.json
            console.log(`FILE_XR_PARSER: 🐍 ===============================`);
            console.log(`FILE_XR_PARSER: 🐍 INITIATING PYTHON ANALYSIS PHASE`);
            console.log(`FILE_XR_PARSER: 🐍 ===============================`);
            console.log(`FILE_XR_PARSER: 🐍 Target file: ${session.targetPath}`);
            console.log(`FILE_XR_PARSER: 🐍 Session ID: ${session.id}`);
            console.log(`FILE_XR_PARSER: 🐍 Expected output: data.json with XR function analysis`);
            const pythonExecutor = new executePython_1.ExecutePython(this.context);
            console.log(`FILE_XR_PARSER: 🐍 ExecutePython instance created`);
            console.log(`FILE_XR_PARSER: 🐍 Calling executeAnalysis...`);
            const analysisData = await pythonExecutor.executeAnalysis(session);
            console.log(`FILE_XR_PARSER: 🐍 Python analysis completed!`);
            console.log(`FILE_XR_PARSER: 🐍 ==============================`);
            console.log(`FILE_XR_PARSER: 🐍 PYTHON ANALYSIS RESULTS`);
            console.log(`FILE_XR_PARSER: 🐍 ==============================`);
            console.log(`FILE_XR_PARSER: 🐍 Result type: ${typeof analysisData}`);
            console.log(`FILE_XR_PARSER: 🐍 Is array: ${Array.isArray(analysisData)}`);
            console.log(`FILE_XR_PARSER: 🐍 Is null/undefined: ${analysisData === null || analysisData === undefined}`);
            if (Array.isArray(analysisData)) {
                console.log(`FILE_XR_PARSER: 🐍 Array length: ${analysisData.length}`);
                if (analysisData.length > 0) {
                    console.log(`FILE_XR_PARSER: 🐍 First element keys: [${Object.keys(analysisData[0]).join(', ')}]`);
                    console.log(`FILE_XR_PARSER: 🐍 Sample data:`, {
                        functionName: analysisData[0].functionName,
                        complexity: analysisData[0].complexity,
                        lineCount: analysisData[0].lineCount,
                        fileName: analysisData[0].fileName
                    });
                }
            }
            else if (analysisData && typeof analysisData === 'object') {
                console.log(`FILE_XR_PARSER: 🐍 Object keys: [${Object.keys(analysisData).join(', ')}]`);
            }
            if (!analysisData || (Array.isArray(analysisData) && analysisData.length === 0)) {
                console.error(`FILE_XR_PARSER: 🐍 ❌ PYTHON ANALYSIS FAILED - NO DATA RETURNED`);
                throw new Error('Python analysis returned no data. Cannot generate XR visualization.');
            }
            console.log(`FILE_XR_PARSER: 🐍 ✅ Python analysis data validation passed`);
            console.log(`FILE_XR_PARSER: � Analysis data summary:`, {
                totalRecords: Array.isArray(analysisData) ? analysisData.length : 1,
                dataStructure: Array.isArray(analysisData) ? 'array' : typeof analysisData,
                sampleKeys: Array.isArray(analysisData) && analysisData.length > 0 ? Object.keys(analysisData[0]) : 'N/A'
            });
            // 7. Save data.json to session output directory
            console.log(`FILE_XR_PARSER: 💾 ===============================`);
            console.log(`FILE_XR_PARSER: 💾 SAVING DATA.JSON PHASE`);
            console.log(`FILE_XR_PARSER: 💾 ===============================`);
            const dataJsonPath = `${session.outputPath}/data.json`;
            console.log(`FILE_XR_PARSER: 💾 Target data.json path: ${dataJsonPath}`);
            console.log(`FILE_XR_PARSER: 💾 Session output directory: ${session.outputPath}`);
            // Ensure output directory exists
            if (!fs.existsSync(session.outputPath)) {
                console.log(`FILE_XR_PARSER: 💾 Creating output directory: ${session.outputPath}`);
                fs.mkdirSync(session.outputPath, { recursive: true });
                console.log(`FILE_XR_PARSER: 💾 ✅ Output directory created successfully`);
            }
            else {
                console.log(`FILE_XR_PARSER: 💾 ✅ Output directory already exists`);
            }
            // Convert analysisData to JSON string with pretty formatting
            console.log(`FILE_XR_PARSER: 💾 Converting analysis data to JSON string...`);
            const jsonString = JSON.stringify(analysisData, null, 2);
            console.log(`FILE_XR_PARSER: 💾 JSON string length: ${jsonString.length} characters`);
            console.log(`FILE_XR_PARSER: 💾 JSON preview (first 200 chars): ${jsonString.substring(0, 200)}${jsonString.length > 200 ? '...' : ''}`);
            // Save the analysis data as data.json
            console.log(`FILE_XR_PARSER: 💾 Writing data.json to disk...`);
            fs.writeFileSync(dataJsonPath, jsonString, 'utf8');
            // Verify the file was written correctly
            if (fs.existsSync(dataJsonPath)) {
                const writtenFileSize = fs.statSync(dataJsonPath).size;
                console.log(`FILE_XR_PARSER: 💾 ✅ data.json saved successfully!`);
                console.log(`FILE_XR_PARSER: 💾 File size: ${writtenFileSize} bytes`);
                console.log(`FILE_XR_PARSER: 💾 File location: ${dataJsonPath}`);
                // Read back first few lines to verify content
                const savedContent = fs.readFileSync(dataJsonPath, 'utf8');
                console.log(`FILE_XR_PARSER: 💾 Verification - saved content preview: ${savedContent.substring(0, 100)}...`);
            }
            else {
                console.error(`FILE_XR_PARSER: 💾 ❌ Failed to save data.json - file does not exist after write`);
                throw new Error('Failed to save data.json file');
            }
            // 7.5. Copy live_sse_fileXR.js as main.js to output directory
            console.log(`FILE_XR_PARSER: 📂 ===============================`);
            console.log(`FILE_XR_PARSER: 📂 COPYING LIVE SSE SCRIPT AS MAIN.JS`);
            console.log(`FILE_XR_PARSER: 📂 ===============================`);
            const liveSSEPath = path.join(this.context.extensionPath, 'templates', 'xr', 'sse', 'live_sse_fileXR.js');
            const mainJSPath = `${session.outputPath}/main.js`;
            console.log(`FILE_XR_PARSER: 📂 Source live SSE script: ${liveSSEPath}`);
            console.log(`FILE_XR_PARSER: 📂 Target main.js path: ${mainJSPath}`);
            try {
                if (fs.existsSync(liveSSEPath)) {
                    console.log(`FILE_XR_PARSER: 📂 ✅ Live SSE script found, copying...`);
                    const liveSSEContent = fs.readFileSync(liveSSEPath, 'utf8');
                    fs.writeFileSync(mainJSPath, liveSSEContent, 'utf8');
                    if (fs.existsSync(mainJSPath)) {
                        const mainJSSize = fs.statSync(mainJSPath).size;
                        console.log(`FILE_XR_PARSER: 📂 ✅ main.js copied successfully!`);
                        console.log(`FILE_XR_PARSER: 📂 File size: ${mainJSSize} bytes`);
                        console.log(`FILE_XR_PARSER: 📂 File location: ${mainJSPath}`);
                    }
                    else {
                        console.error(`FILE_XR_PARSER: 📂 ❌ Failed to copy main.js`);
                    }
                }
                else {
                    console.error(`FILE_XR_PARSER: 📂 ❌ Live SSE script not found at: ${liveSSEPath}`);
                }
            }
            catch (copyError) {
                console.error(`FILE_XR_PARSER: 📂 ❌ Error copying live SSE script:`, copyError);
                // Continue execution even if copy fails
            }
            // 8. Generate XR visualization using TemplateProcessor
            console.log(`FILE_XR_PARSER: 🚀 ===============================`);
            console.log(`FILE_XR_PARSER: 🚀 BABIA_TEMPLATES PHASE - CALLING TEMPLATEPROCESSOR`);
            console.log(`FILE_XR_PARSER: 🚀 ===============================`);
            // Prepare parameters for TemplateProcessor
            const fileName = session.targetName || 'analysis';
            const title = `XR Analysis: ${fileName}`;
            const dataSource = `./data.json`; // Relative path to the data file
            const outputPath = `${session.outputPath}/index.html`; // Main XR visualization file
            console.log(`FILE_XR_PARSER: 🚀 TEMPLATEPROCESSOR PARAMETERS:`);
            console.log(`FILE_XR_PARSER: 🚀 - Chart Type: ${chartType}`);
            console.log(`FILE_XR_PARSER: 🚀 - Chart Name: ${chartMetadata.name}`);
            console.log(`FILE_XR_PARSER: 🚀 - Chart Category: ${chartMetadata.category}`);
            console.log(`FILE_XR_PARSER: 🚀 - Dimension Mappings Count: ${babiaFormatMappings.length}`);
            console.log(`FILE_XR_PARSER: 🚀 - Title: "${title}"`);
            console.log(`FILE_XR_PARSER: 🚀 - Data Source: "${dataSource}"`);
            console.log(`FILE_XR_PARSER: 🚀 - Output HTML Path: "${outputPath}"`);
            console.log(`FILE_XR_PARSER: 🚀 - Template Analysis Data Type: ${typeof analysisData}`);
            console.log(`FILE_XR_PARSER: 🚀 - Template Analysis Data Length: ${Array.isArray(analysisData) ? analysisData.length : 'N/A'}`);
            console.log(`FILE_XR_PARSER: 🚀 DIMENSION MAPPINGS FOR TEMPLATEPROCESSOR:`);
            babiaFormatMappings.forEach((mapping, index) => {
                console.log(`FILE_XR_PARSER: 🚀   ${index + 1}. ${mapping.dimension} → "${mapping.dataField}"`);
            });
            // For file analysis, pass the analysisData to detectDirectoryAnalysis (should return false)
            const templateAnalysisData = analysisData; // Use the Python analysis data for template processing
            console.log(`FILE_XR_PARSER: 🚀 Calling TemplateProcessor.generateXRVisualization()...`);
            console.log(`FILE_XR_PARSER: 🚀 This will generate the index.html file with A-Frame XR content`);
            // Call TemplateProcessor to generate the XR visualization
            const templateResult = await templateProcessor_1.TemplateProcessor.generateXRVisualization(chartType, babiaFormatMappings, title, dataSource, this.context, outputPath, templateAnalysisData // Pass the Python analysis data
            );
            console.log(`FILE_XR_PARSER: 🚀 TemplateProcessor.generateXRVisualization() completed!`);
            console.log(`FILE_XR_PARSER: 🚀 ===============================`);
            console.log(`FILE_XR_PARSER: 🚀 TEMPLATEPROCESSOR RESULTS`);
            console.log(`FILE_XR_PARSER: 🚀 ===============================`);
            console.log(`FILE_XR_PARSER: 🚀 Success: ${templateResult.success}`);
            console.log(`FILE_XR_PARSER: 🚀 Error: ${templateResult.error || 'None'}`);
            if (!templateResult.success) {
                console.error(`FILE_XR_PARSER: 🚀 ❌ TEMPLATE GENERATION FAILED`);
                throw new Error(`Template generation failed: ${templateResult.error}`);
            }
            console.log(`FILE_XR_PARSER: 🚀 ✅ TemplateProcessor completed successfully!`);
            console.log(`FILE_XR_PARSER: 🚀 XR visualization generated at: ${outputPath}`);
            // Verify the generated index.html file
            if (fs.existsSync(outputPath)) {
                const htmlFileSize = fs.statSync(outputPath).size;
                console.log(`FILE_XR_PARSER: 🚀 ✅ index.html verification:`);
                console.log(`FILE_XR_PARSER: 🚀   - File exists: YES`);
                console.log(`FILE_XR_PARSER: 🚀   - File size: ${htmlFileSize} bytes`);
                console.log(`FILE_XR_PARSER: 🚀   - File path: ${outputPath}`);
            }
            else {
                console.error(`FILE_XR_PARSER: 🚀 ❌ index.html file was not generated!`);
            }
            // Load the generated files into the session (for launcher saving)
            console.log(`FILE_XR_PARSER: 📁 ===============================`);
            console.log(`FILE_XR_PARSER: 📁 LOADING GENERATED FILES FOR LAUNCHER`);
            console.log(`FILE_XR_PARSER: 📁 (Parser only processes - Launcher will save)`);
            console.log(`FILE_XR_PARSER: 📁 ===============================`);
            console.log(`FILE_XR_PARSER: 📁 Scanning output directory: ${session.outputPath}`);
            const loadedFiles = await this.loadGeneratedFiles(session.outputPath);
            console.log(`FILE_XR_PARSER: 📁 ===============================`);
            console.log(`FILE_XR_PARSER: 📁 FILE LOADING RESULTS`);
            console.log(`FILE_XR_PARSER: 📁 ===============================`);
            console.log(`FILE_XR_PARSER: 📁 Total files loaded: ${loadedFiles.size}`);
            if (loadedFiles.size > 0) {
                console.log(`FILE_XR_PARSER: 📁 LOADED FILES DETAILS:`);
                Array.from(loadedFiles.entries()).forEach(([fileName, content], index) => {
                    console.log(`FILE_XR_PARSER: 📁   ${index + 1}. ${fileName} (${content.length} chars)`);
                });
                // Check for expected files
                const expectedFiles = ['data.json', 'index.html', 'main.js'];
                expectedFiles.forEach(expectedFile => {
                    if (loadedFiles.has(expectedFile)) {
                        console.log(`FILE_XR_PARSER: 📁 ✅ Expected file found: ${expectedFile}`);
                    }
                    else {
                        console.log(`FILE_XR_PARSER: 📁 ⚠️ Expected file missing: ${expectedFile}`);
                    }
                });
            }
            else {
                console.log(`FILE_XR_PARSER: 📁 ⚠️ No files were loaded from output directory`);
            }
            console.log(`FILE_XR_PARSER: ✅ ===============================`);
            console.log(`FILE_XR_PARSER: ✅ XR FILE ANALYSIS PARSING COMPLETED SUCCESSFULLY`);
            console.log(`FILE_XR_PARSER: ✅ (Returning files to launcher for saving and watching)`);
            console.log(`FILE_XR_PARSER: ✅ ===============================`);
            console.log(`FILE_XR_PARSER: ✅ Session ID: ${session.id}`);
            console.log(`FILE_XR_PARSER: ✅ Chart Type: ${chartType}`);
            console.log(`FILE_XR_PARSER: ✅ Total Mappings: ${babiaFormatMappings.length}`);
            console.log(`FILE_XR_PARSER: ✅ Validation Status: ${validationResult.isValid ? 'VALID' : 'INVALID'}`);
            console.log(`FILE_XR_PARSER: ✅ Files Generated: ${loadedFiles.size}`);
            console.log(`FILE_XR_PARSER: ✅ Python Data Records: ${Array.isArray(analysisData) ? analysisData.length : 'N/A'}`);
            console.log(`FILE_XR_PARSER: ✅ Next: Launcher will save files and start watcher`);
            console.log(`FILE_XR_PARSER: ✅ ===============================`);
            return {
                sessionId: session.id,
                chartType,
                chartMetadata,
                dimensionMappings: babiaFormatMappings,
                validationResult,
                loadedFiles
            };
        }
        catch (error) {
            console.error(`FILE_XR_PARSER: ❌ Error parsing XR file analysis:`, error);
            // Show user-friendly error message
            vscode.window.showErrorMessage(`XR Analysis Configuration Error: ${error instanceof Error ? error.message : String(error)}`);
            throw error;
        }
    }
    /**
     * Display configuration information to the user
     * Shows the selected chart type and dimension mappings with validation status
     */
    displayConfigurationInfo(chartType, chartMetadata, dimensionMappings, validationResult) {
        console.log(`\n🎯 === XR FILE ANALYSIS CONFIGURATION ===`);
        console.log(`📊 Chart Type: ${chartType} (${chartMetadata.name})`);
        console.log(`📝 Chart Description: ${chartMetadata.description}`);
        console.log(`🏷️ Chart Category: ${chartMetadata.category}`);
        console.log(`📐 Total Dimensions: ${chartMetadata.dimensions.length}`);
        console.log(`\n📋 === DIMENSION MAPPINGS ===`);
        if (dimensionMappings.length === 0) {
            console.log(`⚠️ No dimension mappings configured!`);
        }
        else {
            dimensionMappings.forEach((mapping, index) => {
                const dimension = chartMetadata.dimensions.find(d => d.name === mapping.dimension);
                const requiredText = dimension?.required ? '(REQUIRED)' : '(optional)';
                const dataTypeText = dimension?.dataType ? `[${dimension.dataType}]` : '';
                console.log(`  ${index + 1}. ${mapping.dimension} ${requiredText} ${dataTypeText} → "${mapping.dataField}"`);
            });
        }
        console.log(`\n✅ === VALIDATION STATUS ===`);
        console.log(`📊 Validation: ${validationResult.isValid ? '✅ VALID' : '❌ INVALID'}`);
        if (validationResult.errors.length > 0) {
            console.log(`❌ Errors (${validationResult.errors.length}):`);
            validationResult.errors.forEach((error, index) => {
                console.log(`  ${index + 1}. ${error}`);
            });
        }
        if (validationResult.warnings.length > 0) {
            console.log(`⚠️ Warnings (${validationResult.warnings.length}):`);
            validationResult.warnings.forEach((warning, index) => {
                console.log(`  ${index + 1}. ${warning}`);
            });
        }
        console.log(`========================================\n`);
        // Also show summary in VS Code notification
        const mappingsCount = dimensionMappings.length;
        const dimensionsCount = chartMetadata.dimensions.length;
        const requiredDimensionsCount = chartMetadata.dimensions.filter(d => d.required).length;
        const statusIcon = validationResult.isValid ? '✅' : '❌';
        const summaryMessage = `${statusIcon} XR Analysis Config: ${chartType} chart with ${mappingsCount}/${dimensionsCount} dimensions mapped (${requiredDimensionsCount} required)`;
        if (validationResult.isValid) {
            vscode.window.showInformationMessage(summaryMessage);
        }
        else {
            vscode.window.showWarningMessage(summaryMessage + ` - ${validationResult.errors.length} errors found`);
        }
    }
    /**
     * Load generated files from the output directory
     * Scans the output directory and loads all relevant files for the session
     */
    async loadGeneratedFiles(outputPath) {
        console.log(`FILE_XR_PARSER: 📁 Loading generated files from: ${outputPath}`);
        const loadedFiles = new Map();
        try {
            // Ensure output directory exists
            if (!fs.existsSync(outputPath)) {
                console.log(`FILE_XR_PARSER: Output directory does not exist: ${outputPath}`);
                return loadedFiles;
            }
            // Read all files in the output directory
            const files = fs.readdirSync(outputPath, { withFileTypes: true });
            for (const file of files) {
                if (file.isFile()) {
                    const filePath = path.join(outputPath, file.name);
                    try {
                        const content = fs.readFileSync(filePath, 'utf8');
                        loadedFiles.set(file.name, content);
                        console.log(`FILE_XR_PARSER: ✅ Loaded file: ${file.name} (${content.length} chars)`);
                    }
                    catch (error) {
                        console.error(`FILE_XR_PARSER: ❌ Error loading file ${file.name}:`, error);
                    }
                }
            }
            console.log(`FILE_XR_PARSER: 📁 Total files loaded: ${loadedFiles.size}`);
            console.log(`FILE_XR_PARSER: 📋 File list:`, Array.from(loadedFiles.keys()));
        }
        catch (error) {
            console.error(`FILE_XR_PARSER: ❌ Error loading generated files:`, error);
        }
        return loadedFiles;
    }
}
exports.FileXRParser = FileXRParser;


/***/ }),
/* 127 */
/***/ ((__unused_webpack_module, exports) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DimensionValidator = void 0;
/**
 * BabiaXR Dimension Validator
 * Validates dimension mappings against chart requirements
 */
class DimensionValidator {
    /**
     * Validate dimension mappings for a given chart
     */
    static validateMappings(chart, mappings) {
        const result = {
            isValid: true,
            errors: [],
            warnings: []
        };
        // Check for required dimensions
        const requiredDimensions = chart.dimensions.filter(d => d.required);
        const mappedDimensions = new Set(mappings.map(m => m.dimension));
        for (const requiredDim of requiredDimensions) {
            if (!mappedDimensions.has(requiredDim.name)) {
                result.errors.push(`Required dimension '${requiredDim.name}' (${requiredDim.label}) is not mapped`);
                result.isValid = false;
            }
        }
        // Check for invalid dimension names
        const validDimensionNames = new Set(chart.dimensions.map(d => d.name));
        for (const mapping of mappings) {
            if (!validDimensionNames.has(mapping.dimension)) {
                result.errors.push(`Unknown dimension '${mapping.dimension}' for chart type '${chart.name}'`);
                result.isValid = false;
            }
        }
        // Check for duplicate mappings
        const dimensionCounts = new Map();
        for (const mapping of mappings) {
            const count = dimensionCounts.get(mapping.dimension) || 0;
            dimensionCounts.set(mapping.dimension, count + 1);
        }
        for (const [dimension, count] of dimensionCounts) {
            if (count > 1) {
                result.errors.push(`Dimension '${dimension}' is mapped multiple times`);
                result.isValid = false;
            }
        }
        // Check for empty data fields
        for (const mapping of mappings) {
            if (!mapping.dataField || mapping.dataField.trim() === '') {
                result.errors.push(`Dimension '${mapping.dimension}' has no data field specified`);
                result.isValid = false;
            }
        }
        // Add warnings for optional dimensions that are not mapped
        const optionalDimensions = chart.dimensions.filter(d => !d.required);
        for (const optionalDim of optionalDimensions) {
            if (!mappedDimensions.has(optionalDim.name)) {
                result.warnings.push(`Optional dimension '${optionalDim.name}' (${optionalDim.label}) is not mapped`);
            }
        }
        return result;
    }
    /**
     * Validate a specific data field against dimension requirements
     */
    static validateDataField(dimensionName, dataField, chart) {
        const dimension = chart.dimensions.find(d => d.name === dimensionName);
        if (!dimension) {
            return {
                isValid: false,
                error: `Dimension '${dimensionName}' does not exist for chart type '${chart.name}'`
            };
        }
        if (!dataField || dataField.trim() === '') {
            return {
                isValid: false,
                error: `Data field for dimension '${dimensionName}' cannot be empty`
            };
        }
        // Additional validation can be added here for data type checking
        // when we have access to actual data structure
        return { isValid: true };
    }
    /**
     * Get missing required dimensions
     */
    static getMissingRequiredDimensions(chart, mappings) {
        const requiredDimensions = chart.dimensions.filter(d => d.required);
        const mappedDimensions = new Set(mappings.map(m => m.dimension));
        return requiredDimensions
            .filter(d => !mappedDimensions.has(d.name))
            .map(d => d.name);
    }
    /**
     * Check if all required dimensions are mapped
     */
    static areAllRequiredDimensionsMapped(chart, mappings) {
        return this.getMissingRequiredDimensions(chart, mappings).length === 0;
    }
}
exports.DimensionValidator = DimensionValidator;


/***/ }),
/* 128 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * XR Directory Requirements Processor
 * Handles the processing of directory analysis requirements for XR visualization
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.XRDirectoryRequirements = void 0;
const directoryXRParser_1 = __webpack_require__(129);
class XRDirectoryRequirements {
    context;
    constructor(context) {
        this.context = context;
        console.log('XR_DIRECTORY_REQUIREMENTS: Initializing XR Directory Requirements processor...');
    }
    /**
     * Process directory XR analysis requirements
     * This will call directoryXRParser to generate the necessary files
     */
    async processDirectoryXRRequirements(session) {
        console.log(`📁 XR_DIRECTORY_REQUIREMENTS: Processing XR requirements for directory: ${session.targetName}`);
        console.log(`🔗 XR_DIRECTORY_REQUIREMENTS: Chain: XRDirectoryRequirements → directoryXRParser → templateHTMLProcessor`);
        try {
            // Call directoryXRParser to generate XR visualization files
            console.log(`📊 XR_DIRECTORY_REQUIREMENTS: Calling directoryXRParser for session ${session.id}...`);
            const parsingResult = await directoryXRParser_1.directoryXRParser.parseDirectoryForXR(session, this.context);
            if (!parsingResult.success) {
                console.error(`❌ XR_DIRECTORY_REQUIREMENTS: Directory XR parsing failed:`, parsingResult.error);
                return {
                    success: false,
                    loadedFiles: new Map(),
                    error: parsingResult.error
                };
            }
            console.log(`✅ XR_DIRECTORY_REQUIREMENTS: Directory XR parsing completed successfully`);
            console.log(`📄 XR_DIRECTORY_REQUIREMENTS: Generated ${parsingResult.generatedFiles.size} files:`);
            // Log each generated file
            for (const [fileName, content] of parsingResult.generatedFiles) {
                console.log(`   📄 ${fileName} (${content.length} characters)`);
            }
            return {
                success: true,
                loadedFiles: parsingResult.generatedFiles,
                error: undefined
            };
        }
        catch (error) {
            console.error(`❌ XR_DIRECTORY_REQUIREMENTS: Error processing directory XR requirements:`, error);
            return {
                success: false,
                loadedFiles: new Map(),
                error: error instanceof Error ? error.message : String(error)
            };
        }
    }
}
exports.XRDirectoryRequirements = XRDirectoryRequirements;


/***/ }),
/* 129 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Directory XR Parser
 * Handles parsing of directory structure for XR visualization
 * Generates HTML, JS and data files for XR analysis
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.directoryXRParser = exports.DirectoryXRParser = void 0;
const path = __importStar(__webpack_require__(5));
const fs = __importStar(__webpack_require__(6));
const analysisConfigurationStorage_1 = __webpack_require__(87);
const templateProcessor_1 = __webpack_require__(53);
const executePython_1 = __webpack_require__(96);
const sha256Generator_1 = __webpack_require__(36);
class DirectoryXRParser {
    /**
     * Parse directory for XR visualization
     * Generates the necessary files: HTML, JS, and data.json
     */
    async parseDirectoryForXR(session, context) {
        console.log(`🏗️ DIRECTORY_XR_PARSER: Starting XR parsing for directory: ${session.targetName}`);
        console.log(`📂 DIRECTORY_XR_PARSER: Target path: ${session.targetPath}`);
        console.log(`🔍 DIRECTORY_XR_PARSER: Deep analysis: ${session.isDeep}`);
        try {
            // =======================================================
            // STEP 1: GET CONFIGURATION (CHART TYPE & DIMENSIONS)
            // =======================================================
            console.log(`⚙️ DIRECTORY_XR_PARSER: STEP 1 - Getting user configuration...`);
            const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
            const chartType = await storage.getDirectoryChartType();
            const dimensionMappings = await storage.getDimensionMappingDirectory();
            console.log(`📊 DIRECTORY_XR_PARSER: Chart type: ${chartType}`);
            console.log(`🎯 DIRECTORY_XR_PARSER: Dimension mappings:`, dimensionMappings);
            // =======================================================
            // STEP 2: GENERATE REAL DATA.JSON USING PYTHON ANALYSIS
            // =======================================================
            console.log(`📊 DIRECTORY_XR_PARSER: STEP 2 - Generating real data.json using Python analysis...`);
            // Create ExecutePython instance
            const executePython = new executePython_1.ExecutePython(context);
            // Create a specialized session for XR directory analysis
            const xrAnalysisSession = {
                id: session.id,
                targetPath: session.targetPath,
                targetName: session.targetName,
                targetType: 'directory',
                analysisMode: 'XR',
                isDeep: session.isDeep,
                status: 'analyzing',
                startTime: new Date(),
                hash256: session.hash256,
                outputDirectory: session.outputDirectory,
                outputPath: session.outputPath,
                requiredFiles: new Map(),
                templatePaths: new Map(),
                metadata: {},
                // ✅ COPY FILTERED FILES FROM ORIGINAL SESSION - CRITICAL FOR DIRECTORY FILTERING
                filesToHash: session.filesToHash,
                directoriesToAnalyze: session.directoriesToAnalyze
            };
            console.log(`🚀 DIRECTORY_XR_PARSER: Executing Python analysis for directory...`);
            console.log(`📂 DIRECTORY_XR_PARSER: Target path: ${xrAnalysisSession.targetPath}`);
            console.log(`🔍 DIRECTORY_XR_PARSER: Analysis settings:`, {
                analysisMode: xrAnalysisSession.analysisMode,
                isDeep: xrAnalysisSession.isDeep
            });
            // Execute Python analysis with progress reporting
            const analysisResult = await executePython.executeAnalysis(xrAnalysisSession);
            let dataJsonContent;
            let analysisData = [];
            if (!analysisResult || (Array.isArray(analysisResult) && analysisResult.length === 0)) {
                console.warn(`⚠️ DIRECTORY_XR_PARSER: Python analysis returned empty result`);
                // Create empty array for no files
                dataJsonContent = JSON.stringify([], null, 2);
                analysisData = [];
                console.log(`📄 DIRECTORY_XR_PARSER: Using empty array - no files analyzed`);
            }
            else {
                console.log(`✅ DIRECTORY_XR_PARSER: Python analysis completed successfully!`);
                console.log(`📊 DIRECTORY_XR_PARSER: Analysis result:`, {
                    isArray: Array.isArray(analysisResult),
                    fileCount: Array.isArray(analysisResult) ? analysisResult.length : 0,
                    resultType: typeof analysisResult
                });
                // Use analysis result directly as the data.json content (array of files)
                analysisData = Array.isArray(analysisResult) ? analysisResult : [];
                dataJsonContent = JSON.stringify(analysisData, null, 2);
                console.log(`✅ DIRECTORY_XR_PARSER: STEP 2 completed - data.json generated (${dataJsonContent.length} chars)`);
                console.log(`📄 DIRECTORY_XR_PARSER: Generated data for ${analysisData.length} files`);
                // Log sample file data for verification
                if (analysisData.length > 0) {
                    console.log(`📋 DIRECTORY_XR_PARSER: Sample file data:`, {
                        fileName: analysisData[0].fileName,
                        language: analysisData[0].language,
                        totalLines: analysisData[0].totalLines,
                        functionCount: analysisData[0].functionCount,
                        fileSizeBytes: analysisData[0].fileSizeBytes
                    });
                }
                // 🔥 CRITICAL FIX: Update session.filesToHash with actually analyzed files
                // This is essential for the DirectoryWatcherOrchestrator to work properly
                console.log(`🔧 DIRECTORY_XR_PARSER: Updating session.filesToHash with ${analysisData.length} analyzed files...`);
                const filesToHash = [];
                for (const fileData of analysisData) {
                    if (fileData.filePath) {
                        try {
                            // Generate hash for the analyzed file
                            const fileHash = await sha256Generator_1.SHA256Generator.generateFileHash(fileData.filePath);
                            filesToHash.push({
                                filePath: fileData.filePath,
                                hash: fileHash
                            });
                        }
                        catch (hashError) {
                            console.error(`DIRECTORY_XR_PARSER: Error generating hash for ${fileData.filePath}:`, hashError);
                            // Add with empty hash as fallback
                            filesToHash.push({
                                filePath: fileData.filePath,
                                hash: ''
                            });
                        }
                    }
                }
                // Update the original session with actually analyzed files
                session.filesToHash = filesToHash;
                console.log(`✅ DIRECTORY_XR_PARSER: Updated session.filesToHash with ${filesToHash.length} files for watchers`);
            }
            // =======================================================
            // STEP 3: GENERATE HTML USING TEMPLATEPROCESSOR WITH ANALYSIS DATA
            // =======================================================
            console.log(`🌐 DIRECTORY_XR_PARSER: STEP 3 - Generating HTML with TemplateProcessor and analysis data...`);
            // Convert dimension mappings to the required format
            const mappings = Object.entries(dimensionMappings).map(([dimension, dataField]) => ({
                dimension,
                dataField
            }));
            console.log(`🔄 DIRECTORY_XR_PARSER: Converted mappings:`, mappings);
            // Create a temporary output path for HTML generation
            const tempOutputPath = path.join(context.storageUri?.fsPath || '/tmp', 'temp_xr_generation');
            await fs.promises.mkdir(tempOutputPath, { recursive: true });
            const tempHtmlPath = path.join(tempOutputPath, 'index.html');
            const htmlGenerationResult = await templateProcessor_1.TemplateProcessor.generateXRVisualization(chartType, mappings, `Directory Analysis: ${session.targetName}`, 'data.json', // Data source file name
            context, tempHtmlPath, analysisData // Pass the Python analysis data for directory detection
            );
            if (!htmlGenerationResult.success) {
                console.error(`❌ DIRECTORY_XR_PARSER: HTML generation failed:`, htmlGenerationResult.error);
                return {
                    success: false,
                    error: `HTML generation failed: ${htmlGenerationResult.error}`
                };
            }
            const htmlContent = await fs.promises.readFile(tempHtmlPath, 'utf8');
            console.log(`✅ DIRECTORY_XR_PARSER: STEP 3 completed - HTML generated (${htmlContent.length} chars)`);
            // Clean up temp file
            try {
                await fs.promises.unlink(tempHtmlPath);
                await fs.promises.rmdir(tempOutputPath);
            }
            catch (cleanupError) {
                console.warn(`⚠️ DIRECTORY_XR_PARSER: Cleanup warning:`, cleanupError);
            }
            // =======================================================
            // STEP 4: GET LIVE_SSE_XR.JS FILE (saved as main.js)
            // =======================================================
            console.log(`📜 DIRECTORY_XR_PARSER: STEP 4 - Getting live_sse_XR.js file (will be saved as main.js)...`);
            const jsFilePath = path.join(context.extensionPath, 'templates', 'xr', 'sse', 'live_sse_fileXR.js');
            console.log(`📂 DIRECTORY_XR_PARSER: Looking for JS file at: ${jsFilePath}`);
            if (!fs.existsSync(jsFilePath)) {
                console.error(`❌ DIRECTORY_XR_PARSER: live_sse_XR.js not found at: ${jsFilePath}`);
                return {
                    success: false,
                    error: `live_sse_XR.js not found at: ${jsFilePath}`
                };
            }
            const jsContent = await fs.promises.readFile(jsFilePath, 'utf8');
            console.log(`✅ DIRECTORY_XR_PARSER: STEP 4 completed - JS file loaded (${jsContent.length} chars)`);
            // =======================================================
            // STEP 5: PREPARE FINAL FILES MAP
            // =======================================================
            console.log(`📦 DIRECTORY_XR_PARSER: STEP 5 - Preparing final files map...`);
            const generatedFiles = new Map();
            generatedFiles.set('index.html', htmlContent);
            generatedFiles.set('main.js', jsContent); // FIXED: Save as main.js instead of live_sse_XR.js
            generatedFiles.set('data.json', dataJsonContent);
            console.log(`🎉 DIRECTORY_XR_PARSER: All steps completed successfully!`);
            console.log(`📄 DIRECTORY_XR_PARSER: Generated files summary:`);
            console.log(`   📄 index.html: ${htmlContent.length} characters`);
            console.log(`   📄 main.js: ${jsContent.length} characters`);
            console.log(`   📄 data.json: ${dataJsonContent.length} characters`);
            return {
                success: true,
                generatedFiles: generatedFiles
            };
        }
        catch (error) {
            console.error(`❌ DIRECTORY_XR_PARSER: Error during directory XR parsing:`, error);
            return {
                success: false,
                error: error instanceof Error ? error.message : String(error)
            };
        }
    }
}
exports.DirectoryXRParser = DirectoryXRParser;
// Export singleton instance
exports.directoryXRParser = new DirectoryXRParser();


/***/ }),
/* 130 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Launch Pipeline — shared execution logic for all analysis launchers.
 *
 * Encapsulates the common 4-step flow:
 *   1. Process/get required files
 *   2. Save files to storage
 *   3. Start watcher for live updates
 *   4. Start server with SSE
 *
 * Each launcher configures the pipeline via {@link LaunchPipelineConfig} without
 * duplicating the orchestration boilerplate.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.executeLaunchPipeline = executeLaunchPipeline;
const vscode = __importStar(__webpack_require__(1));
const sessionRegistry_1 = __webpack_require__(32);
const saveFiles_1 = __webpack_require__(114);
const sessionServerManager_1 = __webpack_require__(107);
/**
 * Execute the shared 4-step launch pipeline.
 *
 * Maintains **identical** runtime behaviour to the original per-launcher code
 * while eliminating ~400 lines of duplication.
 */
async function executeLaunchPipeline(session, context, config) {
    const log = (msg) => console.log(`${config.logPrefix}: ${msg}`);
    const logError = (msg) => console.error(`${config.logPrefix}: ${msg}`);
    log(`Starting analysis with session ${session.id}`);
    const registry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
    try {
        // ── Optional pre-validation ──────────────────────────────
        if (config.preValidation) {
            await config.preValidation();
        }
        // ── STEP 1: Process requirements ─────────────────────────
        log(`STEP 1 - Getting template files and data.json...`);
        registry.updateSessionStatus(session.id, 'analyzing', config.progress.process);
        const processedFiles = await config.processRequirements();
        log(`Received ${processedFiles.loadedFiles.size} processed template files`);
        for (const [fileName, content] of processedFiles.loadedFiles) {
            log(`  ${fileName} (${content.length} chars)`);
        }
        // ── STEP 2: Save files ───────────────────────────────────
        log(`STEP 2 - Saving processed files to storage...`);
        registry.updateSessionStatus(session.id, 'analyzing', config.progress.save);
        const saveFiles = new saveFiles_1.SaveFiles();
        const savedPath = await saveFiles.saveFilesToStorage(processedFiles.loadedFiles, config.folderName, session.outputDirectory, context);
        log(`Files successfully saved to: ${savedPath}`);
        // Update session with save information
        session.savedFilesPath = savedPath;
        for (const [fileName, content] of processedFiles.loadedFiles) {
            session.requiredFiles.set(fileName, content);
        }
        // ── STEPS 3 & 4: Watcher + Server (order configurable) ──
        if (config.startServerBeforeWatcher) {
            await startServer(session, context, registry, config, log, logError, savedPath);
            await startWatcher(session, registry, config, log, savedPath);
        }
        else {
            await startWatcher(session, registry, config, log, savedPath);
            await startServer(session, context, registry, config, log, logError, savedPath);
        }
        // ── Finalize ─────────────────────────────────────────────
        if (config.setEndTime) {
            session.endTime = new Date();
        }
        log(`Analysis completed successfully!`);
    }
    catch (error) {
        logError(`Error during analysis: ${error}`);
        registry.updateSessionStatus(session.id, 'error', config.rethrowErrors ? 0 : undefined, error instanceof Error ? error.message : String(error));
        if (config.setEndTime) {
            session.status = 'error';
            session.endTime = new Date();
        }
        vscode.window.showErrorMessage(`Failed to start analysis: ${error instanceof Error ? error.message : String(error)}`);
        if (config.rethrowErrors) {
            throw error;
        }
    }
}
// ── Internal helpers ─────────────────────────────────────────────
async function startWatcher(session, registry, config, log, savedPath) {
    const stepProgress = config.startServerBeforeWatcher
        ? config.progress.serverOrWatcher
        : config.progress.watcherOrServer;
    log(`Starting watcher...`);
    registry.updateSessionStatus(session.id, 'analyzing', stepProgress);
    const watcherId = await config.startWatcher(savedPath);
    if (watcherId) {
        log(`Watcher started successfully with ID: ${watcherId}`);
        session.watcherId = watcherId;
    }
    else {
        log(`Watcher could not be started`);
    }
}
async function startServer(session, context, registry, config, log, logError, _savedPath) {
    const stepProgress = config.startServerBeforeWatcher
        ? config.progress.watcherOrServer
        : config.progress.serverOrWatcher;
    log(`Starting server with SSE...`);
    registry.updateSessionStatus(session.id, 'analyzing', stepProgress);
    const sessionServerManager = new sessionServerManager_1.SessionServerManager(context);
    const serverStatus = await sessionServerManager.startServerForSession(session);
    if (serverStatus.isServerActive) {
        log(`Server started successfully on port ${serverStatus.port}`);
        log(`Server URL: ${serverStatus.serverUrl}`);
        if (serverStatus.port) {
            const portRegistered = registry.registerSessionPort(session.id, serverStatus.port);
            log(`Port ${serverStatus.port} registration result: ${portRegistered}`);
        }
        session.assignedPort = serverStatus.port;
        session.serverUrl = serverStatus.serverUrl;
        registry.updateSessionStatus(session.id, 'monitoring', config.progress.done);
        // Optional success callback
        config.onSuccess?.(serverStatus.port, serverStatus.serverUrl);
    }
    else {
        logError(`Server could not be started for session ${session.id}`);
        if (config.startServerBeforeWatcher) {
            // VisualizeDOM pattern: throw to abort remaining steps
            throw new Error(`Failed to start server for analysis`);
        }
        else {
            registry.updateSessionStatus(session.id, 'error', undefined, 'Server could not be started');
        }
    }
}


/***/ }),
/* 131 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * XR Analysis Launcher
 * Handles the launch and orchestration of XR-based analysis for both files and directories.
 *
 * Delegates the common pipeline to {@link executeLaunchPipeline},
 * providing XR-specific configuration (chart validation, dimension mappings,
 * FileWatcherOrchestrator / DirectoryWatcherOrchestrator).
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LauncherXRAnalysis = void 0;
const vscode = __importStar(__webpack_require__(1));
const sessionRegistry_1 = __webpack_require__(32);
const FileRequirementProcessor_1 = __webpack_require__(119);
const fileWatcherOrchestrator_1 = __webpack_require__(94);
const directoryWatcherOrchestrator_1 = __webpack_require__(103);
const analysisConfigurationStorage_1 = __webpack_require__(87);
const dimensionValidator_1 = __webpack_require__(127);
const chartRegistry_1 = __webpack_require__(49);
const launchPipeline_1 = __webpack_require__(130);
const LOG_PREFIX = 'NEW_LAUNCHER_XR_ANALYSIS';
class LauncherXRAnalysis {
    /**
     * Common validation function for XR analysis configuration.
     * Validates chart type and dimension mappings before starting analysis.
     */
    static async validateXRConfiguration(chartType, dimensionMappings, analysisType, registry, sessionId) {
        console.log(`${LOG_PREFIX}: Validating ${analysisType} XR configuration...`);
        const label = analysisType.charAt(0).toUpperCase() + analysisType.slice(1);
        if (!chartType) {
            const msg = `${label} XR Analysis Error: No chart type selected. Please configure chart type in Analysis Settings.`;
            registry.updateSessionStatus(sessionId, 'error', undefined, msg);
            return { isValid: false, error: msg };
        }
        if (!dimensionMappings || Object.keys(dimensionMappings).length === 0) {
            const msg = `${label} XR Analysis Error: No dimension mappings configured. Please configure dimension mappings in Analysis Settings.`;
            registry.updateSessionStatus(sessionId, 'error', undefined, msg);
            return { isValid: false, error: msg };
        }
        const chartMetadata = chartRegistry_1.BabiaChartRegistry.getInstance().getChart(chartType);
        if (!chartMetadata) {
            const msg = `${label} XR Analysis Error: Unknown chart type '${chartType}'. Please select a valid chart type.`;
            registry.updateSessionStatus(sessionId, 'error', undefined, msg);
            return { isValid: false, error: msg };
        }
        const mappingsArray = Object.entries(dimensionMappings).map(([dimension, dataField]) => ({ dimension, dataField: dataField }));
        const validationResult = dimensionValidator_1.DimensionValidator.validateMappings(chartMetadata, mappingsArray);
        if (!validationResult.isValid) {
            const msg = `${label} XR Analysis Error: Invalid dimension mappings:\n${validationResult.errors.join('\n')}`;
            console.error('Validation errors:', validationResult.errors);
            if (validationResult.warnings.length > 0) {
                console.warn('Validation warnings:', validationResult.warnings);
            }
            registry.updateSessionStatus(sessionId, 'error', undefined, msg);
            return { isValid: false, error: msg };
        }
        if (validationResult.warnings.length > 0) {
            console.warn(`${label} XR Analysis Warnings:`, validationResult.warnings);
        }
        console.log(`${LOG_PREFIX}: ${label} XR configuration validated successfully`);
        console.log(`Chart Type: ${chartType}`);
        for (const [dimension, dataField] of Object.entries(dimensionMappings)) {
            console.log(`      ${dimension} → ${dataField}`);
        }
        return { isValid: true };
    }
    /**
     * Common validation and configuration retrieval for XR analysis.
     * Returns validated configuration or throws an error.
     */
    static async validateAndGetXRConfiguration(analysisType, registry, sessionId, context) {
        console.log(`${LOG_PREFIX}: STEP 0 - Validating ${analysisType} XR configuration...`);
        registry.updateSessionStatus(sessionId, 'analyzing', 10);
        const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
        const chartType = analysisType === 'file'
            ? await storage.getChartTypeFile()
            : await storage.getDirectoryChartType();
        const dimensionMappings = analysisType === 'file'
            ? await storage.getDimensionMappingFile()
            : await storage.getDimensionMappingDirectory();
        const result = await this.validateXRConfiguration(chartType, dimensionMappings, analysisType, registry, sessionId);
        if (!result.isValid) {
            vscode.window.showErrorMessage(result.error);
            throw new Error(result.error);
        }
        const label = analysisType.charAt(0).toUpperCase() + analysisType.slice(1);
        console.log(`${LOG_PREFIX}: STEP 0 completed - ${label} XR configuration validated`);
        return { chartType, dimensionMappings };
    }
    /**
     * Launch XR analysis for a file using session
     */
    static async launchFileXRAnalysis(session, context) {
        const registry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
        await (0, launchPipeline_1.executeLaunchPipeline)(session, context, {
            logPrefix: LOG_PREFIX,
            folderName: 'fileAnalysis',
            progress: { process: 30, save: 50, watcherOrServer: 70, serverOrWatcher: 90, done: 100 },
            preValidation: async () => {
                await LauncherXRAnalysis.validateAndGetXRConfiguration('file', registry, session.id, context);
            },
            processRequirements: async () => {
                const processor = new FileRequirementProcessor_1.FileRequirementProcessor(context);
                return processor.processRequirements(session);
            },
            startWatcher: async () => {
                const watcher = new fileWatcherOrchestrator_1.FileWatcherOrchestrator(session, context);
                await watcher.startWatching();
                return session.watcherId;
            },
            onSuccess: (port) => {
                vscode.window.showInformationMessage(`XR Analysis completed for file: ${session.targetName} - Server running on port ${port}`);
            },
        });
    }
    /**
     * Launch XR analysis for a directory using session
     */
    static async launchDirectoryXRAnalysis(session, context) {
        const registry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
        await (0, launchPipeline_1.executeLaunchPipeline)(session, context, {
            logPrefix: LOG_PREFIX,
            folderName: 'directoryAnalysis',
            progress: { process: 30, save: 50, watcherOrServer: 80, serverOrWatcher: 90, done: 100 },
            preValidation: async () => {
                console.log(`${LOG_PREFIX}: Target: ${session.targetName}${session.isDeep ? ' (Deep mode)' : ' (Normal mode)'}`);
                await LauncherXRAnalysis.validateAndGetXRConfiguration('directory', registry, session.id, context);
            },
            processRequirements: async () => {
                const processor = new FileRequirementProcessor_1.FileRequirementProcessor(context);
                return processor.processRequirements(session);
            },
            startWatcher: async () => {
                const watcher = new directoryWatcherOrchestrator_1.DirectoryWatcherOrchestrator(session, context);
                return watcher.startWatching();
            },
            onSuccess: (port) => {
                vscode.window.showInformationMessage(`XR Directory Analysis completed for: ${session.targetName}${session.isDeep ? ' (Deep)' : ''} - Server running on port ${port}`);
            },
        });
    }
}
exports.LauncherXRAnalysis = LauncherXRAnalysis;


/***/ }),
/* 132 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * VisualizeDOM Launcher
 * Launches DOM visualization analysis for HTML files using the new engine.
 *
 * Delegates the common pipeline to {@link executeLaunchPipeline},
 * providing VisualizeDOM-specific configuration (server-before-watcher order,
 * VisualizeDOMRequirements processor, VisualizeDOMWatcher).
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LauncherVisualizeDOM = void 0;
const VisualizeDOMRequirements_1 = __webpack_require__(111);
const visualizeDOMWatcher_1 = __webpack_require__(110);
const launchPipeline_1 = __webpack_require__(130);
const LOG_PREFIX = 'NEW_LAUNCHER_VISUALIZEDOM_ANALYSIS';
class LauncherVisualizeDOM {
    /**
     * Launch VisualizeDOM analysis for HTML files
     */
    static async launchVisualizeDOMAnalysis(session, context) {
        console.log(`${LOG_PREFIX}: Target: ${session.targetName} (${session.targetType})`);
        console.log(`${LOG_PREFIX}: Analysis mode: ${session.analysisMode}`);
        await (0, launchPipeline_1.executeLaunchPipeline)(session, context, {
            logPrefix: LOG_PREFIX,
            folderName: 'visualizeDOMAnalysis',
            progress: { process: 20, save: 40, watcherOrServer: 60, serverOrWatcher: 80, done: 100 },
            // VisualizeDOM starts server before watcher
            startServerBeforeWatcher: true,
            setEndTime: true,
            rethrowErrors: true,
            processRequirements: async () => {
                const requirements = new VisualizeDOMRequirements_1.VisualizeDOMRequirements(context);
                return requirements.getRequiredFiles(session);
            },
            startWatcher: async (savedPath) => {
                try {
                    const watcherId = await visualizeDOMWatcher_1.VisualizeDOMWatcher.startWatching(session.id, session.targetPath, savedPath, context);
                    return watcherId;
                }
                catch (watcherError) {
                    console.error(`${LOG_PREFIX}: Error starting HTML file watcher:`, watcherError);
                    // Continue without watcher — analysis is still valid
                    return undefined;
                }
            },
        });
    }
    /**
     * Validate if file can be visualized with DOM analysis
     */
    static canVisualizeFile(filePath) {
        const htmlExtensions = ['.html', '.htm', '.xhtml'];
        return htmlExtensions.some(ext => filePath.toLowerCase().endsWith(ext));
    }
}
exports.LauncherVisualizeDOM = LauncherVisualizeDOM;


/***/ }),
/* 133 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Files By Language Commands
 * Commands specific to the Files By Language subsection
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FilesByLanguageCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const analysisConfigurationStorage_1 = __webpack_require__(87);
const analysisOrchestrator_1 = __webpack_require__(117);
class FilesByLanguageCommands {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Get files by language command registrations (nested dolls pattern)
     */
    static getCommandRegistrations(context, refreshCallback) {
        console.log('NEW_CODE_ANALYSIS: Collecting Files By Language command registrations');
        const commandRegistrations = [
            {
                commandId: 'newCodeAnalysis.refreshFilesByLanguage',
                callback: () => {
                    console.log('NEW_CODE_ANALYSIS: Refreshing Files by Language section');
                    refreshCallback();
                    vscode.window.showInformationMessage('Files by Language refreshed!');
                },
                description: 'Refresh files by language grouping'
            },
            {
                commandId: 'newCodeAnalysis.openFileFromLanguageGroup',
                callback: (fileUri) => {
                    console.log('NEW_CODE_ANALYSIS: Opening file from language group:', fileUri.fsPath);
                    vscode.window.showTextDocument(fileUri);
                },
                description: 'Open file from language group'
            },
            {
                commandId: 'newCodeAnalysis.runAndAnalyzeFile',
                callback: async (filePath, languageName) => {
                    console.log('NEW_CODE_ANALYSIS: Running and analyzing file:', filePath, 'Language:', languageName);
                    await FilesByLanguageCommands.runAndAnalyzeFile(filePath, languageName, context);
                },
                description: 'Run and analyze file according to current analysis settings'
            }
        ];
        console.log(`NEW_CODE_ANALYSIS: Collected ${commandRegistrations.length} Files By Language command registrations`);
        return commandRegistrations;
    }
    /**
     * Run and analyze file according to current analysis settings
     */
    static async runAndAnalyzeFile(filePath, languageName, context) {
        try {
            // First, open the file
            const workspaceFolder = vscode.workspace.workspaceFolders?.[0];
            if (!workspaceFolder) {
                vscode.window.showErrorMessage('No workspace folder found');
                return;
            }
            const absolutePath = (__webpack_require__(5).resolve)(workspaceFolder.uri.fsPath, filePath);
            const fileUri = vscode.Uri.file(absolutePath);
            // Open the file in editor
            await vscode.window.showTextDocument(fileUri);
            // Get current analysis configuration
            const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
            const analysisMode = await storage.getAnalysisMode();
            console.log(`FILES_BY_LANGUAGE: Running analysis for ${filePath} (${languageName}) in ${analysisMode} mode`);
            // Determine which analysis to run based on file type and configuration
            if (languageName === 'HTML') {
                // HTML files always use DOM Visualization using NEW ENGINE
                console.log('FILES_BY_LANGUAGE: Running DOM Visualization for HTML file using NEW ENGINE');
                await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(absolutePath, 'VisualizeDOM', 'file', context, false);
            }
            else {
                // For other files, use the configured analysis mode with NEW ENGINE
                if (analysisMode === 'XR') {
                    console.log('FILES_BY_LANGUAGE: Running XR Analysis using NEW ENGINE');
                    await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(absolutePath, 'XR', 'file', context, false);
                }
                else if (analysisMode === 'LivePanel') {
                    console.log('FILES_BY_LANGUAGE: Running LivePanel Analysis using NEW ENGINE');
                    await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(absolutePath, 'LivePanel', 'file', context, false);
                }
                else {
                    vscode.window.showWarningMessage(`Unknown analysis mode: ${analysisMode}`);
                }
            }
        }
        catch (error) {
            console.error('FILES_BY_LANGUAGE: Error running analysis:', error);
            vscode.window.showErrorMessage(`Failed to analyze file: ${error}`);
        }
    }
}
exports.FilesByLanguageCommands = FilesByLanguageCommands;


/***/ }),
/* 134 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * File Analysis Commands Module
 * Exports for file analysis commands - Russian dolls pattern
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FileAnalysisXRCommands = exports.FileAnalysisLivePanelCommands = exports.FileAnalysisCommands = void 0;
var fileAnalysisCommands_1 = __webpack_require__(135);
Object.defineProperty(exports, "FileAnalysisCommands", ({ enumerable: true, get: function () { return fileAnalysisCommands_1.FileAnalysisCommands; } }));
var fileAnalysisLivePanelCommands_1 = __webpack_require__(136);
Object.defineProperty(exports, "FileAnalysisLivePanelCommands", ({ enumerable: true, get: function () { return fileAnalysisLivePanelCommands_1.FileAnalysisLivePanelCommands; } }));
var fileAnalysisXRCommands_1 = __webpack_require__(137);
Object.defineProperty(exports, "FileAnalysisXRCommands", ({ enumerable: true, get: function () { return fileAnalysisXRCommands_1.FileAnalysisXRCommands; } }));


/***/ }),
/* 135 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * File Analysis Commands
 * Main coordinator for all file analysis commands (LivePanel and XR)
 * Russian dolls pattern: FileAnalysisCommands -> LivePanel/XR Commands
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FileAnalysisCommands = void 0;
const fileAnalysisLivePanelCommands_1 = __webpack_require__(136);
const fileAnalysisXRCommands_1 = __webpack_require__(137);
class FileAnalysisCommands {
    /**
     * Register all file analysis commands (coordinates LivePanel and XR)
     */
    static registerCommands(context) {
        console.log('FILE_ANALYSIS_COMMANDS: Registering all file analysis commands...');
        const disposables = [];
        // Register LivePanel commands
        console.log('FILE_ANALYSIS_COMMANDS: Registering LivePanel commands...');
        const livePanelCommands = fileAnalysisLivePanelCommands_1.FileAnalysisLivePanelCommands.registerCommands(context);
        disposables.push(...livePanelCommands);
        // Register XR commands  
        console.log('FILE_ANALYSIS_COMMANDS: Registering XR commands...');
        const xrCommands = fileAnalysisXRCommands_1.FileAnalysisXRCommands.registerCommands(context);
        disposables.push(...xrCommands);
        console.log(`FILE_ANALYSIS_COMMANDS: Successfully registered ${disposables.length} file analysis commands`);
        return disposables;
    }
}
exports.FileAnalysisCommands = FileAnalysisCommands;


/***/ }),
/* 136 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * File Analysis LivePanel Commands
 * Commands for analyzing individual files in LivePanel mode
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FileAnalysisLivePanelCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const analysisOrchestrator_1 = __webpack_require__(117);
class FileAnalysisLivePanelCommands {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Register LivePanel file analysis commands
     */
    static registerCommands(context) {
        console.log('FILE_ANALYSIS_LIVEPANEL_COMMANDS: Registering LivePanel analysis commands...');
        const commands = new FileAnalysisLivePanelCommands(context);
        const analyzeFileCommand = vscode.commands.registerCommand('newCodeAnalysis.analyzeFile', (uri) => commands.analyzeFileLivePanel(uri));
        console.log('FILE_ANALYSIS_LIVEPANEL_COMMANDS: LivePanel commands registered successfully');
        return [analyzeFileCommand];
    }
    /**
     * Analyze file in LivePanel mode command handler - NEW ENGINE
     */
    async analyzeFileLivePanel(uri) {
        try {
            let filePath;
            if (uri) {
                // Called from context menu or command palette with URI
                filePath = uri.fsPath;
                console.log(`FILE_ANALYSIS_LIVEPANEL_COMMANDS: LivePanel analysis requested from context menu for: ${filePath}`);
            }
            else {
                // Called from command palette without URI - use active editor
                const activeEditor = vscode.window.activeTextEditor;
                if (!activeEditor) {
                    vscode.window.showWarningMessage('CodeXR: No file selected for LivePanel analysis');
                    return;
                }
                filePath = activeEditor.document.fileName;
                console.log(`FILE_ANALYSIS_LIVEPANEL_COMMANDS: LivePanel analysis requested from command palette for: ${filePath}`);
            }
            // Execute LivePanel analysis using NEW ENGINE - validation handled by orchestrator
            console.log('FILE_ANALYSIS_LIVEPANEL_COMMANDS: Using NEW Unified Engine for analysis');
            await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(filePath, 'LivePanel', 'file', this.context, false // isDeep = false for file analysis
            );
        }
        catch (error) {
            console.error('FILE_ANALYSIS_LIVEPANEL_COMMANDS: Error in LivePanel analyze file command:', error);
            vscode.window.showErrorMessage(`CodeXR: LivePanel analysis failed - ${error}`);
        }
    }
}
exports.FileAnalysisLivePanelCommands = FileAnalysisLivePanelCommands;


/***/ }),
/* 137 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * File Analysis XR Commands
 * Commands for analyzing individual files in XR mode
 * NOW REDIRECTED TO NEW ENGINE
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FileAnalysisXRCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
const languageMetadata_1 = __webpack_require__(138);
const analysisOrchestrator_1 = __webpack_require__(117);
class FileAnalysisXRCommands {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Register XR file analysis commands
     */
    static registerCommands(context) {
        console.log('FILE_ANALYSIS_XR_COMMANDS: 🔗 Registering XR analysis commands (redirected to new engine)...');
        const commands = new FileAnalysisXRCommands(context);
        const analyzeFileXRCommand = vscode.commands.registerCommand('newCodeAnalysis.analyzeFileXR', (uri) => commands.analyzeFileXR(uri));
        console.log('FILE_ANALYSIS_XR_COMMANDS: ✅ XR commands registered successfully (new engine)');
        return [analyzeFileXRCommand];
    }
    /**
     * Analyze file in XR mode command handler (redirected to new engine)
     */
    async analyzeFileXR(uri) {
        console.log('FILE_ANALYSIS_XR_COMMANDS: 🥽 XR File analysis requested - redirecting to new engine...');
        try {
            let filePath;
            if (uri) {
                filePath = uri.fsPath;
                console.log(`FILE_ANALYSIS_XR_COMMANDS: 📄 Using provided URI: ${filePath}`);
            }
            else {
                const activeEditor = vscode.window.activeTextEditor;
                if (!activeEditor) {
                    vscode.window.showWarningMessage('CodeXR: No file selected for XR analysis 🥽');
                    return;
                }
                filePath = activeEditor.document.fileName;
                console.log(`FILE_ANALYSIS_XR_COMMANDS: 📄 Using active editor: ${filePath}`);
            }
            // Check if file is supported using new language detection
            const languageInfo = (0, languageMetadata_1.getLanguageForFile)(filePath);
            if (!languageInfo) {
                vscode.window.showWarningMessage(`CodeXR: File "${path.basename(filePath)}" - Language not supported for XR analysis 🥽`);
                return;
            }
            console.log(`FILE_ANALYSIS_XR_COMMANDS: ✅ File language supported: ${languageInfo.name}`);
            console.log('FILE_ANALYSIS_XR_COMMANDS: 🚀 Passing to AnalysisOrchestrator...');
            // Pass to new engine orchestrator with correct parameters
            await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(filePath, // targetPath
            'XR', // analysisMode
            'file', // targetType
            this.context, // context
            false // isDeep (not applicable for files)
            );
            console.log('FILE_ANALYSIS_XR_COMMANDS: ✅ XR file analysis completed through new engine');
        }
        catch (error) {
            console.error('FILE_ANALYSIS_XR_COMMANDS: ❌ Error in XR file analysis:', error);
            vscode.window.showErrorMessage(`CodeXR: XR analysis failed 🥽 - ${error}`);
        }
    }
}
exports.FileAnalysisXRCommands = FileAnalysisXRCommands;


/***/ }),
/* 138 */
/***/ ((__unused_webpack_module, exports) => {


/**
 * Language metadata for file detection and visualization
 * Maps file extensions to language information including VS Code icons
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ExtensionToLanguageMap = exports.SupportedLanguages = void 0;
exports.getLanguageForFile = getLanguageForFile;
exports.getAllLanguageNames = getAllLanguageNames;
exports.isLanguageSupported = isLanguageSupported;
/**
 * Supported languages with their file extensions and VS Code icon IDs
 * CodeXR Programming Languages Support
 */
exports.SupportedLanguages = [
    { name: "Python", extensions: [".py", ".pyw", ".pyi"], iconId: "python" },
    { name: "Ruby", extensions: [".rb", ".rbw"], iconId: "ruby" },
    { name: "Java", extensions: [".java"], iconId: "java" },
    { name: "C", extensions: [".c", ".h"], iconId: "c" },
    { name: "C++", extensions: [".cpp", ".cxx", ".cc", ".c++", ".hpp", ".hxx", ".hh", ".h++"], iconId: "cpp" },
    { name: "C#", extensions: [".cs"], iconId: "csharp" },
    { name: "Erlang", extensions: [".erl", ".hrl"], iconId: "erlang" },
    { name: "Fortran", extensions: [".f", ".f90", ".f95", ".f03", ".f08"], iconId: "fortran" },
    { name: "GDScript", extensions: [".gd"], iconId: "gdscript" },
    { name: "Go", extensions: [".go"], iconId: "go" },
    { name: "JavaScript", extensions: [".js", ".mjs", ".cjs"], iconId: "javascript" },
    { name: "Kotlin", extensions: [".kt", ".kts"], iconId: "kotlin" },
    { name: "Lua", extensions: [".lua"], iconId: "lua" },
    { name: "Objective-C", extensions: [".m", ".mm"], iconId: "objective-c" },
    { name: "PHP", extensions: [".php", ".phtml", ".php3", ".php4", ".php5"], iconId: "php" },
    { name: "Perl", extensions: [".pl", ".pm"], iconId: "perl" },
    { name: "Scala", extensions: [".scala", ".sc"], iconId: "scala" },
    { name: "Solidity", extensions: [".sol"], iconId: "solidity" },
    { name: "Swift", extensions: [".swift"], iconId: "swift" },
    { name: "TypeScript", extensions: [".ts", ".tsx"], iconId: "typescript" },
    { name: "TTCN-3", extensions: [".ttcn", ".ttcn3"], iconId: "ttcn3" },
    { name: "Vue", extensions: [".vue"], iconId: "vue" },
    { name: "Zig", extensions: [".zig"], iconId: "zig" },
    { name: "HTML", extensions: [".html", ".htm", ".xhtml"], iconId: "html" },
];
/**
 * Create a map from file extension to language info for fast lookup
 */
exports.ExtensionToLanguageMap = new Map();
// Initialize the extension map
exports.SupportedLanguages.forEach(lang => {
    lang.extensions.forEach(ext => {
        exports.ExtensionToLanguageMap.set(ext.toLowerCase(), lang);
    });
});
/**
 * Get language info for a file path based on its extension
 * @param filePath The file path to analyze
 * @returns Language info or null if not recognized
 */
function getLanguageForFile(filePath) {
    const extension = getFileExtension(filePath);
    return exports.ExtensionToLanguageMap.get(extension) || null;
}
/**
 * Extract file extension from a file path
 * @param filePath The file path
 * @returns The lowercase extension including the dot (e.g., ".js")
 */
function getFileExtension(filePath) {
    const lastDot = filePath.lastIndexOf('.');
    if (lastDot === -1 || lastDot === filePath.length - 1) {
        return '';
    }
    return filePath.substring(lastDot).toLowerCase();
}
/**
 * Get all supported language names
 */
function getAllLanguageNames() {
    return exports.SupportedLanguages.map(lang => lang.name);
}
/**
 * Check if a language is supported
 */
function isLanguageSupported(languageName) {
    return exports.SupportedLanguages.some(lang => lang.name === languageName);
}


/***/ }),
/* 139 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Directory Analysis Commands Index
 * Exports for directory analysis command modules
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ProjectAnalysisCommands = exports.DirectoryAnalysisXRCommands = exports.DirectoryAnalysisLivePanelCommands = exports.DirectoryAnalysisCommands = void 0;
var directoryAnalysisCommands_1 = __webpack_require__(140);
Object.defineProperty(exports, "DirectoryAnalysisCommands", ({ enumerable: true, get: function () { return directoryAnalysisCommands_1.DirectoryAnalysisCommands; } }));
var directoryAnalysisLivePanelCommands_1 = __webpack_require__(141);
Object.defineProperty(exports, "DirectoryAnalysisLivePanelCommands", ({ enumerable: true, get: function () { return directoryAnalysisLivePanelCommands_1.DirectoryAnalysisLivePanelCommands; } }));
var directoryAnalysisXRCommands_1 = __webpack_require__(142);
Object.defineProperty(exports, "DirectoryAnalysisXRCommands", ({ enumerable: true, get: function () { return directoryAnalysisXRCommands_1.DirectoryAnalysisXRCommands; } }));
var projectAnalysisCommands_1 = __webpack_require__(143);
Object.defineProperty(exports, "ProjectAnalysisCommands", ({ enumerable: true, get: function () { return projectAnalysisCommands_1.ProjectAnalysisCommands; } }));


/***/ }),
/* 140 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Directory Analysis Commands
 * Commands for analyzing directories
 * Follows the "nested dolls" pattern
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DirectoryAnalysisCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const directoryAnalysisLivePanelCommands_1 = __webpack_require__(141);
const directoryAnalysisXRCommands_1 = __webpack_require__(142);
class DirectoryAnalysisCommands {
    /**
     * Register directory analysis commands
     */
    static registerCommands(context) {
        console.log('DIRECTORY_ANALYSIS: Registering directory analysis commands');
        const disposables = [];
        // Register LivePanel directory analysis commands
        const livePanelDisposables = directoryAnalysisLivePanelCommands_1.DirectoryAnalysisLivePanelCommands.registerCommands(context);
        disposables.push(...livePanelDisposables);
        // Register XR directory analysis commands
        const xrDisposables = directoryAnalysisXRCommands_1.DirectoryAnalysisXRCommands.registerCommands(context);
        disposables.push(...xrDisposables);
        context.subscriptions.push(...disposables);
        console.log(`DIRECTORY_ANALYSIS: Registered ${disposables.length} directory analysis commands`);
        return disposables;
    }
    /**
     * Get command registrations (nested dolls pattern)
     */
    static getCommandRegistrations(context, refreshCallback) {
        console.log('DIRECTORY_ANALYSIS: Collecting directory analysis command registrations');
        const commandRegistrations = [
            {
                commandId: 'newCodeAnalysis.analyzeDirectory',
                callback: async (uri) => {
                    // This will be handled by DirectoryAnalysisLivePanelCommands
                    await vscode.commands.executeCommand('newCodeAnalysis.analyzeDirectory', uri);
                },
                description: 'Analyze directory with LivePanel'
            }
        ];
        console.log(`DIRECTORY_ANALYSIS: Collected ${commandRegistrations.length} directory analysis command registrations`);
        return commandRegistrations;
    }
}
exports.DirectoryAnalysisCommands = DirectoryAnalysisCommands;


/***/ }),
/* 141 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Directory Analysis LivePanel Commands
 * Commands for analyzing directories in LivePanel mode
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DirectoryAnalysisLivePanelCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const analysisOrchestrator_1 = __webpack_require__(117);
class DirectoryAnalysisLivePanelCommands {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Register LivePanel directory analysis commands
     */
    static registerCommands(context) {
        console.log('DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: Registering LivePanel analysis commands...');
        const commands = new DirectoryAnalysisLivePanelCommands(context);
        const analyzeDirectoryCommand = vscode.commands.registerCommand('newCodeAnalysis.analyzeDirectory', (uri) => commands.analyzeDirectoryLivePanel(uri));
        const analyzeDirectoryDeepCommand = vscode.commands.registerCommand('newCodeAnalysis.analyzeDirectoryDeep', (uri) => commands.analyzeDirectoryLivePanelDeep(uri));
        console.log('DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: LivePanel commands registered successfully');
        return [analyzeDirectoryCommand, analyzeDirectoryDeepCommand];
    }
    /**
     * Analyze directory in LivePanel mode command handler
     */
    async analyzeDirectoryLivePanel(uri) {
        try {
            let directoryPath;
            if (uri) {
                // Called from context menu or command palette with URI
                if (uri.scheme === 'file') {
                    const stat = await vscode.workspace.fs.stat(uri);
                    if (stat.type === vscode.FileType.Directory) {
                        directoryPath = uri.fsPath;
                        console.log(`DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: LivePanel analysis requested from context menu for directory: ${directoryPath}`);
                    }
                    else {
                        // If it's a file, use its parent directory
                        const path = __webpack_require__(5);
                        directoryPath = path.dirname(uri.fsPath);
                        console.log(`DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: LivePanel analysis requested from file, using parent directory: ${directoryPath}`);
                    }
                }
                else {
                    vscode.window.showWarningMessage('CodeXR: Invalid URI for directory analysis');
                    return;
                }
            }
            else {
                // Called from command palette without URI - use workspace folder or active file's directory
                if (vscode.workspace.workspaceFolders && vscode.workspace.workspaceFolders.length > 0) {
                    // If there's only one workspace folder, use it
                    if (vscode.workspace.workspaceFolders.length === 1) {
                        directoryPath = vscode.workspace.workspaceFolders[0].uri.fsPath;
                        console.log(`DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: Using workspace folder: ${directoryPath}`);
                    }
                    else {
                        // Multiple workspace folders - let user pick
                        const picked = await vscode.window.showWorkspaceFolderPick();
                        if (!picked) {
                            return; // User cancelled
                        }
                        directoryPath = picked.uri.fsPath;
                        console.log(`DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: User selected workspace folder: ${directoryPath}`);
                    }
                }
                else {
                    // No workspace folders - try to use active editor's directory
                    const activeEditor = vscode.window.activeTextEditor;
                    if (activeEditor) {
                        const path = __webpack_require__(5);
                        directoryPath = path.dirname(activeEditor.document.fileName);
                        console.log(`DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: Using active file's directory: ${directoryPath}`);
                    }
                    else {
                        vscode.window.showWarningMessage('CodeXR: No directory selected for LivePanel analysis');
                        return;
                    }
                }
            }
            // Execute LivePanel directory analysis using NEW ENGINE
            console.log(`DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: Starting NEW ENGINE LivePanel analysis for directory: ${directoryPath}`);
            await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(directoryPath, 'LivePanel', 'directory', this.context, false // isDeep = false for normal directory analysis
            );
        }
        catch (error) {
            console.error('DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: Error in LivePanel analyze directory command:', error);
            vscode.window.showErrorMessage(`CodeXR: Directory LivePanel analysis failed - ${error}`);
        }
    }
    /**
     * Analyze directory in LivePanel Deep mode command handler
     */
    async analyzeDirectoryLivePanelDeep(uri) {
        try {
            let directoryPath;
            if (uri) {
                // Called from context menu or command palette with URI
                if (uri.scheme === 'file') {
                    const stat = await vscode.workspace.fs.stat(uri);
                    if (stat.type === vscode.FileType.Directory) {
                        directoryPath = uri.fsPath;
                        console.log(`DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: LivePanel Deep analysis requested from context menu for directory: ${directoryPath}`);
                    }
                    else {
                        // If it's a file, use its parent directory
                        const path = __webpack_require__(5);
                        directoryPath = path.dirname(uri.fsPath);
                        console.log(`DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: LivePanel Deep analysis requested from file, using parent directory: ${directoryPath}`);
                    }
                }
                else {
                    vscode.window.showErrorMessage('CodeXR: Invalid directory URI for LivePanel Deep analysis');
                    return;
                }
            }
            else {
                // Called from command palette without URI - ask user or use active file's directory
                const workspaceFolders = vscode.workspace.workspaceFolders;
                if (workspaceFolders && workspaceFolders.length > 0) {
                    // Use the first workspace folder
                    directoryPath = workspaceFolders[0].uri.fsPath;
                    console.log(`DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: Using workspace folder for LivePanel Deep analysis: ${directoryPath}`);
                }
                else {
                    // Try to use the directory of the active file
                    const activeEditor = vscode.window.activeTextEditor;
                    if (activeEditor) {
                        const path = __webpack_require__(5);
                        directoryPath = path.dirname(activeEditor.document.fileName);
                        console.log(`DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: Using active file's directory for Deep analysis: ${directoryPath}`);
                    }
                    else {
                        vscode.window.showWarningMessage('CodeXR: No directory selected for LivePanel Deep analysis');
                        return;
                    }
                }
            }
            // Execute LivePanel Deep directory analysis using NEW ENGINE
            console.log(`DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: Deep analysis requested for directory using NEW ENGINE: ${directoryPath}`);
            await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(directoryPath, 'LivePanel', 'directory', this.context, true // isDeep = true for deep directory analysis
            );
        }
        catch (error) {
            console.error('DIRECTORY_ANALYSIS_LIVEPANEL_COMMANDS: Error in LivePanel Deep analyze directory command:', error);
            vscode.window.showErrorMessage(`CodeXR: Directory LivePanel Deep analysis failed - ${error}`);
        }
    }
}
exports.DirectoryAnalysisLivePanelCommands = DirectoryAnalysisLivePanelCommands;


/***/ }),
/* 142 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Directory Analysis XR Commands
 * Commands for analyzing directories in XR mode
 * NOW REDIRECTED TO NEW ENGINE
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DirectoryAnalysisXRCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const analysisOrchestrator_1 = __webpack_require__(117);
class DirectoryAnalysisXRCommands {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Register directory analysis XR commands
     */
    static registerCommands(context) {
        console.log('DIRECTORY_ANALYSIS_XR_COMMANDS: Registering XR analysis commands...');
        const commands = new DirectoryAnalysisXRCommands(context);
        const analyzeDirectoryXRCommand = vscode.commands.registerCommand('newCodeAnalysis.analyzeDirectoryXR', (uri) => commands.analyzeDirectoryXR(uri));
        const analyzeDirectoryXRDeepCommand = vscode.commands.registerCommand('newCodeAnalysis.analyzeDirectoryXRDeep', (uri) => commands.analyzeDirectoryXRDeep(uri));
        const analyzeProjectXRCommand = vscode.commands.registerCommand('newCodeAnalysis.analyzeProjectXR', () => commands.analyzeProjectXR());
        const analyzeProjectXRDeepCommand = vscode.commands.registerCommand('newCodeAnalysis.analyzeProjectXRDeep', () => commands.analyzeProjectXRDeep());
        console.log('DIRECTORY_ANALYSIS_XR_COMMANDS: XR commands registered successfully');
        return [analyzeDirectoryXRCommand, analyzeDirectoryXRDeepCommand, analyzeProjectXRCommand, analyzeProjectXRDeepCommand];
    }
    /**
     * Analyze directory in XR mode command handler
     */
    async analyzeDirectoryXR(uri) {
        try {
            let directoryPath;
            if (uri) {
                // Called from context menu or command palette with URI
                if (uri.scheme === 'file') {
                    const stat = await vscode.workspace.fs.stat(uri);
                    if (stat.type === vscode.FileType.Directory) {
                        directoryPath = uri.fsPath;
                        console.log(`DIRECTORY_ANALYSIS_XR_COMMANDS: XR analysis requested from context menu for directory: ${directoryPath}`);
                    }
                    else {
                        // If it's a file, use its parent directory
                        directoryPath = vscode.Uri.file(uri.fsPath).with({ path: uri.path.substring(0, uri.path.lastIndexOf('/')) }).fsPath;
                        console.log(`DIRECTORY_ANALYSIS_XR_COMMANDS: XR analysis requested from file context, using parent directory: ${directoryPath}`);
                    }
                }
                else {
                    vscode.window.showErrorMessage('Invalid file path for directory analysis');
                    return;
                }
            }
            else {
                // Called from command palette without URI - ask user to select directory
                const folderUris = await vscode.window.showOpenDialog({
                    canSelectFiles: false,
                    canSelectFolders: true,
                    canSelectMany: false,
                    openLabel: 'Select Directory to Analyze (XR)'
                });
                if (!folderUris || folderUris.length === 0) {
                    console.log('DIRECTORY_ANALYSIS_XR_COMMANDS: User cancelled directory selection');
                    return;
                }
                directoryPath = folderUris[0].fsPath;
                console.log(`DIRECTORY_ANALYSIS_XR_COMMANDS: XR analysis requested from dialog for directory: ${directoryPath}`);
            }
            // Use the XR directory analysis engine (unified progress tracking handles the progress bar)
            try {
                await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(directoryPath, 'XR', 'directory', this.context, false);
            }
            catch (error) {
                console.error('DIRECTORY_ANALYSIS_XR_COMMANDS: Directory XR analysis failed:', error);
                vscode.window.showErrorMessage(`Directory XR analysis failed: ${error instanceof Error ? error.message : String(error)}`);
            }
        }
        catch (error) {
            console.error('DIRECTORY_ANALYSIS_XR_COMMANDS: Error in analyzeDirectoryXR:', error);
            vscode.window.showErrorMessage(`Failed to start directory XR analysis: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Analyze directory in XR Deep mode
     */
    async analyzeDirectoryXRDeep(uri) {
        console.log('DIRECTORY_ANALYSIS_XR_COMMANDS: Starting XR Deep directory analysis command...');
        try {
            let directoryPath;
            if (uri) {
                // Called from context menu or command palette with URI
                if (uri.scheme === 'file') {
                    const stat = await vscode.workspace.fs.stat(uri);
                    if (stat.type === vscode.FileType.Directory) {
                        directoryPath = uri.fsPath;
                        console.log(`DIRECTORY_ANALYSIS_XR_COMMANDS: XR Deep analysis requested from context menu for directory: ${directoryPath}`);
                    }
                    else {
                        // If it's a file, use its parent directory
                        directoryPath = vscode.Uri.file(uri.fsPath).with({ path: uri.path.substring(0, uri.path.lastIndexOf('/')) }).fsPath;
                        console.log(`DIRECTORY_ANALYSIS_XR_COMMANDS: XR Deep analysis requested from file context, using parent directory: ${directoryPath}`);
                    }
                }
                else {
                    vscode.window.showErrorMessage('Invalid file path for directory XR Deep analysis');
                    return;
                }
            }
            else {
                // Called from command palette without URI, prompt for directory selection
                const folderUris = await vscode.window.showOpenDialog({
                    canSelectFolders: true,
                    canSelectFiles: false,
                    canSelectMany: false,
                    openLabel: 'Select Directory for XR Deep Analysis'
                });
                if (!folderUris || folderUris.length === 0) {
                    console.log('DIRECTORY_ANALYSIS_XR_COMMANDS: XR Deep analysis cancelled by user');
                    return;
                }
                directoryPath = folderUris[0].fsPath;
                console.log(`DIRECTORY_ANALYSIS_XR_COMMANDS: XR Deep analysis requested from command palette for directory: ${directoryPath}`);
            }
            console.log(`DIRECTORY_ANALYSIS_XR_COMMANDS: XR Deep analysis for directory: ${directoryPath}`);
            // Use the XR Deep directory analysis engine (unified progress tracking handles the progress bar)
            try {
                await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(directoryPath, 'XR', 'directory', this.context, true);
            }
            catch (error) {
                console.error('DIRECTORY_ANALYSIS_XR_COMMANDS: Directory XR Deep analysis failed:', error);
                vscode.window.showErrorMessage(`Directory XR Deep analysis failed: ${error instanceof Error ? error.message : String(error)}`);
            }
        }
        catch (error) {
            console.error('DIRECTORY_ANALYSIS_XR_COMMANDS: Error in analyzeDirectoryXRDeep:', error);
            vscode.window.showErrorMessage(`Failed to start directory XR Deep analysis: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Analyze project in XR mode command handler
     */
    async analyzeProjectXR() {
        try {
            console.log('DIRECTORY_ANALYSIS_XR_COMMANDS: Starting Project XR analysis...');
            // Get the workspace folder (project root)
            const workspaceFolders = vscode.workspace.workspaceFolders;
            if (!workspaceFolders || workspaceFolders.length === 0) {
                vscode.window.showErrorMessage('No workspace folder found. Please open a project folder first.');
                return;
            }
            let projectPath;
            if (workspaceFolders.length === 1) {
                // Single workspace folder
                projectPath = workspaceFolders[0].uri.fsPath;
            }
            else {
                // Multiple workspace folders - let user choose
                const selectedFolder = await vscode.window.showQuickPick(workspaceFolders.map(folder => ({
                    label: folder.name,
                    description: folder.uri.fsPath,
                    folder: folder
                })), { placeHolder: 'Select project to analyze (XR)' });
                if (!selectedFolder) {
                    console.log('DIRECTORY_ANALYSIS_XR_COMMANDS: User cancelled project selection');
                    return;
                }
                projectPath = selectedFolder.folder.uri.fsPath;
            }
            console.log(`DIRECTORY_ANALYSIS_XR_COMMANDS: Project XR analysis for: ${projectPath}`);
            // Use directory XR launcher for project analysis (unified progress tracking handles the progress bar)
            try {
                await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(projectPath, 'XR', 'directory', this.context, false);
            }
            catch (error) {
                console.error('DIRECTORY_ANALYSIS_XR_COMMANDS: Project XR analysis failed:', error);
                vscode.window.showErrorMessage(`Project XR analysis failed: ${error instanceof Error ? error.message : String(error)}`);
            }
        }
        catch (error) {
            console.error('DIRECTORY_ANALYSIS_XR_COMMANDS: Error in analyzeProjectXR:', error);
            vscode.window.showErrorMessage(`Failed to start project XR analysis: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Analyze project in XR Deep mode command handler
     */
    async analyzeProjectXRDeep() {
        try {
            console.log('DIRECTORY_ANALYSIS_XR_COMMANDS: Starting Project XR Deep analysis...');
            // Get the workspace folder (project root)
            const workspaceFolders = vscode.workspace.workspaceFolders;
            if (!workspaceFolders || workspaceFolders.length === 0) {
                vscode.window.showErrorMessage('No workspace folder found. Please open a project folder first.');
                return;
            }
            let projectPath;
            if (workspaceFolders.length === 1) {
                // Single workspace folder
                projectPath = workspaceFolders[0].uri.fsPath;
            }
            else {
                // Multiple workspace folders - let user choose
                const selectedFolder = await vscode.window.showQuickPick(workspaceFolders.map(folder => ({
                    label: folder.name,
                    description: folder.uri.fsPath,
                    folder: folder
                })), { placeHolder: 'Select project to analyze (XR Deep)' });
                if (!selectedFolder) {
                    console.log('DIRECTORY_ANALYSIS_XR_COMMANDS: User cancelled project selection');
                    return;
                }
                projectPath = selectedFolder.folder.uri.fsPath;
            }
            console.log(`DIRECTORY_ANALYSIS_XR_COMMANDS: Project XR Deep analysis for: ${projectPath}`);
            // Show progress notification
            // Use directory XR Deep launcher for project analysis (unified progress tracking handles the progress bar)
            try {
                await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(projectPath, 'XR', 'directory', this.context, true);
            }
            catch (error) {
                console.error('DIRECTORY_ANALYSIS_XR_COMMANDS: Project XR Deep analysis failed:', error);
                vscode.window.showErrorMessage(`Project XR Deep analysis failed: ${error instanceof Error ? error.message : String(error)}`);
            }
        }
        catch (error) {
            console.error('DIRECTORY_ANALYSIS_XR_COMMANDS: Error in analyzeProjectXRDeep:', error);
            vscode.window.showErrorMessage(`Failed to start project XR Deep analysis: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
}
exports.DirectoryAnalysisXRCommands = DirectoryAnalysisXRCommands;


/***/ }),
/* 143 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Project Analysis Commands
 * Commands for analyzing projects from anywhere in VS Code
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ProjectAnalysisCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const analysisOrchestrator_1 = __webpack_require__(117);
class ProjectAnalysisCommands {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Register project analysis commands
     */
    static registerCommands(context) {
        console.log('PROJECT_ANALYSIS_COMMANDS: Registering project analysis commands...');
        const commands = new ProjectAnalysisCommands(context);
        const analyzeProjectCommand = vscode.commands.registerCommand('newCodeAnalysis.analyzeProject', () => commands.analyzeProjectLivePanel());
        const analyzeProjectDeepCommand = vscode.commands.registerCommand('newCodeAnalysis.analyzeProjectDeep', () => commands.analyzeProjectLivePanelDeep());
        console.log('PROJECT_ANALYSIS_COMMANDS: Project analysis commands registered successfully');
        return [analyzeProjectCommand, analyzeProjectDeepCommand];
    }
    /**
     * Analyze current workspace/project in LivePanel mode
     */
    async analyzeProjectLivePanel() {
        try {
            console.log('PROJECT_ANALYSIS_COMMANDS: LivePanel project analysis requested');
            // Get the current workspace folder
            const workspaceFolders = vscode.workspace.workspaceFolders;
            if (!workspaceFolders || workspaceFolders.length === 0) {
                vscode.window.showErrorMessage('No workspace is currently open. Please open a project folder to analyze.');
                return;
            }
            let projectPath;
            if (workspaceFolders.length === 1) {
                // Single workspace folder - use it directly
                projectPath = workspaceFolders[0].uri.fsPath;
                console.log(`PROJECT_ANALYSIS_COMMANDS: Using single workspace folder: ${projectPath}`);
            }
            else {
                // Multiple workspace folders - let user choose
                const workspaceItems = workspaceFolders.map(folder => ({
                    label: folder.name,
                    description: folder.uri.fsPath,
                    folder: folder
                }));
                const selectedItem = await vscode.window.showQuickPick(workspaceItems, {
                    placeHolder: 'Select workspace folder to analyze',
                    canPickMany: false
                });
                if (!selectedItem) {
                    console.log('PROJECT_ANALYSIS_COMMANDS: User cancelled workspace selection');
                    return;
                }
                projectPath = selectedItem.folder.uri.fsPath;
                console.log(`PROJECT_ANALYSIS_COMMANDS: User selected workspace folder: ${projectPath}`);
            }
            // Show progress notification
            vscode.window.withProgress({
                location: vscode.ProgressLocation.Notification,
                title: "Analyzing Project",
                cancellable: false
            }, async (progress) => {
                progress.report({ message: "Starting project analysis..." });
                try {
                    // Use the NEW ENGINE for directory analysis
                    await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(projectPath, 'LivePanel', 'directory', this.context, false // isDeep = false for normal project analysis
                    );
                    vscode.window.showInformationMessage(`Project analysis completed successfully! ✅`, 'View Results').then(action => {
                        if (action === 'View Results') {
                            // The analysis will have opened the results automatically
                            console.log('PROJECT_ANALYSIS_COMMANDS: User chose to view results');
                        }
                    });
                }
                catch (error) {
                    console.error('PROJECT_ANALYSIS_COMMANDS: Project analysis failed:', error);
                    vscode.window.showErrorMessage(`Project analysis failed: ${error instanceof Error ? error.message : String(error)}`);
                }
            });
        }
        catch (error) {
            console.error('PROJECT_ANALYSIS_COMMANDS: Error in analyzeProjectLivePanel:', error);
            vscode.window.showErrorMessage(`Failed to start project analysis: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Analyze current workspace/project in LivePanel Deep mode
     */
    async analyzeProjectLivePanelDeep() {
        try {
            console.log('PROJECT_ANALYSIS_COMMANDS: LivePanel DEEP project analysis requested');
            // Get the current workspace folder
            const workspaceFolders = vscode.workspace.workspaceFolders;
            if (!workspaceFolders || workspaceFolders.length === 0) {
                vscode.window.showErrorMessage('No workspace is currently open. Please open a project folder to analyze.');
                return;
            }
            let projectPath;
            if (workspaceFolders.length === 1) {
                // Single workspace folder - use it directly
                projectPath = workspaceFolders[0].uri.fsPath;
                console.log(`PROJECT_ANALYSIS_COMMANDS: Using single workspace folder for DEEP analysis: ${projectPath}`);
            }
            else {
                // Multiple workspace folders - let user choose
                const workspaceItems = workspaceFolders.map(folder => ({
                    label: folder.name,
                    description: folder.uri.fsPath,
                    folder: folder
                }));
                const selectedItem = await vscode.window.showQuickPick(workspaceItems, {
                    placeHolder: 'Select workspace folder to analyze (Deep)',
                    canPickMany: false
                });
                if (!selectedItem) {
                    console.log('PROJECT_ANALYSIS_COMMANDS: User cancelled workspace selection for DEEP analysis');
                    return;
                }
                projectPath = selectedItem.folder.uri.fsPath;
                console.log(`PROJECT_ANALYSIS_COMMANDS: User selected workspace folder for DEEP analysis: ${projectPath}`);
            }
            // Show progress notification (removed deep analysis warning as requested)
            vscode.window.withProgress({
                location: vscode.ProgressLocation.Notification,
                title: "Analyzing Project (Deep)",
                cancellable: false
            }, async (progress) => {
                progress.report({ message: "Starting deep project analysis (all subdirectories)..." });
                try {
                    // Use the NEW ENGINE for deep directory analysis
                    await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(projectPath, 'LivePanel', 'directory', this.context, true // isDeep = true for deep project analysis
                    );
                    vscode.window.showInformationMessage(`Deep project analysis completed successfully! 🎯`, 'View Results').then(action => {
                        if (action === 'View Results') {
                            // The analysis will have opened the results automatically
                            console.log('PROJECT_ANALYSIS_COMMANDS: User chose to view deep analysis results');
                        }
                    });
                }
                catch (error) {
                    console.error('PROJECT_ANALYSIS_COMMANDS: Deep project analysis failed:', error);
                    vscode.window.showErrorMessage(`Deep project analysis failed: ${error instanceof Error ? error.message : String(error)}`);
                }
            });
        }
        catch (error) {
            console.error('PROJECT_ANALYSIS_COMMANDS: Error in analyzeProjectLivePanelDeep:', error);
            vscode.window.showErrorMessage(`Failed to start deep project analysis: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
}
exports.ProjectAnalysisCommands = ProjectAnalysisCommands;


/***/ }),
/* 144 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Clean Analysis Commands Module
 * Exports for clean analysis functionality
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CleanAnalysisCommands = void 0;
var cleanAnalysisCommands_1 = __webpack_require__(145);
Object.defineProperty(exports, "CleanAnalysisCommands", ({ enumerable: true, get: function () { return cleanAnalysisCommands_1.CleanAnalysisCommands; } }));


/***/ }),
/* 145 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Clean Analysis Command
 * Handles cleaning of analysis files from workspace storage
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CleanAnalysisCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
class CleanAnalysisCommands {
    /**
     * Get command registrations for clean analysis functionality (Nested Dolls pattern)
     */
    static getCommandRegistrations(context, refreshCallback) {
        console.log('CLEAN_ANALYSIS_COMMANDS: Collecting command registrations...');
        const commands = [];
        // Manual clean command
        commands.push({
            commandId: 'codeXR.newCodeAnalysis.cleanAnalysis',
            callback: async () => {
                await CleanAnalysisCommands.executeCleanAnalysis(context);
                if (refreshCallback) {
                    refreshCallback();
                }
            },
            description: 'Clean all analysis directories and files from workspace storage'
        });
        console.log(`CLEAN_ANALYSIS_COMMANDS: Collected ${commands.length} command registrations`);
        return commands;
    }
    /**
     * Execute cleanup of analysis files on plugin startup
     */
    static async executeStartupCleanup(context) {
        try {
            console.log('CLEAN_ANALYSIS_COMMANDS: Executing startup cleanup...');
            // Clean the entire workspace storage
            const cleanupResult = await CleanAnalysisCommands.cleanCompleteWorkspaceStorage(context);
            if (cleanupResult) {
                console.log('CLEAN_ANALYSIS_COMMANDS: Complete workspace storage cleanup completed successfully');
            }
            else {
                console.warn('CLEAN_ANALYSIS_COMMANDS: Complete workspace storage cleanup completed with some errors');
            }
        }
        catch (error) {
            console.error('CLEAN_ANALYSIS_COMMANDS: Error during startup cleanup:', error);
        }
    }
    /**
     * Execute manual cleanup of analysis files
     */
    static async executeCleanAnalysis(context) {
        try {
            console.log('CLEAN_ANALYSIS_COMMANDS: Manual cleanup requested');
            // Show confirmation dialog
            const result = await vscode.window.showWarningMessage('Are you sure you want to clean all analysis files? This will remove all saved analysis data from the current workspace.', { modal: true }, 'Clean Analysis Files', 'Cancel');
            if (result !== 'Clean Analysis Files') {
                console.log('CLEAN_ANALYSIS_COMMANDS: Manual cleanup cancelled by user');
                return;
            }
            // Stop all file watchers first
            console.log('CLEAN_ANALYSIS_COMMANDS: Stopping all file watchers...');
            // Note: Watchers will be stopped when cleaning directories
            // Execute cleanup
            const cleanupResult = await CleanAnalysisCommands.cleanAllAnalysisDirectories(context);
            if (cleanupResult) {
                vscode.window.showInformationMessage('CodeXR: Analysis files cleaned successfully');
                console.log('CLEAN_ANALYSIS_COMMANDS: Manual cleanup completed successfully');
            }
            else {
                vscode.window.showErrorMessage('CodeXR: Failed to clean some analysis files. Check console for details.');
                console.error('CLEAN_ANALYSIS_COMMANDS: Manual cleanup completed with errors');
            }
        }
        catch (error) {
            console.error('CLEAN_ANALYSIS_COMMANDS: Error during manual cleanup:', error);
            vscode.window.showErrorMessage(`CodeXR: Error during cleanup: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Clean complete workspace storage
     */
    static async cleanCompleteWorkspaceStorage(context) {
        try {
            if (!context.storageUri) {
                console.log('CLEAN_ANALYSIS_COMMANDS: No storage URI available, skipping cleanup');
                return true;
            }
            const storagePath = context.storageUri.fsPath;
            if (fs.existsSync(storagePath)) {
                console.log(`CLEAN_ANALYSIS_COMMANDS: Cleaning storage directory: ${storagePath}`);
                await this.deleteDirectoryRecursive(storagePath);
                console.log('CLEAN_ANALYSIS_COMMANDS: Storage directory cleaned successfully');
            }
            else {
                console.log('CLEAN_ANALYSIS_COMMANDS: Storage directory does not exist, nothing to clean');
            }
            return true;
        }
        catch (error) {
            console.error('CLEAN_ANALYSIS_COMMANDS: Error cleaning workspace storage:', error);
            return false;
        }
    }
    /**
     * Clean all analysis directories
     */
    static async cleanAllAnalysisDirectories(context) {
        try {
            if (!context.storageUri) {
                console.log('CLEAN_ANALYSIS_COMMANDS: No storage URI available, skipping cleanup');
                return true;
            }
            const storagePath = context.storageUri.fsPath;
            const analysisPath = path.join(storagePath, 'analysis');
            if (fs.existsSync(analysisPath)) {
                console.log(`CLEAN_ANALYSIS_COMMANDS: Cleaning analysis directory: ${analysisPath}`);
                await this.deleteDirectoryRecursive(analysisPath);
                console.log('CLEAN_ANALYSIS_COMMANDS: Analysis directory cleaned successfully');
            }
            else {
                console.log('CLEAN_ANALYSIS_COMMANDS: Analysis directory does not exist, nothing to clean');
            }
            return true;
        }
        catch (error) {
            console.error('CLEAN_ANALYSIS_COMMANDS: Error cleaning analysis directories:', error);
            return false;
        }
    }
    /**
     * Delete directory recursively
     */
    static async deleteDirectoryRecursive(dirPath) {
        if (!fs.existsSync(dirPath)) {
            return;
        }
        const files = fs.readdirSync(dirPath);
        for (const file of files) {
            const filePath = path.join(dirPath, file);
            const stat = fs.statSync(filePath);
            if (stat.isDirectory()) {
                await this.deleteDirectoryRecursive(filePath);
            }
            else {
                fs.unlinkSync(filePath);
            }
        }
        fs.rmdirSync(dirPath);
    }
}
exports.CleanAnalysisCommands = CleanAnalysisCommands;


/***/ }),
/* 146 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * DOM Visualization Commands Index
 * Central export point for DOM visualization commands
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DOMVisualizationCommands = void 0;
var domVisualizationCommands_1 = __webpack_require__(147);
Object.defineProperty(exports, "DOMVisualizationCommands", ({ enumerable: true, get: function () { return domVisualizationCommands_1.DOMVisualizationCommands; } }));


/***/ }),
/* 147 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * DOM Visualization Commands
 * Handles commands for visualizing HTML DOM structure
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DOMVisualizationCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const analysisOrchestrator_1 = __webpack_require__(117);
const launcherVisualizeDOM_1 = __webpack_require__(132);
const uriPathConverter_1 = __webpack_require__(148);
class DOMVisualizationCommands {
    context;
    constructor(context) {
        this.context = context;
        console.log('DOM_VISUALIZATION_COMMANDS: DOM Visualization commands initialized');
    }
    /**
     * Get command registrations for DOM visualization (nested dolls pattern)
     */
    static getCommandRegistrations(context, refreshCallback) {
        console.log('DOM_VISUALIZATION_COMMANDS: Collecting DOM visualization command registrations');
        const domVisualizationInstance = new DOMVisualizationCommands(context);
        const commandRegistrations = [
            {
                commandId: 'newCodeAnalysis.visualizeDOM',
                callback: (filePath) => domVisualizationInstance.visualizeDOM(filePath),
                description: 'Visualize HTML DOM structure'
            }
        ];
        console.log(`DOM_VISUALIZATION_COMMANDS: Collected ${commandRegistrations.length} command registrations`);
        return commandRegistrations;
    }
    /**
     * Handle DOM visualization command for HTML files - NEW ENGINE
     */
    async visualizeDOM(filePath) {
        try {
            console.log('DOM_VISUALIZATION_COMMANDS: DOM visualization requested (NEW ENGINE)');
            console.log('DOM_VISUALIZATION_COMMANDS: Input type:', typeof filePath, 'Input value:', filePath);
            // Convert input to file system path using utility
            const fsPath = (0, uriPathConverter_1.getFileSystemPathFromEditorOrInput)(filePath);
            if (!fsPath) {
                vscode.window.showErrorMessage('No HTML file selected or file path could not be determined');
                return;
            }
            console.log(`DOM_VISUALIZATION_COMMANDS: File system path: ${fsPath}`);
            // Validate file is HTML using the new launcher
            if (!launcherVisualizeDOM_1.LauncherVisualizeDOM.canVisualizeFile(fsPath)) {
                vscode.window.showWarningMessage(`File "${fsPath}" is not a supported HTML file for DOM visualization`);
                return;
            }
            console.log(`DOM_VISUALIZATION_COMMANDS: Starting VisualizeDOM analysis using NEW ENGINE for: ${fsPath}`);
            // Use the new engine to orchestrate VisualizeDOM analysis
            await analysisOrchestrator_1.AnalysisOrchestrator.orchestrateAnalysis(fsPath, 'VisualizeDOM', 'file', this.context, false // isDeep = false for file analysis
            );
        }
        catch (error) {
            console.error('DOM_VISUALIZATION_COMMANDS: Error visualizing DOM (NEW ENGINE):', error);
            vscode.window.showErrorMessage(`Failed to visualize DOM: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Show available DOM visualization options
     */
    async showDOMVisualizationOptions() {
        const options = [
            {
                label: '$(browser) DOM Structure Tree',
                description: 'Visualize HTML DOM as an interactive tree structure',
                command: 'tree'
            },
            {
                label: '$(graph) DOM Hierarchy Graph',
                description: 'Show DOM elements and their relationships as a graph',
                command: 'graph'
            },
            {
                label: '$(pie-chart) DOM Element Statistics',
                description: 'Analyze DOM element distribution and usage',
                command: 'stats'
            }
        ];
        const selected = await vscode.window.showQuickPick(options, {
            placeHolder: 'Select DOM visualization type',
            canPickMany: false
        });
        if (selected) {
            console.log(`DOM_VISUALIZATION_COMMANDS: Selected visualization type: ${selected.command}`);
            // TODO: Implement different visualization types
            vscode.window.showInformationMessage(`DOM ${selected.command} visualization will be implemented soon!`);
        }
    }
}
exports.DOMVisualizationCommands = DOMVisualizationCommands;


/***/ }),
/* 148 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * URI Path Converter Utility
 * Handles conversion between VS Code URIs and file system paths
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.convertToFileSystemPath = convertToFileSystemPath;
exports.getFileSystemPathFromEditorOrInput = getFileSystemPathFromEditorOrInput;
exports.isValidFileSystemPath = isValidFileSystemPath;
const vscode = __importStar(__webpack_require__(1));
/**
 * Convert VS Code URI or file path to a standard file system path
 * Handles various input types that VS Code commands can receive
 */
function convertToFileSystemPath(input) {
    try {
        // Handle null/undefined
        if (!input) {
            return null;
        }
        console.log('URI_PATH_CONVERTER: Processing input:', typeof input, input);
        // If it's already a string path
        if (typeof input === 'string') {
            // Check if it's a URI string
            if (input.startsWith('file://') || input.startsWith('vscode-')) {
                const uri = vscode.Uri.parse(input);
                console.log('URI_PATH_CONVERTER: Converted URI string to path:', uri.fsPath);
                return uri.fsPath;
            }
            // It's already a file system path
            console.log('URI_PATH_CONVERTER: Input is already a file system path:', input);
            return input;
        }
        // If it's a VS Code URI object
        if (input && typeof input === 'object') {
            // Check if it has fsPath property (VS Code URI)
            if ('fsPath' in input && typeof input.fsPath === 'string') {
                console.log('URI_PATH_CONVERTER: Extracted fsPath from URI object:', input.fsPath);
                return input.fsPath;
            }
            // Check if it has scheme and path properties (URI-like object)
            if ('scheme' in input && 'path' in input) {
                const uri = vscode.Uri.from(input);
                console.log('URI_PATH_CONVERTER: Created URI from object and extracted path:', uri.fsPath);
                return uri.fsPath;
            }
            // Try to convert object to string and parse as URI
            const stringified = String(input);
            if (stringified.startsWith('file://') || stringified.startsWith('vscode-')) {
                const uri = vscode.Uri.parse(stringified);
                console.log('URI_PATH_CONVERTER: Converted stringified object to path:', uri.fsPath);
                return uri.fsPath;
            }
        }
        console.warn('URI_PATH_CONVERTER: Could not convert input to file system path:', input);
        return null;
    }
    catch (error) {
        console.error('URI_PATH_CONVERTER: Error converting input to file system path:', error);
        return null;
    }
}
/**
 * Get file system path from active editor or provided input
 */
function getFileSystemPathFromEditorOrInput(input) {
    try {
        // First try to convert the provided input
        if (input) {
            const convertedPath = convertToFileSystemPath(input);
            if (convertedPath) {
                return convertedPath;
            }
        }
        // Fallback to active editor
        const activeEditor = vscode.window.activeTextEditor;
        if (activeEditor) {
            const editorPath = activeEditor.document.uri.fsPath;
            console.log('URI_PATH_CONVERTER: Using active editor path:', editorPath);
            return editorPath;
        }
        console.warn('URI_PATH_CONVERTER: No valid file path found from input or active editor');
        return null;
    }
    catch (error) {
        console.error('URI_PATH_CONVERTER: Error getting file system path:', error);
        return null;
    }
}
/**
 * Validate that a path is a valid file system path
 */
function isValidFileSystemPath(path) {
    try {
        if (!path || typeof path !== 'string') {
            return false;
        }
        // Basic validation - path should not contain URI schemes
        if (path.includes('://')) {
            return false;
        }
        // Should contain file separators (/ or \)
        if (!path.includes('/') && !path.includes('\\')) {
            return false;
        }
        return true;
    }
    catch (error) {
        console.error('URI_PATH_CONVERTER: Error validating file system path:', error);
        return false;
    }
}


/***/ }),
/* 149 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Analysis File Setting Item
 * Manages the analysis file mode (XR/LivePanel) configuration
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisFileSetting = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const configuration_1 = __webpack_require__(151);
class AnalysisFileSetting {
    context;
    currentMode = 'XR'; // Default mode (will be loaded from storage)
    storage;
    constructor(context) {
        this.context = context;
        console.log('NEW_CODE_ANALYSIS: Initializing Analysis File setting');
        this.storage = configuration_1.AnalysisConfigurationStorage.getInstance(context);
        this.loadSavedMode();
    }
    /**
     * Get the tree item for this setting
     */
    async getSettingItem() {
        // Always get the current value from storage to ensure it's up to date
        const currentMode = await this.storage.getAnalysisFileMode();
        const modeText = currentMode === 'XR'
            ? 'Analysis File with Immersive VR/AR'
            : 'Analysis File with Standard Experience';
        const iconPath = currentMode === 'LivePanel'
            ? new vscode.ThemeIcon('file-code', new vscode.ThemeColor('charts.green'))
            : new vscode.ThemeIcon('vr', new vscode.ThemeColor('charts.purple'));
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(modeText, vscode.TreeItemCollapsibleState.None, 'analysis-result', {
            command: 'newCodeAnalysis.toggleAnalysisFileMode',
            title: 'Toggle Analysis File Mode',
            arguments: []
        }, iconPath, `Click to switch between analysis modes. Current: ${currentMode === 'XR' ? 'Immersive VR/AR experience for detailed analysis' : 'Standard panel for quick review'}`, currentMode, 'analysisFileModeSetting');
    }
    /**
     * Toggle between XR and LivePanel modes
     */
    async toggleMode() {
        this.currentMode = this.currentMode === 'XR' ? 'LivePanel' : 'XR';
        console.log(`NEW_CODE_ANALYSIS: Analysis file mode switched to ${this.currentMode}`);
        await this.saveModeToSettings();
        return this.currentMode;
    }
    /**
     * Get current mode
     */
    getCurrentMode() {
        return this.currentMode;
    }
    /**
     * Set specific mode
     */
    async setMode(mode) {
        this.currentMode = mode;
        await this.saveModeToSettings();
    }
    /**
     * Load saved mode from globalStorage
     */
    async loadSavedMode() {
        try {
            const savedMode = await this.storage.getAnalysisFileMode();
            this.currentMode = savedMode;
            console.log(`NEW_CODE_ANALYSIS: Loaded analysis file mode from storage: ${this.currentMode}`);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error loading saved mode, using default:', error);
            this.currentMode = 'XR'; // Fallback to default
        }
    }
    /**
     * Save mode to globalStorage
     */
    async saveModeToSettings() {
        try {
            await this.storage.setAnalysisFileMode(this.currentMode);
            console.log(`NEW_CODE_ANALYSIS: Saved analysis file mode to storage: ${this.currentMode}`);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error saving mode to storage:', error);
        }
    }
}
exports.AnalysisFileSetting = AnalysisFileSetting;


/***/ }),
/* 150 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * New Code Analysis Tree Items
 * Tree item definitions for the new code analysis view
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.NewCodeAnalysisItemFactory = exports.NewCodeAnalysisTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
/**
 * TODO: Define tree items for new code analysis
 * - Analysis result items
 * - File analysis items
 * - Method/function analysis items
 * - Error/warning items
 */
class NewCodeAnalysisTreeItem extends vscode.TreeItem {
    label;
    collapsibleState;
    newCodeAnalysisItemType;
    constructor(label, collapsibleState, newCodeAnalysisItemType, command, iconPath, tooltip, description, contextValue) {
        super(label, collapsibleState);
        this.label = label;
        this.collapsibleState = collapsibleState;
        this.newCodeAnalysisItemType = newCodeAnalysisItemType;
        // TODO: Set appropriate properties
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip || this.label;
        this.description = description;
        this.contextValue = contextValue || newCodeAnalysisItemType;
    }
}
exports.NewCodeAnalysisTreeItem = NewCodeAnalysisTreeItem;
/**
 * TODO: Factory class for creating different types of analysis items
 */
class NewCodeAnalysisItemFactory {
    /**
     * TODO: Create analysis result item
     */
    static createAnalysisResultItem(data) {
        // TODO: Implementation
        return new NewCodeAnalysisTreeItem('TODO: Analysis Result', vscode.TreeItemCollapsibleState.Collapsed, 'analysis-result');
    }
    /**
     * TODO: Create file analysis item
     */
    static createFileAnalysisItem(filePath) {
        // TODO: Implementation
        return new NewCodeAnalysisTreeItem('TODO: File Analysis', vscode.TreeItemCollapsibleState.Collapsed, 'file-item');
    }
    /**
     * TODO: Create method analysis item
     */
    static createMethodAnalysisItem(methodName) {
        // TODO: Implementation
        return new NewCodeAnalysisTreeItem('TODO: Method Analysis', vscode.TreeItemCollapsibleState.None, 'method-item');
    }
}
exports.NewCodeAnalysisItemFactory = NewCodeAnalysisItemFactory;


/***/ }),
/* 151 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Configuration Module
 * Central exports for all configuration functionality
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisConfigurationStorage = void 0;
var analysisConfigurationStorage_1 = __webpack_require__(87);
Object.defineProperty(exports, "AnalysisConfigurationStorage", ({ enumerable: true, get: function () { return analysisConfigurationStorage_1.AnalysisConfigurationStorage; } }));
__exportStar(__webpack_require__(152), exports);


/***/ }),
/* 152 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Storage Models Module
 * Exports for all storage-related models and types
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DEFAULT_CONFIGURATION_FILE = exports.DEFAULT_ANALYSIS_CONFIGURATION = void 0;
var analysisConfiguration_1 = __webpack_require__(88);
Object.defineProperty(exports, "DEFAULT_ANALYSIS_CONFIGURATION", ({ enumerable: true, get: function () { return analysisConfiguration_1.DEFAULT_ANALYSIS_CONFIGURATION; } }));
Object.defineProperty(exports, "DEFAULT_CONFIGURATION_FILE", ({ enumerable: true, get: function () { return analysisConfiguration_1.DEFAULT_CONFIGURATION_FILE; } }));


/***/ }),
/* 153 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Analysis Directory Setting Item
 * Manages the analysis directory mode (XR/LivePanel) configuration
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisDirectorySetting = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const configuration_1 = __webpack_require__(151);
class AnalysisDirectorySetting {
    context;
    currentMode = 'XR'; // Default mode (will be loaded from storage)
    storage;
    constructor(context) {
        this.context = context;
        console.log('NEW_CODE_ANALYSIS: Initializing Analysis Directory setting');
        this.storage = configuration_1.AnalysisConfigurationStorage.getInstance(context);
        this.loadSavedMode();
    }
    /**
     * Get the tree item for this setting
     */
    async getSettingItem() {
        // Always get the current value from storage to ensure it's up to date
        const currentMode = await this.storage.getAnalysisDirectoryMode();
        const modeText = this.getModeDisplayText(currentMode);
        const iconPath = this.getModeIcon(currentMode);
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(modeText, vscode.TreeItemCollapsibleState.None, 'analysis-result', {
            command: 'newCodeAnalysis.selectAnalysisDirectoryMode',
            title: 'Select Analysis Directory Mode',
            arguments: []
        }, iconPath, `Click to select directory analysis mode. Current: ${this.getModeDescription(currentMode)}`, currentMode, 'analysisDirectoryModeSetting');
    }
    /**
     * Get mode display text
     */
    getModeDisplayText(mode) {
        switch (mode) {
            case 'XR':
                return 'Analysis Directory with Immersive VR/AR';
            case 'XRDeep':
                return 'Analysis Directory with Deep Immersive VR/AR';
            case 'LivePanel':
                return 'Analysis Directory with Standard Experience';
            case 'LivePanelDeep':
                return 'Analysis Directory with Deep Standard Experience';
            default:
                return 'Analysis Directory with Unknown Mode';
        }
    }
    /**
     * Get mode icon
     */
    getModeIcon(mode) {
        if (mode.startsWith('LivePanel')) {
            return new vscode.ThemeIcon('folder', new vscode.ThemeColor('charts.green'));
        }
        else {
            return new vscode.ThemeIcon('vr', new vscode.ThemeColor('charts.purple'));
        }
    }
    /**
     * Get mode description
     */
    getModeDescription(mode) {
        switch (mode) {
            case 'XR':
                return 'Immersive VR/AR experience for directory analysis';
            case 'XRDeep':
                return 'Deep immersive VR/AR experience with recursive directory analysis';
            case 'LivePanel':
                return 'Standard panel for quick directory review';
            case 'LivePanelDeep':
                return 'Deep standard panel with recursive directory analysis';
            default:
                return 'Unknown analysis mode';
        }
    }
    /**
     * Show mode selection picker
     */
    async selectMode() {
        const options = [
            {
                label: '📁 LivePanel',
                description: 'Standard Experience',
                mode: 'LivePanel',
                detail: 'Quick directory analysis with standard UI panel'
            },
            {
                label: '📁 LivePanel Deep',
                description: 'Standard Experience (Recursive)',
                mode: 'LivePanelDeep',
                detail: 'Deep recursive directory analysis with standard UI panel'
            },
            {
                label: '🥽 XR',
                description: 'Immersive VR/AR',
                mode: 'XR',
                detail: 'Immersive VR/AR experience for directory analysis'
            },
            {
                label: '🥽 XR Deep',
                description: 'Immersive VR/AR (Recursive)',
                mode: 'XRDeep',
                detail: 'Deep recursive directory analysis with immersive VR/AR experience'
            }
        ];
        const selected = await vscode.window.showQuickPick(options, {
            title: 'Select Directory Analysis Mode',
            placeHolder: 'Choose how to analyze directories...',
            matchOnDescription: true,
            matchOnDetail: true
        });
        if (selected) {
            await this.setMode(selected.mode);
            return selected.mode;
        }
        return undefined;
    }
    /**
     * Get current mode
     */
    async getCurrentMode() {
        return await this.storage.getAnalysisDirectoryMode();
    }
    /**
     * Set mode programmatically
     */
    async setMode(mode) {
        this.currentMode = mode;
        await this.saveModeToSettings();
        console.log(`NEW_CODE_ANALYSIS: Analysis directory mode set to ${mode}`);
    }
    /**
     * Load the saved mode from VS Code settings
     */
    async loadSavedMode() {
        try {
            this.currentMode = await this.storage.getAnalysisDirectoryMode();
            console.log(`NEW_CODE_ANALYSIS: Loaded analysis directory mode: ${this.currentMode}`);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error loading analysis directory mode:', error);
            // Keep default mode
        }
    }
    /**
     * Save the current mode to VS Code settings
     */
    async saveModeToSettings() {
        try {
            await this.storage.setAnalysisDirectoryMode(this.currentMode);
            console.log(`NEW_CODE_ANALYSIS: Saved analysis directory mode: ${this.currentMode}`);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error saving analysis directory mode:', error);
        }
    }
}
exports.AnalysisDirectorySetting = AnalysisDirectorySetting;


/***/ }),
/* 154 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * View Theme Setting Item
 * Manages the analysis view theme (Dark/Light/Auto) configuration
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ViewThemeSetting = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const configuration_1 = __webpack_require__(151);
class ViewThemeSetting {
    context;
    currentTheme = 'Dark';
    storage;
    constructor(context) {
        this.context = context;
        this.storage = configuration_1.AnalysisConfigurationStorage.getInstance(context);
        this.loadSavedTheme();
    }
    /**
     * Get the tree item for this setting
     */
    async getSettingItem() {
        // Always get the current value from storage to ensure it's up to date
        const currentTheme = await this.storage.getViewTheme();
        const themeText = currentTheme === 'Dark'
            ? 'Visual Theme: Dark Mode'
            : 'Visual Theme: Light Mode';
        const iconPath = this.getThemeIcon(currentTheme);
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(themeText, vscode.TreeItemCollapsibleState.None, 'analysis-result', {
            command: 'newCodeAnalysis.toggleViewTheme',
            title: 'Toggle View Theme',
            arguments: []
        }, iconPath, `Click to switch between dark and light visual themes. Current: ${currentTheme} mode for better ${currentTheme === 'Dark' ? 'night coding' : 'daylight visibility'}`, currentTheme, 'viewThemeSetting');
    }
    /**
     * Get appropriate icon for current theme
     */
    getThemeIcon(theme) {
        const themeToUse = theme || this.currentTheme;
        switch (themeToUse) {
            case 'Dark':
                return new vscode.ThemeIcon('color-mode', new vscode.ThemeColor('charts.blue'));
            case 'Light':
                return new vscode.ThemeIcon('color-mode', new vscode.ThemeColor('charts.yellow'));
            default:
                return new vscode.ThemeIcon('color-mode', new vscode.ThemeColor('charts.foreground'));
        }
    }
    /**
     * Toggle between Dark and Light themes
     */
    async toggleTheme() {
        this.currentTheme = this.currentTheme === 'Dark' ? 'Light' : 'Dark';
        console.log(`NEW_CODE_ANALYSIS: View theme switched to ${this.currentTheme}`);
        await this.saveThemeToSettings();
        return this.currentTheme;
    }
    /**
     * Get current theme
     */
    getCurrentTheme() {
        return this.currentTheme;
    }
    /**
     * Set specific theme
     */
    async setTheme(theme) {
        this.currentTheme = theme;
        await this.saveThemeToSettings();
    }
    /**
     * Load saved theme from globalStorage
     */
    async loadSavedTheme() {
        try {
            const savedTheme = await this.storage.getViewTheme();
            this.currentTheme = savedTheme;
            console.log(`NEW_CODE_ANALYSIS: Loaded view theme from storage: ${this.currentTheme}`);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error loading saved theme, using default:', error);
            this.currentTheme = 'Dark'; // Fallback to default
        }
    }
    /**
     * Save theme to globalStorage
     */
    async saveThemeToSettings() {
        try {
            await this.storage.setViewTheme(this.currentTheme);
            console.log(`NEW_CODE_ANALYSIS: Saved view theme to storage: ${this.currentTheme}`);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error saving theme to storage:', error);
        }
    }
}
exports.ViewThemeSetting = ViewThemeSetting;


/***/ }),
/* 155 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Debounce Time Setting Item
 * Manages the debounce time configuration with predefined and custom values
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AutoAnalysisDelaySetting = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const configuration_1 = __webpack_require__(151);
class AutoAnalysisDelaySetting {
    context;
    currentDelayConfig = { type: 'RealTime', autoAnalysisEnabled: true };
    storage;
    // Predefined delay options with their millisecond values
    PREDEFINED_DELAYS = {
        'RealTime': 0,
        '1s': 1000,
        '3s': 3000,
        '5s': 5000,
        '10s': 10000
    };
    constructor(context) {
        this.context = context;
        this.storage = configuration_1.AnalysisConfigurationStorage.getInstance(context);
        this.loadSavedDelay();
    }
    /**
     * Get the tree item for this setting
     */
    async getSettingItem() {
        // Always get the current value from storage to ensure it's up to date
        const currentDelay = await this.storage.getAutoAnalysisDelay();
        const delayDisplayText = this.getDelayDisplayText(currentDelay);
        const delayText = `Debounce Time: ${delayDisplayText}`;
        const iconPath = this.getDelayIcon(currentDelay);
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(delayText, vscode.TreeItemCollapsibleState.None, 'analysis-result', {
            command: 'newCodeAnalysis.selectAutoAnalysisDelay',
            title: 'Select Debounce Time',
            arguments: []
        }, iconPath, `Click to select delay between code changes and analysis trigger. Current: ${delayDisplayText}`, delayDisplayText, 'autoAnalysisDelaySetting');
    }
    /**
     * Get display text for current delay
     */
    getDelayDisplayText(delayConfig) {
        const configToUse = delayConfig || this.currentDelayConfig;
        if (configToUse.type === 'Custom') {
            const ms = configToUse.customMs || 0;
            return `Custom: ${ms}ms`;
        }
        return configToUse.type;
    }
    /**
     * Get appropriate icon for current delay
     */
    getDelayIcon(delayConfig) {
        const configToUse = delayConfig || this.currentDelayConfig;
        if (configToUse.type === 'RealTime') {
            return new vscode.ThemeIcon('zap', new vscode.ThemeColor('charts.green'));
        }
        else if (configToUse.type === 'Custom') {
            return new vscode.ThemeIcon('settings', new vscode.ThemeColor('charts.purple'));
        }
        else {
            return new vscode.ThemeIcon('clock', new vscode.ThemeColor('charts.blue'));
        }
    }
    /**
     * Cycle through predefined delay options
     */
    async cycleDelay() {
        const predefinedOptions = ['RealTime', '1s', '3s', '5s', '10s'];
        const currentIndex = predefinedOptions.indexOf(this.currentDelayConfig.type);
        let nextIndex;
        if (currentIndex === -1 || currentIndex === predefinedOptions.length - 1) {
            nextIndex = 0; // If custom or last option, go to first
        }
        else {
            nextIndex = currentIndex + 1;
        }
        this.currentDelayConfig = {
            type: predefinedOptions[nextIndex],
            customMs: undefined,
            autoAnalysisEnabled: this.currentDelayConfig.autoAnalysisEnabled
        };
        console.log(`NEW_CODE_ANALYSIS: Auto-analysis delay cycled to ${this.currentDelayConfig.type}`);
        await this.saveDelayToSettings();
        return this.currentDelayConfig;
    }
    /**
     * Set custom delay in milliseconds
     */
    async setCustomDelay(milliseconds) {
        // Validate range (0-20000ms)
        const clampedMs = Math.max(0, Math.min(20000, milliseconds));
        this.currentDelayConfig = {
            type: 'Custom',
            customMs: clampedMs,
            autoAnalysisEnabled: this.currentDelayConfig.autoAnalysisEnabled
        };
        console.log(`NEW_CODE_ANALYSIS: Auto-analysis delay set to custom ${clampedMs}ms`);
        await this.saveDelayToSettings();
        return this.currentDelayConfig;
    }
    /**
     * Get current delay configuration
     */
    getCurrentDelayConfig() {
        return this.currentDelayConfig;
    }
    /**
     * Get current delay in milliseconds
     */
    getCurrentDelayMs() {
        if (this.currentDelayConfig.type === 'Custom') {
            return this.currentDelayConfig.customMs || 0;
        }
        return this.PREDEFINED_DELAYS[this.currentDelayConfig.type] || 0;
    }
    /**
     * Set specific predefined delay
     */
    async setDelay(delayType) {
        this.currentDelayConfig = {
            type: delayType,
            customMs: delayType === 'Custom' ? this.currentDelayConfig.customMs : undefined,
            autoAnalysisEnabled: this.currentDelayConfig.autoAnalysisEnabled
        };
        await this.saveDelayToSettings();
    }
    /**
     * Load saved delay from globalStorage
     */
    async loadSavedDelay() {
        try {
            const savedDelay = await this.storage.getAutoAnalysisDelay();
            this.currentDelayConfig = savedDelay;
            console.log(`NEW_CODE_ANALYSIS: Loaded auto-analysis delay from storage:`, savedDelay);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error loading saved delay, using default:', error);
            this.currentDelayConfig = { type: 'RealTime', autoAnalysisEnabled: true }; // Fallback to default
        }
    }
    /**
     * Save delay to globalStorage
     */
    async saveDelayToSettings() {
        try {
            await this.storage.setAutoAnalysisDelay(this.currentDelayConfig);
            console.log(`NEW_CODE_ANALYSIS: Saved auto-analysis delay to storage:`, this.currentDelayConfig);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error saving delay to storage:', error);
        }
    }
}
exports.AutoAnalysisDelaySetting = AutoAnalysisDelaySetting;


/***/ }),
/* 156 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Auto-Analysis Enabled Setting Item
 * Manages the auto-analysis enabled/disabled configuration
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AutoAnalysisEnabledSetting = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const configuration_1 = __webpack_require__(151);
class AutoAnalysisEnabledSetting {
    context;
    currentEnabled = true; // Default enabled
    storage;
    constructor(context) {
        this.context = context;
        console.log('NEW_CODE_ANALYSIS: Initializing Auto-Analysis Enabled setting');
        this.storage = configuration_1.AnalysisConfigurationStorage.getInstance(context);
        this.loadSavedState();
    }
    /**
     * Get the tree item for this setting
     */
    async getSettingItem() {
        // Always get the current value from storage to ensure it's up to date
        const currentState = await this.storage.getAutoAnalysisEnabled();
        const stateText = currentState ? 'Enabled' : 'Disabled';
        const iconPath = this.getStateIcon(currentState);
        const label = `Auto-Analysis: ${stateText}`;
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(label, vscode.TreeItemCollapsibleState.None, 'analysis-result', {
            command: 'newCodeAnalysis.toggleAutoAnalysisEnabled',
            title: 'Toggle Auto-Analysis',
            arguments: []
        }, iconPath, `Click to ${currentState ? 'disable' : 'enable'} auto-analysis (watchers and debounce time). Current: ${stateText}`, stateText, 'autoAnalysisEnabledSetting');
    }
    /**
     * Get appropriate icon for current state
     */
    getStateIcon(enabled) {
        if (enabled) {
            return new vscode.ThemeIcon('play', new vscode.ThemeColor('charts.green'));
        }
        else {
            return new vscode.ThemeIcon('debug-pause', new vscode.ThemeColor('charts.orange'));
        }
    }
    /**
     * Toggle auto-analysis enabled state
     */
    async toggleEnabled() {
        const newState = !this.currentEnabled;
        await this.setEnabled(newState);
        return newState;
    }
    /**
     * Set specific auto-analysis enabled state
     */
    async setEnabled(enabled) {
        this.currentEnabled = enabled;
        await this.saveStateToSettings();
        const message = enabled ?
            'Auto-Analysis enabled - watchers and debounce time are now active' :
            'Auto-Analysis disabled - watchers and debounce time are paused';
        vscode.window.showInformationMessage(message);
    }
    /**
     * Get current enabled state
     */
    getCurrentState() {
        return this.currentEnabled;
    }
    /**
     * Check if auto-analysis is enabled
     */
    async isEnabled() {
        return await this.storage.getAutoAnalysisEnabled();
    }
    /**
     * Load saved state from storage
     */
    async loadSavedState() {
        try {
            const savedState = await this.storage.getAutoAnalysisEnabled();
            this.currentEnabled = savedState;
            console.log(`NEW_CODE_ANALYSIS: Loaded auto-analysis enabled state from storage: ${this.currentEnabled}`);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error loading saved state, using default:', error);
            this.currentEnabled = true; // Fallback to default (enabled)
        }
    }
    /**
     * Save state to storage
     */
    async saveStateToSettings() {
        try {
            await this.storage.setAutoAnalysisEnabled(this.currentEnabled);
            console.log(`NEW_CODE_ANALYSIS: Saved auto-analysis enabled state to storage: ${this.currentEnabled}`);
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error saving state to storage:', error);
        }
    }
}
exports.AutoAnalysisEnabledSetting = AutoAnalysisEnabledSetting;


/***/ }),
/* 157 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Chart Type File Setting Item
 * Manages the chart type configuration for file analysis
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ChartTypeFileSetting = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const configuration_1 = __webpack_require__(151);
const chartRegistry_1 = __webpack_require__(49);
class ChartTypeFileSetting {
    storage;
    chartRegistry;
    constructor(context) {
        this.storage = configuration_1.AnalysisConfigurationStorage.getInstance(context);
        this.chartRegistry = chartRegistry_1.BabiaChartRegistry.getInstance();
    }
    /**
     * Get the tree item for this setting
     */
    async getSettingItem() {
        // Get current values from storage
        const currentMode = await this.storage.getAnalysisMode();
        const currentChartType = await this.storage.getChartTypeFile();
        // Get chart metadata for better display
        const chartMetadata = this.chartRegistry.getChart(currentChartType);
        const chartName = chartMetadata ? chartMetadata.name : currentChartType;
        const label = `Chart Type (File): ${chartName}`;
        const description = `${chartName} for XR mode`;
        // Always use purple color for chart type icons as requested
        const iconPath = new vscode.ThemeIcon('graph', new vscode.ThemeColor('charts.purple'));
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(label, vscode.TreeItemCollapsibleState.None, 'subsection', {
            command: 'newCodeAnalysis.selectChartTypeFile',
            title: 'Select Chart Type for File Analysis',
            arguments: []
        }, iconPath, `Current chart type: ${chartName} for ${currentMode} mode. Click to change chart type.`, description, 'chartTypeFileSetting');
    }
    /**
     * Get current analysis mode
     */
    async getCurrentAnalysisMode() {
        return this.storage.getAnalysisMode();
    }
    /**
     * Get available chart types based on analysis mode
     */
    async getAvailableChartTypes(mode) {
        console.log(`CHART_TYPE_FILE: Getting available charts for mode: ${mode}`);
        // Get all charts from registry
        const allCharts = this.chartRegistry.getAllCharts();
        // Convert to ChartTypeInfo format
        const chartTypeInfos = allCharts.map((chart) => ({
            id: chart.id,
            name: chart.name,
            description: chart.description,
            category: chart.category,
            supportsMode: ['XR', 'LivePanel'] // All charts support both modes
        }));
        console.log(`CHART_TYPE_FILE: Found ${chartTypeInfos.length} available charts`);
        return chartTypeInfos;
    }
    /**
     * Show chart type selection dialog
     */
    async showChartTypeSelection() {
        console.log('CHART_TYPE_FILE: Opening chart type selection dialog');
        try {
            const currentMode = await this.getCurrentAnalysisMode();
            const availableCharts = await this.getAvailableChartTypes(currentMode);
            if (availableCharts.length === 0) {
                console.error('CHART_TYPE_FILE: No chart types found');
                vscode.window.showErrorMessage('No chart templates available');
                return;
            }
            // Create quick pick items for available charts
            const quickPickItems = availableCharts.map(chart => ({
                label: chart.name,
                description: chart.description,
                detail: `Category: ${chart.category} | Supports: ${chart.supportsMode.join(', ')}`,
                chart: chart
            }));
            // Show quick pick
            const selectedItem = await vscode.window.showQuickPick(quickPickItems, {
                placeHolder: 'Select a chart type for file analysis visualization',
                title: `Chart Type Selection for ${currentMode} Analysis`,
                canPickMany: false
            });
            if (selectedItem && selectedItem.chart) {
                const selectedChart = selectedItem.chart;
                // Set the selected chart type
                await this.setChartType(selectedChart.id);
                console.log(`CHART_TYPE_FILE: Chart type selected: ${selectedChart.name} (${selectedChart.id})`);
                vscode.window.showInformationMessage(`Chart type set to: ${selectedChart.name} for ${currentMode} mode`);
            }
            else {
                console.log('CHART_TYPE_FILE: Chart type selection cancelled');
            }
        }
        catch (error) {
            console.error('CHART_TYPE_FILE: Error in chart type selection:', error);
            vscode.window.showErrorMessage(`Failed to select chart type: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Set chart type and intelligently clean dimension mappings
     */
    async setChartType(chartType) {
        console.log(`CHART_TYPE_FILE: Setting chart type to: ${chartType}`);
        // Validate chart type exists
        if (!this.chartRegistry.hasChart(chartType)) {
            throw new Error(`Invalid chart type: ${chartType}`);
        }
        // Get current dimension mappings before changing chart type
        const currentMappings = await this.storage.getDimensionMappingFile();
        console.log(`[DIMENSION_CLEANER] Current mappings before chart change:`, currentMappings);
        // Save the new chart type
        const validChartType = chartType;
        await this.storage.setChartTypeFile(validChartType);
        console.log(`CHART_TYPE_FILE: Chart type saved successfully: ${chartType}`);
        // Clear all dimension mappings when chart type changes
        await this.storage.setDimensionMappingFile({});
        console.log(`CHART_TYPE_FILE: Dimension mappings cleared for new chart type: ${chartType}`);
    }
    /**
     * Get current chart type
     */
    async getCurrentChartType() {
        return this.storage.getChartTypeFile();
    }
    /**
     * Get current chart metadata
     */
    async getCurrentChartMetadata() {
        const currentChartType = await this.getCurrentChartType();
        return this.chartRegistry.getChart(currentChartType);
    }
    /**
     * Save configuration
     */
    async saveConfiguration(configuration) {
        console.log('CHART_TYPE_FILE: Saving configuration:', configuration);
        if (configuration.chartType) {
            await this.setChartType(configuration.chartType);
        }
    }
    /**
     * Load configuration
     */
    async loadConfiguration() {
        const currentChartType = await this.storage.getChartTypeFile();
        const currentMode = await this.storage.getAnalysisMode();
        const chartMetadata = await this.getCurrentChartMetadata();
        return {
            chartType: currentChartType,
            analysisMode: currentMode,
            chartMetadata: chartMetadata
        };
    }
}
exports.ChartTypeFileSetting = ChartTypeFileSetting;


/***/ }),
/* 158 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Chart Type Directory Setting Item
 * Manages the chart type configuration for directory analysis
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ChartTypeDirectorySetting = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const configuration_1 = __webpack_require__(151);
const chartRegistry_1 = __webpack_require__(49);
class ChartTypeDirectorySetting {
    storage;
    chartRegistry;
    constructor(context) {
        this.storage = configuration_1.AnalysisConfigurationStorage.getInstance(context);
        this.chartRegistry = chartRegistry_1.BabiaChartRegistry.getInstance();
    }
    /**
     * Get the tree item for this setting
     */
    async getSettingItem() {
        const currentChartType = await this.getCurrentChartType();
        const currentMode = await this.getCurrentAnalysisMode();
        // Get chart metadata
        const chartMetadata = this.chartRegistry.getChart(currentChartType);
        const chartName = chartMetadata ? chartMetadata.name : 'Unknown Chart';
        // Create label and description based on current mode
        const label = `Chart Type (Directory): ${chartName}`;
        const description = `${currentMode} Mode`;
        // Use purple color for the graph icon as requested
        const iconPath = new vscode.ThemeIcon('graph', new vscode.ThemeColor('charts.purple'));
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(label, vscode.TreeItemCollapsibleState.None, 'subsection', {
            command: 'newCodeAnalysis.selectChartTypeDirectory',
            title: 'Select Chart Type for Directory Analysis',
            arguments: []
        }, iconPath, `Current chart type: ${chartName} for ${currentMode} mode. Click to change chart type.`, description, 'chartTypeDirectorySetting');
    }
    /**
     * Get current chart type for directory analysis
     */
    async getCurrentChartType() {
        return await this.storage.getDirectoryChartType();
    }
    /**
     * Set chart type for current mode
     */
    async setChartType(chartType) {
        await this.storage.setDirectoryChartType(chartType);
        console.log(`CHART_TYPE_DIRECTORY: Set chart type: ${chartType}`);
        // Clear all dimension mappings when chart type changes
        await this.storage.setDimensionMappingDirectory({});
        console.log(`CHART_TYPE_DIRECTORY: Dimension mappings cleared for new chart type: ${chartType}`);
    }
    /**
     * Get current analysis mode (directory mode)
     */
    async getCurrentAnalysisMode() {
        return await this.storage.getAnalysisDirectoryMode();
    }
    /**
     * Get available chart types for the given mode
     */
    async getAvailableChartTypes(mode) {
        const allCharts = this.chartRegistry.getAllCharts();
        return allCharts.map(chart => ({
            id: chart.id,
            name: chart.name,
            description: chart.description,
            category: chart.category,
            supportsMode: ['XR', 'XRDeep', 'LivePanel', 'LivePanelDeep'] // All charts support all modes for directories
        })).filter(chart => chart.supportsMode.includes(mode));
    }
    /**
     * Show chart type selection dialog
     */
    async showChartTypeSelection() {
        console.log('CHART_TYPE_DIRECTORY: Opening chart type selection dialog');
        try {
            const currentMode = await this.getCurrentAnalysisMode();
            const availableCharts = await this.getAvailableChartTypes(currentMode);
            if (availableCharts.length === 0) {
                console.error('CHART_TYPE_DIRECTORY: No chart types found');
                vscode.window.showErrorMessage('No chart templates available');
                return;
            }
            // Create quick pick items for available charts
            const quickPickItems = availableCharts.map(chart => ({
                label: chart.name,
                description: chart.description,
                detail: `Category: ${chart.category} | Supports: ${chart.supportsMode.join(', ')}`,
                chart: chart
            }));
            // Show quick pick
            const selectedItem = await vscode.window.showQuickPick(quickPickItems, {
                placeHolder: 'Select a chart type for directory analysis visualization',
                title: `Chart Type Selection for ${currentMode} Directory Analysis`,
                canPickMany: false
            });
            if (selectedItem && selectedItem.chart) {
                const selectedChart = selectedItem.chart;
                // Set the selected chart type
                await this.setChartType(selectedChart.id);
                console.log(`CHART_TYPE_DIRECTORY: Chart type selected: ${selectedChart.name} (${selectedChart.id})`);
                vscode.window.showInformationMessage(`Chart type set to: ${selectedChart.name} for ${currentMode} directory analysis`);
            }
            else {
                console.log('CHART_TYPE_DIRECTORY: Chart type selection cancelled');
            }
        }
        catch (error) {
            console.error('CHART_TYPE_DIRECTORY: Error in chart type selection:', error);
            vscode.window.showErrorMessage(`Failed to select chart type: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
}
exports.ChartTypeDirectorySetting = ChartTypeDirectorySetting;


/***/ }),
/* 159 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Dimension Mapping File Setting Item
 * Manages the dimension mapping configuration for file analysis with real BabiaXR chart integration
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DimensionMappingFileSetting = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const configuration_1 = __webpack_require__(151);
const templateCharts_1 = __webpack_require__(50);
const dimensionMappingValidator_1 = __webpack_require__(160);
class DimensionMappingFileSetting {
    storage;
    constructor(context) {
        this.storage = configuration_1.AnalysisConfigurationStorage.getInstance(context);
    }
    /**
     * Get the tree item for this setting
     */
    async getSettingItem() {
        const currentMode = await this.storage.getAnalysisMode();
        const chartType = await this.storage.getChartTypeFile();
        const label = `Dimension Mapping (File)`;
        const description = chartType ? `Dimensions for ${chartType} chart` : 'Select chart type first';
        // Always use purple color for chart-related icons as requested
        const iconPath = new vscode.ThemeIcon('symbol-misc', new vscode.ThemeColor('charts.purple'));
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(label, vscode.TreeItemCollapsibleState.Collapsed, 'subsection', undefined, // No command on the main item
        iconPath, description, chartType ? `Configure ${chartType} dimensions` : 'Select chart type first', 'dimensionMappingFileSetting');
    }
    /**
     * Get children items (chart dimensions)
     */
    async getChildren() {
        const chartType = await this.storage.getChartTypeFile();
        console.log(`DIMENSION_MAPPING_FILE: Getting children for chart type: ${chartType}`);
        if (!chartType) {
            console.log('DIMENSION_MAPPING_FILE: No chart type selected, showing warning');
            return [new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Select Chart Type First', vscode.TreeItemCollapsibleState.None, 'subsection', {
                    command: 'newCodeAnalysis.selectChartTypeFile',
                    title: 'Select Chart Type',
                    arguments: []
                }, new vscode.ThemeIcon('warning', new vscode.ThemeColor('charts.yellow')), 'You must select a chart type before configuring dimensions', 'Click to select chart type', 'select-chart-type-first')];
        }
        return await this.getDimensionItemsForChart(chartType);
    }
    async getDimensionItemsForChart(chartType) {
        console.log(`DIMENSION_MAPPING_FILE: Looking for chart template for type: ${chartType}`);
        const chart = templateCharts_1.chartTemplates.find((c) => c.id === chartType);
        if (!chart || !chart.dimensions) {
            console.log(`DIMENSION_MAPPING_FILE: No chart found or no dimensions for chart type: ${chartType}`);
            return [new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('No dimensions available', vscode.TreeItemCollapsibleState.None, 'subsection', undefined, new vscode.ThemeIcon('error', new vscode.ThemeColor('charts.red')), 'This chart type has no configurable dimensions', undefined, 'no-dimensions')];
        }
        console.log(`DIMENSION_MAPPING_FILE: Found chart "${chart.name}" with ${chart.dimensions.length} dimensions:`, chart.dimensions.map(d => d.name).join(', '));
        const dimensionItems = await Promise.all(chart.dimensions.map((dimension) => this.createDimensionItem(dimension)));
        return dimensionItems;
    }
    async createDimensionItem(dimension) {
        // Check if we have a persistent mapping for this dimension
        const mappedValue = await this.getDimensionMapping(dimension.name) || 'Not configured';
        const label = `${dimension.label}: ${mappedValue}`;
        const description = this.getDimensionDescription(dimension);
        console.log(`DIMENSION_MAPPING_FILE: Creating dimension item for "${dimension.name}" - ${dimension.label}, mapped to: ${mappedValue}`);
        // Use warning icon if dimension is required but not mapped
        let iconPath = await this.getDimensionIcon(dimension);
        if (mappedValue === 'Not configured' && dimension.required) {
            iconPath = new vscode.ThemeIcon('warning', new vscode.ThemeColor('charts.yellow'));
        }
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(label, vscode.TreeItemCollapsibleState.None, 'subsection', {
            command: 'newCodeAnalysis.selectDimensionMappingFile',
            title: 'Select Dimension Mapping',
            arguments: [dimension]
        }, iconPath, description, 'Click to configure dimension mapping', `dimension-${dimension.name}`);
    }
    getDimensionDescription(dimension) {
        const typeStr = dimension.dataType === 'numeric' ? 'numerical only' : 'any value';
        const requiredStr = dimension.required ? 'Required' : 'Optional';
        return `(${typeStr}) • ${requiredStr} • ${dimension.description}`;
    }
    async getDimensionIcon(dimension) {
        const chartType = await this.storage.getChartTypeFile();
        if (!chartType) {
            return new vscode.ThemeIcon('circle-outline', new vscode.ThemeColor('charts.foreground'));
        }
        // Get current dimension mappings
        const dimensionMappings = await this.storage.getDimensionMappingFile();
        // Get validation result using the validator
        const validationResult = dimensionMappingValidator_1.DimensionMappingValidator.validateMappingsForChart(chartType, dimensionMappings);
        // Check if this dimension is mapped
        const mapping = dimensionMappings[dimension.name];
        if (!mapping) {
            // Check if it's required
            const isRequired = dimension.required === true;
            return isRequired
                ? new vscode.ThemeIcon('warning', new vscode.ThemeColor('charts.yellow'))
                : new vscode.ThemeIcon('circle-outline', new vscode.ThemeColor('charts.foreground'));
        }
        // Check if this dimension has conflicts
        const hasConflict = validationResult.conflicts.some((conflict) => conflict.conflictingDimensions.includes(dimension.name));
        if (hasConflict) {
            return new vscode.ThemeIcon('warning', new vscode.ThemeColor('charts.yellow'));
        }
        // Dimension is mapped without conflicts
        return new vscode.ThemeIcon('symbol-field', new vscode.ThemeColor('charts.purple'));
    }
    /**
     * Available data fields for file analysis (real analysis output fields)
     */
    getAvailableDataFields() {
        return ['functionName', 'parameters', 'lineCount', 'complexity', 'cyclomaticDensity'];
    }
    /**
     * Get numeric-only fields (fields that contain numeric values)
     */
    getNumericDataFields() {
        return ['parameters', 'lineCount', 'complexity', 'cyclomaticDensity'];
    }
    /**
     * Get fields available for a specific dimension based on its dataType
     */
    getAvailableFieldsForDimension(dimension) {
        console.log(`DIMENSION_MAPPING_FILE: Getting available fields for dimension "${dimension.name}" with dataType: ${dimension.dataType}`);
        const allFields = this.getAvailableDataFields();
        const numericFields = this.getNumericDataFields();
        if (dimension.dataType === 'numeric') {
            console.log(`DIMENSION_MAPPING_FILE: Dimension "${dimension.name}" requires numeric fields only:`, numericFields);
            return numericFields;
        }
        console.log(`DIMENSION_MAPPING_FILE: Dimension "${dimension.name}" accepts any field type:`, allFields);
        return allFields;
    }
    /**
     * Set dimension mapping (persistent storage)
     */
    async setDimensionMapping(dimensionName, dataField) {
        console.log(`DIMENSION_MAPPING_FILE: Setting mapping for "${dimensionName}" to "${dataField}"`);
        // Get current mappings
        const currentMappings = await this.storage.getDimensionMappingFile();
        // Update the mapping
        const updatedMappings = { ...currentMappings, [dimensionName]: dataField };
        // Save to persistent storage
        await this.storage.setDimensionMappingFile(updatedMappings);
        console.log(`DIMENSION_MAPPING_FILE: Successfully saved mapping for "${dimensionName}" to storage`);
    }
    /**
     * Clear dimension mapping (persistent storage)
     */
    async clearDimensionMapping(dimensionName) {
        console.log(`DIMENSION_MAPPING_FILE: Clearing mapping for "${dimensionName}"`);
        // Get current mappings
        const currentMappings = await this.storage.getDimensionMappingFile();
        // Remove the mapping
        const updatedMappings = { ...currentMappings };
        delete updatedMappings[dimensionName];
        // Save to persistent storage
        await this.storage.setDimensionMappingFile(updatedMappings);
        console.log(`DIMENSION_MAPPING_FILE: Successfully cleared mapping for "${dimensionName}" from storage`);
    }
    /**
     * Get current dimension mapping (persistent storage)
     */
    async getDimensionMapping(dimensionName) {
        const currentMappings = await this.storage.getDimensionMappingFile();
        return currentMappings[dimensionName];
    }
    /**
     * Get current dimension mappings for external use
     */
    async getCurrentMappings() {
        return await this.storage.getDimensionMappingFile();
    }
}
exports.DimensionMappingFileSetting = DimensionMappingFileSetting;


/***/ }),
/* 160 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Dimension Mapping Validator
 * Validates dimension mappings for conflicts and completeness
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DimensionMappingValidator = void 0;
const templateCharts_1 = __webpack_require__(50);
class DimensionMappingValidator {
    /**
     * Validate dimension mappings for a specific chart
     */
    static validateMappingsForChart(chartType, currentMappings) {
        console.log(`[DIMENSION_VALIDATOR] Validating mappings for chart: ${chartType}`);
        console.log(`[DIMENSION_VALIDATOR] Current mappings:`, currentMappings);
        const chart = templateCharts_1.chartTemplates.find(c => c.id === chartType);
        if (!chart) {
            return {
                isValid: false,
                hasConflicts: false,
                hasMissingRequired: false,
                conflicts: [],
                missingRequired: [],
                warnings: [],
                errors: [`Chart type "${chartType}" not found`]
            };
        }
        // Check for conflicts (same data field mapped to multiple dimensions)
        const conflicts = this.findDimensionConflicts(currentMappings);
        // Check for missing required dimensions
        const missingRequired = this.findMissingRequiredDimensions(chart, currentMappings);
        // Generate warnings and errors
        const warnings = [];
        const errors = [];
        if (conflicts.length > 0) {
            for (const conflict of conflicts) {
                warnings.push(`Data field "${conflict.dataField}" is mapped to multiple dimensions: ${conflict.conflictingDimensions.join(', ')}`);
            }
        }
        if (missingRequired.length > 0) {
            errors.push(`Missing required dimension mappings: ${missingRequired.join(', ')}`);
        }
        const result = {
            isValid: conflicts.length === 0 && missingRequired.length === 0,
            hasConflicts: conflicts.length > 0,
            hasMissingRequired: missingRequired.length > 0,
            conflicts,
            missingRequired,
            warnings,
            errors
        };
        console.log(`[DIMENSION_VALIDATOR] Validation result:`, result);
        return result;
    }
    /**
     * Find conflicts where the same data field is mapped to multiple dimensions
     */
    static findDimensionConflicts(mappings) {
        const dataFieldUsage = {};
        // Group dimensions by data field
        for (const [dimension, dataField] of Object.entries(mappings)) {
            if (!dataFieldUsage[dataField]) {
                dataFieldUsage[dataField] = [];
            }
            dataFieldUsage[dataField].push(dimension);
        }
        // Find conflicts (data fields used by multiple dimensions)
        const conflicts = [];
        for (const [dataField, dimensions] of Object.entries(dataFieldUsage)) {
            if (dimensions.length > 1) {
                conflicts.push({
                    dataField,
                    conflictingDimensions: dimensions
                });
            }
        }
        return conflicts;
    }
    /**
     * Find missing required dimensions
     */
    static findMissingRequiredDimensions(chart, mappings) {
        const missing = [];
        for (const dimension of chart.dimensions) {
            if (dimension.required && !mappings[dimension.name]) {
                missing.push(dimension.name);
            }
        }
        return missing;
    }
    /**
     * Check if dimension mappings are ready for XR analysis
     */
    static canExecuteXRAnalysis(chartType, currentMappings) {
        const validation = this.validateMappingsForChart(chartType, currentMappings);
        if (validation.hasMissingRequired) {
            return {
                canExecute: false,
                reason: `Missing required dimensions: ${validation.missingRequired.join(', ')}`
            };
        }
        // Allow execution even with conflicts (user can choose to do it)
        return { canExecute: true };
    }
    /**
     * Get dimension status for UI display
     */
    static getDimensionStatus(dimensionName, chartType, currentMappings) {
        const validation = this.validateMappingsForChart(chartType, currentMappings);
        // Check if this dimension has conflicts
        for (const conflict of validation.conflicts) {
            if (conflict.conflictingDimensions.includes(dimensionName)) {
                return 'conflict';
            }
        }
        // Check if this dimension is missing and required
        if (validation.missingRequired.includes(dimensionName)) {
            return 'missing';
        }
        // Check if mapped
        if (currentMappings[dimensionName]) {
            return 'valid';
        }
        return 'optional';
    }
}
exports.DimensionMappingValidator = DimensionMappingValidator;


/***/ }),
/* 161 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Dimension Mapping Directory Setting Item
 * Manages the dimension mapping configuration for directory analysis with real BabiaXR chart integration
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DimensionMappingDirectorySetting = exports.DIRECTORY_DATA_FIELDS = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const configuration_1 = __webpack_require__(151);
const templateCharts_1 = __webpack_require__(50);
/**
 * Available data fields for directory analysis
 */
exports.DIRECTORY_DATA_FIELDS = [
    // File identification fields
    { id: 'fileName', label: 'File Name', description: 'Name of the file' },
    { id: 'filePath', label: 'File Path', description: 'Full path to the file' },
    { id: 'relativePath', label: 'Relative Path', description: 'Relative path from analysis root' },
    { id: 'language', label: 'Language', description: 'Programming language of the file' },
    // File size and basic metrics
    { id: 'fileSizeBytes', label: 'File Size (Bytes)', description: 'Size of the file in bytes' },
    { id: 'totalLines', label: 'Total Lines', description: 'Total number of lines in the file' },
    { id: 'codeLines', label: 'Code Lines', description: 'Number of lines containing code' },
    { id: 'commentLines', label: 'Comment Lines', description: 'Number of lines containing comments' },
    { id: 'blankLines', label: 'Blank Lines', description: 'Number of blank lines in the file' },
    // Code structure metrics
    { id: 'functionCount', label: 'Function Count', description: 'Number of functions in the file' },
    { id: 'classCount', label: 'Class Count', description: 'Number of classes in the file' },
    // Complexity metrics - EXACT names from data.json
    { id: 'cyclomaticComplexityNumber', label: 'Mean Complexity', description: 'Average cyclomatic complexity of the file' },
    { id: 'maxComplexity', label: 'Max Complexity', description: 'Maximum cyclomatic complexity in the file' },
    { id: 'cyclomaticComplexityDensity', label: 'Complexity Density', description: 'Cyclomatic complexity density of the file' },
    // Function parameter metrics
    { id: 'maxFunctionParameters', label: 'Max Function Parameters', description: 'Maximum number of parameters in any function' },
    { id: 'averageFunctionParameters', label: 'Average Function Parameters', description: 'Average number of parameters across functions' }
];
class DimensionMappingDirectorySetting {
    storage;
    constructor(context) {
        this.storage = configuration_1.AnalysisConfigurationStorage.getInstance(context);
    }
    /**
     * Get the tree item for this setting
     */
    async getSettingItem() {
        console.log('DIMENSION_MAPPING_DIRECTORY: getSettingItem() called');
        const currentMode = await this.storage.getAnalysisDirectoryMode();
        const chartType = await this.storage.getDirectoryChartType();
        console.log(`DIMENSION_MAPPING_DIRECTORY: Got chart type: "${chartType}" for setting item`);
        const label = `Dimension Mapping (Directory)`;
        const description = chartType ? `Dimensions for ${chartType} chart` : '⚠️ Select Chart Type (Directory) first';
        const tooltip = chartType ? `Configure ${chartType} dimensions` : 'You must first select a chart type in "Chart Type (Directory)"';
        // Always use purple color for chart-related icons as requested
        const iconPath = new vscode.ThemeIcon('symbol-misc', new vscode.ThemeColor('charts.purple'));
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(label, vscode.TreeItemCollapsibleState.Collapsed, 'subsection', undefined, // No command on the main item
        iconPath, description, tooltip, 'dimensionMappingDirectorySetting');
    }
    /**
     * Get children items (chart dimensions)
     */
    async getChildren() {
        console.log('DIMENSION_MAPPING_DIRECTORY: getChildren() called - START');
        const chartType = await this.storage.getDirectoryChartType();
        console.log(`DIMENSION_MAPPING_DIRECTORY: Getting children for chart type: "${chartType}" (type: ${typeof chartType})`);
        if (!chartType) {
            console.log('DIMENSION_MAPPING_DIRECTORY: No valid chart type selected, showing warning');
            return [new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('⚠️ Select Chart Type (Directory) First', vscode.TreeItemCollapsibleState.None, 'subsection', {
                    command: 'newCodeAnalysis.selectChartTypeDirectory',
                    title: 'Select Chart Type for Directory Analysis',
                    arguments: []
                }, new vscode.ThemeIcon('warning', new vscode.ThemeColor('charts.yellow')), 'You must first select a chart type in "Chart Type (Directory)" before configuring dimension mappings', 'Click to go to Chart Type (Directory) setting', 'select-chart-type-first')];
        }
        return await this.getDimensionItemsForChart(chartType);
    }
    async getDimensionItemsForChart(chartType) {
        console.log(`DIMENSION_MAPPING_DIRECTORY: Looking for chart template for type: ${chartType}`);
        console.log(`DIMENSION_MAPPING_DIRECTORY: Available chart templates:`, templateCharts_1.chartTemplates.map(c => `${c.id} (${c.name})`));
        const chart = templateCharts_1.chartTemplates.find((c) => c.id === chartType);
        if (!chart || !chart.dimensions) {
            console.log(`DIMENSION_MAPPING_DIRECTORY: No chart found or no dimensions for chart type: ${chartType}`);
            console.log(`DIMENSION_MAPPING_DIRECTORY: Chart found:`, chart ? `Yes (${chart.name})` : 'No');
            console.log(`DIMENSION_MAPPING_DIRECTORY: Chart has dimensions:`, chart?.dimensions ? `Yes (${chart.dimensions.length})` : 'No');
            return [new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('No dimensions available', vscode.TreeItemCollapsibleState.None, 'subsection', undefined, new vscode.ThemeIcon('error', new vscode.ThemeColor('charts.red')), 'This chart type has no configurable dimensions', undefined, 'no-dimensions')];
        }
        console.log(`DIMENSION_MAPPING_DIRECTORY: Found chart "${chart.name}" with ${chart.dimensions.length} dimensions:`, chart.dimensions.map(d => d.name));
        const dimensionMappings = await this.getDirectoryDimensionMappings();
        console.log('DIMENSION_MAPPING_DIRECTORY: Current dimension mappings:', dimensionMappings);
        const items = [];
        for (const dimension of chart.dimensions) {
            const currentMapping = dimensionMappings[dimension.name];
            // Find the display label for the mapped field
            let mappedFieldDisplay = 'Not mapped';
            let iconPath = new vscode.ThemeIcon('symbol-field', new vscode.ThemeColor('charts.purple'));
            if (currentMapping) {
                const mappedField = exports.DIRECTORY_DATA_FIELDS.find(field => field.id === currentMapping);
                mappedFieldDisplay = mappedField ? mappedField.label : currentMapping;
            }
            else {
                // Use warning icon if dimension is required but not mapped
                if (dimension.required) {
                    iconPath = new vscode.ThemeIcon('warning', new vscode.ThemeColor('charts.yellow'));
                }
            }
            console.log(`DIMENSION_MAPPING_DIRECTORY: Dimension "${dimension.name}" mapped to "${currentMapping}" (display: "${mappedFieldDisplay}")`);
            const dimensionItem = new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(`${dimension.label}: ${mappedFieldDisplay}`, vscode.TreeItemCollapsibleState.None, 'subsection', {
                command: 'newCodeAnalysis.selectDimensionMappingDirectory',
                title: `Map ${dimension.label} dimension`,
                arguments: [dimension.name, dimension.label]
            }, iconPath, `${dimension.description}. Currently mapped to: ${mappedFieldDisplay}. Click to change mapping.`, currentMapping ? `→ ${mappedFieldDisplay}` : 'Click to map', `dimension-mapping-directory-${dimension.name}`);
            items.push(dimensionItem);
        }
        return items;
    }
    /**
     * Get directory dimension mappings from storage
     */
    async getDirectoryDimensionMappings() {
        try {
            const mappings = await this.storage.getDimensionMappingDirectory();
            console.log('DIMENSION_MAPPING_DIRECTORY: Retrieved mappings:', mappings);
            return mappings || {};
        }
        catch (error) {
            console.error('DIMENSION_MAPPING_DIRECTORY: Error getting mappings:', error);
            return {};
        }
    }
    /**
     * Set directory dimension mappings in storage
     */
    async setDirectoryDimensionMappings(mappings) {
        try {
            await this.storage.setDimensionMappingDirectory(mappings);
            console.log('DIMENSION_MAPPING_DIRECTORY: Set mappings:', mappings);
        }
        catch (error) {
            console.error('DIMENSION_MAPPING_DIRECTORY: Error setting mappings:', error);
            throw error;
        }
    }
    /**
     * Show dimension mapping selection dialog
     */
    async showDimensionMappingSelection(dimensionName, dimensionLabel) {
        console.log(`DIMENSION_MAPPING_DIRECTORY: Opening mapping selection for dimension: ${dimensionName} (${dimensionLabel})`);
        try {
            const chartType = await this.storage.getDirectoryChartType();
            const chart = templateCharts_1.chartTemplates.find((c) => c.id === chartType);
            if (!chart) {
                vscode.window.showErrorMessage('Chart type not found. Please select a valid chart type first.');
                return;
            }
            const dimension = chart.dimensions.find(d => d.name === dimensionName);
            if (!dimension) {
                vscode.window.showErrorMessage(`Dimension ${dimensionName} not found in chart ${chartType}.`);
                return;
            }
            const currentMappings = await this.getDirectoryDimensionMappings();
            const currentMapping = currentMappings[dimensionName];
            console.log(`DIMENSION_MAPPING_DIRECTORY: Current mapping for ${dimensionName}:`, currentMapping);
            // Filter available fields based on dimension data type
            let availableFields = exports.DIRECTORY_DATA_FIELDS;
            if (dimension.dataType === 'numeric') {
                // Only numeric fields (exclude fileName, filePath, relativePath, and language - all string fields)
                availableFields = exports.DIRECTORY_DATA_FIELDS.filter(field => field.id !== 'fileName' &&
                    field.id !== 'filePath' &&
                    field.id !== 'relativePath' &&
                    field.id !== 'language');
                console.log(`DIMENSION_MAPPING_DIRECTORY: Filtered to ${availableFields.length} numeric fields for dimension ${dimensionName} (excluded string fields: fileName, filePath, relativePath, language)`);
            }
            else {
                console.log(`DIMENSION_MAPPING_DIRECTORY: Using all ${availableFields.length} fields for dimension ${dimensionName} (accepts any data type)`);
            }
            // Create quick pick items from available directory data fields
            const quickPickItems = availableFields.map(field => ({
                label: field.label,
                description: field.description,
                detail: currentMapping === field.id ? '✓ Currently selected' : '',
                fieldId: field.id
            }));
            console.log(`DIMENSION_MAPPING_DIRECTORY: Created ${quickPickItems.length} quick pick items`);
            // Add "Clear mapping" option
            quickPickItems.unshift({
                label: '$(clear-all) Clear mapping',
                description: 'Remove the mapping for this dimension',
                detail: currentMapping ? 'Clear current selection' : 'No mapping to clear',
                fieldId: ''
            });
            console.log(`DIMENSION_MAPPING_DIRECTORY: Showing quick pick with ${quickPickItems.length} items`);
            // Show quick pick
            const selectedItem = await vscode.window.showQuickPick(quickPickItems, {
                placeHolder: `Select data field to map to ${dimensionLabel} dimension`,
                title: `Dimension Mapping: ${dimensionLabel}`,
                canPickMany: false
            });
            if (selectedItem !== undefined) {
                const newMappings = { ...currentMappings };
                if (selectedItem.fieldId === '') {
                    // Clear mapping
                    delete newMappings[dimensionName];
                    console.log(`DIMENSION_MAPPING_DIRECTORY: Cleared mapping for dimension: ${dimensionName}`);
                }
                else {
                    // Check for conflicts with other dimensions
                    const conflictDimension = Object.keys(newMappings).find(key => key !== dimensionName && newMappings[key] === selectedItem.fieldId);
                    if (conflictDimension) {
                        const shouldProceed = await vscode.window.showWarningMessage(`The field "${selectedItem.label}" is already mapped to another dimension. Do you want to proceed? This will clear the previous mapping.`, 'Proceed', 'Cancel');
                        if (shouldProceed !== 'Proceed') {
                            console.log('DIMENSION_MAPPING_DIRECTORY: Mapping cancelled due to conflict');
                            return;
                        }
                        // Clear the conflicting mapping
                        delete newMappings[conflictDimension];
                    }
                    // Set new mapping
                    newMappings[dimensionName] = selectedItem.fieldId;
                    console.log(`DIMENSION_MAPPING_DIRECTORY: Mapped dimension "${dimensionName}" to field "${selectedItem.fieldId}"`);
                }
                // Save mappings
                await this.setDirectoryDimensionMappings(newMappings);
                const mappingText = selectedItem.fieldId ?
                    `mapped to "${selectedItem.label}"` :
                    'mapping cleared';
                vscode.window.showInformationMessage(`Dimension "${dimensionLabel}" ${mappingText}`);
            }
            else {
                console.log('DIMENSION_MAPPING_DIRECTORY: Dimension mapping selection cancelled');
            }
        }
        catch (error) {
            console.error('DIMENSION_MAPPING_DIRECTORY: Error in dimension mapping selection:', error);
            vscode.window.showErrorMessage(`Failed to configure dimension mapping: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    /**
     * Validate current mappings
     */
    async validateMappings() {
        const chartType = await this.storage.getDirectoryChartType();
        if (!chartType) {
            return false;
        }
        const chart = templateCharts_1.chartTemplates.find((c) => c.id === chartType);
        if (!chart) {
            return false;
        }
        const mappings = await this.getDirectoryDimensionMappings();
        // Check if all required dimensions are mapped
        const requiredDimensions = chart.dimensions.filter(d => d.required);
        const missingMappings = requiredDimensions.filter(d => !mappings[d.name]);
        if (missingMappings.length > 0) {
            console.log(`DIMENSION_MAPPING_DIRECTORY: Missing required mappings: ${missingMappings.map(d => d.name).join(', ')}`);
            return false;
        }
        return true;
    }
    /**
     * Get current directory dimension mappings for external use
     */
    async getCurrentMappings() {
        return await this.getDirectoryDimensionMappings();
    }
}
exports.DimensionMappingDirectorySetting = DimensionMappingDirectorySetting;


/***/ }),
/* 162 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Files by Language Sorting Setting Module
 * Exports for Files by Language sorting configuration
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FilesByLanguageSortingSetting = void 0;
var filesByLanguageSorting_1 = __webpack_require__(163);
Object.defineProperty(exports, "FilesByLanguageSortingSetting", ({ enumerable: true, get: function () { return filesByLanguageSorting_1.FilesByLanguageSortingSetting; } }));


/***/ }),
/* 163 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Files by Language Sorting Setting
 * Configuration for sorting Files by Language section
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FilesByLanguageSortingSetting = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const configuration_1 = __webpack_require__(151);
class FilesByLanguageSortingSetting {
    storage;
    constructor(context) {
        this.storage = configuration_1.AnalysisConfigurationStorage.getInstance(context);
    }
    /**
     * Get the tree item for this setting
     */
    async getSettingItem() {
        const currentConfig = await this.storage.getFilesByLanguageSorting();
        const primarySort = `${currentConfig.languageGroupSortBy} ${currentConfig.languageGroupSortDirection}`;
        const secondarySort = currentConfig.languageGroupSecondarySortBy ?
            `, ${currentConfig.languageGroupSecondarySortBy} ${currentConfig.languageGroupSecondarySortDirection}` : '';
        const filesSort = `Files: ${currentConfig.filesSortBy} ${currentConfig.filesSortDirection}`;
        const description = `Groups: ${primarySort}${secondarySort} | ${filesSort}`;
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Files by Language Sorting', vscode.TreeItemCollapsibleState.Collapsed, 'subsection', undefined, new vscode.ThemeIcon('list-filter'), // Changed to list-filter icon as requested
        description, 'Configure sorting for Files by Language section', 'filesByLanguageSortingSetting');
    }
    /**
     * Get children items (sorting options)
     */
    async getChildren() {
        console.log('FILES_BY_LANGUAGE_SORTING: Getting children');
        const children = [];
        const currentConfig = await this.storage.getFilesByLanguageSorting();
        // Language Groups Primary Sorting
        children.push(new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(`Language Groups Primary: ${currentConfig.languageGroupSortBy} (${currentConfig.languageGroupSortDirection})`, vscode.TreeItemCollapsibleState.None, 'subsection', {
            command: 'newCodeAnalysis.selectLanguageGroupPrimarySorting',
            title: 'Select Language Group Primary Sorting',
            arguments: []
        }, new vscode.ThemeIcon('menu'), // Changed to menu icon as requested
        'Primary sorting for language groups', 'Click to change primary sorting for language groups', 'languageGroupPrimarySorting'));
        // Language Groups Secondary Sorting
        const secondaryLabel = currentConfig.languageGroupSecondarySortBy ?
            `${currentConfig.languageGroupSecondarySortBy} (${currentConfig.languageGroupSecondarySortDirection})` :
            'None';
        children.push(new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(`Language Groups Secondary: ${secondaryLabel}`, vscode.TreeItemCollapsibleState.None, 'subsection', {
            command: 'newCodeAnalysis.selectLanguageGroupSecondarySorting',
            title: 'Select Language Group Secondary Sorting',
            arguments: []
        }, new vscode.ThemeIcon('menu'), // Changed to menu icon as requested
        'Secondary sorting for language groups', 'Click to change secondary sorting for language groups', 'languageGroupSecondarySorting'));
        // Files Sorting
        children.push(new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(`Files: ${currentConfig.filesSortBy} (${currentConfig.filesSortDirection})`, vscode.TreeItemCollapsibleState.None, 'subsection', {
            command: 'newCodeAnalysis.selectFilesSorting',
            title: 'Select Files Sorting',
            arguments: []
        }, new vscode.ThemeIcon('file-text'), 'Sorting for files within language groups', 'Click to change file sorting within language groups', 'filesSorting'));
        console.log(`FILES_BY_LANGUAGE_SORTING: Generated ${children.length} sorting options`);
        return children;
    }
    /**
     * Update language group primary sorting
     */
    async updateLanguageGroupPrimarySorting(sortBy, direction) {
        const currentConfig = await this.storage.getFilesByLanguageSorting();
        const updatedConfig = {
            ...currentConfig,
            languageGroupSortBy: sortBy,
            languageGroupSortDirection: direction
        };
        await this.storage.setFilesByLanguageSorting(updatedConfig);
    }
    /**
     * Update language group secondary sorting
     */
    async updateLanguageGroupSecondarySorting(sortBy, direction) {
        const currentConfig = await this.storage.getFilesByLanguageSorting();
        const updatedConfig = {
            ...currentConfig,
            languageGroupSecondarySortBy: sortBy,
            languageGroupSecondarySortDirection: direction
        };
        await this.storage.setFilesByLanguageSorting(updatedConfig);
    }
    /**
     * Update files sorting
     */
    async updateFilesSorting(sortBy, direction) {
        const currentConfig = await this.storage.getFilesByLanguageSorting();
        const updatedConfig = {
            ...currentConfig,
            filesSortBy: sortBy,
            filesSortDirection: direction
        };
        await this.storage.setFilesByLanguageSorting(updatedConfig);
    }
}
exports.FilesByLanguageSortingSetting = FilesByLanguageSortingSetting;


/***/ }),
/* 164 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Profile Configuration Module
 * Exports for profile configuration functionality
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ProfileConfigurationSetting = void 0;
var profileConfiguration_1 = __webpack_require__(165);
Object.defineProperty(exports, "ProfileConfigurationSetting", ({ enumerable: true, get: function () { return profileConfiguration_1.ProfileConfigurationSetting; } }));


/***/ }),
/* 165 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Profile Configuration Setting
 * Manages saving, loading, and resetting analysis configuration profiles
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ProfileConfigurationSetting = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
class ProfileConfigurationSetting {
    context;
    storage;
    constructor(context, storage) {
        this.context = context;
        this.storage = storage;
        console.log('PROFILE_CONFIGURATION: Initializing Profile Configuration setting');
    }
    /**
     * Get the setting item (collapsible)
     */
    async getSettingItem() {
        console.log('PROFILE_CONFIGURATION: Creating profile configuration setting item');
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Profile Configuration', vscode.TreeItemCollapsibleState.Collapsed, 'subsection', undefined, new vscode.ThemeIcon('account', new vscode.ThemeColor('charts.blue')), 'Manage analysis configuration profiles', 'Save, load, reset, and delete configuration profiles', 'profileConfigurationSetting');
    }
    /**
     * Get children items for Profile Configuration
     */
    async getChildren() {
        console.log('PROFILE_CONFIGURATION: Getting Profile Configuration children');
        const children = [];
        // Reset to Defaults
        children.push(new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Reset to Defaults', vscode.TreeItemCollapsibleState.None, 'subsection', {
            command: 'newCodeAnalysis.resetToDefaults',
            title: 'Reset to Default Values',
            arguments: []
        }, new vscode.ThemeIcon('debug-restart', new vscode.ThemeColor('charts.red')), 'Reset all analysis settings to default values', 'Restore factory defaults', 'resetToDefaults'));
        // Save Profile
        children.push(new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Save Profile', vscode.TreeItemCollapsibleState.None, 'subsection', {
            command: 'newCodeAnalysis.saveProfile',
            title: 'Save Current Configuration as Profile',
            arguments: []
        }, new vscode.ThemeIcon('save', new vscode.ThemeColor('charts.green')), 'Save current configuration as a named profile', 'Create new profile', 'saveProfile'));
        // Load Profile
        children.push(new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Load Profile', vscode.TreeItemCollapsibleState.None, 'subsection', {
            command: 'newCodeAnalysis.loadProfile',
            title: 'Load Configuration Profile',
            arguments: []
        }, new vscode.ThemeIcon('folder-opened', new vscode.ThemeColor('charts.blue')), 'Load a previously saved configuration profile', 'Restore saved profile', 'loadProfile'));
        // Delete Profile
        children.push(new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Delete Profile', vscode.TreeItemCollapsibleState.None, 'subsection', {
            command: 'newCodeAnalysis.deleteProfile',
            title: 'Delete Configuration Profile',
            arguments: []
        }, new vscode.ThemeIcon('trash', new vscode.ThemeColor('charts.red')), 'Delete a previously saved configuration profile', 'Remove saved profile', 'deleteProfile'));
        console.log(`PROFILE_CONFIGURATION: Created ${children.length} profile configuration items`);
        return children;
    }
    /**
     * Get default configuration values
     */
    getDefaultConfiguration() {
        return {
            analysisFileMode: 'XR',
            analysisDirectoryMode: 'XR',
            viewTheme: 'Dark', // Default theme
            chartTypeFile: 'boats',
            chartTypeDirectory: 'boats',
            dimensionMappingFile: {
                area: 'parameters',
                height: 'lineCount',
                color: 'complexity'
            },
            dimensionMappingDirectory: {
                area: 'functionCount',
                height: 'totalLines',
                color: 'cyclomaticComplexityNumber'
            },
            autoAnalysisDelay: {
                type: '3s',
                customMs: undefined
            },
            autoAnalysisEnabled: true, // Default to enabled
            filesByLanguageSorting: {
                languageGroupSortBy: 'alphabetical',
                languageGroupSortDirection: 'ascending',
                filesSortBy: 'alphabetical',
                filesSortDirection: 'ascending'
            }
        };
    }
    /**
     * Detect user's VS Code theme
     */
    detectVSCodeTheme() {
        const colorTheme = vscode.window.activeColorTheme;
        if (colorTheme.kind === vscode.ColorThemeKind.Light) {
            return 'light';
        }
        else if (colorTheme.kind === vscode.ColorThemeKind.Dark || colorTheme.kind === vscode.ColorThemeKind.HighContrast) {
            return 'dark';
        }
        return 'dark'; // Default fallback
    }
    /**
     * Reset configuration to defaults
     */
    async resetToDefaults() {
        console.log('PROFILE_CONFIGURATION: Resetting to default values');
        try {
            const defaults = this.getDefaultConfiguration();
            // Always use Dark theme as default
            defaults.viewTheme = 'Dark';
            // Apply all default values
            await this.storage.setAnalysisFileMode(defaults.analysisFileMode);
            await this.storage.setAnalysisDirectoryMode(defaults.analysisDirectoryMode);
            await this.storage.setViewTheme(defaults.viewTheme);
            await this.storage.setChartTypeFile(defaults.chartTypeFile);
            await this.storage.setDirectoryChartType(defaults.chartTypeDirectory);
            await this.storage.setDimensionMappingFile(defaults.dimensionMappingFile);
            await this.storage.setDimensionMappingDirectory(defaults.dimensionMappingDirectory);
            await this.storage.setAutoAnalysisDelay(defaults.autoAnalysisDelay);
            await this.storage.setAutoAnalysisEnabled(defaults.autoAnalysisEnabled);
            await this.storage.setFilesByLanguageSorting(defaults.filesByLanguageSorting);
            console.log('PROFILE_CONFIGURATION: Successfully reset to defaults:', defaults);
            vscode.window.showInformationMessage('Analysis settings reset to default values');
        }
        catch (error) {
            console.error('PROFILE_CONFIGURATION: Error resetting to defaults:', error);
            vscode.window.showErrorMessage(`Failed to reset to defaults: ${error}`);
            throw error;
        }
    }
}
exports.ProfileConfigurationSetting = ProfileConfigurationSetting;


/***/ }),
/* 166 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.registerPythonEnvCommands = registerPythonEnvCommands;
exports.getPythonEnvCommands = getPythonEnvCommands;
exports.deactivatePythonEnvCommands = deactivatePythonEnvCommands;
const vscode = __importStar(__webpack_require__(1));
const pythonEnvCommands_1 = __webpack_require__(167);
/**
 * Python Environment Commands Wrapper
 * Re-exports python environment commands for centralized command registration
 */
let pythonEnvCommands;
/**
 * Registers all Python environment related commands
 */
function registerPythonEnvCommands(context) {
    console.log('PYTHON_ENV: Registering Python environment commands...');
    try {
        // Initialize commands
        pythonEnvCommands = new pythonEnvCommands_1.PythonEnvCommands(context);
        pythonEnvCommands.register(context);
        // Initialize environment on startup
        pythonEnvCommands.initializeOnStartup()
            .then(() => {
            console.log('PYTHON_ENV: Commands registration and initialization completed successfully');
        })
            .catch((error) => {
            console.error('PYTHON_ENV: Initialization failed during command registration:', error);
        });
        console.log('PYTHON_ENV: Python environment commands registered successfully');
    }
    catch (error) {
        console.error('PYTHON_ENV: Failed to register Python environment commands:', error);
        vscode.window.showErrorMessage(`Failed to initialize Python environment commands: ${error}`);
    }
}
/**
 * Get the PythonEnvCommands instance for external access
 */
function getPythonEnvCommands() {
    return pythonEnvCommands;
}
/**
 * Clean up resources when extension is deactivated
 */
function deactivatePythonEnvCommands() {
    console.log('PYTHON_ENV: Deactivating Python environment commands');
    pythonEnvCommands = undefined;
}


/***/ }),
/* 167 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.PythonEnvCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const venvManager_1 = __webpack_require__(98);
/**
 * Python environment command registration and handlers
 */
class PythonEnvCommands {
    venvManager;
    constructor(context) {
        this.venvManager = new venvManager_1.VenvManager(context);
        console.log('PYTHON_ENV: Commands module initialized');
    }
    /**
     * Register all Python environment commands
     */
    register(context) {
        console.log('PYTHON_ENV: Registering commands...');
        const commands = [
            vscode.commands.registerCommand('codeXR.pythonEnv.create', () => this.createEnvironment()),
            vscode.commands.registerCommand('codeXR.pythonEnv.delete', () => this.deleteEnvironment()),
            vscode.commands.registerCommand('codeXR.pythonEnv.status', () => this.showStatus()),
            vscode.commands.registerCommand('codeXR.pythonEnv.installPackage', () => this.installPackage()),
            vscode.commands.registerCommand('codeXR.pythonEnv.reinitialize', () => this.reinitializeEnvironment()),
            vscode.commands.registerCommand('codeXR.pythonEnv.verifyLizard', () => this.verifyLizard())
        ];
        commands.forEach(command => context.subscriptions.push(command));
        console.log(`PYTHON_ENV: Registered ${commands.length} commands`);
    }
    /**
     * Initialize the environment (called on extension startup)
     */
    async initializeOnStartup() {
        console.log('PYTHON_ENV: Initializing environment on startup...');
        try {
            await this.venvManager.initializeEnvironment();
            console.log('PYTHON_ENV: Startup initialization completed successfully');
        }
        catch (error) {
            console.error('PYTHON_ENV: Startup initialization failed:', error);
            // Don't show error to user on startup - just log it
        }
    }
    /**
     * Get the VenvManager instance for external use
     */
    getVenvManager() {
        return this.venvManager;
    }
    /**
     * Command handler: Create new environment
     */
    async createEnvironment() {
        console.log('PYTHON_ENV: Create environment command triggered');
        try {
            const status = this.venvManager.getEnvironmentStatus();
            if (status.exists && status.isValid) {
                const result = await vscode.window.showWarningMessage('A Python virtual environment already exists. Do you want to recreate it?', 'Recreate Environment', 'Cancel');
                if (result !== 'Recreate Environment') {
                    return;
                }
                // Delete existing environment first
                await this.venvManager.deleteEnvironment();
            }
            await this.venvManager.createEnvironment();
        }
        catch (error) {
            console.error('PYTHON_ENV: Create environment command failed:', error);
            vscode.window.showErrorMessage(`Failed to create environment: ${error}`);
        }
    }
    /**
     * Command handler: Delete environment
     */
    async deleteEnvironment() {
        console.log('PYTHON_ENV: Delete environment command triggered');
        try {
            const status = this.venvManager.getEnvironmentStatus();
            if (!status.exists) {
                vscode.window.showInformationMessage('No Python virtual environment exists to delete.');
                return;
            }
            await this.venvManager.deleteEnvironment();
        }
        catch (error) {
            console.error('PYTHON_ENV: Delete environment command failed:', error);
            vscode.window.showErrorMessage(`Failed to delete environment: ${error}`);
        }
    }
    /**
     * Command handler: Show environment status
     */
    async showStatus() {
        console.log('PYTHON_ENV: Status command triggered');
        try {
            const status = this.venvManager.getEnvironmentStatus();
            let message = 'Python Virtual Environment Status:\n\n';
            if (!status.exists) {
                message += '❌ No environment exists\n';
                message += 'Use "Create Python Environment" command to set up a new environment.';
            }
            else {
                message += status.isValid ? '✅ Environment is valid and ready\n\n' : '❌ Environment exists but is invalid\n\n';
                if (status.metadata) {
                    message += `📍 Location: ${status.metadata.venvPath}\n`;
                    message += `🐍 Python Version: ${status.metadata.pythonVersion || 'Unknown'}\n`;
                    message += `📅 Created: ${new Date(status.metadata.createdAt).toLocaleString()}\n`;
                    message += `🔄 Last Validated: ${new Date(status.metadata.lastValidated).toLocaleString()}\n`;
                    message += `📦 Installed Packages: ${status.metadata.dependencies.length}\n`;
                }
                if (status.stats.venvSize !== undefined) {
                    message += `💾 Environment Size: ~${status.stats.venvSize} MB\n`;
                }
                // Check lizard availability
                const lizardCommand = this.venvManager.getLizardCommand();
                if (lizardCommand) {
                    message += `🦎 Lizard: Available\n`;
                }
                else {
                    message += `🦎 Lizard: Not available\n`;
                }
                if (!status.isValid) {
                    message += '\n⚠️ Environment is invalid. Consider recreating it.';
                }
            }
            // Show in information message with option to open output channel for details
            const result = await vscode.window.showInformationMessage(message, { modal: true }, 'Show Details');
            if (result === 'Show Details') {
                this.showDetailedStatus(status);
            }
        }
        catch (error) {
            console.error('PYTHON_ENV: Status command failed:', error);
            vscode.window.showErrorMessage(`Failed to get environment status: ${error}`);
        }
    }
    /**
     * Command handler: Install package
     */
    async installPackage() {
        console.log('PYTHON_ENV: Install package command triggered');
        try {
            const status = this.venvManager.getEnvironmentStatus();
            if (!status.exists || !status.isValid) {
                vscode.window.showErrorMessage('No valid Python environment exists. Create one first.');
                return;
            }
            const packageName = await vscode.window.showInputBox({
                prompt: 'Enter the name of the Python package to install',
                placeHolder: 'e.g., numpy, pandas, matplotlib',
                validateInput: (value) => {
                    if (!value || value.trim().length === 0) {
                        return 'Package name cannot be empty';
                    }
                    // Basic validation for package name
                    if (!/^[a-zA-Z0-9\-_.]+$/.test(value.trim())) {
                        return 'Invalid package name. Use letters, numbers, hyphens, underscores, and dots only.';
                    }
                    return null;
                }
            });
            if (!packageName) {
                return;
            }
            await this.venvManager.installPackage(packageName.trim());
        }
        catch (error) {
            console.error('PYTHON_ENV: Install package command failed:', error);
            vscode.window.showErrorMessage(`Failed to install package: ${error}`);
        }
    }
    /**
     * Command handler: Reinitialize environment
     */
    async reinitializeEnvironment() {
        console.log('PYTHON_ENV: Reinitialize environment command triggered');
        try {
            const result = await vscode.window.showInformationMessage('This will validate and potentially recreate the Python environment. Continue?', 'Reinitialize', 'Cancel');
            if (result !== 'Reinitialize') {
                return;
            }
            await this.venvManager.initializeEnvironment();
            vscode.window.showInformationMessage('Python environment reinitialized successfully!');
        }
        catch (error) {
            console.error('PYTHON_ENV: Reinitialize command failed:', error);
            vscode.window.showErrorMessage(`Failed to reinitialize environment: ${error}`);
        }
    }
    /**
     * Command handler: Verify lizard installation
     */
    async verifyLizard() {
        console.log('PYTHON_ENV: Verify lizard command triggered');
        try {
            const status = this.venvManager.getEnvironmentStatus();
            if (!status.exists || !status.isValid) {
                vscode.window.showErrorMessage('No valid Python environment exists. Create one first.');
                return;
            }
            const isLizardWorking = await this.venvManager.verifyLizardInstallation();
            if (isLizardWorking) {
                const lizardCommand = this.venvManager.getLizardCommand();
                vscode.window.showInformationMessage(`Lizard is installed and working correctly!\n\nCommand: ${lizardCommand}`);
            }
            else {
                const result = await vscode.window.showWarningMessage('Lizard is not working correctly. Would you like to reinstall it?', 'Reinstall Lizard', 'Cancel');
                if (result === 'Reinstall Lizard') {
                    await this.venvManager.installPackage('lizard');
                }
            }
        }
        catch (error) {
            console.error('PYTHON_ENV: Verify lizard command failed:', error);
            vscode.window.showErrorMessage(`Failed to verify lizard: ${error}`);
        }
    }
    /**
     * Show detailed status in output channel
     */
    showDetailedStatus(status) {
        const outputChannel = vscode.window.createOutputChannel('Python Environment Details');
        outputChannel.clear();
        outputChannel.appendLine('=== Python Virtual Environment Details ===\n');
        outputChannel.appendLine(`Environment Exists: ${status.exists}`);
        outputChannel.appendLine(`Environment Valid: ${status.isValid}`);
        outputChannel.appendLine(`State File Exists: ${status.stats.stateExists}`);
        outputChannel.appendLine(`Environment Directory Exists: ${status.stats.envExists}`);
        if (status.stats.venvSize !== undefined) {
            outputChannel.appendLine(`Environment Size: ~${status.stats.venvSize} MB`);
        }
        if (status.metadata) {
            outputChannel.appendLine('\n=== Environment Metadata ===');
            outputChannel.appendLine(`Path: ${status.metadata.venvPath}`);
            outputChannel.appendLine(`Python Version: ${status.metadata.pythonVersion || 'Unknown'}`);
            outputChannel.appendLine(`Created At: ${status.metadata.createdAt}`);
            outputChannel.appendLine(`Last Validated: ${status.metadata.lastValidated}`);
            outputChannel.appendLine(`Is Active: ${status.metadata.isActive}`);
            outputChannel.appendLine('\n=== Installed Packages ===');
            if (status.metadata.dependencies.length > 0) {
                status.metadata.dependencies.forEach((dep) => {
                    outputChannel.appendLine(`  ${dep}`);
                });
            }
            else {
                outputChannel.appendLine('  No packages recorded');
            }
        }
        const pythonPath = this.venvManager.getPythonExecutablePath();
        if (pythonPath) {
            outputChannel.appendLine(`\n=== Python Executable ===`);
            outputChannel.appendLine(`Path: ${pythonPath}`);
        }
        outputChannel.show();
    }
}
exports.PythonEnvCommands = PythonEnvCommands;


/***/ }),
/* 168 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.registerVisualizationSettingsCommands = registerVisualizationSettingsCommands;
const visualizationSettingsCommands_1 = __webpack_require__(169);
/**
 * Visualization Settings Commands Wrapper
 * Re-exports visualization settings commands for centralized command registration
 */
/**
 * Registers all visualization settings related commands
 */
function registerVisualizationSettingsCommands(context) {
    console.log('VISUALIZATION-SETTINGS: Registering visualization settings commands...');
    visualizationSettingsCommands_1.VisualizationSettingsCommands.registerCommands(context);
    console.log('VISUALIZATION-SETTINGS: Visualization settings commands registration complete');
}


/***/ }),
/* 169 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizationSettingsCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
const handleSettingsInteraction_1 = __webpack_require__(60);
const settingsAccessors_1 = __webpack_require__(62);
/**
 * Visualization Settings Commands Class
 * Defines what each visualization settings command does
 */
class VisualizationSettingsCommands {
    context;
    interactionHandler;
    constructor(context) {
        this.context = context;
        console.log('VISUALIZATION-SETTINGS: Initializing visualization settings commands...');
        // Initialize settings accessors for global use
        (0, settingsAccessors_1.initializeSettingsAccessors)(context);
        // Initialize the interaction handler
        this.interactionHandler = new handleSettingsInteraction_1.VisualizationSettingsInteractionHandler(context);
    }
    /**
     * Register all visualization settings commands
     */
    static registerCommands(context) {
        console.log('VISUALIZATION-SETTINGS: Registering visualization settings commands...');
        const commandsInstance = new VisualizationSettingsCommands(context);
        commandsInstance.registerAllCommands();
        console.log('VISUALIZATION-SETTINGS: Visualization settings commands registration complete');
    }
    /**
     * Register individual commands
     */
    registerAllCommands() {
        // Command: Configure setting
        const configureSettingCmd = vscode.commands.registerCommand('codeXR.visualizationSettings.configure', async (settingKey) => {
            try {
                console.log(`VISUALIZATION-SETTINGS: Configure command triggered for: ${settingKey}`);
                await this.interactionHandler.handleSettingConfiguration(settingKey);
            }
            catch (error) {
                console.error('VISUALIZATION-SETTINGS: Error in configure command:', error);
                vscode.window.showErrorMessage(`Failed to configure setting: ${error instanceof Error ? error.message : String(error)}`);
            }
        });
        // Register commands with the extension context
        this.context.subscriptions.push(configureSettingCmd);
        // Store interaction handler for cleanup
        this.context.subscriptions.push({
            dispose: () => this.interactionHandler.dispose()
        });
        console.log('VISUALIZATION-SETTINGS: All commands registered successfully');
    }
    /**
     * Dispose of resources
     */
    dispose() {
        this.interactionHandler.dispose();
    }
}
exports.VisualizationSettingsCommands = VisualizationSettingsCommands;


/***/ }),
/* 170 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Learn More Commands Wrapper
 * Re-exports learn more commands for centralized command registration
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.registerLearnMoreCommands = registerLearnMoreCommands;
const learnMoreCommands_1 = __webpack_require__(171);
/**
 * Registers all learn more related commands
 */
function registerLearnMoreCommands(context) {
    console.log('LEARN_MORE: Registering learn more commands...');
    learnMoreCommands_1.LearnMoreCommands.registerCommands(context);
    console.log('LEARN_MORE: Learn more commands registration complete');
}


/***/ }),
/* 171 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Learn More Commands
 * Handles commands related to learning more about CodeXR functionality
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LearnMoreCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
class LearnMoreCommands {
    /**
     * Register all learn more related commands
     */
    static registerCommands(context) {
        console.log('LEARN_MORE: Registering learn more commands...');
        // Main learn more command
        const learnMoreCommand = vscode.commands.registerCommand('codeXR.learnMore', () => {
            LearnMoreCommands.handleLearnMore();
        });
        context.subscriptions.push(learnMoreCommand);
        console.log('LEARN_MORE: Learn more commands registered successfully');
    }
    /**
     * Handle the main learn more action
     * Opens the CodeXR documentation website
     */
    static handleLearnMore() {
        console.log('LEARN_MORE: Learn more action triggered - opening CodeXR documentation website');
        // Open the CodeXR documentation website
        const websiteUrl = 'https://amontesl.github.io/code-xr-docs/';
        vscode.window.showInformationMessage('Opening CodeXR Documentation Website...', 'Open Website').then(selection => {
            if (selection === 'Open Website') {
                vscode.env.openExternal(vscode.Uri.parse(websiteUrl));
            }
        });
        // Also open directly for immediate access
        vscode.env.openExternal(vscode.Uri.parse(websiteUrl));
    }
}
exports.LearnMoreCommands = LearnMoreCommands;


/***/ }),
/* 172 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.registerGeneralCommands = registerGeneralCommands;
const vscode = __importStar(__webpack_require__(1));
const commonCommands_1 = __webpack_require__(173);
/**
 * Register general/common commands used throughout the extension
 */
function registerGeneralCommands(context) {
    console.log('GENERAL_COMMANDS: Registering general commands');
    // Register the main tree refresh command (replaces codexr.servers.refresh)
    const refreshTreeCommand = vscode.commands.registerCommand('codexr.tree.refresh', () => {
        console.log('GENERAL_COMMANDS: Tree refresh command executed');
        commonCommands_1.CommonCommands.refreshTreeView();
    });
    // Register legacy command for backward compatibility
    const legacyRefreshCommand = vscode.commands.registerCommand('codexr.servers.refresh', () => {
        console.log('GENERAL_COMMANDS: Legacy servers refresh command executed, delegating to tree refresh');
        commonCommands_1.CommonCommands.refreshTreeView();
    });
    // Register a general modular tree refresh command
    const modularTreeRefreshCommand = vscode.commands.registerCommand('codeXR.modularTree.refresh', () => {
        console.log('GENERAL_COMMANDS: Modular tree refresh command executed');
        commonCommands_1.CommonCommands.refreshTreeView();
    });
    // Add commands to subscriptions
    context.subscriptions.push(refreshTreeCommand, legacyRefreshCommand, modularTreeRefreshCommand);
    console.log('GENERAL_COMMANDS: Registered 3 general commands (tree refresh, legacy refresh, modular refresh)');
}


/***/ }),
/* 173 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.CommonCommands = void 0;
const vscode = __importStar(__webpack_require__(1));
/**
 * Common commands utility for shared functionality across the extension
 */
class CommonCommands {
    static modularTreeProvider;
    /**
     * Set the modular tree provider for refresh operations
     */
    static setModularTreeProvider(provider) {
        this.modularTreeProvider = provider;
        console.log('COMMON_COMMANDS: Modular tree provider set for refresh operations');
    }
    /**
     * Refresh the entire tree view
     * This replaces the legacy 'codexr.servers.refresh' command
     */
    static refreshTreeView() {
        console.log('COMMON_COMMANDS: Refreshing tree view');
        if (this.modularTreeProvider && typeof this.modularTreeProvider.refresh === 'function') {
            this.modularTreeProvider.refresh();
            console.log('COMMON_COMMANDS: Tree view refreshed successfully');
        }
        else {
            console.warn('COMMON_COMMANDS: No modular tree provider available for refresh');
            // Fallback: try to execute any existing refresh commands
            try {
                vscode.commands.executeCommand('codeXR.modularTree.refresh');
            }
            catch (error) {
                console.error('COMMON_COMMANDS: Failed to refresh tree view:', error);
            }
        }
    }
    /**
     * Legacy method for backward compatibility
     * @deprecated Use refreshTreeView() instead
     */
    static refreshServers() {
        console.log('COMMON_COMMANDS: Legacy refresh servers called, delegating to refreshTreeView');
        this.refreshTreeView();
    }
}
exports.CommonCommands = CommonCommands;


/***/ }),
/* 174 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Debug Theme Configuration Problem
 * Script to diagnose why theme settings are not being saved/loaded correctly
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DebugThemeProblem = void 0;
const vscode = __importStar(__webpack_require__(1));
const analysisConfigurationStorage_1 = __webpack_require__(87);
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
class DebugThemeProblem {
    /**
     * Main debug function to check theme configuration
     */
    static async debugThemeConfiguration(context) {
        console.log('\n=== 🔍 DEBUG THEME CONFIGURATION PROBLEM ===');
        try {
            const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
            // 1. Check current configuration file path
            const configPath = storage.getConfigurationFilePath();
            console.log(`\n1️⃣ Configuration file path: ${configPath}`);
            // 2. Check if configuration file exists
            const exists = await storage.configurationExists();
            console.log(`   File exists: ${exists}`);
            if (exists) {
                // 3. Read raw file content
                const rawContent = await storage.debugGetConfigurationFileContent();
                console.log(`\n2️⃣ Raw configuration file content:`);
                console.log(rawContent);
                // 4. Check file permissions and metadata
                try {
                    const stats = fs.statSync(configPath);
                    console.log(`\n3️⃣ File metadata:`);
                    console.log(`   Size: ${stats.size} bytes`);
                    console.log(`   Modified: ${stats.mtime}`);
                    console.log(`   Permissions: ${stats.mode.toString(8)}`);
                }
                catch (statsError) {
                    console.error(`   Error reading file stats: ${statsError}`);
                }
            }
            // 5. Load configuration using the storage manager
            console.log(`\n4️⃣ Loading configuration through storage manager:`);
            const loadedConfig = await storage.loadConfiguration();
            console.log(`   Loaded config:`, JSON.stringify(loadedConfig, null, 2));
            // 6. Check current theme specifically
            const currentTheme = await storage.getViewTheme();
            console.log(`\n5️⃣ Current theme from storage: ${currentTheme}`);
            // 7. Force reload and check again
            console.log(`\n6️⃣ Force reloading configuration:`);
            const reloadedConfig = await storage.forceReloadConfiguration();
            console.log(`   Reloaded config:`, JSON.stringify(reloadedConfig, null, 2));
            const reloadedTheme = await storage.getViewTheme();
            console.log(`   Reloaded theme: ${reloadedTheme}`);
            // 8. Test saving a different theme
            console.log(`\n7️⃣ Testing theme save operation:`);
            const originalTheme = currentTheme;
            const testTheme = originalTheme === 'Dark' ? 'Light' : 'Dark';
            console.log(`   Original theme: ${originalTheme}`);
            console.log(`   Setting test theme: ${testTheme}`);
            await storage.setViewTheme(testTheme);
            // Check if it was saved
            const savedTheme = await storage.getViewTheme();
            console.log(`   Theme after save: ${savedTheme}`);
            // Read file again to verify
            if (await storage.configurationExists()) {
                const updatedContent = await storage.debugGetConfigurationFileContent();
                console.log(`\n8️⃣ Updated file content after theme change:`);
                console.log(updatedContent);
            }
            // 9. Restore original theme
            console.log(`\n9️⃣ Restoring original theme: ${originalTheme}`);
            await storage.setViewTheme(originalTheme);
            const restoredTheme = await storage.getViewTheme();
            console.log(`   Restored theme: ${restoredTheme}`);
            // 10. Check directory permissions
            console.log(`\n🔟 Checking directory permissions:`);
            const configDir = path.dirname(configPath);
            try {
                const dirStats = fs.statSync(configDir);
                console.log(`   Directory: ${configDir}`);
                console.log(`   Directory permissions: ${dirStats.mode.toString(8)}`);
                console.log(`   Directory writable: ${fs.constants.W_OK}`);
                // Test write permissions
                fs.accessSync(configDir, fs.constants.W_OK);
                console.log(`   ✅ Directory is writable`);
            }
            catch (dirError) {
                console.error(`   ❌ Directory access error: ${dirError}`);
            }
            console.log('\n=== ✅ DEBUG COMPLETE ===\n');
        }
        catch (error) {
            console.error('\n❌ DEBUG ERROR:', error);
        }
    }
    /**
     * Specific test for theme configuration workflow
     */
    static async testThemeWorkflow(context) {
        console.log('\n=== 🔬 TESTING THEME WORKFLOW ===');
        try {
            const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
            // Step 1: Get initial state
            console.log('\n1️⃣ Initial state:');
            const initialTheme = await storage.getViewTheme();
            console.log(`   Initial theme: ${initialTheme}`);
            // Step 2: Change to opposite theme
            const newTheme = initialTheme === 'Dark' ? 'Light' : 'Dark';
            console.log(`\n2️⃣ Changing to: ${newTheme}`);
            await storage.setViewTheme(newTheme);
            // Step 3: Verify change was saved
            const verifyTheme = await storage.getViewTheme();
            console.log(`   Verified theme: ${verifyTheme}`);
            console.log(`   Change successful: ${verifyTheme === newTheme ? '✅' : '❌'}`);
            // Step 4: Force reload from disk
            console.log('\n3️⃣ Force reloading from disk:');
            const reloadedTheme = await storage.forceReloadConfiguration();
            console.log(`   Reloaded theme: ${reloadedTheme.viewTheme}`);
            console.log(`   Reload matches: ${reloadedTheme.viewTheme === newTheme ? '✅' : '❌'}`);
            // Step 5: Check file content directly
            const fileContent = await storage.debugGetConfigurationFileContent();
            if (fileContent) {
                const parsed = JSON.parse(fileContent);
                console.log(`\n4️⃣ File content theme: ${parsed.configuration.viewTheme}`);
                console.log(`   File matches: ${parsed.configuration.viewTheme === newTheme ? '✅' : '❌'}`);
            }
            // Step 6: Restore original
            console.log(`\n5️⃣ Restoring original theme: ${initialTheme}`);
            await storage.setViewTheme(initialTheme);
            const finalTheme = await storage.getViewTheme();
            console.log(`   Final theme: ${finalTheme}`);
            console.log(`   Restore successful: ${finalTheme === initialTheme ? '✅' : '❌'}`);
            console.log('\n=== ✅ WORKFLOW TEST COMPLETE ===\n');
        }
        catch (error) {
            console.error('\n❌ WORKFLOW TEST ERROR:', error);
        }
    }
    /**
     * Register debug commands
     */
    static registerDebugCommands(context) {
        return [
            vscode.commands.registerCommand('codexr.debug.themeConfiguration', async () => {
                await DebugThemeProblem.debugThemeConfiguration(context);
                vscode.window.showInformationMessage('Theme configuration debug completed. Check console for details.');
            }),
            vscode.commands.registerCommand('codexr.debug.themeWorkflow', async () => {
                await DebugThemeProblem.testThemeWorkflow(context);
                vscode.window.showInformationMessage('Theme workflow test completed. Check console for details.');
            })
        ];
    }
}
exports.DebugThemeProblem = DebugThemeProblem;


/***/ }),
/* 175 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Modular Views Index
 * Central exports for all modular view components
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ModularTreeDataProvider = void 0;
// Main modular tree provider
var ModularTreeDataProvider_1 = __webpack_require__(176);
Object.defineProperty(exports, "ModularTreeDataProvider", ({ enumerable: true, get: function () { return ModularTreeDataProvider_1.ModularTreeDataProvider; } }));
// Common interfaces and utilities
__exportStar(__webpack_require__(177), exports);
__exportStar(__webpack_require__(222), exports);
// Section providers
__exportStar(__webpack_require__(178), exports);
__exportStar(__webpack_require__(182), exports);
__exportStar(__webpack_require__(186), exports);
__exportStar(__webpack_require__(191), exports);
__exportStar(__webpack_require__(214), exports);
__exportStar(__webpack_require__(218), exports);
// New code analysis views (experimental)
__exportStar(__webpack_require__(223), exports);


/***/ }),
/* 176 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ModularTreeDataProvider = void 0;
const vscode = __importStar(__webpack_require__(1));
const baseInterfaces_1 = __webpack_require__(177);
const servers_1 = __webpack_require__(178);
const active_servers_1 = __webpack_require__(182);
const babia_examples_1 = __webpack_require__(186);
const visualize_data_1 = __webpack_require__(191);
const NewCodeAnalysisSectionProvider_1 = __webpack_require__(197);
const visualization_settings_1 = __webpack_require__(214);
const learn_more_1 = __webpack_require__(218);
/**
 * Main modular tree data provider that orchestrates all section providers
 */
class ModularTreeDataProvider {
    context;
    _onDidChangeTreeData = new vscode.EventEmitter();
    onDidChangeTreeData = this._onDidChangeTreeData.event;
    sectionProviders = new Map();
    constructor(context) {
        this.context = context;
        console.log('MODULAR_TREE: Initializing modular tree data provider');
        // Initialize all section providers
        this.initializeSectionProviders();
    }
    /**
     * Initialize all section providers
     */
    initializeSectionProviders() {
        console.log('MODULAR_TREE: Initializing section providers');
        // Create and register all section providers
        const providers = [
            new servers_1.ServersSectionProvider(this.context),
            new active_servers_1.ActiveServersSectionProvider(this.context),
            new babia_examples_1.BabiaExamplesSectionProvider(this.context),
            new visualize_data_1.VisualizeDataSectionProvider(this.context),
            new NewCodeAnalysisSectionProvider_1.NewCodeAnalysisSectionProvider(this.context),
            new visualization_settings_1.VisualizationSettingsSectionProvider(this.context),
            new learn_more_1.LearnMoreSectionProvider(this.context)
        ];
        // Register providers and listen to their changes
        providers.forEach(provider => {
            const sectionName = provider.getSectionName();
            this.sectionProviders.set(sectionName, provider);
            // Listen to provider changes and propagate them
            if (provider.onDidChangeTreeData) {
                provider.onDidChangeTreeData(() => {
                    console.log(`MODULAR_TREE: Section ${sectionName} changed, refreshing tree`);
                    this.refresh();
                });
            }
            console.log(`MODULAR_TREE: Registered section provider: ${sectionName}`);
        });
        console.log(`MODULAR_TREE: Initialized ${providers.length} section providers`);
    }
    /**
     * Get tree item
     */
    getTreeItem(element) {
        return element;
    }
    /**
     * Get children
     */
    async getChildren(element) {
        if (!element) {
            // Root level - return section headers
            console.log('MODULAR_TREE: Loading root sections');
            return this.getRootSections();
        }
        // Get children from the appropriate section provider
        return this.getSectionChildren(element);
    }
    /**
     * Get root sections
     */
    getRootSections() {
        const sections = [];
        // Create section headers from each provider
        this.sectionProviders.forEach((provider, sectionName) => {
            try {
                const sectionItem = provider.getSectionItem();
                // Convert to ModularTreeItem
                const modularItem = new baseInterfaces_1.ModularTreeItem(typeof sectionItem.label === 'string' ? sectionItem.label : sectionItem.label?.label || sectionName.toUpperCase(), sectionItem.collapsibleState || vscode.TreeItemCollapsibleState.Collapsed, sectionName, 'section', sectionItem.command, sectionItem.iconPath, sectionItem.tooltip, sectionItem.description, sectionItem.contextValue);
                sections.push(modularItem);
            }
            catch (error) {
                console.error(`MODULAR_TREE: Error creating section header for ${sectionName}:`, error);
                // Create error section
                sections.push(new baseInterfaces_1.ModularTreeItem(`${sectionName.toUpperCase()} (Error)`, vscode.TreeItemCollapsibleState.None, sectionName, 'error', undefined, new vscode.ThemeIcon('error'), `Error loading ${sectionName} section`));
            }
        });
        console.log(`MODULAR_TREE: Created ${sections.length} root sections`);
        return sections;
    }
    /**
     * Get children for a specific section
     */
    async getSectionChildren(element) {
        const sectionName = element.sectionType;
        const provider = this.sectionProviders.get(sectionName);
        if (!provider) {
            console.error(`MODULAR_TREE: No provider found for section: ${sectionName}`);
            return [];
        }
        try {
            console.log(`MODULAR_TREE: Getting children for section: ${sectionName}`);
            // Convert ModularTreeItem back to the section-specific item type
            let sectionElement = undefined;
            if (element.itemType !== 'section') {
                // Create a section-specific item with the preserved properties
                sectionElement = this.convertToSectionItem(element);
            }
            // Get children from the section provider
            const sectionChildren = await provider.getChildren(sectionElement);
            // Convert to ModularTreeItems
            const modularChildren = sectionChildren.map((child) => {
                // Preserve the original item properties for proper delegation
                const modularItem = new baseInterfaces_1.ModularTreeItem(typeof child.label === 'string' ? child.label : child.label?.label || 'Unknown', child.collapsibleState || vscode.TreeItemCollapsibleState.None, sectionName, child.serverItemType || child.activeServerItemType || child.babiaItemType || child.visualizeDataItemType || child.codeAnalysisItemType || child.newCodeAnalysisItemType || child.visualizationSettingsItemType || child.learnMoreItemType || child.type || 'item', child.command, child.iconPath, child.tooltip, child.description, child.contextValue);
                // Copy over section-specific properties
                if (child.serverItemType) {
                    modularItem.serverItemType = child.serverItemType;
                }
                if (child.activeServerItemType) {
                    modularItem.activeServerItemType = child.activeServerItemType;
                    modularItem.activeServer = child.activeServer;
                }
                if (child.babiaItemType) {
                    modularItem.babiaItemType = child.babiaItemType;
                    modularItem.babiaExample = child.babiaExample;
                }
                if (child.visualizeDataItemType) {
                    modularItem.visualizeDataItemType = child.visualizeDataItemType;
                    modularItem.visualizeDataItem = child.visualizeDataItem;
                }
                if (child.codeAnalysisItemType) {
                    modularItem.codeAnalysisItemType = child.codeAnalysisItemType;
                    modularItem.originalCodeAnalysisItem = child.originalCodeAnalysisItem;
                }
                if (child.newCodeAnalysisItemType) {
                    modularItem.newCodeAnalysisItemType = child.newCodeAnalysisItemType;
                    modularItem.originalNewCodeAnalysisItem = child.originalNewCodeAnalysisItem;
                }
                if (child.visualizationSettingsItemType) {
                    modularItem.visualizationSettingsItemType = child.visualizationSettingsItemType;
                    modularItem.originalSettingsItem = child.originalSettingsItem;
                }
                if (child.learnMoreItemType) {
                    modularItem.learnMoreItemType = child.learnMoreItemType;
                    modularItem.originalLearnMoreItem = child.originalLearnMoreItem;
                }
                return modularItem;
            });
            console.log(`MODULAR_TREE: Retrieved ${modularChildren.length} children for section: ${sectionName}`);
            return modularChildren;
        }
        catch (error) {
            console.error(`MODULAR_TREE: Error getting children for section ${sectionName}:`, error);
            return [new baseInterfaces_1.ModularTreeItem('Error loading items', vscode.TreeItemCollapsibleState.None, sectionName, 'error', undefined, new vscode.ThemeIcon('error'), `Failed to load ${sectionName} items`)];
        }
    }
    /**
     * Convert ModularTreeItem back to section-specific item type
     */
    convertToSectionItem(element) {
        const sectionName = element.sectionType;
        // Create section-specific items based on section type
        switch (sectionName) {
            case 'SERVERS':
                // Import and create ServerTreeItem
                const { ServerTreeItem } = __webpack_require__(180);
                const serverItem = new ServerTreeItem(typeof element.label === 'string' ? element.label : element.label?.label || 'Unknown', element.collapsibleState || vscode.TreeItemCollapsibleState.None, element.serverItemType || 'config-option', element.command, element.iconPath, element.tooltip, element.description, element.contextValue);
                return serverItem;
            case 'activeServers':
                // Import and create ActiveServerTreeItem
                const { ActiveServerTreeItem } = __webpack_require__(184);
                const activeServerItem = new ActiveServerTreeItem(typeof element.label === 'string' ? element.label : element.label?.label || 'Unknown', element.collapsibleState || vscode.TreeItemCollapsibleState.None, element.activeServerItemType || 'server-item', element.command, element.iconPath, element.tooltip, element.description, element.contextValue, element.activeServer);
                return activeServerItem;
            case 'babiaExamples':
                // Import and create BabiaExampleTreeItem
                const { BabiaExampleTreeItem } = __webpack_require__(188);
                const babiaItem = new BabiaExampleTreeItem(typeof element.label === 'string' ? element.label : element.label?.label || 'Unknown', element.collapsibleState || vscode.TreeItemCollapsibleState.None, element.babiaItemType || 'example-item', element.command, element.iconPath, element.tooltip, element.description, element.contextValue, element.babiaExample);
                return babiaItem;
            case 'visualizeData':
                // Import and create VisualizeDataModularTreeItem
                const { VisualizeDataModularTreeItem } = __webpack_require__(193);
                const visualizeItem = new VisualizeDataModularTreeItem(typeof element.label === 'string' ? element.label : element.label?.label || 'Unknown', element.collapsibleState || vscode.TreeItemCollapsibleState.None, element.visualizeDataItemType || 'error', element.command, element.iconPath, element.tooltip, element.description, element.contextValue, element.visualizeDataItem);
                return visualizeItem;
            case 'newCodeAnalysis':
                // Import and create NewCodeAnalysisTreeItem
                const { NewCodeAnalysisTreeItem } = __webpack_require__(150);
                const newCodeAnalysisItem = new NewCodeAnalysisTreeItem(typeof element.label === 'string' ? element.label : element.label?.label || 'Unknown', element.collapsibleState || vscode.TreeItemCollapsibleState.None, element.type || 'item', element.command, element.iconPath, element.tooltip, element.description, element.contextValue);
                return newCodeAnalysisItem;
            case 'visualizationSettings':
                // Import and create VisualizationSettingsModularTreeItem
                const { VisualizationSettingsModularTreeItem } = __webpack_require__(216);
                const settingsItem = new VisualizationSettingsModularTreeItem(typeof element.label === 'string' ? element.label : element.label?.label || 'Unknown', element.collapsibleState || vscode.TreeItemCollapsibleState.None, element.visualizationSettingsItemType || 'error', element.command, element.iconPath, element.tooltip, element.description, element.contextValue, element.originalSettingsItem);
                return settingsItem;
            case 'learnMore':
                // Import and create LearnMoreModularTreeItem
                const { LearnMoreModularTreeItem } = __webpack_require__(220);
                const learnMoreItem = new LearnMoreModularTreeItem(typeof element.label === 'string' ? element.label : element.label?.label || 'Unknown', element.collapsibleState || vscode.TreeItemCollapsibleState.None, element.learnMoreItemType || 'action', element.command, element.iconPath, element.tooltip, element.description, element.contextValue);
                return learnMoreItem;
            default:
                // Return the element as-is for other sections
                return element;
        }
    }
    /**
     * Refresh the tree
     */
    refresh() {
        console.log('MODULAR_TREE: Refreshing modular tree - START');
        console.log('MODULAR_TREE: Firing _onDidChangeTreeData event');
        this._onDidChangeTreeData.fire();
        console.log('MODULAR_TREE: Refreshing modular tree - COMPLETE');
    }
    /**
     * Get section provider by name
     */
    getSectionProvider(sectionName) {
        return this.sectionProviders.get(sectionName);
    }
    /**
     * Handle clicks on items
     */
    async handleItemClick(item) {
        const sectionName = item.sectionType;
        const provider = this.sectionProviders.get(sectionName);
        if (provider && typeof provider.handleClick === 'function') {
            console.log(`MODULAR_TREE: Delegating click to section provider: ${sectionName}`);
            // Convert back to section-specific item for proper handling
            const sectionItem = this.convertToSectionItem(item);
            await provider.handleClick(sectionItem);
        }
        else {
            console.log(`MODULAR_TREE: No click handler for section: ${sectionName}`);
        }
    }
    /**
     * Handle context menu actions
     */
    async handleContextMenu(action, item) {
        const sectionName = item.sectionType;
        const provider = this.sectionProviders.get(sectionName);
        if (provider && typeof provider.handleContextMenu === 'function') {
            console.log(`MODULAR_TREE: Delegating context menu to section provider: ${sectionName}`);
            // Convert back to section-specific item for proper handling
            const sectionItem = this.convertToSectionItem(item);
            await provider.handleContextMenu(action, sectionItem);
        }
        else {
            console.log(`MODULAR_TREE: No context menu handler for section: ${sectionName}`);
        }
    }
    /**
     * Dispose of resources
     */
    dispose() {
        console.log('MODULAR_TREE: Disposing modular tree data provider');
        // Dispose of all section providers
        for (const [sectionName, provider] of this.sectionProviders) {
            if (provider.dispose) {
                console.log(`MODULAR_TREE: Disposing section provider: ${sectionName}`);
                provider.dispose();
            }
        }
        this.sectionProviders.clear();
    }
}
exports.ModularTreeDataProvider = ModularTreeDataProvider;


/***/ }),
/* 177 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.TreeViewUtils = exports.BaseSectionProvider = exports.ModularTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
/**
 * Base tree item for modular sections
 */
class ModularTreeItem extends vscode.TreeItem {
    sectionType;
    itemType;
    // Server-specific properties for compatibility
    serverItemType;
    // Active Server-specific properties for compatibility
    activeServerItemType;
    activeServer;
    // Babia Examples-specific properties for compatibility
    babiaItemType;
    babiaExample;
    // Visualize Data-specific properties for compatibility
    visualizeDataItemType;
    visualizeDataItem;
    // Code Analysis-specific properties for compatibility
    codeAnalysisItemType;
    originalCodeAnalysisItem;
    // Visualization Settings-specific properties for compatibility
    visualizationSettingsItemType;
    originalSettingsItem;
    constructor(label, collapsibleState, sectionType, itemType, command, iconPath, tooltip, description, contextValue) {
        super(label, collapsibleState);
        this.sectionType = sectionType;
        this.itemType = itemType;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip || label;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.ModularTreeItem = ModularTreeItem;
/**
 * Abstract base class for section providers.
 * Eliminates the repeated emitter / refresh / getSectionName boilerplate
 * that was duplicated across all 7 section providers.
 *
 * Subclasses only need to implement:
 *   - getSectionName()
 *   - getSectionItem()
 *   - getChildren()
 */
class BaseSectionProvider {
    context;
    _onDidChangeTreeData = new vscode.EventEmitter();
    onDidChangeTreeData = this._onDidChangeTreeData.event;
    constructor(context) {
        this.context = context;
    }
    /** Refresh the section (fires tree data change event). */
    refresh() {
        this._onDidChangeTreeData.fire();
    }
    /** Convenience: get the tree item representation (identity). */
    getTreeItem(element) {
        return element;
    }
}
exports.BaseSectionProvider = BaseSectionProvider;
/**
 * Common tree view utilities
 */
class TreeViewUtils {
    /**
     * Create a standard error item
     */
    static createErrorItem(message, sectionType) {
        return new ModularTreeItem(message, vscode.TreeItemCollapsibleState.None, sectionType, 'error', undefined, new vscode.ThemeIcon('error'), `Error: ${message}`, 'Error');
    }
    /**
     * Create a standard loading item
     */
    static createLoadingItem(message, sectionType) {
        return new ModularTreeItem(message, vscode.TreeItemCollapsibleState.None, sectionType, 'loading', undefined, new vscode.ThemeIcon('loading~spin'), `Loading: ${message}`, 'Loading...');
    }
    /**
     * Create a standard info item
     */
    static createInfoItem(message, sectionType) {
        return new ModularTreeItem(message, vscode.TreeItemCollapsibleState.None, sectionType, 'info', undefined, new vscode.ThemeIcon('info'), message);
    }
}
exports.TreeViewUtils = TreeViewUtils;


/***/ }),
/* 178 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Servers View Module
 * Exports for the modular Servers section
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServerClickHandler = exports.ServerItemFactory = exports.ServerTreeItem = exports.ServersSectionProvider = void 0;
// Section Provider
var ServersSectionProvider_1 = __webpack_require__(179);
Object.defineProperty(exports, "ServersSectionProvider", ({ enumerable: true, get: function () { return ServersSectionProvider_1.ServersSectionProvider; } }));
// Items
var serverItems_1 = __webpack_require__(180);
Object.defineProperty(exports, "ServerTreeItem", ({ enumerable: true, get: function () { return serverItems_1.ServerTreeItem; } }));
Object.defineProperty(exports, "ServerItemFactory", ({ enumerable: true, get: function () { return serverItems_1.ServerItemFactory; } }));
// Interactions
var handleServerClicks_1 = __webpack_require__(181);
Object.defineProperty(exports, "ServerClickHandler", ({ enumerable: true, get: function () { return handleServerClicks_1.ServerClickHandler; } }));


/***/ }),
/* 179 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServersSectionProvider = void 0;
const vscode = __importStar(__webpack_require__(1));
const baseInterfaces_1 = __webpack_require__(177);
const serverItems_1 = __webpack_require__(180);
const handleServerClicks_1 = __webpack_require__(181);
const serverSettingsManager_1 = __webpack_require__(11);
/**
 * Servers section provider for the modular tree view architecture
 */
class ServersSectionProvider extends baseInterfaces_1.BaseSectionProvider {
    constructor(context) {
        super(context);
        console.log('SERVERS: Servers section provider initialized');
    }
    getSectionName() {
        return 'SERVERS';
    }
    getSectionItem() {
        console.log('SERVERS: Creating main SERVERS section item');
        return new serverItems_1.ServerTreeItem('SERVERS', vscode.TreeItemCollapsibleState.Expanded, 'config-group', undefined, new vscode.ThemeIcon('server-environment'), 'Server configuration and launch options');
    }
    /**
     * Get children for the servers section
     */
    async getChildren(element) {
        if (!element) {
            // Return main section children
            console.log('SERVERS: Loading main servers section children');
            return this.getMainSectionChildren();
        }
        // Handle sub-items based on type
        switch (element.serverItemType) {
            case 'config-group':
                if (element.label === 'Server Configuration') {
                    return this.getConfigurationChildren();
                }
                break;
            default:
                return [];
        }
        return [];
    }
    /**
     * Get main section children
     */
    getMainSectionChildren() {
        console.log('SERVERS: Creating main section children');
        // Get current server configuration for the start server option
        const port = this.getServerPort();
        const httpMode = this.getHttpMode();
        return [
            serverItems_1.ServerItemFactory.createConfigurationGroup(),
            serverItems_1.ServerItemFactory.createStartServerOption(port, httpMode)
        ];
    }
    /**
     * Get configuration children
     */
    getConfigurationChildren() {
        console.log('SERVERS: Creating configuration children');
        return serverItems_1.ServerItemFactory.createConfigurationOptions();
    }
    /**
     * Get current server port from configuration
     */
    getServerPort() {
        const config = serverSettingsManager_1.ServerSettingsManager.getInstance().getLegacyConfig();
        return config.port;
    }
    /**
     * Get current HTTP mode from configuration
     */
    getHttpMode() {
        const config = serverSettingsManager_1.ServerSettingsManager.getInstance().getLegacyConfig();
        return config.httpMode;
    }
    /**
     * Handle tree item clicks
     */
    async handleItemClick(item) {
        console.log(`SERVERS: Item clicked: ${item.label} (type: ${item.serverItemType})`);
        switch (item.serverItemType) {
            case 'config-group':
                await handleServerClicks_1.ServerClickHandler.handleConfigGroupClick(item);
                break;
            case 'launch-option':
                await handleServerClicks_1.ServerClickHandler.handleLaunchServerClick();
                break;
            case 'config-option':
                const labelStr = typeof item.label === 'string' ? item.label : item.label?.label || '';
                const optionType = this.getConfigOptionType(labelStr);
                await handleServerClicks_1.ServerClickHandler.handleConfigOptionClick(optionType);
                break;
        }
    }
    /**
     * Determine configuration option type from label
     */
    getConfigOptionType(label) {
        if (label.includes('HTTP Mode')) {
            return 'httpMode';
        }
        if (label.includes('Port')) {
            return 'port';
        }
        if (label.includes('Auto-Open')) {
            return 'autoOpen';
        }
        if (label.includes('Open Mode')) {
            return 'openMode';
        }
        if (label.includes('Reset')) {
            return 'reset';
        }
        return 'unknown';
    }
}
exports.ServersSectionProvider = ServersSectionProvider;


/***/ }),
/* 180 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServerItemFactory = exports.ServerTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
const serverSettingsManager_1 = __webpack_require__(11);
/**
 * Get current server configuration for dynamic item creation
 */
function getServerConfig() {
    return serverSettingsManager_1.ServerSettingsManager.getInstance().getLegacyConfig();
}
/**
 * Server tree items for the Servers section
 */
class ServerTreeItem extends vscode.TreeItem {
    serverItemType;
    constructor(label, collapsibleState, serverItemType, command, iconPath, tooltip, description, contextValue) {
        super(label, collapsibleState);
        this.serverItemType = serverItemType;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip || label;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.ServerTreeItem = ServerTreeItem;
/**
 * Factory for creating server-related tree items
 */
class ServerItemFactory {
    /**
     * Create server configuration group item
     */
    static createConfigurationGroup() {
        console.log('SERVERS: Creating Server Configuration group item');
        return new ServerTreeItem('Server Configuration', vscode.TreeItemCollapsibleState.Collapsed, 'config-group', undefined, new vscode.ThemeIcon('settings-gear'), 'Configure server settings');
    }
    /**
     * Create start server option item
     */
    static createStartServerOption(port, httpMode) {
        console.log(`SERVERS: Creating Start Local Server option for port ${port} (${httpMode})`);
        return new ServerTreeItem('Start Local Server', vscode.TreeItemCollapsibleState.None, 'launch-option', {
            command: 'codexr.server.launch',
            title: 'Start Local Server'
        }, new vscode.ThemeIcon('play'), `Start server on port ${port} (${httpMode})`);
    }
    /**
     * Create configuration option items
     */
    static createConfigurationOptions() {
        console.log('SERVERS: Creating server configuration option items');
        const config = getServerConfig();
        return [
            new ServerTreeItem(`HTTP Mode: ${config.httpMode}`, vscode.TreeItemCollapsibleState.None, 'config-option', {
                command: 'codexr.server.config.httpMode',
                title: 'Configure HTTP Mode'
            }, new vscode.ThemeIcon(config.httpMode === 'HTTP' ? 'unlock' : 'lock'), `Click to change server mode (currently: ${config.httpMode})`),
            new ServerTreeItem(`Default Port: ${config.port}`, vscode.TreeItemCollapsibleState.None, 'config-option', {
                command: 'codexr.server.config.port',
                title: 'Configure Port'
            }, new vscode.ThemeIcon('symbol-numeric'), `Click to change default port (currently: ${config.port})`),
            new ServerTreeItem(`Auto-Open: ${config.autoOpen ? 'Enabled' : 'Disabled'}`, vscode.TreeItemCollapsibleState.None, 'config-option', {
                command: 'codexr.server.config.autoOpen',
                title: 'Toggle Auto-Open'
            }, new vscode.ThemeIcon(config.autoOpen ? 'check' : 'x'), `Click to toggle auto-open (currently: ${config.autoOpen ? 'enabled' : 'disabled'})`),
            new ServerTreeItem(`Open Mode: ${config.openMode}`, vscode.TreeItemCollapsibleState.None, 'config-option', {
                command: 'codexr.server.config.openMode',
                title: 'Configure Open Mode'
            }, new vscode.ThemeIcon('window'), `Click to change open mode (currently: ${config.openMode})`),
            new ServerTreeItem('Reset to Default', vscode.TreeItemCollapsibleState.None, 'config-option', {
                command: 'codexr.server.config.resetToDefault',
                title: 'Reset to Default Settings'
            }, new vscode.ThemeIcon('refresh'), 'Reset all server configuration to default values')
        ];
    }
}
exports.ServerItemFactory = ServerItemFactory;


/***/ }),
/* 181 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ServerClickHandler = void 0;
const vscode = __importStar(__webpack_require__(1));
/**
 * Handle server-related click interactions
 */
class ServerClickHandler {
    /**
     * Handle configuration group expansion
     */
    static async handleConfigGroupClick(item) {
        console.log('SERVERS: Configuration group clicked, expanding...');
        // Configuration groups are handled automatically by tree expansion
        // Additional logic can be added here if needed
    }
    /**
     * Handle server launch option click
     */
    static async handleLaunchServerClick() {
        console.log('SERVERS: Start Local Server clicked');
        try {
            // Execute the server launch command
            await vscode.commands.executeCommand('codexr.server.launch');
            console.log('SERVERS: Server launch command executed successfully');
        }
        catch (error) {
            console.error('SERVERS: Error launching server:', error);
            vscode.window.showErrorMessage(`Failed to launch server: ${error}`);
        }
    }
    /**
     * Handle configuration option clicks
     */
    static async handleConfigOptionClick(optionType) {
        console.log(`SERVERS: Configuration option clicked: ${optionType}`);
        switch (optionType) {
            case 'http':
            case 'httpMode':
                await vscode.commands.executeCommand('codexr.server.config.httpMode');
                break;
            case 'port':
                await vscode.commands.executeCommand('codexr.server.config.port');
                break;
            case 'autoOpen':
                await vscode.commands.executeCommand('codexr.server.config.autoOpen');
                break;
            case 'openMode':
                await vscode.commands.executeCommand('codexr.server.config.openMode');
                break;
            case 'reset':
                await vscode.commands.executeCommand('codexr.server.config.resetToDefault');
                break;
            default:
                console.warn(`SERVERS: Unknown configuration option: ${optionType}`);
        }
    }
}
exports.ServerClickHandler = ServerClickHandler;


/***/ }),
/* 182 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Active Servers View Module
 * Exports for the modular Active Servers section
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ActiveServerClickHandler = exports.ActiveServerItemFactory = exports.ActiveServerTreeItem = exports.ActiveServersSectionProvider = void 0;
// Section Provider
var ActiveServersSectionProvider_1 = __webpack_require__(183);
Object.defineProperty(exports, "ActiveServersSectionProvider", ({ enumerable: true, get: function () { return ActiveServersSectionProvider_1.ActiveServersSectionProvider; } }));
// Items
var activeServerItems_1 = __webpack_require__(184);
Object.defineProperty(exports, "ActiveServerTreeItem", ({ enumerable: true, get: function () { return activeServerItems_1.ActiveServerTreeItem; } }));
Object.defineProperty(exports, "ActiveServerItemFactory", ({ enumerable: true, get: function () { return activeServerItems_1.ActiveServerItemFactory; } }));
// Interactions
var handleActiveServerClicks_1 = __webpack_require__(185);
Object.defineProperty(exports, "ActiveServerClickHandler", ({ enumerable: true, get: function () { return handleActiveServerClicks_1.ActiveServerClickHandler; } }));


/***/ }),
/* 183 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ActiveServersSectionProvider = void 0;
const vscode = __importStar(__webpack_require__(1));
const activeServerItems_1 = __webpack_require__(184);
const handleActiveServerClicks_1 = __webpack_require__(185);
const activeServerRegistry_1 = __webpack_require__(28);
/**
 * Active Servers section provider - manages running servers display and control
 */
class ActiveServersSectionProvider {
    context;
    _onDidChangeTreeData = new vscode.EventEmitter();
    onDidChangeTreeData = this._onDidChangeTreeData.event;
    clickHandler;
    constructor(context) {
        this.context = context;
        console.log('ACTIVE_SERVERS_MODULAR: Initializing Active Servers section provider');
        this.clickHandler = new handleActiveServerClicks_1.ActiveServerClickHandler(context);
        // Listen to registry changes for active servers
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        console.log(`ACTIVE_SERVERS_MODULAR: Connected to active server registry, current servers: ${registry.getAllServers().length}`);
        registry.onRegistryChange(() => {
            console.log('ACTIVE_SERVERS_MODULAR: Active servers registry changed, refreshing section');
            this.refresh();
        });
    }
    /**
     * Get the section name for identification
     */
    getSectionName() {
        return 'activeServers';
    }
    /**
     * Get the section header item
     */
    getSectionItem() {
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const activeServers = registry.getAllServers();
        const runningCount = activeServers.filter(server => server.status === 'running').length;
        const title = runningCount > 0
            ? `ACTIVE SERVERS (${runningCount} running)`
            : 'ACTIVE SERVERS';
        return new activeServerItems_1.ActiveServerTreeItem(title, vscode.TreeItemCollapsibleState.Expanded, 'no-servers', // Using this as section header type
        undefined, new vscode.ThemeIcon('server-process'), 'Currently running servers', undefined, 'activeServersSection');
    }
    /**
     * Get children items for the Active Servers section
     */
    async getChildren(element) {
        // If element is provided, it means we're getting children for a specific item
        // For the Active Servers section, we only have flat items, so return empty for sub-items
        if (element) {
            return [];
        }
        console.log('ACTIVE_SERVERS_MODULAR: Loading active servers section children');
        const registry = (0, activeServerRegistry_1.getActiveServerRegistry)();
        const activeServers = registry.getAllServers();
        const runningServers = activeServers.filter(server => server.status === 'running');
        console.log(`ACTIVE_SERVERS_MODULAR: Found ${activeServers.length} total servers, ${runningServers.length} running`);
        // Show "No active servers" message if no servers exist
        if (activeServers.length === 0) {
            console.log('ACTIVE_SERVERS_MODULAR: No servers found, showing "No active servers" message');
            return [activeServerItems_1.ActiveServerItemFactory.createNoServersItem()];
        }
        const children = [];
        // Add "Stop All Servers" option if there are 2 or more running servers
        if (runningServers.length >= 2) {
            console.log(`ACTIVE_SERVERS_MODULAR: Adding "Stop All Servers" option for ${runningServers.length} running servers`);
            children.push(activeServerItems_1.ActiveServerItemFactory.createStopAllServersItem(runningServers.length));
        }
        // Add individual server items
        console.log(`ACTIVE_SERVERS_MODULAR: Creating ${activeServers.length} individual server items`);
        const serverItems = activeServers.map(server => activeServerItems_1.ActiveServerItemFactory.createServerItem(server));
        children.push(...serverItems);
        console.log(`ACTIVE_SERVERS_MODULAR: Returning ${children.length} children for Active Servers section`);
        return children;
    }
    /**
     * Refresh the section
     */
    refresh() {
        console.log('ACTIVE_SERVERS_MODULAR: Refreshing Active Servers section');
        this._onDidChangeTreeData.fire();
    }
    /**
     * Handle item clicks (additional method for interaction)
     */
    async handleClick(item) {
        await this.clickHandler.handleActiveServerClick(item);
    }
    /**
     * Handle context menu actions (additional method for interaction)
     */
    async handleContextMenu(action, item) {
        await this.clickHandler.handleContextMenuAction(action, item);
    }
}
exports.ActiveServersSectionProvider = ActiveServersSectionProvider;


/***/ }),
/* 184 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ActiveServerItemFactory = exports.ActiveServerTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
/**
 * Active Server tree items for the Active Servers section
 */
class ActiveServerTreeItem extends vscode.TreeItem {
    activeServerItemType;
    activeServer;
    constructor(label, collapsibleState, activeServerItemType, command, iconPath, tooltip, description, contextValue, activeServer) {
        super(label, collapsibleState);
        this.activeServerItemType = activeServerItemType;
        this.activeServer = activeServer;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip || label;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.ActiveServerTreeItem = ActiveServerTreeItem;
/**
 * Factory for creating active server-related tree items
 */
class ActiveServerItemFactory {
    /**
     * Create "No active servers" message item
     */
    static createNoServersItem() {
        console.log('ACTIVE_SERVERS: Creating "No active servers" message item');
        return new ActiveServerTreeItem('No active servers', vscode.TreeItemCollapsibleState.None, 'no-servers', undefined, new vscode.ThemeIcon('info'), 'No servers are currently running');
    }
    /**
     * Create "Stop All Servers" control option
     */
    static createStopAllServersItem(runningCount) {
        console.log(`ACTIVE_SERVERS: Creating "Stop All Servers" option for ${runningCount} running servers`);
        return new ActiveServerTreeItem('Stop All Servers', vscode.TreeItemCollapsibleState.None, 'control-option', {
            command: 'codeXR.activeServers.stopAllServers',
            title: 'Stop All Servers'
        }, new vscode.ThemeIcon('stop-circle'), `Stop all ${runningCount} running servers`, undefined, 'stopAllServers');
    }
    /**
     * Create individual active server item
     */
    static createServerItem(server) {
        // Use custom name if provided, otherwise fallback to localhost:port
        const label = server.customName?.trim() || `localhost:${server.port}`;
        console.log(`ACTIVE_SERVERS: Creating server item with label: "${label}" (customName: "${server.customName}", port: ${server.port})`);
        const description = ActiveServerItemFactory.getServerDescription(server);
        const icon = ActiveServerItemFactory.getServerIcon(server);
        const tooltip = ActiveServerItemFactory.getServerTooltip(server);
        console.log(`ACTIVE_SERVERS: Creating server item: ${label} (${description})`);
        // Create command to show server actions on left-click
        const command = {
            command: 'codeXR.activeServers.showActions',
            title: 'Show Server Actions',
            arguments: [server.id]
        };
        // Set context value based on certificate mode for conditional menu items
        const contextValue = server.certMode === 'http' ? 'activeServerHttp' : 'activeServerHttps';
        return new ActiveServerTreeItem(label, vscode.TreeItemCollapsibleState.None, 'server-item', command, icon, tooltip, description, contextValue, server);
    }
    /**
     * Get server description based on launch mode
     * @private
     */
    static getServerDescription(server) {
        const mode = server.launchMode === 'browser' ? 'Browser' : 'Panel';
        const status = server.status === 'running' ? '' : ` (${server.status})`;
        return `${mode}${status}`;
    }
    /**
     * Get server icon based on certificate mode and status
     * @private
     */
    static getServerIcon(server) {
        // Base icon selection based on cert mode
        let iconName;
        switch (server.certMode) {
            case 'https-default':
            case 'https-custom':
                iconName = 'shield';
                break;
            case 'http':
            default:
                iconName = 'globe';
                break;
        }
        // Add status indicator if not running
        if (server.status !== 'running') {
            iconName = 'error';
        }
        return new vscode.ThemeIcon(iconName);
    }
    /**
     * Get server tooltip with detailed information
     * @private
     */
    static getServerTooltip(server) {
        const protocol = server.certMode === 'http' ? 'HTTP' : 'HTTPS';
        const mode = server.launchMode === 'browser' ? 'Browser' : 'Panel';
        const status = server.status.charAt(0).toUpperCase() + server.status.slice(1);
        let tooltip = `${protocol} Server (${status})
URL: ${server.url}
Mode: ${mode}
Port: ${server.port}`;
        if (server.htmlFile) {
            const fileName = (__webpack_require__(5).basename)(server.htmlFile);
            tooltip += `\nFile: ${fileName}`;
        }
        if (server.metadata) {
            const metadata = server.metadata; // Type assertion for dynamic metadata
            if (metadata.serverType) {
                tooltip += `\nType: ${metadata.serverType}`;
            }
            if (metadata.portChanged) {
                tooltip += `\nOriginal port was in use`;
            }
            if (metadata.httpsOverridden) {
                tooltip += `\nHTTPS overridden for panel mode`;
            }
        }
        tooltip += `\n\nClick to show details`;
        return tooltip;
    }
}
exports.ActiveServerItemFactory = ActiveServerItemFactory;


/***/ }),
/* 185 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ActiveServerClickHandler = void 0;
const handleServerActions_1 = __webpack_require__(29);
/**
 * Handler for Active Server section interactions
 */
class ActiveServerClickHandler {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Handle clicks on active server items
     */
    async handleActiveServerClick(item) {
        console.log(`ACTIVE_SERVERS_MODULAR: Handling click on active server item: ${item.label} (type: ${item.activeServerItemType})`);
        switch (item.activeServerItemType) {
            case 'server-item':
                await this.handleServerItemClick(item);
                break;
            case 'control-option':
                await this.handleControlOptionClick(item);
                break;
            case 'no-servers':
                // No action needed for informational item
                console.log('ACTIVE_SERVERS_MODULAR: Clicked on "No active servers" info item');
                break;
            default:
                console.warn(`ACTIVE_SERVERS_MODULAR: Unknown active server item type: ${item.activeServerItemType}`);
        }
    }
    /**
     * Handle click on individual server item
     */
    async handleServerItemClick(item) {
        if (!item.activeServer) {
            console.error('ACTIVE_SERVERS_MODULAR: No active server data in server item');
            return;
        }
        console.log(`ACTIVE_SERVERS_MODULAR: Handling server item click for server: ${item.activeServer.id}`);
        // Delegate to existing server actions handler
        await handleServerActions_1.ServerActionHandlers.showServerActions(item.activeServer.id);
    }
    /**
     * Handle click on control options (like "Stop All Servers")
     */
    async handleControlOptionClick(item) {
        console.log(`ACTIVE_SERVERS_MODULAR: Handling control option click: ${item.label}`);
        if (item.contextValue === 'stopAllServers') {
            await this.handleStopAllServers();
        }
        else {
            console.warn(`ACTIVE_SERVERS_MODULAR: Unknown control option: ${item.contextValue}`);
        }
    }
    /**
     * Handle "Stop All Servers" action
     */
    async handleStopAllServers() {
        console.log('ACTIVE_SERVERS_MODULAR: Handling stop all servers action');
        // Delegate to existing stop all servers handler
        await handleServerActions_1.ServerActionHandlers.stopAllServers();
    }
    /**
     * Handle right-click context menu actions
     */
    async handleContextMenuAction(action, item) {
        console.log(`ACTIVE_SERVERS_MODULAR: Handling context menu action: ${action} on item: ${item.label}`);
        switch (action) {
            case 'refresh':
                console.log('ACTIVE_SERVERS_MODULAR: Refreshing active servers view');
                await handleServerActions_1.ServerActionHandlers.refreshServers();
                break;
            case 'stopServer':
                if (item.activeServer) {
                    console.log(`ACTIVE_SERVERS_MODULAR: Stopping server: ${item.activeServer.id}`);
                    await handleServerActions_1.ServerActionHandlers.stopServer(item.activeServer.id);
                }
                break;
            case 'openBrowser':
                if (item.activeServer) {
                    console.log(`ACTIVE_SERVERS_MODULAR: Opening server in browser: ${item.activeServer.id}`);
                    await handleServerActions_1.ServerActionHandlers.openInBrowser(item.activeServer.id);
                }
                break;
            case 'openPanel':
                if (item.activeServer) {
                    console.log(`ACTIVE_SERVERS_MODULAR: Opening server in panel: ${item.activeServer.id}`);
                    await handleServerActions_1.ServerActionHandlers.openInPanel(item.activeServer.id);
                }
                break;
            case 'copyUrl':
                if (item.activeServer) {
                    console.log(`ACTIVE_SERVERS_MODULAR: Copying server URL: ${item.activeServer.id}`);
                    await handleServerActions_1.ServerActionHandlers.copyUrl(item.activeServer.id);
                }
                break;
            case 'showDetails':
                if (item.activeServer) {
                    console.log(`ACTIVE_SERVERS_MODULAR: Showing server details: ${item.activeServer.id}`);
                    await handleServerActions_1.ServerActionHandlers.showServerDetails(item.activeServer.id);
                }
                break;
            default:
                console.warn(`ACTIVE_SERVERS_MODULAR: Unknown context menu action: ${action}`);
        }
    }
}
exports.ActiveServerClickHandler = ActiveServerClickHandler;


/***/ }),
/* 186 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Babia Examples View Module
 * Exports for the modular Babia Examples section
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BabiaExampleClickHandler = exports.BabiaExampleItemFactory = exports.BabiaExampleTreeItem = exports.BabiaExamplesSectionProvider = void 0;
// Section Provider
var BabiaExamplesSectionProvider_1 = __webpack_require__(187);
Object.defineProperty(exports, "BabiaExamplesSectionProvider", ({ enumerable: true, get: function () { return BabiaExamplesSectionProvider_1.BabiaExamplesSectionProvider; } }));
// Items
var babiaExampleItems_1 = __webpack_require__(188);
Object.defineProperty(exports, "BabiaExampleTreeItem", ({ enumerable: true, get: function () { return babiaExampleItems_1.BabiaExampleTreeItem; } }));
Object.defineProperty(exports, "BabiaExampleItemFactory", ({ enumerable: true, get: function () { return babiaExampleItems_1.BabiaExampleItemFactory; } }));
// Interactions
var handleBabiaExampleClicks_1 = __webpack_require__(190);
Object.defineProperty(exports, "BabiaExampleClickHandler", ({ enumerable: true, get: function () { return handleBabiaExampleClicks_1.BabiaExampleClickHandler; } }));


/***/ }),
/* 187 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BabiaExamplesSectionProvider = void 0;
const vscode = __importStar(__webpack_require__(1));
const baseInterfaces_1 = __webpack_require__(177);
const babiaExampleItems_1 = __webpack_require__(188);
const handleBabiaExampleClicks_1 = __webpack_require__(190);
const exampleLauncher_1 = __webpack_require__(45);
/**
 * Babia Examples section provider - manages example loading and launching
 */
class BabiaExamplesSectionProvider extends baseInterfaces_1.BaseSectionProvider {
    clickHandler;
    exampleLauncher;
    constructor(context) {
        super(context);
        console.log('BABIA_EXAMPLES_MODULAR: Initializing Babia Examples section provider');
        this.clickHandler = new handleBabiaExampleClicks_1.BabiaExampleClickHandler(context);
        this.exampleLauncher = new exampleLauncher_1.ExampleLauncher(context);
    }
    getSectionName() {
        return 'babiaExamples';
    }
    getSectionItem() {
        return new babiaExampleItems_1.BabiaExampleTreeItem('BABIA EXAMPLES', vscode.TreeItemCollapsibleState.Collapsed, 'no-examples', undefined, new vscode.ThemeIcon('library'), 'Interactive visualization examples', undefined, 'babiaExamplesSection');
    }
    async getChildren(element) {
        if (element) {
            return [];
        }
        console.log('BABIA_EXAMPLES_MODULAR: Loading Babia examples section children');
        try {
            const examples = await this.exampleLauncher.getExamples();
            if (examples.length === 0) {
                console.log('BABIA_EXAMPLES_MODULAR: No examples found');
                return [babiaExampleItems_1.BabiaExampleItemFactory.createNoExamplesItem()];
            }
            console.log(`BABIA_EXAMPLES_MODULAR: Found ${examples.length} examples`);
            const children = babiaExampleItems_1.BabiaExampleItemFactory.createSortedExampleItems(examples);
            console.log(`BABIA_EXAMPLES_MODULAR: Returning ${children.length} children for Babia Examples section`);
            return children;
        }
        catch (error) {
            console.error('BABIA_EXAMPLES_MODULAR: Error loading Babia examples:', error);
            return [babiaExampleItems_1.BabiaExampleItemFactory.createErrorItem()];
        }
    }
    async handleClick(item) {
        await this.clickHandler.handleBabiaExampleClick(item);
    }
    async handleContextMenu(action, item) {
        await this.clickHandler.handleContextMenuAction(action, item);
    }
}
exports.BabiaExamplesSectionProvider = BabiaExamplesSectionProvider;


/***/ }),
/* 188 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BabiaExampleItemFactory = exports.BabiaExampleTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
const exampleItems_1 = __webpack_require__(189);
/**
 * Babia Example tree items for the Babia Examples section
 */
class BabiaExampleTreeItem extends vscode.TreeItem {
    babiaItemType;
    babiaExample;
    constructor(label, collapsibleState, babiaItemType, command, iconPath, tooltip, description, contextValue, babiaExample) {
        super(label, collapsibleState);
        this.babiaItemType = babiaItemType;
        this.babiaExample = babiaExample;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip || label;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.BabiaExampleTreeItem = BabiaExampleTreeItem;
/**
 * Factory for creating Babia example-related tree items
 */
class BabiaExampleItemFactory {
    /**
     * Create "No examples found" message item
     */
    static createNoExamplesItem() {
        console.log('BABIA_EXAMPLES: Creating "No examples found" message item');
        return new BabiaExampleTreeItem('No examples found', vscode.TreeItemCollapsibleState.None, 'no-examples', undefined, new vscode.ThemeIcon('warning'), 'No Babia examples are available');
    }
    /**
     * Create "Error loading examples" message item
     */
    static createErrorItem() {
        console.log('BABIA_EXAMPLES: Creating error loading examples item');
        return new BabiaExampleTreeItem('Error loading examples', vscode.TreeItemCollapsibleState.None, 'error', undefined, new vscode.ThemeIcon('error'), 'Failed to load Babia examples');
    }
    /**
     * Create individual Babia example item
     */
    static createExampleItem(example) {
        console.log(`BABIA_EXAMPLES: Creating example item: ${example.name} (${example.category})`);
        const icon = BabiaExampleItemFactory.getExampleIcon(example);
        const statusSuffix = example.isValid ? '' : ' (Invalid)';
        const label = `${example.name}${statusSuffix}`;
        // Create command to launch example (only if valid)
        const command = example.isValid ? {
            command: 'codeXR.babiaExamples.launchExample',
            title: 'Launch Example',
            arguments: [example]
        } : undefined;
        const tooltip = example.isValid ?
            `${example.category} example - Click to launch` :
            `${example.category} example - Invalid configuration`;
        return new BabiaExampleTreeItem(label, vscode.TreeItemCollapsibleState.None, 'example-item', command, icon, tooltip, example.category, 'babia-example', example);
    }
    /**
     * Get the appropriate icon for a Babia example
     */
    static getExampleIcon(example) {
        // Use the existing ExampleIcons mapping
        return exampleItems_1.ExampleIcons.getExampleIcon(example.category);
    }
    /**
     * Create sorted example items from a list of examples
     */
    static createSortedExampleItems(examples) {
        console.log(`BABIA_EXAMPLES: Creating sorted tree items for ${examples.length} examples`);
        // Sort examples by category and name
        const sortedExamples = examples.sort((a, b) => {
            if (a.category !== b.category) {
                return a.category.localeCompare(b.category);
            }
            return a.name.localeCompare(b.name);
        });
        return sortedExamples.map(example => BabiaExampleItemFactory.createExampleItem(example));
    }
}
exports.BabiaExampleItemFactory = BabiaExampleItemFactory;


/***/ }),
/* 189 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ExampleIcons = exports.ExampleItemFactory = exports.BabiaExampleTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
/**
 * Tree item for Babia examples display
 */
class BabiaExampleTreeItem extends vscode.TreeItem {
    label;
    collapsibleState;
    type;
    command;
    iconPath;
    tooltip;
    description;
    contextValue;
    example;
    constructor(label, collapsibleState, type, command, iconPath, tooltip, description, contextValue, example) {
        super(label, collapsibleState);
        this.label = label;
        this.collapsibleState = collapsibleState;
        this.type = type;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip;
        this.description = description;
        this.contextValue = contextValue;
        this.example = example;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.BabiaExampleTreeItem = BabiaExampleTreeItem;
/**
 * Example item factory for creating tree items
 */
class ExampleItemFactory {
    /**
     * Create tree item for a Babia example
     */
    static createExampleItem(example) {
        const command = {
            command: 'codeXR.babiaExamples.launchExample',
            title: 'Launch Example',
            arguments: [example]
        };
        const icon = ExampleIcons.getExampleIcon(example.category);
        const tooltip = ExampleItemFactory.createTooltip(example);
        const description = example.isValid ? undefined : '(Invalid)';
        return new BabiaExampleTreeItem(example.name, vscode.TreeItemCollapsibleState.None, 'example', command, icon, tooltip, description, example.isValid ? 'validExample' : 'invalidExample', example);
    }
    /**
     * Create "No examples found" item
     */
    static createNoExamplesItem() {
        return new BabiaExampleTreeItem('No examples found', vscode.TreeItemCollapsibleState.None, 'noExamples', undefined, new vscode.ThemeIcon('info'), 'No Babia examples were found in examples/charts/', undefined, 'noExamples');
    }
    /**
     * Create loading item
     */
    static createLoadingItem() {
        return new BabiaExampleTreeItem('Loading examples...', vscode.TreeItemCollapsibleState.None, 'loading', undefined, new vscode.ThemeIcon('loading~spin'), 'Scanning for Babia examples', undefined, 'loading');
    }
    /**
     * Create tooltip for example
     * @private
     */
    static createTooltip(example) {
        const lines = [
            `Example: ${example.name}`,
            `Category: ${example.category}`,
            `File: ${example.htmlFilePath}`
        ];
        if (example.description) {
            lines.push(`Description: ${example.description}`);
        }
        if (!example.isValid) {
            lines.push('⚠️ This example has issues and may not work properly');
        }
        else {
            lines.push('✅ Click to launch this example');
        }
        return lines.join('\\n');
    }
}
exports.ExampleItemFactory = ExampleItemFactory;
/**
 * Example icons utility
 */
class ExampleIcons {
    /**
     * Get appropriate icon for example category
     */
    static getExampleIcon(category) {
        switch (category.toLowerCase()) {
            case 'pie':
                return new vscode.ThemeIcon('pie-chart');
            case 'bar-chart':
            case 'barsmap':
                return new vscode.ThemeIcon('graph');
            case 'bubble-chart':
                return new vscode.ThemeIcon('circle-large-outline');
            case 'cylinder-chart':
            case 'cylindermap-chart':
                return new vscode.ThemeIcon('package');
            case 'mix':
                return new vscode.ThemeIcon('combine');
            default:
                return new vscode.ThemeIcon('file-code');
        }
    }
    /**
     * Get section icon
     */
    static getSectionIcon() {
        return new vscode.ThemeIcon('library');
    }
}
exports.ExampleIcons = ExampleIcons;


/***/ }),
/* 190 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BabiaExampleClickHandler = void 0;
const vscode = __importStar(__webpack_require__(1));
const exampleLauncher_1 = __webpack_require__(45);
/**
 * Handler for Babia Examples section interactions
 */
class BabiaExampleClickHandler {
    context;
    exampleLauncher;
    constructor(context) {
        this.context = context;
        this.exampleLauncher = new exampleLauncher_1.ExampleLauncher(context);
    }
    /**
     * Handle clicks on Babia example items
     */
    async handleBabiaExampleClick(item) {
        console.log(`BABIA_EXAMPLES_MODULAR: Handling click on example item: ${item.label} (type: ${item.babiaItemType})`);
        switch (item.babiaItemType) {
            case 'example-item':
                await this.handleExampleItemClick(item);
                break;
            case 'no-examples':
            case 'loading':
            case 'error':
                // No action needed for informational items
                console.log(`BABIA_EXAMPLES_MODULAR: Clicked on informational item: ${item.babiaItemType}`);
                break;
            default:
                console.warn(`BABIA_EXAMPLES_MODULAR: Unknown example item type: ${item.babiaItemType}`);
        }
    }
    /**
     * Handle click on individual example item
     */
    async handleExampleItemClick(item) {
        if (!item.babiaExample) {
            console.error('BABIA_EXAMPLES_MODULAR: No example data in example item');
            return;
        }
        if (!item.babiaExample.isValid) {
            console.warn('BABIA_EXAMPLES_MODULAR: Attempted to launch invalid example');
            vscode.window.showWarningMessage(`Example "${item.babiaExample.name}" is invalid and cannot be launched.`);
            return;
        }
        console.log(`BABIA_EXAMPLES_MODULAR: Launching example: ${item.babiaExample.id}`);
        try {
            // Use the example launcher to handle the launch
            await this.exampleLauncher.launchExample(item.babiaExample);
            vscode.window.showInformationMessage(`Example "${item.babiaExample.name}" launched successfully!`);
        }
        catch (error) {
            console.error(`BABIA_EXAMPLES_MODULAR: Error launching example ${item.babiaExample.id}:`, error);
            vscode.window.showErrorMessage(`Failed to launch example "${item.babiaExample.name}": ${error}`);
        }
    }
    /**
     * Handle right-click context menu actions
     */
    async handleContextMenuAction(action, item) {
        console.log(`BABIA_EXAMPLES_MODULAR: Handling context menu action: ${action} on item: ${item.label}`);
        switch (action) {
            case 'refresh':
                console.log('BABIA_EXAMPLES_MODULAR: Refreshing examples view');
                // Refresh will be triggered by the provider
                break;
            case 'launchExample':
                if (item.babiaExample && item.babiaExample.isValid) {
                    console.log(`BABIA_EXAMPLES_MODULAR: Launching example from context menu: ${item.babiaExample.id}`);
                    await this.handleExampleItemClick(item);
                }
                break;
            case 'openInBrowser':
                if (item.babiaExample && item.babiaExample.isValid) {
                    console.log(`BABIA_EXAMPLES_MODULAR: Opening example in browser: ${item.babiaExample.id}`);
                    await this.launchExampleInBrowser(item.babiaExample);
                }
                break;
            case 'openInPanel':
                if (item.babiaExample && item.babiaExample.isValid) {
                    console.log(`BABIA_EXAMPLES_MODULAR: Opening example in panel: ${item.babiaExample.id}`);
                    await this.launchExampleInPanel(item.babiaExample);
                }
                break;
            case 'showDetails':
                if (item.babiaExample) {
                    console.log(`BABIA_EXAMPLES_MODULAR: Showing example details: ${item.babiaExample.id}`);
                    await this.showExampleDetails(item.babiaExample);
                }
                break;
            default:
                console.warn(`BABIA_EXAMPLES_MODULAR: Unknown context menu action: ${action}`);
        }
    }
    /**
     * Launch example specifically in browser
     */
    async launchExampleInBrowser(example) {
        try {
            // For now, use the default launcher - future enhancement could support launch mode selection
            await this.exampleLauncher.launchExample(example);
            vscode.window.showInformationMessage(`Example "${example.name}" launched! (Uses current user configuration for launch mode)`);
        }
        catch (error) {
            console.error(`BABIA_EXAMPLES_MODULAR: Error launching example:`, error);
            vscode.window.showErrorMessage(`Failed to launch example: ${error}`);
        }
    }
    /**
     * Launch example specifically in panel
     */
    async launchExampleInPanel(example) {
        try {
            // For now, use the default launcher - future enhancement could support launch mode selection
            await this.exampleLauncher.launchExample(example);
            vscode.window.showInformationMessage(`Example "${example.name}" launched! (Uses current user configuration for launch mode)`);
        }
        catch (error) {
            console.error(`BABIA_EXAMPLES_MODULAR: Error launching example:`, error);
            vscode.window.showErrorMessage(`Failed to launch example: ${error}`);
        }
    }
    /**
     * Show detailed information about an example
     */
    async showExampleDetails(example) {
        const details = `Example Details:
        
Name: ${example.name}
Category: ${example.category}
File: ${example.htmlFilePath}
Directory: ${example.directory}
Valid: ${example.isValid ? 'Yes' : 'No'}
${example.description ? `Description: ${example.description}` : ''}`;
        vscode.window.showInformationMessage(details, { modal: true });
    }
}
exports.BabiaExampleClickHandler = BabiaExampleClickHandler;


/***/ }),
/* 191 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Visualize Data View Module
 * Exports for the modular Visualize Data section
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizeDataClickHandler = exports.VisualizeDataModularItemFactory = exports.VisualizeDataModularTreeItem = exports.VisualizeDataSectionProvider = void 0;
// Section Provider
var VisualizeDataSectionProvider_1 = __webpack_require__(192);
Object.defineProperty(exports, "VisualizeDataSectionProvider", ({ enumerable: true, get: function () { return VisualizeDataSectionProvider_1.VisualizeDataSectionProvider; } }));
// Items
var visualizeDataItems_1 = __webpack_require__(193);
Object.defineProperty(exports, "VisualizeDataModularTreeItem", ({ enumerable: true, get: function () { return visualizeDataItems_1.VisualizeDataModularTreeItem; } }));
Object.defineProperty(exports, "VisualizeDataModularItemFactory", ({ enumerable: true, get: function () { return visualizeDataItems_1.VisualizeDataModularItemFactory; } }));
// Interactions
var handleVisualizeDataClicks_1 = __webpack_require__(196);
Object.defineProperty(exports, "VisualizeDataClickHandler", ({ enumerable: true, get: function () { return handleVisualizeDataClicks_1.VisualizeDataClickHandler; } }));


/***/ }),
/* 192 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizeDataSectionProvider = void 0;
const vscode = __importStar(__webpack_require__(1));
const visualizeDataItems_1 = __webpack_require__(193);
const handleVisualizeDataClicks_1 = __webpack_require__(196);
const visualizeDataState_1 = __webpack_require__(51);
/**
 * Visualize Data section provider - manages data visualization configuration and launch
 */
class VisualizeDataSectionProvider {
    context;
    _onDidChangeTreeData = new vscode.EventEmitter();
    onDidChangeTreeData = this._onDidChangeTreeData.event;
    clickHandler;
    constructor(context) {
        this.context = context;
        console.log('VISUALIZE_DATA_MODULAR: Initializing Visualize Data section provider');
        this.clickHandler = new handleVisualizeDataClicks_1.VisualizeDataClickHandler(context);
        // Listen to state changes if state manager is available
        if (visualizeDataState_1.VisualizeDataStateManager.hasInstance()) {
            const stateManager = visualizeDataState_1.VisualizeDataStateManager.getInstance(context);
            stateManager.onStateChanged(() => {
                console.log('VISUALIZE_DATA_MODULAR: Visualize data state changed, refreshing section');
                this.refresh();
            });
        }
    }
    /**
     * Get the section name for identification
     */
    getSectionName() {
        return 'visualizeData';
    }
    /**
     * Get the section header item
     */
    getSectionItem() {
        return new visualizeDataItems_1.VisualizeDataModularTreeItem('VISUALIZE DATA', vscode.TreeItemCollapsibleState.Collapsed, 'error', // Using this as section header type
        undefined, new vscode.ThemeIcon('open-preview', new vscode.ThemeColor('charts.foreground')), 'Data visualization configuration and launch', undefined, 'visualizeDataSection');
    }
    /**
     * Get children items for the Visualize Data section
     */
    async getChildren(element) {
        if (!element) {
            // Root level - return main visualize data items
            console.log('VISUALIZE_DATA_MODULAR: Loading visualize data section children');
            return visualizeDataItems_1.VisualizeDataModularItemFactory.createVisualizeDataItems(this.context);
        }
        // Handle sub-items for collapsible sections
        switch (element.visualizeDataItemType) {
            case 'dimension-mapping':
                return this.getDimensionMappingChildren();
            case 'browse-visualizations':
                return this.getBrowseVisualizationChildren();
            default:
                // Most items don't have children
                return [];
        }
    }
    /**
     * Get dimension mapping children
     */
    getDimensionMappingChildren() {
        console.log('VISUALIZE_DATA_MODULAR: Loading dimension mapping children');
        try {
            // Get current state if available
            let state = undefined;
            if (visualizeDataState_1.VisualizeDataStateManager.hasInstance()) {
                const stateManager = visualizeDataState_1.VisualizeDataStateManager.getInstance(this.context);
                state = stateManager.getState();
            }
            return visualizeDataItems_1.VisualizeDataModularItemFactory.createDimensionMappingItems(this.context, state);
        }
        catch (error) {
            console.error('VISUALIZE_DATA_MODULAR: Error loading dimension mapping items:', error);
            return [new visualizeDataItems_1.VisualizeDataModularTreeItem('Error loading dimensions', vscode.TreeItemCollapsibleState.None, 'error', undefined, new vscode.ThemeIcon('error'), 'Failed to load dimension items')];
        }
    }
    /**
     * Get browse visualization children
     */
    async getBrowseVisualizationChildren() {
        console.log('VISUALIZE_DATA_MODULAR: Loading browse visualization children');
        return visualizeDataItems_1.VisualizeDataModularItemFactory.createBrowseVisualizationItems(this.context);
    }
    /**
     * Refresh the section
     */
    refresh() {
        console.log('VISUALIZE_DATA_MODULAR: Refreshing Visualize Data section');
        this._onDidChangeTreeData.fire();
    }
    /**
     * Handle item clicks (additional method for interaction)
     */
    async handleClick(item) {
        await this.clickHandler.handleVisualizeDataClick(item);
    }
    /**
     * Handle context menu actions (additional method for interaction)
     */
    async handleContextMenu(action, item) {
        await this.clickHandler.handleContextMenuAction(action, item);
    }
}
exports.VisualizeDataSectionProvider = VisualizeDataSectionProvider;


/***/ }),
/* 193 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizeDataModularItemFactory = exports.VisualizeDataModularTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
const visualizeDataItems_1 = __webpack_require__(194);
const visualizationRestorer_1 = __webpack_require__(66);
const visualizationItem_1 = __webpack_require__(195);
/**
 * Visualize Data tree items for the Visualize Data section
 */
class VisualizeDataModularTreeItem extends vscode.TreeItem {
    visualizeDataItemType;
    visualizeDataItem;
    constructor(label, collapsibleState, visualizeDataItemType, command, iconPath, tooltip, description, contextValue, visualizeDataItem) {
        super(label, collapsibleState);
        this.visualizeDataItemType = visualizeDataItemType;
        this.visualizeDataItem = visualizeDataItem;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip || label;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.VisualizeDataModularTreeItem = VisualizeDataModularTreeItem;
/**
 * Factory for creating Visualize Data-related tree items
 */
class VisualizeDataModularItemFactory {
    /**
     * Create "Error loading visualize data" message item
     */
    static createErrorItem() {
        console.log('VISUALIZE_DATA_MODULAR: Creating error loading visualize data item');
        return new VisualizeDataModularTreeItem('Error loading visualize data', vscode.TreeItemCollapsibleState.None, 'error', undefined, new vscode.ThemeIcon('error'), 'Failed to load visualize data items');
    }
    /**
     * Create main visualize data items
     */
    static createVisualizeDataItems(context) {
        console.log('VISUALIZE_DATA_MODULAR: Creating visualize data items');
        try {
            const visualizeDataItems = visualizeDataItems_1.VisualizeDataItemFactory.createVisualizeDataItems(context);
            const children = visualizeDataItems.map(item => {
                // Handle collapsible dimension mapping and browse visualizations
                let collapsibleState = vscode.TreeItemCollapsibleState.None;
                let itemType = item.type;
                if (item.type === 'dimension-mapping' && item.collapsibleState === vscode.TreeItemCollapsibleState.Collapsed) {
                    collapsibleState = vscode.TreeItemCollapsibleState.Collapsed;
                }
                else if (item.type === 'browse-visualizations' && item.collapsibleState === vscode.TreeItemCollapsibleState.Collapsed) {
                    collapsibleState = vscode.TreeItemCollapsibleState.Collapsed;
                }
                return new VisualizeDataModularTreeItem(item.label, collapsibleState, itemType, item.command, item.iconPath, item.tooltip, item.description, item.contextValue, item);
            });
            console.log(`VISUALIZE_DATA_MODULAR: Created ${children.length} visualize data items`);
            return children;
        }
        catch (error) {
            console.error('VISUALIZE_DATA_MODULAR: Error creating visualize data items:', error);
            return [VisualizeDataModularItemFactory.createErrorItem()];
        }
    }
    /**
     * Create dimension mapping sub-items
     */
    static createDimensionMappingItems(context, state) {
        console.log('VISUALIZE_DATA_MODULAR: Creating dimension mapping items');
        try {
            // Provide a default state if none provided
            const stateToUse = state || {
                selectedChart: undefined,
                selectedJsonPath: undefined,
                selectedJsonName: undefined,
                jsonAnalysis: undefined,
                dimensionMappings: [],
                isDimensionMappingConfigured: false,
                isReadyToLaunch: false
            };
            const dimensionItems = visualizeDataItems_1.VisualizeDataItemFactory.createDimensionItems(stateToUse);
            const children = dimensionItems.map(item => {
                return new VisualizeDataModularTreeItem(item.label, vscode.TreeItemCollapsibleState.None, 'dimension-item', item.command, item.iconPath, item.tooltip, item.description, item.contextValue, item);
            });
            console.log(`VISUALIZE_DATA_MODULAR: Created ${children.length} dimension mapping items`);
            return children;
        }
        catch (error) {
            console.error('VISUALIZE_DATA_MODULAR: Error creating dimension mapping items:', error);
            return [new VisualizeDataModularTreeItem('Error loading dimensions', vscode.TreeItemCollapsibleState.None, 'error', undefined, new vscode.ThemeIcon('error'), 'Failed to load dimension items')];
        }
    }
    /**
     * Create browse visualizations sub-items
     */
    static async createBrowseVisualizationItems(context) {
        console.log('VISUALIZE_DATA_MODULAR: Creating browse visualization items');
        try {
            const visualizationRestorer = new visualizationRestorer_1.VisualizationRestorer(context);
            // Scan for stored visualizations
            const visualizations = await visualizationRestorer.scanStoredVisualizations();
            // Create items for visualizations
            const visualizationItems = visualizationItem_1.BrowseVisualizationItemFactory.createStoredVisualizationItems(visualizations);
            // Add reset button if there are visualizations
            if (visualizations.length > 0) {
                visualizationItems.push(visualizationItem_1.BrowseVisualizationItemFactory.createResetAllItem());
            }
            // Convert to modular tree items
            const children = visualizationItems.map((item) => {
                return new VisualizeDataModularTreeItem(item.label, vscode.TreeItemCollapsibleState.None, 'stored-visualization', item.command, item.iconPath, item.tooltip, item.description, item.contextValue);
            });
            console.log(`VISUALIZE_DATA_MODULAR: Created ${children.length} browse visualization items`);
            return children;
        }
        catch (error) {
            console.error('VISUALIZE_DATA_MODULAR: Error loading browse visualizations:', error);
            return [new VisualizeDataModularTreeItem('Error loading visualizations', vscode.TreeItemCollapsibleState.None, 'error', undefined, new vscode.ThemeIcon('error'), 'Failed to load stored visualizations')];
        }
    }
}
exports.VisualizeDataModularItemFactory = VisualizeDataModularItemFactory;


/***/ }),
/* 194 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizeDataIcons = exports.VisualizeDataItemFactory = exports.VisualizeDataTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
const visualizeDataState_1 = __webpack_require__(51);
/**
 * Tree item for visualize data items
 */
class VisualizeDataTreeItem extends vscode.TreeItem {
    label;
    collapsibleState;
    type;
    command;
    iconPath;
    tooltip;
    description;
    contextValue;
    constructor(label, collapsibleState, type, command, iconPath, tooltip, description, contextValue) {
        super(label, collapsibleState);
        this.label = label;
        this.collapsibleState = collapsibleState;
        this.type = type;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip;
        this.description = description;
        this.contextValue = contextValue;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.VisualizeDataTreeItem = VisualizeDataTreeItem;
/**
 * Factory for creating visualize data items
 */
class VisualizeDataItemFactory {
    /**
     * Create all visualize data items with current state
     */
    static createVisualizeDataItems(context) {
        console.log('BABIA-TEMPLATES: Creating visualize data items...');
        // Get current state if context is available and state manager exists
        let stateManager;
        let state;
        try {
            if (context && visualizeDataState_1.VisualizeDataStateManager.hasInstance()) {
                stateManager = visualizeDataState_1.VisualizeDataStateManager.getInstance(context);
                state = stateManager.getState();
                console.log('BABIA-TEMPLATES: Retrieved state from manager', {
                    hasChart: !!state.selectedChart,
                    chartName: state.selectedChart?.name,
                    hasJson: !!state.selectedJsonName,
                    jsonName: state.selectedJsonName
                });
            }
            else if (context) {
                // Try to initialize state manager if context is available
                stateManager = visualizeDataState_1.VisualizeDataStateManager.getInstance(context);
                state = stateManager.getState();
                console.log('BABIA-TEMPLATES: Initialized new state manager');
            }
            else {
                console.log('BABIA-TEMPLATES: No context available, using default state');
            }
        }
        catch (error) {
            // State manager not initialized yet, use default values
            console.log('BABIA-TEMPLATES: Error accessing state manager, using defaults:', error);
        }
        const chartDescription = state?.selectedChart
            ? `Selected: ${state.selectedChart.name}`
            : 'No chart selected';
        const jsonDescription = state?.selectedJsonName
            ? `Selected: ${state.selectedJsonName}`
            : 'No file selected';
        const dimensionDescription = state?.isDimensionMappingConfigured
            ? 'Configured'
            : 'Not configured';
        const launchDescription = state?.isReadyToLaunch
            ? 'Ready to launch'
            : 'Configure required settings';
        console.log('BABIA-TEMPLATES: Item descriptions:', {
            chart: chartDescription,
            json: jsonDescription,
            dimension: dimensionDescription,
            launch: launchDescription
        });
        return [
            // Chart Type
            new VisualizeDataTreeItem('Chart Type', vscode.TreeItemCollapsibleState.None, 'chart-type', {
                command: 'codeXR.visualizeData.chartType',
                title: 'Select Chart Type'
            }, new vscode.ThemeIcon('graph'), 'Select visualization chart type', chartDescription, 'visualize-data-chart-type'),
            // Select JSON File
            new VisualizeDataTreeItem('Select JSON File', vscode.TreeItemCollapsibleState.None, 'select-json', {
                command: 'codeXR.visualizeData.selectJson',
                title: 'Select JSON File'
            }, new vscode.ThemeIcon('file-code'), 'Select JSON data file for visualization', jsonDescription, 'visualize-data-select-json'),
            // Dimension Mapping
            VisualizeDataItemFactory.createDimensionMappingItem(state),
            // Launch Visualization - Icon changes based on readiness
            new VisualizeDataTreeItem('Launch Visualization', vscode.TreeItemCollapsibleState.None, 'launch-visualization', {
                command: 'codeXR.visualizeData.launchVisualization',
                title: 'Launch Visualization'
            }, state?.isReadyToLaunch
                ? new vscode.ThemeIcon('rocket') // Ready to launch - rocket icon
                : new vscode.ThemeIcon('warning', new vscode.ThemeColor('charts.yellow')), // Not ready - yellow warning
            'Launch the configured visualization', launchDescription, 'visualize-data-launch'),
            // Browse Visualizations
            new VisualizeDataTreeItem('Browse Visualizations', vscode.TreeItemCollapsibleState.Collapsed, 'browse-visualizations', undefined, // No command - expandable section
            new vscode.ThemeIcon('folder-opened'), 'Browse and launch previously generated visualizations', undefined, 'visualize-data-browse-visualizations')
        ];
    }
    /**
     * Create dimension mapping item with collapsible state
     */
    static createDimensionMappingItem(state) {
        if (!state?.selectedChart || !state?.jsonAnalysis) {
            return new VisualizeDataTreeItem('Dimension Mapping', vscode.TreeItemCollapsibleState.None, 'dimension-mapping', undefined, new vscode.ThemeIcon('settings-gear'), 'Select chart type and JSON file first', 'Not available', 'visualize-data-dimension-mapping');
        }
        const requiredCount = state.selectedChart.dimensions.filter(d => d.required).length;
        const mappedCount = state.dimensionMappings.length;
        const isConfigured = this.areRequiredDimensionsMapped(state);
        const description = isConfigured
            ? `Configured (${mappedCount}/${state.selectedChart.dimensions.length})`
            : `${mappedCount}/${requiredCount} required`;
        return new VisualizeDataTreeItem('Dimension Mapping', vscode.TreeItemCollapsibleState.Collapsed, 'dimension-mapping', undefined, // Remove command to allow expand/collapse behavior
        isConfigured
            ? new vscode.ThemeIcon('check', new vscode.ThemeColor('charts.green'))
            : new vscode.ThemeIcon('settings-gear'), 'Configure dimension mapping for visualization - Click to expand/collapse', description, 'visualize-data-dimension-mapping');
    }
    /**
     * Create dimension items for collapsible dimension mapping
     */
    static createDimensionItems(state) {
        if (!state.selectedChart) {
            return [];
        }
        return state.selectedChart.dimensions.map(dimension => this.createDimensionItem(dimension, state));
    }
    /**
     * Create individual dimension tree item
     */
    static createDimensionItem(dimension, state) {
        const currentMapping = state.dimensionMappings.find(m => m.dimension === dimension.name);
        const isRequired = dimension.required;
        // Check for duplicate field usage
        const isDuplicateField = currentMapping && this.isFieldUsedInOtherMappings(currentMapping.dataField, dimension.name, state);
        // Create label with status
        let label = `${dimension.name}`; // Use actual dimension name (key, size)
        let description = '';
        let tooltip = `${dimension.name}`;
        // Add field mapping status
        if (currentMapping) {
            description = `→ ${currentMapping.dataField}`;
            tooltip += `\nMapped to: ${currentMapping.dataField}`;
            if (isDuplicateField) {
                description += ' (duplicate)';
                tooltip += '\n⚠️ Warning: This field is used in multiple mappings';
                console.log(`DIMENSION-MAPPING: Duplicate field usage detected - '${currentMapping.dataField}' is used for multiple dimensions`);
            }
        }
        else {
            description = 'Not Mapped';
            tooltip += '\nNot mapped - Click to select field';
        }
        // Add data type suffix
        const dataTypeSuffix = dimension.dataType === 'numeric' ? ' (numeric only)' : ' (any value)';
        description += dataTypeSuffix;
        tooltip += `\nData type: ${dimension.dataType === 'numeric' ? 'numeric only' : 'any value'}`;
        // Set icon based on mapping status and requirement
        let iconPath;
        if (currentMapping && isDuplicateField) {
            iconPath = new vscode.ThemeIcon('warning', new vscode.ThemeColor('charts.red'));
        }
        else if (currentMapping) {
            iconPath = new vscode.ThemeIcon('check', new vscode.ThemeColor('charts.green'));
        }
        else if (isRequired) {
            iconPath = new vscode.ThemeIcon('warning', new vscode.ThemeColor('charts.orange'));
        }
        else {
            iconPath = new vscode.ThemeIcon('circle-outline');
        }
        return new VisualizeDataTreeItem(label, vscode.TreeItemCollapsibleState.None, 'dimension-item', {
            command: 'codeXR.visualizeData.mapDimensionField',
            title: 'Map Dimension Field',
            arguments: [dimension.name]
        }, iconPath, tooltip, description, 'visualize-data-dimension-item');
    }
    /**
     * Check if all required dimensions are mapped
     */
    static areRequiredDimensionsMapped(state) {
        if (!state.selectedChart) {
            return false;
        }
        const requiredDimensions = state.selectedChart.dimensions.filter(d => d.required);
        return requiredDimensions.every(dimension => state.dimensionMappings.some(mapping => mapping.dimension === dimension.name));
    }
    /**
     * Check if a field is used in other dimension mappings
     */
    static isFieldUsedInOtherMappings(fieldName, currentDimensionName, state) {
        return state.dimensionMappings.some(mapping => mapping.dataField === fieldName && mapping.dimension !== currentDimensionName);
    }
}
exports.VisualizeDataItemFactory = VisualizeDataItemFactory;
/**
 * Icons for visualize data items
 */
class VisualizeDataIcons {
    static chartType = new vscode.ThemeIcon('graph');
    static selectJson = new vscode.ThemeIcon('file-code');
    static dimensionMapping = new vscode.ThemeIcon('settings-gear');
    static launchVisualization = new vscode.ThemeIcon('play');
    static section = new vscode.ThemeIcon('chart-scatter');
}
exports.VisualizeDataIcons = VisualizeDataIcons;


/***/ }),
/* 195 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.BrowseVisualizationItemFactory = exports.BrowseVisualizationTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
/**
 * Tree item for browse visualizations
 */
class BrowseVisualizationTreeItem extends vscode.TreeItem {
    label;
    collapsibleState;
    type;
    visualization;
    command;
    iconPath;
    tooltip;
    description;
    contextValue;
    constructor(label, collapsibleState, type, visualization, command, iconPath, tooltip, description, contextValue) {
        super(label, collapsibleState);
        this.label = label;
        this.collapsibleState = collapsibleState;
        this.type = type;
        this.visualization = visualization;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip;
        this.description = description;
        this.contextValue = contextValue;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.BrowseVisualizationTreeItem = BrowseVisualizationTreeItem;
/**
 * Factory for creating browse visualization items
 */
class BrowseVisualizationItemFactory {
    /**
     * Create browse visualizations section
     */
    static createBrowseVisualizationsSection() {
        return new BrowseVisualizationTreeItem('Browse Visualizations', vscode.TreeItemCollapsibleState.Expanded, 'browse-section', undefined, undefined, new vscode.ThemeIcon('folder-opened'), 'Browse previously generated visualizations', undefined, 'browse-visualizations-section');
    }
    /**
     * Create items for stored visualizations
     */
    static createStoredVisualizationItems(visualizations) {
        if (visualizations.length === 0) {
            return [
                new BrowseVisualizationTreeItem('No visualizations found', vscode.TreeItemCollapsibleState.None, 'stored-visualization', undefined, undefined, new vscode.ThemeIcon('info'), 'No stored visualizations available. Generate some visualizations first.', undefined, 'no-visualizations')
            ];
        }
        return visualizations.map(visualization => {
            const isValid = visualization.isValid;
            const icon = isValid ? new vscode.ThemeIcon('play') : new vscode.ThemeIcon('warning');
            const tooltip = isValid
                ? `Launch visualization: ${visualization.name}\nPath: ${visualization.folderPath}`
                : `Invalid visualization: ${visualization.name}\nMissing required files in: ${visualization.folderPath}`;
            const description = isValid ? undefined : '⚠️ Invalid';
            return new BrowseVisualizationTreeItem(visualization.name, vscode.TreeItemCollapsibleState.None, 'stored-visualization', visualization, isValid ? {
                command: 'codeXR.browseVisualizations.launch',
                title: 'Launch Visualization',
                arguments: [visualization]
            } : undefined, icon, tooltip, description, 'stored-visualization');
        });
    }
    /**
     * Create reset all visualizations item
     */
    static createResetAllItem() {
        return new BrowseVisualizationTreeItem('Reset All Visualizations', vscode.TreeItemCollapsibleState.None, 'stored-visualization', undefined, {
            command: 'codeXR.browseVisualizations.resetAll',
            title: 'Reset All Visualizations',
            arguments: []
        }, new vscode.ThemeIcon('trash'), 'Delete all stored visualizations', undefined, 'reset-all-visualizations');
    }
}
exports.BrowseVisualizationItemFactory = BrowseVisualizationItemFactory;


/***/ }),
/* 196 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizeDataClickHandler = void 0;
const vscode = __importStar(__webpack_require__(1));
/**
 * Handler for Visualize Data section interactions
 */
class VisualizeDataClickHandler {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Handle clicks on visualize data items
     */
    async handleVisualizeDataClick(item) {
        console.log(`VISUALIZE_DATA_MODULAR: Handling click on visualize data item: ${item.label} (type: ${item.visualizeDataItemType})`);
        // For most visualize data items, the command is already attached to the tree item
        // and will be executed automatically by VS Code
        switch (item.visualizeDataItemType) {
            case 'chart-type':
                console.log('VISUALIZE_DATA_MODULAR: Chart type selection clicked');
                // Command: 'codeXR.visualizeData.chartType' is already attached
                break;
            case 'select-json':
                console.log('VISUALIZE_DATA_MODULAR: Select JSON file clicked');
                // Command: 'codeXR.visualizeData.selectJson' is already attached
                break;
            case 'dimension-mapping':
                console.log('VISUALIZE_DATA_MODULAR: Dimension mapping clicked');
                // This is a collapsible item, no direct action needed
                break;
            case 'dimension-item':
                console.log('VISUALIZE_DATA_MODULAR: Dimension item clicked');
                // Command varies per dimension item and is already attached
                break;
            case 'launch-visualization':
                console.log('VISUALIZE_DATA_MODULAR: Launch visualization clicked');
                // Command: 'codeXR.visualizeData.launchVisualization' is already attached
                break;
            case 'browse-visualizations':
                console.log('VISUALIZE_DATA_MODULAR: Browse visualizations clicked');
                // This is a collapsible item, no direct action needed
                break;
            case 'stored-visualization':
                console.log('VISUALIZE_DATA_MODULAR: Stored visualization clicked');
                // Command varies per stored visualization and is already attached
                break;
            case 'error':
                console.log('VISUALIZE_DATA_MODULAR: Error item clicked - no action');
                break;
            default:
                console.warn(`VISUALIZE_DATA_MODULAR: Unknown visualize data item type: ${item.visualizeDataItemType}`);
        }
    }
    /**
     * Handle right-click context menu actions
     */
    async handleContextMenuAction(action, item) {
        console.log(`VISUALIZE_DATA_MODULAR: Handling context menu action: ${action} on item: ${item.label}`);
        switch (action) {
            case 'refresh':
                console.log('VISUALIZE_DATA_MODULAR: Refreshing visualize data view');
                // Refresh will be triggered by the provider
                break;
            case 'reset':
                await this.handleResetAction(item);
                break;
            case 'configure':
                await this.handleConfigureAction(item);
                break;
            case 'launch':
                await this.handleLaunchAction(item);
                break;
            case 'details':
                await this.handleShowDetails(item);
                break;
            default:
                console.warn(`VISUALIZE_DATA_MODULAR: Unknown context menu action: ${action}`);
        }
    }
    /**
     * Handle reset action
     */
    async handleResetAction(item) {
        console.log(`VISUALIZE_DATA_MODULAR: Handling reset action for: ${item.label}`);
        switch (item.visualizeDataItemType) {
            case 'chart-type':
                // Reset chart type selection
                await vscode.commands.executeCommand('codeXR.visualizeData.resetChartType');
                break;
            case 'select-json':
                // Reset JSON file selection
                await vscode.commands.executeCommand('codeXR.visualizeData.resetJsonFile');
                break;
            case 'dimension-mapping':
                // Reset all dimension mappings
                await vscode.commands.executeCommand('codeXR.visualizeData.resetDimensions');
                break;
            default:
                vscode.window.showInformationMessage(`Reset not available for ${item.label}`);
        }
    }
    /**
     * Handle configure action
     */
    async handleConfigureAction(item) {
        console.log(`VISUALIZE_DATA_MODULAR: Handling configure action for: ${item.label}`);
        switch (item.visualizeDataItemType) {
            case 'chart-type':
                await vscode.commands.executeCommand('codeXR.visualizeData.chartType');
                break;
            case 'select-json':
                await vscode.commands.executeCommand('codeXR.visualizeData.selectJson');
                break;
            case 'dimension-item':
                // Execute the dimension item's command if available
                if (item.command) {
                    await vscode.commands.executeCommand(item.command.command, ...(item.command.arguments || []));
                }
                break;
            default:
                vscode.window.showInformationMessage(`Configuration not available for ${item.label}`);
        }
    }
    /**
     * Handle launch action
     */
    async handleLaunchAction(item) {
        console.log(`VISUALIZE_DATA_MODULAR: Handling launch action for: ${item.label}`);
        if (item.visualizeDataItemType === 'stored-visualization' && item.command) {
            // Launch stored visualization
            await vscode.commands.executeCommand(item.command.command, ...(item.command.arguments || []));
        }
        else {
            // Launch current visualization configuration
            await vscode.commands.executeCommand('codeXR.visualizeData.launchVisualization');
        }
    }
    /**
     * Show details about the item
     */
    async handleShowDetails(item) {
        console.log(`VISUALIZE_DATA_MODULAR: Showing details for: ${item.label}`);
        let details = `Visualize Data Item Details:

Name: ${item.label}
Type: ${item.visualizeDataItemType}
Description: ${item.description || 'No description available'}
Tooltip: ${item.tooltip || 'No tooltip available'}`;
        if (item.command) {
            details += `\nCommand: ${item.command.command}`;
        }
        if (item.contextValue) {
            details += `\nContext: ${item.contextValue}`;
        }
        vscode.window.showInformationMessage(details, { modal: true });
    }
}
exports.VisualizeDataClickHandler = VisualizeDataClickHandler;


/***/ }),
/* 197 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * New Code Analysis Section Provider
 * Tree data provider for the new code analysis view section
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.NewCodeAnalysisSectionProvider = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const handleNewCodeAnalysisClicks_1 = __webpack_require__(198);
const sessionRegistry_1 = __webpack_require__(32);
const subsections_1 = __webpack_require__(199);
// SIMPLIFIED: Direct data service instead of subsection provider
const activeAnalysesDataService_1 = __webpack_require__(213);
/**
 * TODO: Implement tree data provider for new code analysis
 * - Provide tree structure for analysis results
 * - Handle data refresh and updates
 * - Manage analysis state
 * - Integrate with analysis engine
 */
class NewCodeAnalysisSectionProvider {
    context;
    _onDidChangeTreeData = new vscode.EventEmitter();
    onDidChangeTreeData = this._onDidChangeTreeData.event;
    // SIMPLIFIED: Direct services instead of subsection providers
    activeAnalysesDataService;
    analysisSettingsSubsection;
    projectByLanguageSubsection;
    filesByLanguageSubsection;
    constructor(context) {
        this.context = context;
        console.log('NEW_CODE_ANALYSIS: Initializing New Code Analysis section provider');
        // SIMPLIFIED: Initialize direct services
        this.activeAnalysesDataService = activeAnalysesDataService_1.ActiveAnalysesDataService.getInstance();
        this.activeAnalysesDataService.initialize(context);
        // Note: ActiveAnalysesCommands will be initialized by the activeAnalysesDataService
        // No need to initialize it here as it's handled by the data service internally
        // Initialize subsections
        this.analysisSettingsSubsection = new subsections_1.AnalysisSettingsSubsectionProvider(context);
        this.projectByLanguageSubsection = new subsections_1.ProjectByLanguageSubsectionProvider(context);
        this.filesByLanguageSubsection = new subsections_1.FilesByLanguageSubsectionProvider(context);
        // Setup real-time refresh callback for Files by Language
        this.filesByLanguageSubsection.setRefreshCallback(() => {
            console.log('NEW_CODE_ANALYSIS: Files by Language triggered refresh');
            this.refresh();
        });
        // Set up listener for analysis session changes to auto-refresh UI
        const sessionRegistry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
        sessionRegistry.onSessionChanged((session) => {
            console.log(`NEW_CODE_ANALYSIS: Session ${session.id} changed to ${session.status} - auto-refreshing UI`);
            this.refresh();
        });
        // Commands are now registered centrally in src/commands/new_code_analysis
        // following the "nested dolls" architecture pattern
        console.log('NEW_CODE_ANALYSIS: Section provider initialized. Commands registered centrally.');
    }
    /**
     * Get the section name for identification
     */
    getSectionName() {
        return 'newCodeAnalysis';
    }
    /**
     * Get the section header item
     */
    getSectionItem() {
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('NEW ANALYSIS', vscode.TreeItemCollapsibleState.Expanded, 'section', undefined, // command
        new vscode.ThemeIcon('beaker'), 'New Analysis Tools and Settings', undefined, // description
        'newCodeAnalysis.section.main');
    }
    /**
     * Refresh the tree view and all subsections
     */
    refresh() {
        console.log('NEW_CODE_ANALYSIS: ========== REFRESH CALLED ==========');
        console.log('NEW_CODE_ANALYSIS: Refreshing all subsections');
        // SIMPLIFIED: Refresh direct data service instead of subsection
        this.activeAnalysesDataService.refresh();
        this.analysisSettingsSubsection.refresh();
        this.projectByLanguageSubsection.refresh();
        this.filesByLanguageSubsection.refresh();
        // Fire the tree data change event
        console.log('NEW_CODE_ANALYSIS: About to fire _onDidChangeTreeData');
        this._onDidChangeTreeData.fire();
        console.log('NEW_CODE_ANALYSIS: ========== REFRESH COMPLETE ==========');
    } /**
     * TODO: Get tree item representation
     */
    getTreeItem(element) {
        return element;
    }
    /**
     * Get children for tree element
     */
    async getChildren(element) {
        if (!element) {
            // Return root subsections - SIMPLIFIED: Direct data service call
            const subsections = await Promise.all([
                Promise.resolve(this.activeAnalysesDataService.getSubsectionItem()),
                this.analysisSettingsSubsection.getSubsectionItem(),
                this.projectByLanguageSubsection.getSubsectionItem(),
                this.filesByLanguageSubsection.getSubsectionItem()
            ]);
            return subsections;
        }
        // Handle children for specific subsections
        switch (element.contextValue) {
            case 'activeAnalysesSubsection':
                // SIMPLIFIED: Direct data service call with safe mapping
                try {
                    console.log('NEW_CODE_ANALYSIS: Getting active analyses children...');
                    const items = await this.activeAnalysesDataService.getChildren();
                    console.log('NEW_CODE_ANALYSIS: Received items:', items?.length || 0);
                    if (!items || !Array.isArray(items)) {
                        console.warn('NEW_CODE_ANALYSIS: activeAnalysesDataService.getChildren() returned invalid data:', typeof items);
                        return [];
                    }
                    const treeItems = [];
                    for (let i = 0; i < items.length; i++) {
                        const item = items[i];
                        if (!item) {
                            console.warn(`NEW_CODE_ANALYSIS: Found null/undefined item at index ${i}`);
                            continue;
                        }
                        try {
                            const treeItem = new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(item.label || 'Unknown Analysis', vscode.TreeItemCollapsibleState.None, 'analysis-result', undefined, // command
                            item.iconPath, item.description || '', item.description || '', item.contextValue || 'activeAnalysis');
                            // Store the original item for command access
                            treeItem.originalNewCodeAnalysisItem = item;
                            treeItems.push(treeItem);
                        }
                        catch (itemError) {
                            console.error(`NEW_CODE_ANALYSIS: Error creating tree item at index ${i}:`, itemError);
                        }
                    }
                    console.log('NEW_CODE_ANALYSIS: Created', treeItems.length, 'tree items');
                    return treeItems;
                }
                catch (error) {
                    console.error('NEW_CODE_ANALYSIS: Error getting active analyses children:', error);
                    return [];
                }
            case 'analysisSettingsSubsection':
                return this.analysisSettingsSubsection.getChildren();
            case 'projectByLanguageSubsection':
                return this.projectByLanguageSubsection.getChildren();
            case 'filesByLanguageSubsection':
                return this.filesByLanguageSubsection.getChildren();
            // Handle nested settings like dimension mapping
            case 'dimensionMappingFileSetting':
                return this.analysisSettingsSubsection.getSettingChildren(element);
            case 'dimensionMappingDirectorySetting':
                return this.analysisSettingsSubsection.getSettingChildren(element);
            case 'filesByLanguageSortingSetting':
                return this.analysisSettingsSubsection.getSettingChildren(element);
            case 'profileConfigurationSetting':
                return this.analysisSettingsSubsection.getSettingChildren(element);
            // Handle Files by Language nested children
            case 'unsupportedFilesGroup':
                return this.filesByLanguageSubsection.getNestedChildren(element);
            default:
                // Check if it's a language group
                if (element.contextValue?.startsWith('languageGroup_')) {
                    return this.filesByLanguageSubsection.getNestedChildren(element);
                }
                // Check if it's a project directory
                if (element.contextValue?.startsWith('projectDirectory_')) {
                    return this.projectByLanguageSubsection.getNestedChildren(element);
                }
                return Promise.resolve([]);
        }
    }
    /**
     * TODO: Handle item selection
     */
    async onItemSelected(item) {
        await handleNewCodeAnalysisClicks_1.NewCodeAnalysisInteractionHandler.handleItemClick(item);
    }
    /**
     * Dispose of resources
     */
    dispose() {
        console.log('NEW_CODE_ANALYSIS: Disposing New Code Analysis section provider');
        // Dispose of subsections
        if (this.filesByLanguageSubsection) {
            this.filesByLanguageSubsection.dispose();
        }
        // Note: Other subsections can also implement dispose if needed
    }
}
exports.NewCodeAnalysisSectionProvider = NewCodeAnalysisSectionProvider;


/***/ }),
/* 198 */
/***/ ((__unused_webpack_module, exports) => {


/**
 * New Code Analysis Interaction Handler
 * Handles user interactions with new code analysis tree items
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.NewCodeAnalysisInteractionHandler = void 0;
/**
 * TODO: Handle interactions with new code analysis tree items
 * - Click handlers for different item types
 * - Context menu actions
 * - Double-click behaviors
 * - Hover information
 */
class NewCodeAnalysisInteractionHandler {
    /**
     * TODO: Handle tree item clicks
     */
    static async handleItemClick(item) {
        switch (item.newCodeAnalysisItemType) {
            case 'analysis-result':
                // TODO: Handle analysis result click
                break;
            case 'file-item':
                // TODO: Handle file analysis click
                break;
            case 'method-item':
                // TODO: Handle method analysis click
                break;
            default:
                // TODO: Default click behavior
                break;
        }
    }
    /**
     * TODO: Handle context menu actions
     */
    static async handleContextMenuAction(action, item) {
        // TODO: Implementation
    }
    /**
     * TODO: Handle double-click actions
     */
    static async handleDoubleClick(item) {
        // TODO: Implementation
    }
    /**
     * TODO: Get hover information for item
     */
    static getHoverInfo(item) {
        // TODO: Implementation
        return undefined;
    }
}
exports.NewCodeAnalysisInteractionHandler = NewCodeAnalysisInteractionHandler;


/***/ }),
/* 199 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * New Code Analysis View Subsections Module
 * Central exports for all view subsections
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FilesByLanguageSubsectionProvider = exports.ProjectByLanguageSubsectionProvider = exports.AnalysisFileSetting = exports.AnalysisSettingsSubsectionProvider = void 0;
var analysis_settings_1 = __webpack_require__(200);
Object.defineProperty(exports, "AnalysisSettingsSubsectionProvider", ({ enumerable: true, get: function () { return analysis_settings_1.AnalysisSettingsSubsectionProvider; } }));
Object.defineProperty(exports, "AnalysisFileSetting", ({ enumerable: true, get: function () { return analysis_settings_1.AnalysisFileSetting; } }));
// SIMPLIFIED: Removed ActiveAnalysesSubsectionProvider (replaced by direct service usage)
var project_by_language_1 = __webpack_require__(207);
Object.defineProperty(exports, "ProjectByLanguageSubsectionProvider", ({ enumerable: true, get: function () { return project_by_language_1.ProjectByLanguageSubsectionProvider; } }));
var files_by_language_1 = __webpack_require__(210);
Object.defineProperty(exports, "FilesByLanguageSubsectionProvider", ({ enumerable: true, get: function () { return files_by_language_1.FilesByLanguageSubsectionProvider; } }));


/***/ }),
/* 200 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Analysis Settings Subsection Module
 * Exports for analysis settings subsection
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ProfileConfigurationSetting = exports.AutoAnalysisEnabledSetting = exports.AutoAnalysisDelaySetting = exports.ViewThemeSetting = exports.AnalysisDirectorySetting = exports.AnalysisFileSetting = exports.AnalysisSettingsSubsectionProvider = void 0;
var analysisSettingsSubsectionProvider_1 = __webpack_require__(201);
Object.defineProperty(exports, "AnalysisSettingsSubsectionProvider", ({ enumerable: true, get: function () { return analysisSettingsSubsectionProvider_1.AnalysisSettingsSubsectionProvider; } }));
var analysis_file_mode_1 = __webpack_require__(202);
Object.defineProperty(exports, "AnalysisFileSetting", ({ enumerable: true, get: function () { return analysis_file_mode_1.AnalysisFileSetting; } }));
var analysis_directory_mode_1 = __webpack_require__(203);
Object.defineProperty(exports, "AnalysisDirectorySetting", ({ enumerable: true, get: function () { return analysis_directory_mode_1.AnalysisDirectorySetting; } }));
var view_theme_1 = __webpack_require__(204);
Object.defineProperty(exports, "ViewThemeSetting", ({ enumerable: true, get: function () { return view_theme_1.ViewThemeSetting; } }));
var auto_analysis_delay_1 = __webpack_require__(205);
Object.defineProperty(exports, "AutoAnalysisDelaySetting", ({ enumerable: true, get: function () { return auto_analysis_delay_1.AutoAnalysisDelaySetting; } }));
var auto_analysis_enabled_1 = __webpack_require__(206);
Object.defineProperty(exports, "AutoAnalysisEnabledSetting", ({ enumerable: true, get: function () { return auto_analysis_enabled_1.AutoAnalysisEnabledSetting; } }));
var profile_configuration_1 = __webpack_require__(164);
Object.defineProperty(exports, "ProfileConfigurationSetting", ({ enumerable: true, get: function () { return profile_configuration_1.ProfileConfigurationSetting; } }));


/***/ }),
/* 201 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Analysis Settings Subsection Provider
 * Manages the Analysis Settings subsection of New Code Analysis
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisSettingsSubsectionProvider = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const analysis_file_mode_1 = __webpack_require__(202);
const analysis_directory_mode_1 = __webpack_require__(203);
const view_theme_1 = __webpack_require__(204);
const chartTypeFile_1 = __webpack_require__(157);
const chartTypeDirectory_1 = __webpack_require__(158);
const dimensionMappingFile_1 = __webpack_require__(159);
const dimensionMappingDirectory_1 = __webpack_require__(161);
const auto_analysis_delay_1 = __webpack_require__(205);
const auto_analysis_enabled_1 = __webpack_require__(206);
const files_by_language_sorting_1 = __webpack_require__(162);
const profile_configuration_1 = __webpack_require__(164);
const analysisConfigurationStorage_1 = __webpack_require__(87);
class AnalysisSettingsSubsectionProvider {
    context;
    analysisFileSetting;
    analysisDirectorySetting;
    viewThemeSetting;
    chartTypeFileSetting;
    chartTypeDirectorySetting;
    dimensionMappingFileSetting;
    dimensionMappingDirectorySetting;
    autoAnalysisDelaySetting;
    autoAnalysisEnabledSetting;
    filesByLanguageSortingSetting;
    profileConfigurationSetting;
    constructor(context) {
        this.context = context;
        console.log('NEW_CODE_ANALYSIS: Initializing Analysis Settings subsection provider');
        // Initialize setting components
        this.analysisFileSetting = new analysis_file_mode_1.AnalysisFileSetting(context);
        this.analysisDirectorySetting = new analysis_directory_mode_1.AnalysisDirectorySetting(context);
        this.viewThemeSetting = new view_theme_1.ViewThemeSetting(context);
        this.chartTypeFileSetting = new chartTypeFile_1.ChartTypeFileSetting(context);
        this.chartTypeDirectorySetting = new chartTypeDirectory_1.ChartTypeDirectorySetting(context);
        this.dimensionMappingFileSetting = new dimensionMappingFile_1.DimensionMappingFileSetting(context);
        this.dimensionMappingDirectorySetting = new dimensionMappingDirectory_1.DimensionMappingDirectorySetting(context);
        this.autoAnalysisDelaySetting = new auto_analysis_delay_1.AutoAnalysisDelaySetting(context);
        this.autoAnalysisEnabledSetting = new auto_analysis_enabled_1.AutoAnalysisEnabledSetting(context);
        this.filesByLanguageSortingSetting = new files_by_language_sorting_1.FilesByLanguageSortingSetting(context);
        // Initialize profile configuration setting with storage
        const storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
        this.profileConfigurationSetting = new profile_configuration_1.ProfileConfigurationSetting(context, storage);
    }
    /**
     * Get subsection item for Analysis Settings
     */
    async getSubsectionItem() {
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Analysis Settings', vscode.TreeItemCollapsibleState.Expanded, // Expanded by default as requested
        'subsection', undefined, new vscode.ThemeIcon('gear'), 'Configure analysis behavior and preferences', 'Configuration options', 'analysisSettingsSubsection');
    }
    /**
     * Get children items for Analysis Settings
     */
    async getChildren() {
        console.log('NEW_CODE_ANALYSIS: Getting Analysis Settings children');
        try {
            const children = [];
            // Analysis File Mode setting
            const analysisFileItem = await this.analysisFileSetting.getSettingItem();
            children.push(analysisFileItem);
            // Analysis Directory Mode setting
            const analysisDirectoryItem = await this.analysisDirectorySetting.getSettingItem();
            children.push(analysisDirectoryItem);
            // View Theme setting
            const viewThemeItem = await this.viewThemeSetting.getSettingItem();
            children.push(viewThemeItem);
            // Chart Type (File) setting
            const chartTypeFileItem = await this.chartTypeFileSetting.getSettingItem();
            children.push(chartTypeFileItem);
            // Chart Type (Directory) setting - NEW
            const chartTypeDirectoryItem = await this.chartTypeDirectorySetting.getSettingItem();
            children.push(chartTypeDirectoryItem);
            // Dimension Mapping (File) setting
            const dimensionMappingFileItem = await this.dimensionMappingFileSetting.getSettingItem();
            children.push(dimensionMappingFileItem);
            // Dimension Mapping (Directory) setting - NEW
            console.log('NEW_CODE_ANALYSIS: Getting Dimension Mapping Directory setting item');
            const dimensionMappingDirectoryItem = await this.dimensionMappingDirectorySetting.getSettingItem();
            children.push(dimensionMappingDirectoryItem);
            console.log('NEW_CODE_ANALYSIS: Dimension Mapping Directory setting item added to children');
            // Auto Analysis Delay setting
            const autoAnalysisDelayItem = await this.autoAnalysisDelaySetting.getSettingItem();
            children.push(autoAnalysisDelayItem);
            // Auto Analysis Enabled setting
            const autoAnalysisEnabledItem = await this.autoAnalysisEnabledSetting.getSettingItem();
            children.push(autoAnalysisEnabledItem);
            // Files by Language Sorting setting - NEW
            const filesByLanguageSortingItem = await this.filesByLanguageSortingSetting.getSettingItem();
            children.push(filesByLanguageSortingItem);
            // Profile Configuration setting - NEW
            const profileConfigurationItem = await this.profileConfigurationSetting.getSettingItem();
            children.push(profileConfigurationItem);
            console.log(`NEW_CODE_ANALYSIS: Analysis Settings children count: ${children.length}`);
            return children;
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error getting Analysis Settings children:', error);
            // Return error item
            return [new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Error loading settings', vscode.TreeItemCollapsibleState.None, 'error', undefined, new vscode.ThemeIcon('error'), `Failed to load analysis settings: ${error}`, 'Configuration error', 'analysisSettingsError')];
        }
    }
    /**
     * Get specific children for a setting item (for nested items like Dimension Mapping)
     */
    async getSettingChildren(element) {
        console.log(`NEW_CODE_ANALYSIS: Getting children for setting: ${element.contextValue}`);
        try {
            switch (element.contextValue) {
                case 'dimensionMappingFileSetting':
                    // Return children for Dimension Mapping (File) - TODO1, TODO2, TODO3
                    return await this.dimensionMappingFileSetting.getChildren();
                case 'dimensionMappingDirectorySetting':
                    // Return children for Dimension Mapping (Directory)
                    return await this.dimensionMappingDirectorySetting.getChildren();
                case 'filesByLanguageSortingSetting':
                    // Return children for Files by Language Sorting
                    return await this.filesByLanguageSortingSetting.getChildren();
                case 'profileConfigurationSetting':
                    // Return children for Profile Configuration
                    return await this.profileConfigurationSetting.getChildren();
                default:
                    console.log(`NEW_CODE_ANALYSIS: No children available for setting: ${element.contextValue}`);
                    return [];
            }
        }
        catch (error) {
            console.error(`NEW_CODE_ANALYSIS: Error getting children for setting ${element.contextValue}:`, error);
            return [];
        }
    }
    /**
     * Refresh all settings (can be called when configuration changes)
     */
    async refresh() {
        console.log('NEW_CODE_ANALYSIS: Refreshing Analysis Settings subsection');
        // Settings will be refreshed automatically when getChildren() is called
    }
    /**
     * Get analysis file setting instance (for command integration)
     */
    getAnalysisFileSetting() {
        return this.analysisFileSetting;
    }
    /**
     * Get analysis directory setting instance (for command integration)
     */
    getAnalysisDirectorySetting() {
        return this.analysisDirectorySetting;
    }
    /**
     * Get view theme setting instance (for command integration)
     */
    getViewThemeSetting() {
        return this.viewThemeSetting;
    }
    /**
     * Get chart type file setting instance (for command integration)
     */
    getChartTypeFileSetting() {
        return this.chartTypeFileSetting;
    }
    /**
     * Get chart type directory setting instance (for command integration)
     */
    getChartTypeDirectorySetting() {
        return this.chartTypeDirectorySetting;
    }
    /**
     * Get dimension mapping file setting instance (for command integration)
     */
    getDimensionMappingFileSetting() {
        return this.dimensionMappingFileSetting;
    }
    /**
     * Get dimension mapping directory setting instance (for command integration)
     */
    getDimensionMappingDirectorySetting() {
        return this.dimensionMappingDirectorySetting;
    }
    /**
     * Get auto analysis delay setting instance (for command integration)
     */
    getAutoAnalysisDelaySetting() {
        return this.autoAnalysisDelaySetting;
    }
    /**
     * Get auto analysis enabled setting instance (for command integration)
     */
    getAutoAnalysisEnabledSetting() {
        return this.autoAnalysisEnabledSetting;
    }
    /**
     * Get files by language sorting setting instance (for command integration)
     */
    getFilesByLanguageSortingSetting() {
        return this.filesByLanguageSortingSetting;
    }
}
exports.AnalysisSettingsSubsectionProvider = AnalysisSettingsSubsectionProvider;


/***/ }),
/* 202 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Analysis File Mode Component
 * Exports for analysis file mode functionality
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisFileSetting = void 0;
var analysisFileMode_1 = __webpack_require__(149);
Object.defineProperty(exports, "AnalysisFileSetting", ({ enumerable: true, get: function () { return analysisFileMode_1.AnalysisFileSetting; } }));


/***/ }),
/* 203 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Analysis Directory Mode Module
 * Exports for analysis directory mode setting
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AnalysisDirectorySetting = void 0;
var analysisDirectoryMode_1 = __webpack_require__(153);
Object.defineProperty(exports, "AnalysisDirectorySetting", ({ enumerable: true, get: function () { return analysisDirectoryMode_1.AnalysisDirectorySetting; } }));


/***/ }),
/* 204 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * View Theme Setting Module
 * Exports for view theme setting functionality
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ViewThemeSetting = void 0;
var viewTheme_1 = __webpack_require__(154);
Object.defineProperty(exports, "ViewThemeSetting", ({ enumerable: true, get: function () { return viewTheme_1.ViewThemeSetting; } }));


/***/ }),
/* 205 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Auto-Analysis Delay Setting Module
 * Exports for auto-analysis delay setting functionality
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AutoAnalysisDelaySetting = void 0;
var autoAnalysisDelay_1 = __webpack_require__(155);
Object.defineProperty(exports, "AutoAnalysisDelaySetting", ({ enumerable: true, get: function () { return autoAnalysisDelay_1.AutoAnalysisDelaySetting; } }));


/***/ }),
/* 206 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Auto-Analysis Enabled Setting Module
 * Exports for auto-analysis enabled setting functionality
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.AutoAnalysisEnabledSetting = void 0;
var autoAnalysisEnabled_1 = __webpack_require__(156);
Object.defineProperty(exports, "AutoAnalysisEnabledSetting", ({ enumerable: true, get: function () { return autoAnalysisEnabled_1.AutoAnalysisEnabledSetting; } }));


/***/ }),
/* 207 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Project by Language Subsection Module
 * Exports for project by language subsection
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ProjectByLanguageSubsectionProvider = void 0;
var projectByLanguageSubsectionProvider_1 = __webpack_require__(208);
Object.defineProperty(exports, "ProjectByLanguageSubsectionProvider", ({ enumerable: true, get: function () { return projectByLanguageSubsectionProvider_1.ProjectByLanguageSubsectionProvider; } }));


/***/ }),
/* 208 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Project by Language Subsection Provider
 * Manages the Project Structure subsection showing hierarchical directory/file tree
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ProjectByLanguageSubsectionProvider = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const path = __importStar(__webpack_require__(5));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const workspaceFileScanner_1 = __webpack_require__(209);
const supportedLanguages_1 = __webpack_require__(35);
class ProjectByLanguageSubsectionProvider {
    context;
    constructor(context) {
        this.context = context;
        console.log('NEW_CODE_ANALYSIS: Initializing Project Structure subsection provider');
    }
    /**
     * Get the subsection header item
     */
    getSubsectionItem() {
        // Get project name from workspace
        const workspaceFolders = vscode.workspace.workspaceFolders;
        const projectName = workspaceFolders && workspaceFolders.length > 0
            ? path.basename(workspaceFolders[0].uri.fsPath)
            : 'Project Structure';
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(`Project Structure - ${projectName}`, vscode.TreeItemCollapsibleState.Collapsed, 'subsection', undefined, new vscode.ThemeIcon('folder-library', new vscode.ThemeColor('charts.foreground')), 'Project structure organized hierarchically', 'Browse and analyze files and directories', 'projectByLanguageSubsection');
    }
    /**
     * Get children for this subsection (root level files and directories)
     */
    async getChildren() {
        console.log('NEW_CODE_ANALYSIS: Getting Project Structure children');
        try {
            const workspaceFolders = vscode.workspace.workspaceFolders;
            if (!workspaceFolders || workspaceFolders.length === 0) {
                return [new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('No workspace folder open', vscode.TreeItemCollapsibleState.None, 'error', undefined, new vscode.ThemeIcon('error'), 'Open a folder to view project structure', undefined, 'error')];
            }
            const rootPath = workspaceFolders[0].uri.fsPath;
            const items = await this.getDirectoryItems(rootPath, true);
            console.log(`NEW_CODE_ANALYSIS: Found ${items.length} root items`);
            return items;
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error getting project structure:', error);
            return [new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Error reading project structure', vscode.TreeItemCollapsibleState.None, 'error', undefined, new vscode.ThemeIcon('error'), `Failed to read project: ${error}`, undefined, 'error')];
        }
    }
    /**
     * Get nested children for a specific directory item
     */
    async getNestedChildren(element) {
        console.log(`NEW_CODE_ANALYSIS: Getting nested children for: ${element.label}`);
        if (element.contextValue?.startsWith('projectDirectory_')) {
            const directoryPath = element.contextValue.replace('projectDirectory_', '');
            return await this.getDirectoryItems(directoryPath, false);
        }
        return [];
    }
    /**
     * Get items (files and directories) for a specific directory
     */
    async getDirectoryItems(directoryPath, isRoot = false) {
        const items = [];
        try {
            const entries = fs.readdirSync(directoryPath, { withFileTypes: true });
            // Sort: directories first, then files, both alphabetically
            const sortedEntries = entries.sort((a, b) => {
                if (a.isDirectory() && !b.isDirectory()) {
                    return -1;
                }
                if (!a.isDirectory() && b.isDirectory()) {
                    return 1;
                }
                return a.name.localeCompare(b.name);
            });
            for (const entry of sortedEntries) {
                // Skip hidden files/directories and common ignore patterns
                if (this.shouldSkipEntry(entry.name)) {
                    continue;
                }
                const fullPath = path.join(directoryPath, entry.name);
                if (entry.isDirectory()) {
                    const directoryItem = await this.createDirectoryItem(entry.name, fullPath);
                    items.push(directoryItem);
                }
                else {
                    const fileItem = await this.createFileItem(entry.name, fullPath);
                    items.push(fileItem);
                }
            }
        }
        catch (error) {
            console.error(`NEW_CODE_ANALYSIS: Error reading directory ${directoryPath}:`, error);
        }
        return items;
    }
    /**
     * Create a directory tree item
     */
    async createDirectoryItem(name, fullPath) {
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(name, vscode.TreeItemCollapsibleState.Collapsed, 'subsection', {
            command: 'newCodeAnalysis.projectStructure.analyzeDirectory',
            title: 'Analyze Directory',
            arguments: [fullPath]
        }, new vscode.ThemeIcon('folder', new vscode.ThemeColor('charts.foreground')), `Directory: ${name}`, `Click to analyze directory`, `projectDirectory_${fullPath}`);
    }
    /**
     * Create a file tree item
     */
    async createFileItem(name, fullPath) {
        const ext = path.extname(name).toLowerCase();
        const isSupported = (0, supportedLanguages_1.isSupportedExtension)(ext);
        if (isSupported) {
            // Supported file - use language icon and description
            const languageConfig = (0, supportedLanguages_1.getLanguageByExtension)(ext);
            const language = languageConfig?.name || 'Unknown';
            // Get language icon from resources using the language name
            let iconUri = workspaceFileScanner_1.WorkspaceFileScanner.getLanguageIconUri(this.context, language);
            return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(name, vscode.TreeItemCollapsibleState.None, 'subsection', {
                command: 'newCodeAnalysis.projectStructure.analyzeFile',
                title: 'Analyze File',
                arguments: [fullPath]
            }, iconUri, `${language} file: ${name}`, `Click to analyze file`, `projectFile_${fullPath}`);
        }
        else {
            // Unsupported file - use white file icon, no description
            return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(name, vscode.TreeItemCollapsibleState.None, 'subsection', undefined, // No command for unsupported files
            new vscode.ThemeIcon('file', new vscode.ThemeColor('charts.foreground')), name, // Just the filename, no description
            undefined, // No tooltip for unsupported files
            `projectFile_${fullPath}`);
        }
    }
    /**
     * Check if entry should be skipped
     */
    shouldSkipEntry(name) {
        const skipPatterns = [
            /^\./, // Hidden files/directories
            /^node_modules$/,
            /^\.git$/,
            /^\.vscode$/,
            /^dist$/,
            /^build$/,
            /^out$/,
            /^coverage$/,
            /^target$/,
            /^bin$/,
            /^obj$/,
            /^\.DS_Store$/,
            /^Thumbs\.db$/
        ];
        return skipPatterns.some(pattern => pattern.test(name));
    }
    /**
     * Handle refresh for this subsection
     */
    refresh() {
        // TODO: Refresh project by language data
        console.log('NEW_CODE_ANALYSIS: Refreshing Project by Language subsection');
    }
}
exports.ProjectByLanguageSubsectionProvider = ProjectByLanguageSubsectionProvider;


/***/ }),
/* 209 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Workspace File Scanner
 * Scans workspace for files and groups them by programming language
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.WorkspaceFileScanner = void 0;
const vscode = __importStar(__webpack_require__(1));
const languageMetadata_1 = __webpack_require__(138);
class WorkspaceFileScanner {
    /**
     * Scan workspace for all files and group by programming language
     */
    static async scanWorkspaceFiles() {
        console.log('WORKSPACE_FILE_SCANNER: Starting workspace file scan...');
        const supportedFiles = {};
        const unsupportedFiles = [];
        try {
            // Get all workspace folders
            const workspaceFolders = vscode.workspace.workspaceFolders;
            if (!workspaceFolders || workspaceFolders.length === 0) {
                console.log('WORKSPACE_FILE_SCANNER: No workspace folders found');
                return {
                    supportedFiles,
                    unsupportedFiles,
                    totalLanguages: 0,
                    totalFiles: 0
                };
            }
            // Define patterns to exclude
            const excludePatterns = [
                '**/node_modules/**',
                '**/.*/**',
                '**/.git/**',
                '**/dist/**',
                '**/build/**',
                '**/out/**',
                '**/coverage/**',
                '**/.vscode/**',
                '**/target/**',
                '**/__pycache__/**',
                '**/*.pyc',
                '**/*.pyo',
                '**/*.min.js',
                '**/*.min.css',
                '**/package-lock.json',
                '**/yarn.lock',
                '**/Cargo.lock',
                '**/Gemfile.lock'
            ];
            // Scan each workspace folder
            for (const folder of workspaceFolders) {
                console.log(`WORKSPACE_FILE_SCANNER: Scanning folder: ${folder.uri.fsPath}`);
                // Find all files, excluding common build/dependency directories
                const files = await vscode.workspace.findFiles(new vscode.RelativePattern(folder, '**/*'), `{${excludePatterns.join(',')}}`, 10000 // Limit to prevent performance issues
                );
                console.log(`WORKSPACE_FILE_SCANNER: Found ${files.length} files in ${folder.name}`);
                // Process each file
                for (const fileUri of files) {
                    const filePath = fileUri.fsPath;
                    const relativePath = vscode.workspace.asRelativePath(fileUri);
                    // Skip directories (just in case)
                    try {
                        const stat = await vscode.workspace.fs.stat(fileUri);
                        if (stat.type === vscode.FileType.Directory) {
                            continue;
                        }
                    }
                    catch (error) {
                        // Skip files that can't be accessed
                        continue;
                    }
                    // Detect language
                    const languageInfo = (0, languageMetadata_1.getLanguageForFile)(filePath);
                    if (languageInfo) {
                        // File is supported
                        if (!supportedFiles[languageInfo.name]) {
                            supportedFiles[languageInfo.name] = {
                                language: languageInfo,
                                files: []
                            };
                        }
                        supportedFiles[languageInfo.name].files.push(relativePath);
                    }
                    else {
                        // File is not supported for analysis
                        unsupportedFiles.push(relativePath);
                    }
                }
            }
            // Sort files within each language group
            Object.values(supportedFiles).forEach(languageGroup => {
                languageGroup.files.sort();
            });
            // Sort unsupported files
            unsupportedFiles.sort();
            const totalLanguages = Object.keys(supportedFiles).length;
            const totalFiles = Object.values(supportedFiles).reduce((sum, group) => sum + group.files.length, 0) + unsupportedFiles.length;
            console.log(`WORKSPACE_FILE_SCANNER: Scan complete - ${totalLanguages} languages, ${totalFiles} total files`);
            console.log(`WORKSPACE_FILE_SCANNER: Supported languages:`, Object.keys(supportedFiles));
            console.log(`WORKSPACE_FILE_SCANNER: Unsupported files count: ${unsupportedFiles.length}`);
            return {
                supportedFiles,
                unsupportedFiles,
                totalLanguages,
                totalFiles
            };
        }
        catch (error) {
            console.error('WORKSPACE_FILE_SCANNER: Error scanning workspace:', error);
            return {
                supportedFiles,
                unsupportedFiles,
                totalLanguages: 0,
                totalFiles: 0
            };
        }
    }
    /**
     * Get icon URI for a programming language
     */
    static getLanguageIconUri(context, languageName) {
        // Map language names to icon file names
        const iconMapping = {
            'Python': 'python.svg',
            'JavaScript': 'javascript.svg',
            'TypeScript': 'typescript.svg',
            'Java': 'java.svg',
            'C': 'c.svg',
            'C++': 'cplusplus.svg',
            'C#': 'csharp.svg',
            'Go': 'go.svg',
            'Ruby': 'ruby.svg',
            'PHP': 'php.svg',
            'Swift': 'swift.svg',
            'Kotlin': 'kotlin.svg',
            'Scala': 'scala.svg',
            'Lua': 'lua.svg',
            'Perl': 'perl.svg',
            'Erlang': 'erlang.svg',
            'Fortran': 'fortran.svg',
            'Objective-C': 'objectivec.svg',
            'Solidity': 'solidity.svg',
            'TTCN-3': 'ttcn3.svg',
            'Vue': 'vuejs.svg',
            'Zig': 'zig.svg',
            'HTML': 'html5.svg',
            'GDScript': 'godot.svg'
        };
        const iconFileName = iconMapping[languageName] || 'default.svg';
        return vscode.Uri.joinPath(context.extensionUri, 'resources', 'languages_icons', 'color', iconFileName);
    }
}
exports.WorkspaceFileScanner = WorkspaceFileScanner;


/***/ }),
/* 210 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Files by Language Subsection Module
 * Exports for files by language subsection
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FilesByLanguageSubsectionProvider = void 0;
var filesByLanguageSubsectionProvider_1 = __webpack_require__(211);
Object.defineProperty(exports, "FilesByLanguageSubsectionProvider", ({ enumerable: true, get: function () { return filesByLanguageSubsectionProvider_1.FilesByLanguageSubsectionProvider; } }));


/***/ }),
/* 211 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Files by Language Subsection Provider
 * Manages the Files by Language subsection of New Code Analysis
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FilesByLanguageSubsectionProvider = void 0;
const vscode = __importStar(__webpack_require__(1));
const newCodeAnalysisItems_1 = __webpack_require__(150);
const workspaceFileScanner_1 = __webpack_require__(209);
const filesByLanguageSortingUtils_1 = __webpack_require__(212);
const analysisConfigurationStorage_1 = __webpack_require__(87);
class FilesByLanguageSubsectionProvider {
    context;
    cachedSummary = null;
    lastScanTime = 0;
    CACHE_DURATION = 30000; // 30 seconds cache
    storage;
    fileWatcher = null;
    refreshCallback = null;
    constructor(context) {
        this.context = context;
        console.log('NEW_CODE_ANALYSIS: Initializing Files by Language subsection provider');
        this.storage = analysisConfigurationStorage_1.AnalysisConfigurationStorage.getInstance(context);
        this.setupFileWatcher();
    }
    /**
     * Get the subsection header item
     */
    async getSubsectionItem() {
        const summary = await this.getWorkspaceFilesSummary();
        const description = `(${summary.totalLanguages} languages - ${summary.totalFiles} files) - Individual files organized by programming language`;
        return new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Files by Language', vscode.TreeItemCollapsibleState.Collapsed, 'subsection', undefined, new vscode.ThemeIcon('folder'), // Changed to folder icon in white as requested
        description, // This goes to tooltip
        description, // This goes to description (visible text)
        'filesByLanguageSubsection');
    }
    /**
     * Get children for this subsection (language groups + unsupported files)
     */
    async getChildren() {
        console.log('NEW_CODE_ANALYSIS: Getting Files by Language children');
        const summary = await this.getWorkspaceFilesSummary();
        const sortingConfig = await this.storage.getFilesByLanguageSorting();
        const children = [];
        // Sort language groups according to configuration
        const sortedLanguageGroups = filesByLanguageSortingUtils_1.FilesByLanguageSortingUtils.sortLanguageGroups(summary.supportedFiles, sortingConfig);
        for (const languageGroup of sortedLanguageGroups) {
            const fileCount = languageGroup.fileCount;
            // Get icon for this language
            const iconUri = workspaceFileScanner_1.WorkspaceFileScanner.getLanguageIconUri(this.context, languageGroup.languageName);
            const languageItem = new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(languageGroup.languageName, vscode.TreeItemCollapsibleState.Collapsed, 'subsection', undefined, iconUri, `${fileCount} files`, `${fileCount} files of ${languageGroup.languageName}`, `languageGroup_${languageGroup.languageName}`);
            children.push(languageItem);
        }
        // Add unsupported files group if there are any
        if (summary.unsupportedFiles.length > 0) {
            const unsupportedItem = new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem('Unsupported Files', vscode.TreeItemCollapsibleState.Collapsed, 'subsection', undefined, new vscode.ThemeIcon('file-text', new vscode.ThemeColor('editorWarning.foreground')), `${summary.unsupportedFiles.length} files`, `${summary.unsupportedFiles.length} files not supported for analysis`, 'unsupportedFilesGroup');
            children.push(unsupportedItem);
        }
        console.log(`NEW_CODE_ANALYSIS: Files by Language children count: ${children.length}`);
        return children;
    }
    /**
     * Get files for a specific language group
     */
    async getLanguageGroupFiles(languageName) {
        console.log(`NEW_CODE_ANALYSIS: Getting files for language: ${languageName}`);
        const summary = await this.getWorkspaceFilesSummary();
        const languageGroup = summary.supportedFiles[languageName];
        if (!languageGroup) {
            console.log(`NEW_CODE_ANALYSIS: Language group not found: ${languageName}`);
            return [];
        }
        // Get sorting configuration and sort files
        const sortingConfig = await this.storage.getFilesByLanguageSorting();
        const sortedFiles = await filesByLanguageSortingUtils_1.FilesByLanguageSortingUtils.sortFilesInGroup(languageGroup.files, sortingConfig);
        const children = [];
        const iconUri = workspaceFileScanner_1.WorkspaceFileScanner.getLanguageIconUri(this.context, languageName);
        for (const filePath of sortedFiles) {
            const fileName = (__webpack_require__(5).basename)(filePath);
            const fileItem = new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(fileName, vscode.TreeItemCollapsibleState.None, 'file-item', {
                command: 'newCodeAnalysis.runAndAnalyzeFile',
                title: 'Run and Analyze File',
                arguments: [filePath, languageName]
            }, iconUri, filePath, `${vscode.workspace.asRelativePath(filePath)} - Click to run and analyze`, // Added file path before description
            `languageFile_${languageName}_${encodeURIComponent(filePath)}`);
            children.push(fileItem);
        }
        console.log(`NEW_CODE_ANALYSIS: Found ${children.length} files for ${languageName}`);
        return children;
    }
    /**
     * Get unsupported files
     */
    async getUnsupportedFiles() {
        console.log(`NEW_CODE_ANALYSIS: Getting unsupported files`);
        const summary = await this.getWorkspaceFilesSummary();
        // Sort unsupported files alphabetically for consistency
        const sortedUnsupportedFiles = [...summary.unsupportedFiles].sort();
        const children = [];
        for (const filePath of sortedUnsupportedFiles) {
            const fileName = (__webpack_require__(5).basename)(filePath);
            const fileExtension = (__webpack_require__(5).extname)(filePath);
            // Safely get workspace root path
            let fullFilePath = undefined;
            try {
                const workspaceFolders = vscode.workspace.workspaceFolders;
                if (workspaceFolders && workspaceFolders.length > 0) {
                    const workspaceRoot = workspaceFolders[0].uri.fsPath;
                    const resolvedPath = (__webpack_require__(5).resolve)(workspaceRoot, filePath);
                    fullFilePath = vscode.Uri.file(resolvedPath);
                }
                else {
                    // If no workspace folder, treat filePath as absolute
                    fullFilePath = vscode.Uri.file(filePath);
                }
            }
            catch (error) {
                console.error('FILES_BY_LANGUAGE: Error creating file URI for:', filePath, error);
                // Fallback to basic file URI
                try {
                    fullFilePath = vscode.Uri.file(filePath);
                }
                catch (fallbackError) {
                    console.error('FILES_BY_LANGUAGE: Failed to create fallback URI for:', filePath, fallbackError);
                    fullFilePath = undefined;
                }
            }
            const fileItem = new newCodeAnalysisItems_1.NewCodeAnalysisTreeItem(fileName, vscode.TreeItemCollapsibleState.None, 'file-item', fullFilePath ? {
                command: 'vscode.open',
                title: 'Open File',
                arguments: [fullFilePath]
            } : undefined, // Don't add command if URI creation failed
            new vscode.ThemeIcon('file-text'), filePath, `${fileExtension} file - Click to open`, `unsupportedFile_${encodeURIComponent(filePath)}`);
            children.push(fileItem);
        }
        console.log(`NEW_CODE_ANALYSIS: Found ${children.length} unsupported files`);
        return children;
    }
    /**
     * Get workspace files summary with caching
     */
    async getWorkspaceFilesSummary() {
        const now = Date.now();
        // Return cached result if still valid
        if (this.cachedSummary && (now - this.lastScanTime) < this.CACHE_DURATION) {
            console.log('NEW_CODE_ANALYSIS: Using cached workspace files summary');
            return this.cachedSummary;
        }
        // Scan workspace files
        console.log('NEW_CODE_ANALYSIS: Scanning workspace files...');
        this.cachedSummary = await workspaceFileScanner_1.WorkspaceFileScanner.scanWorkspaceFiles();
        this.lastScanTime = now;
        return this.cachedSummary;
    }
    /**
     * Force refresh of files data
     */
    async refresh() {
        console.log('NEW_CODE_ANALYSIS: Refreshing Files by Language subsection');
        this.cachedSummary = null;
        this.lastScanTime = 0;
        // Next call to getWorkspaceFilesSummary() will trigger a fresh scan
    }
    /**
     * Get specific children for nested items
     */
    async getNestedChildren(element) {
        console.log(`NEW_CODE_ANALYSIS: Getting nested children for: ${element.contextValue}`);
        if (element.contextValue?.startsWith('languageGroup_')) {
            const languageName = element.contextValue.replace('languageGroup_', '');
            return await this.getLanguageGroupFiles(languageName);
        }
        if (element.contextValue === 'unsupportedFilesGroup') {
            return await this.getUnsupportedFiles();
        }
        return [];
    }
    /**
     * Set refresh callback for real-time updates
     */
    setRefreshCallback(callback) {
        this.refreshCallback = callback;
    }
    /**
     * Setup file watcher for real-time updates
     */
    setupFileWatcher() {
        try {
            console.log('NEW_CODE_ANALYSIS: Setting up file watcher for Files by Language');
            // Create file watcher for all files in workspace
            this.fileWatcher = vscode.workspace.createFileSystemWatcher('**/*');
            // Handle file creation
            this.fileWatcher.onDidCreate((uri) => {
                console.log('NEW_CODE_ANALYSIS: File created:', uri.fsPath);
                this.handleFileChange();
            });
            // Handle file deletion
            this.fileWatcher.onDidDelete((uri) => {
                console.log('NEW_CODE_ANALYSIS: File deleted:', uri.fsPath);
                this.handleFileChange();
            });
            // Handle file renaming/moving (this triggers delete + create)
            // No need for onDidChange as we only care about structure changes
            // Add to context subscriptions for cleanup
            this.context.subscriptions.push(this.fileWatcher);
            console.log('NEW_CODE_ANALYSIS: File watcher setup completed');
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error setting up file watcher:', error);
        }
    }
    /**
     * Handle file system changes
     */
    handleFileChange() {
        try {
            console.log('NEW_CODE_ANALYSIS: Handling file system change for Files by Language');
            // Invalidate cache immediately
            this.cachedSummary = null;
            this.lastScanTime = 0;
            // Trigger refresh if callback is available
            if (this.refreshCallback) {
                console.log('NEW_CODE_ANALYSIS: Triggering Files by Language refresh');
                this.refreshCallback();
            }
        }
        catch (error) {
            console.error('NEW_CODE_ANALYSIS: Error handling file change:', error);
        }
    }
    /**
     * Dispose of resources
     */
    dispose() {
        console.log('NEW_CODE_ANALYSIS: Disposing Files by Language subsection provider');
        if (this.fileWatcher) {
            this.fileWatcher.dispose();
            this.fileWatcher = null;
        }
        this.refreshCallback = null;
        this.cachedSummary = null;
    }
}
exports.FilesByLanguageSubsectionProvider = FilesByLanguageSubsectionProvider;


/***/ }),
/* 212 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Files by Language Sorting Utility
 * Provides sorting functionality for Files by Language section
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.FilesByLanguageSortingUtils = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
class FilesByLanguageSortingUtils {
    /**
     * Sort language groups according to configuration
     */
    static sortLanguageGroups(filesByLanguage, sortingConfig) {
        // Convert to array with metadata
        const languageGroups = Object.entries(filesByLanguage).map(([languageName, group]) => ({
            languageName,
            files: group.files,
            fileCount: group.files.length,
            language: group.language
        }));
        // Apply primary sorting
        languageGroups.sort((a, b) => {
            const primaryResult = this.compareLanguageGroups(a, b, sortingConfig.languageGroupSortBy, sortingConfig.languageGroupSortDirection);
            // If primary comparison is equal and there's secondary sorting
            if (primaryResult === 0 && sortingConfig.languageGroupSecondarySortBy && sortingConfig.languageGroupSecondarySortDirection) {
                return this.compareLanguageGroups(a, b, sortingConfig.languageGroupSecondarySortBy, sortingConfig.languageGroupSecondarySortDirection);
            }
            return primaryResult;
        });
        return languageGroups;
    }
    /**
     * Sort files within a language group according to configuration
     */
    static async sortFilesInGroup(files, sortingConfig) {
        const sortedFiles = [...files];
        if (sortingConfig.filesSortBy === 'alphabetical') {
            // Alphabetical sorting
            sortedFiles.sort((a, b) => {
                const comparison = path.basename(a).localeCompare(path.basename(b));
                return sortingConfig.filesSortDirection === 'ascending' ? comparison : -comparison;
            });
        }
        else if (sortingConfig.filesSortBy === 'lastModified') {
            // Last modified sorting - need to get file stats
            const fileStats = await Promise.all(sortedFiles.map(async (filePath) => {
                try {
                    // Get absolute path for workspace file
                    const workspaceFolder = vscode.workspace.workspaceFolders?.[0];
                    if (!workspaceFolder) {
                        return { filePath, mtime: 0 };
                    }
                    const absolutePath = path.resolve(workspaceFolder.uri.fsPath, filePath);
                    const fileUri = vscode.Uri.file(absolutePath);
                    const stat = await vscode.workspace.fs.stat(fileUri);
                    return { filePath, mtime: stat.mtime };
                }
                catch (error) {
                    console.warn(`Failed to get stat for file: ${filePath}`, error);
                    return { filePath, mtime: 0 };
                }
            }));
            // Sort by modification time
            fileStats.sort((a, b) => {
                const comparison = a.mtime - b.mtime;
                return sortingConfig.filesSortDirection === 'ascending' ? comparison : -comparison;
            });
            return fileStats.map(stat => stat.filePath);
        }
        return sortedFiles;
    }
    /**
     * Compare two language groups for sorting
     */
    static compareLanguageGroups(a, b, sortBy, direction) {
        let comparison = 0;
        if (sortBy === 'alphabetical') {
            comparison = a.languageName.localeCompare(b.languageName);
        }
        else if (sortBy === 'fileCount') {
            comparison = a.fileCount - b.fileCount;
        }
        return direction === 'ascending' ? comparison : -comparison;
    }
}
exports.FilesByLanguageSortingUtils = FilesByLanguageSortingUtils;


/***/ }),
/* 213 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Active Analyses Data Service
 * Provides data for the Active Analyses TreeView
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.ActiveAnalysesDataService = void 0;
const vscode = __importStar(__webpack_require__(1));
const path = __importStar(__webpack_require__(5));
const sessionRegistry_1 = __webpack_require__(32);
class ActiveAnalysesDataService {
    static instance;
    _onDidChangeTreeData = new vscode.EventEmitter();
    onDidChangeTreeData = this._onDidChangeTreeData.event;
    sessionRegistry = null;
    constructor() {
        console.log('ACTIVE_ANALYSES_DATA_SERVICE: Initialized');
    }
    static getInstance() {
        if (!ActiveAnalysesDataService.instance) {
            ActiveAnalysesDataService.instance = new ActiveAnalysesDataService();
        }
        return ActiveAnalysesDataService.instance;
    }
    initialize(context) {
        if (!this.sessionRegistry) {
            this.sessionRegistry = sessionRegistry_1.UnifiedSessionRegistry.getInstance(context);
            // Listen to session changes
            this.sessionRegistry.onSessionChanged(() => {
                this.refresh();
            });
        }
    }
    /**
     * Refresh the tree view
     */
    refresh() {
        console.log('ACTIVE_ANALYSES_DATA_SERVICE: Refreshing tree view');
        this._onDidChangeTreeData.fire();
    }
    /**
     * Get tree children
     */
    getChildren(element) {
        if (!element) {
            // Root level - return all active analyses
            return Promise.resolve(this.getAllActiveAnalyses());
        }
        else {
            // No children for analysis items - flat list
            return Promise.resolve([]);
        }
    }
    /**
     * Get tree item
     */
    getTreeItem(element) {
        try {
            if (!element) {
                console.error('ACTIVE_ANALYSES_DATA_SERVICE: getTreeItem called with null/undefined element');
                return new vscode.TreeItem('Error: Invalid item');
            }
            const item = new vscode.TreeItem(element.label || 'Unknown Analysis');
            item.description = element.description;
            item.contextValue = element.contextValue;
            // Safely set resourceUri
            if (element.resourceUri) {
                try {
                    item.resourceUri = element.resourceUri;
                }
                catch (uriError) {
                    console.warn('ACTIVE_ANALYSES_DATA_SERVICE: Error setting resourceUri:', uriError);
                    item.resourceUri = undefined;
                }
            }
            item.iconPath = element.iconPath;
            // CRITICAL: Add sessionId to the TreeItem so commands can locate the session
            // VS Code passes the TreeItem object to context menu commands, not the original element
            item.sessionId = element.sessionId;
            item.id = element.id;
            item.analysisType = element.analysisType;
            item.status = element.status;
            item.targetPath = element.targetPath;
            item.serverUrl = element.serverUrl;
            item.assignedPort = element.assignedPort;
            console.log('ACTIVE_ANALYSES_DATA_SERVICE: Created TreeItem with session info:', {
                sessionId: element.sessionId,
                id: element.id,
                label: element.label,
                targetPath: element.targetPath
            });
            // Add command to show details when clicked - with safe argument handling
            item.command = {
                command: 'codeXR.new_code_analysis.activeAnalyses.showDetails',
                title: 'Show Details',
                arguments: element ? [element] : [] // Ensure arguments array is never undefined
            };
            // Analysis items are always leaf nodes (no children)
            item.collapsibleState = vscode.TreeItemCollapsibleState.None;
            return item;
        }
        catch (error) {
            console.error('ACTIVE_ANALYSES_DATA_SERVICE: Error in getTreeItem:', error);
            return new vscode.TreeItem('Error: Failed to create tree item');
        }
    }
    /**
     * Get all active analyses
     */
    getAllActiveAnalyses() {
        const analyses = [];
        try {
            if (!this.sessionRegistry) {
                console.warn('ACTIVE_ANALYSES_DATA_SERVICE: Session registry not initialized');
                return analyses;
            }
            // Get all sessions with safety check
            let allSessions;
            try {
                allSessions = this.sessionRegistry.getAllSessions();
            }
            catch (error) {
                console.error('ACTIVE_ANALYSES_DATA_SERVICE: Error getting sessions from registry:', error);
                return analyses;
            }
            if (!Array.isArray(allSessions)) {
                console.warn('ACTIVE_ANALYSES_DATA_SERVICE: getAllSessions() returned non-array:', typeof allSessions);
                return analyses;
            }
            console.log(`ACTIVE_ANALYSES_DATA_SERVICE: Found ${allSessions.length} sessions`);
            for (const session of allSessions) {
                if (!session) {
                    console.warn('ACTIVE_ANALYSES_DATA_SERVICE: Found null/undefined session in registry');
                    continue;
                }
                if (session.status !== 'closed') {
                    try {
                        // Determine type based on session properties
                        const type = session.targetType === 'directory' ? 'directory' : 'file';
                        const analysisItem = this.createAnalysisItem(session, type);
                        if (analysisItem) {
                            analyses.push(analysisItem);
                        }
                    }
                    catch (error) {
                        console.error('ACTIVE_ANALYSES_DATA_SERVICE: Error creating analysis item for session:', session.id, error);
                    }
                }
            }
        }
        catch (error) {
            console.error('ACTIVE_ANALYSES_DATA_SERVICE: Error getting analyses:', error);
        }
        console.log(`ACTIVE_ANALYSES_DATA_SERVICE: Returning ${analyses.length} active analyses`);
        return analyses;
    }
    /**
     * Create analysis item for tree view
     */
    createAnalysisItem(session, type) {
        try {
            if (!session) {
                console.warn('ACTIVE_ANALYSES_DATA_SERVICE: Cannot create analysis item - session is null/undefined');
                return null;
            }
            if (!session.id) {
                console.warn('ACTIVE_ANALYSES_DATA_SERVICE: Cannot create analysis item - session has no ID');
                return null;
            }
            // Use analysis type icon instead of status icon for better visual differentiation
            const analysisIcon = this.getAnalysisTypeIcon(session.analysisMode, type);
            const analysisTypeLabel = this.getAnalysisTypeLabel(session.analysisMode);
            // Safe property access with fallbacks
            const targetName = session.targetName || (session.targetPath ? path.basename(session.targetPath) : 'Unknown');
            const status = session.status || 'unknown';
            const analysisMode = session.analysisMode || 'unknown';
            const targetPath = session.targetPath;
            // Create resourceUri safely with validation
            let resourceUri = undefined;
            if (targetPath && typeof targetPath === 'string' && targetPath.trim().length > 0) {
                try {
                    // Validate the path doesn't contain illegal URI characters
                    const normalizedPath = path.normalize(targetPath.trim());
                    resourceUri = vscode.Uri.file(normalizedPath);
                }
                catch (uriError) {
                    console.warn('ACTIVE_ANALYSES_DATA_SERVICE: Invalid path for URI creation:', targetPath, uriError);
                    resourceUri = undefined;
                }
            }
            console.log('ACTIVE_ANALYSES_DATA_SERVICE: Creating analysis item for session:', {
                sessionId: session.id,
                targetName: targetName,
                targetPath: targetPath
            });
            return {
                id: session.id,
                sessionId: session.id, // For command compatibility
                label: targetName,
                description: `${analysisTypeLabel} - ${this.getStatusLabel(status)}`,
                analysisType: analysisMode,
                status: status,
                filePath: targetPath || undefined,
                targetPath: targetPath || undefined,
                serverUrl: session.serverUrl || undefined,
                assignedPort: session.assignedPort || undefined,
                resourceUri: resourceUri,
                // Fix contextValue to match package.json patterns - remove type from the middle
                contextValue: `activeAnalysis.${status}`,
                iconPath: analysisIcon,
                // SIMPLIFIED: Direct session reference for commands
                session: session
            };
        }
        catch (error) {
            console.error('ACTIVE_ANALYSES_DATA_SERVICE: Error creating analysis item:', error);
            return null;
        }
    }
    /**
     * Get status icon
     */
    getStatusIcon(status) {
        switch (status) {
            case 'creating':
                return new vscode.ThemeIcon('sync~spin');
            case 'analyzing':
                return new vscode.ThemeIcon('loading~spin');
            case 'monitoring':
            case 'completed':
                return new vscode.ThemeIcon('check');
            case 'error':
            case 'failed':
                return new vscode.ThemeIcon('error');
            case 'closed':
            case 'closing':
                return new vscode.ThemeIcon('close');
            default:
                return new vscode.ThemeIcon('file');
        }
    }
    /**
     * Get analysis type icon based on analysis mode and target type
     */
    getAnalysisTypeIcon(analysisMode, targetType) {
        if (targetType === 'file') {
            switch (analysisMode) {
                case 'LivePanel':
                case 'FileLivePanel':
                    // Verde para LivePanel de archivos
                    return new vscode.ThemeIcon('file', new vscode.ThemeColor('charts.green'));
                case 'XR':
                case 'FileXRAnalysis':
                    // Morado para XR de archivos
                    return new vscode.ThemeIcon('file', new vscode.ThemeColor('charts.purple'));
                case 'VisualizeDOM':
                case 'FileVisualizeDOM':
                    // Naranja para VisualizeDOM de archivos
                    return new vscode.ThemeIcon('file', new vscode.ThemeColor('charts.orange'));
                default:
                    return new vscode.ThemeIcon('file');
            }
        }
        else { // directory
            switch (analysisMode) {
                case 'LivePanel':
                case 'DirectoryLivePanel':
                case 'DeepDirectoryLivePanel':
                    // Verde para LivePanel de directorios
                    return new vscode.ThemeIcon('folder', new vscode.ThemeColor('charts.green'));
                case 'XR':
                case 'DirectoryXRAnalysis':
                case 'DeepDirectoryXRAnalysis':
                    // Morado para XR de directorios
                    return new vscode.ThemeIcon('folder', new vscode.ThemeColor('charts.purple'));
                case 'VisualizeDOM':
                    // Naranja para VisualizeDOM de directorios (poco común pero por si acaso)
                    return new vscode.ThemeIcon('folder', new vscode.ThemeColor('charts.orange'));
                default:
                    return new vscode.ThemeIcon('folder');
            }
        }
    }
    /**
     * Get status label
     */
    getStatusLabel(status) {
        switch (status) {
            case 'creating':
                return 'Creating...';
            case 'analyzing':
                return 'Analyzing...';
            case 'monitoring':
                return 'Monitoring';
            case 'completed':
                return 'Ready';
            case 'error':
                return 'Error';
            case 'failed':
                return 'Failed';
            case 'closed':
                return 'Closed';
            case 'closing':
                return 'Closing...';
            default:
                return status;
        }
    }
    /**
     * Get analysis type label
     */
    getAnalysisTypeLabel(analysisType) {
        switch (analysisType) {
            // New engine modes
            case 'LivePanel':
                return 'Live Panel';
            case 'XR':
                return 'XR Analysis';
            case 'VisualizeDOM':
                return 'Visualize DOM';
            // Legacy modes
            case 'FileLivePanel':
                return 'Live Panel';
            case 'FileVisualizeDOM':
                return 'Visualize DOM';
            case 'FileXRAnalysis':
                return 'XR Analysis';
            case 'DirectoryLivePanel':
                return 'Directory Live Panel';
            case 'DeepDirectoryLivePanel':
                return 'Deep Directory Live Panel';
            case 'DirectoryXRAnalysis':
                return 'Directory XR Analysis';
            case 'DeepDirectoryXRAnalysis':
                return 'Deep Directory XR Analysis';
            default:
                return analysisType;
        }
    }
    /**
     * Get analysis by ID
     */
    getAnalysisById(id) {
        try {
            if (!this.sessionRegistry) {
                console.warn('ACTIVE_ANALYSES_DATA_SERVICE: Session registry not initialized');
                return null;
            }
            return this.sessionRegistry.getSession(id);
        }
        catch (error) {
            console.error(`ACTIVE_ANALYSES_DATA_SERVICE: Error getting analysis ${id}:`, error);
            return null;
        }
    }
    /**
     * Get analysis count by status
     */
    getAnalysisCountByStatus() {
        const analyses = this.getAllActiveAnalyses();
        const counts = {
            creating: 0,
            analyzing: 0,
            monitoring: 0,
            completed: 0,
            error: 0,
            failed: 0,
            closed: 0,
            closing: 0,
            total: analyses.length
        };
        for (const analysis of analyses) {
            switch (analysis.status) {
                case 'creating':
                    counts.creating++;
                    break;
                case 'analyzing':
                    counts.analyzing++;
                    break;
                case 'monitoring':
                    counts.monitoring++;
                    break;
                case 'completed':
                    counts.completed++;
                    break;
                case 'error':
                    counts.error++;
                    break;
                case 'failed':
                    counts.failed++;
                    break;
                case 'closed':
                    counts.closed++;
                    break;
                case 'closing':
                    counts.closing++;
                    break;
            }
        }
        return counts;
    }
    /**
     * Get active analyses (alias for getAllActiveAnalyses for compatibility)
     */
    getActiveAnalyses() {
        return this.getAllActiveAnalyses();
    }
    /**
     * Get subsection item for the tree (SIMPLIFIED integration)
     */
    getSubsectionItem() {
        try {
            const activeCount = this.getAllActiveAnalyses().length;
            return {
                label: `Active Analyses (${activeCount})`,
                collapsibleState: 1, // Expanded
                contextValue: 'activeAnalysesSubsection',
                newCodeAnalysisItemType: 'subsection',
                iconPath: new vscode.ThemeIcon('play-circle', activeCount > 0 ? new vscode.ThemeColor('charts.green') : new vscode.ThemeColor('charts.gray')),
                tooltip: `${activeCount} active analysis sessions`,
                description: activeCount > 0 ? `${activeCount} running` : 'No active sessions'
            };
        }
        catch (error) {
            console.error('ACTIVE_ANALYSES_DATA_SERVICE: Error creating subsection item:', error);
            return {
                label: 'Active Analyses (Error)',
                collapsibleState: 1,
                contextValue: 'activeAnalysesSubsection',
                newCodeAnalysisItemType: 'subsection',
                iconPath: new vscode.ThemeIcon('error'),
                tooltip: 'Error loading active analyses',
                description: 'Error'
            };
        }
    }
}
exports.ActiveAnalysesDataService = ActiveAnalysesDataService;


/***/ }),
/* 214 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Visualization Settings View Module
 * Exports for the modular Visualization Settings section
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizationSettingsClickHandler = exports.VisualizationSettingsModularItemFactory = exports.VisualizationSettingsModularTreeItem = exports.VisualizationSettingsSectionProvider = void 0;
// Section Provider
var VisualizationSettingsSectionProvider_1 = __webpack_require__(215);
Object.defineProperty(exports, "VisualizationSettingsSectionProvider", ({ enumerable: true, get: function () { return VisualizationSettingsSectionProvider_1.VisualizationSettingsSectionProvider; } }));
// Items
var visualizationSettingsItems_1 = __webpack_require__(216);
Object.defineProperty(exports, "VisualizationSettingsModularTreeItem", ({ enumerable: true, get: function () { return visualizationSettingsItems_1.VisualizationSettingsModularTreeItem; } }));
Object.defineProperty(exports, "VisualizationSettingsModularItemFactory", ({ enumerable: true, get: function () { return visualizationSettingsItems_1.VisualizationSettingsModularItemFactory; } }));
// Interactions
var handleVisualizationSettingsClicks_1 = __webpack_require__(217);
Object.defineProperty(exports, "VisualizationSettingsClickHandler", ({ enumerable: true, get: function () { return handleVisualizationSettingsClicks_1.VisualizationSettingsClickHandler; } }));


/***/ }),
/* 215 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizationSettingsSectionProvider = void 0;
const vscode = __importStar(__webpack_require__(1));
const visualizationSettingsItems_1 = __webpack_require__(216);
const handleVisualizationSettingsClicks_1 = __webpack_require__(217);
const settingsStorage_1 = __webpack_require__(57);
/**
 * Visualization Settings section provider - manages visualization rendering preferences
 */
class VisualizationSettingsSectionProvider {
    context;
    _onDidChangeTreeData = new vscode.EventEmitter();
    onDidChangeTreeData = this._onDidChangeTreeData.event;
    clickHandler;
    visualizationSettingsStorage;
    constructor(context) {
        this.context = context;
        console.log('VISUALIZATION_SETTINGS_MODULAR: Initializing Visualization Settings section provider');
        this.clickHandler = new handleVisualizationSettingsClicks_1.VisualizationSettingsClickHandler(context);
        this.visualizationSettingsStorage = new settingsStorage_1.VisualizationSettingsStorage(context);
    }
    /**
     * Get the section name for identification
     */
    getSectionName() {
        return 'visualizationSettings';
    }
    /**
     * Get the section header item
     */
    getSectionItem() {
        return new visualizationSettingsItems_1.VisualizationSettingsModularTreeItem('VISUALIZATION SETTINGS', vscode.TreeItemCollapsibleState.Collapsed, 'error', // Using this as section header type
        undefined, new vscode.ThemeIcon('settings-gear'), 'Configure visualization rendering preferences', undefined, 'visualizationSettingsSection');
    }
    /**
     * Get children items for the Visualization Settings section
     */
    async getChildren(element) {
        // If element is provided, it means we're getting children for a specific item
        // For the Visualization Settings section, we only have flat items, so return empty for sub-items
        if (element) {
            return [];
        }
        console.log('VISUALIZATION_SETTINGS_MODULAR: Loading visualization settings section children with dynamic color icons');
        try {
            const currentSettings = this.visualizationSettingsStorage.getSettings();
            console.log(`VISUALIZATION_SETTINGS_MODULAR: Loading settings for dynamic icons: ${JSON.stringify(currentSettings)}`);
            const children = await visualizationSettingsItems_1.VisualizationSettingsModularItemFactory.createVisualizationSettingsItems(currentSettings, this.context);
            console.log(`VISUALIZATION_SETTINGS_MODULAR: Created ${children.length} visualization settings items with dynamic icons`);
            return children;
        }
        catch (error) {
            console.error('VISUALIZATION_SETTINGS_MODULAR: Error loading visualization settings items:', error);
            return [visualizationSettingsItems_1.VisualizationSettingsModularItemFactory.createErrorItem()];
        }
    }
    /**
     * Refresh the section
     */
    refresh() {
        console.log('VISUALIZATION_SETTINGS_MODULAR: Refreshing Visualization Settings section');
        this._onDidChangeTreeData.fire();
    }
    /**
     * Handle item clicks (additional method for interaction)
     */
    async handleClick(item) {
        await this.clickHandler.handleVisualizationSettingsClick(item);
    }
    /**
     * Handle context menu actions (additional method for interaction)
     */
    async handleContextMenu(action, item) {
        await this.clickHandler.handleContextMenuAction(action, item);
    }
    /**
     * Get current settings (for external access)
     */
    getCurrentSettings() {
        return this.visualizationSettingsStorage.getSettings();
    }
    /**
     * Update a single setting (for external access)
     */
    async updateSetting(key, value) {
        await this.visualizationSettingsStorage.updateSetting(key, value);
        this.refresh();
    }
}
exports.VisualizationSettingsSectionProvider = VisualizationSettingsSectionProvider;


/***/ }),
/* 216 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizationSettingsModularItemFactory = exports.VisualizationSettingsModularTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
const visualizationSettingsItems_1 = __webpack_require__(58);
/**
 * Visualization Settings tree items for the Visualization Settings section
 */
class VisualizationSettingsModularTreeItem extends vscode.TreeItem {
    visualizationSettingsItemType;
    originalSettingsItem;
    constructor(label, collapsibleState, visualizationSettingsItemType, command, iconPath, tooltip, description, contextValue, originalSettingsItem) {
        super(label, collapsibleState);
        this.visualizationSettingsItemType = visualizationSettingsItemType;
        this.originalSettingsItem = originalSettingsItem;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip || label;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.VisualizationSettingsModularTreeItem = VisualizationSettingsModularTreeItem;
/**
 * Factory for creating Visualization Settings-related tree items
 */
class VisualizationSettingsModularItemFactory {
    /**
     * Create "Error loading settings" message item
     */
    static createErrorItem() {
        console.log('VISUALIZATION_SETTINGS_MODULAR: Creating error loading settings item');
        return new VisualizationSettingsModularTreeItem('Error loading settings', vscode.TreeItemCollapsibleState.None, 'error', undefined, new vscode.ThemeIcon('error'), 'Failed to load visualization settings');
    }
    /**
     * Create visualization settings items with dynamic color icons
     */
    static async createVisualizationSettingsItems(settings, context) {
        console.log('VISUALIZATION_SETTINGS_MODULAR: Creating visualization settings items with dynamic color icons');
        try {
            const settingsItems = await visualizationSettingsItems_1.VisualizationSettingsItemFactory.createVisualizationSettingsItems(settings, context);
            const children = settingsItems.map(item => {
                return new VisualizationSettingsModularTreeItem(item.label, vscode.TreeItemCollapsibleState.None, 'settings-field', item.command, item.iconPath, item.tooltip, item.description, item.contextValue, item);
            });
            console.log(`VISUALIZATION_SETTINGS_MODULAR: Created ${children.length} visualization settings items with dynamic icons`);
            return children;
        }
        catch (error) {
            console.error('VISUALIZATION_SETTINGS_MODULAR: Error creating visualization settings items:', error);
            return [VisualizationSettingsModularItemFactory.createErrorItem()];
        }
    }
}
exports.VisualizationSettingsModularItemFactory = VisualizationSettingsModularItemFactory;


/***/ }),
/* 217 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizationSettingsClickHandler = void 0;
const vscode = __importStar(__webpack_require__(1));
/**
 * Handler for Visualization Settings section interactions
 */
class VisualizationSettingsClickHandler {
    context;
    constructor(context) {
        this.context = context;
    }
    /**
     * Handle clicks on visualization settings items
     */
    async handleVisualizationSettingsClick(item) {
        console.log(`VISUALIZATION_SETTINGS_MODULAR: Handling click on visualization settings item: ${item.label} (type: ${item.visualizationSettingsItemType})`);
        // For most visualization settings items, the command is already attached to the tree item
        // and will be executed automatically by VS Code
        switch (item.visualizationSettingsItemType) {
            case 'settings-field':
                console.log('VISUALIZATION_SETTINGS_MODULAR: Settings field clicked');
                // The command to open the color picker is already attached to the item
                // VS Code will execute it automatically
                break;
            case 'error':
                console.log('VISUALIZATION_SETTINGS_MODULAR: Error item clicked - no action');
                break;
            default:
                console.warn(`VISUALIZATION_SETTINGS_MODULAR: Unknown visualization settings item type: ${item.visualizationSettingsItemType}`);
        }
    }
    /**
     * Handle right-click context menu actions
     */
    async handleContextMenuAction(action, item) {
        console.log(`VISUALIZATION_SETTINGS_MODULAR: Handling context menu action: ${action} on item: ${item.label}`);
        switch (action) {
            case 'refresh':
                console.log('VISUALIZATION_SETTINGS_MODULAR: Refreshing visualization settings view');
                // Refresh will be triggered by the provider
                break;
            case 'configure':
                await this.handleConfigureAction(item);
                break;
            case 'reset':
                await this.handleResetAction(item);
                break;
            case 'resetAll':
                await this.handleResetAllAction();
                break;
            case 'export':
                await this.handleExportSettings();
                break;
            case 'import':
                await this.handleImportSettings();
                break;
            default:
                console.warn(`VISUALIZATION_SETTINGS_MODULAR: Unknown context menu action: ${action}`);
        }
    }
    /**
     * Handle configure action (open color picker)
     */
    async handleConfigureAction(item) {
        console.log(`VISUALIZATION_SETTINGS_MODULAR: Configuring setting: ${item.label}`);
        if (item.command) {
            // Execute the item's command (usually opens color picker)
            await vscode.commands.executeCommand(item.command.command, ...(item.command.arguments || []));
        }
        else {
            // Fallback - try to extract setting field and use generic command
            if (item.originalSettingsItem?.settingField) {
                await vscode.commands.executeCommand('codeXR.visualizationSettings.configure', item.originalSettingsItem.settingField.key);
            }
            else {
                vscode.window.showWarningMessage(`Cannot configure: ${item.label}`);
            }
        }
    }
    /**
     * Handle reset single setting action
     */
    async handleResetAction(item) {
        console.log(`VISUALIZATION_SETTINGS_MODULAR: Resetting setting: ${item.label}`);
        try {
            if (item.originalSettingsItem?.settingField) {
                await vscode.commands.executeCommand('codeXR.visualizationSettings.reset', item.originalSettingsItem.settingField.key);
                vscode.window.showInformationMessage(`Reset ${item.label} to default value`);
            }
            else {
                vscode.window.showWarningMessage(`Cannot reset: ${item.label}`);
            }
        }
        catch (error) {
            console.error(`VISUALIZATION_SETTINGS_MODULAR: Error resetting setting:`, error);
            vscode.window.showErrorMessage(`Failed to reset ${item.label}: ${error}`);
        }
    }
    /**
     * Handle reset all settings action
     */
    async handleResetAllAction() {
        console.log('VISUALIZATION_SETTINGS_MODULAR: Resetting all settings');
        const confirmation = await vscode.window.showWarningMessage('Reset all visualization settings to default values?', { modal: true }, 'Reset All', 'Cancel');
        if (confirmation === 'Reset All') {
            try {
                await vscode.commands.executeCommand('codeXR.visualizationSettings.resetAll');
                vscode.window.showInformationMessage('All visualization settings have been reset to default values');
            }
            catch (error) {
                console.error('VISUALIZATION_SETTINGS_MODULAR: Error resetting all settings:', error);
                vscode.window.showErrorMessage(`Failed to reset all settings: ${error}`);
            }
        }
    }
    /**
     * Handle export settings action
     */
    async handleExportSettings() {
        console.log('VISUALIZATION_SETTINGS_MODULAR: Exporting settings');
        try {
            await vscode.commands.executeCommand('codeXR.visualizationSettings.export');
        }
        catch (error) {
            console.error('VISUALIZATION_SETTINGS_MODULAR: Error exporting settings:', error);
            vscode.window.showErrorMessage(`Failed to export settings: ${error}`);
        }
    }
    /**
     * Handle import settings action
     */
    async handleImportSettings() {
        console.log('VISUALIZATION_SETTINGS_MODULAR: Importing settings');
        try {
            await vscode.commands.executeCommand('codeXR.visualizationSettings.import');
        }
        catch (error) {
            console.error('VISUALIZATION_SETTINGS_MODULAR: Error importing settings:', error);
            vscode.window.showErrorMessage(`Failed to import settings: ${error}`);
        }
    }
}
exports.VisualizationSettingsClickHandler = VisualizationSettingsClickHandler;


/***/ }),
/* 218 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * Learn More Views Index
 * Central exports for learn more view components
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LearnMoreClickHandler = exports.LearnMoreModularItemFactory = exports.LearnMoreModularTreeItem = exports.LearnMoreSectionProvider = void 0;
var LearnMoreSectionProvider_1 = __webpack_require__(219);
Object.defineProperty(exports, "LearnMoreSectionProvider", ({ enumerable: true, get: function () { return LearnMoreSectionProvider_1.LearnMoreSectionProvider; } }));
var learnMoreItems_1 = __webpack_require__(220);
Object.defineProperty(exports, "LearnMoreModularTreeItem", ({ enumerable: true, get: function () { return learnMoreItems_1.LearnMoreModularTreeItem; } }));
Object.defineProperty(exports, "LearnMoreModularItemFactory", ({ enumerable: true, get: function () { return learnMoreItems_1.LearnMoreModularItemFactory; } }));
var handleLearnMoreClicks_1 = __webpack_require__(221);
Object.defineProperty(exports, "LearnMoreClickHandler", ({ enumerable: true, get: function () { return handleLearnMoreClicks_1.LearnMoreClickHandler; } }));


/***/ }),
/* 219 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Learn More Section Provider
 * Manages the Learn More section for CodeXR tutorials and documentation
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LearnMoreSectionProvider = void 0;
const vscode = __importStar(__webpack_require__(1));
const baseInterfaces_1 = __webpack_require__(177);
const learnMoreItems_1 = __webpack_require__(220);
const handleLearnMoreClicks_1 = __webpack_require__(221);
/**
 * Learn More section provider - provides access to CodeXR learning resources
 */
class LearnMoreSectionProvider extends baseInterfaces_1.BaseSectionProvider {
    clickHandler;
    constructor(context) {
        super(context);
        console.log('LEARN_MORE: Initializing Learn More section provider');
        this.clickHandler = new handleLearnMoreClicks_1.LearnMoreClickHandler(context);
    }
    getSectionName() {
        return 'learnMore';
    }
    getSectionItem() {
        return new learnMoreItems_1.LearnMoreModularTreeItem('LEARN MORE', vscode.TreeItemCollapsibleState.Collapsed, 'section', undefined, new vscode.ThemeIcon('book', new vscode.ThemeColor('charts.foreground')), 'Access CodeXR documentation, tutorials, and examples', 'Official docs & tutorials', 'learnMoreSection');
    }
    async getChildren(element) {
        if (!element) {
            console.log('LEARN_MORE: Loading learn more section children');
            return learnMoreItems_1.LearnMoreModularItemFactory.createLearnMoreItems(this.context);
        }
        return [];
    }
    handleItemClick(item) {
        console.log('LEARN_MORE: Item clicked:', item.label);
        this.clickHandler.handleClick(item);
    }
}
exports.LearnMoreSectionProvider = LearnMoreSectionProvider;


/***/ }),
/* 220 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


/**
 * Learn More Items
 * Tree view items for the Learn More section
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LearnMoreModularItemFactory = exports.LearnMoreModularTreeItem = void 0;
const vscode = __importStar(__webpack_require__(1));
/**
 * Base class for Learn More modular tree items
 */
class LearnMoreModularTreeItem extends vscode.TreeItem {
    label;
    collapsibleState;
    learnMoreItemType;
    command;
    iconPath;
    tooltip;
    description;
    contextValue;
    constructor(label, collapsibleState, learnMoreItemType, command, iconPath, tooltip, description, contextValue) {
        super(label, collapsibleState);
        this.label = label;
        this.collapsibleState = collapsibleState;
        this.learnMoreItemType = learnMoreItemType;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip;
        this.description = description;
        this.contextValue = contextValue;
        this.command = command;
        this.iconPath = iconPath;
        this.tooltip = tooltip;
        this.description = description;
        this.contextValue = contextValue;
    }
}
exports.LearnMoreModularTreeItem = LearnMoreModularTreeItem;
/**
 * Factory for creating Learn More tree items
 */
class LearnMoreModularItemFactory {
    /**
     * Create learn more section items
     */
    static createLearnMoreItems(context) {
        console.log('LEARN_MORE: Creating learn more items');
        const items = [];
        // Main "Learn More" action item
        const learnMoreItem = new LearnMoreModularTreeItem('Open Documentation Website', vscode.TreeItemCollapsibleState.None, 'action', {
            command: 'codeXR.learnMore',
            title: 'Learn More',
            arguments: []
        }, new vscode.ThemeIcon('globe', new vscode.ThemeColor('charts.foreground')), 'Visit the official CodeXR documentation website for tutorials, guides, and examples', 'Official docs & tutorials', 'learnMoreAction');
        items.push(learnMoreItem);
        return items;
    }
}
exports.LearnMoreModularItemFactory = LearnMoreModularItemFactory;


/***/ }),
/* 221 */
/***/ ((__unused_webpack_module, exports) => {


/**
 * Learn More Interactions Handler
 * Handles user interactions within the Learn More section
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.LearnMoreClickHandler = void 0;
class LearnMoreClickHandler {
    context;
    constructor(context) {
        this.context = context;
        console.log('LEARN_MORE: Initializing Learn More click handler');
    }
    /**
     * Handle clicks on Learn More items
     */
    handleClick(item) {
        console.log('LEARN_MORE: Handling click on learn more item:', item);
        // For now, clicks are handled by commands
        // Future interactions can be implemented here
    }
}
exports.LearnMoreClickHandler = LearnMoreClickHandler;


/***/ }),
/* 222 */
/***/ ((__unused_webpack_module, exports) => {


/**
 * Supported languages configuration for file detection and analysis
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SUPPORTED_LANGUAGES = void 0;
exports.getLanguageByExtension = getLanguageByExtension;
exports.getAllSupportedExtensions = getAllSupportedExtensions;
exports.isSupportedExtension = isSupportedExtension;
exports.getLanguageStats = getLanguageStats;
exports.SUPPORTED_LANGUAGES = {
    // Web Technologies
    html: {
        extensions: ['.html', '.htm'],
        name: 'HTML'
    },
    javascript: {
        extensions: ['.js', '.mjs', '.cjs'],
        name: 'JavaScript'
    },
    typescript: {
        extensions: ['.ts', '.tsx'],
        name: 'TypeScript'
    },
    vue: {
        extensions: ['.vue'],
        name: 'Vue'
    },
    // Backend Languages
    python: {
        extensions: ['.py', '.pyw', '.pyi'],
        name: 'Python'
    },
    ruby: {
        extensions: ['.rb', '.rbw'],
        name: 'Ruby'
    },
    php: {
        extensions: ['.php', '.phtml', '.php3', '.php4', '.php5'],
        name: 'PHP'
    },
    perl: {
        extensions: ['.pl', '.pm'],
        name: 'Perl'
    },
    // System Languages
    c: {
        extensions: ['.c', '.h'],
        name: 'C'
    },
    cplusplus: {
        extensions: ['.cpp', '.cxx', '.cc', '.hpp', '.hxx'],
        name: 'C++'
    },
    csharp: {
        extensions: ['.cs'],
        name: 'C#'
    },
    go: {
        extensions: ['.go'],
        name: 'Go'
    },
    rust: {
        extensions: ['.rs'],
        name: 'Rust'
    },
    zig: {
        extensions: ['.zig'],
        name: 'Zig'
    },
    // JVM Languages
    java: {
        extensions: ['.java'],
        name: 'Java'
    },
    kotlin: {
        extensions: ['.kt', '.kts'],
        name: 'Kotlin'
    },
    scala: {
        extensions: ['.scala', '.sc'],
        name: 'Scala'
    },
    // Mobile
    objectivec: {
        extensions: ['.m', '.mm'],
        name: 'Objective-C'
    },
    swift: {
        extensions: ['.swift'],
        name: 'Swift'
    },
    // Scripting
    lua: {
        extensions: ['.lua'],
        name: 'Lua'
    },
    // Specialized
    solidity: {
        extensions: ['.sol'],
        name: 'Solidity'
    },
    gdscript: {
        extensions: ['.gd'],
        name: 'GDScript'
    },
    ttcn3: {
        extensions: ['.ttcn', '.ttcn3'],
        name: 'TTCN-3'
    },
    erlang: {
        extensions: ['.erl', '.hrl'],
        name: 'Erlang'
    },
    fortran: {
        extensions: ['.f90', '.f95', '.f03', '.f08', '.f'],
        name: 'Fortran'
    }
};
/**
 * Get language configuration by file extension
 */
function getLanguageByExtension(extension) {
    const normalizedExt = extension.toLowerCase();
    for (const [key, config] of Object.entries(exports.SUPPORTED_LANGUAGES)) {
        if (config.extensions.includes(normalizedExt)) {
            return { ...config, icon: key }; // Add the key as icon identifier
        }
    }
    return null;
}
/**
 * Get all supported extensions as a flat array
 */
function getAllSupportedExtensions() {
    return Object.values(exports.SUPPORTED_LANGUAGES)
        .flatMap(config => config.extensions);
}
/**
 * Check if a file extension is supported
 */
function isSupportedExtension(extension) {
    return getLanguageByExtension(extension) !== null;
}
/**
 * Get language statistics for logging
 */
function getLanguageStats() {
    const languages = Object.keys(exports.SUPPORTED_LANGUAGES);
    const extensions = getAllSupportedExtensions();
    return {
        totalLanguages: languages.length,
        totalExtensions: extensions.length
    };
}


/***/ }),
/* 223 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * New Code Analysis Views Module
 * Exports for view components
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.NewCodeAnalysisInteractionHandler = exports.NewCodeAnalysisItemFactory = exports.NewCodeAnalysisTreeItem = exports.NewCodeAnalysisSectionProvider = void 0;
var NewCodeAnalysisSectionProvider_1 = __webpack_require__(197);
Object.defineProperty(exports, "NewCodeAnalysisSectionProvider", ({ enumerable: true, get: function () { return NewCodeAnalysisSectionProvider_1.NewCodeAnalysisSectionProvider; } }));
var items_1 = __webpack_require__(224);
Object.defineProperty(exports, "NewCodeAnalysisTreeItem", ({ enumerable: true, get: function () { return items_1.NewCodeAnalysisTreeItem; } }));
Object.defineProperty(exports, "NewCodeAnalysisItemFactory", ({ enumerable: true, get: function () { return items_1.NewCodeAnalysisItemFactory; } }));
var interactions_1 = __webpack_require__(225);
Object.defineProperty(exports, "NewCodeAnalysisInteractionHandler", ({ enumerable: true, get: function () { return interactions_1.NewCodeAnalysisInteractionHandler; } }));


/***/ }),
/* 224 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * New Code Analysis Items Module
 * Exports for view items
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.NewCodeAnalysisItemFactory = exports.NewCodeAnalysisTreeItem = void 0;
var newCodeAnalysisItems_1 = __webpack_require__(150);
Object.defineProperty(exports, "NewCodeAnalysisTreeItem", ({ enumerable: true, get: function () { return newCodeAnalysisItems_1.NewCodeAnalysisTreeItem; } }));
Object.defineProperty(exports, "NewCodeAnalysisItemFactory", ({ enumerable: true, get: function () { return newCodeAnalysisItems_1.NewCodeAnalysisItemFactory; } }));


/***/ }),
/* 225 */
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


/**
 * New Code Analysis Interactions Module
 * Exports for interaction handlers
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.NewCodeAnalysisInteractionHandler = void 0;
var handleNewCodeAnalysisClicks_1 = __webpack_require__(198);
Object.defineProperty(exports, "NewCodeAnalysisInteractionHandler", ({ enumerable: true, get: function () { return handleNewCodeAnalysisClicks_1.NewCodeAnalysisInteractionHandler; } }));


/***/ }),
/* 226 */
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {


var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.VisualizeDataModel = void 0;
const vscode = __importStar(__webpack_require__(1));
const fs = __importStar(__webpack_require__(6));
const visualizeDataState_1 = __webpack_require__(51);
/**
 * Visualize Data Model
 * Handles state management and validation for visualize data functionality
 */
class VisualizeDataModel {
    /**
     * Reset the visualize data state completely
     * This should be called on extension activation to ensure clean state
     */
    static resetVisualizeDataState(context) {
        console.log('VISUALIZE-STATE: Resetting visualize data state on extension activation');
        try {
            // Clear any existing state from workspace storage
            context.workspaceState.update('visualizeDataState', undefined);
            console.log('VISUALIZE-STATE: Cleared workspace storage');
            // If state manager instance exists, reset it
            if (visualizeDataState_1.VisualizeDataStateManager.hasInstance()) {
                const stateManager = visualizeDataState_1.VisualizeDataStateManager.getInstance(context);
                stateManager.reset();
                console.log('VISUALIZE-STATE: Reset existing state manager instance');
            }
            // Force creation of new clean state manager
            const stateManager = visualizeDataState_1.VisualizeDataStateManager.getInstance(context);
            const state = stateManager.getState();
            console.log('VISUALIZE-STATE: State reset complete', {
                selectedChart: state.selectedChart?.id || 'none',
                selectedJsonPath: state.selectedJsonPath || 'none',
                dimensionMappings: state.dimensionMappings.length,
                isReadyToLaunch: state.isReadyToLaunch
            });
        }
        catch (error) {
            console.error('VISUALIZE-STATE: Error during state reset:', error);
        }
    }
    /**
     * Validate that a file path still exists
     */
    static validateFilePath(filePath) {
        try {
            return fs.existsSync(filePath);
        }
        catch (error) {
            console.warn('VISUALIZE-STATE: Error checking file existence:', error);
            return false;
        }
    }
    /**
     * Validate the current state and clean up invalid entries
     */
    static validateAndCleanState(context) {
        console.log('VISUALIZE-STATE: Validating and cleaning current state');
        if (!visualizeDataState_1.VisualizeDataStateManager.hasInstance()) {
            console.log('VISUALIZE-STATE: No state manager instance to validate');
            return;
        }
        const stateManager = visualizeDataState_1.VisualizeDataStateManager.getInstance(context);
        const state = stateManager.getState();
        let needsUpdate = false;
        // Check if selected JSON file still exists
        if (state.selectedJsonPath) {
            if (!this.validateFilePath(state.selectedJsonPath)) {
                console.log('VISUALIZE-STATE: Selected JSON file no longer exists, clearing:', state.selectedJsonPath);
                stateManager.updateSelectedJson('', '');
                needsUpdate = true;
            }
            else {
                console.log('VISUALIZE-STATE: Selected JSON file is valid:', state.selectedJsonPath);
            }
        }
        // If state was modified, trigger refresh
        if (needsUpdate) {
            console.log('VISUALIZE-STATE: State was cleaned, triggering UI refresh');
            vscode.commands.executeCommand('codexr.servers.refresh');
        }
        else {
            console.log('VISUALIZE-STATE: State validation complete, no changes needed');
        }
    }
    /**
     * Get state summary for debugging
     */
    static getStateSummary(context) {
        if (!visualizeDataState_1.VisualizeDataStateManager.hasInstance()) {
            return 'No state manager instance';
        }
        const stateManager = visualizeDataState_1.VisualizeDataStateManager.getInstance(context);
        const state = stateManager.getState();
        return `State Summary:
  - Chart: ${state.selectedChart?.name || 'Not selected'}
  - JSON File: ${state.selectedJsonName || 'Not selected'}
  - JSON Path Valid: ${state.selectedJsonPath ? this.validateFilePath(state.selectedJsonPath) : 'N/A'}
  - Dimension Mappings: ${state.dimensionMappings.length}
  - Mapping Configured: ${state.isDimensionMappingConfigured}
  - Ready to Launch: ${state.isReadyToLaunch}`;
    }
}
exports.VisualizeDataModel = VisualizeDataModel;


/***/ }),
/* 227 */
/***/ ((__unused_webpack_module, exports) => {


/**
 * Extension Context Holder
 *
 * Lightweight singleton that stores the VS Code ExtensionContext once
 * during activation, eliminating the need to thread `context` through
 * every `getInstance(context)` call across the codebase.
 *
 * Usage:
 *   // In extension.ts activate():
 *   initializeExtensionContext(context);
 *
 *   // In any consumer (no context parameter needed):
 *   import { getExtensionContext } from '../core/extensionContext';
 *   const ctx = getExtensionContext();
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.initializeExtensionContext = initializeExtensionContext;
exports.getExtensionContext = getExtensionContext;
let _context = null;
/**
 * Store the extension context. Must be called once during activate().
 * Subsequent calls are ignored (idempotent).
 */
function initializeExtensionContext(context) {
    if (!_context) {
        _context = context;
        console.log('CORE: Extension context initialized');
    }
}
/**
 * Retrieve the stored extension context.
 * @throws if called before initializeExtensionContext()
 */
function getExtensionContext() {
    if (!_context) {
        throw new Error('Extension context not initialized. Call initializeExtensionContext(context) in activate() first.');
    }
    return _context;
}


/***/ }),
/* 228 */,
/* 229 */
/***/ ((module) => {

module.exports = require("node:net");

/***/ }),
/* 230 */
/***/ ((module) => {

module.exports = require("node:os");

/***/ })
/******/ 	]);
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = __webpack_modules__;
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/ensure chunk */
/******/ 	(() => {
/******/ 		__webpack_require__.f = {};
/******/ 		// This file contains only the entry chunk.
/******/ 		// The chunk loading function for additional chunks
/******/ 		__webpack_require__.e = (chunkId) => {
/******/ 			return Promise.all(Object.keys(__webpack_require__.f).reduce((promises, key) => {
/******/ 				__webpack_require__.f[key](chunkId, promises);
/******/ 				return promises;
/******/ 			}, []));
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/get javascript chunk filename */
/******/ 	(() => {
/******/ 		// This function allow to reference async chunks
/******/ 		__webpack_require__.u = (chunkId) => {
/******/ 			// return url for filenames based on template
/******/ 			return "" + chunkId + ".extension.js";
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/require chunk loading */
/******/ 	(() => {
/******/ 		// no baseURI
/******/ 		
/******/ 		// object to store loaded chunks
/******/ 		// "1" means "loaded", otherwise not loaded yet
/******/ 		var installedChunks = {
/******/ 			0: 1
/******/ 		};
/******/ 		
/******/ 		// no on chunks loaded
/******/ 		
/******/ 		var installChunk = (chunk) => {
/******/ 			var moreModules = chunk.modules, chunkIds = chunk.ids, runtime = chunk.runtime;
/******/ 			for(var moduleId in moreModules) {
/******/ 				if(__webpack_require__.o(moreModules, moduleId)) {
/******/ 					__webpack_require__.m[moduleId] = moreModules[moduleId];
/******/ 				}
/******/ 			}
/******/ 			if(runtime) runtime(__webpack_require__);
/******/ 			for(var i = 0; i < chunkIds.length; i++)
/******/ 				installedChunks[chunkIds[i]] = 1;
/******/ 		
/******/ 		};
/******/ 		
/******/ 		// require() chunk loading for javascript
/******/ 		__webpack_require__.f.require = (chunkId, promises) => {
/******/ 			// "1" is the signal for "already loaded"
/******/ 			if(!installedChunks[chunkId]) {
/******/ 				if(true) { // all chunks have JS
/******/ 					installChunk(require("./" + __webpack_require__.u(chunkId)));
/******/ 				} else installedChunks[chunkId] = 1;
/******/ 			}
/******/ 		};
/******/ 		
/******/ 		// no external install chunk
/******/ 		
/******/ 		// no HMR
/******/ 		
/******/ 		// no HMR manifest
/******/ 	})();
/******/ 	
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module is referenced by other modules so it can't be inlined
/******/ 	var __webpack_exports__ = __webpack_require__(0);
/******/ 	module.exports = __webpack_exports__;
/******/ 	
/******/ })()
;
//# sourceMappingURL=extension.js.map