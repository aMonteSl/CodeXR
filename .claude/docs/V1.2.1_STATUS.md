# v1.2.1 Status Snapshot — Cleanup & Optimization

> **Snapshot date: 2026-08-03.** This is the *volatile* document for v1.2.1 — update it whenever a cleanup item lands, a measurement changes, or a new debt is found. Supersedes `V1.2.0_STATUS.md` as the active status doc; that file stays as the historical record of the 1.2.0 cycle and must not be rewritten.
> Labels: **[confirmed]** measured/verified directly in this repo · **[self-assessed]** a doc's own claim, not independently validated · **[inferred]** conclusion from evidence · **[open]** unresolved question.

## What this version is for

**1.2.1 is a maintenance release. No new features.** The scope is: delete what is dead, shrink what is oversized, tighten what silently allows rot, and make the existing paths cheaper. Every change must be **behaviour-neutral for the user** unless the CHANGELOG says otherwise.

If a task turns out to need a behaviour change to be worth doing, stop and ask — it belongs in 1.3.0, not here.

## Version and branch state [confirmed]

- `package.json` version: `1.2.1`. CHANGELOG has `[1.2.1] – Unreleased`; **1.2.0 released 2026-07-30 (Thursday)**.
- Branch `v1.2.1`, cut from `master` at `bcb34bd5` (the PR #6 merge), pushed and tracking `origin/v1.2.1`.
- Branch `v1.2.0` **deleted** (local + remote) after verifying `master` contained it: zero unmerged commits and **identical tree hash** `15122d77`. The **tag `v1.2.0` is kept** at `c3e09927` — that is the recoverable pointer to the release.
- `master` is the only principal branch and the PR target. There is no `main` branch; a rename was considered and explicitly declined (2026-08-03) to avoid breaking CI and existing clones.
- Remaining branches: `master`, `v1.2.1`.

## Baseline measurements (2026-08-03, before any cleanup) [confirmed]

These are the "before" numbers. Re-measure at the end of the cycle and record the delta.

| Metric | Baseline | Command |
|---|---|---|
| TypeScript files in `src/` | 369 | `find src -name "*.ts" \| wc -l` |
| TypeScript lines in `src/` | 52,605 | `find src -name "*.ts" -exec wc -l {} +` |
| `npm run lint` | clean (see caveat below) | `npm run lint` |
| `console.log` calls in `src/` | 1,261 | `grep -rn "console\.log" src --include="*.ts" \| wc -l` |
| Files importing the real logger | 15 | `grep -rl "core/logging/logger" src --include="*.ts"` |
| `: any` / `as any` / `<any>` in `src/` | 193 | `grep -rn ": any\b\|as any\b\|<any>" src --include="*.ts"` |
| `@ts-ignore` / `@ts-expect-error` | **0** — keep it there | `grep -rn "@ts-ignore\|@ts-expect-error" src` |
| `TODO` markers in `src/` + `templates/` | 30 (in 28 lines) | `grep -rn "TODO\|FIXME\|HACK" src templates` |
| Unit test files | 55 | `find test -name "*.test.cjs" \| wc -l` |
| `resources/languages_icons/no_color/` | 34 MB, 25 files | `du -sh` |
| `resources/languages_icons/color/` | 2.8 MB | `du -sh` |
| `.git` pack size | **1.03 GiB** | `git count-objects -vH` |

## Cleanup backlog — ranked

Ranked by value-to-risk. Take **one item per session**, verify, update this doc and the CHANGELOG, commit. Do not bundle items.

### 1. ESLint config barely checks anything — the highest-leverage fix [confirmed]

`eslint.config.mjs` defines **five rules** (`naming-convention` for imports, `curly`, `eqeqeq`, `no-throw-literal`, `semi`) and **all of them are `warn`**. There is no `no-unused-vars`, no `no-explicit-any`, no `no-floating-promises`, and the TypeScript plugin is loaded but essentially unused.

**Consequence:** `npm run lint` passing today is close to meaningless, and `npm test` includes it as a gate. Dead variables, unused imports and unawaited promises pass silently — which is exactly the rot this version is supposed to remove.

**Do this first**, because every later item is easier once the linter can actually find things. Suggested order: enable `@typescript-eslint/no-unused-vars` and `no-unused-imports` → fix the fallout → then consider `no-floating-promises` (needs type-aware linting, `parserOptions.project`) → decide per-rule whether `warn` or `error`. Expect a large first fallout; land the config and the fixes together so the gate is never left red.

### 2. Dead file: `configurationManagerExample.ts` [confirmed]

`src/code_analysis/configuration/configurationManagerExample.ts` has **zero importers anywhere in `src/`** and contains 44 `console.log` calls. It is an example file that shipped into the source tree. Delete it.

### 3. `debugThemeProblem.ts` is a registered debug command [confirmed]

`src/code_analysis/configuration/debugThemeProblem.ts` (42 `console.log`) **is** imported — `src/commands/index.ts:15` registers `DebugThemeProblem`. So it is not dead, it is a debug tool exposed to end users in the command palette. Decide deliberately: remove the command, or gate it behind a development-only condition. **Do not just delete it** — the command ID is registered and `assertUniqueCommandIds` runs over that array.

### 4. 1,261 `console.log` vs a logger nobody uses [confirmed]

`src/core/logging/logger.ts` exports `ScopedLogger` and `CodeXRLogger`, but only **15 files** use it against **1,261** raw `console.log` calls. For a VS Code extension this is the wrong surface: `console.log` goes to a devtools console the user never opens, not to an output channel.

Worst offenders: `executePython.ts` (100), `multiServerLauncher.ts` (44), `configurationManagerExample.ts` (44, dead — see item 2), `debugThemeProblem.ts` (42), `serverControl.ts` (41), `visualizeDOMParser.ts` (35).

Migrate **area by area, not repo-wide in one commit**. Start with `executePython.ts` since it is both the worst offender and on the hot analysis path.

### 5. 34 MB of dead icons in the repo [confirmed measurement, self-assessed deadness]

`resources/languages_icons/no_color/` is 34 MB across 25 PNGs (~1.4 MB each). `V1.2.0_STATUS.md:132` states this set has been dead since `95f7c367` and it is already excluded from the VSIX `files` allowlist — so removing it does **not** change the published package, only the working tree and future clones. **Confirm the deadness independently** (grep for `no_color` across `src/`, `templates/`, `package.json`) before deleting.

Related, and *not* dead — these ship: `color/ttcn3.svg` (1.4 MB) and `color/default.svg` (1.3 MB) are SVGs with embedded bitmaps, plus `icon.png` and `favicon.ico` at ~1.2 MB each. Re-encoding them is a real VSIX size win. Carried over from `V1.2.0_STATUS.md:132`, where the debt was flagged but deliberately not taken.

### 6. Oversized modules [confirmed]

There is precedent: `httpServer.ts` went 2,102 → 487 lines by responsibility split, with byte-identical characterization (`V1.2.0_STATUS.md:513`). Same technique applies to:

| File | Lines |
|---|---|
| `src/servers/runtime/collaboration/collaborationRoomServer.ts` | 1,174 |
| `src/python_env/runtime/venvManager.ts` | 851 |
| `src/active_servers/views/interactions/handleServerActions.ts` | 815 |
| `src/servers/runtime/broadcast/screenBroadcastSignalingServer.ts` | 781 |
| `src/code_analysis/views/subsections/active_analyses/commands/activeAnalysesCommands.ts` | 685 |
| `src/servers/runtime/multiServerLauncher.ts` | 677 |

**Read `AI_WORKFLOWS.md` §refactor first.** These are live server/session paths; a split must be behaviour-neutral and characterization-tested, not a vibe reorganization.

### 7. 193 `any` escapes [confirmed]

193 occurrences of `: any` / `as any` / `<any>` in `src/`. Note the good news: **zero** `@ts-ignore` — the codebase never silences the compiler outright. Tighten opportunistically while working an area rather than as one mass sweep, and keep the zero at zero.

### 8. `.git` is 1.03 GiB — **propose only, do not execute** [confirmed measurement]

The pack is dominated by binaries committed long ago: `code-xr-0.0.8.vsix` (55 MB and again at 38/36 MB), `code-xr-0.0.9.vsix` (37.6 MB ×3), `code-xr-0.0.6.vsix` (31.7 MB ×2), and `media/v1.1.0/gifts/*.gif` (43.5, 40.6, 37.5 MB).

Shrinking this requires **rewriting history** (`git filter-repo`), which changes every commit hash, forces a force-push to `origin`, and requires every clone to be re-made. That is destructive and outward-facing. **Never do it as part of a routine cleanup session.** If it is ever wanted, it is its own decision with its own explicit approval — and the current `.gitignore` should be checked first so no *new* build artifacts keep landing in history. **[open]**

### 9. Known duplication — trace before touching [self-assessed, from V1.2.0_STATUS.md]

- `src/active_servers/views/` and `src/views/active_servers/` both exist. `AI_WORKFLOWS.md:42` explicitly warns this is **historical, not accidental** — do not "clean up" the aggregation wiring without tracing imports first.
- `LegacyServerConfig` / `getLegacyConfig` in `servers/storage`: live API with a bad name (`V1.2.0_STATUS.md:507`). Renaming is a real refactor, not a rename-in-place.
- Avatar IDs are duplicated across 4 places: `collaborationProfile.ts`, `CODEXR_AVATAR_IDS` in the room server, `SKINS` in the browser runtime, and a regex in `clientCore.js` (`V1.2.0_STATUS.md:601`). Consolidating crosses the TS↔runtime seam — read `XR_COMPONENTS.md` first.

## Carried-over debt from 1.2.0 (not cleanup — do not silently absorb)

These are open items from the 1.2.0 cycle that are **not** part of the cleanup scope. Listed so they are not lost:

- Several features are marked "confirmed en tests; **pendiente F5**" — live validation by the user was never completed (export offline bundle, cross-network defaults, screen-share role controls, kick flow). See `V1.2.0_STATUS.md`.
- Documentation debt: `docs/xr-testing/XR_DEBUG_COMMANDS.md:382` references the removed `revealOffsetY` parameter; `ROADMAP_V1.2.0.md:249-251` and its config screenshots are stale on the cross-network default.
- The `.vsix` attached to the 1.2.0 GitHub release carries an older README (`V1.2.0_STATUS.md:124`).

## Session log

*(Append one section per session: what was cleaned, what was measured before/after, which gates ran, what was deliberately not touched.)*

### Branch opened + 1.2.0 dated — DONE [confirmed] (2026-08-03, rama `v1.2.1`)

Closed the 1.2.0 cycle and opened this one. Verified `master` contained `v1.2.0` by ancestry **and** identical tree hash before deleting the branch; kept the tag. Dated the CHANGELOG release **2026-07-30**, bumped `package.json` to `1.2.1`, added the `[1.2.1] – Unreleased` section, created this doc, and pointed `CLAUDE.md` / `INDEX.md` / `AI_WORKFLOWS.md` at it. Measured the baseline table above. **No source code touched.**
